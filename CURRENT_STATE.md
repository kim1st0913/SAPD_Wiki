# CURRENT_STATE: SAPD Wiki

本文件用于 Codex 每次开工前快速读取，帮助主控 Agent 避免默认加载过长历史文档。

## 当前主线

- 2026-07-26 `PLAN-ENV-MD P8 PASS / 当前源码App实包UAT待下次DMG`：按用户最终人工验收删除字典分类标题中的“主数据分类”，三组只显示“信息化环境 / 环境子类 / 信息化对象”；定向门禁已模拟真实委托事件，三类关联均为单击展开、再次单击收起，并验证“查看环境映射”携带精确环境、子类和对象ID进入环境映射而非直接进入draw.io底图。稳定5173 home、health、workspace projection通过；完整pre-commit与pre-dmg全部通过，搜索质量恢复为38/38、成熟度合同232项通过，环境字典为77条主数据、77条定义、125条关联使用。正式基础库、用户库和环境字典包验收前后SHA-256不变，未写真实用户数据。旧0.2.0 staging后端离线启动、自检与环境目录页面通过，但其成熟度计算/评分导出不匹配当前源码，已按旧产物处理，不能替代当前源码App实包验收；下一次DMG构建后仍需执行App Runtime人工UAT。本轮未启动系统Chrome、未构建DMG、未stage、commit或push。
- 2026-07-26 `PLAN-ENV-MD P7.1 PASS / 待页面人工验收`：按用户要求沿用P2受控语义裁定方式，为10个信息化环境和51个信息化对象补齐定义；原始Excel只提供名称与上下文，因此定义明确标记为基于名称、29/67既有关系上下文形成的裁定文本，不冒充源原文。正式基础库只更新61个空 `description` 并新增61条确定性审计，主数据身份、编号、原关系和来源证据哈希未变。字典把环境的错误“自身上下文”投影改为29条真实子类上下文，三类关联使用为29/29/67共125条，环境摘要与展开数量逐项一致；页面修正提示/工具栏/表格三轨布局，定义完整换行，关联上下文按现有表格样式展开。临时库首次/重复apply、独立恢复、正式写入前热备份、P7.1/P7继承审计、旧目录、P1-4、API 77条定义/125条关系和GitHub数据边界通过；用户库与源Excel未变。恢复证据位于 `data/exports/worker-verify/plan-env-md/p7-1-20260726T051754Z/`。稳定5173已恢复；应用内浏览器受宿主策略禁止访问该loopback入口，本轮没有绕过策略或启动系统Chrome，最终视觉仍需用户在现有入口人工确认。
- 2026-07-26 `PLAN-ENV-MD P7 PASS / 进入P8门禁`：经用户授权，shared runtime 的 `environmentMasterDictionary` 已受控开启并通过稳定5173重启观察。对象目录按10个信息化环境、16个环境子类类型、51个信息化对象展示77条唯一主数据，可展开106条声明关系；旧10/29/67目录 fallback 继续保留。Dashboard保持10个环境、51个对象、6类作用域的既有投影口径，环境映射深链、API网关层关系定位、40条用户批注引用和收藏/批注只读端点通过；应用内浏览器无横向溢出或控制台错误。P5—P7历史/当前门禁、用户引用完整性、数据/保护边界及5173 stable profile通过，P7报告位于 `data/exports/worker-verify/plan-env-md/p7-20260726T022354Z/`。正式基础库、用户库、源Excel、旧环境树、146个拆分投影、底图和Dashboard包哈希均未变化；系统Chrome未启动，App/DMG未验收。完整内容 smoke 仍只有P7前已存在的全局搜索合并去重断言失败，不归因于本次切换。下一步为P8发布回归；P6恢复包继续保留，任何P7回退优先只关闭开关，不自动恢复数据库。
- 2026-07-26 `PLAN-ENV-MD P6 PASS / 已由P7继续`：在停止5173及MCP写者、取得单写者锁并完成可恢复热备份后，正式基础库已事务性执行P2迁移；首次回填61个既有主数据编号、新增16个 `environment_segment_type`、29条 `instance_of`、58条来源引用和106条迁移审计，重复执行新增全部为0。正式状态为10个信息化环境、16个环境子类类型、51个信息化对象、29个子类上下文和67个对象上下文；原7757条关系的ID、内容和证据哈希未变。用户库、源Excel、旧环境树、146个拆分投影、底图和Dashboard包保持不变，字典包与已验收P4影子包字节一致。P6独立审计、SQLite完整性/外键、P0—P5兼容、数据/保护边界、38/38搜索质量、Local MCP只读解析及5173正式API通过。恢复包位于 `data/exports/worker-verify/plan-env-md/p6-20260726T015418Z/`，含169个文件及可独立恢复的基础库/用户库热备份；须保留至P8通过并取得用户归档授权。正式基础库SHA-256为 `5216bce208b234bbbd89d16d1c54c48d264aa54fb0a84d925c595adc89956038`；P6原停止点已经由上一条P7受控切换结果取代。
- 2026-07-26 `PLAN-ENV-MD` P4—P5已完成：P4只在 SQLite 临时备份副本应用P2计划，新增加法型 `environment-dictionary-v1` 影子包与只读 `/api/v1/environments/dictionary`，输出10/16/51条主数据、29/67条关系上下文和106条显式用法关系；旧环境树、146个拆分投影、底图语义/详情和Dashboard包哈希保持。P5在现有“信息化环境-对象目录”接入默认关闭的 `environmentMasterDictionary` 开关，启用态按信息化环境/环境子类类型/信息化对象三类展示77条唯一主数据并可展开106条关系上下文，导航显式携带环境/子类/对象ID；关闭、缺包、API失败和schema不兼容均回退旧10/29/67目录，空数据不伪造fallback。专项P4/P5、P0—P3兼容、旧目录、P1-4、静态套件、API和应用内浏览器验收通过；P4/P5报告位于 `data/exports/worker-verify/plan-env-md/p4-20260725T164302Z/` 与 `p5-20260725T170159Z/`。P4/P5结束时正式数据未修改、功能开关为false；随后P6正式迁移状态以上一条为准。
- 2026-07-26 `PLAN-ENV-MD` P3已完成：在 SQLite 临时备份副本首次回填61个既有主数据编号、新增16个 `environment_segment_type`、29条唯一 `instance_of`、58条最小来源引用和106条迁移审计；重复执行全部新增为0。临时候选字典以开关 `false → true → false` 验证10/16/51主数据、29/67上下文和106条用法关系后删除；随后精确删除新增关系/对象/证据/审计并恢复61个编号，逻辑表、FTS、Schema、计数、外键和完整性快照与演练前完全一致，事务中途故障注入也自动回滚。独立P3门禁通过，报告位于 `data/exports/worker-verify/plan-env-md/p3-20260725T162703Z/`。正式基础库、用户库、源Excel和正式包哈希未变，物理 SQLite 文件哈希未被误作回滚判据，正式apply仍为false。下一步为P4影子字典导出/API，不进入正式apply。
- 2026-07-25 MCP Web Runtime 自动生命周期已完成：平台集成模式使用当前用户应用私有持久 Runtime，保存用户的启用 / 停止意图、本地端口、OAuth verifier key、稳定 instance/runtime identity、客户端与 token-family verifier。用户显式启动后，重启 SAPD Wiki 会自动恢复 MCP；Sidecar 意外退出按 `1 / 2 / 5 / 10 / 30s` 退避自动拉起；用户显式停止、改端口或重置则取消自动启动。5173 实测首次启用、主服务重启后无需点击自动回到“已启动”，以及只终止 MCP 子进程后 PID `29763 → 31063` 自动恢复，页面无 error / warning；MCP 专项 `162/162` 和控制面定向 `47/47` 通过。此次从旧临时 Runtime 迁移到持久 Runtime 后客户端授权需完成一次重新授权，之后才可验证真实 Codex token 持久；App / DMG 与 Windows 平台仍未验收。未修改正式基础数据或用户业务数据，未 stage、commit 或 push。
- 2026-07-24 macOS CurrentUser 证书与 MCP 启动链路已在正式 Web 开发入口跑通：连续修复 Keychain 私密写入、把根 CA 误作服务器 leaf 的信任自检、中断后空 generation 目录、信任检查时间戳导致的状态版本漂移，以及服务错误发送“leaf + 自签根证书”的 TLS 链。服务器现只发送 leaf，根 CA 仅保留在当前用户、`127.0.0.1` SSL 信任中；旧测试证书已通过应用重置精确删除，新证书已重新生成并安装，5173 页面显示 `MCP 已启动 / 连接正常 / 等待客户端授权`，服务地址为 `https://127.0.0.1:28775/mcp`。macOS `security verify-cert`、实际发送链 `1` 张、完整 MCP `158/158`、前端设置合同、Python 编译、diff-check 和 5173 stable profile 通过。系统 `curl` 不读取这条 hostname-scoped CurrentUser 信任，未为兼容而扩大为账户级全用途 trustRoot；真实 Codex 客户端、Windows、App / DMG 仍待后续验证。未写系统级信任、用户业务数据，未 stage、commit 或 push。
- 2026-07-24 `C1 前 C0-4—C0-7 开发已完成，待人工 Web 验证`：稳定证书实现已补齐 generation stage / activate / remove 原语、CurrentUser 信任合同与 macOS / Windows 默认禁写平台适配器、持久事务日志、跨进程单写者锁、失败恢复、24 小时旧代保留和精确清理、重置闭环及运行中信任漂移停机。Web Dev 的 macOS Keychain / CurrentUser Trust 接线只在显式 `--mcp-platform-integration` 下启用，默认 5173 仍使用隔离 fake，因此本轮没有真实系统写入。AI 功能集成页和顶部状态已覆盖操作阶段、信任 / 密钥异常、60 / 30 / 7 / 0 天证书窗口、旧代清理、重连与重新初始化。MCP Python `155` 项、系统设置前端合同、用户库治理和语法检查通过；应用内浏览器在 `/#/settings/ai-integration` 验证 `not_configured`、无“剩余 0 天”误报、`319px` 窄屏无横向溢出。C1、真实 Codex 配置、真实 Keychain / 系统信任实验、App / DMG 和 Windows 实机验证均未启动；未写正式 / 用户业务数据，未 stage、commit 或 push。
- 2026-07-24 MCP 正式知识访问规则已按用户裁定统一为“基础知识库全部业务内容可调用”：正式 Runtime 直接以只读、immutable SQLite 打开基础库，通过 5 个固定参数化工具返回对象完整 `description`、业务元数据、关系与脱敏来源证据，不再要求 `public_summary / ai_summary` 或按 active/public 二次过滤。用户数据库、源文件本体、本机路径、系统配置与凭据、日志、数据库写入和客户端 SQL 仍为硬排除。新 scope 为 `sapd.base.knowledge.read`，旧 M0-T synthetic 公开摘要合同只保留为历史验证基线。正式库完整条款与 deprecated 对象只读验收通过；MCP 查询前后基础库和用户库 SHA-256 均不变。页面验收发现既有 Web schema 检查会无条件刷新用户库元数据时间，现已改为仅版本变化时写入；修复后页面重载前后用户库哈希、40 条批注及业务更新时间不变。MCP `155` 项、系统设置合同和应用内页面检查通过。稳定 5173 已改由守护脚本保留 `.venv-local-mcp-web/bin/python` 路径启动，home / health / workspace projection 正常。运行时内容 smoke 仍暴露一项既有全局搜索去重断言，未在本轮跨范围处理。未配置真实客户端、未修改系统信任、未写用户业务数据、未构建 App/DMG，未 stage、commit 或 push。
- 2026-07-24 MCP 稳定证书 C0-B Web 安全传递已完成并进入人工 UAT：固定证书身份由 Web 父进程持有，口令只在匿名继承通道验证 PID、UID、nonce、generation 与父进程后单次交付给 Sidecar；Sidecar 不再启动时生成临时长期证书，停止 / 再启动保持同一 CA 指纹。不安全端点或身份统一返回 `KEY_PASSPHRASE_IPC_UNSAFE` 且不监听，口令、secret reference 和私钥路径未进入 argv、环境、日志、lease 或公开控制状态。MCP Python `126` 项和系统设置前端合同审计通过；5173 已真实完成生成证书、启动、loopback TLS 检查、停止并保留证书，页面当前为“证书有效、MCP 已停止”，console warning / error 为 `0`。本批仍为 fake-first：未写 macOS Keychain、Windows DPAPI 或 CurrentUser 系统信任，未配置 Codex、真实客户端、App 或打包，未写正式数据 / 用户数据，未 stage、commit 或 push。
- 2026-07-24 Git 工作区本地收敛已完成：正式目录 `/Users/kim1st/Documents/kim note/06_dev_projects/SAPD_Wiki` 已检出唯一日常分支 `main`；10 个旧本地功能分支、8 个 linked worktree 和临时 main worktree 均已移除。有效产品 checkpoint、MCP 组件历史、Web 集成历史和证书设计基线均保留在 main 历史中；完整恢复点位于仓库外 `SAPD_Wiki-recovery-20260724-branch-convergence/`，含全 refs bundle、dirty patch、untracked archive、用户库热备份和报告历史归档。远端未 push，`origin/main` 与 `origin/codex/windows-electron` 均未修改。
- 2026-07-24 收敛态已在正式主目录真实数据路径完成验收：完整 pre-commit 通过，搜索质量 `38/38`，成熟度合同 `232` 项，MCP `102/102`，runtime suite、5173 home / health / workspace projection、GitHub / JSON / 保护基线和本地目录契约通过。合并中恢复了顶栏 License 状态入口，P1-5 审计术语与正式“文件下载路径”统一，并把前端治理阈值精确更新到已验收快照。用户 SQLite 逻辑 dump 与收敛前热备份一致，关键计数为 Issue `40`、决策 `0`、收藏 `1`、工作区项 `0`；runtime smoke 生成的唯一临时成熟度报告已移入外部 recovery quarantine，报告文件清单恢复一致。未启动系统 Chrome，未构建 DMG。
- 2026-07-24 当前未提交工作包括本轮已验收的 C0-A/C0-B 证书控制面、前端、安全通道和测试，以及明确保留的 Windows / Electron 打包源码与说明、`data/` 本地数据、证书设计 v1.1 文档和证书产品化计划 v0.4。它们都保留在 main 工作目录；继续开发仍须遵守单 checkout 单 writer，任何提交、推送、平台信任或打包都需按用户授权执行。
- 2026-07-24 稳定 5173 错误 worktree 事件已恢复并加防复发：`/private/tmp/SAPD_Wiki-main-merged` 曾占用 5173，页面因此读取该 worktree 的空用户库并缺少完整内容索引，出现 ISSUE `0` 和指南待补充；正式主目录用户库的 `40` 条 Issue、指南 JSON、75 页安全技术架构幻灯片和图片均未丢失。5173 已重启到正式项目目录，应用内浏览器确认 ISSUE 页、Dashboard 统计和指南正常。`dev_server_guard` 现把 primary Git worktree、base DB、user DB、data root 纳入 stable profile，linked worktree 不能启动 5173，主目录 `--start` 会替换错误 profile 的 SAPD 服务。定向测试 `10/10`、MCP 套件 `102/102`、运行 profile、28775 释放和 diff-check 通过；未写或覆盖真实用户库。
- 2026-07-23 完整 MCP Web 控制链路已选择性合入 shared runtime，稳定开发入口保持 `http://127.0.0.1:5173/`。AI 功能集成页现通过同源、会话保护的 `/api/v1/system/mcp/*` 控制真实隔离 Sidecar，可完成 HTTPS / OAuth / Streamable HTTP 启动、状态查询、诊断、授权决策、客户端撤销、端口修改和停止；旧 `synthetic_web` 前端状态机已从生产前端移除。MCP 默认端口为 `28775`，`5173` 被代码、控制层和测试门禁永久保留给 stable Web，禁止 Sidecar 或 synthetic 测试占用。Sidecar 使用项目隔离环境 `.venv-local-mcp-web`，父 Web 进程收到 `SIGINT / SIGTERM` 时会清理子进程、临时证书与 Runtime；非 loopback host、无 Origin 的浏览器写请求、超限控制请求和端口冲突均被拒绝。顶部系统设置 / MCP 状态监测与现有路径设置保持不变，成熟度开发数据仍为 `5` 条。MCP 专项 Python 测试 `99` 条与前端合同审计、quick suite、数据 / 交付边界、5173 内容 smoke 和真实 start → ready → stop 回路均通过；验证后 `28775` 已释放，stable 5173 正常。新 DMG 未构建，真实客户端配置、系统信任、真实数据、用户库和 App Runtime 均未修改。

- 2026-07-23 本地 MCP v0.5 已取得 Work `PASS`，T0–T2 隔离开发成果已经代码审查并按当前 dirty 主工作区的边界选择性合入；没有整分支覆盖 macOS / Electron / Windows 或成熟度并行改动。当前正式数据、D0-Pilot、M1、真实客户端配置、系统信任、App 接线和打包仍未因本次 Web 合入自动开放。
- 2026-07-23 本地 MCP 工程权威设计已根据 Work 有条件批准反馈升级为 `docs/01-architecture/sapd-wiki-local-mcp-requirements-and-prd-v0.5.md`：Gate 0 拆为 `M0-T` 隔离技术验证轨与需单独数据变更授权的 `D0` 内容就绪轨。`M0-T` 可在限定目录使用 synthetic fixture、测试证书、`ReadOnlyRuntimeProbe` 和状态机模拟，但禁止接入生产启动路径、正式 `ReadOnlyRuntimeContext`、App UI、正式数据库或交付包；`D0-Pilot` 是 M1 计划门禁，`D0-Release` 是真实摘要接入与发布门禁。v0.5 增加加密 PKCS#8（PEM 编码）+ Keychain/DPAPI 口令候选、fixture Schema、`summary_hash` / 唯一键 / manifest digest / 内部审核追责合同，并把生产双平台 UI 验收后移到 M3。当前基础库事实仍为对象 `4678`、关系 `7757`、stable identity `100%`、无 `ai_summary`、来源 `public=0 / confidential=10 / unknown=5`，正式可开放摘要仍为 `0`；跟踪为 `OI-199`。本轮未启动 spike、未修改运行代码、Schema、SQLite、正式 JSON、用户库、证书、系统信任、App 设置或 DMG。
- 2026-07-20 成熟度评分目标模型已按方案 2 升级为 shared runtime `v2.2`：每个评估点的组织与角色、制度与流程、平台与工具、数据与信息均分别填写当前状态、目标状态及可选说明；原单一 `targetLevel / targetReason` 在缺少新字段时自动扩展到四维，现有目标数据可直接进入新界面。关注点“下级评估设置”在原卡片内拆成当前与目标两组，使用相同三列轨道保持文字、滑块和按钮上下对齐；文案为“下级目标状态设置 / 清空下级所有评分 / 统一设置下级目标状态”，目标控件使用 Apple Shell 既有金棕语义色。目标规则统一为大于等于当前：逐项目标维度不得低于同维度当前状态；统一下级目标的最小等级等于所有适用下级四维当前最高等级，反向统一当前的最大等级等于已有目标最低等级，L5 当前允许 L5 目标。规则由前端双向禁选与动作防御、后端 `targetDimensionConflicts`、新旧 XLSX 导入共同执行；综合兼容校验按真实四维指数比较，不再以四舍五入后的展示等级误判相等状态。报告模型为 V3，XLSX 为 `maturity-score-exchange-v2.2` 并兼容旧 V2.1。专项 `231/231`、XLSX `22/22`、5173 maturity smoke、语法检查和应用内浏览器 Design QA 通过；报告历史巡检仅有 `demo-project-002` 一份既有旧模型报告未被改写。真实用户库、正式基础数据、ETL、DMG 均未修改，App 实包回归后置。
- 2026-07-20 成熟度项目队列与项目信息编辑完成 shared runtime / delivery profile 修订：开发服务 `/api/v1/maturity/workspace` 恢复 `5` 条受控测试项目，Mac App / ZIP bundle 运行时仍只投影 `2` 条交付测试项目；项目概览新增“编辑项目信息”，支持修改项目名称、组织、行业 / 规模、客户特点 / 约束、负责人、计划时间、评估人员与备注，并同步客户上下文快照、修改历史及结果 / 报告失效重算。专项审计 `230/230`、开发 / 交付 profile 直测和 macOS bundle parity 通过；未改评分公式、正式基础数据、SQLite、真实用户库或既有 DMG，App 实包回归留待下一次打包。
- 2026-07-19 后续正式导入防复发契约已确认但尚未实现，跟踪为 `OI-198`：approve 只能从 `reviewing` 在单事务内进入，同一 job 的重复 / 并发批准不得产生二次写入；`source_references` 按 target、来源文件、Sheet / 单元格、raw value 和 hash 的完整证据键幂等复用；导出、数据审计和业务验收完成后使用默认 dry-run、显式 apply 的按 job `finalize-import` 清理 staging / review；未显式传 job ID 的正式导出只允许选择最新 approved 任务。权威契约为 `docs/03-import-etl/import-approval-idempotency-and-retention-contract.md`。实现前禁止重复 approve，正式导出应显式传 approved job ID，本轮一次性固定基线清理脚本不得用于未来 job。截至该轮未关闭问题数为 `6`，已关闭归档问题数为 `194`。本轮只补文档，不改 ETL / CLI / schema、正式 SQLite、源 Excel、正式 JSON、用户库或 DMG。
- 2026-07-18 macOS `V0.2.0` 双版本测试包已完成：授权版与免授权版使用同一 `20260718-143253Z` 时间戳，均包含当前成熟度前端 / API / 基础数据、3 个受控项目 manifest 和 4 套现有 Markdown / HTML / JSON 报告制品；冻结后端显式收集 `openpyxl 3.1.5`。两份 DMG 均约 `170M`，SHA-256 分别为 `4ea0148c373ddb7cc8aa5c948dcf41bcf48dcb4ef0ef852f7850afc55162f8d1`、`88f277e4fa955204b7147aa451870275b752b01f0bb8228d61dd3feca6be90dd`。DMG 校验、深度签名、版本 / license mode、Runtime `--check-only`、空 `user_schema_0.3` 用户库、成熟度 `3 / 10 / 32 / 91 / 160 / 185` Runtime smoke 和 185 行 XLSX 模板导出通过；完整 pre-DMG 套件通过。实包只携带干净用户库模板，成熟度报告仅在目标目录不存在时首次复制，不覆盖已有用户数据。剩余为测试机首次初始化、授权弹窗、窗口行为和真实导出路径人工 UAT；当前仍为 ad-hoc 签名、未公证的内部测试交付。
- 2026-07-17 成熟度对象专用评分依据增加当前字典映射前置门禁：以 `capability-tree.json`、`capability-workbench.json` 和当前基础模板为对象主表，完整核对 91 个关注点、160 条服务关系、185 个评估点。工程字典自身通过，映射结果为关注点 `76 已对齐 / 8 名称语义待复核 / 7 无来源`，服务关系 `125 / 27 / 8`，评估点 `147 / 28 / 10`，因此门禁 `BLOCKED`。源服务在 22 个关注点存在漂移，只作证据，不覆盖当前字典。缺级、遗漏、业务不可评分等分类已冻结，待 8+7 项身份 / 绑定确认后重新按当前字典生成；`OI-197` 跟踪。未改运行代码、字典、源 Excel、正式数据、用户库或 DMG。
- 2026-07-17 Codex 工程指令完成第二轮优化：根 `AGENTS.md` 在既有权威顺序、任务型上下文、设计基线和单 checkout 单 writer 基础上，新增用户对 SAPD 内部子 Agent 的持续授权。父 Agent 负责关键路径、隔离编排、集成和合并态验收；编码子任务使用 forked workspace 与不重叠 write set，长生命周期独立 task 才使用 worktree。慢任务先区分模型、命令、审批、工具挂起、上下文压力和并发写入，再恢复或按需交接；`codex-session-handoff` 已同步为“先诊断恢复、后选择交接”。旧执行线/fan-in 文档已收敛为历史说明。业务代码、数据、用户库和 DMG 未改。
- 2026-07-17 成熟度第三十一轮按用户“评估点情况收窄、每组居中、成熟度值与四维读数同字号同中线”完成 shared runtime 修订：根因是摘要共享网格仍把评估点列按 `0.85fr` 分配，同时后置通用规则把评估点内部改成 intrinsic flex、把成熟度值组保留为 `align-self:end`，因此各子格内容贴左且当前 / 目标值比“组织与角色 + 数值”组合块低约 `16.5px`。现中宽三组改为约 `0.90 : 1.60 : 0.65`，评估点恢复两个等宽子格；当前、目标、四维、适用性、进度都在各自子格水平居中，成熟度等级 / 连续分数与四维均值共用 `28px` token，并把垂直中心差收敛到 `2.5px`。应用内 Browser `1600×900` 实测评估点 / 四维宽度 `210 / 503.68px`、子格中心偏差不超过 `0.5px`、横向溢出和 console warning / error 为 `0`；`T-AS.AD / T-AS.AM / G-SP.SM` 三个 L2 与项目概览负向对照通过，`1280×720 / 540×900` 回流无溢出。专项审计 `222/222`、语法、maturity HTTP/API smoke、数据边界和 5173 stable 通过；三份强制文档同步为第三十一轮 / 业务 `0.1.44`。全局治理仍只被其他活动改动的 `app.js=12772 > 12758` 旧预算阻断，本轮未抬阈值。`OI-192` 保持“已修复 / 待用户验收”；正式数据、用户库与 DMG 未改。
- 2026-07-17 成熟度第三十轮按用户“数字就要一排显示，各区域的宽度有优化空间”收口 L2 四维均值：根因是第二十九轮把 `1130px` 容器误作窄屏阈值，使仍有 `735px` 读数宽度的桌面工作台提前落入两列两行。现中宽三组改为 `0.85 : 1.40 : 0.85`，四维均值保持四个完整名称 / 数值同一行；仅小于 `480px` 容器才回流两列两行。应用内 Browser `1280×720` 实测四列各 `169.25px`、数值 Y 轴跨度 `0`、完整名称无截断、页面横向溢出 `0`；专项审计 `220/220`、语法、maturity HTTP/API smoke、数据边界和 5173 stable 通过。三份强制文档同步为第三十轮 / 业务 `0.1.43`，本轮无评分规则变化。`OI-192` 保持“已修复 / 待用户验收”；影响面为 Web / App shared runtime，DMG App 后置，正式数据和用户库未改。
- 2026-07-17 成熟度第二十九轮按用户“最后一次”收口 L2 四维均值：根因是旧 `.maturity-v19-dimension-summary` 将四个业务标签与数值横向混排，并用 `ellipsis + hidden` 把完整名称压成“组织…”。现四项各自采用“完整标签在上、数值在下”的读数列；宽屏四标签与“适用性 / 评估进度”标题轨道误差不超过 `1.4px`，四个数值同一基线、`tabular-nums`，中窄宽度回流两列两行且不截断。应用内 Browser `2048×1152 / 1280×720` 复验无横向溢出，console warning / error 与禁显字段为 `0`；专项审计 `218/218`、语法、maturity HTTP/API smoke、数据边界和 5173 stable 通过。三份强制文档同步为第二十九轮 / 业务 `0.1.42`，本轮无评分规则变化。`OI-192` 保持“已修复 / 待用户验收”；影响面为 Web / App shared runtime，DMG App 后置，正式数据和用户库未改。
- 2026-07-17 成熟度第二十八轮按“一个问题一个问题修”只收口 current / target 双单元：父成熟度组从错误的两行直接网格改为完整高度单轨，两个子格各自承载标题与 `28px` 等级 / 分数；宽桌面成熟度组最小 `280px`，`1130px` 以下提前进入身份独占行的三列结构。应用内 Browser 在 `1600×900` 实测两格均高 `108.8px`、两组间距 `32.2px`，`2048×1152 / 786×900` 间距 `41.7 / 202.4px`，三视口横向溢出、console 与禁显字段均为 `0`。专项审计 `216/216`、语法、maturity HTTP/API smoke 与 Design QA 通过；三份强制文档同步为第二十八轮 / 业务 `0.1.41`，本轮无评分规则变化。`OI-192` 仍为“已修复 / 待用户验收”；影响面为 Web / App shared runtime，DMG App 后置，正式数据和用户库未改。
- 2026-07-17 成熟度第二十七轮 shared runtime 已完成 Web 复验修订，`OI-192` 保持“已修复 / 待用户验收”：评分项目页外层容器成为唯一纵向滚动 owner，评分表单与四维打分区均 `overflow=visible`；应用内 Browser 实测宽屏项目页 `1000 / 1524 / auto`、表单 `1104 / 1104 / visible`，窄屏项目页 `1306 / 2774 / auto`、表单 `1622 / 1622 / visible`，无双滚动。L2 四标题同基线、等级 / 分数同为 `28px`，`760px` 以下按“成熟度 → 维度均值 → 评估点情况”分组换行；项目概览取消撑满视口，项目 / 进度宽度比 `2.0`。结果“分层评价”改为“能力类别评分”，T / G / M 与当前分数间距均为 `7px`，标题提升到 `14px`。报告页明确完整结果覆盖并保留 Markdown / HTML 两个独立按钮，后端两种输出均包含总体结果、四维、能力类别、L2、差距与人工文字。三份强制文档同步为第二十七轮 / 业务 `0.1.40`，专项审计 `214/214`；语法、成熟度 smoke、数据边界、定向 diff-check、5173 stable 和同画布设计对照通过。组合套件的 P1-5 范围断言、技术服务可见行旧断言与 `app.js +14` 行预算来自同仓库其他活动改动，本轮未越权修改。影响面为 Web / App shared runtime，DMG App 后置；正式 JSON / SQLite、源 Excel、用户库、保护基线和 DMG 未改。
- 2026-07-16 成熟度第二十六轮 shared runtime 已完成 Web 复验修订，`OI-192` 保持“已修复 / 待用户验收”：评分表单成为唯一纵向滚动 owner，四维打分列取消内部滚动条，评估概览在表单内 sticky；L2 四标题同基线且等级 / 分数同为 `26px`。结果标题使用 `L3 充分定义` 等“等级 + 名称”，四维真实画布 `390px` / 半径上限 `150px`，精确值改为两列信息块；项目概览按 `2:1`、移除知识快照，首页宽屏按约 `1.85:1` 左右分栏，默认模板 canonical 名称统一为“SAPD标准能力成熟度模板”。三份强制文档同步为第二十六轮 / 业务 `0.1.39`，专项审计 `213/213`，成熟度 HTTP/API smoke 已按 Markdown / HTML 报告契约通过；应用内 Browser `1280×720` 与 `2048×1152` 的单滚动、摘要基线、雷达几何、概览比例、首页回流、页签、横向溢出、console 和禁显字段均通过。组合套件剩余失败来自共享脏工作区的 Issue P1-5、技术服务可见行旧断言与 `app.js` 行数预算，本轮未越权修改。影响面为 Web / App shared runtime，DMG App 回归仍后置；正式数据、用户库、保护基线和 DMG 未改。
- 2026-07-17 首页 P2-1 V8 已按用户最新业务裁定完成 shared frontend 纠偏：面板标题由“继续工作”改为“工作事项”，删除把 ISSUE 与成熟度评估混合成共同进度的状态概览；两条独立工作流仍各自保留真实数量、显式快捷继续、最近 `5 / 3` 条记录、更多菜单和本地更新时间。知识库六项基础统计改为非对称 Mosaic，实测形成 3 种区块尺寸，但每块仍只表达一个既有业务对象，不引入权重、达成率或跨粒度合计。P2 V8 静态 / 5173 专项、成熟度专项 `213/213`、overview HTTP smoke、语法、5173 stable 和应用内浏览器 `1280×720 / 720×900` 通过；混合概览 `0`、工作流 `2`、行数 `5 / 3`、菜单 `2`、快捷入口 `2`、知识区块 `6`、横向溢出 / console / 禁显字段均为 `0`。三份成熟度强制文档明确本轮无业务规则变化；正式 JSON / SQLite、源 Excel、用户库、受保护基线与 DMG 未修改，App 回归后置。
- 2026-07-16 成熟度第二十五轮 shared runtime 已完成 Web 修订并保持 `OI-192 已修复 / 待用户验收`：评分上下文取消 sticky，评分表单成为稳定工作区，只有四维打分列纵向滚动，评估概览与保存区不动；L2 当前 / 目标等级与分数压缩为无冗余后缀的双层读数。结果宽桌面左列固定“全能力分组雷达 → 紧接四维雷达”，右列独占分层统计；结果首排只保留目标成熟度 `L4  4.09`，删除“目标指数 / 5.00”和“同粒度后端聚合”；二级视图更名为“客户评估结果 / 评分明细清单”并复用下划线 Tab。第二十四轮不适用 / 无证据不阻塞、模板管理和 Markdown / HTML 报告契约保持。三份强制文档同步为第二十五轮 / 业务 `0.1.38`，专项审计 `207/207`；应用内 Browser 实测只有打分区 `scrollTop 0→360`，项目页与概览位置不变，结果双列 `600px + 360px`，横向溢出、console warning / error、主区禁显字段为 `0`。5173 stable，`dataState=ready`，L0 `3`、L1 `10`、L2 `32`、关注点 `91`、服务 `160`、评估点 `185`；完整套件剩余失败来自其他活动会话的 Issue v6、技术服务可见行和 `app.js` 治理预算，本轮未越权修改。正式 JSON / SQLite、源 Excel、用户库、受保护基线、Draw.io 和 DMG 未修改，App 回归后置。
- 2026-07-16 工作台列表名称已按用户裁定统一为 `ISSUE清单`：AppShell 导航、Dashboard 入口、列表页标题 / 可访问名称、删除提示和全局搜索黄金样例同步；单条对象、范围、搜索、导出和操作仍使用 `Issue`，未改路由、用户库、Issue 数据或交互。语法、静态 / 5173 P1-5、滚动契约、搜索质量探针、两条 HTTP smoke、定向 diff-check 和应用内浏览器均通过，旧名称、横向溢出与 console warning / error 均为 `0`。属于 Web / App shared frontend，DMG App 后置。
- 2026-07-16 安全指南目录项使用 `成熟度模型使用指南`，源文档与下载文件保留完整名称“SAPD 成熟度模型使用指南”；指南正文升级为 v1.3，第 3 章按 V2.1 业务设计和真实工程改为“项目队列 → 三步新建浮层 → 模板 → 评分执行 → 评分检查 → 结果 → 报告快照”，补齐 SERVICE / FOCUS、四维 L1—L5、适用性、下级评估设置、自动保存和目标冲突，文件解析与匹配审查只作为导入辅助路径。页面标题、可访问名称、现行导航清单、三份成熟度文档和下载名 `SAPD-成熟度模型使用指南-v1.3.html` 已同步。P1-6 V5 静态 / 5173 专项、P2 静态 / 5173 专项、语法、diff-check 与应用内浏览器通过，旧默认流程、横向溢出与 console warning / error 均为 `0`。属于 Web / App shared frontend；未改成熟度评分 / 聚合、正式数据、用户库、受保护基线或 DMG，App 回归后置。
- 2026-07-16 成熟度第二十三轮 shared runtime 已完成 Web 收口：结果页把 L2 精确值唯一归属按 T / G / M 分组的可折叠成熟度热力表，并按“热力表 → 后端总体优先级 Top 10 → 四维优先改进 Top 10”排列；目录真实点击保持原 `scrollTop`，DIRECT FOCUS 不显示伪单项 Tab，适用性归属标题行；评分检查按目标冲突、未完成、不适用核对、无证据信息分组折叠，完成门禁只由前两类拥有。固定评分上下文已从错误旧 `104px` 高度修正为自然高度，中宽桌面按“能力标题 + 三列统计”排布，实测 `clientHeight=scrollHeight=160px`、共享滚动后位置稳定。三份成熟度文档同步为第二十三轮 / 业务 `0.1.35`；专项审计 `198/198`、Apple Shell、maturity HTTP/API smoke、语法 / diff-check、5173 stable 与应用内浏览器 `1440×1024 / 820×1024` 均通过，页面横向溢出、console error / warning 和主区禁显字段为 `0`。`dataState=ready`，关键数为 L0 `3`、L1 `10`、L2 `32`、关注点 `91`、服务 `160`、评估点 `185`；正式数据包、dataClient、ViewModel、评分公式 / 聚合、ETL、正式 JSON / SQLite、源 Excel、用户库、受保护基线、Draw.io 和 DMG 未修改，App 回归后置。
- 2026-07-16 前端 P2-1—P2-3 shared runtime 用户复验修订已完成：总览在能力 `32`、关注点 `91`、技术服务 `160`、模块 / 措施 `102 / 32`、标准控制项 `4,893` 和字典目录 `8` 外，通过 `/api/v1/dashboard/knowledge-summary` 增加环境 `10`、对象 `51`、作用域类型 `6`、LC-AP / LC-DT 阶段 `8 / 7`、指南 `5`、幻灯片类 `3`、建模语言 `1`、图示视图 `1`，仍不渲染达成率 / 覆盖率；首页标题已统一由 AppShell 唯一持有，显示用户指定的 `SAPD WIKI / 工作台与知识库概览 / 说明文案`，Dashboard 正文不再重复标题；能力目录工具栏 Grid 拉伸已修复且收敛为无标题、无说明、无外框的“全部展开 / 收起到 L0”双按钮，最近定位控件与状态已删除，能力、技术服务、技术模块、工作职能、岗位参考和标准框架默认收起；成熟度指南为 `244px` 目录、`1120px / 17px` 阅读列、`194px` 封面。P2 静态 / 5173 专项、能力清单轻量 smoke、定向 diff-check 和应用内浏览器通过；完整 `static + frontend` 的本任务相关门禁均通过，最终被其他并行成熟度样式变更的 `100px locked project-header height` 断言阻断，未从本任务越权修改。正式 JSON / SQLite、源 Excel、受保护字典 / 标准 / 生命周期基线、成熟度规则、用户库和 DMG 未修改，App 回归后置。
- 2026-07-15 成熟度评分执行第十六轮 shared runtime 已完成：五档矩阵只显示放大的 `L1—L5`，当前对象 Rubric 固定在本维度行内；目标理由与证据说明合并为唯一“评估说明（可选）”，适用项完成 / 导入条件统一为“四维评分 + 目标等级”，评分公式与聚合不变。评分概览只保留综合分、当前 / 目标等级和四维雷达；L0 / L1 / L2 的当前汇总、四维雷达与第二雷达桌面同排，第二雷达依次命名为“下属能力域 / 归属能力 / 归属关注点雷达图”。三份成熟度文档同步为第十六轮 / 业务 `0.1.27`；专项审计 `150/150`、Apple Shell、maturity HTTP/API smoke 和应用内浏览器 `1440×1024 / 640×800` 均通过，横向溢出与 console error 为 `0`。正式数据包、dataClient、ViewModel、ETL、正式 JSON / SQLite、用户库、受保护基线、Draw.io 和 DMG 未修改；App 回归后置。
- 2026-07-15 `OI-196` P1-5 V5 已完成 Issue 范围唯一入口修订：顶部重复的“Issue 范围”下拉及范围状态 chip 已删除，左侧目录成为唯一页面范围所有者；顶部只保留状态、优先级、完整局部搜索和清除筛选，清除筛选不再重置左侧目录。应用内浏览器 `1280×720` 验证顶部范围控件 `0`、左侧目录 `1`，选中 `/capability-mapping` 后执行状态筛选再清除，范围仍保持且队列为 `12 / 40 条`，页面横向溢出与控制台错误均为 `0`。P1-5 V5、滚动契约、完整 frontend / runtime、治理、数据边界和 5173 状态通过。未修改 Issue 用户库、数据、导出规则、API、正式 JSON / SQLite 或 DMG；影响面为 Web / App shared runtime，App 回归后置。
- 2026-07-15 成熟度评分执行第十五轮 shared runtime 已完成：按用户裁定把评估执行改为四行五档 L1—L5 矩阵，把评估概览改为可折叠 Vibrancy inspector，并删除“维度得分明细”；概览保留综合分、目标 / 进度、关注点四维雷达和强弱 / 极差。L0 / L1 / L2 在当前节点四维雷达之外新增直属下级雷达，评估结果新增总体四维雷达并保留全部 L2 能力雷达；缺失结果不补 `0 / L1`，总体目标只作等轴参考。三份成熟度文档已同步为第十五轮 / `0.1.26`，专项审计新增矩阵、同粒度双雷达、行所有权和窄屏壳层断言；5173 应用内浏览器覆盖 L0 / L1 / L2 / 关注点、结果页、`1440×1024` 与 `640×800`，横向溢出和 console error 均为 `0`。未修改评分 / 聚合规则、API projection、dataClient、ViewModel、ETL、正式 JSON / SQLite、源 Excel、用户库、Draw.io 或 DMG；App 回归后置。
- 2026-07-15 `OI-196` 第三轮用户复验修订已完成 shared runtime 实现，并于 2026-07-16 按用户确认关闭：根因是旧专项审计只校验共享 class 和圆角存在，没有校验组件完整 DOM、位置无关几何和真实图片盒边界，导致标题区 `30/26px` 私有覆盖被误判为全局胶囊、Issue 只借用搜索外壳却丢失计数 / 前后箭头、图片填满容器盒却没有贴合内容的四边框。现明确图 1 为全局 Apple Shell segmented 权威（外层 `42px / 16px`、按钮 `34px / 12px`），移除标题区私有压缩；Issue 恢复完整页面局部搜索组件，箭头按筛选后的稳定 Issue ID 导航；T 根能力摘要使用全宽单区，真实 `<img>` 缩放盒绘制四边发丝框。应用内浏览器 `1280×720` 实测图片从约 `100×45` 提升为 `455×202`、四边均 `1px`，Issue 搜索从 `1/33` 导航到 `2/33`，页面横向溢出为 `0`。P1-2 V11、P1-5 V4、P0-2、完整 frontend / runtime 套件与治理审计通过。Web 导出、能力清单滚动、标准默认收起和单页无伪 Tab 的上一轮修复保持；未修改源 Excel、SQLite、正式 JSON、字典 / 标准 / 生命周期基线、用户库、Draw.io 原图或 DMG，App 回归后置。
- 2026-07-14 成熟度评分执行页底部空带已完成根因修复：旧评分壳层只扣减 `43px`，与真实 `100px` 项目锁定头相差 `57px`，并叠加项目正文 `22px` bottom padding；左侧完整目录还会反向撑高父 grid。现评分正文、壳层和目录统一按 Apple Shell 可用高度计算，目录只在内部滚动，底部保存栏与页面底边连续。三份成熟度强制文档、专项审计和 `design-qa.md` 已同步；Web 5173 应用内浏览器 `2048×1152` 通过，DMG App 未回归。评分、聚合、Rubric、模板、API projection、正式 JSON / SQLite、用户库和 DMG 均未修改。
- 2026-07-14 共享目录外壳四角问题已完成根因修复：能力、环境、维护、内容四个目录家族统一注册 `.shell-directory-pane`，只允许父 pane 持有 Apple Shell `1px border + 10px radius + overflow:hidden`，滚动正文固定为无边框、无圆角、无阴影；能力目录折叠态已验证宽度为 `0` 且不残留边线。专项配置、P0-2 审计和治理基线同步更新；目录文字、层级、选中 / 折叠逻辑、数据、P0-3、Draw.io 原图、成熟度与 DMG 均未修改。影响面为 Web / App shared runtime，5173 已做应用内浏览器回归，DMG App 后置。
- 2026-07-14 P1-3 生命周期宽表已完成 V3 根因修复，`OI-195` 于 2026-07-16 经用户确认关闭：V2 虽建立唯一纵向 owner，但遗漏 owner 内 LC-AP 13 个、LC-DT 57 个 sticky 单元格的持续重绘成本，也未覆盖最终可见空值。现 lifecycle lane 是经过 `contain` 隔离的唯一业务纵向 owner，宽表容器只横向滚动，全部表头 / 首列退出 sticky，“横向滚动查看更多字段”已删除；`null`、空串和纯 `/`、`-`、破折号占位统一显示 `—`，业务标点和合法数值 `0` 保留。专项审计真实渲染 8 个 LC-AP、7 个 LC-DT 阶段；应用内浏览器 `1280×720` 取证为两页 sticky `0`、提示 `0`、纯 `/` / `-` `0`、页面横向溢出 `0`。`OI-194` 的模块字典引用结论继续有效，后续复验范围不足由 `OI-195` 补齐。正式 JSON / SQLite、ViewModel、受保护字典 / 标准 / 生命周期数据、源 Excel、用户库和 DMG 未修改；影响面为 Web / App 共享前端，DMG App 本轮未直接回归。
- 2026-07-14 全工程前端 P1-2 已修订为 V3：能力页继续保留用户认可的图 2 结构——当前对象位于画布外，画布内为左 Tab / 右搜索，`26px / 18px` 两级圆角、目录 `240px`、`220—300px` 拖拽和窄屏折叠不变。用户随后指出环境两个 Tab 四角仍有双弧，根因确认是 `.environment-detail-pane` 与内部 `semantic-panel / environment-basemap-lab-main` 同时绘制边框、圆角和背景；现统一以 `.environment-detail-pane` 为唯一外轮廓所有者，Tab 根、Draw.io 主画布和映射工作台全部去除外层边框 / 圆角 / 阴影，由父外壳裁切。应用内浏览器在 `1898×1064` 分别验证“信息化环境视图”和“信息化环境-安全技术 → 云数据中心 → 管理平台”，四角均只剩一条轮廓、console error `0`；P1-2 V3、P0-2、`quick` 和 5173 加载通过。该任务当时识别出的 `viewModels.js` 19 行治理差异已在 P1-3 V2 中完成来源审查、专项断言和基线更新，`OI-193` 已自动关闭。系统 Chrome 两次在建立 DevTools target 前退出，不计为页面通过；P0-3、Draw.io 四项冻结资产、正式数据和 DMG 未修改，DMG App 回归后置。
- 2026-07-14 全工程前端 P1-3、P1-4、P1-5 已按顺序完成 shared runtime 实施：P1-3 为 LC-AP / LC-DT 增加当前阶段 / 过程上下文、13px 正文和局部横向宽表滚动，V3 进一步取消所有粘滞表格单元格、删除横向提示并统一最终空值为 `—`；P1-4 把字典结构色收敛为中性层级、蓝色只留给显式选择，标准 Tab 补齐方向键 / Home / End 与 roving tabindex，普通行改为发丝分隔；P1-5 把 Issue 改为六列紧凑队列，搜索页只保留顶部一个主输入、20 条分页、首个命中高亮，并按 `query + filter + page` 恢复滚动。三个包分别使用独立 CSS、配置和专项审计，可逐包回退；已接入 `static / frontend` 套件。当前 P1-3 V3 专项与 runtime 通过；完整前端总控的治理门禁被其他并行任务对 `app.js`、P1-2 / P1-4 / P1-5 CSS 的未登记增长阻断，未由本轮混改。未启动系统 Chrome、未做 DMG App 回归；未修改 P0-3、成熟度业务 / 评分、Draw.io 原图、ViewModel / ETL、正式 JSON / SQLite、受保护字典 / 标准 / 生命周期数据、源 Excel、用户库或 DMG。
- 2026-07-14 全工程前端 P1-1“共享运行状态模板”已于 2026-07-16 按用户确认关闭：新增共享 `RuntimeState` 与独立 Apple Shell 状态样式，显式区分 `loading / empty / missing_file / error / no-selection`；404 只表示缺文件，非 404 HTTP、解析和请求异常进入 error，成功但无记录进入 empty，未选择对象不再伪装成“暂无数据”。能力映射、信息化环境、标准 / 字典代表页面已接入；加载使用骨架，错误和缺文件提供局部重试，重试清理对应缓存，并且只有路由和选择快照都未被用户新操作替代时才恢复原对象。专项配置与审计已接入 `static / frontend` 套件；完整 `static + frontend`、P0-1 / P0-2 / P0-4、P1-1 5173、前端治理、三页 HTTP/API smoke、diff-check 和 5173 stable 均通过。影响分类为 `shared runtime`，Web 与 DMG App 共用代码；DMG App 回归后置。未修改 P0-3 图谱布局 / 碰撞算法、成熟度业务 / 评分、Draw.io 原图、ETL、正式数据包、SQLite、用户库或 DMG。
- 2026-07-14 全工程前端 P0-3“能力图谱碰撞治理”已按用户裁定完整回退并暂停：全量 91 个关注点复核证明上一版只通过少数黄金样例，仍存在标签重叠、连线侵入、交叉、UUID 编码节点和“恢复自动布局”含义不清 / 无有效反馈，不能作为系统性修复。`app.js`、`styles.css`、`LocalRelationNetworkGraph.js` 已精确恢复到冻结的实施前 SHA-256；P0-3 控制器、视图策略、配置、专项审计及测试挂钩已移除，`relationGraphModel` 未改。P0-1、P0-2、P0-4、前端治理、完整 `static / frontend` 与 capability HTTP/API smoke 通过。`OI-138` 保持“暂不修复 / 已回退”；旧版碰撞是已知限制，不再继续局部修补。环境图、Draw.io、能力 / 标准数据、成熟度、正式 JSON / SQLite、用户库和 DMG 均未修改。
- 2026-07-13 全工程前端 P0-4“标准与 Issue 壳层派生修复”已实施：8 个标准深链统一恢复“安全标准 / 框架”全局入口和当前框架双层位置，并保留 `10px` 导航留白；Issue 以 `workbenchSelectedIssueId` 作为唯一详情当前对象，首次进入 inspector 宽度为 `0`，行点击或键盘显式打开，关闭后还原队列宽度与来源行焦点，批量勾选不再驱动详情，路由重入不泄漏旧选择。P0-4 专项、滚动 owner 和完整 `frontend` 套件通过；5173 应用内浏览器已覆盖初始、打开、关闭、键盘及路由重入。标准 / Issue 数据、用户库、路由清单、成熟度模块和 DMG 未修改，DMG App 回归后置。
- 2026-07-13 全工程前端 P0-2“Apple Shell 与共享布局基座”已实施并按旧 DMG `0.1.7` 视觉真值完成标题舒适度修正：品牌退出页面 `h1`，页面标题区成为唯一主标题所有者；顶栏 / 页头固定为 `56px / 96px`，标题 `24px / 1.13`、说明 `12px / 1.45`、页头内边距 `12px 18px`、文本组间距 `5px`，1440×1024 主区计算高度 `872px`。共享壳字号 / 圆角为 `12 / 14 / 16 / 24px` 和 `6 / 10 / 14px`。成熟度继续使用 `main-only`、`maturityShellHeaderActions` 和居中 workflow overlay，业务、评分、聚合和结果规则不变。P0-2 v4、成熟度 `99/99`、P0-1、完整 `static / frontend` 套件、治理审计和应用内浏览器验收通过；`OI-191` 已修复并待用户验收，DMG App 回归后置。
- 2026-07-13 全工程前端 P0-1“正确性与安全边界门禁”已实施：统一命令覆盖 L0 / L1 / L2 / 关注点对象闭环、Draw.io 原图哈希和外部 overlay、状态 / 对象语义色、代表键盘 / 动态播报及 14 个主区禁显字段；同时修复能力视角 Tab 不可见焦点、环境定位无播报和 `localRelationMap.sourceEvidence` 展示边界。`frontend`、`runtime`、`static` 套件与环境底图 72 项契约均通过；未启动系统 Chrome，DMG App 回归后置。
- 2026-07-06 `OI-189` 已关闭，用户确认验收通过：LC-DT 数据重要程度安全策略矩阵以源表 `LC-DT 安全技术服务、模块、策略映射表` 的单行作为权威粒度，禁止按阶段全集或模块全集扩展单行服务。已重新导出 `lifecycle-knowledge.json`、`lifecycle-workbench.json`，并固化全局 / 局部搜索定位规则：生命周期搜索结果必须通过 `route + selected_process_id + target_ref` 定位到矩阵行或具体 chip。源 Excel、SQLite、字典包、标准包、环境包、用户 Issue / 批注数据库和 macOS 打包产物未修改。
- 2026-07-08 最新 macOS DMG 已基于当前工作区升级打包到正式版本 `0.1.7`，构建时间戳为 `20260708-030834Z`。授权版为 `apps/macos/SAPDWiki/dist/license/SAPD-Wiki-0.1.7-license-20260708-030834Z-mac-arm64.dmg`，SHA-256 `25f5502795bd69539335c45020b09fdf047d48d937b8e330a9cc94329a0f0912`；无授权版为 `apps/macos/SAPDWiki/dist/no-license/SAPD-Wiki-0.1.7-no-license-20260708-030834Z-mac-arm64.dmg`，SHA-256 `904b55c29a9b8891b16adc1684348ebf301dd56639151b9850220cd9b51aab97`；两包大小均为 `163M`。`package_dmg.sh` 继续默认一次生成 `license` / `no-license` 两个 DMG，版本号和时间戳一致，文件名和存储路径区分变体。授权版启动先弹授权窗口，正确授权码 `Passc0de` 后显示 `已激活` 且不受限制；不知道授权码可跳过进入 30 天试用；到期后不能跳过。无授权版不弹授权窗口，前端授权状态显示 `无限制版`。`Info.plist` 已统一为 `CFBundleShortVersionString=0.1.7`、`SAPDWikiDisplayVersion=0.1.7`，并通过 `SAPDWikiLicenseMode=license/no-license` 区分变体。该 DMG 为 ad-hoc signed 非公证包，外部分发仍可能触发 macOS `隐私与安全性` 手动允许打开。用户已否决正式 Developer ID 签名公证方案和安装 / 自动更新机制方案，按内测交付现状接受并归档 `OI-190`。
- 2026-07-08 发布验收矩阵已建立：`docs/09-delivery/release-acceptance-matrix-0.1.md` 把 SAPD Wiki DMG 发布对象定义为“同源知识库 Runtime 在本地桌面 App 中，带用户状态、授权状态、保存路径和导出路径后，能否稳定完成顾问核心工作流”。矩阵包含 `dev-smoke`、`pre-dmg`、`internal-release`、`public-release` 四级，G0-G3 自动验收、M1-M7 人工验收、P0-P3 阻断分级、证据目录和发布判定模板；`scripts/audit_mac_dmg_browser_parity_contract.mjs` 已新增 `release_acceptance_matrix_defines_execution_gates` 门禁。后续不能只说“Web/DMG 通过”，必须按矩阵给出证据、阻断项和业务接受限制。
- 2026-07-08 Web / App 双运行面 bug 修复规则已固化：后续每个 bug 根因修复都必须先声明 `shared runtime`、`data / ETL / JSON package`、`web-only`、`app-only` 或 `release blocker` 分类；完成反馈必须写明 `影响面：Web / App / 两者 / 暂未覆盖`、`根因层` 和 `验证范围`。`app-only` 和 P0 / P1 `release blocker` 不能用 5173 通过关闭，必须进入 DMG App 验收、发布矩阵证据目录或 `known-limitations.md`。
- 2026-07-14 `PLAN-MAT-WS` 第十一轮滑块比例与下级评估设置已完成 Web 实现与自动契约，`OI-192` 保持已修复并待用户验收：四维单项与下级批量初始化继续共用五档原生 range，桌面轨道缩短至最大 `290px`并加厚至 `30px`，thumb 为 `34px`，比例接近用户提供的 Codex Desktop 强度控件。原“统一设置下级”更名为“下级评估设置”并始终可打开：无下级评分时可统一应用，有评分时只可确认清空，清空全部下级 SERVICE 的 `elements / reviewElements` 后才再次开放统一应用；适用性、目标、理由、依据、证据和备注均保留。三份成熟度强制文档、Issue、Design QA 和审计已同步；专项 `126/126`与其他回归结果见 `progress.md`。应用内 Browser 运行库仍因 `Cannot redefine property: process` 无法建立会话，按规则未启动系统 Chrome，视觉比例和交互状态待用户固定入口验收。本轮不改评分、聚合、完成率分母或对象粒度，不修改正式 SQLite / JSON、源 Excel、字典 / 标准 / 生命周期基线、用户库或 DMG。
- 2026-07-09 用户已裁定取消额外预览端口、额外分支和额外工作区流程；后续数据或前端试验回到当前 `main`，以受控 demo 页 / demo 数据方式加载和验证。当前基准数据库 `data/database/sapd_wiki.sqlite3` 仍默认视为稳定基准，后续打包默认使用该基准包；demo 数据不得直接覆盖正式 SQLite、正式 JSON、源 Excel、用户库或 DMG，正式提升必须另行确认写入范围、回退方案和审计清单。
- 2026-07-09 AI / 人工智能安全能力体系扩展回到 demo-first：先在当前 `main` 形成 demo 页 / demo 数据和关系样例，验证页面与业务口径；正式接入基础库、标准包、字典包、SQLite 或 JSON 前，必须另行确认权威源、对象粒度、写入范围和审计清单。当前未启动正式数据接入，不修改源 Excel、SQLite、正式 JSON 或 DMG。
- 2026-07-09 已同步 Product Design 对成熟度评估的信息架构口径：成熟度说明类内容放入 `安全指南`；评估工作模块放入 `工作台 > SAPD 成熟度评估`；能力数据只读引用 `知识库字典 > 安全能力清单` 的权威数据，不在 maturity 模块中另建能力主数据。
- 2026-07-06 macOS DMG 用户库、导出和系统设置规则已按最新口径修正：`.app` 内 Runtime 携带干净空用户库模板；首次启动提示用户选择 `App 保存位置` 和 `文件下载路径`，文件下载路径默认指向 `<App 保存位置>/export`；用户库路径为 `<App 保存位置>/Runtime/data/user/sapd_wiki_user.sqlite3`，仅当目标库不存在时从包内模板创建；后续新版 App 默认复用该用户库，不再因 Runtime 指纹变化覆盖已有数据，也不再默认复用旧 `~/Library/Application Support/SAPD Wiki/Runtime`。点击 macOS 顶部 `SAPD Wiki` 菜单只保留 `系统设置...` 和 `退出 SAPD Wiki`；保存位置和下载路径统一在系统设置窗口中展示，右侧提供 `更改路径` 按钮。批注 / Issue 导出由后端写入设置的下载路径并返回 `output_path`，前端显示导出中动画和完成后的实际路径。
- 2026-07-06 `OI-157` 已按当前 macOS DMG 交付口径重新验收并自动关闭归档：上一轮 ZIP 验收口径错误，旧 `apps/macos/SAPDWiki/dist/SAPD-Wiki-mac-arm64.dmg` 内 Runtime 仍为 `user_schema_0.1` 且 `--export-user-notes` 导出 `.json`，不能作为通过结论。已用当前源码重建 backend、重新打包 DMG，并从 DMG 挂载后的 `.app` 复制 Runtime 验收。新 DMG 为 `apps/macos/SAPDWiki/dist/SAPD-Wiki-mac-arm64.dmg`，SHA-256 `5e78d0d04e6165ce06254d406490317b84273877e5d37dd6bac49812e27d7ce0`；通过 `user_schema_0.3`、`--check-only`、DMG 校验、codesign 校验、包内诊断脚本、批注 Markdown 导出、诊断包边界和 127.0.0.1 runtime API smoke。批注导出为 `user-notes-export-*.md`，通用诊断包不包含用户 SQLite 或批注正文。
- 2026-07-06 文档与 Issue 治理规则已收敛：当前 `docs/` 非归档 Markdown 文档约 `107` 个、`31540` 行；若把 `frontend/design-handoff/` 设计交接材料纳入设计文档治理口径，非归档 Markdown 约 `125` 个。后续默认不为小修、小 bug、一次性排查或临时方案新增文档；默认不为低严重性小问题新建 `OI`。只有全局契约、数据 / 审计 / 安全边界、中高严重性、无法本轮闭环、或需要用户业务判断 / 人工验收的问题才进入 `docs/06-implementation/open-issues.md`。修复后若需要用户验收，最终反馈必须明确给出固定入口、导航路径、预期现象和关闭条件。
- 2026-07-06 设计文档治理规则已收敛：设计材料按用途分为信息架构 / brief、全局设计基线、页面实现规格、Stitch / Product Design 参考材料、专题设计契约和交付体验设计。后续页面实现只以 `frontend/design-handoff/implementation-specs/` 中 active / implementation-source 规格为直接依据；Stitch 输入 / 输出、截图、prompt 和旧 brief 只能作为 reference，必须先转成 implementation spec 或补入现有 spec 才能驱动代码。小 UI 修复、文案、局部样式和一次性截图反馈不新增设计文档。
- 2026-07-16 问题台账继续按用户验收口径收敛：`OI-146 / OI-147` 已按源表 `作用域-安全技术服务-安全技术模块映射` H 列合并单元格展开后的目标级安全系统关系修复并二次复核关闭；`OI-135` 已按用户确认关闭归档；`OI-157` 已按当前 macOS DMG 交付口径重新打包验收并自动关闭归档；`OI-190` 已按用户“两个方案都否决，忽略”标记为业务接受并归档；`OI-196` 已于 2026-07-16 按用户确认关闭，覆盖 P1-2 / P1-4 / P1-5。当前 `docs/06-implementation/open-issues.md` 未关闭问题数为 `4`，已关闭归档问题数为 `194`；`OI-192`、`OI-191` 已修复并待用户验收，`OI-138` 因 P0-3 全量业务验收失败标记为“暂不修复 / 已回退”，`OI-128` 保持长期问题。环境映射中的安全系统关系只允许来自源表行级服务-目标组合及其 H 列事实；不得从安全技术服务级聚合系统反推到模块 / 措施，不得让源表 H 列为空的目标继承系统，也不得保留源表不存在的服务-目标组合。`API网关` 的权威源行为第 `205-209` 行，均归属 `应用安全防护`；此前把第 `198` 行 `I-AP&T-AS.AM-01 / API安全防护` 误写成 `API网关` 的反例已纠正。`OI-135` 最终契约为“发布级干净基础库 + 用户库独立迁移 + 中间产物隔离”：`security_work` 源表关系行可重复，但 `knowledge_items` 主对象必须唯一；已正式替换 `data/database/sapd_wiki.sqlite3` 为 clean base stable candidate，正式基础库 `security_work=80`、重复标题 `0`、`knowledge_items.stable_ref=4678/4678`。真实 `data/user/sapd_wiki_user.sqlite3` 已完成 2 条 legacy target_ref 迁移，`user_target_ref_migrations` 写入 `applied=2`，正式库组合审计 `legacyBaseRefs=0`；运行时和打包创建入口已统一到 `user_schema_0.3`，触发用户 API 后正式用户库 `user_meta.schema_version=user_schema_0.3`。正式 apply 报告与回退路径见 `data/exports/worker-verify/oi-135-formal-apply/20260706T063552Z/oi135-formal-apply-report.md`。
- 2026-07-06 `OI-188` 已关闭，用户确认验收通过：`M-* -00` 管理类安全技术服务全局搜索最终契约为“双目标返回 + 精确定位”，即 `/knowledge/technical-services` 字典定义结果和 `/capability-mapping` 能力关系结果都是合法目标；字典结果必须展开并定位服务清单具体行，能力关系结果必须打开 `技术视角` 并定位对应安全技术服务 chip。不得再把两类结果误判为竞争关系并过滤其中一个。
- 2026-07-05 `OI-176 / OI-177` 已关闭，用户确认验收通过：字典引用的 `type/id/code/title` 必须以权威字典为准，`scope_type` 统一从 `maintenance/scopes.json` / 安全能力作用域目录取 canonical title；安全技术模块与安全技术措施不得跨粒度兜底，`主机防火墙`、`主机恶意代码防护`、`主机入侵防御（HIPS）`、`终端安全工作区` 不得以 `security_technical_measure` 类型出现在运行包或 split projection。已修改导出层、split 生成层、字典一致性审计，并受控修正当前正式 `public/data` 运行包；未修改源 Excel、SQLite、标准包、用户 Issue / 批注数据库或 macOS 打包产物。当前验证：字典引用审计 `errors=0`（仅剩既有 `work-function-candidate` warnings）、定向反例探针 `scopeOldTitle=0 / forbiddenMeasureTyped=0`、OI-149 split candidate / formal runtime、基线完整性、JSON 边界、GitHub 数据边界和内容 smoke 均通过。
- 2026-06-20 Object Scope `163 vs 230` 已定性并修正审计口径：原始 `作用域-安全技术服务-安全技术模块映射` E 列中一个合并单元格可包含多个作用域，必须按 scope code 拆分为多个规范 `objectScopeRelations`；不得把整格组合文本作为一个独立 scope。已修正 `scripts/audit_scope_service_module_mapping.py` 的 current helper：优先读取正式 `relations.applies_to_scope`，tree fallback 也会拆分组合 scope 文本；回归计数 `currentCompositeScopeTitleIssueCount=0`。当前 `sourceObjectScopeRelations=163`、`candidateObjectScopeRelations=163`、`formalRelationsAppliesToScope=163`、`helperNormalizedObjectScopeRelations=163`、`helperOnly=0`、`candidateMissing=0`。本次只修审计口径和 worker-verify 报告，未改原始 Excel、SQLite 或正式 `environment-workbench.json`。
- 2026-06-20 Environment Mapping 业务规则已修正并固化：`信息化环境 - 信息化对象 - 安全技术服务 - 安全技术模块 / 措施` 是架构师定义的字典型结构，对象下已填写的服务、模块和措施就是对象级正式定义。不得用全局安全技术服务-模块理论关系反推每个对象必须补齐全部模块 / 服务；原 `581 / 59 coverage gap` 口径废弃，只保留为全局目录对照项，不是业务缺口、错误、风险、blocker 或业务待确认主流程。后续 Environment Mapping hard error 只检查服务引用一致性、模块 / 措施引用一致性、已填写服务+模块/措施组合一致性、系统+模块归属一致性、信息化环境 / 对象引用、1:N 关系导出展示、`pendingRelationCount`、`invalidRowCount` 和 code/title/id 一致性。当前状态为 `environmentMappingStatus=CONTROLLED_SOURCE_CANDIDATE_HARD_ERRORS_CLEAR_PRE_APPLY_VERIFICATION_READY`，可以做 pre-apply verification，但仍不是 `READY_TO_REPLACE`，不得未经用户确认正式替换 `environment-workbench.json`、`node-details` 或正式 review checklist。
- 2026-06-15 P0 临时优先级：`OI-140` 知识库字典、安全标准 / 框架和生命周期基线事故已止血，并完成 SQLite 事实源重建；当前运行基线与 SQLite 导出基线已对齐，关闭前只读巡检通过，`OI-140` 已关闭并归档。标准 canonical title 已由用户确认并作为正式显示名，4 个生命周期来源安全技术措施已确认并补入前端维护基线。当前不再继续 P0 事故修复写入线，后续任何原始数据修改、导入、导出或 workbench 重建，都必须先基于 runtime baseline 生成候选包和 normalized diff，不允许全量重导或直接覆盖。
- 2026-06-15 `OI-142 / Security Technical Service Catalog Visibility` 已完成 `AD-01` 分组位置修复和真实浏览器取证，用户已验收：上一轮曾错误将安全技术服务清单改为按服务编号优先排序，现已回退。随后用户截图证明 `I-DI&T-AS.AD-01 数据分库分表` 被排到 `I-DI` 分组底部；真实根因是 `compareTechnicalServiceRows` 使用 `(Number(sortOrder) || 999999)`，把能力树第一个关注点的合法 `sortOrder=0` 误判为缺失。当前已改为保留 `0` 的有限数字比较，默认排序为 `安全能力 / 关注点顺序 -> 服务字典 sourceOrder -> 服务编号兜底`，实际加载版本为 `technical-service-order-visibility-20260615-2`。专项审计和真实 Chrome 截图确认 `I-DI&T-AS.AD-01`、`I-NT&T-AS.AD-01`、`I-AP&T-AS.AD-01`、`I-HD&T-AS.AD-01`、`I-OS&T-AS.AD-01`、`I-PE&T-AS.AD-01` 均为各自分组第 1 行；服务字典、split 包和 index 均为 `160`，最终表格服务行 `160`，`missingRenderedServices=0`。
- 2026-06-16 `OI-143 / Security Technical Service Reference Integrity` 已修复并进入待观察：清单页和服务字典已对齐，此前全工程服务引用专项审计发现正式运行包、supporting data 和 review 派生产物仍有旧安全技术服务引用。当前按“只修引用、不改字典、不重导、不恢复 Environment Mapping 写入线”的边界新增候选生成脚本，输出目录为 `data/exports/worker-verify/security-technical-service-reference-candidate/`。用户已逐文件确认并正式替换 `lifecycle-knowledge.json`、`lifecycle-workbench.json`、`maintenance/scopes.json`、`shared-lookups.json`，备份分别位于 `formal-apply/20260616T023312Z/`、`formal-apply/20260616T030941Z/`、`formal-apply/20260616T031255Z/maintenance/`、`formal-apply/20260616T031710Z/`。随后备份并重生成 `review/environment-manual-review-checklist.json`；因生成脚本原样带入源表 / consistency rows 中旧服务 raw value，已最小修正 `scripts/build_environment_manual_review_checklist.py`，让 review 输出按 `maintenance/services.json` canonical 化安全技术服务字段，重生成前备份位于 `formal-apply/20260616T032739Z/review/`。当前安全技术服务引用专项审计已 `pass`：`issueCount=0`、`formalRuntimeIssueCount=0`、`nonRuntimeIssueCount=0`；未修改服务字典、SQLite、原始 Excel、Environment Mapping、标准包或前端 UI。
- 2026-06-15 `OI-141 / LC-DT Source Update` 当前状态：安全技术服务字典已受控更新到 `160` 条；用户确认的 LC-DT 源表问题 1-6 已按字典写回原始 `data/raw-samples/wiki sample.xlsx`，问题 7 的 `数据流转监测和泄漏防护` 已补入 `LC-DT 数据生命周期` 表。当前 LC-DT 源表审计为 `ready`：双表 7 个阶段一致、服务引用 `156/156` 命中字典、模块 / 措施引用无新增候选、待确认问题为 0。已按用户确认正式替换 `frontend/capability-browser/public/data/lifecycle-workbench.json`，替换前备份为 `data/exports/worker-verify/lcdt-source-update/formal-apply/20260615T115526Z/lifecycle-workbench.before-lcdt-preapply-20260615T115526Z.json`；旧 hash 为 `c0f727ff1f74c83a9f9f48b5af9231b8fb11b76c9313a82ee9647691f73a7a83`，正式替换后 hash 为 `f69e7afc7dbae2d784769b0c81cf3f39e9b4f6041e3ef6d56d0f3f4e753a0674`，业务内容与候选包语义一致。维护包、能力包、环境包、标准包、SQLite 和原始 Excel 未在本轮替换中修改。
- 2026-06-15 已按用户批准完成 `P0 Source-of-Truth Reconciliation 1.2`：替换前已备份当前 SQLite 到 `data/exports/worker-verify/p0-source-of-truth-reconciliation-1.2/pre-replacement-backup/sapd_wiki.before-p0-sotr-1.2.20260615-162417.sqlite3`。替换前 current hash 为 `52470cbd6fd7cb15852fba352705dd6f028b21f06ffa580cbf7f6edcd5c49f0b`，候选库与替换后 current hash 均为 `7880968a6d10cf2ed4d4b3546329098d555ca265e5965c2f3e23327c058fc8eb`。替换后 6 类受保护类型计数为 `work_function_layer=4`、`work_function=86`、`security_work=80`、`process_reference=78`、`application_system_type=3`、`standard_control=3416`；`audit_dictionary_standard_baseline_integrity.py`、`audit_protected_baseline_no_regression.py`、`audit_json_package_boundary.py`、正式 JSON hash check 和内容级 smoke 均通过。正式 `public/data` 未覆盖，runtime baseline JSON hash 保持一致，未执行全量导出 / core reset，未恢复 Environment Mapping 写入线；`OI-140` 已关闭，归档于 `docs/05-archive/open-issues-history/2026-06.md`。
- 2026-06-15 已完成 `P0 Recovery Runtime Baseline Freeze 1.0`：当前前端冻结 JSON 已固化为运行基线，产物目录为 `data/exports/worker-verify/p0-runtime-baseline-freeze/`，包含 `runtime-baseline-manifest.*`、`runtime-baseline-counts.*`、`runtime-baseline-risk-boundary.*` 和 `future-source-data-change-procedure.md`。关键计数为 `securityWorks=80`、`securityProcesses=10`、`workFunctionLayers=4`、`securityTechnicalMeasures=30`、`standards.controls=4893`、`managementMapping=613`、`standardMapping=4033`、`lifecycle.relations=542`、`standards.frameworks=7`。冻结当时 SQLite hash 记录为 `52470cbd6fd7cb15852fba352705dd6f028b21f06ffa580cbf7f6edcd5c49f0b`，6 类受保护类型计数均为 `0`；该缺口已在 1.2 替换后补齐。
- 2026-06-15 已完成 `P0 Source-of-Truth Reconciliation 1.1`：基于当前运行冻结 JSON、当前 SQLite 和 2026-06-01 SQLite 备份生成隔离候选库 `data/exports/worker-verify/p0-source-of-truth-reconciliation-1.1/protected-baseline-reconciled-candidate.sqlite`，候选库 hash 为 `7880968a6d10cf2ed4d4b3546329098d555ca265e5965c2f3e23327c058fc8eb`。当前 SQLite 6 类受保护类型在候选库中已补齐；候选导出目录为 `data/exports/worker-verify/p0-source-of-truth-reconciliation-1.1/exports-candidate-sqlite/`，未覆盖正式 `public/data`。候选导出 vs 运行冻结 JSON normalized diff 为 `0`，候选库 baseline audit 与 JSON boundary audit 均通过，readiness 为 `ready_for_manual_approval`；该候选库已在 1.2 用户批准后替换正式 SQLite。
- 2026-06-15 新主控接手后完成页面运行态轻量验收与恢复状态封存：`review_standard_framework_canonical_names.mjs` 返回 `requires_user_confirmation=0`；`audit_capability_standard_mapping_canonicalization.mjs` 返回重复标准组、重复控制项、占位控制项、未匹配标准映射均为 `0`；`frontend_content_smoke_check.mjs --url http://127.0.0.1:5173` 返回 `pass`，代表对象 API 均为 `dataState=ready`；`frontend_smoke_check.mjs` 覆盖 `/standards/mlps-level-3`、`/standards/cis-csc-v8`、`/knowledge/management-workflows` 均通过且未启动系统 Chrome。已重新生成 `data/exports/worker-verify/p0-recovery-stable-snapshot/`，快照 `fileCount=54`，`audit_json_package_boundary.py` 为 `errors=0 warnings=0`，`check_github_data_boundary.py` 为 OK。
- 2026-06-15 已完成 `P0 Baseline Canonical Data Correction 1.1`：已按用户确认正式补入 4 个生命周期来源安全技术措施，正式维护包 `security_technical_measures=30`；新增项为 `应用程序威胁建模`、`制品安全加固`、`IaC代码安全测试`、`数据销毁`，来源证据来自 LC-AP / LC-DT。标准 / 框架名称后续已按用户确认，以 `安全能力映射` 中的 canonical 名称同步到左侧目录、`standards-data.json`、`standards-index.json` 和标准分片标题；报告入口为 `data/exports/worker-verify/standard-framework-canonical-name-review.json/md`，当前 `requires_user_confirmation=0`。
- 2026-06-15 已完成 `P0 Source-of-Truth Reconciliation 1.0`：只读对账与候选库生成完成，产物位于 `data/exports/worker-verify/p0-source-of-truth-reconciliation/`。候选库 `protected-baseline-reconciled-candidate.sqlite` 复制当前 SQLite 后只补入当前缺失且 2026-06-01 备份存在的 reconciliation 范围对象；6 类受保护缺失类型在候选库中已补齐，候选库基线审计 `errors=0 warnings=0`。此前候选 / 2026-06-01 备份比正式前端维护包多出的 4 个 `security_technical_measures` 已在 `P0 Baseline Canonical Data Correction 1.1` 中正式补入前端维护基线，不再作为旧 B 类误恢复风险；SQLite 恢复已在 1.2 用户批准后按备份、替换、审计流程完成。
- 2026-06-15 已完成 `P0 Baseline Recovery Closure 1.5`：当前可运行恢复状态已备份到 `data/exports/worker-verify/p0-recovery-stable-snapshot/`，manifest 为 `snapshot-manifest.json/md`；修改边界、根因分析、防复发方案和人工核对计划分别位于 `data/exports/worker-verify/p0-recovery-modification-boundary.*`、`p0-root-cause-analysis.*`、`p0-prevention-plan.*`、`p0-post-recovery-manual-validation-plan.md`。当前应先人工核对数据准确性，不继续 Environment Mapping Triage、正式 UI 字段对齐、maturity、新页面或视觉修复。
- 2026-06-15 追加完成 `P0 Capability & Work Dictionary Binding Recovery 1.4` 和 `P0 Security Work List Empty Page Fix`：`安全管理工作/流程清单`、`安全能力映射 -> 管理视角`、`安全能力映射 -> 标准 / 框架映射` 的前端数据投影已恢复；当前 `securityWorks=80`、`securityWorkRows=92`、`navigationSecurityWorks=80`、`tabSecurityWorks=80`、`securityWorkFocuses=91/91`、`managementMapping=613`、`standardControl=1745`、`standardMapping=4033`。已将 `index.html` 脚本版本提升到 `p0-capability-work-binding-20260615-2`，并修正安全工作清单计数优先取维护包，避免浏览器继续加载旧 `viewModels.js` 或维护页计数来源不足造成空表 / 0 计数。报告入口：`data/exports/worker-verify/p0-capability-work-binding-audit.json/md`；内容级 smoke 入口：`node scripts/frontend_content_smoke_check.mjs --url http://127.0.0.1:5173`。
- 标准 / 框架页面卡死已定位为 `standards-index.json` / `standards-data.json` 的 `dataPath` 指向 `/private/tmp/...` 临时恢复目录；当前已修复为 `./public/data/standards/...` 相对路径，并在导出器、审计脚本和前端加载兜底中加防线。
- `P0 Baseline Incident Containment & Selective Recovery 1.2` 历史结果：当时将 `security_technical_measures` 从 30 回滚到 26；2026-06-15 后续复核确认其中 4 项生命周期来源措施应纳入，`P0 Baseline Canonical Data Correction 1.1` 已正式补回到 30。`lifecycle-workbench.json` 已补回 `relations=542`；报告入口为 `data/exports/worker-verify/p0-*.json/md`。
- `bootstrap-local-data --profile core --reset` 已在 `src/sapd_wiki/cli.py` 增加保护基线拦截；如确需执行，必须先取得用户明确授权并使用 `--allow-protected-baseline-reset`。
- 在用户确认前，暂停 Environment Mapping 后续治理、正式 UI 字段对齐、reimport 和 maturity / Phase 7。
- 已导入 Sheet 的业务含义复核 + 前端关系展示校正。
- 用户已明确：此前多会话和频繁交接是父 Agent 不主动调用子 Agent、长任务又缺少卡顿恢复方案时的无奈替代。后续只有在存在可独立推进的实质工作流时，才由一个父 Agent 编排最小数量的内部子 Agent；小修和强耦合跨模块任务保持父 Agent 串行，能够冻结接口并划分非重叠 write set 的实现才并行。
- Delivery Bundle 早期 ZIP alpha 仍作为历史试发材料保留；当前 macOS 交付验收以 DMG 为准，最新 DMG 已按 `OI-157` 完成批注 Markdown 导出与 Runtime 验收。
- 当前重点不是新增数据源，也不是扩展新模块，而是把已导入数据的业务语义、页面归属和关系展示校正清楚。
- Frontend Baseline 1.0 已确认作为当前前端对齐工作的基线说明。
- 前后端分离本轮已阶段性收口，收口说明见 `docs/01-architecture/frontend-backend-separation-closure.md`。

## 当前页面范围

Frontend Baseline 1.0 当前覆盖四页：

1. `安全能力映射`
   - 主视角：安全能力 / 安全关注点。
   - 技术视角：安全关注点 -> 作用域 -> 安全技术服务 -> 安全技术模块 / 安全技术措施。
   - 管理视角：安全关注点 -> 管理工作 -> 安全流程（L2 流程组 / L3 流程 / L4 活动）或安全职能（4 层）。
2. `LC-AP安全开发生命周期`
   - 主视角：LC-AP 安全开发生命周期阶段。
   - 核心关系：阶段、主要活动、安全活动、安全策略要求、开发技术服务、安全技术服务、安全技术模块、安全技术措施、开发类产品组件、来源证据。
3. `信息化环境维度`
   - 主视角：信息化环境 / 环境子类 / 信息化对象。
   - 核心关系：环境、环境子类、对象、作用域、安全技术服务、安全技术模块、安全系统、产品、来源证据。
   - 该页是第一批核心数据的第三个业务视角，不是新 Sheet 扩展。
4. `LC-DT数据生命周期安全`
   - 主视角：LC-DT 数据处理过程。
   - 核心关系：数据处理过程、处理子场景、安全技术服务、安全技术模块、安全技术措施、来源证据。

## 当前禁止事项

- 不默认启动 Phase 7 多格式增强。
- 不默认启动 maturity M1。
- 不默认新增 Sheet 扩展。
- 不默认重构数据库 schema。
- 不默认大改 ETL。
- 不默认引入 React / Vue 重构当前静态 MVP 前端。
- 不默认大规模搬迁目录、重排文档编号或大拆 `app.js` / `styles.css`；结构治理优先采用索引、说明、归档和小步收口。
- 不在主展示区暴露非业务字段：`sheet`、`row`、`column`、`raw_value`、`source_file`、`import_id`、`source_id`、`source_ref`、`source_label`、`debug`、`raw`、`metadata`、`intermediate`、`generated_at`。
- 不静默覆盖用户文件或未确认业务判断。
- 不允许提交或同步原始数据、SQLite 数据库、ETL 中间产物、前端生成 JSON、指南 / 标准生成资源或导出包到 GitHub；提交前可用 `python3 scripts/check_github_data_boundary.py` 检查。
- 不允许新增页面绕过 `dataClient` 或 `/api/v1/*` 契约直接读取原始数据、数据库或临时 JSON。
- 不允许前端组件实现 ETL、主数据归一、跨表匹配、成熟度评分或业务关系推断。

## 全工程前后端分离规则

- 后端负责数据导入、清洗、标准化、匹配、关系生成、评分、校验、导出和页面数据投影。
- 前端负责导航、布局、筛选、交互状态、表格 / 树 / 关系视图展示和用户反馈。
- 所有页面数据优先通过 `/api/v1/*` 本地 API 和 `dataClient` 消费；`public/data/*.json` 仅作为后端生成的离线兼容包或 API 不可用时的 fallback。
- 新增页面、字段、关系或 maturity 能力前，先更新后端契约和文档，再进入前端实现。
- ViewModel 只能做展示层整理，不承担业务事实生成、关系推断、评分和客户评估结论。

## 当前下一步

主控与专项 task 不再通过固定 thread ID 或 fan-in 规则维护。当前用户要求、当前工作树和经过核对的交接包共同定义活动状态；同一 checkout 的同一文件集只允许一个 writer。当前任务内并行优先使用父 Agent 管理的内部子 Agent 和隔离 workspace；独立 task 只用于用户明确要求的长期工作流。

`OI-128C` 最新结论：用户 2026-06-05 后续抽查提出的定位高亮落到文字后方、L0-L2 批注无常态高亮、普通态高亮线需要加深加粗但不遮挡文字、指南 / 幻灯片页无法添加批注等问题已修复，并已基本验收通过。当前批注设计已作为全局基线固化到 `docs/06-implementation/global-annotation-requirements-and-regression-matrix.md` 和 `docs/06-implementation/frontend-global-design-baseline-2026-05-30.md`；后续新增页面必须按“新页面接入清单”声明页面对象、值锚点、行锚点、幻灯片 / 子页上下文和回归命令，不再逐页重新调试。2026-06-06 已完成 `OI-128C` checkpoint：`b93a9f1 Finalize OI-128C annotation baseline`。后续只按 bug fix 处理，不继续开发新的批注功能或混入工作台 V2 / V3。

2026-06-06 已完成总 backlog 收敛，入口为 `docs/07-governance/backlog-convergence-2026-06-06.md`。当前 dirty worktree checkpoint 已完成：`e23c6d7 Document backlog convergence and frontend planning` 固化 Product Design 审阅、dashboard 契约草案和 backlog 收敛；`f305d1a Checkpoint DB governance and route stability` 固化用户库 / stable key 治理、临时库 smoke 和 `OI-136 / FE-ROUTE` 深层路由修复。用户已明确：`analytics_summary` 是 P0，但不独占当前最高优先级；Delivery Bundle / 打包任务先往后排。当前已完成三个 P0 代码闭环：`analytics_summary` 已完成 exporter / audit / `data_package_summary` / `dataClient` / dashboard 消费；`OI-135 + DB-11 + DB-2` 已新增 `scripts/migrate_db_contracts.mjs`，提供默认 dry-run、临时库 apply、自动备份、项目真实库写入显式确认门和审计闭环；`OI-128 / OI-135` 已完成工作台总览、数据篮和导出最小闭环，`/api/v1/user/workspaces`、`/api/v1/user/data-baskets`、`/api/v1/user/export-profiles`、`/api/v1/user/exports/preview`、`POST /api/v1/user/exports` 和 `GET /api/v1/user/exports/:id/download` 均具备 token / 临时 runtime smoke；2026-07-06 `OI-135` 已完成真实库 apply：clean base stable candidate 已替换正式 `data/database/sapd_wiki.sqlite3`，真实用户库 2 条旧 UUID target_ref 已迁到 stable ref 并记录 `user_target_ref_migrations`。当前导出文件为受控 JSON 验证闭环，不是最终多格式契约。导出格式与字段边界契约入口为 `docs/06-implementation/user-export-format-contract.md`；用户已确认后续导出表格基本参考原始数据，优先 Excel / CSV / Markdown，幻灯片材料导出 PDF，第一批业务数据集为能力全量映射、信息化环境安全映射、字典与标准框架数据，且 CSV / Excel sheet 字段草案已固化。`scripts/audit_analytics_summary_contract.mjs` 可验证 `capability_focus=91`、覆盖率分母、标准控制项 `1745 / 4893` grain 分离、禁止字段泄露、`dataClient` 契约和 dashboard 消费契约；生成 JSON 仍属于前端离线数据包，不纳入 Git。下一步如继续后端主线，可确认关闭 `OI-135` 或进入第一批业务导出数据集的导出器实现，不再做泛化周边评估。

历史执行线文档只作为考古入口，不再作为当前 Codex task 清单。用户要求交接时使用 `codex-session-handoff`：先区分交接到已有 task、创建新继任 task 或只生成交接包；用户提供 task ID 时必须核对该 task，禁止按最近会话猜测主控。没有明确要求时不创建新 task，也不归档旧 task。

安全能力映射页数据加载反复回退已登记为 `OI-132 / EL-024`。后续继续修改安全能力页前，必须先做数据加载稳定性治理：区分真实空数据、workspace-view 未加载、projection fallback、完整 workbench fallback、对象 mismatch 和重渲染缺失；不得再用局部空态文案或组件补丁替代加载契约治理。

ArchiMate 建模语言页显示效果和加载效率已登记为 `OI-133 / EL-025`。当前页面已按用户最新纠偏修正：恢复两层标题结构，最大标题为 `安全架构建模语言`，第二标题为 `ArchiMate® 3.2 - 企业架构建模标准`；tab 组跟在最大标题后面并居右，`全页面显示` / `下载 PDF` 跟在第二标题后面；下方区域阅读器和不精确热区均已删除；主体只保留可纵向滚动的整张 ArchiMate Poster；点击图片或 `全页面显示` 时在当前页面内打开 Image Lightbox / Fullscreen Modal，不使用新窗口、`Blob` 页面或浏览器原生下载栏；预览层深色底色、默认 contain 适配屏幕、右上角关闭、Esc 关闭、点击遮罩空白关闭，工具栏只保留放大、缩小和适应，支持鼠标滚轮缩放和按住拖动平移；本地整图预览资源已从 PDF 重新导出为 `6741 x 4768` 高分辨率 JPG；正常页面 Poster 已去掉多余 padding，让图片与容器宽度贴合。当前状态为 `已修复 / 待人工验收`。

信息化环境首页 draw.io 底图导入已登记为 `OI-137`。用户明确要求导入 draw.io 第三页 `信息化环境及对象底图`，作为信息化环境安全能力映射首页实例图，不能复用 `SAPD 元素图例` 样例文字；最新方向已改为“可绑定业务数据的语义 HTML 底图”，不是纯 SVG Viewer，也不是重新设计。当前按 fallback 直接解析 `.drawio` 第三页 `mxGraphModel`，以原始 `mxCell` / `mxGeometry` / `style` / `edge` 为唯一结构和坐标来源，生成 `frontend/capability-browser/generated/environmentBasemap.html`、`environmentBasemap.css`、`environmentBasemap.generated.js` 和 `environmentBasemap.semantic.json`：每个 vertex 转成带 `data-mx-id` 等属性的绝对定位 HTML 节点，每条 edge 转成 SVG overlay 连线。当前状态为 `已修复 / 待人工验收`；由于本机没有 `drawio` / `diagrams.net` CLI，复杂 draw.io 私有图标和 edgeStyle 自动路由为近似映射，若人工验收要求官方像素级一致，再恢复官方 draw.io / diagrams.net 导出器链路。

`OI-137` 信息化环境页设计要求已按用户 2026-06-08 多轮反馈固化，后续继续修改该页必须先按以下约束验收，不能再凭感觉补控件：

- 视觉基准：严格参考 `安全架构建模语言 / ArchiMate® 3.2 - 企业架构建模标准` 页面内容面板，而不是重新设计一套底图工具栏。
- 主标题区：`信息化环境安全能力映射` 右侧只保留 `环境底图 / 归纳表格` tab；`导出数据`、`编辑映射` 属于未定义功能入口，必须删除，后续由用户单独定义后再加。
- 内容面板：使用 ArchiMate 内容面板同类结构和视觉语言；二级标题为 `信息化环境及对象底图`，语义与字体必须和 ArchiMate 二级标题一致，即 `h2` + 同一 CSS 标题规则。
- 二级标题右侧：只放真实可用操作。当前没有 PDF 下载，因此只保留和 ArchiMate 同样按钮样式的 `全页面显示`；不得显示统计胶囊，不得显示 `适应 / − / +` 可见缩放按钮组，不得让工具条单独换行漂出。
- 底图下方：删除固定提示栏 `点击底图中的业务对象查看详情。`；节点详情 / 关系高亮如需恢复，必须另行按用户定义设计，不得默认加一条说明栏。
- 交互保留：底图仍可默认适应、滚轮缩放、拖拽平移和全页面显示，但这些能力不等于需要在标题栏暴露一组可见缩放按钮。
- 底图内部：`Environment Basemap Draw.io Style Fidelity & Canvas Bounds Fix 1.0` 已执行。必须按 draw.io 图例渲染信息化对象右上角角标，当前 semantic 区分视觉 `drawioType / iconType` 与业务 `objectType / bindStatus`；已识别 `grouping`、`location`、`communication_network`、`facility`、`application_component`、`node`、`device`、`system_software`、`data_object` 和 `actor`。有效内容边界归一化为 `contentBounds={minX:160,minY:150,maxX:3150,maxY:2180,padding:48}`，canvas 为 `3086×2126`；生成底图自身背景为透明，不再叠加 Draw.io 白板网格。没有 source / target 的自由箭头按 mxPoint 绘制；带 source / target 但显式端点漂出对象过远的边线回贴到源 / 目标对象之间。
- 验收断言：`AppShell` 不含 `导出数据` / `编辑映射`，`EnvironmentLocalRelationMap` 不渲染 `data-environment-basemap-action` / `data-environment-basemap-zoom-label` / `environmentBasemapSelection`，标题使用 `h2`，CSS 不保留旧 `environment-basemap-toolbar` / `environment-basemap-tool` 可见工具条样式。

Delivery Bundle 当前 macOS 交付物按 DMG 验收：2026-07-08 已按正式版本 `0.1.7` 重新生成双变体 DMG，构建时间戳为 `20260708-030834Z`。授权版 `apps/macos/SAPDWiki/dist/license/SAPD-Wiki-0.1.7-license-20260708-030834Z-mac-arm64.dmg`，SHA-256 `25f5502795bd69539335c45020b09fdf047d48d937b8e330a9cc94329a0f0912`；无授权版 `apps/macos/SAPDWiki/dist/no-license/SAPD-Wiki-0.1.7-no-license-20260708-030834Z-mac-arm64.dmg`，SHA-256 `904b55c29a9b8891b16adc1684348ebf301dd56639151b9850220cd9b51aab97`；两包均为 `163M`。已验证两包 `hdiutil verify`、两套 staging App `codesign --verify --deep --strict`、`Info.plist` 版本键和 `SAPDWikiLicenseMode`、包内用户库 `user_notes=0`、DMG 根目录中文 `README-FIRST.md`、Runtime 中文 `README-FIRST.md`、两层 README 均含 `Changelog`、初始化路径 `<所选父级保存位置>/SAPDWiki/Runtime` 和默认下载路径 `<所选父级保存位置>/SAPDWiki/export`；本地生成的 DMG 未带 `com.apple.quarantine` 扩展属性。授权规则：授权版 macOS 壳层启动前先检查授权状态，未激活时显示授权窗口，试用未到期可跳过，到期后禁止跳过；正确授权码激活后不受限制。无授权版不显示授权输入窗口，不启用 30 天倒计时。前端顶栏从 `/api/v1/health.license` 读取授权状态并替换原 `!` 入口。红色关闭按钮隐藏主窗口并把 App 退到后台；Dock / 菜单重开和最小化恢复统一走主窗口恢复函数，避免 WebView 隐藏或不重绘后只剩黑屏。旧 ZIP alpha 设计入口仍为 `docs/09-delivery/zip-bundle-1.0-alpha-design.md`，但不再作为当前 macOS 版本验收口径；既有 alpha 试发材料仍在 `/Users/kim1st/Documents/kim note/04_workspace/research/知识库工程/sapd wiki bundle/dist/releases/0.1.0-alpha/`。Windows 构建脚本和验收清单已就绪，但 Windows 原生 `SAPD-Wiki-Backend.exe` 仍需 Windows x64 环境实测；release manifest 中 Windows 保持 `pending / not_verified`。如需发布本轮 DMG，必须先确认复制范围、checksum 和 release manifest 更新。

优先推进 Frontend Baseline 1.0 的三页 Gap Check 和必要校正：

1. 先做只读差距检查，明确页面现状、缺口、风险和涉及文件。
2. 用户确认后，再进入小范围前端实现。
3. 实现时优先复用当前前端、`dataClient`、后端 API / 数据包契约、ViewModel 展示整理和统一组件风格。
4. 若发现数据缺口，记录为数据契约或待确认问题，不在前端临时硬编码业务关系。

## 必读文件

按 `AGENTS.md` 的任务分类读取，不再设置所有任务统一必读集：

- 局部小修：最近的 `AGENTS.md`、目标 owner、相关测试和必要设计基线。
- 新主控或交接恢复：经过核对的交接包、`AGENTS.md`、当前 dirty 工作树、本文、`progress.md` 最近相关部分和 `findings.md` 当前相关决策。
- 数据、App、发布或跨模块任务：在上述基础上只追加对应契约、Open Issue、验收矩阵或专项 Skill。
- `docs/00-overview/master-context-restore.md` 只解释恢复方式，不再承载静态项目状态。

## 不必默认读取的长文档

以下文件或目录不要每次开工默认读取，只在任务明确相关时读取：

- `task_plan.md` 的完整历史段落
- `docs/05-archive/findings-history/`
- `docs/05-archive/progress-history/`
- `docs/05-archive/context-slimming-2026-05-15/`
- `docs/08-maturity/`
- `docs/05-archive/`
- `data/exports/`
- `frontend/capability-browser/public/data/*.json`
- 大型前端源码文件，除非任务需要检查或修改对应页面

## 轻量结构治理入口

- `scripts/README.md`：脚本分类、长期工具和专题脚本边界。
- `docs/03-import-etl/README.md`：导入与 ETL 文档索引。
- `docs/06-implementation/open-issues.md`：当前未关闭问题入口；已关闭问题通过 `docs/06-implementation/open-issues-index.md` 定位到归档。
- `node scripts/govern_open_issues.mjs`：Open Issues 轻量治理脚本，用于归档已关闭长记录并刷新全量索引。
- `docs/07-governance/capability-mapping-change-control.md`：安全能力映射页变更分级、暂停条件和治理审计入口。
- `node scripts/audit_frontend_governance.mjs`：前端高风险文件基线审计，防止 `styles.css`、`app.js`、`viewModels.js` 和能力映射关键组件继续无意识膨胀。
- `node scripts/audit_frontend_lazy_load_contract.mjs`：前端按需加载契约审计，检查知识库字典和安全标准 / 框架的 `required` / `supplemental` 分片、标准页 tab loader 和组件内取数边界。
- `node scripts/audit_capability_viewmodel_contract.mjs --url http://127.0.0.1:5173`：安全能力映射页 ViewModel 当前对象一致性审计，验证 L0 / L1 / L2 / 关注点不会误用默认关注点或错粒度 projection。
- 安全能力映射页 `renderCapabilities()` 已拆出显式 `loadState` 阶段，后续继续治理时优先沿该边界推进，不要重新把加载判断写回渲染主体。
- 安全能力映射页对象级契约入口为 `/api/v1/capabilities/workspace-view`；前端通过 `dataClient.getCapabilityWorkspaceView()` 按当前选中对象读取，旧 `workspace-projection` 仅作为兼容 fallback。

## 当前 Agent 工作规则

- 对用户不是开发人员这一点保持友好解释，把复杂任务拆成可确认、可回退的小步骤。
- 复杂任务开始前说明：要解决什么、会读或改哪些文件、完成后得到什么、是否需要用户判断。
- 做较大实现、重构或技术选型前，先确认任务边界并读取必要上下文。
- 默认中文记录说明性内容；代码标识、文件名、命令、字段名、对象 `type`、API 路径保留英文原文。
- 只读任务不得修改文件。
- 如果用户明确禁止读取、运行或打开某类内容，严格遵守。
- 未经用户明确要求，不提交、不推送、不建 PR；提交时只 stage 明确相关文件并执行适用的数据边界检查，不依赖固定收口 task。
- 完成反馈报告结果、修改范围、影响面、验证和剩余风险；页面入口、数据状态、用户状态写入和 DMG 证据只在对应边界适用时报告。

## 重连处理规则

如果后续再次出现多次对话重连、主控长时间不继续、或上下文恢复明显变慢：

- 先核对当前 task 状态、dirty 工作树、最后有效输出和正在运行的必要工具。
- 不要默认读取归档、完整历史、导出包、大型 JSON、宽泛 diff 或全量进程列表。
- 用户要求交接时使用 `codex-session-handoff`，核对目标 task 后发送状态完整但历史精简的交接包。
- 不因一次等待超时或暂时无文本输出自动创建、停止或归档 task。

## Codex 轻量执行入口

当用户只说“继续执行”“执行”“排查一下”“修一下”时，默认按以下顺序处理：

1. 读取最近的 `AGENTS.md`，检查目标 dirty 文件并确认 owner。
2. 若是新主控、交接恢复或材料性跨边界任务，再读取本文及 `progress.md` / `findings.md` 的相关部分。
3. owner 和任务明确后直接进入最小实现；不明确时只做最小补充读取或询问一个必要问题。
4. 不默认读取归档、导出包、大型 JSON、数据库备份、完整历史、全量进程、全量 diff、完整 DOM 或长 console log。
5. 前端修改使用固定预览 `http://127.0.0.1:5173/`、目标静态检查和一次批量浏览器/DOM 验收；不默认启动系统 Chrome或另起长期端口。
6. 只有发现数据、API、用户状态、对象粒度、App、打包、发布或安全边界时才进入相应专项 Skill 和扩大验收。

详细规则见 `docs/07-governance/codex-performance-workflow.md`。
