# GB/T 22239-2019 与原始表 `等保三级测评清单` 检查结果

生成日期：2026-05-18

更新记录：

- 2026-05-18 用户已修正原始表后复查，`8.2.3.1 访问控制` 已补入。
- 2026-05-18 用户确认 `F列 / DSP安全策略项` 不做映射、不进入数据库；F 列空值不再作为待补齐问题处理。

## 1. 输入文件

| 类型 | 文件 / 工作表 |
|---|---|
| 标准 PDF | `/Users/kim1st/Documents/work@qax/01.战略咨询规划部/技术架构组/法律法规库/0.法律法规、标准/359项网络与数据安全国标/359项网络与数据安全国标原文/06四大合规/26.等保/119.GB_T 22239-2019信息安全技术 网络安全等级保护基本要求.pdf` |
| 项目原始数据表 | `data/raw-samples/wiki sample.xlsx` / `等保三级测评清单` |

说明：PDF 为扫描图片型文件，`pypdf` 无法直接抽取正文。本轮已安装并使用 `tesseract` + `chi_sim+eng` 对第 8 章相关页做 OCR；OCR 文本用于定位条款编号和抽样核对，最终结论仍建议在正式导入前由人工抽查关键页。

## 2. 总体结论

- 原始表 `等保三级测评清单` 当前有 118 行、6 列，其中数据行 113 条。
- 原始表所有数据行都属于 `8第三级安全要求`，覆盖 `8.1` 到 `8.5`。
- 原始表条款编号无重复，排序正常。
- 与 GB/T 22239-2019 PDF 第 8 章 OCR 结果比对后，曾发现原始表缺少 1 条明确条款：`8.2.3.1 访问控制`；用户修正后已复查通过。
- 原始表中 `8.4.2.1`、`8.4.2.2` 与 PDF OCR 中 `8.4.2 安全区域边界` 下的两段要求内容相对应，可暂不视为缺失。
- 用户确认 `F列 / DSP安全策略项` 不做映射、不进入数据库；该列空值不影响后续 GB/T 22239 控制项入库。

## 3. 原始表结构

| 列 | 表头 | 说明 |
|---|---|---|
| B | `等级保护` | 当前均为 `8第三级安全要求` |
| C | `等保要求` | 如 `8.1安全通用要求`、`8.2云计算安全扩展要求` |
| D | `等保控制项` | 如 `8.1.1安全物理环境` |
| E | `等保三级控制要求` | 条款编号 + 条款名称 + 要求文本 |
| F | `DSP安全策略项` | 不入库字段；用户确认不做映射、不进入数据库 |

## 4. 数量核对

| 分组 | 原始表条数 |
|---|---:|
| `8.1安全通用要求` | 71 |
| `8.2云计算安全扩展要求` | 16 |
| `8.3移动互联安全扩展要求` | 9 |
| `8.4物联网安全扩展要求` | 8 |
| `8.5工业控制系统安全扩展要求` | 9 |
| 合计 | 113 |

原始表按控制项统计：

| 控制项 | 条数 |
|---|---:|
| `8.1.1安全物理环境` | 10 |
| `8.1.2安全通信网络` | 3 |
| `8.1.3安全区域边界` | 6 |
| `8.1.4安全计算环境` | 11 |
| `8.1.5安全管理中心` | 4 |
| `8.1.6安全管理制度` | 4 |
| `8.1.7安全管理机构` | 5 |
| `8.1.8安全管理人员` | 4 |
| `8.1.9安全建设管理` | 10 |
| `8.1.10安全运维管理` | 14 |
| `8.2.* 云计算安全扩展要求` | 15 |
| `8.3.* 移动互联安全扩展要求` | 9 |
| `8.4.* 物联网安全扩展要求` | 8 |
| `8.5.* 工业控制系统安全扩展要求` | 9 |

## 5. 发现的数据问题

### 5.1 `8.2.3.1 访问控制` 已补入

PDF OCR 在 `8.2.3 安全区域边界` 下识别到：

```text
8.2.3.1 访问控制
本项要求包括：
a) 应在虚拟化网络边界部署访问控制机制，并设置访问控制规则；
b) 应在不同等级的网络区域边界部署访问控制机制，设置访问控制规则。
```

此前原始表在 `8.2.3安全区域边界` 下只有：

- `8.2.3.2 入侵防范`，原始表第 80 行；
- `8.2.3.3 安全审计`，原始表第 81 行。

用户修正后，原始表当前已经补入：

| 字段 | 建议值 |
|---|---|
| `等级保护` | `8第三级安全要求` |
| `等保要求` | `8.2云计算安全扩展要求` |
| `等保控制项` | `8.2.3安全区域边界` |
| `等保三级控制要求` | `8.2.3.1访问控制本项要求包括：a)应在虚拟化网络边界部署访问控制机制，并设置访问控制规则；b)应在不同等级的网络区域边界部署访问控制机制，设置访问控制规则。` |
| `DSP安全策略项` | `CLD-11云接入点安全`，但 F 列已确认不入库 |

复查结论：该缺失项已修复。当前 `8.2.3安全区域边界` 下有 `8.2.3.1`、`8.2.3.2`、`8.2.3.3` 三条，编号连续。

### 5.2 `8.4.2` 的编号形态需人工确认

PDF OCR 在 `8.4.2 安全区域边界` 下识别到两段要求文本，但未稳定识别出 `8.4.2.1`、`8.4.2.2` 小节标题：

- “应保证只有授权的感知节点可以接入。”
- “本项要求包括：a) 应能够限制与感知节点通信的目标地址…… b) 应能够限制与网关节点通信的目标地址……”

原始表将其拆为：

- `8.4.2.1 接入控制`；
- `8.4.2.2 入侵防范`。

判断：原始表内容与 PDF OCR 中 `8.4.2` 下的两段内容可以对上，暂不判为缺失；但由于 PDF 扫描页 OCR 对标题识别不稳定，建议后续人工看原 PDF 对该处编号形态做一次确认。

### 5.3 `DSP安全策略项` 不入库说明

用户确认原始表 `F列 / DSP安全策略项` 不做映射、不进入数据库。以下 8 条虽然 F 列为空，但该字段不是 GB/T 22239-2019 原文字段，也不是本轮入库字段，因此不作为数据问题或待补齐事项处理：

| Excel 行 | 条款 |
|---:|---|
| 6 | `8.1.1.4 防雷击` |
| 7 | `8.1.1.5 防火` |
| 21 | `8.1.3.6 可信验证` |
| 25 | `8.1.4.4 入侵防范` |
| 26 | `8.1.4.5 恶意代码防范` |
| 37 | `8.1.6.1 安全策略` |
| 44 | `8.1.7.4 沟通和合作` |
| 100 | `8.3.4.2 移动应用软件开发` |

处理口径：导入 `等保三级测评清单` 时忽略 F 列，只使用等保原文相关字段；不得因 F 列为空阻断导入或创建待补齐问题。

## 6. 暂未发现的问题

- 未发现条款编号重复。
- 未发现原始表条款顺序倒置。
- 未发现 `8.1`、`8.3`、`8.5` 分组中明确缺失的条款编号。
- OCR 抽样比对中，未发现明显整段错位或复制到错误条款的问题。

## 7. 处理建议

1. `8.2.3.1 访问控制` 已补入，无需继续作为缺失项处理。
2. 人工查看 PDF 中 `8.4.2 安全区域边界` 对应页，确认原始表拆成 `8.4.2.1`、`8.4.2.2` 是否符合正式编号。
3. `F列 / DSP安全策略项` 已确认不入库，后续导入和校验忽略该列。
4. 正式导入前，建议将 `等保三级控制要求` 拆为结构化字段：`control_id`、`control_title`、`requirement_text`，避免后续只能靠字符串前缀解析。
