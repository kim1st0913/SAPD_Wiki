# high-level 新流程复核清单（2026-05-27）

## 复核结论

用户修改原始数据后，`安全能力-安全管理元素（high level）` 中的 L3 流程参考已经全部能在 `安全职能流程清单（完善L4）` 目录主表中找到对应项。

当前结论：

- high-level 表不再存在需要补入流程目录主数据的新流程。
- 本清单原先列出的 6 条待复核项已全部解除。
- 流程目录主数据边界不变：目录页仍只以 `安全职能流程清单（完善L4）` 为主数据来源，high-level 表只作为能力 / 关注点到流程的映射关系来源。

## 核对口径

- high-level 来源列：`安全能力-安全管理元素（high level）!H`，即 `L3流程参考（结合信息化对象）`。
- 流程目录来源列：`安全职能流程清单（完善L4）!E`，即 `L3流程参考`。
- 匹配方式：去除空白和中英文括号差异后，按 L3 流程标题精确匹配。
- 读取方式：`openpyxl`，`data_only=True`，并按 Excel 合并单元格锚点值继承。

## 当前源表核对结果

| 指标 | 结果 |
|---|---:|
| high-level 表 L3 流程参考总数 | 89 |
| high-level 表唯一 L3 流程参考 | 78 |
| 流程目录主表 L3 流程参考总数 | 89 |
| 流程目录主表唯一 L3 流程参考 | 78 |
| high-level 不在流程目录主表中的流程数 | 0 |

## 已解除的原待复核项

| 原编号 | 原待复核流程 | 当前源表状态 |
|---|---|---|
| HLP-001 | 资产安全持续管理流程 | high-level 已调整为 `资产安全持续管理流程（ 资产持续监测、整改）`，与流程目录主表 `E7` 对齐 |
| HLP-002 | 安全编码及检查流程 | high-level 已调整为 `安全编码及测试流程`，与流程目录主表 `E18` 对齐 |
| HLP-003 | 制品检测流程 | high-level 已调整为 `制品检测发布流程`，与流程目录主表 `E20` 对齐 |
| HLP-004 | 制品上线安全检查流程 | high-level 已调整为 `制品上线安全检测流程`，与流程目录主表 `E21` 对齐 |
| HLP-005 | 攻防演练/沙盘推演流程 | 流程目录主表已存在 `E57 攻防演练/沙盘推演流程` |
| HLP-006 | 威胁情报生产流程 | high-level 已调整为 `威胁情报共享流程`，与流程目录主表 `E60` 对齐 |

## 导出包同步状态

已执行第二批数据重新 ETL / 审核 / 导出，当前 `frontend/capability-browser/public/data/maintenance-knowledge.json` 已同步用户刚修改后的原始 Excel：

- 当前导出包流程目录为 78 条。
- 当前源表流程目录唯一 L3 流程为 78 条。
- `安全职能流程清单（完善L4）!E57 攻防演练/沙盘推演流程` 已进入导出包。
- high-level 表、流程目录主表和导出包三方差集均为 0。

本轮同步 job：`6bfc916c-efc9-4b6b-805c-9ac7c279485d`。

## 本次不做事项

- 不把 high-level 表流程自动补入流程目录。
- 不修改原始 Excel。
- 不修改 ETL、导出逻辑或前端页面。
