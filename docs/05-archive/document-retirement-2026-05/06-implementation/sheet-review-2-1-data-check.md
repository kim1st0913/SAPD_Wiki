# Sheet Review 2.1：第二批管理类 Sheet 落地前数据核对清单

本报告围绕第二批管理 / 流程 / 职能 / 岗位 Sheet 生成落地前数据核对清单。本轮只读分析，不修改原始 Excel、不修改 ETL、不修改前端、不修改数据库 schema。

补充口径：原“标准与岗位参考”页面后续应改名为“岗位参考页面”，下设两个页签 / sheet 页：`GB/T 42446-2023` 和 `Gartner 工作岗位参考`。GB/T 与安全职能已有 `安全职能 -> GB/T` 单向映射基础，本轮输出 `GB/T -> 安全职能` 反向核对表；Gartner 输出 `Gartner -> 安全职能` 候选映射表，复核前不作为最终事实。

用户补充确认：

- 安全工作需要独立建立编码，编码参考能力关注点，可增加部分简写。
- “岗位参考页面”命名确认，下面分 `GB/T 42446-2023` 和 `Gartner 工作岗位参考` 两个页签。
- L3 流程异常涉及的原始表空白和其他错误已由用户修订，下一轮需复导验证。
- GB/T 未映射的 2 条已由用户补充到原始数据，下一轮需复导验证。
- Gartner 候选映射先按当前结果落地页面格式，后续通过统一问题 `OI-038` 单独人工校对。

## 检查范围

| Sheet | 检查内容 |
|---|---|
| `安全能力-安全工作` | 安全工作编码缺失、重复、冲突和格式异常 |
| `安全能力-安全管理元素（high level）` | L2 安全能力到 L2 流程组严格约束 |
| `安全职能流程清单（完善L4）` | 同名 L3 流程跨 L2 流程组 |
| `安全工作职能清单` | GB/T 到安全职能反向核对 |
| `gartner工作岗位参考` | Gartner 到安全职能自动候选映射 |

## 结果摘要

| 检查项 | 结果 | 问题归属 | 输出文件 |
|---|---:|---|---|
| 安全工作编码检查 | 80 条安全工作记录；80 条缺少独立安全工作编码 | `user_business_review_required` | `data/exports/worker-verify/sheet-review-2-1-security-work-code-check.csv` |
| L2 安全能力 -> L2 流程组严格约束 | 0 个 L2 安全能力违反约束 | `no_issue` | `data/exports/worker-verify/sheet-review-2-1-l2-process-group-constraint.csv` |
| 同名 L3 流程跨 L2 流程组 | 3 条异常 | `user_business_review_required` | `data/exports/worker-verify/sheet-review-2-1-l3-cross-process-group-check.csv` |
| GB/T -> 安全职能反向映射 | 27 条 GB/T 任务参考；25 条有映射；2 条未映射 | `user_business_review_required` | `data/exports/worker-verify/sheet-review-2-1-gbt-to-work-function-review.csv` |
| Gartner -> 安全职能候选映射 | 28 条岗位参考；28 条有候选；0 条未匹配；20 条候选偏宽 | `user_business_review_required` | `data/exports/worker-verify/sheet-review-2-1-gartner-to-work-function-candidates.csv` |

## 1. 安全工作编码检查

结论：`安全能力-安全工作` 原始表当前没有独立安全工作编码列。因此本轮将 80 条安全工作记录全部标记为 `missing_code`。本轮不自动生成编码。

建议动作：在原始表中补充稳定安全工作编码。编码规则参考能力关注点，可增加部分简写；本轮不自动生成编码，后续 Sheet Review 2.2 可先输出建议编码清单供用户确认。

## 2. L2 安全能力 -> L2 流程组严格约束检查

未发现违反约束记录。当前数据中每个 L2 安全能力最多对应一个 L2 流程组。

## 3. 同名 L3 流程跨 L2 流程组检查

发现同名 L3 流程跨 L2 流程组记录：

| L3 流程 | L2 流程组数量 | L2 流程组 | 来源行 |
|---|---:|---|---|
| 安全需求分析及威胁建模流程 | 2 | 开发安全管控流程组；身份访问管理运营流程组 | 开发安全管控流程组: 安全能力-安全管理元素（high level）:17；身份访问管理运营流程组: 安全职能流程清单（完善L4）:17 |
| 开发安全资源持续管理流程 | 2 | 开发安全管控流程组；身份访问管理运营流程组 | 开发安全管控流程组: 安全能力-安全管理元素（high level）:22；身份访问管理运营流程组: 安全职能流程清单（完善L4）:22 |
| 软件供应链安全检测流程 | 2 | 开发安全管控流程组；身份访问管理运营流程组 | 开发安全管控流程组: 安全能力-安全管理元素（high level）:19；身份访问管理运营流程组: 安全职能流程清单（完善L4）:19 |

## 4. GB/T -> 安全职能反向映射核对表

已生成 27 条 GB/T 任务参考核对记录，其中 25 条可由原始表 G 列的 `安全职能 -> GB/T` 映射反向得到安全职能，2 条仅作为 I/J 列引用任务存在，当前未映射到安全职能。

注意：反向映射是展示 / 查询投影，不等于新增原始关系；未映射项不自动补关系。

## 5. Gartner -> 安全职能候选映射核对表

已生成 28 条 Gartner 岗位参考候选映射记录，其中 28 条有候选安全职能，0 条未匹配，20 条候选范围偏宽需要重点复核。

注意：Gartner 映射是自动候选关系，复核前不得写成最终事实，不进入前端主展示的最终关系。

## 需用户确认项

1. 是否在原始表为 80 条安全工作补充独立编码。
2. GB/T 未映射的 2 条引用任务是否需要建立安全职能映射。
3. GB/T 已反向映射的 25 条结果是否符合业务理解。
4. Gartner 的 28 条候选映射哪些接受、哪些删除、哪些调整。
5. “岗位参考页面”改名和两个页签：`GB/T 42446-2023`、`Gartner 工作岗位参考`，后续前端落地时执行。

## 后续建议

可以进入 `Sheet Review 2.2：第二批数据修正落地`。下一轮重点是复导用户已修订的原始表，生成安全工作建议编码清单，补齐岗位参考页面和 Gartner 候选映射待复核状态。
