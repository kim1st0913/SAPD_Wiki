# progress.md

本文件是当前会话恢复入口，只保留最近状态、最近完成事项、关键验证和历史索引。完整执行历史已归档到 `docs/05-archive/progress-history/2026-05.md`。

## 当前状态（2026-05-31）

- 当前分支：`main`。
- 当前主线：Frontend Baseline 1.0 四页关系工作台校正；重点仍是业务语义复核、前端关系展示校正、数据契约治理和字段边界收口。
- 固定预览入口：`http://127.0.0.1:5173/`。前端展示和用户验收默认只看该端口。
- 当前前端设计方向：以 `frontend/capability-browser/apple-morandi-color-demo.html` 为正式颜色基准，走 Apple / iOS / macOS shell 风格。
- 安全能力映射页进入变更控制治理：新增 `docs/07-governance/capability-mapping-change-control.md` 和 `node scripts/audit_frontend_governance.mjs`，后续相关修改先过分级与基线审计。
- `renderCapabilities()` 已完成第一步结构拆分，并已收敛出显式 `loadState` 阶段；当前目标是保持行为不变并降低后续继续治理的回退风险。
- 安全能力映射页已新增 `/api/v1/capabilities/workspace-view` 对象级页面契约，前端优先通过 `dataClient.getCapabilityWorkspaceView()` 按当前选中对象接入。
- 当前禁止事项：不修改 ETL、数据库、数据模型、导出 JSON、workbench JSON、`dataClient` 数据来源边界或 ViewModel 业务逻辑；主展示区不得暴露 `sheet`、`row`、`column`、`raw_value`、`source_file`、`import_id`、`source_id`、`source_ref`、`source_label`、`debug`、`raw`、`metadata`、`intermediate`、`generated_at`。

## 最近完成事项

- 2026-06-01 前端响应式外壳收口：针对 MacBook / 外接显示器不同视口下 LC-AP 宽表撑出整页、左侧 Manifest 目录被横向拖动裁切的问题，新增应用外壳宽度 containment、主内容 `min-width: 0` 边界和 LC-AP 表格局部横向滚动约束；1181-1440px 使用紧凑完整目录，1180px 以下保留图标窄栏策略；同步刷新 `styles.css` 缓存版本号，未修改数据链路、ETL、数据库、ViewModel 或导出 JSON。
- 2026-06-01 全局侧边栏目录紧凑可读收敛：将全局侧边栏从“加宽保证完整显示”调整为紧凑基准，收窄桌面侧栏宽度、降低一级导航字号和图标尺寸，长中文入口允许自然换行，避免挤占主工作区；同步更新全局基线和展示原则，未修改数据链路、ETL、数据库、ViewModel 或导出 JSON。
- 2026-05-31 安全知识新增安全能力清单：新增 `/knowledge/capabilities` 二级页面入口，接入安全知识导航；新增 `CapabilityDirectoryMaintenanceTable.js`，按 L0 能力分类、L1 能力域、L2 安全能力三级分组展开展示安全关注点明细；`viewModels.js` 新增 `capability-directory` 展示投影，只使用 `capability-tree.json` 中的业务字段，不新增 ETL、schema、数据库或导出 JSON。
- 2026-06-01 安全能力清单字段补充：安全能力清单表格补齐原始业务字段展示口径，关注点明细行同时展示上级安全能力定义和关注点描述；仍按 L0 / L1 / L2 归纳展开，不展示来源行列、调试字段或中间字段。
- 2026-06-01 安全能力清单表格列优化：移除明细行重复显示的“层级 / 关注点”列，分组层级仍由 L0 / L1 / L2 分组行表达，表格宽度让给编码、名称、安全能力定义和关注点描述。
- 2026-06-01 安全能力清单定义列收敛：安全能力定义只保留在 L2 安全能力分组行，关注点明细行改为编码、名称、关注点描述三列。
- 2026-06-01 安全能力清单标签与列宽优化：L0 / L1 / L2 胶囊改为“能力分类 / 能力域 / 能力”并去除黑点；关注点编码前新增“关注点”胶囊；L2 定义去掉“安全能力定义”前缀并单行完整输出，关注点表格压缩编码列、放大描述列。
- 2026-06-01 安全能力清单 L0/L1 分组说明收敛：删除 L0 / L1 分组行下方重复的“能力分类 / 能力域”说明，只保留胶囊与标题；L2 分组行继续保留能力定义正文。
- 2026-06-01 安全知识层级分组基准收敛：把“能力分类 / 能力域 / 能力 / 关注点”作为安全知识层级分组显示基准，补充逐层缩进和重点标题口径；安全技术模块清单同步使用“领域分类 / 安全系统”无圆点胶囊；安全能力清单表格宽度收敛到 1920px 宽屏优先阅读。
- 2026-06-01 安全知识目录表格自适应宽度基线：安全能力清单移除固定超宽 `min-width`，改为随父容器 `100%` 自适应和比例列宽；文档明确安全知识目录表格不得用固定超宽撑出一屏，长描述在当前宽度内单行截断并保留完整 tooltip。
- 2026-06-01 安全能力清单描述行高修正：修复关注点描述列继承旧 `.maintenance-description-cell` 三行 clamp 导致行高被撑大的问题，改为单行省略并保留完整 tooltip；全局基线同步明确分组行和明细行都不得因长描述自动换行撑高。
- 2026-06-01 安全能力清单列宽与缩进细调：关注点编码列改为固定 260px 且不换行，层级缩进收敛为 `10 / 30 / 50 / 56px`，关注点名称列按当前表最长名称计算受控宽度，描述列获得剩余空间并保留右侧省略号。
- 2026-06-01 OI-122 ViewModel 对象一致性审计：新增 `scripts/audit_capability_viewmodel_contract.mjs`，模拟安全能力映射页 ViewModel 使用 `workspace-view` 的输入输出，断言 L0 / L1 / L2 / 关注点的当前对象、聚合关注点集合、技术 / 管理 / 标准映射行和 `localRelationMapSource` 不错粒度。
- 2026-06-01 Open Issues 轻量治理：新增 `scripts/govern_open_issues.mjs`，将 `docs/06-implementation/open-issues.md` 瘦身为当前未关闭问题入口；新增 `docs/06-implementation/open-issues-index.md` 全量索引，并归档 118 条已关闭长记录到 `docs/05-archive/open-issues-history/2026-06.md`；当前未关闭问题数为 7，重复编号 `OI-044` / `OI-092` 在索引中用 `OI-xxx#n` 区分。
- 2026-05-31 安全能力映射页对象级契约接入：新增 `/api/v1/capabilities/workspace-view`，返回 `contract`、当前 `selected`、图谱、技术 / 管理 / 标准映射行和 tabs；`dataClient` 新增 `getCapabilityWorkspaceView()`，`app.js` 按当前选中对象请求并保存 `capabilityWorkspaceView`，`viewModels.js` 允许 L0 / L1 / L2 / 关注点使用匹配当前对象的 workspace view 映射行；旧 `workspace-projection` 保留兼容 fallback。
- 2026-05-31 安全能力映射页加载状态机整理：在 `renderCapabilities()` 拆分基础上新增显式 `loadState`，将 `initial`、`workbench_background_requested`、`focus_projection_pending`、`workbench_pending`、`no_selection`、`ready` 阶段集中到 `createCapabilityLoadState()` 和 `resolveCapabilityDetailLoadState()`；本轮不修改视觉、ETL、数据包、后端 projection 或业务推断。
- 2026-05-31 安全能力映射页渲染函数拆分：将 `renderCapabilities()` 初步拆为 `capabilityInitialDataReady()`、`createCapabilityLoadState()`、`resolveCapabilityDetailLoadState()`、`buildCapabilityViewModel()`、`resolveCapabilitySelection()`、`renderCapabilityTree()`、`renderCapabilityPendingDetail()` 和 `renderCapabilityDetail()`；本轮不修改视觉、ETL、数据包、后端 projection 或业务推断。
- 2026-05-31 安全能力映射页变更控制治理：新增治理文档、前端治理基线配置和 `audit_frontend_governance.mjs`，把 `styles.css`、`app.js`、`viewModels.js`、能力映射矩阵组件和图谱组件纳入基线审计；同步更新治理入口、脚本说明、`CURRENT_STATE.md` 和 `OI-122`。
- 2026-05-30 全局侧边栏图标对比度修正：提高左侧一级导航彩色图标中前景图案的对比度，使用白色前景、轻描边和阴影，并单独处理选中态图标内层底色；同步写入全局基线和展示原则，未修改数据链路、ETL、数据库、ViewModel 或导出 JSON。
- 2026-05-30 轻规划 PDF 指南接入：已将 `data/raw-samples/ds design/03 轻规划设计报告模版-v0.3-20250814.pdf` 按现有指南标准导出为 46 张 `2400x1350` 幻灯片，新增 `light-planning.json`，接入 `dataClient`、本地 API、`GUIDE_ROUTE_PACKAGES`、`content-views.json` 和 `/guides/light-planning`；固定端口 `5173` smoke 通过，未绑定 PDF 的其他指南二级页仍保持空状态。
- 2026-05-30 能力映射标题区 L0 编码与统计修正：`安全技术能力 T`、`安全治理能力 G`、`安全管理能力 M` 这类 L0 分类在标题区改为编码位显示 `T/G/M`，标题去掉尾码；右侧轻量统计删除 `标准` 卡，统计网格从 6 格改为 5 格；同步更新脚本和样式缓存版本，未修改数据链路、ETL、数据库、ViewModel 或导出 JSON。
- 2026-05-30 能力映射标题区与四视角 Tab 位置修正：删除关注点标题区的“从当前关注点核对技术、管理和标准 / 框架映射。”说明行，提升标题文字尺寸和位置；为四视角 Tab 与工作台外框增加内侧呼吸位。
- 2026-05-30 能力映射四视角 Tab 视觉收敛：将 `能力关系图谱 / 技术视角 / 管理视角 / 标准 / 框架映射` 局部覆盖为轻量 Apple segmented control，并写入全局基线和展示原则。
- 2026-05-30 业务参考来源展示统一：LC-AP / LC-DT 表格内 `参考来源` 统一为“标签 + 浅色圆角引用框”；补齐 LC-DT `policy.reference` 渲染路径，避免来源裸文本显示。
- 2026-05-30 多 Tab 设计原则补充：全局基线明确视角切换类、阶段序列类、业务分类类和筛选 chip 的不同宽度与信息承载规则。
- 2026-05-30 LC-AP / LC-DT 阶段 Tab 测算等宽修正：阶段 Tab 宽度由当前 AP / DT 阶段组中最长阶段名称和编码测算，箭头移除，保留 hover / click / active 动效。
- 2026-05-30 生命周期 Tab 与能力矩阵交互收口：能力映射技术 / 管理矩阵移除行级 `data-capability-id`，避免点击安全技术服务、模块或措施误触发能力选择 / 能力图谱跳转。
- 2026-05-30 全局字体权重与安全技术服务 chip 统一：全局字体族改为 macOS system UI 优先，标题、导航、Tab、阶段按钮和 chip 字重收敛；安全技术服务统一为浅蓝语义 chip。

## 最近验证

- 2026-06-01 前端响应式外壳验证：`git diff --check -- frontend/capability-browser/styles.css frontend/capability-browser/index.html`、固定 `5173` 服务状态、`/development-security` 1440 与 1728 视口轻量 smoke、GitHub 数据边界检查通过；项目默认 smoke 未启动系统 Chrome，已用本机 Chrome/DevTools 响应式模式做轻量视觉确认，确认左侧目录在本地页面可完整显示且窄屏断点保留图标栏。
- 2026-06-01 全局侧边栏目录紧凑可读验证：`git diff --check` 通过；固定 `5173` 服务状态通过；overview 轻量 smoke 通过；GitHub 数据边界检查通过。
- 2026-05-31 安全能力清单页面验证：`node --check frontend/capability-browser/viewModels.js`、`node --check frontend/capability-browser/components/CapabilityDirectoryMaintenanceTable.js`、`node --check frontend/capability-browser/app.js`、`node --check frontend/capability-browser/components/AppShell.js`、`node scripts/audit_frontend_governance.mjs`、`git diff --check`、固定 `5173` 服务状态、`node scripts/frontend_smoke_check.mjs --page maintenance --route /knowledge/capabilities --url http://127.0.0.1:5173/`、`python3 scripts/check_github_data_boundary.py` 均通过；ViewModel 抽查为 L0 `3`、L1 `10`、L2 `32`、关注点 `91`，主表 HTML 未出现 `sheet`、`column`、`raw_value`、`source_file`、`import_id`、`source_id`、`source_ref`、`source_label`、`debug`、`metadata`、`intermediate`、`generated_at`。
- 2026-06-01 安全能力清单字段补充验证：`node --check frontend/capability-browser/viewModels.js`、`node --check frontend/capability-browser/components/CapabilityDirectoryMaintenanceTable.js`、`node scripts/audit_frontend_governance.mjs`、`git diff --check`、固定 `5173` 服务状态、`node scripts/frontend_smoke_check.mjs --page maintenance --route /knowledge/capabilities --url http://127.0.0.1:5173/`、`python3 scripts/check_github_data_boundary.py` 均通过；抽查确认 L0 `3`、L1 `10`、L2 `32`、关注点 `91`，关注点行包含安全能力定义和关注点描述。
- 2026-06-01 安全能力清单表格列优化验证：`node --check frontend/capability-browser/components/CapabilityDirectoryMaintenanceTable.js`、组件渲染断言、`git diff --check`、固定 `5173` 服务状态、`node scripts/frontend_smoke_check.mjs --page maintenance --route /knowledge/capabilities --url http://127.0.0.1:5173/` 均通过；确认表头不再包含“层级”，明细行不再重复显示“关注点”。
- 2026-06-01 安全能力清单定义列收敛验证：`node --check frontend/capability-browser/components/CapabilityDirectoryMaintenanceTable.js`、组件渲染断言、`git diff --check`、固定 `5173` 服务状态、`node scripts/frontend_smoke_check.mjs --page maintenance --route /knowledge/capabilities --url http://127.0.0.1:5173/` 均通过；确认关注点明细行不再显示安全能力定义，L2 分组行仍显示定义。
- 2026-06-01 安全能力清单标签与列宽优化验证：`node --check frontend/capability-browser/components/CapabilityDirectoryMaintenanceTable.js`、组件渲染断言、`git diff --check`、固定 `5173` 服务状态、`node scripts/frontend_smoke_check.mjs --page maintenance --route /knowledge/capabilities --url http://127.0.0.1:5173/` 均通过；确认不再输出 `relation-chip` 黑点标签和“安全能力定义：”前缀。
- 2026-06-01 安全能力清单 L0/L1 分组说明收敛验证：`node --check frontend/capability-browser/components/CapabilityDirectoryMaintenanceTable.js`、组件渲染断言、`git diff --check`、固定 `5173` 服务状态、`node scripts/frontend_smoke_check.mjs --page maintenance --route /knowledge/capabilities --url http://127.0.0.1:5173/` 均通过；确认 L0 / L1 不再重复显示下方说明。
- 2026-06-01 安全知识层级分组基准验证：`node --check frontend/capability-browser/components/CapabilityDirectoryMaintenanceTable.js`、`node --check frontend/capability-browser/components/TechnologyModuleMaintenanceTable.js`、组件渲染断言、`git diff --check`、`node scripts/audit_frontend_governance.mjs`、固定 `5173` 服务状态、`/knowledge/capabilities` 与 `/knowledge/technical` 轻量 smoke、`python3 scripts/check_github_data_boundary.py` 均通过。
- 2026-06-01 安全知识目录表格自适应宽度验证：`node --check frontend/capability-browser/components/CapabilityDirectoryMaintenanceTable.js`、组件渲染断言、`git diff --check`、固定 `5173` 服务状态、`/knowledge/capabilities` 轻量 smoke、`node scripts/audit_frontend_governance.mjs`、`python3 scripts/check_github_data_boundary.py` 均通过；`open-issues.md` 补回已关闭 `OI-122` 的治理审计引用，不重开问题。
- 2026-06-01 安全能力清单描述行高验证：`node --check frontend/capability-browser/components/CapabilityDirectoryMaintenanceTable.js`、单行描述渲染断言、`git diff --check`、固定 `5173` 服务状态、`/knowledge/capabilities` 轻量 smoke、`node scripts/audit_frontend_governance.mjs`、`python3 scripts/check_github_data_boundary.py` 均通过。
- 2026-06-01 安全能力清单列宽与缩进验证：`node --check` 覆盖 `CapabilityDirectoryMaintenanceTable.js` 和 `TechnologyModuleMaintenanceTable.js`，列宽 / 缩进渲染断言、`git diff --check`、固定 `5173` 服务状态、`/knowledge/capabilities` 与 `/knowledge/technical` 轻量 smoke、`python3 scripts/check_github_data_boundary.py` 通过；`node scripts/audit_frontend_governance.mjs` 仍因既有 `styles.css` 与 `viewModels.js` 行数超过当前治理基线失败，本轮未回退这些已有改动。
- 2026-06-01 OI-122 workspace-view 契约治理验证：补齐 invalid object 的 `standardMappingRows` 空数组，并增强 `scripts/audit_capability_projection_contract.mjs` 对有效对象 / 无效对象的必需顶层字段、数组字段和 invalid object 空数组边界校验；`python3 -m py_compile src/sapd_wiki/api_server.py`、`node --check scripts/audit_capability_projection_contract.mjs`、`git diff --check -- src/sapd_wiki/api_server.py scripts/audit_capability_projection_contract.mjs`、固定 `5173` 重启、`node scripts/audit_capability_projection_contract.mjs --url http://127.0.0.1:5173`、`node scripts/audit_frontend_governance.mjs`、`node scripts/frontend_smoke_check.mjs --page capability --route /capability-mapping --url http://127.0.0.1:5173/`、`python3 scripts/check_github_data_boundary.py` 均通过；契约审计首次在普通沙箱中 `fetch failed`，已按本机服务访问重跑通过。
- 2026-06-01 OI-122 ViewModel 对象一致性验收：`node --check scripts/audit_capability_viewmodel_contract.mjs`、`git diff --check -- scripts/audit_capability_viewmodel_contract.mjs scripts/README.md`、固定 `5173` 服务状态和 `node scripts/audit_capability_viewmodel_contract.mjs --url http://127.0.0.1:5173` 通过；审计覆盖 `T`、`T-AS`、`T-AS.AD`、`T-AS.AD-01`、`T-OF`、`T-OF.AT`、`T-OF.AT-02`、`G-SP.SM-02`，其中 `T` 保持 63 个关注点聚合、366 条技术映射、63 条管理映射、63 条标准映射，关注点级对象使用 `backend_projection`，非关注点对象使用 `viewmodel_fallback`。
- 2026-06-01 OI-122 对象级治理最终验收：`node --check` 覆盖 `audit_capability_viewmodel_contract.mjs`、`audit_capability_projection_contract.mjs`、`viewModels.js`、`app.js`，`python3 -m py_compile src/sapd_wiki/api_server.py`、固定 `5173` 服务状态、workspace-view 契约审计、ViewModel 对象一致性审计、前端治理审计、capability 轻量 smoke、GitHub 数据边界检查和 `git diff --check` 均通过；未启动系统 Chrome / Playwright 视觉回归。
- 2026-06-01 OI-122 状态关闭：`docs/06-implementation/open-issues.md` 中 `OI-122` 已从“处理中”更新为“已修复”；CSS token / table / chip 边界治理和图谱布局截图基线保留为后续增强，不再阻塞本 issue 关闭。
- 2026-06-01 Open Issues 治理验证：治理前 `open-issues.md` 为 1846 行、125 个 issue；治理后 `open-issues.md` 为 118 行、`open-issues-index.md` 为 164 行、归档文件为 1621 行；`node scripts/govern_open_issues.mjs` 可重复运行并保持 `issues=125`、`active=7`、`archived=118`；`git diff --check` 通过。
- 2026-06-01 `OI-092#3` 业务接受关闭：用户确认 LC-DT 后续完全参考当前原始数据，以前旧版数据不再使用；`OI-092#3`（LC-DT 来源证据重复）按 `业务接受` 归档，不再作为待确认阻塞。修正 `govern_open_issues.mjs` 的幂等合并逻辑，避免重复运行时遗失未关闭 issue 或重复输出“当前问题详情”；治理脚本重跑后 `active=6`、`archived=119`，`OI-021` 已恢复为待确认，`git diff --check` 通过。
- 2026-06-01 `OI-042` 业务接受关闭：用户确认以主工程能力清单为能力唯一数据源，成熟度评估表 V2 不再作为能力清单基准；`OI-042` 按 `业务接受` 归档，不再要求把 `M-PS.CT` 反向补入 V2。`node scripts/govern_open_issues.mjs` 重跑后 `active=5`、`archived=120`，`open-issues.md` 当前入口剩 93 行。
- 2026-06-01 `OI-021` / `OI-038` 举例核查与 Gartner 映射显示修复：抽查 `OI-021` 当前流程映射示例为 `T-AS.AD-01`，L2 `网络安全架构管控流程组`、L3 `网络安全技术架构持续管理流程`、L4 显示 `待补充`、相关方含 `信息化管理职能` / `安全专家职能` 等；修复 `OI-038` 页面未显示候选映射的问题，把 `sheet-review-2-2-gartner-to-work-function-candidates.csv` 接入 `maintenance-knowledge.json` 导出，并在 `Gartner 工作岗位参考` 表格 / 详情显示候选安全职能、映射状态和匹配依据。
- 2026-06-01 `OI-038` Gartner 映射显示验证：`python3 -m py_compile src/sapd_wiki/exports.py`、`node --check frontend/capability-browser/viewModels.js`、`node --check frontend/capability-browser/components/StandardRoleReferenceTable.js`、`python3 scripts/sapd_wiki.py export-maintenance-knowledge`、`python3 scripts/data_package_summary.py --package maintenance`、组件渲染断言、固定 `5173` 服务状态、`node scripts/frontend_smoke_check.mjs --page maintenance --route /knowledge/role-references --url http://127.0.0.1:5173/` 和 `git diff --check -- src/sapd_wiki/exports.py frontend/capability-browser/viewModels.js frontend/capability-browser/components/StandardRoleReferenceTable.js frontend/capability-browser/public/data/maintenance-knowledge.json` 均通过；`gartner_roles=28`、带候选映射 `28`、缺失 `0`。
- 2026-06-01 `OI-021` 用户确认关闭：用户确认能力详情应按 `L2流程组 -> L3流程 -> L4关键活动 -> 组织职能相关方` 显示，且 L4 源数据为空时显示 `待补充`；`OI-021` 已标记 `已修复` 并由 `govern_open_issues.mjs` 归档，治理结果 `active=4`、`archived=121`。`OI-038` 保持 `待确认`，本轮不继续修。
- 2026-06-01 新增 `OI-123`：登记 `maintenance-knowledge.json` 单体过大且跨页面耦合，需要拆分与按需加载治理；短期保留兼容包，不立即硬拆。`node scripts/govern_open_issues.mjs` 通过，当前 `issues=126`、`active=5`、`archived=121`。
- 2026-06-01 `OI-053` 用户确认关闭：`NIST 800-53rev5` 的 `PE-12(1)` 英文名称差异已按修订后原始表导入并验证，用户要求已修正则关闭；`OI-053` 已标记 `已修复` 并由 `govern_open_issues.mjs` 归档，治理结果 `issues=126`、`active=4`、`archived=122`。
- 2026-06-01 `OI-066` 业务接受关闭：用户确认 `DSP2级策略清单（2024年版本）` 作废，后续只参考 `DSP策略清单（2026）`；2024 版本缺少 `AST-31` 不再构成待修复问题。`OI-066` 已标记 `业务接受` 并由 `govern_open_issues.mjs` 归档，治理结果 `issues=126`、`active=3`、`archived=123`。
- 2026-06-01 `OI-075` 用户确认关闭：记录中已包含 LC-AP parser 修复、重新导入导出、`SERVICE_ISSUES=0`、`MODULE_ISSUES=0`、`lifecycle-workbench.json data_state=ready` 和页面 smoke 通过；用户确认该问题之前已修复确认。`OI-075` 已标记 `已修复` 并由 `govern_open_issues.mjs` 归档，治理结果 `issues=126`、`active=2`、`archived=124`。
- 2026-06-01 DSP 2024 活跃引用清理：用户确认 `DSP2级策略清单（2024年版本）` 明确不用并建议删除；复查当前 `wiki sample.xlsx` 已无 2024 sheet、仅保留 `DSP策略清单（2026）`，SQLite 活跃库仅有 `DSP策略清单（2026）` 来源引用。同步更新剩余 Sheet 建模文档、样例清单和 `translate_dsp_2026_sheet.py`，去掉对 2024 sheet / SCF 2024.3 的活跃依赖；archive / backups 保留历史追溯。
- 2026-05-31 安全能力映射页对象级契约验证：`python3 -m py_compile src/sapd_wiki/api_server.py`、`node --check frontend/capability-browser/dataClient.js`、`node --check frontend/capability-browser/app.js`、`node --check frontend/capability-browser/viewModels.js`、`node --check scripts/audit_capability_projection_contract.mjs`、`python3 scripts/dev_server_guard.py --restart`、`node scripts/audit_capability_projection_contract.mjs --url http://127.0.0.1:5173`、workspace-view 抽样检查、capability 轻量 smoke、GitHub 数据边界检查和 `node scripts/audit_frontend_governance.mjs` 通过。
- 2026-05-31 安全能力映射页加载状态机整理验证：`node --check frontend/capability-browser/app.js`、`node scripts/audit_frontend_governance.mjs`、`git diff --check`、固定 `5173` 服务状态、`node scripts/audit_capability_projection_contract.mjs --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page capability --route /capability-mapping --url http://127.0.0.1:5173/`、`python3 scripts/check_github_data_boundary.py` 均通过。
- 2026-05-31 安全能力映射页渲染函数拆分验证：`node --check frontend/capability-browser/app.js`、`node scripts/audit_frontend_governance.mjs`、`git diff --check`、固定 `5173` 服务状态、`node scripts/audit_capability_projection_contract.mjs --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page capability --route /capability-mapping --url http://127.0.0.1:5173/`、`python3 scripts/check_github_data_boundary.py` 均通过；projection audit 首次在普通沙箱中 `fetch failed`，已按本机服务访问重跑通过。
- 2026-05-31 安全能力映射页变更控制治理验证：`node --check scripts/audit_frontend_governance.mjs`、`node scripts/audit_frontend_governance.mjs`、`git diff --check` 通过。
- 2026-05-30 业务参考来源漏点修复验证：`rg` 确认 `policy.reference` 只进入 `sourceNote(policy.reference)`；`node --check frontend/capability-browser/components/ApplicationSecurityLifecycle.js` 通过；`git diff --check` 通过；`/data-security` 轻量 smoke 通过。
- 2026-05-30 全局字体权重与安全技术服务 chip 验证：固定 `5173` 服务状态通过；HTTP smoke 覆盖 `capability /capability-mapping`、`dev-lifecycle`、`maintenance /knowledge/technical-services`、`standards /standards/nist-csf-2` 均通过。
- 2026-06-01 GB/T 42446 工作任务描述补齐：从图片型 PDF `028_GB_T 42446-2023 信息安全技术 网络安全从业人员能力基本要求.pdf` OCR 识别表 2 `网络安全主要工作任务及任务描述`，在 `export-maintenance-knowledge` 导出阶段按工作任务名称补齐 `gbt_42446_references[].description`；`/knowledge/gbt-42446` 新增 `工作任务描述` 列，并同步字段契约和页面设计说明。
- 2026-06-01 GB/T 42446 工作任务描述验证：`export-maintenance-knowledge` 后 `maintenance-knowledge.json` 为 `ready`，`gbt_42446_references=27` 且 27 条均有 `description`；`node --check` 覆盖 `StandardRoleReferenceTable.js`、`dataClient.js`，`python3 -m py_compile src/sapd_wiki/exports.py`，`git diff --check`、固定 `5173` 服务状态、`/knowledge/gbt-42446` 轻量 smoke、GitHub 数据边界检查均通过；前端治理审计仍因既有 `styles.css` / `viewModels.js` 行数超过基线失败，本轮未修改这两个文件。
- 2026-06-01 AppShell 侧边目录修正：收紧 Manifest 二级目录左缩进，把 `secondary-navigation` 缩进从 38px 调整为 24px，并在 AppShell 挂载时给 `body` 添加 `app-shell-locked`，让全局目录固定在 viewport，不随页面横向移动。
- 2026-06-01 AppShell 侧边目录验证：`node --check frontend/capability-browser/components/AppShell.js`、定点 CSS/HTML/JS 断言、`git diff --check`、固定 `5173` 服务状态、`/knowledge/capabilities` 轻量 smoke、GitHub 数据边界检查通过；前端治理审计仍因既有 `styles.css` / `viewModels.js` 行数超过基线失败。
- 2026-06-01 Gartner 岗位参考衍生列移除：按用户浏览器批注移除 `Gartner 工作岗位参考` 主表的 `候选安全职能`、`映射状态`、`匹配依据` 三个候选映射维护列，详情面板同步只保留原始业务字段 `分类`；页面提示改为“主表只展示已确认的原始业务字段”。
- 2026-06-01 Gartner 岗位参考衍生列验证：`node --check` 覆盖 `StandardRoleReferenceTable.js`、`viewModels.js`，组件渲染断言确认 Gartner 页签表头仅为 `角色 / 描述` 且 HTML 不含 `候选安全职能 / 映射状态 / 匹配依据 / 待复核`，`git diff --check`、固定 `5173` 服务状态、`/knowledge/functions` 轻量 smoke、GitHub 数据边界检查通过；前端治理审计仍因既有 `styles.css` 行数超过基线失败。
- 2026-06-01 安全能力清单缩进与编码列宽修正：`CapabilityDirectoryMaintenanceTable.js` 将能力分类 / 能力域 / 能力 / 关注点缩进分别调整为 4px / 18px / 32px / 38px，编码列宽从 260px 收紧为 220px，并更新组件缓存版本。
- 2026-06-01 安全能力清单缩进验证：`node --check CapabilityDirectoryMaintenanceTable.js`、定点组件渲染断言、`git diff --check`、固定 `5173` 服务状态、`/knowledge/capabilities` 轻量 smoke、GitHub 数据边界检查通过；前端治理审计仍因既有 `styles.css` 超过基线失败。
- 2026-06-01 安全知识长文本全局基线：`frontend-global-design-baseline-2026-05-30.md` 明确关键定义 / 描述字段必须完整换行展示；`styles.css` 全局取消 `.maintenance-description-cell span` 和 `.standard-group-description` 的固定行数 / `nowrap` 截断；`CapabilityDirectoryMaintenanceTable.js` 改用 `fullText()` 输出安全能力定义和关注点描述，缓存版本更新为 `knowledge-full-text-20260601-1`。
- 2026-06-01 安全知识长文本全局验证：`node --check CapabilityDirectoryMaintenanceTable.js`、完整换行样式断言、`git diff --check`、固定 `5173` 服务状态、`/knowledge/capabilities` 轻量 smoke、GitHub 数据边界检查通过；前端治理审计仍因既有 `styles.css` 行数和 `important` 数超过基线失败，本轮未新增 `important`，`open-issues.md` 已补回已归档 `OI-122` 审计引用。
- 2026-06-01 安全能力清单默认收起：`CapabilityDirectoryMaintenanceTable.js` 取消默认展开第一组，页面无搜索时所有 L0 能力分类默认收起；搜索状态继续自动展开命中结构。验证通过 `node --check`、默认收起 / 搜索展开渲染断言、`git diff --check`、固定 `5173` 状态、`/knowledge/capabilities` 轻量 smoke 和 GitHub 数据边界检查；前端治理审计仍仅因既有 `styles.css` 基线超限失败。

## 当前问题索引

- `OI-001`：关注点 `T-AD.SV-01` 被误判为未挂接，已修复。
- `OI-112`：全局字段命名与显示样式一致性，已修复。
- `OI-113`：前端整体色系未统一到 Apple / Morandi 体系，已修复并继续做组件级增强。
- `OI-114`：能力关系图谱布局修复尝试造成视觉回退，已回退。
- `OI-115`：刷新后层级能力节点误用默认关注点投影数据，已修复。
- `OI-116`：Apple demo 组件级对齐误伤 LC-AP 阶段 Tab 和模块表默认展开，已修复。
- `OI-117`：安全能力 projection 缺少对象粒度契约，已修复。
- `OI-118`：关注点 projection 前端缺少请求防串包校验，已修复。
- `OI-119`：全局表格字号、空值和安全技术对象 chip 口径不统一，已修复。
- `OI-120`：L0 能力节点在完整 workbench 未返回前显示 0 服务，已修复。
- `OI-121`：能力映射标准表格和管理表格字体口径不一致，已修复。
- `OI-122`：安全能力映射页数据加载、ViewModel、图谱和 CSS 覆盖耦合导致反复回退，已修复。

## 历史索引

| 归档文件 | 内容 |
|---|---|
| `docs/05-archive/progress-history/2026-05.md` | 2026-05 完整执行记录、根目录 `progress.md` 瘦身快照和本轮轻量结构治理记录 |
| `docs/05-archive/findings-history/2026-05.md` | 2026-05 历史发现 |
| `docs/05-archive/context-slimming-2026-05-15/` | 2026-05-15 上下文瘦身快照 |

## 维护规则

- 本文件只保留最近 1-3 次重要执行；超过 120 行时继续归档到 `docs/05-archive/`。
- 详细过程、长命令输出和阶段性历史不写入根目录 `progress.md`。
