# Open Issues

本文件现在只保留当前仍需处理或确认的问题、问题模板和治理入口。已关闭问题的完整记录已归档，避免当前入口继续膨胀。

## 治理入口

- 当前未关闭问题数：6
- 已关闭归档问题数：134
- 全量索引：`docs/06-implementation/open-issues-index.md`
- 已关闭问题归档：`docs/05-archive/open-issues-history/2026-06.md`
- 重复编号待治理：`OI-044`、`OI-092`，索引中使用 `OI-xxx#n` 区分历史条目。

## 当前未关闭问题

| 编号 | 状态 | 标题 |
|---|---|---|
| OI-038 | 待确认 | Gartner 与安全职能候选映射需后续人工校对 |
| OI-128 | 部分完成 | USER-WRITE-UI-1：批注 / 工作台用户写入入口 |
| OI-133 | 已修复 / 待人工验收 | ArchiMate 建模语言页显示效果与加载效率优化 |
| OI-135 | 正式迁移脚本完成 / 真实库 apply 待显式确认 | 用户库治理与兼容表迁移清理 |
| OI-137 | 已修复 / 待人工验收 | 信息化环境首页需导入 draw.io 第三页实例底图 |
| OI-139 | 新增 / 待修复 | 作用域-安全技术服务-安全技术模块映射导入关系与当前 JSON 投影不一致 |

## 问题记录模板

## OI-000：问题标题

- 状态：
- 类型：数据 / 前端 / ETL / 文档 / 需求
- 对象或页面：
- 现象：
- 影响：
- 当前处理：
- 需要确认：
- 修复说明：
- 验证结果：

## 当前问题详情

## OI-139：作用域-安全技术服务-安全技术模块映射导入关系与当前 JSON 投影不一致

- 状态：持续数据清理 / 待人工核对
- 类型：数据 / ETL / 投影
- 对象或页面：原始 Sheet `作用域-安全技术服务-安全技术模块映射`，影响 `信息化环境安全能力映射` 的 `归纳表格` 和 `环境底图` 节点详情浮层。
- 现象：2026-06-11 执行 `Scope-Service-Module Mapping Reimport 1.0` 审计，直接用 `openpyxl` 从 `data/raw-samples/wiki sample.xlsx` 读取该 Sheet 的 merged cells、样式和行列坐标，生成 normalized rows 与关系集后，发现当前 `environment-workbench.json` 与源表重导入结果存在关系级差异：重导入 `service-measure=123`，当前 JSON 为 `273`；重导入 `module-system=214`，当前 JSON 为 `157`；当前 JSON 多出 162 条措施关系、缺少 61 条模块-安全系统关系，并存在 3 条跨对象继承疑似样例。
- 影响：当前视图数据不能视为可信；`安全技术服务 -> 安全技术模块/措施 -> 安全系统` 关系可能出现缺失、错配、过度聚合或跨对象继承，进而影响归纳表格和环境底图浮层的业务解释。
- 当前处理：已新增并修订 `scripts/audit_scope_service_module_mapping.py`，只生成审计文件，不覆盖正式数据包。审计产物位于 `data/exports/worker-verify/scope-service-module-mapping-reimport-audit.md`、`scope-service-module-mapping-reimport-audit.json`、`scope-service-module-mapping-normalized-rows.json`、`scope-service-module-mapping-relations.json`。该 Sheet 共 `1327` 行、`8` 列、`417` 个 merged ranges；核心字段合并区域数为：`信息化环境=12`、`环境子类=31`、`信息化对象=66`、`作用域=66`、`安全技术服务=11`、`安全技术模块/措施=117`、`安全系统=116`。2026-06-11 按用户修订口径废止裸 `informationObject` 名称判重，改用 `信息化环境 + 环境子类 + 信息化对象` 作为对象实例唯一键；重新审计后 `duplicateInformationObjectContexts=0`，原 `PC终端操作系统`、`PC终端设备`、`PC终端软件应用`、`办公人员` 等 8 个同名对象降级为 `sameNameDifferentContexts=8` 信息提示，不再作为高风险重复对象阻断。
- 需要确认：1.4 已持久替换正式 `environment-workbench.json` 与 `environmentBasemap.node-details.json`，但只完成 HTTP smoke，未执行系统 Chrome / 浏览器视觉回归。当前浮窗 / 归纳表格组件是否展示 `securitySystems` 字段仍取决于后续 UI 字段对齐任务；正式页面仍需用户人工检查重点节点的服务数量、模块 / 措施分组和安全系统展示缺口。
- 修复说明：本轮新增 `scripts/build_environment_workbench_reimport_candidate.py`，基于审计 normalized relations 生成 `data/exports/worker-verify/environment-workbench-reimport-candidate.json`、`environment-workbench-reimport-candidate-diff.json/md` 和 `duplicate-object-resolution-report.json/md`。候选包中对象实例和关系均绑定 `contextKey=信息化环境||环境子类||信息化对象`，同名不同上下文不会合并；本轮未替换正式 `public/data/*.json`，未修改前端 UI、SVG、Draw.io、数据库 schema 或原始 Excel。
- 2026-06-11 下游兼容性验证：新增 `scripts/verify_environment_reimport_candidate_downstream.py`，生成 shadow 包和替换前 readiness 报告。`environment-workbench-reimport-shadow.json` 保留正式包关键顶层结构，并以 candidate 关系构建 `environment_scope_tree`；`environment-scope-service-matrix-shadow-rows.json` 可生成 `信息化环境 / 环境子类 / 信息化对象 / 作用域 / 安全技术服务 / 安全系统 / 安全技术模块 / 安全技术措施` 八列，安全系统独立成列，模块和措施分开。shadow 统计为 `shadowEnvironments=10`、`shadowObjectContexts=67`、`matrixRows=788`、`moduleSystemRelations=214`、`securitySystemCells=566`、`moduleCells=612`、`measureCells=123`，校验错误为 `0`。底图节点 shadow diff 显示当前 `node-details` 仍由旧包派生，多个节点 current 模块 / 措施数量明显高于 shadow；正式替换前必须同步重算 node-details 并做页面级回归。
- 2026-06-12 替换候选 Bundle：新增 `scripts/build_environment_reimport_replacement_bundle.py` 和 `scripts/verify_environment_reimport_replacement_bundle.py`。Bundle 位于 `data/exports/worker-verify/environment-reimport-replacement-bundle/`，包含候选 `environment-workbench.json`、重算 `environmentBasemap.node-details.json`、矩阵行、manifest、workbench diff、node-details diff、页面回归报告和最终 readiness 报告。统计为 `environmentCount=10`、`objectContextCount=67`、`relationCount=1741`、`matrixRows=788`、`detailReadyNodes=91`、`missingDetailNodes=0`、`moduleSystemRelations=214`、`securitySystemCells=566`、`moduleCells=612`、`measureCells=123`。
- 2026-06-12 正式替换：正式 `frontend/capability-browser/public/data/environment-workbench.json` 已替换为最新 replacement bundle，替换后 hash 为 `2361374d7a38274cf82db026e60a21c121085483bb40ab711b8a13ab77ec7f94`；正式 `frontend/capability-browser/generated/environmentBasemap.node-details.json` 已替换为最新 replacement bundle，替换后 hash 为 `08d758480813ce133056a17b30a80e08d70c1b0beeb03d603a7356f5307dc73a`。旧正式文件已备份到 `data/exports/worker-verify/environment-reimport-official-backup/`。1.4 官方替换报告位于 `data/exports/worker-verify/environment-reimport-1.4-official-replacement-report.json/md`。正式数据中安全系统已进入 `environment-workbench`，并在 `node-details` 中以 `services[].securitySystems` 形式存在；本轮未改前端 UI，因此安全系统字段展示仍需后续 `Environment Mapping UI Data Field Alignment 1.0`。
- 2026-06-12 1.5 重跑与人工核对表：用户重新保存原始 Excel 368/369 行后，已再次从 `data/raw-samples/wiki sample.xlsx` 读取该 Sheet 并重建正式包。最新正式 `environment-workbench.json` hash 为 `5238862c2f917dacc74c618d6c101cd923f4288adc62a565659e1ecf828db4de`，最新正式 `environmentBasemap.node-details.json` hash 为 `012f4b555d464fb483ecd0269d526d3600168cf1c95592b97c2c2d963cbc3efc`；最新统计为 `objectService=696`、`serviceModule=613`、`serviceMeasure=123`、`moduleSystem=214`、`detailReadyNodes=91`、`missingDetailNodes=0`。临时核对表已改为 Excel 关系核对视图，列顺序为 `信息化环境 / 环境子类 / 信息化对象 / 作用域 / 安全技术服务 / 安全技术模块/安全技术措施 / 安全系统 / 核对提示 / 证据`；`作用域` 按对象上下文合并为一个单元格，模块/措施与安全系统按连续相同值做视觉合并。最新核对表仍显示 `duplicateServiceRowCount=128`、`scopeCompletenessIssueContextCount=32`、`shouldPauseUiAlignment=true`，需继续人工核对；专项检查确认 `传统数据中心||工作负载||物理主机操作系统` 不再存在重复服务，Excel 368/369 分别对应 `I-OS&T-AS.IA-03 操作系统资源授权` 和 `I-OS&T-AS.IA-04 操作系统凭证管理`，但该上下文仍被作用域反查提示缺少 `I-AP / I-NT / I-US`，需人工确认是否为源表作用域漏填或服务编号跨作用域。
- 2026-06-12 作用域反查补全：按用户要求，所有根据安全技术服务编号反查出的缺失作用域已在导入投影层补充，不修改原始 Excel。补全规则以 `信息化环境||环境子类||信息化对象` 为上下文，原始合并单元格作用域保留，缺失作用域标记为 `service_scope_reverse_check`；本次共补充 `58` 条 object-scope 关系，`objectScopeRelations` 从 `98` 增至 `156`，`scopeCompletenessIssueCount` 从 `32` 降为 `0`。最新正式 `environment-workbench.json` hash 为 `1e9e8d3e9270d03c407ff25f0f15ea40df85c8d3c0f168ec6ad64291ac5c77d4`，最新正式 `environmentBasemap.node-details.json` hash 为 `9620e8b3c70895a52126a99791b1c24712fee21d95b6f2ffc41117017b2f5e10`。临时核对表显示 `scopeCompletenessIssueContextCount=0`；`传统数据中心||工作负载||物理主机操作系统` 的合并作用域已包含 `I-OS 操作系统 / I-AP 软件应用 / I-NT 网络 / I-US 用户`。
- 2026-06-12 原始 Excel 作用域写回与核对视图清理：新增 `scripts/apply_scope_supplements_to_source_excel.py`，将补全作用域写回原始 `data/raw-samples/wiki sample.xlsx` 的 E 列合并单元格左上角，写回前备份到 `data/exports/worker-verify/source-excel-backup/wiki sample.before-scope-writeback.20260612-155248.xlsx`。本轮更新 `32` 个 E 列合并单元格、跳过 `0` 个，写回前后 E 列 merged ranges 完全一致，重新审计后 `scopeCompletenessIssueCount=0` 且 `scope` 字段 merged ranges 仍为 `66`。正式数据包重新替换：`environment-workbench.json` hash 为 `8e97701b522ddd2566b06a1c5aefad451380732efbbf6ee43416dfec485ac83e`，`environmentBasemap.node-details.json` hash 为 `22ec19eb377391d0e185627760c79b0cbd90e19623316b48ee369b4379e8d30c`。临时核对表已删除右侧行证据面板，仅保留表格内 `证据` 列，便于横向核对 Excel 关系。
- 2026-06-12 终端操作系统重复服务与模块关系重导入：用户修正原始 Excel 后重新审计，`objectServiceRelations` 从 `696/711`（上一轮/本轮）增至 `711`，`serviceModuleRelations` 从 `613/622` 增至 `622`，全局重复服务组从 `63` 降至 `49`，临时核对表重复服务行从 `128` 降至 `99`，高风险上下文从 `30` 降至 `23`。正式数据包重新替换：`environment-workbench.json` hash 为 `3f517e4e9c5a8998ea83ce328f1c4a8de79df0dc6df78e371fa053593e470abf`，`environmentBasemap.node-details.json` hash 为 `8c4c21cf7efa4472adfcf6d50465cbb53a6283d787aa0f83a04ab0c23171cc16`。专项检查显示 `园区网||PC终端||PC终端操作系统`、`分支机构||PC终端||PC终端操作系统`、`云数据中心||工作负载||物理主机操作系统`、`传统数据中心||工作负载||物理主机操作系统`、`园区网/分支机构||移动终端||移动终端操作系统` 已无重复服务；仍残留重复服务的操作系统上下文为 `云数据中心||工作负载||虚拟主机操作系统`、`远程办公接入||PC终端||PC终端操作系统`、`运维管理网络||PC终端||PC终端操作系统`、`工业控制网络||L3层||L3主机操作系统`。
- 2026-06-12 16:58 数据修正后再次重导入：用户继续修正原始 Excel 后重新审计，`objectServiceRelations=731`、`serviceModuleRelations=640`、`serviceMeasureRelations=123`、`moduleSystemRelations=214`、`scopeCompletenessIssueCount=0`。全局重复服务组从 `49` 降至 `29`，临时核对表重复服务行从 `99` 降至 `59`，高风险上下文从 `23` 降至 `15`。正式数据包重新替换：`environment-workbench.json` hash 为 `e7ca633b1ef64e7db9d6eda845518a3dd4d8d4bd3e9c8ce844efe119c8d25789`，`environmentBasemap.node-details.json` hash 为 `a062682b92228d0a623207dfdd0a1dd5c42cb4f98a83bbd188a1126972836a16`。专项检查显示操作系统类上下文中仅 `工业控制网络||L3层||L3主机操作系统` 仍有重复服务；此前残留的 `云数据中心||工作负载||虚拟主机操作系统`、`远程办公接入||PC终端||PC终端操作系统`、`运维管理网络||PC终端||PC终端操作系统` 已无重复服务。全表剩余重复主要集中在应用 / 数据库 / 大数据平台对象，例如 `传统数据中心||应用及数据||数据库`、`传统数据中心||应用及数据||应用前端`、`云数据中心||大数据平台/数据中台||数据分析层`、`数据服务层` 等。
- 2026-06-12 18:03 数据清理后再次重导入：检测到原始 Excel 已重新保存，重新审计后 `sheetMaxRow=1326`、`normalizedRowCount=772`、`objectScopeRelations=156`、`objectServiceRelations=739`、`serviceModuleRelations=640`、`serviceMeasureRelations=123`、`moduleSystemRelations=214`、`scopeCompletenessIssueCount=0`、`duplicateInformationObjectContextCount=0`、`sameNameDifferentContextCount=8`，全局重复服务上下文从 `29` 降至 `21`。replacement bundle 通过临时替换 HTTP 回归后正式替换：`environment-workbench.json` hash 为 `f1106b7ec96e3600776fc6f3a6eb7b1d1f7b5c6ccfac137d2a6fe6feb7e32a7d`，`environmentBasemap.node-details.json` hash 为 `dcc810a11f1f8dd47c341e17534fba4e997b91762867e8dd800c597396b64d99`，旧正式文件已备份到 `data/exports/worker-verify/environment-reimport-official-backup/`。临时核对表更新为 `67` 个上下文、`772` 行、`highRiskContextCount=8`、`mediumRiskContextCount=3`、`duplicateServiceRowCount=42`、`scopeCompletenessIssueContextCount=0`。剩余高风险对象为 `网络周界||企业内跨网边界||跨网边界`、`云数据中心||大数据平台/数据中台||数据分析层`、`云数据中心||大数据平台/数据中台||数据服务层`、`云数据中心||大数据平台/数据中台||数据管理层`、`传统数据中心||应用及数据||应用前端`、`传统数据中心||应用及数据||数据库`、`运维管理网络||网络||运维管理网络入口边界`、`工业控制网络||L3层||L3主机操作系统`。
- 2026-06-12 18:46 规则修订与正式包刷新：按用户确认修订人工核对规则，`安全技术措施` 不要求关联安全系统；仅当 `安全技术模块` 在原表全局 `moduleSystemRelations` 中曾有关联系统、但当前行安全系统为空时，才提示安全系统缺失风险。已修改 `scripts/build_environment_manual_review_checklist.py` 并重新生成核对表，`missing_security_system` / `missing_security_system_for_known_module` 风险项均为 0。18:46 原始 Excel 重新生成的 replacement bundle 已通过 HTTP 回归并正式替换，`environment-workbench.json` hash 更新为 `41aa2b91782fb250eb99effe0f41b4ed7d7f50479d910e3b205abf00b7245bbe`，`environmentBasemap.node-details.json` hash 保持 `dcc810a11f1f8dd47c341e17534fba4e997b91762867e8dd800c597396b64d99`。最新临时核对表仍为 `67` 个上下文、`772` 行、`highRiskContextCount=8`、`mediumRiskContextCount=3`、`duplicateServiceRowCount=42`、`scopeCompletenessIssueContextCount=0`；剩余高风险均来自重复安全技术服务行，不再来自措施缺少安全系统。
- 2026-06-12 `N/A（…）` 措施关系修订：用户抽查发现 `云数据中心||网络||云数据中心内部 虚拟网络` 中 `I-NT&T-AS.AD-01 网络平面及区域划分`、`I-NT&T-AS.AD-02 网络冗余` 在前端显示为 `/`，但原始 Excel G 列分别为 `N/A（网络基础架构安全设计）`、`N/A（网络高可用性设计）`。复查确认旧导入规则把 `N/A(...)` 归入 pending / 空关系，导致正式包缺少安全技术措施。已修订 `scripts/audit_scope_service_module_mapping.py`：`N/A（说明类安全设计）` 作为 `securityTechnicalMeasure` 导入，单独 `/` 仍不生成关系，措施仍允许不关联安全系统。重新审计后 `serviceMeasureRelations=180`、`pendingRelationCount=0`；正式 `environment-workbench.json` hash 为 `cdd9619a82fb2f92a3e03178cadeef54962cb626935a4f59a6aa9e362558bff8`，正式 `environmentBasemap.node-details.json` hash 为 `9079bd79c74d36b971c569a68b4048c06b52799ab0c85608c043ea51216d1d64`。抽样确认 Excel 行 60 / 61、340 / 341、455、728、755 均已形成 `has_measure` 关系，不再显示为 `/`。
- 2026-06-12 安全技术措施目录导入口径修订：用户已将原始 Excel 中 `N/A（…）` 形式的安全技术措施值清理为括号内措施名称。复查发现只改文本后，部分单元格仍保留旧说明类 / 灰色样式，旧脚本会把 `网络基础架构安全设计` 判为 pending，把 `应用系统高可用性设计` 判为 invalid。已修订 `scripts/audit_scope_service_module_mapping.py` 和 `src/sapd_wiki/exports.py`：模块优先按 `安全技术模块清单` 和模块样式识别；否则 G 列非空且安全系统列为空 / `N/A` 或旧说明样式时，按 `securityTechnicalMeasure` 导入，样式只作为证据，不再一票否决措施目录。源表扫描已确认 `remaining_na_wrapped_values=0`；重新审计后 `serviceMeasureRelations=180`、`pendingRelationCount=0`、`invalidRowCount=0`；正式 `environment-workbench.json` hash 为 `e89fc7c7b54744a2f04ab6219900e688ad861f9e2a8fdaf3b94528be24a142ba`，正式 `environmentBasemap.node-details.json` hash 为 `9079bd79c74d36b971c569a68b4048c06b52799ab0c85608c043ea51216d1d64`。
- 2026-06-12 G101 源数据修正同步：确认原始 Excel `G101` 已为 `云平台相关高可用设计如快照`，不再是 `N/A(...)` 包裹文本，且全表 `N/A(...)` 残留为 `0`。重新审计和替换正式包后，`serviceMeasureRelations=180`、`pendingRelationCount=0`、`invalidRowCount=0`；正式 `environment-workbench.json` hash 为 `3b76a20908962018a14a990d5c6599bf5a28bd85ecdf57a4ee397f485bfbf3cb`，正式 `environmentBasemap.node-details.json` hash 为 `9079bd79c74d36b971c569a68b4048c06b52799ab0c85608c043ea51216d1d64`。人工核对表中 `excelRow=101` 已显示 `moduleOrMeasureRaw=云平台相关高可用设计如快照`、`moduleOrMeasureKind=measure`、`risk=low`。
- 2026-06-12 同名不同上下文规则固化：用户确认同名信息化对象出现在不同 `信息化环境 / 环境子类` 下是允许的业务规则，不作为数据风险。人工核对表仍保留 `sameNameDifferentContext=是/否` 信息字段，但不再输出“同名信息化对象跨多个上下文”核对提示，也不再生成 `same_name_different_context` issue type。重新生成核对表后 `sameNameYesRows=233`、`promptOrIssueRows=0`；剩余 `highRiskContextCount=8` 继续来自重复安全技术服务行等真实待核问题。
- 2026-06-12 跨表一致性审计 1.0：用户修订确认同一对象上下文下同一安全技术服务可以合法映射多个模块 / 措施 / 安全系统，因此废止 `objectContextKey + securityTechnicalService` 直接判重。已将重复错误键改为 `objectContextKey + securityTechnicalService + childType + securityTechnologyModuleOrMeasure + securitySystem`；`repeatedServiceWithDifferentChildren` 仅作为信息提示。新增 `scripts/audit_environment_module_catalog_consistency.py` 读取原始 `安全技术模块清单`，基于 `安全系统 -> 安全技术模块 -> 安全技术服务` 目录基准输出跨表一致性报告。最新审计读取 `catalogRelations=390`，结果为 `duplicateExactServiceChildRelationCount=1`、`repeatedServiceWithDifferentChildrenCount=79`、`moduleServiceMismatch=75`、`systemModuleMismatch=132`、`serviceModuleCoverageGapCandidate=552`、`moduleServiceCoverageGapCandidate=77`、`securitySystemCoverageOrMismatch=127`、`moduleMeasureClassificationIssue=3`、`scopeCompletenessIssues=0`、`hasIusScopeGap=false`。剩余唯一完整重复关系样例为 `工业控制网络||L3层||L3主机操作系统` 中 `I-OS&T-AS.AM-01 -> 主机安全管理 -> 主机安全防护`（Excel 行 729 / 740）。本轮仅生成审计和临时核对表，不替换正式数据包，不修改正式业务 UI。
- 2026-06-12 最新原始 Excel 与 SVG 同步：用户已从原始 Sheet 删除唯一完整重复关系，并再次更新 `data/raw-samples/drawio sample-信息化环境及对象底图.drawio.svg`，修复名称与表格不匹配并补充 `数据共享层`。重新审计结果为 `sheetMaxRow=1325`、`mergedRangeCount=417`、`normalizedRowCount=771`、`objectScope=156`、`objectService=739`、`serviceModule=640`、`serviceMeasure=180`、`moduleSystem=214`、`duplicateExactServiceChildRelationCount=0`、`duplicateServicesInObjectContextCount=0`、`scopeCompletenessIssueCount=0`。正式 `environment-workbench.json` 已替换为 hash `41422f6b418fe1ffe59d03cc608c99f6e699cda7d6090099dd47c93429043142`，正式 `environmentBasemap.node-details.json` 已替换为 hash `3a7573a5bcd7c336d265d807eb4efcc950b60afdad9e243daa44d4518111b97b`；replacement bundle 中 node-details 摘要计数已修正为按 `objectId/objectName` 详情实体去重。剩余待核不再是完整重复关系，而是跨表目录一致性：`moduleServiceMismatch=75`、`systemModuleMismatch=132`、`serviceModuleCoverageGapCandidate=552`、`moduleServiceCoverageGapCandidate=77`、`securitySystemCoverageOrMismatch=127`、`moduleMeasureClassificationIssue=3`。
- 2026-06-12 移动终端软件应用名称同步：用户已将 Draw.io 和原始表中的 `移动应用` 配套调整为 `移动终端软件应用`。复查原始 Excel 行 `497 / 564 / 640` 分别覆盖 `园区网 / 移动终端 / 移动终端软件应用`、`分支机构 / 移动终端 / 移动终端软件应用`、`客户访问接入 / 移动终端 / 移动终端软件应用`。已删除底图绑定覆盖中的过期 `ignoredMxIds`，重新导入并正式替换环境事实源；当前 `environment-workbench.json` hash 为 `a51a7f099eb577572ad14643621b9a023c8217af2d06ffbb7d8bd0c87df54c23`，`environmentBasemap.node-details.json` hash 为 `e16cfa8504a569f2d8d02ac40c8a7e23cf4397041921c02fef3e4256bd97cfcf`。三处 `移动终端软件应用` 均按 contextKey 独立绑定，`客户访问接入||移动终端||移动终端软件应用` 不再 ignored；详情投影已补齐 `directSecuritySystemCount/securitySystemCount` 和 `directScopeGroups[].securitySystems`。
- 验证结果：2026-06-12 `python3 -m py_compile scripts/audit_scope_service_module_mapping.py scripts/build_environment_workbench_reimport_candidate.py scripts/verify_environment_reimport_candidate_downstream.py scripts/build_environment_reimport_replacement_bundle.py scripts/verify_environment_reimport_replacement_bundle.py` 通过；`python3 scripts/audit_scope_service_module_mapping.py`、`python3 scripts/build_environment_workbench_reimport_candidate.py`、`python3 scripts/verify_environment_reimport_candidate_downstream.py`、`python3 scripts/build_environment_reimport_replacement_bundle.py` 均通过；`python3 scripts/verify_environment_reimport_replacement_bundle.py` 通过本地 HTTP smoke 且未启动系统 Chrome；`python3 scripts/dev_server_guard.py --status`、`node scripts/frontend_smoke_check.mjs --page environment --route /environment-mapping --url http://127.0.0.1:5173`、`python3 scripts/check_github_data_boundary.py`、`git diff --check` 均通过。系统 Chrome / 浏览器视觉回归未执行。2026-06-12 追加 `N/A（…）` 措施修订后，`python3 -m py_compile scripts/audit_scope_service_module_mapping.py scripts/build_environment_manual_review_checklist.py`、`python3 scripts/audit_scope_service_module_mapping.py`、`python3 scripts/build_environment_workbench_reimport_candidate.py`、`python3 scripts/verify_environment_reimport_candidate_downstream.py`、`python3 scripts/build_environment_reimport_replacement_bundle.py`、`python3 scripts/build_environment_manual_review_checklist.py`、`python3 scripts/dev_server_guard.py --status`、环境页 HTTP smoke、`python3 scripts/check_github_data_boundary.py`、`git diff --check` 均通过。

## OI-138：关注点关系图谱标签与节点 / 连线碰撞

- 状态：部分修复 / 待人工验收
- 类型：前端 / 图谱布局 / 设计
- 对象或页面：`安全能力映射` 关注点关系图谱，路由 `/capability-mapping`，组件 `LocalRelationNetworkGraph`。
- 现象：用户截图指出关注点图谱中部分圆点、标签和连线仍存在碰撞，例如底部节点标签贴近或压到纵向连线，局部空间没有被充分利用。
- 影响：图谱阅读时会误解节点归属或连线路径，降低关系图谱作为“关系检查视图”的可读性。
- 当前处理：2026-06-08 已按“小步修正、不大调”的原则，仅在 `LocalRelationNetworkGraph` 中新增标签候选位选择器。每个非中心节点标签会在右、左、上、下和四个斜向位置中选择碰撞成本最低的位置，避让其它节点、已放置标签和主要连线；节点布局、业务关系、颜色语义和整体图谱结构不变。Canvas 新增 `data-layout-label-overlaps` 指标，便于后续回归。用户反馈首次修复后页面“没有变化”，复查发现动态加载的 `LocalRelationNetworkGraph.js` 版本号未递增，浏览器可能仍使用旧组件；已同步递增 `app.js` 和图谱组件动态加载版本，并提高曲线路径避让权重、让外圈叶子节点优先把标签放到上下外侧。随后用户继续通过 Product Design 复核指出截图中仍能看到曲线贴近 / 穿过非目标节点附近；已追加轻量边线避障，不移动节点，只在普通曲线靠近无关圆点时调整控制点向外侧绕开。2026-06-08 曾尝试为具体关注点图谱新增结构化径向扇区布局，但用户明确反馈“不是扇区，而是星形分布，分散的，不碰撞”；现已撤回 `focus_relation_overview` 扇区策略，恢复原先星形 / 力导向分散效果，只保留并收紧标签候选位和边线轻量避让，避免因硬分区造成节点集中。2026-06-08 用户进一步指出“节点碰撞=0”不能代表最深层连线不交叉；已将图谱质量指标扩展为四项：`data-layout-overlaps`、`data-layout-label-overlaps`、`data-layout-edge-crossings`、`data-layout-edge-node-intrusions`，分别覆盖节点、标签、边线交叉和边线侵入非端点节点安全区。2026-06-08 按用户明确复现对象 `T-AS.AD-01` 重新测试后确认，默认图谱测试无效；真实对象初始为 `edgeCrossings=5`、`edgeNodeIntrusions=2`、`labelOverlaps=1`。当前已针对技术视角分支做局部解缠：只处理 `view_technical` 下的作用域 / 服务 / 模块子树，按作用域语义让数据、网络、物理、软件、主机、硬件分散占位，用户截图中的 `软件应用` 与 `数据与信息 -> 数据分库分表 -> 数据安全存储` 局部交叉已消除；全图仍有管理视角 2 处交叉、标准分支若干侵入和 1 处标签重叠，后续需继续局部治理，不能标记为全量完成。
- 需要确认：用户在 `http://127.0.0.1:5173/capability-mapping` 人工检查截图中类似位置是否仍有不可接受的标签压线、压节点或遮挡；如仍存在具体点位，继续只对标签避让规则做局部修正，不做大范围重排。
- 修复说明：修改 `frontend/capability-browser/models/relationGraphModel.js`，删除具体关注点图谱的 `strategy: "focus_relation_overview"`；修改 `frontend/capability-browser/components/LocalRelationNetworkGraph.js`，移除扇区树状布局函数，保留标签宽度估算、标签 box、圆点 / 标签 / 曲线路径碰撞评分和布局指标，并将边线避障收紧为轻量偏移，同时新增边线交叉与边线侵入非端点节点安全区统计，普通业务线改为星形径向出线路径，并对关注点图谱的技术视角子树增加局部分支解缠；修改 `frontend/capability-browser/app.js` 递增模型与组件动态加载版本；修改 `frontend/capability-browser/index.html` 递增 `app.js` 缓存版本。未修改数据、ETL、数据库、用户库、图谱业务数据或 CSS 视觉基线。
- 验证结果：2026-06-08 `node --check frontend/capability-browser/components/LocalRelationNetworkGraph.js`、`node --check frontend/capability-browser/models/relationGraphModel.js`、`node --check frontend/capability-browser/app.js`、`python3 scripts/dev_server_guard.py --status`、`node scripts/frontend_smoke_check.mjs --page capability --route /capability-mapping --url http://127.0.0.1:5173` 和定向 `git diff --check` 均通过；内置 Browser 在 `/capability-mapping` 检查确认实际加载 `app.js?v=capability-graph-focus-untangle-20260608-3` 与对应组件版本，旧 `focus_relation_overview` 未加载；真实复现对象 `T-AS.AD-01` 当前为 `data-layout-overlaps=0`、`data-layout-label-overlaps=1`、`data-layout-edge-crossings=2`、`data-layout-edge-node-intrusions=5`、`data-layout-min-gap=15`，其中剩余交叉位于管理视角分支，不在用户截图指出的技术视角数据分支局部；未启动系统 Chrome，内置 Browser 截图接口有时超时。

## OI-137：信息化环境首页需导入 draw.io 第三页实例底图

- 状态：已修复 / 待人工验收
- 类型：前端 / 设计 / Draw.io 导入
- 对象或页面：`信息化环境维度`，路由 `/environment-mapping`，组件 `EnvironmentLocalRelationMap`。
- 现象：信息化环境安全能力映射首页原先是代码根据环境数据生成的概念性拓扑，不是用户指定的 draw.io 第三张“信息化环境及对象底图”；用户明确要求该图是实例化图，不能套用 `SAPD 元素图例` 的样例文字。
- 影响：如果继续使用代码生成拓扑或 SAPD 图标示例文字，页面首页会偏离用户的实例化底图意图，无法作为信息化环境和对象映射的业务底图。
- 当前处理：2026-06-07 已确认本机没有可调用的 `drawio` / `diagrams.net` CLI，无法使用官方导出器；按用户最新要求，当前不是纯 SVG Viewer，而是直接解析 `.drawio` 第三页 `mxGraphModel`，以原始 `mxCell` / `mxGeometry` / `style` / `edge` 信息为依据生成语义 HTML 底图。每个 Draw.io vertex 转换为一个绝对定位 HTML 节点并保留 `data-mx-id`、`data-mx-parent`、`data-label`、`data-object-type`、`data-object-code`、`data-bind-status`、`data-source-page` 等属性；每条 Draw.io edge 转换为 SVG overlay 连线；另生成可维护的 `environmentBasemap.semantic.json`，供后续绑定信息化对象、作用域、安全技术服务和安全技术模块 / 措施。2026-06-08 已完成 Environment Basemap Binding 1.0：新增可审计绑定脚本和 override 文件，基于精确名称、规范化名称、别名、父级容器上下文和手工 override 入口；按用户确认将 `OT`、`数据中心机房`、`IT`、`客户`、`互联网`、`外联网` 标记为 `ignored`，不绑定保护对象数据；`工厂` 绑定 `工业控制网络`。当前 97 个节点绑定结果为 `bound=91`、`candidate=0`、`unbound=0`、`ignored=6`。同日按用户给出的 ArchiMate 内容面板基准继续收紧页面结构：`环境底图 / 归纳表格` tab 保持在主标题右侧，删除全局 `导出数据` / `编辑映射` 假按钮，删除底部 `点击底图中的业务对象查看详情。` 提示栏，底图二级标题栏固定为左标题、右侧基准按钮区，仅保留真实可用的 `全页面显示`，不再展示标题区统计胶囊，也不再渲染 `适应 / − / +` 可见缩放按钮组；标题语义与字体改为和 ArchiMate 基准相同的 `h2` + 同一 CSS 标题规则。2026-06-08 执行 `Environment Basemap Draw.io Style Fidelity & Canvas Bounds Fix 1.0`：生成器按 Draw.io `style` 字段输出视觉 `drawioType / iconType`，当前识别 `grouping`、`location`、`communication_network`、`facility`、`application_component`、`node`、`device`、`system_software`、`data_object`、`actor`；语义节点保留 `originalGeometry` 并新增 `normalizedGeometry`，有效内容边界为 `contentBounds={minX:160,minY:150,maxX:3150,maxY:2180,padding:48}`，canvas 收敛为 `3086×2126`；生成底图自身背景改为透明，不再叠加 Draw.io 白板网格；没有 source / target 的自由箭头按 mxPoint 绘制，带 source / target 但显式端点漂出对象过远的边线回贴到对象之间。2026-06-11 已将 Lab 验证后的 Viewer 正式独立为 `EnvironmentBasemapViewer.js`，正式 `环境底图` Tab 继续复用该共享 Viewer；同时关闭 `/environment-basemap-lab` 测试页路由、导航入口、workspace 容器和 Lab render 分支。2026-06-12 用户更新 `data/raw-samples/drawio sample-信息化环境及对象底图.drawio.svg`，将网络周界中的 `外联网边界` 改为与原始数据一致的 `外联网出口边界`；已同步到正式 `environmentBasemap.svg`，并将 `mxId=7M0SGe99AU4GMtvV9rx2-53` 人工绑定到 `网络周界||外联网边界||外联网出口边界`。同时修订绑定脚本以忽略 `reimport:*` 镜像候选并从 `contextKey` 补上下文，修订 node-details 生成器优先使用正式对象 `scope_mappings` 和 `contextKey`；重算后仍为 `bound=91`、`candidate=0`、`ignored=6`、`detailReadyNodes=91`、`missingDetailNodes=0`，抽样 `外联网出口边界` 为 `network_boundary`，路径 `网络周界 / 外联网边界 / 外联网出口边界`，直接服务 `5`、模块 `5`。
- 需要确认：用户在固定入口 `http://127.0.0.1:5173/environment-mapping` 人工验收语义 HTML 底图与 draw.io 打开效果的整体一致性，以及节点点击 / 缩放 / 平移是否满足后续数据绑定操作。`数据中心机房` 是地点容器，包含 `传统数据中心` / `云数据中心` 但自身不绑定数据；`互联网` / `外联网` 是互联网边界或外联网边界外联的信息化环境，不再绑定到 `互联网边界` / `外联网边界`；`园区网出口边界` 已按原始 draw.io 标签识别。`网络边界`、`内部网络`、`外部网络` 不是当前图里的独立语义节点，而是由具体边界 / 网络节点承载，例如 `互联网入口边界`、`园区网内部网络`、`L2内部网络` 等，因此本轮不作为缺口项。由于当前为直接 mxGraphModel 解析而非官方 draw.io CLI，复杂 draw.io 私有图标、部分文本细节和 `edgeStyle` 自动路由为近似映射；若后续要求官方像素级一致，优先恢复官方 draw.io / diagrams.net 导出器链路。
- 修复说明：重写 `scripts/generate_environment_basemap_from_drawio.mjs`，生成 `frontend/capability-browser/generated/environmentBasemap.html`、`environmentBasemap.css`、`environmentBasemap.generated.js` 和 `environmentBasemap.semantic.json`；新增 `scripts/bind_environment_basemap_semantics.mjs`、`environmentBasemap.binding-overrides.json`、`environmentBasemap.binding-report.json` 和 `environmentBasemap.binding-report.md`；修改 `frontend/capability-browser/components/EnvironmentLocalRelationMap.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/app.js` 和 `frontend/capability-browser/index.html`，接入页面内语义 HTML Viewer 和绑定状态展示。2026-06-08 追加修改 `frontend/capability-browser/components/AppShell.js`、`frontend/capability-browser/components/EnvironmentLocalRelationMap.js` 和 `frontend/capability-browser/styles.css`，对齐 ArchiMate 页标题 / tab / 面板样式并移除未定义按钮；同日继续修改 `scripts/generate_environment_basemap_from_drawio.mjs` 与生成的底图 HTML / CSS / semantic / manifest，修正边线端点和角标渲染。2026-06-11 新增 `frontend/capability-browser/components/EnvironmentBasemapViewer.js`，删除旧 `EnvironmentBasemapLab.js`，并从 `AppShell.js`、`app.js`、`index.html` 和 Lab workspace 样式中移除测试页入口。不改 ETL、数据库、用户库、`environment-workbench.json`、SVG、semantic、node-details 或 `SAPD 元素图例` registry。
- 验证结果：2026-06-07 `node scripts/generate_environment_basemap_from_drawio.mjs` 输出 `pageIndex=3`、`cells=170`、`nodes=97`、`edges=73`；semantic JSON 中 `nodes=97`、`edges=73`，与源 mxCell vertex / edge 计数一致；`node --check scripts/generate_environment_basemap_from_drawio.mjs`、`node --check frontend/capability-browser/generated/environmentBasemap.generated.js`、`node --check frontend/capability-browser/components/EnvironmentLocalRelationMap.js`、`node --check frontend/capability-browser/app.js` 通过；`python3 scripts/dev_server_guard.py --status` 固定 `5173` 服务健康；`node scripts/frontend_smoke_check.mjs --page environment --route /environment-mapping --url http://127.0.0.1:5173` 轻量 HTTP smoke 通过，未启动系统 Chrome；源码断言确认 HTML 中存在 97 个 `.basemap-node`、73 条 `.basemap-edge`，节点包含 `data-mx-id`、`data-object-type`、`data-bind-status`；`python3 scripts/check_github_data_boundary.py` 与 `git diff --check` 通过。2026-06-08 绑定验证补充通过：`node scripts/bind_environment_basemap_semantics.mjs` 生成报告，断言 `invalidBound=0`、`disallowedDuplicateBoundObjectIds=0`，提升权限只读 fetch 确认 `5173` 可访问 HTML / CSS / semantic JSON / binding report JSON / binding report MD。2026-06-08 页面样式追加验证通过：相关 `node --check`、环境页 HTTP smoke、`python3 scripts/check_github_data_boundary.py`、定向 `git diff --check` 和源码结构断言均通过；结构断言确认 `导出数据` / `编辑映射` 文本已从 `AppShell` 删除、`environmentHeaderTabs` 仍存在、底图标题区已移除统计胶囊、底部详情提示 DOM 已移除、可见标题栏操作只剩 `全页面显示`，旧 `适应 / − / +` 缩放工具组已不再渲染，环境底图标题已改为复用 ArchiMate 标题字体规则。2026-06-08 `Style Fidelity & Canvas Bounds Fix 1.0` 验证通过：重新生成和绑定后仍为 `nodes=97`、`edges=73`、`bound=91`、`ignored=6`；断言确认生成底图 `.environment-basemap` 背景为 `transparent`、非 actor / unknown 节点角标缺失数为 0、`originalGeometry / normalizedGeometry / drawioType / iconType / fontSize` 缺失数为 0、红框顶部边线回到节点区域 `y≈183`、右侧边线回到节点区域 `x=2758→2873`；资源级 5173 fetch 确认页面实际服务的 HTML 包含 `data-drawio-type` 和 `basemap-corner-icon`，semantic JSON 包含 `contentBounds` 和 `styleSummary`。2026-06-11 Lab Cleanup 验证通过：`node --check frontend/capability-browser/components/EnvironmentBasemapViewer.js`、`node --check frontend/capability-browser/components/EnvironmentLocalRelationMap.js`、`node --check frontend/capability-browser/components/AppShell.js`、`node --check frontend/capability-browser/app.js`、`python3 scripts/dev_server_guard.py --status`、`node scripts/frontend_smoke_check.mjs --page environment --route /environment-mapping --url http://127.0.0.1:5173`、`python3 scripts/check_github_data_boundary.py`、`git diff --check` 均通过；源码断言确认 `index.html`、`app.js`、`AppShell.js` 不再包含 `/environment-basemap-lab`、`EnvironmentBasemapLab`、`environmentBasemapLabWorkspace`、`environmentBasemapLabMount`，且 `index.html` 已加载 `EnvironmentBasemapViewer.js`。未执行系统 Chrome / Playwright 视觉回归。
- 2026-06-12 最新 SVG 覆盖验证：底图生成脚本已改为从 `data/raw-samples/drawio sample-信息化环境及对象底图.drawio.svg` 内嵌 `mxfile` 读取 Draw.io XML，避免继续使用旧 `.drawio` 的旧 mxId。重新生成后 `cells=172`、`nodes=98`、`edges=74`；`数据共享层` 新节点 `mxId=AkYI7P9UyD1rbXxni8A_-171` 已绑定到 `云数据中心||大数据平台/数据中台||数据共享层`，`外联网出口边界` 新节点 `mxId=AkYI7P9UyD1rbXxni8A_-66` 已绑定到 `网络周界||外联网边界||外联网出口边界`。重新绑定后 `bound=91`、`candidate=0`、`unbound=0`、`ignored=7`、`missingExpectedObjects=0`；重算 node-details 后 `detailReadyNodes=91`、`missingDetailNodes=0`。
- 2026-06-12 移动终端软件应用覆盖验证：用户已将 Draw.io 中客户访问接入区域的 `移动应用` 配套改为 `移动终端软件应用`。删除过期 `ignoredMxIds` 后重新绑定，当前底图为 `nodes=98`、`edges=74`、`bound=92`、`candidate=0`、`unbound=0`、`ignored=6`；`mxId=AkYI7P9UyD1rbXxni8A_-151` 已从 ignored 转为 `bound`，绑定对象为 `客户访问接入||移动终端||移动终端软件应用`，与园区网、分支机构下的同名对象按 contextKey 分别独立建模。
- 2026-06-11 Detail Semantics 3.0：已重写 `environmentBasemap.node-details.json` 的详情投影语义，新增 `contextPath` / `contextPathText`、`directScopeGroups`、`inheritedScopeGroups` 和 direct / inherited 汇总计数；`PC终端` 等环境子类改为 `environment_object_category`，`PC终端设备` / `PC终端操作系统` / `PC终端软件应用` 保持具体对象或内部元素语义，边界类节点单独走 `network_boundary` / `security_scope`，`互联网` / `外联网` 保持外联环境说明且不绑定边界服务。服务聚合口径改为当前 `mxId` 所在上下文的直接作用域组，不再按 label 全局合并同名对象，也不把上级对象服务无差别复制给子元素；继承口径单独保留在 `inheritedScopeGroups` 并由 Viewer 标注来源。Viewer 浮窗按 `detailType` 使用不同模板，服务 / 模块 / 措施按作用域完整展示，取消 `+N` 简略隐藏，内容过多时在浮窗内部滚动。
- 2026-06-11 Detail Semantics 3.0 验证：`node scripts/generate_environment_basemap_node_details.mjs` 输出 `detailReadyNodes=91`、`missingDetailNodes=0`，`detailTypeDistribution={environment:10,environment_segment:3,environment_object_category:12,network_boundary:21,actor:4,information_object:41,ignored:6}`；抽样确认 `PC终端` 为 `environment_object_category`，路径含 `办公环境 / 园区 / PC终端`、`接入环境 / 远程办公接入 / PC终端`；`PC终端设备` 为 `information_object / 设备`；`PC终端软件应用` 为 `information_object / 软件应用`，direct services 为 7、inherited services 为 0，正式页面点击不再出现 `+28`；`远程办公接入` 为 `environment`，`园区出口边界` 为 `network_boundary`，`互联网` / `外联网` 为外联环境说明。通过 `node --check scripts/generate_environment_basemap_node_details.mjs`、`node --check frontend/capability-browser/components/EnvironmentBasemapViewer.js`、`node --check frontend/capability-browser/app.js`、环境页 HTTP smoke、`python3 scripts/check_github_data_boundary.py`、`git diff --check`；并用 Codex in-app Browser 在正式 `/environment-mapping` 页点击样例节点确认浮窗模板和无 `+N`。未启动系统 Chrome。
- 2026-06-11 Detail Template & Scroll Fix 3.1：进一步收紧详情浮窗字段边界，停止生成和展示 `接入环境`、`办公环境`、`数据中心环境`、`工业环境`、`外联环境` 等推断环境大类；`contextPath` 改为仅由真实业务层级组成：`信息化环境 / 环境子类 / 信息化对象 / 内部元素`，例如 `远程办公接入`、`园区 / PC终端`、`园区 / PC终端 / PC终端软件应用`、`数据中心机房 / 运维管理终端 / PC终端设备`。Viewer 头部不再重复展示完整上下文路径，各 `detailType` 改用固定字段模板：环境只展示环境子类、下属对象数量、作用域数量和服务数量；环境子类 / 对象类别展示所属信息化环境、下属对象、作用域和服务摘要；具体对象 / 边界才展开作用域、服务、模块、措施；ignored / actor 只展示说明且不展示保护对象清单。服务、模块、措施、作用域和对象胶囊复用全局 `relation-chip` 语义样式；浮窗滚轮事件已在 popover 内 `stopPropagation`，不再冒泡触发画布缩放。
- 2026-06-11 Detail Template & Scroll Fix 3.1 验证：`node scripts/generate_environment_basemap_node_details.mjs` 仍为 `detailReadyNodes=91`、`missingDetailNodes=0`、`detailTypeDistribution={environment:10,environment_segment:3,environment_object_category:12,network_boundary:21,actor:4,information_object:41,ignored:6}`；断言确认推断大类在 `contextPath` 中出现次数为 0，`PC终端=environment_object_category`，`PC终端设备=information_object / 设备`，`远程办公接入=environment`，`PC终端软件应用` 无 `+N` 截断逻辑；`node --check`、正式环境页 HTTP smoke、`python3 scripts/check_github_data_boundary.py` 和 `git diff --check` 通过。未启动系统 Chrome。
- 2026-06-11 bound actor 文案修正：确认 `办公人员` 是已绑定节点，`semantic` 中为 `bindStatus=bound`、`objectType=information_object`、`bindingReason=name_match`，`node-details` 中为 `detailType=actor`；Viewer 已将 bound actor 浮窗从 `角色 / 不绑定保护对象数据` 改为 `绑定数据`，说明为“已绑定信息化对象数据；因其为人员角色不展开服务 / 模块 / 措施清单”，并展示所属信息化环境 / 环境子类。`客户` 等 `ignored_by_override` 节点仍保留 ignored 模板。
- 2026-06-11 人员节点分类修正：进一步确认底图里 actor 图形共有 5 个，其中 `办公人员×3`、`运维管理人员×1` 均已绑定到 `information_object`，`客户×1` 为 `ignored_by_override`；已调整 `scripts/generate_environment_basemap_node_details.mjs`，使已绑定到 `information_object` 的 actor 图形归类为 `information_object / 人员`，走信息化对象模板展示作用域、服务、模块、措施；`客户` 保持 ignored 模板。重新生成后 `information_object=45`、`ignored=6`、`actorNodes=0`、`missingDetailNodes=0`。
- 2026-06-11 归纳表格字段口径修正：左侧环境树层级标识由前端自造 `E0 / E1 / E2` 改为业务标签 `环境 / 子类 / 对象`；归纳表格中信息化对象的作用域改为该对象的唯一作用域集合，不再按每个作用域重复展开；技术链路行粒度改为按安全技术模块 / 措施聚合关联服务，以体现 `安全技术服务 : 安全技术模块 = N : 1` 的关系。待后续业务确认：`作用域-安全技术服务-安全技术模块映射` 导入时 `_security_module_titles_from_catalog_alias()` 会把一个模块单元格按模块清单别名展开为多个安全技术模块，是否符合原始表格口径需单独数据治理确认。
- 2026-06-11 环境映射行级投影修正：确认 `environment-workbench.json` 曾把全局 `安全技术模块清单` 中的 `服务 -> 模块` 目录关系复制到环境对象，导致 `云数据中心出口边界` 等对象的模块 / 措施远大于原始表格；已改为环境页只使用 `作用域-安全技术服务-安全技术模块映射` 的行级关系，并在导出中新增 `environment_scope_tree` 作为归纳表事实源。parser 同步修复同一信息化对象内服务列空白时承接上一条服务的问题，补回 `文件检测沙箱` 这类原始表格承接行；归纳表展示列收敛为 `作用域 / 安全技术服务 / 安全技术模块/措施 / 安全系统`，安全系统保留在对应行。

## OI-038：Gartner 与安全职能候选映射需后续人工校对

- 状态：待确认
- 类型：数据 / 需求
- 对象或页面：岗位参考页面，`Gartner 工作岗位参考` 页签
- 现象：Sheet Review 2.1 / 2.2 已生成 28 条 Gartner 岗位参考到安全职能的候选映射，其中 20 条候选范围偏宽；2026-06-01 复查发现页面数据包未带入候选映射，`Gartner 工作岗位参考` 页签未显示映射数据。
- 影响：当前候选映射可先用于页面格式和关系展示落地，但不能视为最终业务确认结果。
- 当前处理：用户确认 Gartner 映射先按当前候选结果执行，页面格式先做好；2026-06-01 已把 `sheet-review-2-2-gartner-to-work-function-candidates.csv` 接入 `maintenance-knowledge.json` 导出，并在页面表格 / 详情显示候选安全职能、映射状态和匹配依据；后续单独验证校对。
- 需要确认：后续由用户逐条检查 `data/exports/worker-verify/sheet-review-2-2-gartner-to-work-function-candidates.csv`，确认哪些候选接受、删除或调整。
- 修复说明：页面显示缺口已修复；候选映射继续作为 `待复核` 数据保留，不作为最终正式关系。
- 验证结果：2026-06-01 重新导出 `maintenance-knowledge.json` 后，`gartner_roles=28`，其中 28 条均包含 `candidate_work_functions`；组件渲染断言确认 `Gartner 工作岗位参考` 表格包含“候选安全职能”列，示例“首席信息安全官（CISO）”显示 `2 安全负责职能`、`10 安全管理职能`、`27 规划计划管理职能`。
## OI-128：USER-WRITE-UI-1：批注 / 工作台用户写入入口

- 状态：部分完成
- 类型：前端 / 用户数据 / Delivery Bundle
- 对象或页面：ZIP alpha 桌面包、`sapd_wiki_user.sqlite3`、安全能力映射 / 知识库字典 / 标准框架 / 安全指南、右侧浮层批注抽屉与工作台。
- 现象：ZIP alpha 后端已具备 `user_favorites` 写入 API 和 user DB 自动创建能力；`OI-128A / OI-128B` 已验证页面可写入用户库，但当前横向 `加入关注清单 / 收藏备注` 条语义较弱，不适合作为长期用户工作入口。
- 影响：当前 Windows 包只能验收解压启动、页面访问、base 数据读取、user DB 自动创建、日志和诊断包；页面级用户写入能力不能作为本轮验收项。
- 当前处理：2026-06-03 已新增正式设计文档 `docs/06-implementation/user-workspace-v1-to-v4-design.md`，固定 V1A 收藏 / 轻备注、V1B 备注 / 标签、V2 我的工作区、V3 新增 / 复制编辑、V4 导出路线。`OI-128A` 已先在安全能力映射页对象详情区实现关注清单 / 收藏备注入口，并补齐开发 API 与 ZIP runtime 的收藏保存 / 撤销路径；`OI-128B` 已把同一用户动作组件复用到安全知识、标准 / 框架和安全指南页面。2026-06-04 已新增 `docs/06-implementation/workspace-annotation-and-capability-remix-design.md`，固定后续下线横向收藏条，改为右侧上浮批注抽屉和工作台方向；同日 `OI-128C` 已实现右侧浮层批注抽屉第一版。2026-06-04 用户截图复核发现第一版存在列表冗余、不可编辑、切页不收起、上下文重复、当前页计数错误、搜索框异常、草稿跨页残留和行 / 字段锚点缺失等问题；`OI-128C` 修正版已继续收敛右侧抽屉 UX / 逻辑、Apple shell 组件基线和 Office Word 式批注栏设计，新增精确上下文锚点定位、固定宽度抽屉、长标题截断悬停展示和原页面纯高亮标记。2026-06-05 进一步确认 LC-AP / LC-DT 页面缺少统一值级锚点，已把生命周期字段、生命周期 chip、数据场景、策略文本和参考数据接入 `data-annotation-value` / `data-copy-text` 契约；共享 `display.relationChip` 也默认输出同一锚点属性。2026-06-05 追加全工程批注契约治理：普通 `td` 表格单元格增加值级兜底，知识库字典、标准 / 框架折叠目录定位可自动展开父级分组，手写关系 chip 和详情字段统一接入 `display.annotationValueAttrs`，`scripts/audit_user_annotation_contract.mjs` 升级为跨页面动态渲染审计，防止值批注再次退化为行批注。2026-06-05 用户再次截图验证发现：`/standards/nist-csf-2` 保存后无可见常驻高亮、`/development-security` 高亮样式偏离约定基准、`/data-security` / 模块值定位后高亮不可见或回退；已新增 `docs/06-implementation/global-annotation-requirements-and-regression-matrix.md` 作为全局批注需求与页面级验收矩阵。2026-06-05 继续按用户“批注是叠加层”判断修复 overlay 架构：知识库表格行渲染后统一挂载稳定 `data-annotation-target-ref`，批注标记 / 定位优先使用稳定目标，再退回旧 `field_value` / `table_row` 兼容锚点；行级和单元格级高亮穿透到 `td` / `th`，避免被标准页、CSF 行、生命周期矩阵等表格背景盖住；资源版本更新到 `annotation-global-20260605-2`。2026-06-05 最新严格回归已收口：当前用户库 24 条保存批注真实 Chrome 严格回归 `24/24 pass`，覆盖值级 / 行级粒度、点击后常态保留、无批注误高亮、模块 / 措施 chip 定位和多页面上下文恢复。2026-06-05 二次人工抽查问题已修复：安全技术措施 / 模块 / 服务常态与定位高亮、点击页面后常态保留、非首屏定位抽屉滚动保持、抽屉视口完整性均纳入真实 Chrome 严格回归并通过；下一步只做最终人工抽查和 checkpoint，不继续扩大批注功能。
- 追加修复：2026-06-05 用户再次确认安全技术措施 / 模块 / 服务仍无常态和定位高亮。复查确认根因是旧回归只验证 `data-user-note-anchor-marked / active` 属性，未验证视觉样式；技术 chip 的语义色规则，尤其 `.relation-chip.technical-chip:not(.module-chip):not(.measure-chip)`，会覆盖批注背景。已将批注 chip 覆盖层放到所有 chip 语义色规则之后，并补齐 `technical-chip service-chip` specificity；资源版本更新到 `annotation-global-20260605-4`。
- 追加视觉修复：2026-06-05 用户截图确认普通态高亮仍呈横向黄色长条。已将普通态从背景铺底改为贴文字的琥珀下划线；行级普通态只保留左侧标识，不再铺整行黄色；关系 chip 保留原语义底色并叠加低噪声下划线 / 边框。脚本新增 `normalStripeOk` 和 `normal_visual_stripe_too_wide`，防止普通态宽背景条再次被验收脚本放过；资源版本更新到 `annotation-global-20260605-5`。
- 追加后续变更：2026-06-05 用户继续反馈定位后高亮落到文字后方、L0-L2 批注无常态高亮、普通态高亮线需要加深加粗但不遮挡文字、几个指南 / 幻灯片页无法添加批注。已补齐 L0-L2 树节点对象高亮、幻灯片页 / slide stage 批注目标、定位滚动 `inline: nearest`、普通态下划线加深加粗，以及标准映射按需加载后的值锚点。标准映射 ViewModel 固定为 projection / workbench 为主，已加载 standards 包只补充不覆盖，避免 `NIST CSF` 加载后覆盖后端 projection 中的 `AT-6`；同时把 ISO 等按需加载标准框架 / 控制项补入 `localRelationMap`，保证关注点页 DOM 具备稳定 `standard-framework-name` 值锚点。
- 基线固化：2026-06-05 用户确认该问题基本验收通过，后续有问题再按 bug fix 处理。当前批注设计已作为全局基线固化到 `global-annotation-requirements-and-regression-matrix.md` 与 `frontend-global-design-baseline-2026-05-30.md`，并新增“新页面接入清单”。后续新增页面必须先声明页面对象、值锚点、行锚点、幻灯片 / 子页上下文和回归命令，不再逐页重新调试批注样式、定位逻辑和抽屉行为。
- 追加修复：2026-06-08 用户要求右侧批注抽屉顶部“当前页 N 条 / 批注 / 收起”区域锁定不滑动，并指出首次 sticky 修法遮挡内容、过于草率。已撤回 sticky 叠层方案，改为 `.annotation-drawer-panel` 固定外框不滚动、`.annotation-drawer-header` 作为普通固定头部、`.annotation-drawer-fixed` 固定对象信息 / 添加批注 / 保存按钮、`.annotation-drawer-scroll` 仅承载下方批注列表滚动；抽屉底部增加 48px 留白，不再贴到页面最底；删除保存按钮左侧“写入用户库，不修改基础数据”说明文字；同步切换滚动记忆和重绘保留逻辑，递增 `styles.css` 缓存版本，并在 `scripts/audit_user_annotation_contract.mjs` 增加 `annotation_drawer_locked_header_contract_missing` 防回归断言。
- 最新验证：2026-06-05 带视觉断言和长条防回归的真实 Chrome 回归通过：`node scripts/audit_saved_user_annotations.mjs --url http://127.0.0.1:5173 --allow-system-chrome --debug-port 9410 --width 1800 --height 1200 --compact` 返回 `noteCount=24`、`passed=24`、`failed=0`、`failures=[]`、`consoleIssues=[]`。脚本已新增 `normalVisualHighlight` / `activeVisualHighlight` / `normalStripeOk`，第 6 安全技术措施、第 7 安全技术模块、第 8 安全技术服务、第 24 安全技术模块均满足常态与定位视觉高亮，截图对应的 `1.6 接收其他应用数据` 也满足 `normalStripeOk=true`。
- 最新验证：2026-06-05 后续变更真实 Chrome 全量回归通过：`node scripts/audit_saved_user_annotations.mjs --url http://127.0.0.1:5173 --allow-system-chrome --debug-port 9433 --width 1800 --height 1200 --compact` 返回 `noteCount=28`、`auditedNoteCount=28`、`passed=28`、`failed=0`、`failures=[]`、`consoleIssues=[]`。单点与连续场景验证包括 `--only-ordinal 1` 的 ISO 标准框架值、`--only-ordinal 23` 的 `AT-6` 控制项，以及 `--from-ordinal 15 --to-ordinal 23 --debug-state` 的标准包加载后不覆盖 projection 场景，均通过。
- 提交后补充验证：2026-06-05 checkpoint 提交后发现当前用户库已扩展到 33 条保存批注，首次补跑全量回归抓出 4 条指南 / 幻灯片缩略图批注缺少定位态视觉高亮。已只针对 `.guide-slide-stage` / `.guide-thumb` 定位态补齐琥珀下沿并推进 `styles.css` 缓存版本到 `annotation-global-20260605-8`。随后定点 4 条回归通过，全量真实 Chrome 回归 `node scripts/audit_saved_user_annotations.mjs --url http://127.0.0.1:5173 --allow-system-chrome --debug-port 9446 --width 1800 --height 1200 --compact` 返回 `noteCount=33`、`auditedNoteCount=33`、`passed=33`、`failed=0`、`failures=[]`、`consoleIssues=[]`。
- 需要确认：后续是否进入导出格式扩展、能力重组、导入和 Skill 集成。
- 修复说明：`OI-128A / OI-128B / OI-128C` 已实现，`OI-128C` 二次严格回归已通过，待最终人工抽查与 checkpoint。当前入口以 `user_notes` 承载正式批注，`user_favorites.note` 仅作为过渡收藏备注显示；收藏不再作为主业务动作，正式入口改为 `批注`、`待复核`、`数据篮` 和 `工作台`。2026-06-07 已补齐工作台总览和数据篮最小 API：`/api/v1/user/workspaces` 与 `/api/v1/user/data-baskets` 均支持创建、读取、删除容器，`/:id/items` 支持读取和 upsert 条目，`/items/:item_id` 支持删除条目，所有写入仍走本地 token 和 loopback 校验。新建行 / 字段 / 值批注使用带 route、view、目录 / 子页面、对象、tab 和表格坐标的 `v2` 锚点，旧缺上下文批注不再为了视觉反馈误高亮所有同名值。页面组件必须显式声明 `data-annotation-value`；普通业务 `td` 作为全局兜底值锚点；折叠目录定位必须先展开父级并恢复常驻标记；知识库对象行通过稳定 `data-annotation-target-ref` 暴露 overlay 锚点；批注层负责右键、抽屉、保存、状态、tooltip、常驻提示和定位高亮，原页面只暴露锚点，不承担批注业务逻辑。用户写入只影响 `sapd_wiki_user.sqlite3`，不回写基础库。
- 验证结果：2026-06-03 通过本地 `5173` API 写入 / 读取 / 删除闭环验证：`POST /api/v1/user/favorites` 写入 `base:capability_focus:OI-128A-SMOKE` 成功，`GET` 可读，`DELETE` 后列表消失；`node scripts/frontend_smoke_check.mjs --page capability --url http://127.0.0.1:5173` 通过。2026-06-03 `OI-128B` 真实 Chrome 回归覆盖 `/knowledge/technical-services`、`/knowledge/capabilities`、`/standards/mlps-level-3`、`/guides/security-architecture-modeling-language`，均有 `userObjectActionsProbe.count=1`、`consoleIssues=0`。2026-06-04 `OI-128C` 通过 `POST /api/v1/user/notes`、`GET`、`PATCH`、`DELETE` 本地 API 闭环；真实 Chrome 回归覆盖 `/capability-mapping`、`/knowledge/technical-services`、`/standards/mlps-level-3`、`/guides/security-architecture-modeling-language`，均确认右侧批注抽屉存在、可展开、旧横向条数量为 0、`workspaceWidthDelta=0`、`consoleIssues=0`。2026-06-05 `node scripts/audit_user_annotation_contract.mjs` 通过，动态渲染样例确认 `LC-AP=14`、`LC-DT=11`、参考数据 `3` 个值级锚点。2026-06-05 全局版审计通过，覆盖能力映射技术 / 管理、信息化环境、技术服务、技术模块、技术措施、LC-AP 参考数据、详情面板、标准 / 框架和折叠目录定位契约。2026-06-05 overlay 修复后 `node --check frontend/capability-browser/app.js`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/audit_frontend_display_contract.mjs`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`python3 scripts/dev_server_guard.py --status`、`git diff --check` 均通过；轻量 smoke 覆盖 `/capability-mapping`、`/environment-mapping`、`/development-security`、`/data-security`、`/knowledge/capabilities`、`/knowledge/technical-services`、`/knowledge/technical`、`/knowledge/technical-measures`、`/knowledge/functions`、`/knowledge/processes`、`/standards/nist-csf-2`、`/standards/mlps-level-3`、`/standards/iso-27001-2022`、`/standards/cis-csc-v8`、`/standards/crf`、`/standards/nist-800-53-rev5`、`/standards/dsp-level-2`、`/guides/security-architecture-modeling-language` 均通过；本轮未启动系统 Chrome，避免复现 Chrome 崩溃。2026-06-05 严格真实 Chrome 回归通过：`node scripts/audit_saved_user_annotations.mjs --url http://127.0.0.1:5173 --allow-system-chrome --debug-port 9397 --width 1800 --height 1200 --compact` 返回 `noteCount=24`、`passed=24`、`failed=0`、`consoleIssues=[]`，并逐条满足 `granularityOk=true`、`persistentAfterClick=true`、`unexpectedMarkedCount=0`。2026-06-05 二次严格真实 Chrome 回归通过：`node scripts/audit_saved_user_annotations.mjs --url http://127.0.0.1:5173 --allow-system-chrome --debug-port 9404 --width 1800 --height 1200 --compact` 返回 `noteCount=24`、`passed=24`、`failed=0`、`consoleIssues=[]`，并逐条满足 `normalMarkedBeforeLocate=true`、`activeAfterJump=true`、`granularityOk=true`、`persistentAfterClick=true`、`drawerPanelOk=true`、`currentNoteCardOk=true`、`drawerScrollPreserved=true`、`locateButtonClicked=true`、`unexpectedMarkedCount=0`。2026-06-07 数据篮最小 API smoke 通过：`node scripts/smoke_user_data_basket_api.mjs` 在临时 ZIP bundle / 临时 user DB 中验证 token 拒绝、创建数据篮、条目 upsert、读取、删除条目和删除数据篮闭环。2026-06-08 批注抽屉固定表单 / 独立列表滚动区验证通过：`node --check frontend/capability-browser/components/UserAnnotationDrawer.js`、`node --check frontend/capability-browser/app.js`、`node --check scripts/audit_user_annotation_contract.mjs`、`node scripts/audit_user_annotation_contract.mjs`、定向结构断言 `node -e ...`、定向 `git diff --check`、`python3 scripts/dev_server_guard.py --status`、`node scripts/frontend_smoke_check.mjs --page capability --route /capability-mapping --url http://127.0.0.1:5173` 均通过；内置 Browser 在 `/capability-mapping` 验证抽屉展开后固定区 bottom 为 `430`、保存按钮 bottom 为 `415`、列表滚动区 top 为 `430`、底部留白为 `48px`、保存按钮左侧文案仅剩 `保存批注`，滚动列表后固定区 top 保持 `152` 不变；本轮未启动系统 Chrome。
## OI-133：ArchiMate 建模语言页显示效果与加载效率优化

- 状态：已修复 / 待人工验收
- 类型：前端 / 设计 / 性能
- 对象或页面：`安全指南 / 安全架构建模语言`，路由 `/guides/security-architecture-modeling-language`。
- 现象：`archimate建模` 会话已把 PDF iframe 改为整页 JPG + 6 个区域 JPG，并提供 PDF 下载，但页面效果仍未达到用户预期。用户 2026-06-07 明确否定“下方区域阅读器”方案，要求在 PDF / Poster 图本身上分块，点击相应分块后弹出相关位置放大图。
- 影响：如果继续保留下方区域阅读器，用户会把该页理解成素材陈列或双重阅读路径；Poster 本体与放大区域之间的空间关系不够直接。
- 当前处理：2026-06-07 按用户最新纠偏再次修正：恢复两层标题结构，最大标题为 `安全架构建模语言`，第二标题为 `ArchiMate® 3.2 - 企业架构建模标准`；tab 组跟在最大标题后面并居右，`全页面显示` / `下载 PDF` 跟在第二标题后面；删除下方区域阅读器、区域目录、区域卡片和不够精确的热区浮层；主体空间留给整张 ArchiMate Poster，容器允许纵向滚动；点击图片或 `全页面显示` 时在当前页面内打开 Image Lightbox / Fullscreen Modal，并对预览容器调用 Fullscreen API；不再使用 `window.open`、`target="_blank"`、`Blob` 页面或浏览器原生新窗口 UI；预览层深色底色、默认 contain 适配屏幕，右上角关闭、Esc 关闭、点击遮罩空白区域关闭，工具栏只保留放大、缩小和适应，不显示下载按钮；支持鼠标滚轮缩放和按住拖动平移；缩放以适应屏幕后的显示宽度为基准，按钮和滚轮均采用小步进并带宽度过渡，避免第一次操作直接跳成超大图；本地整图预览资源已从 PDF 重新导出为 `6741 x 4768` 高分辨率 JPG，退出后恢复原页面和当前路由状态。2026-06-08 修复标题区 tab 与实际内容页不同步问题：`AppShell` 不再硬编码第一个 tab 为 active，`ArchiMate® 3.2` / `SAPD 元素图例` 和信息化环境页 `环境底图` / `归纳表格` 标题区 tab 均改为消费同一份 active tab 状态。
- 需要确认：用户在固定入口 `http://127.0.0.1:5173/guides/security-architecture-modeling-language` 人工验收上方 tab 位置、Poster 可滚动区域、页面内全屏预览清晰度、关闭按钮和 Esc / 空白关闭是否符合预期；如通过，可将状态改为 `已修复`。
- 修复说明：本轮只改 `frontend/capability-browser/app.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html`、`frontend/capability-browser/components/AppShell.js` 和状态文档；不改数据库、数据包、ETL、用户库或 `SAPD 元素图例` registry。PDF 不嵌入页面，只保留上方下载入口；当前不做 Poster 分块点击，避免不精确切割造成误导。
- 验证结果：2026-06-07 `rg 'download="archimate-poster-overview|下载图片|>下载<|window.open|target="_blank"|createObjectURL|new Blob|Blob\(' frontend/capability-browser/app.js frontend/capability-browser/styles.css` 无命中；`sips -g pixelWidth -g pixelHeight frontend/capability-browser/public/data/guides/archimate-poster/archimate-poster-overview.jpg` 确认为 `6741 x 4768`；`node --check frontend/capability-browser/app.js`、`node --check frontend/capability-browser/components/AppShell.js` 通过；`python3 scripts/dev_server_guard.py --status` 通过，固定 `5173` 单一项目服务健康；`node scripts/frontend_smoke_check.mjs --page content --route /guides/security-architecture-modeling-language --url http://127.0.0.1:5173` 轻量 HTTP smoke 通过，未启动系统 Chrome；内置浏览器检查确认点击 `全页面显示` 后工具栏文本为 `− 适应 ＋` 且下载控件数量为 0，图片自然尺寸为 `6741 x 4768`，初始适应宽约 `984px`，第一次滚轮仅放大到约 `1063px`，再点一次放大按钮约 `1240px`，拖动后滚动位置发生变化，Esc 可关闭并恢复原页面；源码确认 `requestModelingPosterFullscreen()` 会在浏览器提供 `requestFullscreen` 时对预览容器调用 Fullscreen API，内置浏览器运行时本身未暴露该 API，无法在该环境断言 `document.fullscreenElement`；`git diff --check` 通过；`python3 scripts/check_github_data_boundary.py` 通过。2026-06-07 追加验证 `SAPD 元素图例`：四个分组默认全部 `open=false`，点击第一个分组标题后变为 `open=true`，再次点击恢复 `open=false`；分组内 `展开 / 收起` 文字状态胶囊已删除，二级标题行右侧 `全部展开` / `全部收起` 可统一控制四个分组；关系线图例新增 `连接关系 / Association` 后又按用户原图样例替换为两条实线，上方无箭头、下方右侧开放箭头，关系线图例网格改为一行 4 个；源码断言确认 `MODELING_RELATION_LEGENDS` 包含 4 类关系且 `连接关系` 只包含 `association-none` / `association-end`；`node --check frontend/capability-browser/app.js`、content smoke 和相关 `git diff --check` 均通过。
## OI-135：用户库治理与兼容表迁移清理

- 状态：正式迁移脚本完成 / 真实库 apply 待显式确认
- 类型：数据 / 前端 / Delivery Bundle / 治理
- 对象或页面：`sapd_wiki_user.sqlite3`、`user_notes`、`user_favorites`、右侧批注抽屉、后续用户工作台 / 数据篮 / 导出 / 自定义能力。
- 现象：正式批注入口已切到 `user_notes`，旧 `user_favorites` 仍作为收藏 / 轻备注兼容表存在；后续工作台、数据导出、能力重组、导入和 Skill 集成都依赖用户库长期治理。
- 影响：若不治理，旧表语义、测试数据、迁移 / 备份 / 恢复边界和 base/user read model 合并规则会影响 Delivery Bundle 与用户工作台稳定性。
- 当前处理：已完成第一轮设计收敛，入口为 `docs/06-implementation/user-database-governance-and-stable-key-design.md`；已新增 `scripts/audit_user_db_governance_contract.mjs`、`scripts/audit_stable_key_contract.mjs`、`scripts/plan_user_schema_0_3_migration.mjs`、`scripts/smoke_db_migration_contracts.mjs`、`scripts/migrate_db_contracts.mjs`、`docs/06-implementation/user-db-compatibility-report-2026-06-06.md` 和 `docs/06-implementation/base-stable-key-and-redirect-migration-design-2026-06-06.md`。正式迁移脚本默认只写 `/private/tmp` 复制库，`--apply` 才写目标库；若目标在项目 `data/user`、`data/database` 或 `data/base` 下，还必须显式传 `--confirm-project-db-write`，并在写入前自动备份。2026-06-07 已补齐工作台总览、数据篮、导出配置、导出预览、导出执行和下载最小 API 及 runtime 表确保，当前真实基础库和真实用户库未写入，不改前端。
- 需要确认：后续如进入发布前真实库 apply，需先执行默认 dry-run、确认备份路径和输出，再显式运行 `--apply --confirm-project-db-write`；后续导出按 `docs/06-implementation/user-export-format-contract.md` 推进。2026-06-07 用户已确认表格导出基本参考原始数据，优先 Excel / CSV / Markdown，幻灯片材料导出 PDF；第一批业务数据集为能力全量映射、信息化环境安全映射、字典与标准框架数据；各数据集的 Excel sheet / CSV 文件和字段草案已补齐。下一步可进入格式代码扩展、字段选择 UI 或正式导出中心，不回到前端按钮先行。
- 修复说明：设计、审计入口、dry-run、临时库 migration smoke 和正式迁移脚本三段式已完成。正式脚本与设计 SQL 对齐，支持 `both/user/base` 范围、默认 dry-run、临时库 apply、自动备份、真实项目库写入确认门和 JSON 输出。复制用户库验证 `user_schema_0.3` 13 张表可创建；复制基础库验证 `stable_key` / `stable_ref` / `public_id` 覆盖 4660 个对象和 7654 条关系，`base_id_redirects` 最小表结构合格。工作台总览和数据篮最小 API 已按 `user_schema_0.3` 的 `user_workspaces` / `user_workspace_items` / `user_data_baskets` / `user_data_basket_items` 表结构落地，数据篮作为导出前置容器，不等同于收藏。导出最小闭环已按 `user_export_profiles` / `user_export_jobs` 落地：`export-profiles` 可创建、读取、删除配置，`exports/preview` 生成 `draft` 预览记录、字段边界摘要和 change log，`POST /exports` 基于 preview job 生成受控 JSON 文件，`GET /exports/:id/download` 下载文件。
- 验证结果：2026-06-06 已通过三个只读子 Agent fan-in 复核现有 user DB/API、stable_key / redirect 证据和工作台 / 数据篮 / 导出产品对象；`node scripts/audit_user_db_governance_contract.mjs --db data/user/sapd_wiki_user.sqlite3 --json` 通过，发现 `user_schema_0.2`、`user_notes=34`、legacy favorite note `1` 条；`node scripts/plan_user_schema_0_3_migration.mjs` 通过，只读输出 16 个 schema 计划动作和 6 类数据处理动作。2026-06-07 正式脚本验证通过：`node scripts/migrate_db_contracts.mjs --json` 默认 dry-run 输出 `writesPerformed=false`、`writesPerformedOnProjectDatabases=false`；`node scripts/migrate_db_contracts.mjs --apply --user-db /private/tmp/sapd_wiki_user_contract_apply.sqlite3 --base-db /private/tmp/sapd_wiki_base_contract_apply.sqlite3 --work-dir /private/tmp/sapd-wiki-db-contract-apply --json` 对临时库 apply 通过并生成备份，输出 `writesPerformedOnProjectDatabases=false`、`userSchemaVersion=user_schema_0.3`、`knowledgeItemsUpdated=4660`、`knowledgeRelationsUpdated=7654`；对真实项目库直接 `--apply` 被确认门拦截。迁移后审计通过：`node scripts/audit_user_db_governance_contract.mjs --db /private/tmp/sapd_wiki_user_contract_apply.sqlite3 --require-v03 --json` 和 `node scripts/audit_stable_key_contract.mjs --base-db /private/tmp/sapd_wiki_base_contract_apply.sqlite3 --user-db /private/tmp/sapd_wiki_user_contract_apply.sqlite3 --json` 均通过，仅保留 legacy favorite note、redirect 示例类型未覆盖和页面锚点需上下文解析 warning。2026-06-07 数据篮 / 工作台 / 导出 API 验证通过：`python3 -m py_compile scripts/run_local_server.py`、`node --check scripts/smoke_user_data_basket_api.mjs`、`node --check scripts/audit_user_db_governance_contract.mjs`、`node scripts/audit_user_db_governance_contract.mjs`、`python3 scripts/check_github_data_boundary.py` 均通过；`node scripts/smoke_user_data_basket_api.mjs` 在临时 ZIP bundle / 临时 user DB 中验证 token 拒绝、工作台、数据篮、导出配置、禁止字段拦截、导出预览、导出执行、下载文件和下载字段边界闭环，真实项目库未写入。
