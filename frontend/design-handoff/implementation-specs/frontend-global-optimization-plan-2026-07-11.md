# SAPD Wiki 全工程前端优化方案

状态：`implementation-in-progress / implementation-source`

最后修改日期：`2026-07-13`

读者：项目负责人、后续前端实现 Agent、视觉验收人员。

用途：以当前 `5173` 真实工程页面为证据，定义 SAPD Wiki 全工程 Apple Shell 视觉优化方向、跨页面规则、页面类型改造顺序和验收口径。本文件是后续实现依据；成熟度合成视觉稿是页面级参考，不替代本文件的全局契约。

2026-07-12 补充口径：第 4 章记录审计风险，第 4A 章逐条给出风险对应的实现方案、验收口径和优先级。用户单独提出的能力图谱碰撞、Draw.io 不可变和成熟度完整设计属于对应页面的强制约束，不替代其他风险的解决方案。

2026-07-13 成熟度四维评分补充口径：组织与角色、制度与流程、平台与工具、数据与信息必须独立判定；页面必须持续提供当前模板的维度 L1—L5 rubric，不得用等级名称、服务映射、证据数量或其他维度分数代替判定标准。

2026-07-13 成熟度选定方案补充口径：第二轮采用 Product Design 方案 1 并将未点名区域设为当轮不可变基线。第三轮用户明确要求复用安全能力映射目录，故本轮授权把横向 L0 / L1 / L2 与关注点列表合并为左侧评分目录；项目步骤、L2 汇总、关注点定义、评分表单、`DIRECT / CHILD_ROLLUP`、逐点评估适用性和无服务 `FOCUS` 契约继续保留。

2026-07-13 成熟度第二轮验收补充口径：第二轮八项修正白名单覆盖滚动位置稳定、列表 / 服务评分区分层、能力导航无原生横向滚动条、字体与进度层级、用户可见 demo 信息清零、适用性方框勾选与取消下级下拉、当前等级定义就地提示、关注点定义和单等级初始化。评分执行页不回写关注点评级；结果页后端聚合口径不变。

2026-07-13 成熟度运行实现补充口径：评分工作区已按当前 L2 聚焦为“左侧 L0 / L1 / L2 / FOCUS 评分目录 + 右侧 L2 汇总、关注点摘要、服务 Tab 和单评估点四维表单”，不再以十列表格或三套横向导航平铺。1—5 评分按钮和当前 Rubric 采用全工程共享输入、选中与焦点语言；Rubric 由 API 返回。居中新建项目浮层的关闭、遮罩、`Esc` 和“取消”统一为直接结束未创建流程，不再使用关闭确认阻塞状态。

2026-07-13 成熟度第三轮联动记录：本轮新增评分目录状态、服务 Tab、右上角“保存并转到下一项”、等级定义平滑聚焦、L2 能力四维雷达和覆盖 / 排除统计；同时把不适用排除口径从总体层统一到关注点、L2、L1 / L0 和总体层。模板结构、Rubric 来源、四维评分公式、正式 SQLite / JSON、源 Excel、用户库和 DMG 均未改变。
2026-07-13 评分目录标签微调联动记录：目录节点状态标签改为只显示语义圆点，不重复展示状态文字；文字由 `aria-label` / `title`、目录图例和工作台状态保留，确保视觉扫描更轻量且仍可访问。本轮无数据、算法、模板或聚合规则变化。

2026-07-13 成熟度交接收口联动记录：本轮无全工程业务、数据、评分或共享组件规则变化；原因是仅在成熟度既有实现内修正主业务文字低于全局排版基线的问题，为评分表单底部动作增加滚动可见性，并清理旧关闭确认状态残留。该实现继续复用 Apple Shell 现有输入、按钮、边框与状态色，不新增页面模板或独立 token。

2026-07-13 成熟度真实工程复刻联动记录：本轮无全工程业务、数据或评分规则变化；新增的全局实施约束是“页头信息只能归属一层”。成熟度评分执行页的进度、保存状态与次级工具迁入真实 App Shell 页头，禁止再叠加项目详情头或常驻工具条；低频功能统一进入“更多”浮层，但不得删除。页面继续复用全局 Tab、按钮、输入、边框、焦点和状态 token；SERVICE / FOCUS 同构且保持真实工程数据，不以设计图样例数值覆盖后端结果。

2026-07-13 P0-2 实施联动记录：Apple Shell 与共享布局基座已进入真实工程。品牌退出页面 `h1`，页面标题区成为唯一主标题所有者；顶栏 / 页头固定为 `56px / 96px`，1440×1024 计算主区高度为 `872px`；全局导航收敛为单业务域展开并自动滚入当前项。知识字典和指南的第二辅助层使用可关闭 overlay，目录仍可常驻但不再与永久 inspector 同时占用网格列。成熟度项目列表、项目页、评分页和结果页正式纳入同一 Apple Shell：工作区固定为 `main-only`，项目列表主动作进入共享页头，项目页不再创建第二个主标题层，新建项目继续使用居中 workflow overlay。共享字号固定为 `12 / 14 / 16 / 24px`，圆角固定为 `6 / 10 / 14px`，实施块不含业务表格全局覆盖。本轮未改变成熟度业务、评分或聚合规则，也未修改能力图谱碰撞算法、Draw.io 原图、数据或 ETL。

2026-07-13 P0-2 舒适度与成熟度一致性修正记录：复盘用户截图后确认，原 P0-2 把全局标题区由舒适基线压缩到 `82px / 22px`，并在评分执行页移除了项目上下文；服务切换另造卡片、目录删除状态文字后仍保留固定列宽，结果页把 L2 能力错误投影成单能力四维雷达。现统一修正为 `96px` 共享页头，并以后续旧 DMG 精确对照确定 `24px / 1.13` 标题、`12px / 1.45` 说明、`12px 18px` 内边距、`5px` 文本组间距；六个成熟度项目页签保持同一项目上下文；服务切换复用 Apple Shell 共享 Tab；目录按“状态圆点 + 层级标签 + 业务名称”紧邻排布；结果页展示后端全部 L2，并按技术、治理、管理分组。未修改 Draw.io 原图、根因语义色、评分 / 聚合、API projection、正式数据和 ETL。

2026-07-13 旧 DMG 标题视觉真值联动记录：用户再次以当前程序和旧 DMG 截图复核，确认上轮 `26px / 14px / 10px 24px` 的标题、说明和内边距仍偏离旧版。现把已挂载 DMG `0.1.7` 的运行计算样式作为共享 App Shell 真值：页头 `96px`，标题 `24px / 1.13`，说明 `12px / 1.45`，面包屑 `12px / 1.375`，页头内边距 `12px 18px`，文本组间距 `5px`，标题与标签间距 `7px`。参数进入 P0-2 配置与审计，禁止业务页局部覆写；本轮无成熟度业务、评分、聚合、模板、API projection 或数据规则变化。

2026-07-13 成熟度评分与结果左图右数联动记录：本轮固化三条共享前端规则。第一，业务子页不得用 `:has()` 等页面专用选择器覆写同级 Apple Shell Tab 高度，六个成熟度项目页签统一使用 `52px` 导航。第二，共享服务 Tab 在桌面端等分可用宽度、优先一屏展示全部对象，重复的尾部作用域编码从 Tab 移除，窄屏才内部滚动。第三，数据密集图表不得单图占满整行：全 L2 雷达使用中文短名并采用左图右数，右侧按后端 L0 / L1 / L2 结果展示 T/G/M、能力域数量、低于目标数量和后端差距候选。评分格居中、选中等级平滑放大且只显示当前完整 Rubric。以上为共享布局、Tab、选中反馈和分析图表规则补充；成熟度业务评分、聚合、模板、API projection 和正式数据均无变化。

2026-07-13 成熟度第五轮评分工作台联动记录：用户截图证明“单行强压服务 Tab + 表单头部操作 + Rubric 下沉整行”没有通过人工验收。本轮将共享控件规则修正为：业务对象全名优先于单行；服务 Tab 直接承载逐项适用性方框，右侧工作台不足 `1000px` 时允许三列换行；项目步骤导航在业务滚动容器内锁定；评分表单删除重复标题、适用性和展开收起区，主动作移到底部；五个评分格居中，选中格先放大，再在同一行右侧显现当前定义；未选择不显示解释占位。成熟度用户可见目标文案统一为“目标等级”。以上只改变 shared frontend 信息布局、交互反馈和文案，不改变 `targetLevel` 字段、评分、聚合、Rubric、API projection 或正式数据。

2026-07-14 成熟度第六轮评分交互联动记录：用户继续否决第五轮三项结果，说明根因不是尺寸不足，而是控件归属仍然错误。服务状态、完整名称和适用性必须属于同一 Tab 项；选中评分格必须自己扩展并在格内承载等级名和完整定义，不能保留独立“当前等级定义”列；项目身份区和六个步骤页签必须作为一个整体 sticky。新增 FE-R76—FE-R78 作为覆盖旧 FE-R69 / FE-R71 / FE-R73 / FE-R75 冲突描述的最终验收口径。本轮不改变成熟度业务数据、评分、聚合、Rubric、API 或 DMG 包装边界。

2026-07-13 分轨实施排期口径：第 8 章按既有 `FE-Rxx` 优先级整理为可直接执行的修复批次，第 10 章拆分“全局壳 / 总览”等混合优先级。成熟度必须继承 P0-2 共享壳层；其余 `FE-R35—FE-R39、FE-R51—FE-R61` 业务页面与评分交互仍保留独立规格和验收轨道，不与后续非成熟度页面批次混改。

2026-07-13 P0-1 实施记录：`FE-R08 / R12 / R46 / R49 / R50` 已建立统一机器门禁。门禁以显式选中 ID、能力工作区 API、原始 `environmentBasemap.svg` 哈希、共享语义角色和主区禁显字段为权威源；同时修复能力关系视角 Tab 的可见键盘焦点、全局 / 环境定位动态播报，以及 `localRelationMap` 误携带 `sourceEvidence` 的展示边界。成熟度运行代码、Draw.io 原图、正式数据和后端业务关系均未修改。

2026-07-13 P0-4 实施记录：`FE-R27 / R33 / R34` 已按 P0-2 Apple Shell 契约进入真实工程。标准深链恢复时统一展开“安全标准 / 框架”业务域，并让当前框架在导航可视区内保留 `10px` 安全留白；Issue 工作台取消 `rows[0]`、批量勾选和旧路由选择的隐式当前对象，首次进入只显示作用域与队列，明确点击或键盘 `Enter / Space` 才按需打开详情，关闭后归还队列宽度并把焦点返回来源行。Issue 路由重入会清空详情与批量选择，只展开工作台域。本轮未修改标准数据、Issue / 批注用户库、路由清单、业务筛选排序、成熟度模块或 DMG。

成熟度联动维护口径：后续凡修改成熟度业务规则、评分流程、模板结构、页面信息架构、交互控件、视觉布局或验收规则，必须在同一任务中同步更新本文件、`maturity-assessment-v2-1-complete-frontend-design-2026-07-12.md` 和 `SAPD_成熟度评估业务设计_V2.1_20260712.md`。本文件只记录成熟度变更对全工程布局、控件、密度、状态与共享组件的影响，不替代成熟度页面实现规格或业务设计。

维护入口：`frontend/design-handoff/implementation-specs/`。当全站页面完成统一改造并由全局设计基线承接全部稳定规则后，本文件转为历史交接材料。

## 1. 本轮目标与边界

- 目标一：按用户最新确认的成熟度评分页方向，采用左侧安全能力评分目录 + 右侧评分工作台、服务 Tab、L2 / 关注点唯一评分来源、逐点评估适用性、无服务 FOCUS、加长评分依据、评估证据说明和稳定保存状态。
- 目标二：审计整个工程的代表性真实页面，不把结论局限在成熟度模块。
- 目标三：形成可分阶段实施、可自动审计、可人工验收的全局优化方案。
- 本文件持续承接全工程方案；2026-07-13 已实施成熟度共享前端与 API 投影，其余页面仍按本方案分阶段推进。未修改正式 JSON、SQLite、源 Excel、用户库或 DMG。

## 2. 审计范围

本轮按页面类型抽样，不按路由数量机械逐页检查。代表性类型包括：

1. 全局导航 / 总览页；
2. 安全能力关系画布；
3. 信息化环境关系画布；
4. LC-AP / LC-DT 生命周期工作台；
5. 知识库字典表格；
6. 安全标准 / 框架表格；
7. 指南文档阅读页；
8. Issue 工作台；
9. 成熟度项目、评分和结果页；
10. 全局搜索、页面内搜索、右侧 Issue / 详情等共享交互层。

## 3. 已确认的成熟度页面方向

### 3.1 用户选择

- 主结构：从全部评估项纵向平铺改为 L0 分类、L1 主 Tab、L2 次级 Tab；一次只展示当前 L2。
- 完整页面：项目内步骤导航保留；主工作区改为左侧 L0 / L1 / L2 / FOCUS 评分目录与右侧 L2 汇总、关注点摘要、服务 Tab 和主评分表单。评分区继续采用“维度 + 1—5 + 加长评分依据、当前 rubric 下沉一行”。
- 能力切换：复用安全能力映射目录的层级轨道、缩进与展开控件；节点以绿 / 黄 / 冷灰圆点标签分别表达已完成 / 进行中 / 未开始，蓝色只表示当前路径，文字由目录图例与可访问名称提供。
- 汇总区：L2 必须有不可折叠的当前 / 目标 / 差距 / 完成度 / 四维平均分汇总；关注点按块展示后端平均分。
- 打分区：组织、流程、工具、数据使用成熟度 1—5 按钮，并持续完整显示当前已选等级的维度标准；主区不展开全部 L1—L5；人工目标继续使用下拉，是否适用使用方框勾选。
- 证据区：每个评估点增加“评估证据说明（可选）”，文件、链接和等级为次级关联；无证据不阻塞流程。
- 批量与反馈：L2 / 关注点在 `DIRECT` 和 `CHILD_ROLLUP` 中选择一个有效评分来源；直接评分填写节点四维。关注点“统一设置下级”仅在全部下级未评分时选择一个等级初始化全部下级四维，任一下级任一维度已评分后锁定；评分执行页不显示下级聚合出的关注点评级，结果页仍消费后端聚合。
- 评分标准：对象专用 rubric 优先，通用四维五级矩阵只作回退；四个维度分别选择“当前稳定实现的最高完整等级”，计划状态只进入人工目标，不进入当前分。
- 评估点：每个 `SERVICE / FOCUS` 都显示是否适用；完全没有安全技术服务时直接评估真实关注点，不生成空服务或伪作用域。

### 3.2 本质规则

成熟度评分页的主语是“在目录明确定位后完成一个评估点判断”。项目内步骤导航 → 左侧评分目录 → 右侧 L2 汇总 / 关注点摘要 / 服务 Tab / 主评分表单构成唯一工作流，不再并列横向能力 Tab、关注点列表和上一 / 下一服务分页。L2 / 关注点必须明确唯一评分来源：`DIRECT` 消费节点四维，`CHILD_ROLLUP` 消费下级实际结果，二者不能在一次 calculation run 混用。统一设置与实际统计反馈必须分离；当前行的维度标准、加长评分依据、目标、证据和适用性处于同一上下文。任何评分重渲染都必须保持当前目录路径、服务 Tab、滚动位置、页面位置和焦点。

### 3.3 当前真实页问题证据

- 当前真实评分页同时常驻全局导航、能力目录、评分表、右侧说明区，1440px 视口下业务名称被大量截断。
- 评分按钮本身清楚，但被过窄列宽和多层面板削弱。
- 评分说明与证据和当前行分离，用户需要反复确认右侧 inspector 是否仍对应当前行。
- 页面信息密度并非过高，问题是同时可见的结构层级过多。

## 4. 全工程审计工作记录

### 4.1 全局导航 / 总览页

当前优点：一级导航识别度高，工作台、能力、环境、生命周期、知识和标准的业务域可快速扫描。

当前风险：总览页仍以多组指标块、入口块和覆盖率块表达系统，容易落入仪表盘模板；共享页头和内容首屏占用较多垂直空间。应用内浏览器全页截图在半透明顶栏处出现黑块渲染伪影，已拒绝作为页面缺陷证据；页面结构由真实运行态和局部可见区域交叉核对。

### 4.2 安全能力映射页

当前优点：全局导航、能力树、主画布三层关系清楚；语义色已能区分当前对象、技术、管理和标准角色。

当前风险：主画布占据大面积空间，但核心图谱在当前 L0 级状态下过小，视觉重心弱；页面同时使用外层工作区、内层大圆角面板、画布边框和工具条背板，层级边界过多；次级目录与主画布之间的宽度比例缺少按对象粒度变化的规则；关注点图谱在分支负载不均、长中文标签、共享节点和跨分支关联同时存在时，仍可能出现节点 / 标签碰撞、边穿节点和连线交叉，不能只靠增加斥力或缩小字号解决。

### 4.3 信息化环境映射页

当前优点：真实业务底图是页面主角，页面没有再增加第二套目录；视图切换和缩放操作集中在画布顶部。

当前风险：原始 Draw.io 图是用户确认的设计基准，其颜色、边框、字体、节点位置和连线样式均不得修改。页面需要解决的是默认缩放下对象名称过小、用户首先看到“整张图”而不是当前任务、画布工具条与全局页头控制层级不清楚、选中节点后的关联详情过重，以及搜索 / 定位反馈可能遮挡原图。应用内浏览器全页截图在半透明顶栏处出现黑块渲染伪影，已拒绝作为页面缺陷证据。

### 4.4 LC-AP 生命周期页

当前优点：阶段 Tab 是全站最清晰的序列导航之一，当前阶段和阶段编号能快速定位；主表保留了阶段目标、活动、模式、服务和策略等业务关系。

当前风险：首屏同时展示多个宽列，主审阅字段字号偏小，右侧列在 1440px 下被裁切；大量浅色单元格和局部 chip 同时出现，视觉焦点不稳定；`/` 空值虽然不是 chip，但仍作为高频可见内容，降低完成度感；当前阶段的关键行动没有从整张宽表中脱颖而出。

### 4.5 LC-DT 生命周期页

当前优点：数据处理场景、子场景、服务、模块 / 措施和策略矩阵采用纵向分区，比 LC-AP 更接近可扫描工作台；服务作用域色卡有业务辨识度。

当前风险：阶段顶部控件、场景大表、服务 chip 区、模块 / 措施区和策略矩阵仍使用多套局部边界；信息密度较高但层级节奏不统一；服务色彩较多，需要确保只表达作用域，不与状态色竞争。应用内浏览器全页截图在半透明顶栏处出现黑块渲染伪影，已拒绝作为页面缺陷证据。

### 4.6 知识库字典：安全能力清单

当前优点：三列表头、分层行和数量信息清楚，表格比关系画布更接近稳定的 Apple Shell 信息系统；左侧知识目录提供明确的同域导航。

当前风险：默认全部折叠时，三条 L0 行之后留下大面积空白，页面缺少“展开第一层 / 搜索 / 最近定位”的任务引导；三条分类行使用近似整行蓝底，状态与结构层级的颜色区分不足；页头数量 chip 过多，容易与筛选条件混淆。

### 4.7 安全标准 / 框架页

当前优点：框架标题、搜索、框架内部视图切换、分级表格和能力映射列构成了稳定的数据阅读路径；分组行和控制项行的层级清楚。

当前风险：`CSF Core / CSF Tiers` 使用较厚的双层分段背板，和全站较轻的 Tab 语言不一致；分组行、数据行、映射 chip、数量 badge 和交替底色同时使用多种边界，视觉噪声偏高；页头数量 chip 仍像状态筛选；左侧全局导航较长时当前标准入口不易在首屏保持可见。应用内浏览器全页截图在半透明顶栏处出现黑块渲染伪影，已拒绝作为页面缺陷证据。

### 4.8 指南文档页

当前优点：全局导航、指南目录、正文阅读三层职责明确；目录项可直接定位章节；正文表格和长文排版可读。

当前风险：成熟度指南仍保留大面积高饱和蓝紫渐变 hero，与项目“低噪声 Apple Shell、非营销页、禁止通用蓝紫 AI 风格”的基线冲突；hero 占据首屏大量高度，用户需要向下滚动后才能进入方法论内容；正文卡片边界、外层 iframe / 阅读面边界和指南目录面板形成三层容器。指南页应有文档封面感，但不能使用营销 hero 作为主要识别手段。

### 4.9 Issue 工作台

当前优点：范围、处理队列、选中详情构成明确的三栏 Review Queue；状态、页面、优先级和搜索集中在顶部；批量选择与导出入口可见。

当前风险：页头状态 chip、筛选条、左侧范围卡片、中间每条 Issue 卡片和右侧 inspector 同时存在，出现明显“卡片套工作台”感；中间队列表格已出现横向滚动，说明列宽分配和信息优先级未收敛；未选择时右栏保持大面积空白，应提供选择提示、快捷键或最近项，而不是纯空白；全局侧栏保留其他已展开业务域时，导航高度和认知负担进一步增加。应用内浏览器全页截图在半透明顶栏处出现黑块渲染伪影，已拒绝作为页面缺陷证据。

### 4.10 成熟度项目列表

当前优点：项目、模板、状态、当前结果、完成度、证据覆盖和最近更新字段完整；右侧当前项目摘要能帮助快速继续工作。

当前风险：页面被标题区、demo 提示、四段统计、筛选区、表格和右侧摘要连续横切，像多个组件拼装；`平均评分完成度` 等统计权重过高，抢占项目选择任务；右侧摘要长期占宽，导致项目表列宽紧张；当前列表页和评分页分别采用不同的摘要 / inspector 语言，模块内部不统一。

### 4.11 全局搜索

当前优点：查询、分类 facets、分页和结果定位契约完整；命中词高亮能帮助快速判断相关性。

当前风险：顶栏搜索和页面内搜索同时占据首屏，形成重复入口；大量分类 chip 与结果数量并列，筛选状态、统计和导航的视觉语义混在一起；每条结果都使用完整圆角卡片，重复高亮文字造成“卡片流”，不利于连续扫描。建议保留顶栏快捷搜索，把结果页收敛为单一搜索工具条、轻量 facets 行和分隔线列表。

### 4.12 共享根因

1. **壳层重复**：全局侧栏、页面目录、局部筛选、永久 inspector 经常同时存在；1440px 下主任务宽度被结构层吃掉。
2. **容器过多**：页面、模块、工具条、数据区和每条记录都各自使用边框、底色或圆角，造成“卡片套卡片”。
3. **生成式界面的语义色冲突**：作用域标签、结构层级、选择、状态和关系角色在应用生成的表格、chip、图谱与控制层中没有完全统一优先级。该根因不包含信息化环境原始 Draw.io 图；原图颜色和样式是不可变设计基准。
4. **密度失衡**：部分总览和指南偏松、表格和成熟度评分偏紧；现有成熟度 CSS 出现 `9px—22px` 多档字号和 `2px—999px` 多档圆角，说明模块语言没有完全继承全局 token。
5. **常驻详情过度**：没有选择时仍保留空 inspector，有选择时又把解释内容放到远离当前对象的位置。

## 4A. 全工程逐风险优化方案

### 4A.0 阅读规则

- 每个 `FE-Rxx` 对应第 4 章的一条已识别风险；不能用全局 token 或通用原则代替页面级方案。
- “方案”说明具体改什么；“不改边界”说明不能顺手改变什么；“验收”用于后续实现和回归。
- 优先级含义：P0 为全局基础或高频主任务，P1 为核心业务页面，P2 为体验收口。优先级不表示低优先项可以永久不做。

### 4A.1 全局壳与总览页

| ID | 已识别风险 | 具体优化方案 | 不改边界 | 验收 | 优先级 |
|---|---|---|---|---|---|
| FE-R01 | 总览由指标块、入口块和覆盖率块组成，像通用仪表盘 | 改为“继续工作、最近访问、需关注事项、知识域入口”四个任务区；继续工作和需关注事项使用分隔线列表，知识域入口使用紧凑导航行；只有会改变用户决策的运行状态才显示数字 | 不在前端重算跨数据包指标，不把维护审计信息暴露给普通用户 | 首屏能直接继续最近任务；普通用户首屏大型统计卡为 0；任务入口不重复左侧全部导航 | P2 |
| FE-R02 | 顶栏、页面标题和内容首区共同占用过多纵向空间 | 顶栏固定 `56px`；共享页面标题区固定 `96px` 并使用旧 DMG 的紧凑文本节奏；页面动作与标题同一基线 | 不移除全局搜索、授权状态和必要的 App 级动作，不允许业务页另造第二主页头 | 1440×1024 下主内容在首屏至少显示 650px 高度；同一页面标题只出现一次 | P0 |
| FE-R03 | 全局侧栏多个业务域同时展开，当前入口可能移出首屏 | 侧栏改为单业务域展开；路由变化时当前项自动滚入可见区；支持折叠为图标栏，折叠状态按运行面保存 | 不改变现有业务域、路由和导航 Manifest | 任一路由刷新后当前项可见；侧栏无页面级纵向溢出；键盘能展开、收起和定位当前项 | P0 |

### 4A.2 安全能力映射页

| ID | 已识别风险 | 具体优化方案 | 不改边界 | 验收 | 优先级 |
|---|---|---|---|---|---|
| FE-R04 | 大画布中核心图谱过小，关系不是视觉中心 | 图谱加载后按当前对象和有效业务节点计算可见边界，使图谱默认占画布可用面积的 70%—85%；不同粒度采用不同布局策略，L0/L1 偏结构总览，L2/关注点偏关系展开 | 不通过放大空白节点、虚构关系或隐藏真实关系制造“饱满”效果 | 1440px 下核心图谱无需手动放大即可阅读；默认标签不低于 12px | P1 |
| FE-R05 | 外层工作区、内层圆角面板、画布边框和工具条背板形成多层容器 | 页面只保留侧栏与一个主工作面；图谱标题、视角 Tab 和控制条进入同一头部；画布只保留单一边界，移除大圆角套层和装饰渐变 | 保留技术、管理、标准矩阵及必要的来源折叠区 | 主画布外可见容器边界不超过 2 层；Tab、工具条与画布对齐 | P1 |
| FE-R06 | 能力目录和画布比例不随对象粒度变化 | 目录宽度桌面默认为 240px，可在 220—300px 内调整；关注点关系视图允许一键折叠目录；小于 1100px 自动折叠为按需抽屉 | 当前对象仍必须来自显式选择 ID，不得用首项或旧状态驱动主区 | 目录展开 / 折叠不改变当前对象；主画布最小宽度满足图谱可读阈值 | P1 |
| FE-R07 | 关注点图谱存在节点、标签、边和分支碰撞 | 按第 7A 节实施分支负载扇区、实测标签包围盒、共享节点端口、弧形路由、叶节点聚合和视图降级；图谱下方矩阵始终保留完整业务关系 | 不删除关系，不缩小到不可读字号，不把聚合节点写回数据层 | `nodeOverlaps=0`、`labelOverlaps=0`、`edgeNodeIntrusions=0`；结构视图 `edgeCrossings=0`；高负载反例通过 | P0 |
| FE-R08 | 图谱选择、矩阵和详情可能出现当前对象错位 | 统一使用路由选中 ID / `target_ref` 驱动图谱中心、矩阵筛选和按需详情；搜索定位先切视角、展开簇、再居中 | 不允许 `rows[0]`、旧缓存或最近一次选择替代当前对象 | L0、L1、L2、关注点四类选择均满足左侧选择、图谱中心、矩阵和详情对象一致 | P0 |

### 4A.3 信息化环境映射页

| ID | 已识别风险 | 具体优化方案 | 不改边界 | 验收 | 优先级 |
|---|---|---|---|---|---|
| FE-R09 | 默认适配后原图文字过小，用户先看到整图而非任务 | 默认仍执行“适应画布”，同时保留上次用户缩放；从搜索、路由或目录进入时，自动定位并放大目标节点邻域，提供“返回全图” | Draw.io 原图颜色、字体、尺寸、位置、边框、连线和图例全部不可修改 | 全图入口完整；目标定位后节点与上下文可读；返回全图不丢选择 | P1 |
| FE-R10 | 画布工具条与全局页头层级不清楚 | 将适应、100%、缩放、全屏、上次焦点合并为一条紧凑画布工具条；工具条固定在画布外沿，不覆盖标题、图例和节点 | 不把应用工具写入原始 SVG，不修改原图内容 | 工具条与全局动作视觉层级不同；390、1024、1440px 不遮挡原图 | P1 |
| FE-R11 | 节点关联详情过重，长期挤压画布 | 默认不显示详情；选择节点后打开可关闭的按需侧面板或底部抽屉，关闭后画布恢复全部宽度；详情按作用域、服务、模块 / 措施、系统分组 | 详情数据必须来自 `environment-workbench` 同对象投影，不从 SVG 文本反推 | 无选择时详情占宽为 0；关闭后缩放和焦点保持；详情对象与选中节点一致 | P1 |
| FE-R12 | 搜索 / 定位反馈可能遮挡或改写原图 | 使用 SVG 外部 overlay 焦点环和面包屑提示；高亮不改变节点填色、边框和字体；自动避让原图图例 | 禁止 CSS filter、统一染色、字体覆盖或节点重排 | 定位态与原始 SVG diff 仅包含 overlay；焦点环可由键盘和关闭操作移除 | P0 |

### 4A.4 LC-AP 生命周期页

| ID | 已识别风险 | 具体优化方案 | 不改边界 | 验收 | 优先级 |
|---|---|---|---|---|---|
| FE-R13 | 多个宽列导致主审阅字段小且右侧裁切 | 建立列优先级：阶段目标、主要活动、安全活动为 P0；模式、服务、策略为 P1；组件与参考来源进入行内展开。固定首列和表头，局部表格允许有提示的横向滚动 | 不删除业务字段，不把低频字段静默隐藏到不可访问状态 | 1440px 首屏 P0 列完整；页面级无横向滚动；展开区可访问全部 P1 / P2 字段 | P1 |
| FE-R14 | 字号过小，长文本扫描困难 | 主业务单元格 13—14px、行高 1.55；长活动文本按语义段落换行；编码和辅助说明 12px | 不截断关键业务名称，不依赖 tooltip 承载唯一完整值 | 主要活动和安全活动无需 hover 可读；业务正文不低于 13px | P1 |
| FE-R15 | 浅色单元格、chip 和底色过多，焦点不稳定 | 只对安全技术服务、模块 / 措施等业务对象使用 chip；活动、策略和说明使用普通文本；状态色仅表达状态；分组靠间距和分隔线 | 生命周期阶段色和服务作用域色继续按既有全局契约 | 普通文本不再批量胶囊化；同一行高饱和语义色不超过两类 | P1 |
| FE-R16 | `/` 空值高频出现，降低完成度感 | 空值默认显示低对比 `—`；只有业务上需要说明时使用“未提供 / 不适用”；空值不做 chip | 不把真实缺失改写成已有数据，不全局 forward fill | 空值语义可区分缺失与不适用；表格中 `/` 显示计数为 0 | P1 |
| FE-R17 | 当前阶段关键行动没有从宽表中脱颖而出 | 阶段 Tab 下增加一条轻量阶段摘要，显示阶段目标、当前行数和主要审阅入口；默认排序保持源顺序，选中行用低饱和背景 | 不新增 KPI 卡，不改变源表顺序 | 用户进入阶段后能在一屏找到阶段目标和第一条主要活动 | P1 |

### 4A.5 LC-DT 生命周期页

| ID | 已识别风险 | 具体优化方案 | 不改边界 | 验收 | 优先级 |
|---|---|---|---|---|---|
| FE-R18 | 阶段控件、场景表、服务区、模块区和策略矩阵使用多套边界 | 统一为“阶段工具条 + 场景主表 + 关系明细 + 策略矩阵”四段；段落用标题、间距和发丝线，不再每段套大圆角卡 | 保留现有阶段 Tab 和矩阵业务粒度 | 主区大容器边界不超过 2 层；四段标题和滚动归属清楚 | P1 |
| FE-R19 | 高密度信息缺少一致节奏 | 场景主表默认展开；服务、模块 / 措施和策略按当前行渐进披露；sticky 阶段栏和当前场景栏维持上下文 | 不把阶段全集反向扩展到单行，不改变行级权威粒度 | 滚动到明细时仍能识别阶段和场景；同一时刻只展开一个场景明细 | P1 |
| FE-R20 | 服务作用域色可能与状态色竞争 | 服务色只出现在服务 chip；完成、警告、缺失、选择使用独立图标和文字；矩阵普通背景保持中性 | 保留既有服务作用域颜色契约，不修改业务色编码 | 状态可在灰度下识别；作用域色不用于整行状态背景 | P1 |

### 4A.6 知识库字典

| ID | 已识别风险 | 具体优化方案 | 不改边界 | 验收 | 优先级 |
|---|---|---|---|---|---|
| FE-R21 | 默认折叠后只有少量 L0 行和大面积空白 | 默认展开到 L1；提供“全部展开 / 收起到 L0 / 最近定位”；搜索结果自动展开祖先路径；空白区域提供操作提示而非装饰插图 | 不改变能力层级和源顺序 | 首次进入可看到 L0 与 L1；搜索命中能定位到具体业务行 | P2 |
| FE-R22 | 分类行整行蓝底，结构色与选择色混淆 | L0 / L1 使用不同明度的中性分组行；蓝色只用于当前选择、链接和主动作；层级用缩进、字重和展开图标表达 | 不取消能力分类 T/G/M 的业务标签 | 未选择分组行不使用选择蓝；选中态与层级态可同时识别 | P1 |
| FE-R23 | 页头数量 chip 像筛选条件 | 静态数量改成标题后的弱元数据；真正可点击的筛选保留标准 filter 样式和选中状态 | 不删除用户需要的对象总数 | 用户可从样式和键盘语义区分统计与筛选；静态数字不可获得按钮焦点 | P1 |

### 4A.7 安全标准 / 框架页

| ID | 已识别风险 | 具体优化方案 | 不改边界 | 验收 | 优先级 |
|---|---|---|---|---|---|
| FE-R24 | `CSF Core / CSF Tiers` 分段控件过厚，与全站 Tab 不一致 | 改用内容宽度的轻量视图 Tab；选中态使用底线 / 轻底色，不再双层背板 | 不改变两个视图的业务含义和路由状态 | 控件高度与全站视图 Tab 一致；键盘左右键切换 | P1 |
| FE-R25 | 分组行、数据行、映射 chip、badge 和交替底色同时使用多种边界 | 框架分组使用中性分组行；控制项使用发丝线；只有能力映射对象使用 chip；移除无意义交替底色和重复 badge | 保留控制项层级、能力映射和业务参考来源 | 一个普通数据行不同时出现整行底色、外框和多个 badge | P1 |
| FE-R26 | 页头数量 chip 与筛选混淆 | 同 FE-R23，数量改为弱元数据；搜索、层级和映射状态才进入筛选工具条 | 不删除框架和控制项总数 | 静态统计不可点击；筛选有明确标签和清除入口 | P1 |
| FE-R27 | 左侧导航较长时当前标准入口可能不在首屏 | 复用 FE-R03 单域展开和自动滚入；标准内部框架目录独立承担同域切换 | 不合并全局导航与框架目录 | 刷新任一标准深链后，全局入口和当前框架均可见 | P0 |

### 4A.8 指南文档页

| ID | 已识别风险 | 具体优化方案 | 不改边界 | 验收 | 优先级 |
|---|---|---|---|---|---|
| FE-R28 | 大面积蓝紫渐变 hero 偏营销，正文进入过晚 | 在指南源 HTML 的下一版本中改为文档式封面：标题、版本、适用对象、更新时间和 2—3 个章节入口；封面高度控制在 160—220px | 原 HTML 仍是正文视觉权威；不得在 App 外层用 CSS 强行重写 iframe 内容 | 1440×1024 首屏可看到正文第一节；无大面积饱和渐变 | P1 |
| FE-R29 | 指南目录、外层阅读面和正文卡片形成三层容器 | App 只保留目录与隔离文档面两层；文档内部以章节和表格组织，不再逐节套卡；iframe 外层不重复标题和厚边框 | 保持同源 iframe 隔离和章节定位 | 默认可见容器层级不超过 2；目录点击定位稳定 | P1 |
| FE-R30 | 指南首屏信息密度偏松 | 文档封面后直接进入目录摘要和正文；标题、版本、更新信息紧凑排列；正文阅读列 680—760px | 不压缩正文到小字号，不改写业务内容 | 正文 14—16px，行长 65—75ch；首屏正文占比达到 50% 以上 | P2 |

### 4A.9 Issue 工作台

| ID | 已识别风险 | 具体优化方案 | 不改边界 | 验收 | 优先级 |
|---|---|---|---|---|---|
| FE-R31 | 页头 chip、筛选条、范围卡、中间 Issue 卡和 inspector 造成卡片套工作台 | 改为“范围筛选 + 紧凑队列表格 / 分隔线列表 + 按需详情”；页头状态改为筛选视图，单条 Issue 不再使用完整卡片 | 保留批量选择、导出、定位、编辑和状态流转 | 默认主区普通记录卡片数为 0；筛选和批量动作靠近队列 | P1 |
| FE-R32 | 中间队列横向滚动，信息优先级未收敛 | 默认列固定为选择、Issue 摘要、页面 / 对象、状态、优先级、更新时间；正文、来源、负责人和历史进入详情；允许列设置 | 不删除 Issue 正文和定位信息 | 1440px 队列无横向滚动；1024px 仅隐藏 P1 列 | P1 |
| FE-R33 | 未选择时右侧保持大面积空白 | 未选择时详情宽度为 0；选择后侧栏滑入，关闭后完整归还队列宽度；支持键盘上下选择和 Enter 打开 | 不默认选择 `rows[0]`，不让旧选择驱动当前详情 | 首次进入无空 inspector；关闭详情后队列列宽恢复 | P0 |
| FE-R34 | 全局侧栏保留其他业务域展开，增加认知负担 | 复用 FE-R03 单域展开；Issue 页面只展开工作台域 | 不改变其他域入口 | Issue 路由下当前域之外的二级项默认收起 | P0 |

### 4A.10 成熟度评估

| ID | 已识别风险 | 具体优化方案 | 不改边界 | 验收 | 优先级 |
|---|---|---|---|---|---|
| FE-R35 | 项目列表被标题、demo 提示、统计带、筛选、表格和永久摘要横向切碎；新建项目曾被改成全页状态或右侧抽屉，均偏离早期浮层交互 | 按 V2.1 完整规格改为紧凑页头、状态视图、单行工具条、全宽项目表和行内按需展开；删除统计卡带和永久右摘要；新建项目恢复为项目列表上方居中的三步浮层页，不切路由、不替换列表、不贴靠右侧 | 项目业务字段、状态流转和动态动作全部保留；V2.1 项目字段与三步校验不变 | 项目列表主任务占首屏；1440px P0 列完整；新建浮层四边留白、左右居中，背景列表状态不重置 | P0 |
| FE-R36 | 成熟度模块与 Apple Shell 的字体、重点层级和控件语言不一致 | 统一页面标题、目录层级、L2 汇总、关注点、评估点、正文和辅助文字层级；1—5、输入、目录状态和服务 Tab 复用 Apple Shell token，取消主业务区 10—11px 小字和三层同权重大胶囊 | 项目列表、评分、复核、结果仍按各自任务使用不同信息结构；服务 Tab 只承载对象与状态，不承载聚合分 | 1440px 可在 3 秒内识别当前 L2、当前关注点、待评分项和主动作；主业务名称不低于 14px；目录状态与当前路径语义分离 | P0 |
| FE-R37 | 全部评估项纵向平铺，位置模型分散在能力 Tab、关注点列表和服务分页中，维度标准不可见 | 使用“左侧安全能力目录 + 右侧评分工作台”；目录统一承载 L0 / L1 / L2 / FOCUS 定位，右侧保留不可折叠 L2 汇总、关注点定义、服务 Tab 和四维表单；四维使用 1—5 按钮并常驻显示当前已选等级的完整标准 | 一次只编辑当前评估点；当前对象来自目录 / 服务 Tab 的显式 ID；当前与逐层结果均由后端返回 | 目录路径、服务 Tab 和表单当前对象一致；每个维度可核对当前等级完整标准；窄屏目录退化为工作台上方限高区 | P0 |
| FE-R38 | 证据覆盖与完成度并列，且评估点缺少可直接记录判断事实的证据描述入口 | 每个评估点行内展开区固定增加“评估证据说明（可选）”文本框；无证据使用中性语义，证据覆盖进入辅助信息和报告限制说明，不进入评分完成条件 | V2.1 证据继续非必填，可关联评估点 / 维度；不自动改变评分 | 每个评估点均可输入并自动保存证据说明；空值仍可复核、计算和生成报告 | P0 |
| FE-R39 | 当前 demo 与 V2.1 项目、模板和文件交换契约不一致 | 完整执行 `maturity-assessment-v2-1-complete-frontend-design-2026-07-12.md` 的 MAT-P0 / P1 / P2，并建立旧入口到新入口的功能迁移表 | 不写主工程字典，不提前正式化 maturity 数据库或 DMG；不得因视觉重做删除现有 demo 功能 | V2.1 完整验收清单和现有 demo 功能保留清单全部通过后才能称为设计实现完成 | P0 |
| FE-R51 | 成熟度范围过大，长表难以定位且评分后重渲染跳位 | 复用安全能力映射目录作为评分主导航，目录内按 L0 / L1 / L2 / FOCUS 逐级展开；重渲染前捕获真实 `.maturity-v1-project-page` scroll owner，重渲染后重新查询新节点并在当前帧、下一帧和试算返回后恢复位置与焦点 | 不改变模板层级、源顺序或当前对象显式 ID；目录只承担定位和状态，不参与评分聚合 | 首次进入恢复当前路径；连续评分、适用性勾选和输入后页面保持原位，不跳回页首 | P0 |
| FE-R52 | 服务重复录入，关注点批量入口与聚合反馈语义混淆 | 仅当全部下级四维均未评分时开放关注点单等级初始化；选择一个等级写入全部下级四维；任一下级任一维度已有评分后锁定。评分执行页关注点头部只显示定义、数量和完成度，结果页后端聚合 | 统一设置不是父级评分；不批量设置适用性、目标或理由；不把下级汇总分回写成关注点执行页输入或反馈 | 空白下级可一次初始化；任一下级评分后批量入口禁用；单项调整不覆盖其他服务；结果页聚合仍正确 | P0 |
| FE-R53 | 四维控件只有等级数字或名称，用户容易凭总体印象重复打分 | 每个维度提供居中的 1—5 按钮、当前等级完整定义和评分依据；主区删除全部五级对照，来源元数据留在契约与审计且不显示“通用等级定义”标签 | 四维独立判定；数据维度不等于证据数量，工具维度不由服务映射或部署事实自动生成；前端不编造 rubric | 当前评估点能读取 20 条 rubric；选中等级后只显示当前完整定义；修改一个维度不联动其他维度；缺少 rubric 时阻塞 | P0 |
| FE-R54 | L2 / 关注点直接评分与下级汇总可能同时生效，导致重复计分和结果错乱 | 为每个节点增加互斥 `score_source_mode=DIRECT/CHILD_ROLLUP`；直接模式消费节点四维，汇总模式锁定父级 1—5 并只读显示下级实际统计分；切换经确认并保留非生效历史 | 不删除旧评分，不由前端猜测有效来源，不把“统一设置下级”当父级分数 | 同一 calculation run 每节点只有一个有效来源；冲突响应进入阻塞态；迁移项目结果不变 | P0 |
| FE-R55 | 方案 1 的独立 rubric 列压缩评分依据，长判断事实难以录入和复核 | 采用方案 2 评分区：三列为维度、1—5、弹性评分依据；评分依据最小 `380px`，当前 rubric 下沉为跨评分与依据列的整行并完整显示 | 不隐藏当前等级标准，不把关键标准仅放 tooltip，不在主区增加全部等级对照 | 1440px 评分依据最小宽度达标；输入两行可读；当前标准与对应维度保持相邻 | P0 |
| FE-R56 | 部分版本没有逐点评估适用性，且无服务关注点容易生成空服务或缺失评分入口 | 每个 `SERVICE / FOCUS` 固定显示是否适用；完全无服务时直接生成真实 `FOCUS` 评估点。以 `G-SP.SM-01` 固化黄金样例 | 不伪造作用域 / 服务；治理管理参考服务只作证据提示；不适用项不进入计算 | 每点评估适用性可单独覆盖；`G-SP.SM-01` 只有一个 FOCUS 评分对象；不适用原因必填 | P0 |
| FE-R57 | 视觉重做可能丢失已有前端功能，且用户可见页面仍暴露 demo / 演示身份 | 保留项目、模板、导入导出、自动保存、筛选跳转、复核、结果和报告功能；用户可见 demo 信息清零，内部 fixture ID 和开发模式只保留在实现层 | 不要求保留旧视觉、错误业务语义或用户可见重置提示；不改正式数据和交付模式 | 标题、项目、客户、提示和主操作无 demo / 演示；核心前端功能仍可恢复 | P0 |
| FE-R58 | 新建项目关闭动作被脏表单确认状态拦截，用户点击取消、关闭或 `Esc` 后仍停留在浮层 | 关闭按钮、遮罩、`Esc` 和第一步“取消”共用一个直接关闭命令；结束未创建流程、丢弃未保存表单并恢复触发按钮焦点 | 不创建新项目，不删除已存在草稿，不重置项目列表状态 | 四种退出路径均一次关闭；列表筛选、滚动和当前行保持；新项目数量不增加 | P0 |
| FE-R59 | 设计反馈可能被扩大成未授权重组 | 第二轮使用方案 1 白名单；第三轮只有用户明确点名的评分目录、服务 Tab、结果雷达、统计重排和评分动画可改变结构，其余项目步骤、表单字段和业务契约保持 | 不以 Apple Shell、响应式或 FOCUS 变体为理由删除未点名功能；不把 SERVICE / FOCUS 拼成概念板 | 本轮结构变化可逐条映射到用户六项反馈；核心功能和对象粒度无静默变化 | P0 |
| FE-R60 | 能力切换、关注点列表和服务评分区形成三套位置模型，弱字号和错位指标降低可用性 | 合并为左侧评分目录与右侧服务 Tab；目录 / 工作台使用独立表面和分隔线；目录状态、当前路径、进度和指标字号按主次对齐；`960—1439px` 目录收窄至约 `258px` | 不改能力顺序、对象粒度、项目导航或结果字段 | 1440 / 1280 / 1024 / 390px 无页面级和目录横向滚动；关注点文字可读；当前 / 完成 / 进行中 / 未开始状态可辨 | P0 |
| FE-R61 | 适用性下拉和当前下级下拉占空间、上一 / 下一服务分页无法直接判断各项状态 | 适用性改为原生方框勾选；取消当前下级评估点下拉和上一 / 下一分页，多个服务复用 Apple Shell 共享单行 Tab；桌面等分可用宽度、移除尾部作用域编码并优先一屏展示，在当前表单右上角提供“保存并转到下一项” | 不改变不适用计算边界、服务真实顺序或自动保存；窄屏才在 Tab 容器内部滚动，省略名称仍通过可访问名称提供全称 | 键盘和鼠标均可勾选适用性；不适用原因规则不变；桌面服务 Tab 尽量一屏可见并显示逐项状态，页面级无横向滚动 | P0 |
| FE-R62 | 评分过程缺少统一目录状态，当前选中与完成状态容易混用 | L0 / L1 / L2 / FOCUS 每个目录节点显示“已完成 / 进行中 / 未开始 / 不适用”语义圆点标签；文字由目录图例与 `aria-label` / `title` 提供，蓝色只表示当前路径 | 状态由真实适用评估点、完成条件和输入事实计算；不能由前端猜测后端分数 | 绿 / 黄 / 冷灰 / 不适用均可通过标签和可访问名称辨识；父节点状态与子项适用分母一致 | P0 |
| FE-R63 | 关注点或服务不适用虽退出总体分，但能力 / 能力域完成率可能仍把整组不适用当作 100% 平均 | 所有聚合层显式累计 `applicableItemCount / completedItemCount / notApplicableItemCount`；完成率只用适用评估点作分母，不适用数量单独披露 | 不适用不等于 0 分或已完成；剩余适用项按原权重比例重新归一 | 混合适用 / 不适用样例在关注点、L2、L1 / L0 和总体层的分数与完成率一致；全不适用节点状态为 N/A | P0 |
| FE-R64 | 结果页卡片、能力域条形图、成熟度分布和证据分布同时争抢首屏，且单能力四维雷达不能回答“全体系能力短板在哪里” | 首屏按“总体结论 → 左图右数的 L2 全能力分组雷达 / TGM 层级统计 → 覆盖 / 排除口径 → 能力域结果”排序；雷达用中文短名，每条轴是一项后端 L2，右侧展示 T/G/M 总体、L1 当前 / 目标、L2 数量和后端差距候选；分布和证据默认折叠 | 图表与统计只消费后端 `categoryResults / subCategoryResults / capabilityResults / gapItems`；量尺固定 1—5，前端不重新平均指数或生成正式评价；未评分保留轴且不按 0 分计算 | 全部 L2 同屏可见且三组边界清楚；左图右数可核对 L0/L1/L2；当前 / 目标线可区分；精确值在展开明细和 L2 表可查 | P0 |
| FE-R65 | 1—5 按钮选中后定义变化不够聚焦，用户难以建立评分位置与标准的对应 | 五个按钮在评分列居中；选中按钮使用约 `1.14` 倍短时平滑放大，当前等级完整定义区使用 220ms 显现动画；提供 `prefers-reduced-motion` 降级 | 不改变评分值、Rubric 文本、键盘焦点或保存逻辑；不使用持续脉冲或装饰动画 | 鼠标 / 键盘选择后完整定义立即更新；主区无“对照全部 L1—L5”；动画结束后布局稳定；减少动态效果下无动画 | P0 |
| FE-R66 | P0-2 与首次舒适度修正先后把旧 DMG 标题基线漂移为 `82px / 22px` 和 `96px / 26px` | 桌面共享页面标题区固定 `96px`，按旧 DMG 恢复 `24px / 1.13` 标题、`12px / 1.45` 说明、`12px 18px` 内边距、`5px` 文本组间距和 `7px` 标题标签间距 | 保持唯一 `h1`、共享动作槽和 P0-2 壳层归属，不回退到业务组件自带主页头；业务页不得局部覆写共享节奏 | 全局导航、安全能力映射和成熟度在同一视口下计算样式一致，标题、标签、说明不碰撞且页面无横向溢出 | P0 |
| FE-R67 | 评分执行页成为六个项目页签中唯一丢失项目资料且曾覆写项目 Tab 高度，切换后用户失去项目锚点并感到页面跳动 | 六个页签统一保留不超过 `48px` 的项目上下文行，并统一使用 `52px` 项目导航；进度、自动保存和“更多”仍由共享页头动作槽承载 | 项目上下文不是第二个 `h1`，评分页不得用页面专用选择器覆写共享导航高度 | 任意页签切换后项目名、客户、模板、更新时间和导航底线位置一致；评分页不再例外 | P0 |
| FE-R68 | 结果可视化把能力 L2 与评分四维混为同一轴粒度，只能看单个能力，无法比较全体系 | 使用 FE-R64 的 L2 全能力分组雷达；技术 / 治理 / 管理只负责分区，轴标签使用稳定中文短名，右侧补后端 L0/L1/L2 统计 | 不从服务数、证据数或其他维度推导能力分组与得分；只使用后端字段 | 所有后端 L2 都有轴；组内顺序稳定；未评分不显示为 0；完整名称、精确值和层级统计可核对 | P0 |
| FE-R69 | 评分服务切换另造卡片式标准，与 Apple Shell 已有 Tab 语言不一致 | 复用 `maintenance-section-tabs / maintenance-section-tab`，状态用圆点与可访问名称补充；桌面等分宽度并隐藏重复作用域，窄屏才内部滚动 | 不改变服务顺序、选择 ID、保存和聚合语义 | 与知识字典 / 标准等共享 Tab 的高度、圆角、选中态一致；桌面尽量一屏展示；无多行大卡片 | P0 |
| FE-R70 | 目录删除状态文字后仍为旧文字标签预留固定列宽，状态、层级和名称出现大空隙 | 桌面节点网格收敛为 `16px + max-content + minmax(0,1fr) + auto`；图标状态仅占真实圆点宽度，层级标签紧邻名称 | 保留层级轨道、缩进、展开、状态 `aria-label / title` 和右侧数量 | L0 / L1 / L2 / FOCUS 四类节点的圆点、层级、名称连续成组；长名称省略但不挤压数量 | P0 |
| FE-R71 | 服务 Tab 为坚持单行而把完整业务名压成省略号或纵向断字，且适用性远离当前对象 | Tab 显示完整对象名，并在名称后直接放逐项方框；右侧工作台不足 `1000px` 时按三列换行，移动窄屏才内部横向滚动 | 不改变真实顺序、当前选中 ID、不适用计算或自动保存 | 1440 / 1024 / 390px 可读完整当前服务名；逐项勾选与对象一一对应；页面级横向溢出为 0 | P0 |
| FE-R72 | 当前服务在 Tab 与表单头重复，展开 / 收起无任务价值，保存主动作悬在表单顶部 | 删除评分表单重复标题、适用性和展开收起区；底部左侧固定分母规则，右侧放“保存并转到下一项” | 保留平台参考、四维、目标、证据、备注、自动保存和真实下一项顺序 | 表单内展开 / 收起按钮为 0；主动作唯一且在底部；不适用规则不被自动保存状态覆盖 | P0 |
| FE-R73 | 上轮虽有缩放，但定义仍在五格下方，切换时位置关联弱且未选择占位制造噪声 | 评分行固定“维度 / 五格 / 当前定义 / 评分依据”；五格居中，选中格先 `1.14` 倍放大，定义延后约 `55ms` 在右侧显现；未选择定义位为空 | Rubric 原文、四维独立性、键盘焦点和减少动态效果偏好不变 | 切换任一等级均触发平滑状态动画；当前定义不在五格下方；未选择无解释文字；工作台无横向溢出 | P0 |
| FE-R74 | 关注点右侧把适用性、完成、适用 / 不适用数量和批量锁定拆成大按钮与散落文字 | 合并为紧凑状态面板：评分状态、关注点适用、完成、适用、不适用、已有下级评分 / 统一设置下级 | 不把子项聚合分回写为关注点分；批量初始化锁定契约不变 | 状态与数量在同一视觉组；“已有下级评分”不再伪装成大禁用主按钮 | P0 |
| FE-R75 | 项目步骤在评分长页滚动后离开视野，且目标文案“人工目标 / 目标”等多种叫法并存 | `52px` 项目步骤栏在项目滚动容器内 sticky；用户可见目标统一为“目标等级”，内部字段继续使用 `targetLevel` | 不锁定全局页头以下所有内容，不改变目标来源、必填和聚合规则 | 滚动评分表单时六个页签仍可见；成熟度组件用户可见“人工目标”计数为 0 | P0 |
| FE-R76 | 服务状态圆点、完整名称和适用性方框虽然相邻，但视觉上仍像三个分离控件，长名称被压成难读的断行 | 同一 Tab 项使用统一边框、底色和选中态；内部依次排列状态圆点、完整对象名、17px 适用性方框；桌面最多三列，窄屏才在局部容器滚动 | 不合并评分状态与适用状态，不改变服务顺序、当前 ID 或自动保存 | 6 个服务在 1280px 工作台形成 3×2 网格；方框不再成为独立蓝色大按钮；完整名称无省略号 | P0 |
| FE-R77 | 选中格仅放大 `1.14` 倍且定义位于独立列，用户无法把评分值与标准理解为同一控件 | 五个评分格组成动态分段控件；选中轨道从约 `42px` 平滑扩展到至少 `300px`，格内同时显示数字、等级名和完整定义，其余四格继续可见 | Rubric 仍来自后端；不改变四维独立性、评分值、键盘语义、保存或减少动态效果降级 | 任意 `1 → 5` 切换后只有当前格扩展；格内定义与所选等级一致；独立定义区 DOM 计数为 0 | P0 |
| FE-R78 | 只锁定 `52px` 项目页签，项目名称、客户、模板和更新时间仍随长表滚出，用户失去当前项目锚点 | 把 `48px` 项目身份区和 `52px` 六步骤页签包为一个 `100px` 整体 sticky 区域，滚动时两行保持原相对位置 | 不锁定成熟度全局页头，不改变六步骤顺序、项目字段或页面滚动 owner | 滚动前后整体区域 top 与项目页 top 相同；项目身份区、六步骤页签都持续可见 | P0 |

### 4A.11 全局搜索

| ID | 已识别风险 | 具体优化方案 | 不改边界 | 验收 | 优先级 |
|---|---|---|---|---|---|
| FE-R40 | 顶栏搜索和结果页搜索重复 | 在搜索结果路由中把顶栏搜索升级为当前查询主输入，内容区不再重复第二个搜索框；内容区只保留结果上下文、分类和筛选 | 保留全局快捷搜索、历史和 `/search?q=` 深链 | 搜索结果页可编辑查询但 DOM 中只有一个主搜索输入 | P1 |
| FE-R41 | 分类 chip 同时承担统计、导航和筛选 | 分类改为轻量 facet 列表 / Tab，显示名称和数量；筛选条件进入独立 filter；当前选中有文字和 `aria-current` | facets 计数继续来自 API 全量命中，不用当前窗口重算 | 统计不可误触为筛选；清除条件后总数和分类恢复一致 | P1 |
| FE-R42 | 结果逐条卡片化，重复高亮造成视觉噪声 | 改为分隔线结果列表；一行显示标题、对象类型、路径和一段摘要；只高亮最能解释命中的片段，重复出现不全部着色 | 保留 `route + object_type + target_ref` 精确定位契约 | 20 条结果连续扫描无卡片墙；点击定位仍到达正确业务对象 | P1 |
| FE-R43 | 搜索结果上下文容易在分页和筛选后丢失 | 查询、分类、页码和筛选写入 URL / 页面状态；sticky 工具条显示当前查询和总数；返回结果页恢复滚动位置 | 不混用全局与各业务域搜索历史 | 翻页、点击结果再返回后，查询、分类、页码和滚动位置保持 | P1 |

### 4A.12 共享组件、状态与可访问性

| ID | 已识别风险 | 具体优化方案 | 不改边界 | 验收 | 优先级 |
|---|---|---|---|---|---|
| FE-R44 | 壳层重复，主任务宽度被目录和 inspector 同时占用 | 建立“每页默认最多一个次级导航或 inspector 常驻”契约；第二辅助层改为按需抽屉、弹出目录或行内展开 | 自定义模板等确需三栏的结构编辑页可例外，但必须说明任务理由 | 1440px 普通页面不存在目录 + 永久 inspector 同时常驻 | P0 |
| FE-R45 | 容器和圆角过多 | 只保留页面工作面、独立浮层和可交互选区三类容器；普通列表使用分隔线；统一圆角 `6 / 10 / 14px` | 不取消真正需要边界的输入、浮层和画布 | 页面级嵌套大圆角容器不超过 2 层；普通记录不逐条卡片化 | P0 |
| FE-R46 | 应用生成界面的语义色混用 | 固定选择蓝、完成绿、警告金、审阅紫、错误红；服务作用域、技术 / 管理 / 标准关系色只表达对象角色，不表达状态 | 不修改 Draw.io 原图任何颜色和样式 | 同一颜色在系统生成界面只有一个主语义；状态在灰度下仍可识别 | P0 |
| FE-R47 | 页面密度和字体档位不统一 | 全局字号收敛为 12 / 14 / 16 / 24px；表格正文 13—14px；标题区、工具条、行高和段落间距形成三档密度 | 不因 Apple 风格而扩大无效留白，不降低专业表格信息密度 | 主业务文本不低于 12px；模块局部不存在 9—11px 业务正文 | P0 |
| FE-R48 | 空详情、空图、加载和错误状态语言不一致 | 建立共享状态模板：骨架加载、局部重试、可行动空态、无数据不画空图、无选择不占详情宽度 | 数据 `missing_file`、`empty`、`error` 不得被统一伪装成“暂无” | 代表页面分别覆盖 ready / empty / missing_file / error；状态文案说明下一步 | P1 |
| FE-R49 | 键盘、焦点和只靠颜色表达存在风险 | 所有 Tab、树、表格选择、评分、图谱节点、抽屉和工具条建立可见 focus ring；目录状态使用带可访问名称的图形标签；动态保存和导入使用 `aria-live` | 不以自定义 div 替代原生按钮、复选框和输入语义 | 键盘可完成代表性主流程；WCAG AA 对比度；节点标签与 `aria-label` / `title` 同步，无不可解释的纯颜色状态 | P0 |
| FE-R50 | 低频来源 / 调试字段可能进入主区 | 主区只显示业务字段；参考来源使用折叠业务参考模式；原始来源和导入技术证据只进默认折叠维护区 | 不丢失来源追踪能力 | 主区禁止字段计数为 0；展开维护证据后仍可追踪来源 | P0 |

## 5. 全局 Apple Shell 设计契约

### 5.1 视觉原则

- **Shell 有材质，内容无玻璃**：半透明 / blur 仅用于全局导航、顶栏和浮动控制层；表格、文档、画布和表单保持实色背景。
- **一屏一主任务**：页面首屏只保留一个主工作区；二级目录、详情 inspector、统计区三者最多常驻一个。
- **内容先于容器**：优先用间距、分隔线和排版组织层级；只有可交互选区、浮层和真正独立模块使用圆角容器。
- **颜色有唯一语义**：蓝色=当前选择 / 主要操作，鼠尾草绿=完成 / 通过，沙金=需要处理的警告，淡紫=当前审阅行 / 复核上下文，红色=阻断 / 错误；业务作用域色不得复用状态色含义。信息化环境 Draw.io 原图不纳入该规则。
- **工作台，不是营销页**：移除大面积蓝紫渐变 hero、虚构 KPI 装饰和视觉噱头；总览页回答“接下来做什么”，不堆“看起来很繁忙”的卡片。

### 5.2 建议 token

| 类别 | 建议值 | 用途 |
|---|---|---|
| 页面底色 | `oklch(0.972 0.008 86)` | 暖珍珠灰，降低纯白眩光 |
| 主表面 | `oklch(0.995 0.003 86)` | 表格、正文和输入区 |
| 次表面 | `oklch(0.952 0.010 86)` | 分组行、工具条、空态 |
| 主文字 | `oklch(0.245 0.025 248)` | 正文与关键数字 |
| 次文字 | `oklch(0.49 0.020 246)` | 说明、元数据 |
| 选择蓝 | `oklch(0.62 0.17 252)` | 主操作、当前选择 |
| 审阅紫 | `oklch(0.72 0.10 300)` | 当前展开行，低饱和使用 |
| 完成绿 | `oklch(0.67 0.11 155)` | 已完成、已确认、检查通过 |
| 警告金 | `oklch(0.76 0.12 78)` | 需要用户处理的警告，不用于可选证据 |
| 关键线 | `oklch(0.86 0.014 86)` | 分隔线、表格线 |
| 圆角 | `6 / 10 / 14px` | 控件 / 局部面板 / 浮层；不再自由新增 |
| 字号 | `12 / 14 / 16 / 24px` | 辅助 / 正文 / 小标题 / 页面标题；业务正文禁用 9—11px |

### 5.3 层级与组件规则

- 全局侧栏桌面宽度建议 `220—240px`，支持折叠；当前业务域自动滚入可见区，只展开一个一级域。
- 顶栏高度建议 `52—56px`，保留全局搜索、当前页面上下文和少量全局动作。
- 页面标题区使用共享 `96px` 基线；统计只在能改变决策时出现，最多 3 项并降为次级信息。
- 主按钮使用单一实色；次按钮描边；危险操作只在确认上下文中使用红色。
- chip 只表达标签、筛选值或业务作用域，不用来承载普通数字、段落或所有状态。
- 表格以分隔线为主，分组行用轻底色；不对普通数据行使用整行饱和色。
- 空详情区不常驻。宽屏可在选中后打开 inspector；窄屏改为抽屉或行内展开。

## 6. 六类页面模板

| 页面类型 | 默认结构 | 关键约束 |
|---|---|---|
| 总览 / 工作台 | 标题 + 下一步行动 + 最近对象 + 少量运行状态 | 不做 KPI 卡片墙；不重复所有导航入口 |
| 树 + 关系画布 | 可折叠树 + 自适应画布 + 浮动工具条 | 树宽按粒度收缩；选中对象自动适配画布；图谱占主视觉；碰撞预算和降级视图有明确契约 |
| 宽数据表 | 标题 + 单行工具条 + sticky 表头 + 渐进披露 | 1440px 首屏保证主字段完整；低频列进入详情；避免横向滚动作为默认路径 |
| 文档 / 指南 | 可折叠目录 + 680—760px 阅读列 + 轻量封面 | 禁止营销 hero；章节定位与来源优先 |
| 搜索 / Review Queue | 筛选工具条 + 分隔线列表 + 按需详情 | 结果不用重复卡片；无选择不占 inspector；保留键盘导航 |
| 成熟度评分 | 项目导航 + 左侧 L0/L1/L2/FOCUS 评分目录 + 右侧 L2 汇总、关注点摘要、服务 Tab 和主评分表单 | 目录状态使用语义圆点标签与可访问名称；SERVICE / FOCUS 同构；关注点单等级初始化、适用性方框、当前等级定义、保存并转到下一项、无页面级横向滚动和评分后保持原位必须可见 |

## 7. 成熟度 V2.1 完整页面体系

成熟度模块不再按“评分页局部优化”处理。V2.1 的项目列表、新建项目、固定 / 自定义模板、四维评分、文件交换、复核、客户 / 内部结果和报告快照均按独立页面任务完整设计，直接实现规格见：

- `frontend/design-handoff/implementation-specs/maturity-assessment-v2-1-complete-frontend-design-2026-07-12.md`

全局层只保留以下稳定规则：

1. 成熟度所有路由继承全局 Apple Shell：共享顶栏、侧栏、唯一页面标题和 `main-only` 工作区；项目列表与项目页不得再创建第二套主页头。
2. 项目列表是操作队列，不是 KPI 首页；全宽表格取代常驻项目摘要，主动作进入共享页头。
3. 新建项目固定评估企业组织，不提供项目作用域或评估对象类型；入口在项目列表上方打开居中的三步 workflow overlay，不使用右侧抽屉或占满工作台的创建页。
4. 评分页保持项目内步骤导航，主区改为左侧安全能力评分目录与右侧评分工作台；目录统一定位 L0 / L1 / L2 / 关注点，右侧保留 L2 汇总、关注点定义、服务 Tab 和主评分表单，无常驻右侧 inspector。
5. 每个适用评估点使用居中的 1—5 按钮填写四维当前等级，并持续完整显示当前已选等级标准；主区不显示全部 L1—L5 对照；当前总体由后端计算并只读显示。
6. 四维按各自标准独立判定当前稳定实现的最高完整等级；对象专用 rubric 优先，通用矩阵只作明确标记的回退，rubric 缺失时不得由前端生成。
7. L2 / 关注点在 `DIRECT / CHILD_ROLLUP` 中只有一个有效来源；评分执行页不回写关注点评级；结果页显示后端聚合。“统一设置下级”仅在全部下级未评分时选择一个等级初始化全部下级四维，任一维度已有评分后锁定。
8. 每个 `SERVICE / FOCUS` 显示是否适用；无安全技术服务时直接评估关注点，不生成空服务或伪作用域。
9. 评分区使用三列弹性布局，评分依据最小 `380px`，当前 rubric 紧邻维度下沉一行并完整显示；目标等级和目标理由必填，每点提供可选证据说明。
10. 客户主结果最细到 L2；关注点、服务、四维原始分和证据进入内部下钻与附录。
11. 视觉改版保留项目、模板、导入导出、筛选跳转、自动保存、复核、结果和报告功能；用户可见 demo 信息清零。
12. SERVICE 与无服务 FOCUS 分别生成一张独立设计图；FOCUS 图复制同一页面结构，只把路径、关注点列表和右侧评估对象替换为真实 `G-SP.SM / G-SP.SM-01` 内容。
13. 结果页增加固定 1—5 量尺的 L2 全能力分组雷达；全部 L2 使用中文短名并按技术、治理、管理三段同屏展示，采用左图右数，右侧展示后端 T/G/M、L1/L2 统计和差距候选评价；未评分项保留轴且不按 0 分计算，不适用项不进入任何完成率分母。

## 7A. 安全能力关注点图谱碰撞治理

### 7A.1 设计目标

图谱必须同时满足“关系完整、标签可读、布局稳定、可解释”。不能通过删除业务关系、把所有标签缩到 9—10px 或无限缩放画布来制造无碰撞假象。

### 7A.2 碰撞分类

- 节点与节点重叠；
- 标签与标签重叠；
- 连线穿过无关节点或标签；
- 跨分支连线交叉；
- 高负载分支挤压低负载分支；
- 自动适配后整体过小，虽不重叠但不可读。

### 7A.3 布局策略

1. 以当前关注点为固定中心，技术 / 管理 / 标准为稳定扇区；扇区角宽由分支负载和标签实际宽度决定，不再平均分配。
2. 先做结构化种子布局，再做节点碰撞、标签碰撞、边穿节点和边交叉四类优化；中文标签的碰撞半径按真实文本包围盒测量，不只按字符数估算。
3. 同一作用域或同一关系角色使用局部簇；共享对象只保留一个业务节点，跨簇关系使用独立端口和弧形路由。
4. 高密度叶节点默认聚合为“服务 12 / 模块 18 / 控制项 36”等可展开簇；展开簇不删除关系，完整关系同步保留在下方矩阵。
5. 当布局无法在当前宽度同时满足可读阈值时，自动进入“聚合总览”，而不是继续缩小字号；用户可进入“仅看技术 / 管理 / 标准”或“只看当前分支”。
6. 用户拖拽 / 固定属于当前会话的视图状态，不写回业务关系；提供“恢复自动布局”。

### 7A.4 交互降噪

- 默认边线低对比；悬停 / 键盘聚焦节点时，只强调一跳关系，其余降至背景。
- 点击边或节点在行内 / 按需详情中显示关系说明，不常驻空 inspector。
- 搜索定位先切到相关视图和展开对应簇，再执行居中和短暂焦点环。
- 图谱、关系矩阵和详情使用同一个当前对象 ID，避免视觉选择与数据对象错位。

### 7A.5 自动验收阈值

- `nodeOverlaps = 0`；
- `labelOverlaps = 0`；
- `edgeNodeIntrusions = 0`；
- 结构树视图 `edgeCrossings = 0`；含真实跨分支业务关系时必须低于版本化阈值并可通过视图过滤降为 0；
- 1440px 默认适配后业务标签字号不低于 `12px`；
- 布局相同输入必须稳定可重放；
- L0、L1、L2、关注点四类对象分别保留黄金样例和高负载反例。

## 7B. 信息化环境 Draw.io 不可变设计边界

1. 原始 Draw.io 的颜色、字体、节点尺寸、位置、边框、连线、分区和视觉语义全部不可修改。
2. 禁止通过 CSS filter、统一填色、字体覆盖或节点重排把原图“适配”成 Apple Shell。
3. Apple Shell 只作用于原图外部的页面壳、工具条、搜索、缩放、状态和按需详情。
4. 默认打开使用“适应画布”，搜索 / 路由定位使用独立 overlay 焦点环，不写入或覆盖 SVG 原样式。
5. 节点详情按需打开；关闭后完整归还画布宽度。详情中的作用域、服务、模块 / 措施和系统继续使用应用生成界面的业务标签规则。
6. 提供适应、100%、放大、缩小、全屏、返回上次焦点；控件不遮挡原图标题、图例和关键节点。
7. 原图过密时通过定位、局部放大和 minimap 解决，不重新设计原图。

## 8. 非成熟度修复执行路线

### 8.1 范围与执行规则

- 本轮只排工程共享前端和非成熟度页面。成熟度 `FE-R35—FE-R39、FE-R51—FE-R61` 由独立会话处理，本轮不修改、不合并验收、不计入完成率。
- 保留第 4A 章现有 P0 / P1 / P2 判断，不重新给风险改级。下表只补充同一优先级内的实施先后、依赖和完成门槛。
- 每个批次单独形成可回退 diff，先完成自动断言，再进入 5173 人工验收。普通页面视觉修改不得顺带改数据、ETL、评分、关系推断或 Draw.io 原图。
- `FE-R49` 可访问性、`FE-R50` 字段边界和 `FE-R46` 语义色不是最后统一补做的装饰项。它们先建立门禁，后续每个页面批次都必须继续通过。
- P0 未全部通过前，不启动 P2；P1 可以在不修改同一文件的前提下并行，但不得绕过 P0 壳层与对象一致性契约。

### 8.2 P0：先固化契约、壳层和阻断级交互

| 执行序号 | 风险 ID | 修复包 | 具体实施内容 | 前置依赖 | 完成门槛 |
|---|---|---|---|---|---|
| P0-1 | `FE-R08 / R12 / R46 / R49 / R50` | 正确性与安全边界门禁 | 统一 `selected_id / target_ref` 到图谱、矩阵和详情的当前对象链路；为 Draw.io 建立只允许外部 overlay 的不可变 diff；固化选择蓝、状态色和对象角色色边界；补齐原生语义、focus ring、动态状态播报和主区禁用字段审计 | 无 | L0、L1、L2、关注点四类对象一致；Draw.io 原始 SVG 样式 diff 为 0；主区禁止字段为 0；代表流程可只用键盘完成 |
| P0-2 | `FE-R02 / R03 / R44 / R45 / R47` | Apple Shell 与共享布局基座 | 收敛顶栏、页面标题区、单业务域展开、当前导航自动滚入、页面最多一个常驻辅助层；成熟度路由同样继承唯一标题、共享页头动作槽、`main-only` 工作区和居中 workflow overlay；把容器、圆角、字号、行高和分隔线收敛到共享 token 与页面模板，不用全局覆盖强改各业务表格 | P0-1 门禁已可运行 | 1440×1024 主内容首屏高度至少 650px；同页标题只出现一次；普通页面不存在目录和永久 inspector 同时常驻；成熟度项目列表不再叠加第二主页头；嵌套大圆角容器不超过 2 层 |
| P0-3 | `FE-R07` | 安全能力关注点图谱碰撞治理 | 实施分支负载扇区、真实标签包围盒、共享节点端口、弧形路由、叶节点聚合、仅看单分支和恢复自动布局；聚合只属于视图层，完整关系继续由矩阵提供 | P0-1 对象链路；P0-2 可用画布宽度 | `nodeOverlaps=0`、`labelOverlaps=0`、`edgeNodeIntrusions=0`；结构视图 `edgeCrossings=0`；1440px 标签不低于 12px；相同输入布局稳定可重放 |
| P0-4 | `FE-R27 / R33 / R34` | 标准与 Issue 的壳层派生修复 | 标准深链打开后同时保证全局标准入口和当前框架可见；Issue 默认不选 `rows[0]`、不保留空 inspector，选中后按需打开并在关闭后归还队列宽度；Issue 路由只展开工作台域 | P0-2 | 任一标准深链刷新后双层当前位置可见；Issue 首次进入详情占宽为 0；关闭详情后列宽恢复；路由和旧选择不串位 |

#### P0-1 已实施门禁

- 机器契约：`config/frontend-p0-1-correctness-boundary.json`。
- 单一执行入口：`node scripts/audit_frontend_p0_1_correctness_boundary_contract.mjs --url http://127.0.0.1:5173`。
- 当前覆盖：L0 / L1 / L2 / 关注点四个黄金对象、12 个 ViewModel 对象、19 项服务作用域色检查、Draw.io 原图 SHA-256 与 overlay-only 策略、5 类状态 token、14 个主区禁显字段、Tab / 树 / 图谱 / 环境定位的代表键盘与动态播报契约。
- 当前结论：P0-1 自动门禁已完成并接入 `frontend` 测试套件；P0-2 已在共享运行前端实施。浏览器人工键盘流和 DMG App 回归进入后续 P0 / 发布验收，不用 5173 HTTP 结果替代。

#### P0-2 已实施基座

- 机器契约：`config/frontend-p0-2-apple-shell-layout.json`。
- 单一执行入口：`node scripts/audit_frontend_p0_2_apple_shell_layout_contract.mjs --url http://127.0.0.1:5173`。
- 当前覆盖：`56px` 顶栏、`96px` 页面标题区、`24px / 1.13` 页面标题、`12px / 1.45` 说明、`12px 18px` 页头内边距、唯一页面 `h1`、单业务域展开、当前导航自动滚入、普通页面最多一个 resident auxiliary、第二辅助层 overlay、成熟度 `main-only` 工作区 / 共享页头动作槽 / 居中创建浮层、`12 / 14 / 16 / 24px` 字号和 `6 / 10 / 14px` 圆角。
- 当前结论：1440×1024 计算主区高度为 `872px`；P0-2 门禁已接入 `static` 与 `frontend` 套件。成熟度已纳入共享壳且六个项目页签保持同一项目上下文；未改变业务、评分和聚合口径，未用共享 CSS 覆盖业务表格；DMG App 回归仍按发布矩阵补证据。

#### P0-4 已实施门禁

- 机器契约：`config/frontend-p0-4-standard-issue-shell.json`。
- 单一执行入口：`node scripts/audit_frontend_p0_4_standard_issue_shell_contract.mjs --url http://127.0.0.1:5173`。
- 标准覆盖：8 个正式框架深链统一归属 `standards` 导航域；刷新或直接进入后展开全局标准入口，当前框架自动滚入，并在导航上下边界保留 `10px` 留白。
- Issue 覆盖：`workbenchSelectedIssueId` 是唯一详情当前对象；首次进入 inspector 宽度为 `0`；行点击与 `Enter / Space` 显式打开，方向键移动焦点；关闭详情后恢复队列宽度和来源行焦点。批量 checkbox 只服务批量集合，不再驱动详情；路由重入不恢复旧详情或旧批量选择。
- 当前结论：P0-4 专项门禁、滚动 owner 审计和完整 `frontend` 套件已通过；5173 应用内浏览器已覆盖初始、打开、关闭、键盘和路由重入状态。标准 / Issue 数据、用户库、路由清单与业务筛选排序未改变；DMG App 回归按发布矩阵后置。

P0 完成后应得到一个稳定底座：页面不会因为壳层挤压主任务，不会因旧选择显示错误对象，不会改写 Draw.io 设计基准，图谱高负载场景有明确降级路径，键盘与字段边界有持续门禁。

### 8.3 P1：按页面族完成核心工作流

| 执行序号 | 风险 ID | 修复包 | 具体实施内容 | 前置依赖 | 完成门槛 |
|---|---|---|---|---|---|
| P1-1 | `FE-R48` | 共享运行状态模板 | 建立 `loading / empty / missing_file / error / no-selection` 五类状态；加载使用骨架，错误提供局部重试，无选择不占详情宽度，缺文件和空数据不得合并成“暂无” | P0-2 | 代表页面五类状态可区分；状态文案说明下一步；状态切换不改变当前路由和对象 |
| P1-2 | `FE-R04—R06 / R09—R11` | 能力与环境双画布工作台 | 能力图谱按对象粒度适配 70%～85% 可用画布，合并画布头部并允许目录折叠；环境页只优化原图外部的适应、100%、缩放、全屏、返回焦点、目标定位和按需详情 | P0-1、P0-2、P0-3；环境定位继续服从 P0-1 Draw.io 门禁 | 能力图谱默认可读且目录折叠不换对象；环境无选择时详情占宽为 0；定位 overlay 不覆盖图例；关闭详情后保持缩放与焦点 |
| P1-3 | `FE-R13—R20` | LC-AP / LC-DT 宽表与渐进披露 | 建立列优先级、sticky 表头与首列、局部横向滚动提示；正文 13～14px；普通文本退出 chip；空值改为 `— / 未提供 / 不适用`；LC-DT 统一四段结构并保持阶段和场景上下文 | P0-2、P1-1 | 1440px 首屏 P0 列完整；页面级无横向滚动；业务正文不低于 13px；`/` 空值计数为 0；滚动到明细仍能识别阶段和当前场景 |
| P1-4 | `FE-R22—R26` | 知识字典与标准表格 | 分离层级中性色和选择蓝；静态数量改为弱元数据；标准视图使用轻量 Tab；普通行只保留发丝线，能力映射对象才使用 chip；移除无意义交替底色和重复 badge | P0-2、P0-4 | 未选择分组行不使用选择蓝；静态数量不可聚焦；标准视图键盘可切换；普通行不同时出现整行底色、外框和多个 badge |
| P1-5 | `FE-R31 / R32 / R40—R43` | Issue 队列与全局搜索 | Issue 改为紧凑队列表格或分隔线列表，收敛默认列；搜索结果页只保留一个主查询输入，facet 与筛选分离，结果改为分隔线列表并把查询、分类、页码、滚动写入可恢复状态 | P0-2、P0-4、P1-1 | 1440px Issue 队列无横向滚动；普通 Issue 记录卡片数为 0；搜索页主输入为 1；20 条结果可连续扫描；返回结果页后查询、页码和滚动恢复 |
| P1-6 | `FE-R28 / R29` | 指南阅读页去营销化 | 下一版指南源 HTML 使用文档式封面，控制封面高度；App 只保留目录和隔离文档面两层，不在外层 CSS 强行改写 iframe 正文 | P0-2 | 1440×1024 首屏可看到正文第一节；无大面积蓝紫渐变；默认容器层级不超过 2；章节定位稳定 |

P1 每完成一个页面族，就执行该页面轻量 smoke、字段边界、键盘主路径、1440 / 1024 / 390px 布局检查。环境和能力页面还必须分别重跑 Draw.io 不可变审计与图谱碰撞审计。

### 8.4 P2：收口任务引导与整体完成度

| 执行序号 | 风险 ID | 修复包 | 具体实施内容 | 前置依赖 | 完成门槛 |
|---|---|---|---|---|---|
| P2-1 | `FE-R01` | 总览任务化 | 用“继续工作、最近访问、需关注事项、知识域入口”替代指标卡墙；只保留会改变用户决策的数字，不重复左侧完整导航 | P0、P1 主页面入口稳定 | 首屏可直接继续最近任务；大型统计卡为 0；入口不与侧栏机械重复 |
| P2-2 | `FE-R21` | 知识字典首次进入与定位 | 默认展开到 L1，提供全部展开、收起到 L0、最近定位；搜索命中自动展开祖先路径 | P1-4 | 首次进入可见 L0 与 L1；搜索能定位具体业务行；大面积空白有任务提示 |
| P2-3 | `FE-R30` | 指南阅读密度 | 紧凑排列版本、适用对象和更新时间，封面后直接进入目录摘要与正文，阅读列保持 680～760px | P1-6 | 首屏正文占比超过 50%；正文 14～16px；行长 65～75ch |
| P2-4 | 非新增风险 | 全工程回归收口 | 按同源 shared runtime 完成 Web 视觉回归、响应式、对比度和键盘验收；DMG App 在后续发布矩阵中补做，不用 5173 结果替代 App 结论 | P0、P1、P2-1～P2-3 | 5173 全部自动门禁通过；无页面级横向溢出；对比度达到 WCAG AA；明确列出 DMG App 未覆盖项 |

### 8.5 推荐实施单元

每个批次按同一顺序收口，避免视觉修改与数据问题混在一起：

1. 复核对应 `FE-Rxx`、页面类型和不改边界；
2. 先补或更新自动断言，再修改共享 token、组件或页面；
3. 只改一个页面族，不跨批次顺手清理；
4. 运行语法、专项审计、内容 smoke、页面 smoke 和 5173 状态检查；
5. 在 1440px 完成主流程人工验收，再检查 1024px 和 390px 退化；
6. 记录影响面为 `shared runtime`，DMG App 结论进入发布验收矩阵，不在本轮文档任务中提前宣称通过。

## 9. 验收矩阵

本轮修复队列只使用下表中的非成熟度验收项；成熟度相关行继续保留为独立模块契约，但不计入本轮完成率。

| 验收项 | 通过标准 |
|---|---|
| 主任务宽度 | 1440px 下成熟度、宽表和 Review Queue 的主字段不因永久空详情区被裁切 |
| 壳层数量 | 任一页面默认最多一个次级导航或 inspector 常驻；不得二者同时出现 |
| 容器密度 | 普通记录使用分隔线列表 / 表格，不使用逐条完整卡片 |
| 排版 | 业务正文不小于 12px，核心正文优先 14px；标题层级不超过 4 档 |
| 状态色 | 蓝 / 绿 / 金 / 紫 / 红含义固定；业务作用域色不冒充状态色 |
| 键盘 | Tab 顺序与视觉顺序一致；评分 1—5、筛选、展开、保存均可键盘完成 |
| 对比度 | 普通文字和交互状态达到 WCAG AA；不能只靠颜色表达状态 |
| 响应式 | 1440、1024、390px 无页面级横向溢出；数据表局部滚动必须有视觉提示 |
| 字段边界 | 主展示区不得出现 `sheet`、`row`、`column`、`raw_value`、`source_file`、`import_id`、`source_id`、`source_ref`、`source_label`、`debug`、`raw`、`metadata`、`intermediate`、`generated_at` |
| 数据契约 | 视觉改造不得在前端重新推断 ETL、评分、映射或对象粒度 |
| 成熟度评分来源 | 每个 L2 / 关注点在 `DIRECT / CHILD_ROLLUP` 中只有一个有效来源；评分执行页不回写关注点评级，结果页继续使用后端聚合 |
| 成熟度批量与交互 | 全部下级未评分时只选一个等级初始化四维；任一下级任一维度已评分后锁定；服务完整名称后直接勾选适用性；工作台不足 `1000px` 时服务 Tab 三列换行；评分格居中，选中格先放大再在右侧显示当前完整定义；底部保存并转到下一项后页面保持原位 |
| 成熟度视觉层级 | 左侧评分目录与右侧工作台分层；目录节点以绿 / 黄 / 冷灰圆点标签表达状态，图例与可访问名称补充文字，蓝色只表示当前路径；目录、进度与指标对齐可辨 |
| 成熟度功能保留 | 核心前端功能无静默删除；用户可见 demo 信息清零 |
| 成熟度结果与统计 | L2 全能力分组雷达固定 1—5 量尺、中文短名并按技术、治理、管理分段；左图右数可核对后端 T/G/M、L1 与 L2 统计及候选评价；当前 / 目标精确值可见，未评分不按 0 分计算；适用、完成、待完成、不适用数量和分母说明独立呈现 |

## 10. 页面健康度摘要

| 页面类型 | 当前健康度 | 修复优先级 | 本轮定位 |
|---|---|---|---|
| 全局应用壳 | 可用，但顶栏、标题、侧栏和辅助层挤压主任务 | P0 | 先做共享底座 |
| 总览页 | 可用，视觉模板化，任务入口不够直接 | P2 | 壳层和核心页稳定后收口 |
| 安全能力映射 | 对象链路已有基础，碰撞和高负载可读性仍是高风险 | P0 + P1 | P0 先保正确和无碰撞，P1 再收布局 |
| 信息化环境映射 | 原图基准完整，overlay 边界和外部控制层待优化 | P0 + P1 | P0 保证原图不可变，P1 优化定位、工具条和详情 |
| LC-AP | 数据完整，横向密度过高 | P1 | 宽表优先级与渐进披露 |
| LC-DT | 结构较好，局部边界和节奏不统一 | P1 | 四段工作流与上下文保持 |
| 知识库字典 | 层级色和统计语义待统一，默认态任务感弱 | P1 + P2 | P1 修层级与元数据，P2 修首次进入 |
| 安全标准 / 框架 | 深链导航可见性和表格边界待统一 | P0 + P1 | P0 保证定位，P1 收表格语言 |
| 指南 | 可读，hero 与产品基线冲突 | P1 + P2 | P1 去营销化，P2 调阅读密度 |
| Issue 工作台 | 功能完整，空 inspector、卡片化和列宽分配影响主任务 | P0 + P1 | P0 释放主宽度，P1 重排队列 |
| 全局搜索 | 定位契约完整，输入、facet 和结果视觉语义混合 | P1 | 单输入、轻 facet、分隔线结果 |
| 共享状态与可访问性 | 已有局部实现，缺少全页面族统一门禁 | P0 + P1 | P0 建门禁，P1 建状态模板 |
| 成熟度 | 独立会话继续修复 | 独立轨道 | 不进入本轮队列和完成率 |

## 11. 禁止事项

- 不新增蓝紫渐变 hero、玻璃内容卡、发光边框或纯装饰图表。
- 不把所有信息都做成 chip、badge、圆角卡片或永久 inspector。
- 不因“Apple 风格”而大量留白；SAPD 是专业信息系统，密度应高效但有节奏。
- 不用前端样式掩盖数据粒度、空值、字段泄露或加载错误。
- 不让新模块继续复制独立 token；先进入共享 token 和页面模板。
- 不修改信息化环境原始 Draw.io 图的颜色、字体、位置、尺寸、边框、连线或图例。
- 不以删关系、隐藏关键对象或缩小到不可读字号的方式宣称图谱“无碰撞”。

## 12. 取证、限制与外部参考

- 真实底板：`http://127.0.0.1:5173/` 当前工程，审计视口 `1440 × 1024`。
- 稳定截图证据已覆盖能力关系、LC-AP、能力字典、指南、成熟度项目列表和成熟度评分页；其余页面在运行态完成结构检查。
- 应用内浏览器对部分半透明顶栏生成黑块截图伪影；这些截图被拒绝，黑块未作为产品问题或设计结论。
- 参考原则来自 [Apple Materials](https://developer.apple.com/design/human-interface-guidelines/materials)、[Apple Sidebars](https://developer.apple.com/design/human-interface-guidelines/sidebars)、[SAP Fiori Flexible Column Layout](https://experience.sap.com/fiori-design-web/flexible-column-layout/)、[SAP Fiori Toolbar](https://experience.sap.com/fiori-design-web/toolbar-overview/)、[SAP Fiori Table](https://experience.sap.com/fiori-design-web/table-overview/) 和 [Carbon Data Table](https://carbondesignsystem.com/components/data-table/usage/)。外部参考只用于验证交互与信息架构原则，不覆盖 SAPD Wiki 的既有业务、字段和数据契约。

## 13. 运行面影响

- 影响分类：`shared runtime` 设计方案；若实施，将同时影响 Web 和 DMG App 的共享前端。
- 根因层：`shared frontend / design system`。
- 本轮验证范围：5173 真实页面、应用内浏览器取证、静态设计契约和成熟度业务审计；已改共享运行代码，但未做 DMG App 回归，因此不构成发布验收结论。
