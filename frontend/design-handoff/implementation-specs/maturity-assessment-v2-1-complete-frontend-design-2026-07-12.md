# SAPD 成熟度评估 V2.1 完整前端设计

状态：`implemented-in-controlled-demo / implementation-source`

最后修改日期：`2026-07-14`

读者：项目负责人、成熟度模块前后端实现人员、测试与验收人员。

用途：把《SAPD 成熟度评估业务设计 V2.1》转换为当前 SAPD Wiki 真实工程可实施的完整页面体系、交互规则、状态模型和验收口径。本文件是成熟度 demo 后续优化的直接实现规格。

维护入口：`frontend/design-handoff/implementation-specs/`。当 V2.1 页面、接口与测试全部稳定并合并进长期成熟度设计基线后，本文件转为版本交接材料。

2026-07-13 选定方案增量：用户确认以 Product Design 方案 1 为完整页面底板。方案 1 的页面壳、项目内导航、L0 / L1 / L2 能力切换区位置、不可折叠 L2 汇总、当前聚焦评估点摘要、当前 L2 关注点列表和主评分表单均为不可变结构；只允许实施用户明确列出的局部修改。评分区替换为方案 2 的“维度 + 1—5 + 加长评分依据、当前标准下沉一行”布局，并新增评分来源互斥、逐点评估适用性和无服务 `FOCUS` 状态。SERVICE 与无服务 FOCUS 必须各输出一张独立设计图，不得拼接，也不得为 FOCUS 另造页面样式。

2026-07-13 运行实现联动：受控 demo 已将评分执行页切换为 `左侧 L0 / L1 / L2 / 关注点评分目录 → 右侧当前 L2 汇总 / 关注点摘要 / 服务 Tab / SERVICE 或 FOCUS 四维表单`。四维使用独立 1—5 按钮，每个评估点由 API / ViewModel 返回 20 条 Rubric；页面不生成评分标准和聚合分。新建项目的关闭按钮、遮罩、`Esc` 和第一步“取消”均直接结束未创建流程并回到原项目列表，不再进入无法退出的内联确认状态。

2026-07-13 交接收口联动记录：本轮无业务规则、评分算法、聚合口径、模板结构或对象粒度变化；原因是本轮只落实既有视觉与退出契约。运行样式将关注点名称、维度名称、当前 Rubric、评分依据和目标 / 证据标签恢复到可读层级，评分表单底部主动作在页面滚动后保持可见；同时删除旧关闭确认状态的残留代码 / 样式，并把关闭按钮、遮罩、`Esc` 和第一步“取消”纳入同一静态与浏览器验收。

2026-07-13 第二轮业务验收联动记录：本轮有评分交互与执行页反馈规则变化。以用户九张截图和八项反馈为最终裁定：评分后保持原位；评估点列表与服务评分区分层；能力切换无原生横向滚动条；适用性方框勾选；当前下级评估点不使用下拉菜单；只显示当前等级定义；关注点单等级初始化；下级已有任一维度评分后锁定；评分执行页不回写关注点评级，结果页后端聚合；用户可见 demo 信息清零。模板结构、四维算法、正式数据和结果页聚合口径不变。

2026-07-13 真实工程复刻联动记录：本轮无业务规则、评分算法、聚合口径、模板结构或对象粒度变化；原因是只将已确认的方案 1 几何层级忠实落到真实 App Shell。评分执行页取消重复项目详情头，进度 / 自动保存和“更多”进入 App Shell 页头；搜索、筛选、能力跳转、导入导出和返回项目列表全部保留在“更多”浮层内。L0 / L1 / L2、L2 汇总、关注点摘要、关注点列表与 SERVICE / FOCUS 四维表单保持原顺序；运行页面保留真实关注点数量和真实后端汇总，不复制视觉稿中的样例数量或数值。SERVICE 与 FOCUS 已分别形成独立运行截图并纳入 `design-qa.md`。

2026-07-13 第三轮评分目录与结果可视化联动记录：本轮用户明确授权以安全能力映射模块目录替代原横向能力切换与关注点列表，前述“方案 1 不可变区”仅保留为历史验收记录，不再约束评分主区。评分执行页形成唯一的位置模型：左侧目录统一承载 L0 / L1 / L2 / 关注点定位，节点用绿 / 黄 / 冷灰 / 中性灰状态标签表达完成、进行中、未开始与不适用；右侧使用服务 Tab 切换当前评估点，评分表单右上角提供“保存并转到下一项”。关注点与安全技术服务标记不适用后，从当前分、目标分、能力聚合和所有完成率分母中排除，只单独披露排除数量。结果页新增固定 1—5 量尺的 L2 能力四维雷达，把总体结论、能力雷达、覆盖 / 排除口径、能力域对比和 L2 明细分层呈现。该变更不改变模板结构、Rubric 来源、四维评分公式或正式数据边界。

2026-07-13 评分目录标签微调联动记录：用户验收要求目录节点的状态标签只保留视觉标签，不重复显示“已完成 / 进行中 / 未开始 / 不适用”文字。节点状态改为可访问的 `aria-label` + `title` 加语义色圆点，目录图例、评分工作台状态和结果统计继续保留文字说明；本轮无业务规则、评分算法、聚合口径、模板结构或数据边界变化。

2026-07-13 P0-2 Apple Shell 联动记录：成熟度模块不再作为共享壳层例外。项目列表、项目概览、模板、评分、复核、结果和报告路由统一继承全局顶栏、侧栏、唯一页面标题和 `main-only` 工作区；项目列表的“更多 / 新建评估项目”进入共享页头动作槽，项目页只允许紧凑的项目上下文，不得创建第二个主页头。新建项目继续使用项目列表之上的居中三步 workflow overlay，并复用全局 `14px` 浮层圆角。该联动只调整壳层归属和视觉容器，不改变项目字段、评分、聚合、模板或数据规则。

2026-07-13 Apple Shell 舒适度与结果粒度修正联动记录：本轮修正 P0-2 实施中五处偏差。共享页面标题区恢复为 `56px` 顶栏 + `96px` 页面标题区，并经后续旧 DMG 精确对照确定 `24px / 1.13` 标题、`12px / 1.45` 说明、`12px 18px` 内边距和 `5px` 文本组间距；六个项目页签全部保留同一条不超过 `48px` 的项目上下文，评分执行不再例外。当前关注点的下级切换复用 Apple Shell 共享服务 Tab，单行、内容宽度、内部横向滚动，不再另造卡片标准；评分目录状态圆点与层级标签紧邻业务名称，删除状态文字后不得继续预留文字列宽。结果页使用 **L2 全能力分组雷达**：后端 `capabilityResults` 的全部 L2 轴按 `topCategoryId` 分成技术、治理、管理三段，当前值与人工目标固定在 1—5 量尺；未评分能力保留轴和中性状态，明确不按 0 分计算。该变更只调整前端信息架构和表达粒度，不改变项目字段、模板、Rubric、评分、聚合、API projection 或正式数据。

2026-07-13 旧 DMG 标题视觉真值联动记录：成熟度项目列表和全部项目页继续继承全局 Apple Shell，不新增成熟度专用标题样式。共享页头以旧 DMG `0.1.7` 为视觉真值：页头 `96px`，标题 `24px / 1.13`，说明 `12px / 1.45`，面包屑 `12px / 1.375`，页头内边距 `12px 18px`，文本组间距 `5px`，标题与标签间距 `7px`。本轮只修复共享视觉 token 与审计；成熟度业务、评分、聚合、模板、Rubric、API projection 和数据规则均无变化。

2026-07-13 评分交互与结果统计微调联动记录：本轮统一六个项目页签的 `52px` Apple Shell 导航高度，评分执行不再覆写项目 Tab 高度。共享服务 Tab 在桌面端等分可用宽度、优先一屏展示全部真实评估点，去除与当前对象重复的尾部作用域编码；窄屏才启用 Tab 容器内部滚动。四维 1—5 按钮在评分列居中，选中项约 `1.14` 倍平滑放大，定义区只显示当前等级完整 Rubric，不再提供“对照全部 L1—L5”。结果页采用“左图右数”：左侧全 L2 雷达使用中文短名，右侧直接展示后端 L0（T/G/M）、L1 能力域、L2 数量 / 低于目标数量和后端差距候选评价。前端只做计数、分组与排序表达，不重算指数、目标或正式结论；本轮无业务评分、聚合、模板、API projection 或正式数据规则变化。

2026-07-13 第五轮评分工作台验收纠偏联动记录：本轮以用户六张截图和逐条裁定覆盖此前与之冲突的服务 Tab、评分定义和操作位置描述。安全技术服务 Tab 必须显示完整对象名，并把逐项“是否适用”方框紧跟名称；右侧工作台不足 `1000px` 时使用三列换行，不得用纵向断字或省略号换取单行。关注点右侧统一为状态、完成、适用、不适用和“已有下级评分”紧凑面板。当前评估点不再重复渲染服务标题 / 适用性 / 展开收起 / 顶部主动作区；五个评分格在评分列居中，选中格先以约 `1.14` 倍平滑放大，再在同一行右侧显示当前等级完整定义，未选择时不显示提示文字。用户可见“人工目标”统一为“目标等级”。“保存并转到下一项”移到表单底部，并与“不适用项不会进入评分、聚合或完成率分母。”同栏；六个项目页签滚动后保持锁定。该变更只调整 shared frontend 页面信息架构、控件位置和文案，不改变 `targetLevel` 字段、评分、聚合、Rubric、API projection 或正式数据。

2026-07-14 第六轮评分交互重新裁定：用户明确否决第五轮对三项描述的实现解释，本节覆盖所有与之冲突的旧记录。第一，安全技术服务的状态圆点、完整对象名和适用性方框必须形成同一 Tab 项，方框紧随对象内容，不得表现成脱离标签的大块独立按钮；桌面使用最多三列的紧凑 Tab 网格。第二，等级定义不再占用独立列或相邻区域；选择 1—5 后，选中评分格从约 `42px` 平滑扩展到至少 `300px`，数字、等级名和完整定义全部位于选中格内部，其余四格继续可见并可直接切换。第三，锁定对象是项目身份区与六个步骤页签组成的完整 `100px` 区域，不是仅锁定 `52px` 页签。以上只修正 shared frontend 控件归属、动效和滚动锚点，不改变评分、聚合、Rubric、`targetLevel`、适用分母、API projection 或正式数据。

### 评分目录状态契约

- `已完成`：当前节点全部适用评估点均完成四维、人工目标和目标理由；目录节点使用绿色圆点标签，文字通过 `aria-label` / `title` 保留。
- `进行中`：至少一个适用评估点已开始但尚未全部完成，或节点内同时存在适用与不适用项；目录节点使用黄色圆点标签，文字通过 `aria-label` / `title` 保留。
- `未开始`：当前节点的适用评估点尚无评分、目标、依据或证据输入；目录节点使用冷灰圆点标签，文字通过 `aria-label` / `title` 保留。
- `不适用`：节点下没有适用评估点；目录节点使用中性灰标签，文字通过 `aria-label` / `title` 保留；不计 0 分，不进入任何上层分数与完成率分母。
- 当前选中路径使用蓝色边界与底色表达，不能与完成状态共用同一个视觉语义。

## 1. 权威源与本轮边界

- 业务权威源：`SAPD_成熟度评估业务设计_V2.1_20260712.md`。
- 视觉权威源：当前 SAPD Wiki Apple Shell、全局设计基线、2026-07-11 用户确认的纵向评分矩阵方向。
- 当前 demo 只用于验证页面与业务闭环，继续只读主工程字典；不得写正式 SQLite、正式 JSON、源 Excel、用户库或 DMG。
- 当前实现与 V2.1 冲突时，以 V2.1 为准；历史 V1/V2 设计只作差异参考。

### 1.1 三文档联动维护契约

成熟度模块的业务、交互、页面或验收规则发生变化时，本文件必须与以下两份文件在同一任务中同步维护：

1. `SAPD_成熟度评估业务设计_V2.1_20260712.md`：维护业务流程、模板、评分、聚合、结果和报告口径；
2. `frontend-global-optimization-plan-2026-07-11.md`：维护成熟度页面对全工程布局、控件、密度、状态与共享组件规则的影响。

禁止只修改运行代码或单份设计文档。若变更不影响某一层，仍需在对应文件中明确记录“本轮无规则变化”及原因。

## 2. V2.1 对现有 demo 的强制变化

| 主题 | 当前 demo / 旧设计 | V2.1 页面契约 |
|---|---|---|
| 评估对象 | 存在对象类型、对象名称或项目作用域概念 | 固定为企业组织，不显示对象类型选择，不配置项目作用域 |
| 项目字段 | 项目信息较简化 | 完整覆盖客户组织、行业、规模、特点、偏好与约束、负责人、时间和成员 |
| 固定模板 | 可能按项目作用域生成评估项 | 结构完全只读，只按字典真实关注点—作用域—服务映射生成 |
| 技术评估点 | 服务与关注点规则不够显式 | 有真实服务映射时每条映射生成 `SERVICE`；完全无映射才生成 `FOCUS` |
| 治理 / 管理 | 关联服务可能被当作评分项 | 关注点为最小评分项；服务只作为 `PLATFORM_EVIDENCE_REFERENCE` |
| 自定义模板 | 主要调整能力和关注点 | 完整支持 L0/L1/L2/关注点、作用域、服务、角色和映射；L0 可删除 |
| 当前评分 | 页面存在四维评分，但信息层级分散 | 四维是唯一当前评分输入；系统当前指数和等级只读 |
| 上层评分来源 | 关注点主要作为下级统一设置和聚合反馈，L2 主要作为聚合层 | L2 与关注点均可在 `DIRECT` 直接评分和 `CHILD_ROLLUP` 下级汇总中二选一；一个 calculation run 只消费一个来源 |
| 上层汇总反馈 | 统一设置与聚合反馈容易共用 1—5 控件 | 评分执行页不回写关注点评级；`CHILD_ROLLUP` 的统计分只在后端结果聚合与结果页展示。“统一设置下级”仅在全部下级未评分时选择一个等级初始化子项四维草稿 |
| 目标 | 目标等级已有，理由权重不足 | 每个适用评估点的目标等级与目标理由均为必填 |
| 证据 | “待补证据”容易被理解为阻塞 | 证据明确标记“可选”；无证据为中性信息，不阻止复核、计算和报告 |
| 客户结果 | 结果层级混合 | 客户主展示最细到 L2；关注点、服务、四维原始分和证据进入内部下钻 / 附录 |
| 文件交换 | 入口和类型不够清晰 | 固定评分、自定义模板结构、自定义评分三类任务分别校验和展示行级结果 |

## 3. 模块信息架构

### 3.1 一级入口

```text
工作台
└─ 成熟度评估
   ├─ 项目列表
   ├─ 新建项目
   ├─ 项目工作区
   │  ├─ 项目概览
   │  ├─ 模板
   │  ├─ 四维评分
   │  ├─ 复核
   │  ├─ 结果
   │  └─ 报告
   └─ 文件交换（按需抽屉 / 任务页）
```

### 3.2 项目工作区状态导航

- `draft`：默认落在项目概览，主动作是“选择模板”。
- `template_configuring`：默认落在模板页，主动作是“校验并发布版本”。
- `scoring`：默认落在四维评分，主动作是“继续评分”或“提交复核”。
- `score_review`：默认落在复核页，主动作是“确认复核”或“退回修改”。
- `completed`：默认落在结果页，主动作是“生成报告快照”。
- `reported`：默认落在报告页，主动作是“查看 / 导出报告”。
- `archived`：只读打开项目概览，保留历史结果和报告快照入口。

## 4. 项目列表完整设计

### 4.1 页面任务

项目列表不是统计首页，而是成熟度项目的操作队列。用户必须能在一屏内判断：哪个项目需要我做什么、做到哪一步、下一步从哪里继续。

### 4.2 页面结构

1. **紧凑页头**：标题“成熟度评估”、一句业务说明、主按钮“新建评估项目”。次级全局操作放入“更多”，包括模板管理、导出项目包和历史导入任务。
2. **状态视图**：`进行中 / 待复核 / 已完成 / 全部`。这是项目状态筛选，不做大型统计卡。
3. **单行工具条**：项目 / 客户搜索、模板类型、负责人、行业、更新时间、列设置、清空筛选。
4. **全宽项目表**：默认占据主区全部宽度，不常驻右侧项目摘要。
5. **按需项目预览**：点击项目行后在该行下方展开简要动作区；也可直接进入项目。无选择时不预留空白 inspector。

### 4.3 默认列与优先级

| 优先级 | 列 | 表达方式 |
|---|---|---|
| P0 | 项目 / 客户 | 项目名称为主行；客户组织、行业和规模为副行 |
| P0 | 状态 / 下一步 | 状态文字 + 下一步动作，不只显示颜色 |
| P0 | 模板 | 模板名称；`固定` / `自定义` 为轻量标签 |
| P0 | 当前 / 目标 | `L2 2.38 → L3 3.20`；未正式计算显示“未计算” |
| P0 | 评分完成度 | 细进度条 + `126 / 185`，完成定义包含四维、目标和理由 |
| P0 | 待复核 | 数量；0 时降为弱文本 |
| P0 | 最近更新 | 相对时间 + 完整时间 tooltip |
| P0 | 操作 | 单一动态主动作 + 更多菜单 |
| P1 | 目标达成率 | 默认可见于宽屏；窄屏进入展开区 |
| P1 | 项目负责人 | 宽屏可见；可作为筛选 |
| P2 | 证据覆盖率 | 默认不单独占列；放入展开区，明确“辅助信息” |

### 4.4 动态操作

| 状态 | 行内主动作 | 更多操作 |
|---|---|---|
| 草稿 | 继续设置 | 编辑项目信息、删除草稿 |
| 模板配置中 | 继续配置 | 导出 / 导入模板结构、查看校验 |
| 评分中 | 继续评分 | 导出 / 导入评分、复制历史草稿 |
| 待复核 | 进入复核 | 查看评分、导出评分表 |
| 已完成 | 查看结果 | 生成报告、导出项目包 |
| 已生成报告 | 查看报告 | 查看结果、导出项目包、归档 |
| 已归档 | 查看快照 | 恢复到列表视图，不允许改历史评分 |

### 4.5 行内展开内容

- 项目负责人、评估人员、计划时间；
- 模板 / 知识快照 / 算法版本；
- 当前、目标、差距、目标达成率；
- 评分完成度、不适用项数量、证据覆盖率（辅助信息）；
- 最近一次导入结果和报告快照；
- 与当前状态匹配的 1 个主动作和不超过 3 个次动作。

### 4.6 页面状态

- 首次空态：说明“从企业组织项目开始”，提供“新建评估项目”，不展示空表和 0 值指标。
- 搜索空态：保留筛选上下文，提供“清空筛选”。
- 加载态：表头保留，行使用骨架；不在页面中央只放 spinner。
- 错误态：说明项目列表或单项摘要哪个失败，支持局部重试。
- 已归档视图：默认只读，状态和报告快照清楚可见。

## 5. 新建项目完整设计

### 5.1 承载方式：居中浮层页

恢复成熟度模块最早使用的浮层交互基准：点击“新建评估项目”后，在项目列表上方弹出一个居中的大尺寸浮层页。它不是右侧抽屉，也不是替换整个工作台的独立创建页。

- 复用工程早期的 `.maturity-v1-modal-backdrop + .maturity-v1-modal` 结构语义：遮罩覆盖当前应用视口，浮层水平、垂直居中。
- 桌面端浮层宽度为 `min(780px, calc(100vw - 48px))`，最大高度为 `calc(100dvh - 48px)`；上下左右必须留出可见背景，不贴靠任何一侧。
- 项目列表、筛选和应用壳继续保留在浮层后方，使用低噪声半透明遮罩弱化；背景暂停交互，但不得卸载、重排或重置状态。
- 浮层具有完整四边边框、`8px` 圆角和克制阴影，视觉上是一张独立工作页，不是普通小型确认框。
- 浮层内部固定为四段：标题区、步骤条、当前步骤内容区、底部操作区。只有内容区滚动，标题、步骤和操作始终可见。
- 标题区显示“新建评估项目”、`第 1 / 3 步` 和关闭按钮；步骤条为 `客户与项目 → 选择模板 → 确认创建`。
- 底部操作区每一步只保留一个主动作。第一步为“下一步：选择模板”，第二步为“下一步：确认信息”，第三步为“创建并进入项目”；“返回”和“取消”为次动作。
- 浮层打开、前后步切换和关闭不改变项目列表的筛选、排序、分页与滚动位置。
- 同一时刻只允许存在一个创建浮层，不叠加第二层弹窗，也不再出现右侧抽屉或新的全页向导。

### 5.2 第一步：客户与项目

浮层宽度允许使用紧凑双列字段网格，一次只完成项目事实。长文本字段横跨两列，计划开始 / 结束时间成对展示；小屏自动退化为单列。

- 项目名称（必填）；
- 客户组织 / 客户信息（必填）；
- 客户所属行业（必填）；
- 企业规模（必填）；
- 客户特点（可选，长文本）；
- 客户偏好与约束（可选，长文本）；
- 项目负责人（必填）；
- 计划开始 / 结束时间、评估人员、备注（可选）。

点击“下一步”时只校验本步字段。错误显示在对应字段下方，焦点移动到第一个错误；已填写内容不丢失。

禁止出现：项目作用域、评估对象类型、部门 / 系统 / 业务域 / 专题对象、项目默认目标等级。

### 5.3 第二步：选择评估模板

模板选择使用纵向选项列表，不做大卡片墙，也不在本步展开完整模板编辑器。

- 固定知识库模板：清楚显示“只读结构”“按知识快照真实关系生成评估点”；可查看版本、快照、L0/L1/L2/关注点 / 评估点数量，不展示编辑按钮。
- 自定义模板：可以选择已发布模板，或“从固定模板复制为新自定义模板”；明确创建后将进入模板配置。
- 每个模板选项只展示影响决策的字段：类型、版本、来源、适用说明、评估点数量、最近发布。
- 未选择模板时禁用“下一步”；模板摘要加载失败时保留当前选择和第一步输入，支持在浮层内局部重试。

### 5.4 第三步：确认并创建

使用分组定义列表汇总已填写信息，不重复展示可编辑长表单。每组提供“修改”入口，直接返回对应步骤并保留全部内容。

- 汇总客户、项目、负责人和模板；
- 明确评估对象固定为企业组织；
- 明确固定模板作用域来自字典映射；
- 自定义模板提示需先完成校验和发布；
- 主按钮为“创建并进入项目”，不再同时出现“保存”“保存并计算”等多个蓝色动作。

创建成功后关闭浮层，并进入新项目对应的状态页面。创建失败时浮层保持打开，所有输入与当前步骤保持不变；字段错误就地提示，系统错误显示在底部操作区上方，并提供重试。

### 5.5 关闭、草稿与键盘规则

- 关闭按钮、背景遮罩、`Esc` 和第一步“取消”在任意步骤都直接关闭浮层，并把焦点返回“新建评估项目”；它们不创建项目，也不写入新草稿。
- 关闭时丢弃本次尚未保存的创建表单内容；若本次是继续编辑项目列表中已有草稿，则只放弃本次未保存变化，不删除已经存在的草稿记录。
- 不在关闭路径中展开内联确认区或第二层模态框。需要保存草稿时必须由用户触发独立、明确的“保存草稿”命令，不能把“取消”解释为保存。
- 浮层使用焦点约束，Tab 不进入被遮罩的项目列表；步骤条使用 `aria-current="step"`，错误提示与字段建立可访问关联。
- 动效只表达状态：遮罩与浮层以 `160—200ms` ease-out 淡入，浮层最多伴随 `6px` 的纵向归位；不得使用横向滑入、弹跳、缩放或装饰性序列。

### 5.6 响应式规则

- `≥1024px`：浮层宽度固定为 `780px`，四周至少保留 `24px`，项目列表作为完整背景上下文可辨识。
- `720—1023px`：浮层宽度为 `calc(100vw - 40px)`，最大高度为 `calc(100dvh - 40px)`，仍保持居中和四边留白。
- `<720px`：浮层宽度为 `calc(100vw - 24px)`，最大高度为 `calc(100dvh - 24px)`；双列字段退化为单列，但仍保持浮层边界，不切换为右侧 sheet 或独立创建路由。

### 5.7 验收口径

1. 点击“新建评估项目”后，项目列表 DOM、路由、筛选、分页和滚动位置均不被替换或重置。
2. `1440px` 桌面端浮层宽度为 `780px`，左右留白基本相等，四边均可见背景；不得贴右、贴左或占满工作台。
3. 每一步只展示当前步骤内容，必须经过“下一步”逐步校验，不允许把全部字段一次性铺开。
4. 前进、返回、局部失败和重试均保留已填写内容；关闭、遮罩、`Esc` 和“取消”结束未创建流程并丢弃未保存表单。
5. 创建成功后浮层关闭并进入新项目；失败时不退出、不清空。
6. 项目作用域、评估对象类型、项目默认目标等级和非业务来源字段均不得出现。
7. 标题、步骤、内容和底部动作无裁切；浮层内容滚动不带动项目列表；键盘焦点不会逃逸到背景。
8. 实现不得使用“遮罩左列 + 右侧面板”的布局、`border-left` 抽屉边界或 `translateX` 横向进入动效；页面不存在占满主工作区的 `.maturity-v2-create-page` 创建状态。

## 6. 项目工作区框架

### 6.1 项目页头

- 第一行：返回项目列表、项目名称、客户组织、状态、保存状态；
- 第二行：模板、版本、知识快照、项目负责人和最近更新；
- 右侧动作按状态变化，不同时出现多个蓝色主按钮；
- 项目事实不做四块 KPI 卡。

### 6.2 项目内导航

`项目概览 / 模板 / 四维评分 / 复核 / 结果 / 报告` 使用轻量 Tab。未到达的阶段可查看说明但不可执行正式动作；已完成阶段保留只读访问。

### 6.3 项目概览

- 当前阶段与下一步；
- 项目 / 客户事实；
- 模板与知识快照；
- 评分完成度和待复核数量；
- 最近导入、计算和报告运行；
- 当前结果只有在已计算时出现，证据覆盖率放在辅助信息区。

## 7. 模板页面完整设计

### 7.1 固定模板项目

使用只读树表：能力 L0（如有）→ L1 → L2 → 关注点 → 评估点预览。顶部展示只读说明、版本和快照；支持搜索、展开层级、导出评分表、复制为自定义模板。不得显示结构编辑、删除或权重修改控件。

评估点预览必须清楚区分：

- 技术关注点有真实映射：`SERVICE`，显示真实作用域和安全技术服务；
- 技术关注点无服务映射：`FOCUS`，不显示空作用域；
- 治理 / 管理关注点：`FOCUS`，平台参考服务只作为说明，不单独计数为评估点。

### 7.2 自定义模板配置

该页允许三栏，因为任务本身是结构编辑，不沿用评分页的无目录规则：

1. 左栏：可选 L0 / L1 / L2 树，支持重排和移动；
2. 中栏：关注点、作用域、服务和映射配置；
3. 右栏：来源、原归属、当前归属、变更类型、服务角色、评估点预览和校验。

关键交互：

- 删除 L0 前要求重新分配 L1；
- 移动 L2 / 关注点时明确提示“服务映射将随关注点移动”；
- `ASSESSMENT_POINT` 与 `PLATFORM_EVIDENCE_REFERENCE` 使用文字 + 图标区分；
- 发布前显示阻塞错误、需确认警告和信息提示三类校验；
- 已发布版本只读，修改必须“创建新版本”。

## 8. 四维评分页完整设计

### 8.1 结构裁定

评分页不再把全部评估项从上到下铺成一张超长层级矩阵，也不使用目录 + 主区 + inspector 三栏结构。采用“左侧评分目录 + 右侧评分工作台”的两栏工作区：

```text
左侧：L0 → L1 → L2 → FOCUS 评分目录
右侧：当前 L2 汇总 → 关注点摘要 → 服务 Tab → 当前评估点四维评分
```

- 目录复用安全能力映射的层级轨道、缩进和展开控件；无 L0 的自定义模板直接从 L1 开始。
- L0 / L1 / L2 / FOCUS 节点显示圆点状态标签与完成数 / 适用数；状态文字保留在可访问名称、目录图例和右侧工作台，不进入服务 Tab 的聚合分。
- 默认恢复上次工作的目录路径与当前评估点；无历史位置时按模板源顺序定位第一个有效路径。搜索和“跳转到能力”直接切换到命中 L2，并展开对应关注点。
- 页面一次只编辑当前服务 Tab 或 FOCUS，避免 32 个 L2 及全部服务在同一滚动面连续出现。
- 不常驻右侧 inspector。评分标准、目标、证据和复核信息均在当前评估点行下方展开。
- 选择有服务映射的关注点后，右侧服务 Tab 按模板顺序展示 `SERVICE`；无服务映射时直接展示该关注点自身的 `FOCUS` 表单。
- 桌面目录宽度约 `304px`，主评分区占据剩余宽度；`< 960px` 时目录转换为主区上方的限高树，不压缩评分依据和 rubric。

#### 8.1.1 第三轮授权变化与保留边界

第三轮用户明确授权以安全能力映射目录替代三行能力切换与当前 L2 关注点列表。下列区域继续保持顺序、层级和功能：

1. 全局侧栏、顶部全局搜索和右上角全局动作；
2. 面包屑、`成熟度评估` 页标题、页面说明、总体适用点 / 完成度 / 自动保存和“更多”；
3. 项目概览、评估模板、评分执行、评分复核、评估结果、报告快照项目内导航；
4. L0 / L1 / L2 / 关注点业务层级、源顺序和显式选中 ID；
5. 当前 L2 汇总面板中的能力名称、当前、目标、差距、完成度和四维平均字段及顺序；
6. 当前聚焦评估点摘要中的编码、名称、定义、下级数量、完成度和批量动作位置；评分执行页不显示下级汇总出的当前平均或目标反馈；
7. 左侧评分目录 + 右侧主评分表单的左右分栏、服务 Tab、当前指数 / 人工目标 / 目标理由 / 评估证据说明和“保存并转到下一项”。

本轮授权变化仅包括：评分目录及四态标签；服务 Tab；右上角保存并转到下一项；当前等级定义平滑聚焦；结果页能力雷达与统计重排；不适用分母统一。上一轮已确认的加长评分依据、`SERVICE / FOCUS` 同构、四维独立评分和 API Rubric 契约继续保留。未在此清单内的功能删除或对象粒度变化一律视为回归。

### 8.2 顶部工具条

- 当前项目 / 模板只读上下文；
- 评分进度：完成定义为四维自评 + 目标等级 + 目标理由；
- “跳转到能力”命令，结果按 L0 / L1 / L2 / 关注点分组；
- 搜索评估点；
- 筛选：未评分、待复核、已确认、不适用、无证据（辅助）；
- 导出评分表、导入评分数据；
- 自动保存状态。

工具条下方保留方案 1 的三行能力切换几何结构和 `能力域（L0/L1/L2）` 行标签，只在原区域内收敛 Apple Shell 控件表面，不改变业务顺序、组宽和信息层级：

- **L0**：保留方案 1 的横向分组与标签，使用 `36px` 高低噪声 segmented control；冷灰轨道、白色选中实面、`1px` 细描边、轻阴影和 Apple blue 文字，不使用整块高饱和蓝底。
- **L1**：保留方案 1 与 L0 同行的内容宽度分组，不改成新的整行导航；选中项使用白色实面、细蓝描边和 `14px / 650` 文字，未选项保持中性表面。
- **L2**：保留方案 1 的独立第二行和原有能力标签宽度；选中项使用浅蓝灰实面 + 细蓝描边，未选项保持冷灰白。宽度不足时在本区域内自然换行，不出现原生横向滚动条。
- 三层均只显示能力名称，不显示分数、数量、状态或完成度；能力切换区与下方 L2 汇总继续使用方案 1 的间距和 `1px` 分隔关系。

### 8.3 L2 汇总面板

选择 L2 后必须先展示不可折叠的 L2 汇总面板，不再只把 L2 作为一条可收起标题行。汇总面板回答“这个能力现在处于什么水平、为什么、还差多少”：

- 第一行保持方案 1 字段顺序：`能力 L2 编码 + 名称`、当前成熟度 / 等级、目标 / 等级、差距、完成度、四维平均；评分来源仅作为“当前 L2”标题旁的紧凑二段控件，不新增一行、不移动既有指标；
- 第二行：组织与角色、制度与流程、平台与工具、数据与信息四个维度的后端聚合平均分，默认显示 1 位小数；
- 第三行：关注点汇总表，逐行显示关注点名称、适用评估点数、四维平均分、当前平均分、目标平均分和状态；
- 若 L2 应用了短板约束或上限规则，同时显示“原始平均分 → 规则后结果”和规则说明，不隐藏修正过程；
- 证据覆盖率只放在辅助信息区，不与当前分、目标或完成度并列成主指标。

汇总面板使用一块连续表面和分隔线，不拆成 KPI 卡墙。`DIRECT` 模式展示节点四维的整数选择及后端计算结果；`CHILD_ROLLUP` 模式展示下级实际统计分。所有指数、平均分、等级、差距和完成度均由 maturity API 返回，前端不得重新计算。

### 8.4 关注点分块与执行页反馈边界

当前 L2 内按关注点分块。一个关注点只显示一次，随后连续展示该关注点下全部真实安全技术服务评估点；不得把同一关注点重复成多个“关注点 + 单服务”条目。

关注点头部只回答“当前评估什么、定义是什么、下级有多少、完成多少”：显示编码、名称、关注点定义、下级评估点数量、完成度、保存状态和批量入口。若当前关注点无服务，则显示“关注点直接评估”。

评分执行页不回写关注点评级：安全技术服务逐项评分后，关注点头部不得显示由下级汇总出的当前平均、目标等级、四维平均或“汇总分已变化”等反馈，避免把聚合结果误读为可编辑的父级分数。关注点与 L2 的正式聚合继续由后端计算，并在“评估结果”页与 L2 只读结果区展示；前端评分执行页不自行计算，也不把聚合结果作为关注点输入。

#### 8.4.1 L2 / 关注点评分来源与统一设置

每个 L2 和关注点具有互斥字段 `score_source_mode`：

| 模式 | 页面输入 | 页面反馈 | 计算边界 |
|---|---|---|---|
| `DIRECT` 直接评分 | 当前节点四个维度分别使用 1—5 按钮；可填写对应评分依据、目标和理由 | 显示后端按节点四维计算的当前指数 / 等级 | 当前节点直接评分是唯一有效来源；下级记录可保留但不进入本次计算 |
| `CHILD_ROLLUP` 下级汇总 | 父级四维 1—5 不可提交；只提供“统一设置下级”和“查看计算明细” | 显示来自 N 个适用下级的实际四维加权平均分、当前 / 目标、范围与完成度 | 下级当前生效结果是唯一有效来源；父级直接评分保留为非生效历史，不进入本次计算 |

- 新建节点默认 `DIRECT`，模板可以锁定默认模式；已有下级评分或汇总结果的 demo / 历史项目迁移后保持 `CHILD_ROLLUP`，不得因默认值覆盖既有结果。
- 模式切换必须通过显式确认浮层完成，说明“哪些数据将退出当前计算、哪些数据仅作为历史保留、切换后的有效来源”。切换不删除任何评分记录，并记录切换人、时间和原因。
- 同一 calculation run 对同一节点只允许一个有效来源；API 若同时返回 `direct_effective=true` 与 `rollup_effective=true`，页面进入阻塞错误态，不自行选一个结果继续显示。
- “统一设置下级”是关注点单等级初始化，不是父级评分：仅当该关注点全部下级安全技术服务的四个维度均未评分时可用；用户只选择一个 `L1—L5` 等级，系统将该等级同时带入所有下级的组织、流程、工具、数据四个维度，并标记 `FOCUS_BATCH`。
- 下级已有任一维度评分后锁定统一初始化入口，不允许再批量覆盖。后续只能逐个安全技术服务调整，单项调整标记 `ITEM`。批量入口不设置适用性、人工目标或目标理由。
- `CHILD_ROLLUP` 的实际统计结果只进入后端结果聚合，不在评分执行页关注点头部反馈，也不与“统一设置下级”的 1—5 控件共用表面。

### 8.5 评估点折叠行

列顺序：

1. 安全技术服务 / 关注点评估项：关注点已在分组头显示；服务行只显示当前真实服务编码 / 名称，无服务映射时显示“关注点评估项”；
2. 类型 / 作用域：`SERVICE` 显示真实作用域，`FOCUS` 显示“关注点评估”；
3. 是否适用：原生方框勾选，默认勾选为“适用”；取消勾选即“不适用”；
4. 四维摘要：组织 / 流程 / 工具 / 数据显示已选整数分，未评分显示短横线；实际评分按钮在展开区；
5. 系统当前：只读指数与成熟度等级；
6. 人工目标：原生下拉菜单，选项为成熟度 L1—L5；
7. 状态：未评分、已评分、待复核、已确认、退回、不适用；
8. 服务名称 / 关注点评估项名称作为当前对象标识；多个下级服务使用“上一服务 / 下一服务”按钮连续切换，当前下级评估点不使用下拉菜单；表单控件不得触发行展开或重置选择。

折叠行可在状态下方显示一行证据说明预览；无内容时显示中性“未填写证据说明（可选）”，不能用警告色暗示阻塞。

当“是否适用”取消勾选时，该服务评估点或关注点评估点退出当前与目标聚合；四维评分、人工目标及相关评分依据立即置灰且不可编辑，只保留“是否适用”和“不适用原因”可操作。重新勾选“适用”后恢复原草稿值和编辑能力。

“是否适用”必须出现在每一个 `SERVICE` 与 `FOCUS` 评估点上，不能只放在关注点统一设置区，也不能因为上层节点使用 `CHILD_ROLLUP` 而隐藏。默认值为“适用”；批量设置只负责初始化，下级单项始终可覆盖。

### 8.6 行内展开评分区

1. **对象说明**：关注点说明、评估点类型、真实作用域 / 服务；治理 / 管理 `FOCUS` 在平台与工具维度旁展示参考服务。
2. **四维评分**：组织与角色、制度与流程、平台与工具、数据与信息各一行；每行使用 `1 / 2 / 3 / 4 / 5` 方形评分按钮，并显示当前维度的等级标准与评分依据输入。采用用户确认的方案 2 评分区：主行只保留“维度、评分按钮、加长评分依据”，当前选中等级标准下沉为紧邻该维度的整行说明。
3. **系统当前结果**：四维加权后的指数、等级和百分制只读展示；不提供人工总体分输入。
4. **人工目标**：使用原生下拉菜单选择成熟度 L1—L5；目标理由必填。
5. **评估证据说明（可选）**：每个评估点固定提供独立多行文本框，可记录访谈、制度、配置、运行记录、抽样结果和判断依据；文件 / 链接和证据等级作为次级关联。
6. **补充信息**：评分说明、备注；选择“不适用”时只保留不适用原因并要求必填。
7. **动作**：取消、保存、保存并转到下一项。正式计算放在项目 / 复核阶段，不让单行按钮造成“保存即权威计算”的误解。

桌面评分区建议使用 `grid-template-columns: minmax(120px, 160px) minmax(260px, 320px) minmax(380px, 1fr)`。评分依据占满第三列，最小宽度 `380px`、默认最小高度 `40px`，输入增长到两行后可继续自适应增高；不得把评分依据压缩成截图方案 1 中的窄尾列。当前标准行从第二列跨到第三列，只显示 `L4 量化控制。具体维度标准文本`；不再保留持续占宽的独立 rubric 列或“对照全部 L1—L5”入口。

#### 8.6.1 无安全技术服务的 FOCUS 完整样例

以真实关注点 `G-SP.SM-01 具有明确的网络安全战略计划与目标` 为样例：

- 所属路径显示 `安全治理能力 G → G-SP 网络安全战略计划 → G-SP.SM 安全战略管理能力`；当前评估点类型固定为 `FOCUS`，标题副文案为“关注点评估点 · 无独立安全技术服务”；
- 左侧评分目录按真实层级显示 `G-SP.SM-01`、`G-SP.SM-02`、`G-SP.SM-03`，选中 `G-SP.SM-01`；右侧不生成空服务、不显示“暂无服务”占位 Tab、不伪造作用域，直接使用同一评分表单评估关注点自身；
- 该 `FOCUS` 默认 `DIRECT`，直接填写四维 1—5、各维度加长评分依据、人工目标与理由、评估证据说明、备注和是否适用；
- 若治理 / 管理对象存在平台参考服务，只在平台与工具维度的辅助说明中显示 `PLATFORM_EVIDENCE_REFERENCE`，不生成独立评分项，也不改变评分来源；
- 当该 `FOCUS` 被设为不适用时，退出其上层 `CHILD_ROLLUP` 的分子与分母，并要求填写不适用原因。

### 8.7 维度评分按钮与 L1—L5 标准

每个维度使用同一结构：`维度名称 → 1—5 评分按钮 → 当前标准提示 → 评分依据`。

- 单个评分按钮使用原生 `button / radio` 语义，桌面建议 `40 × 36px`；五个按钮在评分列水平居中，按钮文字显示 `1—5`，相邻说明显示完整成熟度等级名称。
- hover 或键盘 focus 只更新标准预览，不立即改变分数；click、Space 或 Enter 才提交选择。指针离开后恢复显示当前已选等级标准。
- 标准预览是按钮下方的常驻区域，不把关键标准只藏在 tooltip。显示格式为 `L4 量化控制。具体维度标准文本`，文本必须完整展示，不使用单行截断。
- 评分主区不显示“对照全部 L1—L5”。选中等级使用 Apple blue 实底、描边和约 `1.14` 倍平滑放大；定义区使用 `220ms` 左右的轻微显现动画，同时保留数字、`aria-pressed` 和焦点环，不能只靠颜色表达。
- 标准必须来自当前模板版本的维度 rubric，“对象专用标准优先、通用标准回退”的数据优先级不变。模板未提供维度专用标准时可按后端契约回退通用成熟度定义，但评分主界面只显示当前等级名称和定义，不显示“通用等级定义”来源标签；前端不得硬编码或自行生成标准。
- 四个维度可以选不同分数。评估点总体分仍由后端按四维权重计算，页面不提供人工总体分按钮。

#### 8.7.1 四维评分判定契约

四维评分必须分别判断，不允许因为某一维度达到某级而自动联动其他维度。评估人员应选择“当前已经稳定实现的最高完整等级”，不能把建设计划、采购意向或目标状态计入当前分；若某级核心要求只部分满足，应选择已完整满足的较低等级，并在评分依据中记录差距。

评分标准采用以下优先级：

1. 当前模板版本中与评估对象、维度和等级绑定的专用 rubric；
2. 当前模板版本中的通用四维 rubric；
3. 下表通用矩阵。使用第 3 级回退时保留来源元数据供审计，评分主界面仍只显示当前等级定义。

| 成熟度等级 | 组织与角色 | 制度与流程 | 平台与工具 | 数据与信息 |
|---|---|---|---|---|
| L1 非正式执行 | 没有明确分配责任和职责，主要依赖临时指派人员，缺乏稳定团队和清晰职责定位。 | 依靠个人经验，缺乏文档化和标准化；要求没有统一明确规定，主要口头传达。 | 工具使用非常有限，覆盖窄、功能不完整，主要依靠个别人员手工操作。 | 业务数据没有系统记录和收集，数据分散，无法系统分析和利用。 |
| L2 计划跟踪 | 有人员主动承担部分职责，但未正式明确；出现问题时责任边界仍可能不清。 | 已形成初步流程或制度，但尚未标准化，执行不严格且覆盖有限。 | 已具备基础措施和工具，但能力、覆盖和关键环节支撑仍有限。 | 开始记录和收集部分业务数据，但质量和覆盖率有限，尚不能形成全面信息支撑。 |
| L3 充分定义 | 职责和责任已正式分配，角色定义清楚，流程所有者和责任人明确。 | 已形成标准化流程，制度正式发布并执行，能够在组织范围内贯彻实施。 | 配置完整、专业的技术措施，覆盖关键环节和业务流程，并具备必要集成能力。 | 全面记录和收集业务数据，能够分析处理并形成初步风险预警和情报支持。 |
| L4 量化控制 | 通过自动化流程促使责任人主动履职，形成有效激励和持续参与机制。 | 流程健全完整、内嵌最佳实践并逐步自动化；操作有记录且可重复。 | 技术措施相互融合，实现主要领域的自动化管理和监控，并具备实时响应和调整能力。 | 信息收集系统化，实时支撑管理决策和考核，形成闭环管理，并使用分析模型提升决策效率和准确性。 |
| L5 持续优化 | 能根据战略变化灵活调整角色责任，责任人自我激励、自驱管理，团队高度自主。 | 流程、政策和程序高度标准化与自动化，可随环境和威胁变化持续优化。 | 技术措施深度融合，利用人工智能、机器学习等实现高效威胁检测和自适应响应，并与业务高度协同。 | 决策与考核指标动态调整，可实时响应战略和计划变化，提供前瞻性安全保障。 |

#### 8.7.2 维度边界与禁止联动

- **组织与角色**只判断客户在当前评估点上的职责、岗位、所有者、协同和履职机制，不以项目评估人员是否充足代替客户现状。
- **制度与流程**判断制度是否存在、正式发布、实际执行、覆盖、留痕、可重复和持续优化；只有文档但未执行，不能据此评为 L3 及以上。
- **平台与工具**判断当前评估点在对应作用域中的实际部署、覆盖、集成、自动化和自适应能力；字典存在服务映射、已采购产品或仅完成部署，不自动等于工具成熟。
- **数据与信息**判断业务数据的记录、质量、覆盖、分析、指标、决策支持和预测能力；它不是“上传了多少证据”，证据数量和证据覆盖率不得自动改变该维度分数。
- 治理 / 管理 `FOCUS` 的 `PLATFORM_EVIDENCE_REFERENCE` 只辅助判断平台与工具维度，不生成评分，也不自动抬高该维度。
- 人工目标仍是评估点总体目标，不是四维目标；V2.1 不提供四个目标维度输入。
- 后端按四维最终等级和模板权重计算当前指数；默认四维等权 `0.25`，前端不得自行平均、取最高、取最低或直接写总体分。

#### 8.7.3 Rubric 数据与页面状态

API / ViewModel 应为当前评估点返回 4 个维度 × 5 个等级共 20 条可追溯 rubric，至少包含 `dimension_code`、`level`、`level_name`、`criteria`、`source_type` 和 `source_version`。来源差异由数据契约和审计保留；评分主界面只显示当前已选等级的完整定义，不展示全部五级对照。任一适用评估点缺少某维度全部五级标准时，评分区进入 `rubric_missing` 阻塞态，不允许前端临时生成文字。

适用评估点完成四维自评后可标记“已评分”；提交复核仍需同时具备人工目标等级和目标理由。不适用时整个评估点退出计算，不能只把单个维度设为不适用。证据继续非必填，但每个维度应提供评分依据输入，便于复核人员理解为什么选择该等级。

### 8.8 评估证据描述框

- 字段标签固定为“评估证据说明（可选）”，每个评估点一份，复用业务字段 `evidence_summary` / 当前 ViewModel 等价字段。
- 默认显示 3 行，输入后可自适应增长到 8 行；建议上限 1000 个中文字符，显示剩余字数但不打断输入。
- placeholder：`说明用于支持本次评分的访谈、制度、配置、日志、报告或运行事实。`
- 文本变化进入现有自动保存状态；保存失败保留内容并提供重试，不因重渲染丢失焦点。
- 可选关联“整体 / 组织 / 流程 / 工具 / 数据”维度；文件、链接、证据等级和共享证据属于次级功能。
- 空证据说明不阻止评分、复核、计算或报告，不使用黄色、红色和“待补”文案。

### 8.9 自评、复核与批量操作

- 评分阶段只显示自评；
- 复核阶段在每个维度显示“自评值 → 复核值”，复核未调整时不重复绘制两套控件；
- 发生调整必须填写复核原因；
- 最终值为后端 `review_level ?? self_level`，前端只读展示。
- 批量设置只写草稿，并在提交复核前逐项确认；
- “统一设置”是关注点下级服务的就地批量赋值入口；带入值必须记录 `FOCUS_BATCH` 来源，单项调整记录 `ITEM` 来源；
- 单个服务调整后，关注点及全部能力层汇总必须基于实际子项即时重算，不得继续展示旧的统一值；
- 批量设置“不适用”必须填写批次原因，可逐项覆盖；
- 批量目标允许生成待确认草稿，但每个适用项最终必须有目标理由；
- 从历史项目复制后，所有值标记“复制草稿”，不能直接成为已确认评分。

### 8.10 保存、状态与位置稳定性

- 每次评分、目标、适用状态、理由或备注变化均立即保存到当前项目草稿；页面明确显示“已保存 / 保存失败”和“等待汇总 / 正在试算 / 已完成汇总”；
- 服务行状态按输入进度变化为“未评分、填写中 1—3/4、待设置目标、待补目标理由、已评分、已确认、不适用”；
- 关注点、L2、L1、L0 显示后端聚合状态和平均分，不由前端自行推断权威得分；
- 评分控件变化触发重渲染和后端试算时，必须保存并恢复当前 L0 / L1 / L2 Tab、关注点展开状态、页面滚动 owner 和当前控件焦点；不得跳回页首或改变路由；
- 受控 demo 的浏览器本地状态必须同时保存评分条目、匹配聚合结果、最后保存时间和最后计算时间；正式持久化阶段由 maturity API 提供同等语义。

### 8.11 Apple Shell 评分工作区规则

- 页面标题 `24px / 700`；L2 汇总标题 `18px / 700`；关注点标题 `15px / 650`；评估点名称 `14px / 650`；正文 `13—14px`；主业务信息不得降到 `10—11px`。
- L0 segmented control、L1 / L2 Tab、评分按钮、输入框和状态均复用 Apple Shell token；纯蓝只用于当前选择、焦点和主动作，面积保持克制。
- 页面背景、L2 汇总、关注点分块和评估点展开区通过表面层级与分隔线建立重点，不使用嵌套卡片、渐变、厚阴影和五个同权重大下拉框。
- L2 汇总与当前关注点可使用 `position: sticky` 保持上下文，但不得遮挡工具条或形成两个纵向滚动容器。
- 内容出现和 Tab 切换使用 `150—200ms` ease-out；不做整页重载动画。

### 8.12 现有前端功能保留与用户可见 demo 信息清零

本轮按用户明确授权完成评分目录与结果可视化重构；不得以“视觉优化”为由删除已经形成的前端功能闭环。页面标题、项目名称、客户名称、提示条和操作文案不得再向用户显示“demo / 演示”；内部 fixture ID、开发模式和本地持久化标识可以保留，但不属于用户可见界面。后续实现至少保留：

1. 项目列表的状态视图、搜索、筛选、动态操作和行内预览；
2. 项目列表上方居中的三步新建项目浮层页；
3. 项目概览、固定模板和自定义模板路径；
4. 评分搜索、状态 / 证据筛选、跳转到 L2、评分与模板导入导出、批量日志；
5. 自动保存、浏览器本地持久化、刷新恢复，以及当前目录路径、服务 Tab、滚动位置和焦点保持；
6. 每点评估适用性与不适用原因、自评 / 复核、人工目标 / 理由、证据等级 / 证据说明、备注；
7. 复核队列、结果页和报告快照；开发数据重置不作为正式前端页面的用户可见主功能。

除第三轮授权变化外，功能入口、业务字段和对象粒度不得改变。若旧功能与 V2.1 规则冲突，应按本文件替换其业务语义，并在迁移说明中列出旧入口到新入口的映射。

### 8.13 两张独立设计图交付契约

1. **SERVICE 图**：以方案 1 原图为完整底板，保留全部不可变区；选中 `T-AS.AD-01` 下的真实服务 `I-AP&T-AS.AD-01 应用架构管控`，只呈现七项授权变化。
2. **无服务 FOCUS 图**：复制 SERVICE 图的同一页面壳、项目导航、能力切换、L2 汇总、聚焦摘要、左右分栏、评分行和底部动作；仅把业务路径替换为 `G-SP → G-SP.SM → G-SP.SM-01`，左侧显示三个真实关注点，右侧直接评估选中 FOCUS。
3. 两图必须分别生成、分别展示，不得上下拼接、左右拼接、做成概念板或在第二张中另造导航 / 表单 / 字号 / 间距体系。

## 9. 文件交换完整设计

使用右侧任务抽屉启动，复杂结果进入独立任务页。三类任务不得混在一个模糊“导入文件”入口：

1. 固定模板评分数据；
2. 自定义模板结构；
3. 自定义模板评分数据。

流程：选择类型 → 下载 / 选择对应模板 → 上传 → 校验版本与结构 → 查看成功 / 警告 / 失败行 → 确认部分导入 → 写入草稿。

行级结果表展示业务对象、字段、错误代码和处理状态；`sheet_name`、`row_number`、`raw_value_json` 只允许进入折叠的技术证据区，不进入主业务表。

## 10. 复核页完整设计

### 10.1 筛选与队列

- 阻塞项：适用项缺少四维任一评分、缺少目标等级、缺少目标理由，不适用项缺少原因，或结构校验失败；
- 复核项：自评待确认、复核有调整、已退回；
- 信息项：无证据、证据等级为空。信息项不得计入“待完善”阻塞数。

### 10.2 行内复核

按评估点展开查看四维自评、复核调整、系统当前结果、目标和理由、可选证据、评分说明。复核操作为“确认”“调整并确认”“退回”，退回必须填写原因。

### 10.3 完成条件

只有业务文档第 16 章的必填和结构条件通过时，允许完成复核。无证据不阻止按钮，不显示红 / 金告警。

## 11. 结果页完整设计

### 11.1 双视图

- **客户结果**（默认）：总体、L0 / L1 摘要、L2 能力结果；最细到 L2。
- **内部明细**：关注点、服务、作用域、四维、目标理由、可选证据、复核和来源。

### 11.2 客户结果结构

1. 紧凑总体摘要：当前、目标、差距、目标达成率、结果状态；
2. 左图右数结果区：左侧为全部 L2 的固定 1—5 分组雷达，右侧为 T/G/M 总体、L1 能力域、L2 数量 / 低于目标数量和后端差距候选评价；
3. 评估覆盖与排除：适用、完成、待完成、不适用及分母说明；
4. L0 能力域当前—目标对比；
5. L2 四维热力表与精确值；
6. L2 差距 Top N；
7. 已确认的能力演进建议。

证据覆盖率单独放在“评估材料完整性”说明区，不与评分完成度并列成主要结果。

### 11.3 图表规则

- 图表只消费后端固化 calculation run；
- 当前与目标不能只靠颜色区分，同时使用点形 / 线形 / 文本；
- 雷达轴必须覆盖全部后端 L2，显示稳定、唯一的中文短名；完整编码、业务名和精确值继续在展开明细与 L2 结果表提供；
- 右侧 T/G/M 与 L1/L2 统计只消费 `categoryResults / subCategoryResults / capabilityResults / gapItems`；前端可以分组、计数和排序，不得重新平均指数或生成正式评价；
- 精确值始终能在结果表查看；
- 不生成空图；
- 服务多不等于权重大，图形尺寸不得由服务数量决定；
- 内部关注点 / 服务图表不得混入客户主结果。

## 12. 报告页完整设计

- 顶部显示项目、模型、模板、知识快照、算法和 calculation run；
- 左侧章节导航，主区为报告快照预览；
- 草稿预览与正式快照使用明确状态；
- 正式快照不可被后续评分覆盖；
- 导出 Markdown、HTML、JSON、项目包；
- 历史快照按生成时间和 run 展示；
- 无证据只进入限制说明，不把报告标记为无效。

## 13. Apple Shell 视觉与组件规则

- `/workbench/maturity` 及其项目深层路由必须继承全局 `56px` 顶栏、`96px` 页面标题区、侧栏和唯一 `h1`；成熟度组件不得再渲染并列的主页头。
- 成熟度工作区使用 `main-only` 共享布局。项目列表的“更多 / 新建评估项目”和项目状态 / 主动作进入 `#maturityShellHeaderActions`；不得在业务主区复制同一组动作。
- 项目概览、评估模板、评分执行、评分复核、评估结果、报告快照六个页签必须保留同一条不超过 `48px` 的次级项目上下文行，只用于项目名、客户、模板、更新时间和状态；它不是第二个页面标题层。评分页的进度、自动保存和“更多”继续放入共享页头，不能以此为由删除项目上下文。
- 新建项目是居中 workflow overlay，必须保留背景项目列表和原筛选状态；不得改成右侧抽屉、全屏工作台或常驻辅助栏，并复用 Apple Shell overlay 圆角 token。
- 全局壳保持当前 Apple Shell；内容区使用实色暖珍珠白 / 冷灰表面，不使用玻璃内容卡。
- 页面标题 `24px`，L2 汇总标题 `18px`，小标题 `16px`，关注点标题 `15px`，评估点名称与业务正文 `14px`，辅助信息不小于 `12px`；主要业务名称不得使用 `10—11px`。
- 圆角仅使用 `6 / 10 / 14px`；表格行和普通记录不用完整卡片边界。
- 选择蓝只用于当前选择和主动作；完成绿用于通过 / 已确认；沙金只用于真正需要处理的警告；淡紫用于当前复核上下文；红色用于阻断和错误。
- “无证据”使用中性蓝灰，不使用待办金色；证据覆盖率必须标注“辅助信息”。
- 能力层级 L0/L1/L2 必须带“能力”语境；成熟度等级使用“成熟度 L1—L5”或纯数值，避免混淆。
- L0 / L1 / L2 / FOCUS 使用层级目录语言；目录状态承载圆点标签、完成数和适用数，文字由图例与可访问名称补充，分数仍只进入 L2 汇总或结果页。
- **共享服务 Tab** 复用 Apple Shell `maintenance-section-tabs / maintenance-section-tab` 的低噪声分段面；必须显示完整对象名，不用省略号或纵向断字隐藏业务语义，并在名称后直接提供逐项“是否适用”方框。右侧工作台宽度大于 `1000px` 时按真实项数等分一行；不足 `1000px` 时用三列紧凑换行，窄屏再使用 Tab 容器内部横向滚动。Tab 显示编码、名称、逐项状态和适用性，不显示尾部作用域编码，也不承载父级聚合分。

## 14. 响应式与可访问性

- `>= 1440px`：项目表全宽；评分页使用约 `304px` 目录 + 弹性主评分区左右分栏，完整显示 L2 汇总、服务 Tab、评分来源和加长评分依据。
- `960—1439px`：项目表隐藏低优先级列；目录宽度收窄至约 `258px`；当右侧评分工作台不足 `1000px` 时，共享服务 Tab 改为三列紧凑换行并保留完整文字，页面不得产生横向滚动；L2 汇总的核心结果保持可读。
- `< 960px`：全局侧栏折叠；项目表改为分隔线列表；评分目录移到工作台上方并限高，L2 汇总与关注点头改为两层；评分维度在可用宽度内保持“维度 / 五格 / 当前定义 / 评分依据”，到移动窄屏才纵向排列。
- 1—5 评分控件使用原生 button / radio 语义、键盘方向键和清楚 focus ring；不能只靠蓝底表达选中。
- 目录节点状态提供语义圆点标签，并通过 `aria-label` / `title` 暴露文字；目录图例、评分工作台、目标和当前至少保留文本标签。
- 评分按钮的平滑放大和定义显现动画在 `prefers-reduced-motion: reduce` 下禁用。
- 动态保存、导入、计算和报告生成使用 `aria-live`；错误与字段建立明确关联。
- 表头 sticky 时保持足够对比度，局部横向滚动必须有提示。

## 15. 前后端与字段边界

- 前端不得生成评估点、推断作用域 / 服务关系、计算权威当前分、聚合目标或应用上限规则。
- 项目创建 API 不接受 `project_scope`、可选择的 `assessment_object_type` 或项目默认目标。
- 固定模板结构在项目期只读；自定义模板发布后只读。
- 客户主视图不得展示 `sheet`、`row`、`column`、`raw_value`、`source_file`、`import_id`、`source_id`、`source_ref`、`source_label`、`debug`、`raw`、`metadata`、`intermediate`、`generated_at`。
- 文件行号和原始值只允许在导入错误的折叠技术证据区出现。

## 16. 实施顺序

### MAT-P0：业务纠偏与项目列表

1. 删除项目作用域和对象类型 UI / demo 字段；补齐客户与项目字段。
2. 重做项目列表为完整操作队列，移除常驻摘要和统计卡带。
3. 修正证据为可选的文案、状态、筛选和完成条件。
4. 固化固定模板 `SERVICE / FOCUS / PLATFORM_EVIDENCE_REFERENCE` 页面表达。
5. 建立 L2 / 关注点 `DIRECT / CHILD_ROLLUP` 评分来源、迁移和审计字段契约。

### MAT-P1：评分与复核

1. 落地左侧 L0 / L1 / L2 / 关注点评分目录和右侧当前 L2 单页评分工作区；
2. 完成不可折叠的 L2 汇总面板、关注点定义 / 完成状态和服务 Tab；
3. 用维度 1—5 评分按钮、常驻标准提示和全部等级对照替换四维下拉；
4. 为每个评估点补充可自动保存的“评估证据说明（可选）”；
5. 完成直接评分 / 下级汇总互斥状态、只读实际统计分和模式切换确认；
6. 完成关注点单等级初始化、任一下级已评分后的批量锁定、服务单项覆盖，以及结果页关注点 / 能力逐级平均分回算；
7. 完成逐点评估适用性、FOCUS 直接评估、目标等级 / 理由、不适用原因、自动保存、状态变化和 Tab / 滚动 / 焦点保持；
8. 重做复核队列，分离阻塞项与无证据信息项。

### MAT-P2：模板、文件、结果与报告

1. 完整自定义模板配置和版本校验；
2. 三类文件交换任务及行级结果；
3. 客户 / 内部双结果视图、固定 1—5 量尺并按技术、治理、管理分组的 L2 全能力分组雷达，以及覆盖 / 排除统计；
4. calculation run 绑定的报告快照和历史导出。

## 17. 验收清单

1. 新建项目无项目作用域、对象类型和默认目标控件；
2. 项目列表字段、状态、动态动作和空 / 错 / 加载态完整；
3. 固定模板无结构编辑入口，只显示真实作用域 / 服务映射；
4. 治理 / 管理参考服务不生成独立评分行；
5. 同一关注点不混合 `SERVICE` 和 `FOCUS` 评分项；
6. 每个适用点必须有四维自评、目标等级和目标理由；
7. 当前总体分和等级只读，由后端返回；
8. “是否适用”以紧跟服务 Tab 完整名称的方框勾选呈现且默认适用；取消勾选后四维评分和目标等级置灰，不适用原因必填且该项退出计算；
9. 无证据不阻止评分、复核、计算和报告；
10. 客户结果最细到 L2，内部明细才能看到关注点和服务；
11. 自定义模板所有移动和修改保留来源、原归属和当前归属；
12. 文件导入不允许改变已发布模板结构；
13. 1440、1024、390px 无页面级横向溢出；
14. 键盘可完成项目筛选、L1 / L2 Tab 切换、四维评分按钮、目标下拉、展开、保存和复核；
15. 同一关注点下存在多个安全技术服务时，只显示一个关注点分组头，全部服务评分行在该分组内连续排列；
16. 点击四维评分按钮、目标等级和是否适用方框不会触发选择重置；评分、勾选、输入和切换后页面保持原位，不跳回页首。
17. 首次进入评分页打开一个 L2 工作区；切换 L0 / L1 / L2、搜索和跳转不会改变已保存评分值或当前路由；
18. 仅当全部下级未评分时，“统一设置下级”允许选择一个等级并同时带入全部下级四维；任一下级任一维度已有评分后入口锁定；
19. 每次评分变更均显示保存和试算状态，刷新页面后恢复匹配的评分条目与聚合结果；
20. 评分执行页关注点摘要显示定义、下级数量和完成度，不回写下级汇总出的当前 / 目标 / 四维分；L2 只读汇总和评估结果页继续显示后端聚合结果；
21. 每个评估点均有“评估证据说明（可选）”文本框，空值不阻塞流程；
22. 每个维度的 1—5 按钮均可查看该维度 L1—L5 标准，标准来自当前模板版本；
23. 四个维度分别判定，修改一个维度不会自动修改其他维度；对象专用 rubric 优先于通用矩阵；
24. `数据与信息` 与证据覆盖率严格分离，服务映射或产品部署也不会自动生成 `平台与工具` 分数；
25. 当前评估点可读取 20 条完整 rubric；rubric 缺失时显示阻塞状态，不由前端补写；
26. 主展示区无非业务字段泄露。
27. 每个 L2 与关注点在 `DIRECT / CHILD_ROLLUP` 中只有一个有效评分来源；同一 calculation run 不会同时消费父级直接分和下级汇总分。
28. `DIRECT` 通过当前节点四维 1—5 生成后端只读指数，不提供人工总体分；`CHILD_ROLLUP` 的父级 1—5 不可提交，实际统计分只在后端结果聚合与结果页显示。
29. 模式切换前明确说明退出计算的数据范围；切换后旧数据仍可追溯但不进入当前结果，已有下级评分的 demo / 历史项目不会被默认 `DIRECT` 覆盖。
30. 每个 `SERVICE` 与 `FOCUS` 都以方框勾选显示“是否适用”；多个下级通过服务 Tab 切换，不显示当前下级下拉菜单或上一 / 下一服务分页；不适用项要求原因并退出当前与目标计算。
31. 评分区在宽屏按“维度 / 五格 / 当前等级定义 / 评分依据”同一行组织；五格在评分列居中，当前定义位于其右侧且未选择时为空，不再下沉到五格下方。右侧工作台不得产生页面级横向滚动。
32. 左侧评分目录沿用安全能力映射目录的层级轨道、缩进、展开控件和轻分隔线；L0 / L1 / L2 / FOCUS 节点显示状态与适用分母，目录内无横向滚动。
33. `G-SP.SM-01` 无独立安全技术服务时只生成一个 `FOCUS` 直接评估点，无空服务、伪作用域或重复评分对象。
34. 用户可见标题、项目、客户、提示和主操作无 `demo / 演示` 文案；项目、模板、导入导出、筛选跳转、自动保存、复核、结果和报告功能不因视觉重做丢失。
35. 方案 1 不可变区是第二轮历史基线；第三轮经用户明确授权后，只由“左侧评分目录 + 右侧评分工作台”替代横向能力切换与关注点列表，项目步骤、L2 汇总、关注点定义、四维表单和数据契约继续保留。
36. SERVICE 与无服务 FOCUS 为两张独立图片，不拼接；两图除业务对象与授权变化外，页面壳、布局、字体层级、间距和控件体系一致。
37. 新建项目任意步骤点击关闭按钮、背景遮罩、`Esc` 或第一步“取消”均立即关闭浮层，项目列表筛选、排序、滚动和当前行状态不变，且不生成新项目。
38. 左侧评分目录与右侧安全技术服务评分区通过独立表面和分隔线区分；目录节点以绿色 / 黄色 / 冷灰圆点标签表达已完成 / 进行中 / 未开始，文字由 `aria-label` / `title` 与目录图例提供，当前选中路径使用独立蓝色语义。
39. 评分目录和服务 Tab 在 1440、1024、390px 均无页面级横向滚动条；工作台不足 `1000px` 时服务 Tab 使用三列换行显示完整名称，移动窄屏才内部滚动；整体进度、项目导航、指标和值仍可辨识。
40. 选中任一维度等级后，在五格右侧同一行显示当前等级定义；未选择时定义位为空且不显示“请选择”或解释提示，主界面不显示“通用等级定义”来源标签。
41. 选中等级按钮先使用约 `1.14` 倍平滑放大，定义区延后约 `55ms` 显现；切换等级时重复同一动效。系统开启减少动态效果时禁用位移和动画。
42. 关注点整体取消适用时，全部子评估点退出当前分、目标分、关注点 / L2 / L1 / L0 聚合和完成率分母；必须记录关注点不适用原因。
43. 单个安全技术服务取消适用时，只排除该评估点，剩余适用项按原权重比例重新归一；能力和能力域完成率使用实际适用评估点数作分母。
44. 结果页 **L2 全能力分组雷达**一次展示后端返回的全部 L2 能力，每项 L2 占一条轴，并按 `topCategoryId` 连续分为技术、治理、管理三段；量尺固定 1—5，当前能力指数和目标等级同时显示。雷达使用稳定中文短名；未评分能力保留轴与中性状态，明确不按 0 分计算；完整编码、业务名和精确值在“全部 L2 精确值”和 L2 结果表中可查。
45. 结果统计分为总体结论、能力雷达、评估覆盖与排除、能力域结果、L2 能力表和差距优先级；成熟度 / 证据分布默认折叠，避免首屏信息争抢。
46. 共享服务 Tab 保持模板真实顺序并显示逐项状态、完整名称与紧跟名称的适用性方框；去掉尾部作用域编码，右侧工作台不足 `1000px` 时三列换行，窄屏才内部滚动。“保存并转到下一项”位于当前评分表单底部右侧，左侧固定显示不适用分母规则；保存后选择下一个真实评估点，不使用“上一项 / 下一项”作为 Tab 标签。
47. 成熟度项目列表和全部项目深层路由都必须由 App Shell 提供唯一页面标题；成熟度组件内不得出现第二个主页头。
48. 成熟度项目列表主动作必须进入共享页头动作槽；在 1280px 宽度下，移除旧本地主页头后，状态视图应紧邻共享页头进入首屏。
49. 成熟度项目工作区必须声明 `main-only`，不得引入常驻目录或 inspector；评分目录属于业务主区内部结构，不计为 Shell auxiliary layer。
50. 新建项目必须声明为 Apple Shell workflow overlay，使用居中三步浮层和 `14px` overlay 圆角；关闭后恢复原项目列表、筛选和滚动状态。
51. 全局页面标题区在桌面端保持 `96px` 高度和 `24px / 1.13` 主标题，说明使用 `12px / 1.45`，页头内边距为 `12px 18px`，面包屑、标题、说明形成稳定三层节奏；不得通过单页覆写换取业务主区空间。
52. 六个项目页签切换时项目上下文行不消失，项目名、客户、模板和最近更新时间位置稳定；项目上下文和共享主页头不得重复成为两个 `h1`。
53. 评分目录节点在删除“已完成 / 进行中 / 未开始 / 不适用”可见文字后，状态圆点、层级标签和业务名称必须紧邻；目录不得为已删除的状态文字保留固定列宽。
54. 六个项目页签的导航条高度统一为 `52px` 并在项目滚动容器内锁定显示；评分执行不得用页面专用选择器覆盖 Tab 高度。五个评分格在评分列居中，选中格平滑放大后在右侧显示完整 Rubric，未选时不显示定义说明，且主区不存在“对照全部 L1—L5”。结果页保持左图右数：右侧可核对 T/G/M、L1 与 L2 统计及后端候选评价。

## 18. 运行面影响

- 影响分类：`shared runtime` 设计规格；实施后 Web 和 DMG App 共享前端均受影响。
- 根因层：`shared frontend + maturity business contract + API projection`。
- 2026-07-13 已修改共享前端、成熟度 API 投影和受控 demo 契约；未修改正式 SQLite、正式 JSON、源 Excel、用户库或 DMG。
- 2026-07-13 P0-2 补充只修改共享壳层归属、成熟度前端容器和自动门禁；本轮无业务、评分、聚合、API projection 或数据规则变化。
- Web 先在 5173 验证；DMG App 仍需在后续发布矩阵补共享前端回归，本文件和 5173 结论不构成 App 验收结论。
