# progress.md

本文件是当前会话恢复入口，只保留最近状态、最近关键动作、验证摘要和历史索引。完整执行历史已归档到 `docs/05-archive/progress-history/`。

## 当前状态（2026-06-07）

- 当前分支：`main`。
- 固定预览入口：`http://127.0.0.1:5173/`。前端展示和用户验收默认只看该端口。
- 当前主控主线：三个 P0 已完成代码闭环。`analytics_summary` 已完成 exporter / audit / `data_package_summary` / `dataClient` / dashboard 消费；`OI-135 + DB-11 + DB-2` 已完成设计、审计脚本、dry-run、临时库 smoke、正式迁移脚本三段式、工作台总览最小 API、数据篮最小 API 和导出最小闭环；真实基础库 / 用户库未写入，后续 apply 必须显式确认并自动备份。Delivery Bundle / 打包任务继续后排。
- Open Issues 当前未关闭：`OI-038`、`OI-128`、`OI-133`、`OI-135`、`OI-137`、`OI-138`；`OI-133` 已按用户最新纠偏完成整图优先修复 / 待人工验收，`OI-137` 已把 draw.io 第三页实例底图导入信息化环境首页 / 待人工验收，`OI-138` 已完成关注点图谱标签避让小步修复 / 待人工验收，`OI-136` 已修复并归档。
- 当前禁止事项：不默认改 ETL、数据库、数据模型、基础数据包、导出 JSON、用户库数据或业务关系推断；不 `git add .`；主展示区不得暴露 `sheet`、`row`、`column`、`raw_value`、`source_file`、`import_id`、`source_id`、`source_ref`、`source_label`、`debug`、`raw`、`metadata`、`intermediate`、`generated_at`。
- 会话卡顿交接规则已固化：只有用户明确说“当前会话卡顿，需要交接”或等价表达时，Codex 才能自动创建同名递增新线程、写入交接包并在当前会话最终回复后归档；没有明确卡顿交接请求时，不得自动创建新线程。

## 当前子 Agent fan-in（2026-06-06）

- `019ea527-048a-7442-b3f4-09777960496d` / Curie / worker：`completed / fan-in`；Backend/Data Agent 已完成 `Environment Basemap Node Detail Projection 1.0`，输出 `environmentBasemap.node-details.json`，主控核验 `detailReadyNodes=91`、`missingDetailNodes=0`、`ignoredNodes=6`。
- `019ea531-bdb4-79e3-a9eb-a2182548bd79` / Volta / worker：`completed / fan-in`；Frontend Agent 已完成 `Environment Basemap Interaction 1.0`，接入 `environmentBasemap.node-details.json`，实现 bound 节点详情、ignored 节点说明和空白清选；主控待 QA 只读验收。
- `019ea544-84c8-78a2-895a-1ab23480826c` / Feynman / explorer：`completed / fan-in`；QA Agent 已完成只读验收，确认 node-details 与 semantic 对齐、bound / ignored 点击行为可用、字段边界无泄露、HTTP smoke 和 `git diff --check` 通过；记录一个非阻塞交互风险：父级容器点击可能被内部子节点命中。
- 历史已关闭子 Agent：Faraday、Arendt、Ptolemy、Kuhn、Nash 均为 `completed / fan-in / closed`，详见历史 progress 记录。

## 最近完成事项

- 2026-06-08 修复 `OI-128 / 批注抽屉` 顶部随列表滑走问题：用户指出首次 sticky 修法遮挡内容、过于草率后，已撤回 sticky 叠层方案，并按最新截图把锁定边界下移到“保存批注”区域。现在 `.annotation-drawer-panel` 固定外框不滚动，`.annotation-drawer-header` 和 `.annotation-drawer-fixed` 固定展示当前页、对象信息、添加批注输入框和保存按钮，`.annotation-drawer-scroll` 只滚动下方已有批注列表；抽屉底部留出 48px，不再贴到页面最底；删除保存按钮左侧“写入用户库，不修改基础数据”说明文字。同步切换滚动记忆和重绘保留逻辑，递增 `styles.css` 缓存版本，并在 `scripts/audit_user_annotation_contract.mjs` 增加结构分层防回归断言。本轮只改 `frontend/capability-browser/components/UserAnnotationDrawer.js`、`frontend/capability-browser/app.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html`、`scripts/audit_user_annotation_contract.mjs` 和状态文档，不改数据库、ETL、数据包或用户库。
- 2026-06-08 修复 `OI-138 / 关注点关系图谱标签碰撞`：按用户截图要求遵循图谱自适应和不碰撞原则，只在 `LocalRelationNetworkGraph` 增加标签候选位避让，不改节点布局、业务关系、颜色语义或页面结构。每个非中心节点标签会在右 / 左 / 上 / 下 / 斜向位置中按节点、已放置标签和曲线路径碰撞成本选择位置；外圈叶子节点优先把标签放到上下外侧；canvas 新增 `data-layout-label-overlaps` 指标。用户反馈首次修复后页面“没有变化”，已补齐 `app.js` 与动态组件版本号递增，确认页面实际加载 `capability-graph-label-avoidance-20260608-1`；验证显示 `/capability-mapping` 当前图谱 `data-layout-overlaps=0`、`data-layout-label-overlaps=0`、`data-layout-min-gap=22`。
- 2026-06-08 修复 `OI-133 / ArchiMate` 标题区 tab 状态不同步：`AppShell` 原先在页面标题行静态把第一个 tab 渲染为 active，导致内容已经切到 `SAPD 元素图例` 时，标题区仍高亮 `ArchiMate® 3.2 - 企业架构建模标准`。本轮将标题区 `ArchiMate® 3.2 / SAPD 元素图例` 和信息化环境页 `环境底图 / 归纳表格` 两组 tab 改为消费 `app.js` 中同一份 active tab 状态，避免 shell 重绘后回到第一个 tab。本轮只改 `frontend/capability-browser/components/AppShell.js`、`frontend/capability-browser/app.js` 和状态文档，不改数据库、ETL、数据包或用户库。
- 2026-06-07 按用户最新纠偏继续修正 `OI-133 / ArchiMate 建模语言页`：恢复两层标题结构，最大标题为 `安全架构建模语言`，第二标题为 `ArchiMate® 3.2 - 企业架构建模标准`；tab 组跟在最大标题后面并居右，`全页面显示` / `下载 PDF` 跟在第二标题后面；删除下方 `区域阅读`、区域目录、区域卡片和不精确热区；主体只保留可纵向滚动的整张 ArchiMate Poster；点击图片或 `全页面显示` 时在当前页面内打开 Image Lightbox / Fullscreen Modal，并对预览容器调用 Fullscreen API；不再使用 `window.open`、`target="_blank"`、`Blob` 页面或浏览器原生新窗口 UI；预览层支持右上关闭、Esc 关闭、点击遮罩空白关闭、放大、缩小、适应、鼠标滚轮缩放和按住拖动平移；预览工具栏已删除下载按钮；缩放以适应屏幕后的显示宽度为基准，按钮和滚轮均采用小步进并带宽度过渡，避免第一次操作直接跳成超大图；本地整图预览资源从 PDF 重新导出为 `6741 x 4768` 高分辨率 JPG；正常页面 Poster 去掉多余 padding，让图片与容器宽度贴合。同步 `open-issues.md`、`task_plan.md`、`CURRENT_STATE.md` 和优化计划。本轮不改数据库、ETL、用户库或 `SAPD 元素图例` registry；高分 JPG 位于已忽略前端资源目录，不纳入 Git 提交。
- 2026-06-07 继续微调 `OI-133 / ArchiMate 建模语言页`：第二标题栏 `ArchiMate® 3.2 - 企业架构建模标准` 及 `全页面显示` / `下载 PDF` 操作区改为 sticky 锁定在内容区顶部，避免向下滚动查看 Poster 时标题栏消失。本轮只改 `frontend/capability-browser/styles.css`，不改数据、数据库、ETL 或其他页面。
- 2026-06-07 继续微调 `OI-133 / ArchiMate 建模语言页`：取消外侧内容区滚动条，`content-list` 改为 `overflow: hidden`，页面高度由 `modeling-language-guide-panel` 吃满，只有 Poster viewer 保留 `overflow: auto`。这样页面不再出现外侧和图片容器双滚动条。
- 2026-06-07 修复 `OI-133 / SAPD 元素图例` 滚动回归：前一条单滚动条规则只应作用于 `ArchiMate® 3.2` Poster tab，不能影响 `SAPD 元素图例`。本轮给 `contentWorkspace` 增加 `data-modeling-language-tab` 标记，CSS 按 tab 区分：`overview` 隐藏外层滚动、只保留 Poster viewer 滚动；`elements` 恢复 `.content-list` 外层滚动，分组展开后内容可以撑开并滚动。
- 2026-06-07 修复 dashboard 显示不全回归：前一轮为 ArchiMate 单滚动条增加的通用 `overview-workspace` 高度 / overflow 规则覆盖了工作台总览自身滚动，导致 dashboard 内容被裁切。本轮在更靠后的样式层恢复 `.app-shell-integrated .overview-workspace` 的 `display: block`、`overflow: auto` 和原有内边距，确保 dashboard 使用自身滚动条完整展示。
- 2026-06-07 修复 `OI-133 / SAPD 元素图例` 折叠交互回归：按用户要求元素图例页默认全部收起，移除第一个分组默认 `open`；同时为 `.modeling-legend-section-summary` 增加显式点击与键盘 Enter / Space 切换逻辑，避免当前页面事件链导致原生 `<details>` 展开 / 收起不生效。
- 2026-06-07 继续修正 `OI-133 / SAPD 元素图例` 折叠控制：删除每个图例分组标题右侧的 `展开 / 收起` 文字状态胶囊，仅保留数量和箭头；在 `SAPD 元素图例` 二级标题行右侧新增 `全部展开` / `全部收起` 两个胶囊按钮，统一控制四个图例分组。
- 2026-06-07 固化会话卡顿交接规则：更新 `docs/07-governance/execution-line-convergence-workflow.md`，明确只有用户说“当前会话卡顿，需要交接”或等价表达时，Codex 才能停止当前执行、整理交接包、创建同名递增新线程、写入新线程初始提示并在当前会话最终回复后归档；没有明确卡顿交接请求时，不得自动创建新线程。同步 `CURRENT_STATE.md` 和 `docs/00-overview/master-context-restore.md`。本轮只改治理文档，不改代码、前端、数据库、数据包或用户库。
- 2026-06-07 根目录 `progress.md` 再次瘦身：将 2026-06-05 至 2026-06-07 的长验证清单归档摘要写入 `docs/05-archive/progress-history/2026-06.md`，根目录保留轻量恢复入口。
- 2026-06-07 完成 `OI-133 / ArchiMate 建模语言页` 第一轮页面优化：页面曾从“整页海报 + 6 个区域缩略图”调整为“区域目录 + 当前区域阅读器 + SAPD 映射说明”；该方案已被用户否定，后续热区点击方案也因切割不够精确被暂缓，当前以整图优先和近满屏查看为准。
- 2026-06-07 完成 `OI-128 / OI-135` 工作台总览和数据篮最小 API：`/api/v1/user/workspaces`、`/api/v1/user/data-baskets` 和 `/items` 支持创建、读取、条目 upsert、删除和 token 防护 smoke。真实基础库 / 用户库未写入。
- 2026-06-07 完成 `OI-135` 导出配置最小 API：ZIP runtime 启动时确保 `user_export_profiles` / `user_export_jobs` 表；新增 `/api/v1/user/export-profiles` 配置创建 / 读取 / 删除，新增 `/api/v1/user/exports/preview` 只生成 `draft` 预览记录和字段边界摘要，`GET /api/v1/user/exports/:id` 读取预览记录；当前不生成文件、不写 `output_path`、不改前端按钮、不写真实基础库或真实用户库。
- 2026-06-07 完成 `OI-135` 导出执行 / 下载最小闭环：新增 `POST /api/v1/user/exports`，基于已有 preview `job_id` 生成受控 JSON 文件到 ZIP bundle 的 `data/exports/` 并更新 job 为 `completed`；新增 `GET /api/v1/user/exports/:id/download` 下载该文件。当前只实现 JSON 最小闭环，不改前端入口，不写真实项目库。
- 2026-06-07 固化 `OI-135` 导出格式契约：新增 `docs/06-implementation/user-export-format-contract.md`，明确当前 JSON 只是最小验证闭环，后续 CSV / Markdown / ZIP / Excel 必须先确认导出对象、格式用途、字段边界、文件结构和实现顺序后再编码；同步 `CURRENT_STATE.md`、`task_plan.md`、`open-issues.md` 和用户工作区 / 用户库治理设计文档。
- 2026-06-07 按用户确认更新 `OI-135` 导出格式契约：后续导出表格基本参考原始数据，优先 Excel / CSV / Markdown；幻灯片材料单独导出 PDF；第一批业务数据集固定为能力全量映射、信息化环境安全映射、字典与标准框架数据。同步恢复入口和问题状态，下一步可先做三类业务数据集的 CSV / Excel schema 草案。
- 2026-06-07 补齐第一批业务导出数据集字段草案：`capability_full_mapping` 包含能力总览、技术视角映射、管理视角映射、标准映射；`environment_technology_mapping` 包含环境总览、环境-技术服务映射、环境-技术模块映射；`reference_dictionary_and_standards` 包含业务字典、标准框架、标准映射。下一步可进入导出器实现。
- 2026-06-07 完成 `OI-137 / 信息化环境首页 draw.io 底图导入`：从 `data/raw-samples/drawio sample.drawio` 第三页 `信息化环境及对象底图` 抽取 97 个顶点和 73 条连线，生成 `frontend/capability-browser/generated/environmentBasemap.generated.js`；环境页第一个 tab 改为 `环境底图`，以页面内 HTML / SVG 渲染实例底图，保留 `PC终端`、`云数据中心`、`远程办公接入`、`工厂` 等图中文字，不使用 `SAPD 元素图例` 样例文字；新增 `全页面显示`，对底图容器调用 Fullscreen API，不打开新窗口。
- 2026-06-07 追加修正 `OI-133 / SAPD 元素图例`：在关系线图例中新增 `连接关系 / Association`，说明其用于表达两个架构元素之间的一般连接或弱关联；线型样式参考 `访问关系` 的三种展示方式，并使用独立 `line-association` / `variant-association-*` CSS 类，避免后续调样式时影响访问关系。
- 2026-06-07 按用户给出的原图样例继续修正 `OI-133 / SAPD 元素图例` 关系线图例：`连接关系 / Association` 从访问关系样式中拆出，改为两条实线，上方无箭头、下方右侧开放箭头；关系线图例网格改为一行 4 个，与上方元素图例的横向排布一致。
- 2026-06-07 继续修正 `OI-137 / 信息化环境首页 draw.io 底图导入`：按用户纠偏将底图从近似静态 SVG 改为 HTML 交互图，连线保留 SVG 层，draw.io 顶点按原始坐标转成可点击 / 可聚焦的 HTML button；点击节点会高亮并在底部显示当前元素名称、类型和来源 cell id；背景容器、连线和前景节点分层显示；新增 `basemapFontSize` 全局文字适配，按框体宽高、行数和 draw.io 字号计算最终字号，减少文字溢出。
- 2026-06-07 完成 `OI-135 + DB-11 + DB-2` 正式迁移脚本三段式：`scripts/migrate_db_contracts.mjs` 默认 dry-run，只写 `/private/tmp` 复制库；`--apply` 才写目标库，真实项目库写入还需 `--confirm-project-db-write` 并自动备份。
- 2026-06-07 完成 `analytics_summary` P0 代码闭环：exporter、audit、`data_package_summary`、`dataClient.getAnalyticsSummary()` 和 dashboard 消费均已落地；生成 JSON 属于已忽略前端离线数据包，不纳入 Git。

## 最近验证

- 2026-06-08 `OI-128 / 批注抽屉顶部锁定` 验证：`node --check scripts/audit_user_annotation_contract.mjs`、`node --check frontend/capability-browser/components/UserAnnotationDrawer.js`、`python3 scripts/dev_server_guard.py --status`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/frontend_smoke_check.mjs --page capability --route /capability-mapping --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page content --route /guides/security-architecture-modeling-language --url http://127.0.0.1:5173`、`git diff --check -- frontend/capability-browser/styles.css frontend/capability-browser/index.html scripts/audit_user_annotation_contract.mjs` 均通过；未启动系统 Chrome。
- 2026-06-08 `OI-133 / tab 状态同步` 验证：`node --check frontend/capability-browser/components/AppShell.js`、`node --check frontend/capability-browser/app.js`、`python3 scripts/dev_server_guard.py --status`、`node scripts/frontend_smoke_check.mjs --page content --route /guides/security-architecture-modeling-language --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page environment --route /environment-mapping --url http://127.0.0.1:5173`、定向源码断言和 `git diff --check -- frontend/capability-browser/app.js frontend/capability-browser/components/AppShell.js` 均通过；本地 Playwright 不可用，Browser 插件工具未暴露，未启动系统 Chrome。
- 2026-06-07 `OI-133 / ArchiMate` 最新验证：`rg 'download="archimate-poster-overview|下载图片|>下载<|window.open|target="_blank"|createObjectURL|new Blob|Blob\(' frontend/capability-browser/app.js frontend/capability-browser/styles.css` 无命中；`sips -g pixelWidth -g pixelHeight frontend/capability-browser/public/data/guides/archimate-poster/archimate-poster-overview.jpg` 确认为 `6741 x 4768`；`node --check frontend/capability-browser/app.js`、`node --check frontend/capability-browser/components/AppShell.js` 通过；`python3 scripts/dev_server_guard.py --status` 通过，固定 `5173` 单一项目服务健康；`node scripts/frontend_smoke_check.mjs --page content --route /guides/security-architecture-modeling-language --url http://127.0.0.1:5173` 轻量 HTTP smoke 通过，未启动系统 Chrome；内置浏览器检查确认点击 `全页面显示` 后工具栏文本为 `− 适应 ＋` 且下载控件数量为 0，图片自然尺寸为 `6741 x 4768`，初始适应宽约 `984px`，第一次滚轮仅放大到约 `1063px`，再点一次放大按钮约 `1240px`，拖动后滚动位置发生变化，Esc 可关闭并恢复原页面；源码确认 `requestModelingPosterFullscreen()` 会在浏览器提供 `requestFullscreen` 时对预览容器调用 Fullscreen API，内置浏览器运行时本身未暴露该 API，无法在该环境断言 `document.fullscreenElement`；`git diff --check` 通过；`python3 scripts/check_github_data_boundary.py` 通过。
- 2026-06-07 `OI-133 / ArchiMate` sticky 标题栏验证：`node --check frontend/capability-browser/app.js`、`python3 scripts/dev_server_guard.py --status`、`node scripts/frontend_smoke_check.mjs --page content --route /guides/security-architecture-modeling-language --url http://127.0.0.1:5173`、`git diff --check` 均通过；内置浏览器检查确认 `.modeling-language-guide-header` 为 `position: sticky`，滚动查看时标题栏保持在内容区顶部。
- 2026-06-07 `OI-133 / ArchiMate` 单滚动条验证：`node --check frontend/capability-browser/app.js`、`node scripts/frontend_smoke_check.mjs --page content --route /guides/security-architecture-modeling-language --url http://127.0.0.1:5173`、`git diff --check` 均通过；内置浏览器检查确认外层 `.content-list` 为 `overflowY: hidden` 且 `scrollHeight == clientHeight`，Poster viewer 为 `overflowY: auto` 且只保留图片内部滚动。
- 2026-06-07 `OI-133 / SAPD 元素图例` 滚动验证：`node --check frontend/capability-browser/app.js`、`python3 scripts/dev_server_guard.py --status`、`node scripts/frontend_smoke_check.mjs --page content --route /guides/security-architecture-modeling-language --url http://127.0.0.1:5173`、`git diff --check` 均通过；内置浏览器检查确认切到 `SAPD 元素图例` 后 `.content-list` 为 `overflowY=auto`，分组展开时内容高度可超过容器高度并触发滚动。
- 2026-06-07 dashboard 显示不全回归验证：`git diff --check -- frontend/capability-browser/styles.css`、`python3 scripts/dev_server_guard.py --status` 均通过；内置浏览器检查 `http://127.0.0.1:5173/` 确认 `#overviewWorkspace` 为 `overflowY=auto`、`scrollHeight=2395` 大于 `clientHeight=560`，滚动后 `scrollTop` 从 `0` 到 `900`，页面整体 `documentScrollTop` 保持 `0`。
- 2026-06-07 `OI-133 / SAPD 元素图例` 折叠交互验证：`node --check frontend/capability-browser/app.js`、`node scripts/frontend_smoke_check.mjs --page content --route /guides/security-architecture-modeling-language --url http://127.0.0.1:5173`、`git diff --check -- frontend/capability-browser/app.js` 均通过；内置浏览器检查确认进入 `SAPD 元素图例` 后四个分组默认 `open=false`，点击第一个标题后变为 `open=true`，再次点击恢复 `open=false`。
- 2026-06-07 `OI-133 / SAPD 元素图例` 总控验证：`node --check frontend/capability-browser/app.js`、`node scripts/frontend_smoke_check.mjs --page content --route /guides/security-architecture-modeling-language --url http://127.0.0.1:5173`、`git diff --check -- frontend/capability-browser/app.js frontend/capability-browser/styles.css` 均通过；内置浏览器检查确认分组内 `.modeling-legend-section-state` 数量为 `0`，二级标题行右侧存在 `全部展开` / `全部收起`，点击后四个图例分组可全部 `open=true` / 全部 `open=false`。
- 2026-06-07 会话卡顿交接规则固化验证：`rg '当前会话卡顿，需要交接|同名递增|不得自动创建新线程|交接包' CURRENT_STATE.md docs/00-overview/master-context-restore.md docs/07-governance/execution-line-convergence-workflow.md progress.md -n` 通过；`git diff --check -- CURRENT_STATE.md docs/00-overview/master-context-restore.md docs/07-governance/execution-line-convergence-workflow.md progress.md docs/05-archive/progress-history/2026-06.md` 通过。
- 2026-06-07 `OI-133 / ArchiMate`：`node --check frontend/capability-browser/app.js`、`python3 scripts/dev_server_guard.py --status`、`node scripts/frontend_smoke_check.mjs --page content --route /guides/security-architecture-modeling-language --url http://127.0.0.1:5173`、`git diff --check -- frontend/capability-browser/app.js frontend/capability-browser/styles.css` 均通过；未启动系统 Chrome。
- 2026-06-07 `AN-SUM-DASHBOARD`：`node --check frontend/capability-browser/app.js`、`node scripts/audit_analytics_summary_contract.mjs`、`node scripts/frontend_smoke_check.mjs --page overview --url http://127.0.0.1:5173`、`python3 scripts/check_github_data_boundary.py`、`git diff --check` 均通过。
- 2026-06-07 `OI-135 / DB-2`：`node --check scripts/migrate_db_contracts.mjs`、默认 dry-run、临时库 apply、迁移后 `audit_user_db_governance_contract.mjs --require-v03` 和 `audit_stable_key_contract.mjs` 均通过；真实项目库 `--apply` 被确认门拦截。
- 2026-06-07 数据篮 API：`python3 -m py_compile scripts/run_local_server.py`、`node --check scripts/smoke_user_data_basket_api.mjs`、`node scripts/audit_user_db_governance_contract.mjs`、`node scripts/smoke_user_data_basket_api.mjs` 均通过，真实项目库未写入。
- 2026-06-07 导出配置最小 API：`python3 -m py_compile scripts/run_local_server.py`、`node --check scripts/smoke_user_data_basket_api.mjs`、`node --check scripts/audit_user_db_governance_contract.mjs`、`node scripts/audit_user_db_governance_contract.mjs`、`python3 scripts/check_github_data_boundary.py` 均通过；`node scripts/smoke_user_data_basket_api.mjs` 首次在沙箱内因本地端口绑定权限失败，提升权限后通过，临时 ZIP runtime 验证 `exportProfileAuthRejected`、`exportProfileCreated`、`exportProfileListed`、`exportProfileForbiddenFieldsRejected`、`exportPreviewCreated`、`exportJobRead`、`exportProfileDeleted` 全部为 `true`，真实项目库未写入。
- 2026-06-07 导出执行 / 下载最小闭环：`python3 -m py_compile scripts/run_local_server.py`、`node --check scripts/smoke_user_data_basket_api.mjs`、`node --check scripts/audit_user_db_governance_contract.mjs`、`git diff --check` 均通过；提升权限后运行 `node scripts/smoke_user_data_basket_api.mjs` 通过，临时 ZIP runtime 验证 `exportExecuted`、`exportDownloaded`、`exportDownloadBoundaryChecked` 均为 `true`，下载 JSON 中未出现 `sheet`、`row`、`raw_value`、`source_ref`、`generated_at` 等禁止字段键，真实项目库未写入。
- 2026-06-07 导出格式契约验证：`git diff --check`、`node scripts/audit_user_db_governance_contract.mjs`、`python3 scripts/check_github_data_boundary.py` 通过；`python3 scripts/data_package_summary.py --package capability-workbench`、`--package environment-workbench`、`--package standards`、`--package shared-lookups`、`--package content` 通过，确认相关数据包 `data_state=ready`。曾误用脚本不支持的 `--package standards-index`、`--package content-views`，脚本返回 invalid choice，已改用支持的 `standards`、`content` 包名完成验证。
- 2026-06-07 `OI-137 / 信息化环境首页 draw.io 底图导入` 验证：`node scripts/generate_environment_basemap_from_drawio.mjs` 输出 `pageIndex=3`、`nodes=97`、`edges=73`；`node --check scripts/generate_environment_basemap_from_drawio.mjs`、`node --check frontend/capability-browser/generated/environmentBasemap.generated.js`、`node --check frontend/capability-browser/components/EnvironmentLocalRelationMap.js`、`node --check frontend/capability-browser/app.js` 均通过；`python3 scripts/dev_server_guard.py --status` 通过，固定 `5173` 单一项目服务健康；`node scripts/frontend_smoke_check.mjs --page environment --url http://127.0.0.1:5173` 轻量 HTTP smoke 通过，未启动系统 Chrome；源码断言确认生成数据包含 `信息化环境及对象底图`、`PC终端`、`云数据中心`、`远程办公接入`、`工厂`，且不包含 `SAPD 元素图例`；`git diff --check` 通过。
- 2026-06-07 `OI-133 / SAPD 元素图例` 连接关系图例验证：`node --check frontend/capability-browser/app.js`、`python3 scripts/dev_server_guard.py --status`、`node scripts/frontend_smoke_check.mjs --page content --route /guides/security-architecture-modeling-language --url http://127.0.0.1:5173`、`git diff --check -- frontend/capability-browser/app.js frontend/capability-browser/styles.css` 均通过；源码断言确认 `MODELING_RELATION_LEGENDS` 包含 `服务关系`、`数据流 / 控制流`、`访问关系`、`连接关系` 共 4 类关系，且存在 `line-association`、`variant-association-none`、`variant-association-both` 样式。
- 2026-06-07 `OI-133 / 连接关系样式替换` 验证：`node --check frontend/capability-browser/app.js`、`node scripts/frontend_smoke_check.mjs --page content --route /guides/security-architecture-modeling-language --url http://127.0.0.1:5173`、`git diff --check -- frontend/capability-browser/app.js frontend/capability-browser/styles.css` 均通过；源码断言确认关系线图例 `grid-template-columns: repeat(4...)`，`连接关系` 仅包含 `association-none` / `association-end` 两条线，且 `line-association` 为实线并不再继承访问关系虚线样式。
- 2026-06-07 `OI-137 / 信息化环境首页 draw.io 底图交互化` 验证：`node --check frontend/capability-browser/components/EnvironmentLocalRelationMap.js`、`node --check frontend/capability-browser/app.js`、`python3 scripts/dev_server_guard.py --status`、`node scripts/frontend_smoke_check.mjs --page environment --route /environment-mapping --url http://127.0.0.1:5173` 均通过；源码断言确认存在 `data-environment-basemap-node` HTML button、`environmentBasemapSelection` 选中信息面板、`basemapFontSize` 字体适配函数、背景 / 前景节点分层，以及 `PC终端`、`远程办公接入`、`云数据中心`、`工厂` 等原图文字。
- 2026-06-07 `OI-137 / 信息化环境底图严格修正`：修正 draw.io 到 HTML 的转译规则，生成器新增 `align`、`verticalAlign`、`textDirection`、`fontWeight`、`spacing`、`entryX/Y`、`exitX/Y`、`sourcePoint` / `targetPoint` 和默认颜色归一；前端改为百分比坐标 / 响应式舞台、按边界锚点绘制连线、补 SVG 开放箭头、按原图对齐文字并渲染 ArchiMate appType 角标。`node --check scripts/generate_environment_basemap_from_drawio.mjs`、`node --check frontend/capability-browser/components/EnvironmentLocalRelationMap.js`、`node --check frontend/capability-browser/generated/environmentBasemap.generated.js`、`node scripts/frontend_smoke_check.mjs --page environment --route /environment-mapping --url http://127.0.0.1:5173`、`python3 scripts/check_github_data_boundary.py` 和定向 `git diff --check` 均通过；未启动系统 Chrome。
- 2026-06-07 `OI-137 / 信息化底图设计方案修正`：根据用户截图对比，确认继续用 HTML 盒子仿画复杂 draw.io 会导致微型图元、字号和连线在缩放时互相挤压；已改为高保真 SVG 视觉层 + 可点击 SVG DOM 节点方案，连线先绘制、节点后绘制，避免线条压住文字；节点保留 `data-environment-basemap-node`、点击高亮、底部选中信息和 Enter / 空格键选中。`node --check frontend/capability-browser/components/EnvironmentLocalRelationMap.js`、`node --check frontend/capability-browser/app.js`、`node scripts/frontend_smoke_check.mjs --page environment --route /environment-mapping --url http://127.0.0.1:5173`、`python3 scripts/check_github_data_boundary.py`、`git diff --check` 均通过；资源版本号递增到 `environment-basemap-20260607-3`。
- 2026-06-07 `OI-137 / Draw.io 语义 HTML 底图`：按用户最新确认，目标不是纯 SVG Viewer，而是可绑定业务数据的语义 HTML 底图；当前直接解析 `.drawio` 第三页 `mxGraphModel`，以原始 `mxCell` / `mxGeometry` / `style` / `edge` 为依据生成 `frontend/capability-browser/generated/environmentBasemap.html`、`environmentBasemap.css`、`environmentBasemap.generated.js` 和 `environmentBasemap.semantic.json`。每个 vertex 是带 `data-mx-id`、`data-object-type`、`data-bind-status` 的绝对定位 HTML 节点，每条 edge 是 SVG overlay 连线；页面提供适应、缩放、拖拽平移、全屏、点击高亮和 hover label / mxId。`node scripts/generate_environment_basemap_from_drawio.mjs` 输出 `cells=170`、`nodes=97`、`edges=73`；资源版本号递增到 `environment-basemap-20260607-5`。
- 2026-06-07 `OI-137 / 语义 HTML 底图验证`：`node --check scripts/generate_environment_basemap_from_drawio.mjs`、`node --check frontend/capability-browser/generated/environmentBasemap.generated.js`、`node --check frontend/capability-browser/components/EnvironmentLocalRelationMap.js`、`node --check frontend/capability-browser/app.js`、`python3 scripts/dev_server_guard.py --status`、`node scripts/frontend_smoke_check.mjs --page environment --route /environment-mapping --url http://127.0.0.1:5173`、`python3 scripts/check_github_data_boundary.py`、`git diff --check` 均通过；源码断言确认 HTML 节点 `97`、SVG 连线 `73`、semantic JSON 节点 `97`、连线 `73`，且节点包含 `data-mx-id`、`data-object-type`、`data-bind-status`；提升权限只读 fetch 确认 `5173` 可访问生成的 HTML / CSS / JSON。
- 2026-06-08 `OI-137 / Environment Basemap Binding 1.0`：新增 `scripts/bind_environment_basemap_semantics.mjs` 和 `frontend/capability-browser/generated/environmentBasemap.binding-overrides.json`，按精确名称、规范化名称、别名、父级容器上下文和手工 override 入口绑定 draw.io 语义节点；更新 `environmentBasemap.semantic.json`、`environmentBasemap.html`、`environmentBasemap.generated.js`，新增 `environmentBasemap.binding-report.json` / `.md`。按用户确认，`OT`、`数据中心机房`、`IT`、`客户`、`互联网`、`外联网` 属于数据归类 / 地点容器 / 外联环境，不绑定保护对象数据，标记为 `ignored`；`工厂` 绑定 `工业控制网络`；`互联网` / `外联网` 不再绑定到 `互联网边界` / `外联网边界`。当前统计：`nodes=97`、`edges=73`、`bound=91`、`candidate=0`、`unbound=0`、`ignored=6`。
- 2026-06-06 `OI-136 / FE-ROUTE`：`node --check scripts/frontend_smoke_check.mjs`、`node --check frontend/capability-browser/app.js`、`python3 scripts/dev_server_guard.py --status` 和三类深层路由轻量 HTTP smoke 均通过；本轮未启动系统 Chrome。

## 当前问题索引

- `OI-038`：Gartner 与安全职能候选映射需后续人工校对，状态 `待确认`。
- `OI-128`：USER-WRITE-UI-1：批注 / 工作台用户写入入口，状态 `部分完成`；`OI-128A/B/C` 已实现，`OI-128C` 已基本验收。
- `OI-133`：ArchiMate 建模语言页显示效果与加载效率优化，状态 `已修复 / 待人工验收`；当前已按用户最新纠偏改为整图优先、上方 tab、可滚动 Poster 和页面内 Image Lightbox / Fullscreen Modal，不做不精确热区，不打开新窗口或 `Blob` 页面。
- `OI-135`：用户库治理与兼容表迁移清理，状态 `正式迁移脚本完成 / 真实库 apply 待显式确认`。
- `OI-136`：深层路由直接访问未加载前端样式，状态 `已修复 / 已归档`。
- `OI-137`：信息化环境首页需导入 draw.io 第三页实例底图，状态 `已修复 / 待人工验收`；当前首页第一个 tab 已改为 `环境底图`，以 HTML / SVG 渲染 draw.io 第三页实例图。

## 历史索引

| 归档文件 | 内容 |
|---|---|
| `docs/05-archive/progress-history/2026-06.md` | 2026-06 完整执行记录、Open Issues 治理、前端治理、数据口径确认和本轮 progress 瘦身摘要 |
| `docs/05-archive/progress-history/2026-05.md` | 2026-05 完整执行记录、根目录 `progress.md` 瘦身快照和结构治理记录 |
| `docs/05-archive/findings-history/2026-05.md` | 2026-05 历史发现 |
| `docs/05-archive/open-issues-history/2026-06.md` | 已关闭 Open Issues 长记录 |
| `docs/05-archive/context-slimming-2026-05-15/` | 2026-05-15 上下文瘦身快照 |

## 维护规则

- 本文件只保留最近状态、最近 5-10 条重要执行和恢复入口；超过 120 行时继续归档到 `docs/05-archive/progress-history/`。
- 详细过程、长命令输出和阶段性历史不写入根目录 `progress.md`。

## 2026-06-08 Environment Basemap 联动与 Viewer 稳定摘要
- Node Detail Projection / Interaction / Viewer Stabilization 已完成：`node-details` 统计保持 `nodes=97`、`edges=73`、`bound=91`、`ignored=6`、`detailReadyNodes=91`、`missingDetailNodes=0`；前端点击 bound / ignored 节点可展示业务详情 / 忽略说明；viewer 已改为 toolbar / viewport / panZoomLayer / detail 分层，支持默认 fit、鼠标位置缩放、拖拽平移、拖拽 click 抑制和 500ms 延迟 tooltip。验证：相关 `node --check`、环境页 HTTP smoke、资源级 5173 fetch、结构断言、`python3 scripts/check_github_data_boundary.py` 和 `git diff --check` 均通过；未修改 draw.io、semantic 坐标、`environment-workbench.json` 或关系高亮。

## 2026-06-08 Environment Basemap 页面结构对齐
- 按 `安全架构建模语言 / ArchiMate® 3.2` 页面结构调整信息化环境页：`AppShell` 在 `信息化环境维度` 主标题行右侧新增 `环境底图 / 归纳表格` tab，内容面板内移除旧 tab 条；2026-06-08 追加按用户给出的 ArchiMate 内容面板基准收紧底图面板，删除全局 `导出数据` / `编辑映射` 假按钮，删除底部 `点击底图中的业务对象查看详情。` 提示栏，底图二级标题栏固定为左标题、右侧基准按钮区，仅保留真实可用的 `全页面显示`，不再展示统计胶囊，也不再渲染 `适应 / − / +` 可见缩放按钮组；标题语义与字体改为和 ArchiMate 基准相同的 `h2` + 同一 CSS 标题规则。验证：`node --check` 相关前端文件、环境页 HTTP smoke、结构断言、资源级 5173 fetch、`python3 scripts/check_github_data_boundary.py` 和 `git diff --check` 通过；本轮未启动系统 Chrome。
- 继续按用户红框截图修正底图内部 fidelity：`scripts/generate_environment_basemap_from_drawio.mjs` 改为用对象区域约束边线端点，离散到空白区的显式端点不再撑大画布，带 source / target 但端点漂出对象过远的边线回贴到源 / 目标对象之间；`environmentBasemap.css` 的 `appType` 角标改为随字体缩放，并覆盖区域块、网络、节点、系统软件和应用组件。重新生成并绑定后保持 `nodes=97`、`edges=73`、`bound=91`、`ignored=6`；断言确认顶部红框边线回到 `y≈285`，右侧红框边线回到 `x=2870→2985`，`工厂`、`L3内部网络`、`L3主机操作系统`、`业务应用`、`广域网骨干节点` 均有右上角角标。验证：生成器 / 绑定脚本 / 生成 JS / 相关前端 `node --check`、`python3 scripts/dev_server_guard.py --status`、环境页 HTTP smoke、`python3 scripts/check_github_data_boundary.py`、`git diff --check` 均通过；内置 Browser 访问 `5173` 被安全策略阻止，未绕过。
- 执行 `Environment Basemap Draw.io Style Fidelity & Canvas Bounds Fix 1.0`：生成器新增视觉类型 `drawioType / iconType`、`originalGeometry / normalizedGeometry`、`contentBounds` 和 `styleSummary`，当前识别 `grouping=6`、`location=6`、`communication_network=10`、`facility=2`、`application_component=24`、`node=25`、`device=6`、`system_software=12`、`data_object=1`、`actor=5`，未知节点为 0；底图自身背景改为透明，避免和 Viewer 网格叠加；canvas 收敛为 `3086×2126`，`contentBounds={minX:160,minY:150,maxX:3150,maxY:2180,padding:48}`；红框顶部边线回到 `y≈183`，右侧边线回到 `x=2758→2873`；绑定保持 `nodes=97`、`edges=73`、`bound=91`、`candidate=0`、`unbound=0`、`ignored=6`。验证：生成器 / 生成 JS / 组件 / app `node --check`、绑定脚本、style 断言、`python3 scripts/dev_server_guard.py --status`、环境页 HTTP smoke、资源级 5173 fetch、`python3 scripts/check_github_data_boundary.py`、`git diff --check` 均通过；未做业务联动、关系高亮、node-details 或 `environment-workbench.json` 修改。
- 执行 `Environment Basemap HTML Fidelity System 1.0`：生成器建立统一 `DRAWIO_ELEMENT_STYLE_MAP`，按 SAPD 元素图例页的颜色和 `renderArchimateCornerIcon` SVG 路径复用角标规则，新增 `environmentBasemap.style-fidelity-report.json/.md`；HTML 拆为 container / edge / node / label-icon 四层，避免连线压住节点文字和角标；绑定状态不再灰化或改色节点；字体按 Draw.io `fontSize/fontStyle/textDirection/rotation` 优先，缺省再按元素类型和大区标题兜底；重新生成后保持 `nodes=97`、`edges=73`、`bound=91`、`candidate=0`、`unbound=0`、`ignored=6`，`contentBounds` 和 canvas 不变。验证：生成器 / 生成 JS / 组件 / app `node --check`、绑定脚本、style fidelity 断言、`python3 scripts/dev_server_guard.py --status`、环境页 HTTP smoke、资源级 5173 fetch、内置 Browser DOM 回归、`python3 scripts/check_github_data_boundary.py`、`git diff --check` 均通过；Browser 截图接口超时，未启动系统 Chrome；未修改业务联动、node-details、`environment-workbench.json` 或原始 draw.io。
- 执行 `Environment Basemap Renderer Fidelity 2.0`：生成器继续修通用 renderer 规则，容器标题改为独立 label box 并按宽度严格换行，竖排文字按窄高节点 / `textDirection` 推断，`大数据平台/数据中台` 等长标题不再误判单行；连线改为读取原始 waypoints，缺省 `orthogonalEdgeStyle / elbowEdgeStyle` 使用直角 fallback，不再用中心点斜线硬连；输出 `environmentBasemap.fidelity-report.json/.md` 别名，报告显示 `overflowRiskNodes=0`、`unsupportedEdges=0`、`edgeRouteKinds={orthogonal_fallback:32,orthogonal_straight:36,original_waypoints:5}`、`whiteboardBackgroundRendered=false`。重新生成并绑定后保持 `nodes=97`、`edges=73`、`bound=91`、`candidate=0`、`unbound=0`、`ignored=6`，node-details 统计保持 `detailReadyNodes=91`、`missingDetailNodes=0`；验证：生成器 / 绑定脚本 / 生成 JS / 组件 / app `node --check`、`python3 scripts/dev_server_guard.py --status`、环境页 HTTP smoke、资源级 5173 fetch、`python3 scripts/check_github_data_boundary.py`、`git diff --check` 均通过；本轮未启动系统 Chrome，Browser 工具未暴露，未修改业务联动、node-details、`environment-workbench.json` 或原始 draw.io。
