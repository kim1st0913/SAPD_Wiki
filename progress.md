# progress.md
本文件是当前会话恢复入口，只保留最近状态、最近关键动作、验证摘要和历史索引。完整执行历史已归档到 `docs/05-archive/progress-history/`。

## 当前状态（2026-07-13）

- 当前分支：`main`；最近提交状态以 `git log -1 --oneline` 和远端 `origin/main` 为准。工作区存在改动时，提交必须只 stage 明确相关文件，不能使用 `git add .`。
- Git / GitHub 收口专用会话已建立并置顶：`SAPD Wiki GitHub 提交收口专用 2026-07-07`，thread id `019f3bee-d713-7301-be67-6d555012a5c9`。后续普通开发会话默认不提交、不推送、不建 PR；需要 checkpoint / push 时优先交给该会话分组收口。
- 固定预览入口：`http://127.0.0.1:5173/`。2026-07-13 全工程前端 P0-1、P0-2 与 P0-4 已实施。P0-4 统一 8 个标准深链的全局入口 / 当前框架双层定位与 `10px` 导航留白；Issue 取消 `rows[0]`、批量勾选和旧路由选择的隐式详情，首次 inspector 宽度为 `0`，显式打开、关闭还宽并返回焦点，路由重入不泄漏旧选择。P0-4 专项、滚动 owner 与完整 `frontend` 套件通过；应用内浏览器覆盖初始、打开、关闭、键盘和路由重入。P0-1 四级对象、Draw.io 哈希 / overlay、语义色、键盘 / 播报及字段门禁继续通过；P0-2 旧 DMG 标题真值与 Apple Shell 基座继续通过。未修改标准 / Issue 数据、用户库、路由清单、成熟度、正式数据、SQLite、源 Excel 或 DMG；未启动系统 Chrome，DMG App 回归后置。
- 主控问题处理契约已写入 `AGENTS.md`：用户给出的截图、搜索词、单条数据或页面现象默认是问题样例，不是单点任务；必须先定系统规则、边界和验收，再改代码 / 数据 / 页面，并在完成反馈说明固化了哪条规则。
- 发布验收矩阵已新增为 `docs/09-delivery/release-acceptance-matrix-0.1.md`：SAPD Wiki DMG 发布验收对象明确为“同源知识库 Runtime 在本地桌面 App 中，带用户状态、授权状态、保存路径和导出路径后，能否稳定完成顾问核心工作流”；`audit_mac_dmg_browser_parity_contract.mjs` 已纳入矩阵关键门禁检查。后续不能只说“Web/DMG 通过”，必须按 G0-G7 自动 / 人工矩阵给出证据、阻断级别和业务接受限制。
- Web / App 双运行面 bug 修复规则已写入 `AGENTS.md`、`docs/09-delivery/mac-dmg-browser-parity-contract.md` 和 `docs/09-delivery/release-acceptance-matrix-0.1.md`：每个 bug 根因修复先声明 `shared runtime`、`data / ETL / JSON package`、`web-only`、`app-only` 或 `release blocker`，完成反馈必须给出影响面、根因层和验证范围；`app-only` 与 P0 / P1 `release blocker` 不能只用 5173 通过关闭。
- 2026-07-14 `PLAN-MAT-WS` 第六轮评分工作台根因纠偏完成实现与 Design QA：服务 Tab 改为状态、完整名称和适用性方框共享一个卡片边界，1280px 下三列两行；四维评分未选格为 `42px`，当前格平滑扩展到约 `432 × 66px`，数字、等级名和定义全部在格内；项目名称 / 模板 / 更新时间与六个项目步骤合并为 `100px` sticky 整区。真实点击 `3 → 2 → 3` 后轨道和定义同步、滚动 `253.5 → 253.5`；滚动 `520px` 后完整项目区仍固定；1280 / 1024 / 390px 页面级横向溢出均为 `0`。三份成熟度强制文档、Design QA、`OI-192` 和专项审计已同步；`node --check`、Python 编译、成熟度 `109/109`、maturity smoke、完整 `static,frontend` 套件、数据边界、`git diff --check`、5173 守护和应用内浏览器空日志均通过。未修改正式 SQLite / JSON、源 Excel、字典 / 标准 / 生命周期基线、用户库或 DMG；待用户按固定入口人工验收，DMG App 证据后置。
- 2026-07-13 第二轮 V2.1 Web 验收与方案 1 真实工程复刻已闭环：评分执行页取消重复项目详情头，进度 / 自动保存与“更多”进入真实 App Shell 页头；L0 / L1 / L2、L2 汇总、关注点摘要、关注点列表和 SERVICE / FOCUS 四维表单保持同构，低频工具完整收纳，真实数据不被视觉稿样例覆盖。评分 / 适用性真实点击后保持原位，批量初始化、等级定义和结果页后端聚合规则不变。三份成熟度文档已同步；专项审计 `82/82`、maturity HTTP/API smoke、`1440 / 1024 / 390px` 无横向溢出、字段边界和控制台检查通过；5173 守护状态初检短暂 `warn` 后复检为 `pass`。全量 content smoke 本地包检查通过，API 阶段仍出现 `fetch failed`，不作为成熟度专项通过项；正式 SQLite / JSON、源 Excel、用户库和 DMG 未修改。
- 2026-07-13 本轮根因规则已固化：评分页只允许一个页头信息归属层，设计稿中的样例数量和分值不得覆盖真实工程数据；后续代码任务必须以两张 visual truth、真实路由和 `design-qa.md` 的同屏证据验收，不能只按文字近似复刻。本轮不写正式 SQLite / JSON、源 Excel、用户库或 DMG。
- 用户已裁定取消额外预览端口、额外分支和额外工作区；新数据与试验回到当前 `main`，仅通过受控 demo 页 / demo 数据验证。稳定 SQLite、正式 JSON、源 Excel、用户库和 DMG 不得被 demo 覆盖；`PLAN-AI-CAP-EXT` 同样保持 demo-first，正式提升必须另行确认权威源、写入范围、回退和审计。
- 文档 / Issue 控增量与禁止范围：小修默认直接闭环，不新建文档或 `OI`；只有跨模块、数据 / 审计 / 安全、中高严重性、无法本轮关闭或需业务判断的问题进入 `open-issues.md`。不得默认修改源 Excel、SQLite、正式 JSON、标准 / 字典 / LC / 环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。
- 当前已知业务状态：按用户 2026-07-06 最新验收口径，`OI-146 / OI-147` 已按源表行级服务-目标组合与 H 列合并单元格展开后的目标级安全系统关系修复并二次复核关闭；`OI-135` 已按用户确认关闭归档；`OI-157` 已按当前 macOS DMG 交付口径重新打包验收并自动关闭归档；`OI-190` 已按用户“两个方案都否决，忽略”标记为业务接受并归档。当前未关闭问题数为 `4`、已关闭归档问题数为 `190`；`OI-192`、`OI-191` 已修复并待用户验收，另有 `OI-138`、`OI-128` 两项长期问题。环境映射安全系统关系规则已固化：只认源表 `作用域-安全技术服务-安全技术模块映射` 的行级服务-目标组合及 H 列事实，不从服务级聚合反推模块 / 措施，不让 H 列为空的目标继承系统，也不保留源表不存在的服务-目标组合；`OI-135` 已完成真实库 apply：正式基础库已替换为 clean base stable candidate，正式用户库 2 条 legacy target_ref 已迁到 stable ref，正式库组合 `legacyBaseRefs=0`，运行时和打包创建入口已统一到 `user_schema_0.3`，正式用户库 `user_meta.schema_version=user_schema_0.3`。
- 最新 macOS DMG 交付物：2026-07-08 已基于当前工作区升级到正式版本 `0.1.7` 并重新生成双变体 DMG，构建时间戳为 `20260708-030834Z`。授权版为 `apps/macos/SAPDWiki/dist/license/SAPD-Wiki-0.1.7-license-20260708-030834Z-mac-arm64.dmg`，大小 `163M`，SHA-256 `25f5502795bd69539335c45020b09fdf047d48d937b8e330a9cc94329a0f0912`；无授权版为 `apps/macos/SAPDWiki/dist/no-license/SAPD-Wiki-0.1.7-no-license-20260708-030834Z-mac-arm64.dmg`，大小 `163M`，SHA-256 `904b55c29a9b8891b16adc1684348ebf301dd56639151b9850220cd9b51aab97`。系统内版本已统一为 `0.1.7`；`Info.plist` 为 `CFBundleShortVersionString=0.1.7`、`SAPDWikiDisplayVersion=0.1.7`，并用 `SAPDWikiLicenseMode=license/no-license` 区分包类型。本轮规则：外层和 Runtime 内层 `README-FIRST.md` 均保留 `Changelog` 并新增 `0.1.7` 条目；用户选择保存位置时选的是父级目录，App 自动创建 `SAPDWiki` 子目录，用户库路径为 `<所选父级保存位置>/SAPDWiki/Runtime/data/user/sapd_wiki_user.sqlite3`，默认下载路径为 `<所选父级保存位置>/SAPDWiki/export`。授权版启动前弹授权窗口，正确授权码 `Passc0de` 后显示 `已激活`；不知道授权码可跳过进入 30 天试用；试用到期后禁止跳过。无授权版不弹授权窗口，不启用 30 天倒计时。DMG 为 ad-hoc signed 非公证包；本地生成文件未带 `com.apple.quarantine` 扩展属性，外部分发仍可能需要在 macOS `隐私与安全性` 中手动允许打开。

## 最近完成事项

- 2026-07-12 `Maturity V2.1 Scoring Workspace Complete Design Revision`：依据用户当前页面截图和六项反馈，完成评分工作区完整设计修订。页面从全部评估项纵向平铺改为 `L0 分类 → L1 主 Tab → L2 次级 Tab → L2 汇总 → 关注点分块 → 评估点行内评分`；L2 增加不可折叠的当前 / 目标 / 差距 / 完成度 / 四维平均分汇总；关注点反馈由“混合”下拉改为后端加权平均分，统一设置拆为独立批量动作；每个维度改用 1—5 方形按钮并常驻显示模板 L1—L5 标准；每个评估点增加可自动保存的“评估证据说明（可选）”。同时固化 Apple Shell 字号层级、响应式、键盘与平均分边界，并同步业务设计、成熟度实现规格、全工程优化方案、展示治理、计划、发现和当前状态。当前运行代码尚未调整；未修改 demo 数据、SQLite、正式 JSON、源 Excel、用户库或 DMG。
- 2026-07-12 `Full Frontend Risk-to-Solution Matrix`：按用户纠偏补齐“所有已识别风险都必须输出方案”的交付缺口。在 `frontend-global-optimization-plan-2026-07-11.md` 新增 `FE-R01—FE-R50` 逐风险矩阵，覆盖全局壳 / 总览、能力映射、环境映射、LC-AP、LC-DT、知识字典、标准框架、指南、Issue、成熟度、全局搜索及共享组件 / 状态 / 可访问性；每项均明确具体方案、不改边界、验收和 P0/P1/P2。能力图谱碰撞、Draw.io 原图不可变和成熟度 V2.1 完整设计已作为对应页面强制约束整合，Draw.io 继续排除在应用语义色根因之外；成熟度可选证据不再使用警告金。同步更新 `task_plan.md`、`findings.md` 和本文件；未修改运行前端、数据或 DMG。首次大块 patch 因目标上下文已变化而校验失败，改为分段 patch 后完成，未产生部分写入。
- 2026-07-12 `Maturity V2.1 Complete Frontend Design + Global Plan Correction`：完整读取最新 `SAPD_成熟度评估业务设计_V2.1_20260712.md` 并对照当前 demo，新增 `frontend/design-handoff/implementation-specs/maturity-assessment-v2-1-complete-frontend-design-2026-07-12.md`。方案完整覆盖项目列表、新建项目、固定 / 自定义模板、全宽纵向四维评分、文件交换、复核、客户 / 内部结果和报告快照；固化删除项目作用域和对象类型、固定模板只读真实映射、治理 / 管理服务只作平台维度参考、目标与理由必填、证据可选且不阻塞、客户主结果最细到 L2。同步修订全工程方案：安全能力映射新增节点 / 标签 / 边碰撞治理和自动验收阈值；信息化环境明确 Draw.io 原图颜色、字体、位置、边框与连线全部不可修改；语义色根因只适用于应用生成界面，不包含 Draw.io。更新 `CURRENT_STATE.md`、`task_plan.md`、`findings.md` 和本文件；未修改运行前端、demo 数据、SQLite、正式 JSON、源 Excel、用户库或 DMG。
- 2026-07-11 `PLAN-FE-GLOBAL 全工程前端设计审计与优化方案`：按用户纠偏把范围从成熟度模块扩展到整个工程，并基于 5173 真实页面完成全局壳、关系画布、LC-AP / LC-DT、字典、标准、指南、Issue、搜索和成熟度 10 类页面取证。新增 `frontend/design-handoff/implementation-specs/frontend-global-optimization-plan-2026-07-11.md`，固化全局根因为壳层重复、容器过多、语义色冲突、密度失衡和常驻详情过度；形成 Apple Shell token、六类页面模板、P0 / P1 / P2 路线、验收矩阵和禁止事项。成熟度融合稿按用户选择采用纵向矩阵、无内部目录、行内展开评分区、1—5 方形打分与证据状态。本轮仅修改设计 / 计划 / 发现 / 进度文档，未修改运行前端、SQLite、正式 JSON、源 Excel、用户库或 DMG；5173 真实页面与应用内浏览器已完成设计取证，部分半透明顶栏全页截图存在工具渲染伪影并被拒绝作为页面缺陷证据。2026-07-13 续补非成熟度实施队列：保留既有 `FE-Rxx` 优先级，将修复拆为 P0-1～P0-4、P1-1～P1-6、P2-1～P2-4，补齐前置依赖、实施内容和完成门槛；拆分“全局壳 P0 / 总览 P2”等混合优先级。成熟度 `FE-R35—FE-R39、FE-R51—FE-R61` 明确隔离为独立会话轨道。本轮未修改运行前端、成熟度文档、数据、SQLite、源 Excel、用户库或 DMG。
- 2026-07-12 `Maturity Assessment Workbench V2.1 Hierarchical Scoring Contract`：按用户共性规则完成固定 / 自定义模板共用评分页的第二轮收敛。页面改为 `L0 → L1 → L2 → 关注点 → 服务评估点` 渐进层级，首次进入和刷新默认仅显示 3 个 L0；关注点四维 / 目标 / 适用状态作为批量命令带入全部适用子项，服务可单项覆盖，关注点与能力摘要按真实子项回流更新。保存与异步汇总保留路由、展开路径、滚动容器、窗口位置和输入焦点，项目草稿展示保存 / 待汇总 / 已汇总状态并在刷新后恢复。同步更新 V2.1 业务设计、前端实现规格和全工程前端优化方案的修改日期与契约；专项审计扩展到 `67` 项。5173 应用内浏览器验证 `T-AS.AD-01`：关注点统一设为 L3 后 5 个适用服务全部带入、不适用服务保持置灰；单项改为 L1 后关注点变为“混合”、指数 `3 → 2.9`、L2 指数 `3.35 → 3.31`；控件屏幕位置保持 `812.0859375px`，刷新后评分保留且层级回到 L0 默认折叠；恢复演示数据后控制台错误为 0。maturity smoke、完整 frontend 套件、数据边界、diff 和 5173 stable 状态均通过。未修改正式 SQLite / JSON、源 Excel、用户库或 DMG；影响分类 `shared runtime`，Web 已验证，DMG App 未重建。
- 2026-07-13 `Maturity Four-Dimension Rubric Contract`：依据用户截图和 V2.1 业务设计明确四维评分要求。固化组织与角色、制度与流程、平台与工具、数据与信息的 L1—L5 通用矩阵；四维独立选择当前稳定实现的最高完整等级，对象专用 rubric 优先，通用矩阵只作回退。每个适用评估点必须返回 `4×5=20` 条可追溯 rubric；缺失时进入 `rubric_missing`，前端不得临时生成。数据与信息不等于证据数量，平台与工具不由服务映射、采购或单纯部署自动得分，计划状态只进入目标。同步更新业务设计、成熟度前端规格和全工程前端方案，修改日期均为 `2026-07-13`；专项审计由 `67` 扩展至 `69` 项并通过，Python 编译通过。未修改运行前端、正式 SQLite / JSON、源 Excel、用户库或 DMG；影响分类为 `shared runtime design contract`，后续代码对齐后再做 Web / App 回归。
- 2026-07-10 `Maturity Guide Tooltip + Compact Hero Follow-up`：按用户复验截图处理成熟度指南整页悬浮标题、重复眉标和封面偏高。根因规则：页面级 annotation target 只维护当前页面上下文，不能把覆盖整份 iframe 的容器注册为可悬停值级锚点；iframe 的可访问名称使用 `aria-label`，不得使用会触发 Chrome 原生 tooltip 的 `title`；独立 HTML 嵌入已有应用壳时，可按用户明确裁定移除重复品牌眉标并收紧 hero 密度，但不得改写正文和其他指南。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/assets/guides/maturity-model-usage.html`、`frontend/capability-browser/index.html`、`scripts/audit_user_annotation_contract.mjs`、`design-qa.md` 和本文件；未修改下载目录源 HTML、正式 SQLite / JSON、源 Excel、用户库或 DMG。desktop hero 从约 `355px` 收紧到 `265px`，眉标 `count=0`，整页 `data-annotation-tooltip` / iframe `title` 均为空，390px 无横向溢出。验证通过：前端 JS 语法、批注契约审计（含 `maturityGuideEmbedContract`）、5173 stable、指南轻量 smoke、GitHub 数据边界、定向 `git diff --check`、应用内浏览器 hover / desktop / mobile / 字段边界和错误日志检查；未启动系统 Chrome，未做 DMG App 回归。
- 2026-07-09 `SVG Association Service Chip Readability Follow-up`：按用户截图继续修复信息化环境 SVG 关联详情里安全技术服务胶囊出现“蓝色空块 / 文字不可读”的问题。根因规则：SVG 关联详情中的服务 chip 是业务证据，必须始终显示 `I-xx&T-... + 名称`；批注锚点高亮只能叠加边框 / 下划线，不能抹掉服务作用域色卡的可读背景和文字；SVG 关联路径也必须复用全局单层 tooltip 契约，不能再输出浏览器原生 `title`。修改 `frontend/capability-browser/components/EnvironmentBasemapViewer.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html`、`frontend/capability-browser/app.js`、`scripts/audit_service_scope_chip_color_contract.mjs`、`scripts/audit_user_annotation_contract.mjs`、`config/frontend-governance-baseline.json` 和本文件。新增审计断言：SVG 关联 chip 不输出 native title、批注标记态服务 chip 保持 `--sapd-service-bg/text` 可读性、`传统数据中心 / 应用及数据 / 应用前端` 的服务列表必须全部是带 code/name 的业务 chip（当前 `count=70 / blankCount=0`）。验证通过：相关 JS / 审计脚本语法检查、`node scripts/audit_service_scope_chip_color_contract.mjs`（`status=pass / checkCount=19 / failedCount=0`）、`node scripts/audit_user_annotation_contract.mjs`、前端治理、environment / capability 轻量 smoke、5173 状态、提权执行 `node scripts/run_project_test_suite.mjs --suite frontend --url http://127.0.0.1:5173` 通过；未启动系统 Chrome，未启动 DMG GUI。未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。
- 2026-07-10 `Maturity Model Usage Guide Original HTML Runtime Fix`：按用户复验截图修复成熟度指南首次点击空白、刷新后才显示、右侧重复标题条和原 HTML 样式丢失。根因规则：自带完整视觉系统的文档型 HTML 必须以原始文件为样式权威，通过隔离文档面加载；应用只保留全局壳和章节目录，不再把大 HTML 下载为字符串后二次注入，也不手工仿写其 CSS。`frontend/capability-browser/assets/guides/maturity-model-usage.html` 已替换为用户提供的原始 HTML，`app.js` 改为同源 iframe 文档面并复用 6 项章节导航，`styles.css` 复用幻灯片布局隐藏右侧重复标题条，`index.html` 更新缓存版本。未修改正式 SQLite / JSON、源 Excel、用户库或 DMG；验证结果见本轮完成反馈。
- 2026-07-09 `Service Scope Chip Color Contract Follow-up`：按用户复验截图修复上一轮全局色卡优化失败的问题。根因规则：安全技术服务 chip 虽已带 `data-service-scope`，但默认 CSS 选择器优先级高于各作用域选择器，导致 `I-DI / I-NT / I-HD / I-OS / I-PE` 等仍回落为默认蓝色；环境 SVG / 详情里的“作用域”对象还缺少独立 scope palette 属性；环境对象树点击重渲染时未保留树容器滚动位置。修改 `frontend/capability-browser/styles.css`、`app.js`、`components/EnvironmentBasemapViewer.js`、`components/EnvironmentLocalRelationMap.js`、`index.html`、`scripts/audit_service_scope_chip_color_contract.mjs`、`config/frontend-governance-baseline.json` 和本文件。新增审计断言：per-code 色卡选择器必须压过默认规则，能力技术视角表格必须用 `I-DI / I-HD / I-NT` 混合样例验收，SVG scope 胶囊必须使用 `data-scope-palette`，环境树点击必须恢复本地 scroll owner，HUD 不得覆盖图例。验证通过：相关 JS 语法检查、`node scripts/audit_service_scope_chip_color_contract.mjs`（`status=pass / checkCount=16 / failedCount=0`）、前端治理 / lazy-load / scroll 审计、5173 状态、environment / capability / lifecycle 轻量 smoke、提权执行 `node scripts/run_project_test_suite.mjs --suite frontend --url http://127.0.0.1:5173` 通过；未启动系统 Chrome。未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。
- 2026-07-09 `Service Scope Chip Color Contract`：按用户反馈全局优化安全技术服务胶囊作用域颜色，不再只让 `信息化环境-安全技术` tab 生效。根因规则：安全技术服务胶囊颜色必须由服务自身的作用域编码 `I-AP / I-DI / I-NT / I-US / I-OS / I-HD / I-PE` 决定；作用域对象本身不能冒充服务胶囊；SVG 底图关联映射、能力映射、LC-AP、知识库字典维护表和详情面板必须复用同一展示契约。修改 `frontend/capability-browser/displayLabels.js`、`styles.css`、`index.html`、`components/FocusScopeServiceMatrix.js`、`components/ApplicationSecurityLifecycle.js`、`components/EnvironmentBasemapViewer.js`、`components/TechnicalMeasureMaintenanceTable.js`、`components/DetailInspector.js`、`scripts/audit_service_scope_chip_color_contract.mjs`、`scripts/run_project_test_suite.mjs`、`config/frontend-governance-baseline.json`、`scripts/README.md` 和本文件；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。验证通过：相关前端 JS 和审计脚本 `node --check`、`node scripts/audit_service_scope_chip_color_contract.mjs`（`status=pass / checkCount=12 / failedCount=0`）、提权执行 `node scripts/run_project_test_suite.mjs --suite frontend --url http://127.0.0.1:5173` 通过、5173 守护和 environment / capability / lifecycle / maintenance 轻量 smoke 通过；未启动系统 Chrome。
- 2026-07-09 `Environment Basemap Node Detail Context Contract`：按用户截图反馈修复信息化环境底图 SVG 节点详情与 `信息化环境安全能力映射` 数据不一致的问题。根因规则：底图节点详情不能使用旧 `shadow:*` 绑定 ID 或组件局部拼接作为权威源，必须在生成阶段按 `信息化环境||环境子类||信息化对象` 业务上下文重新解析到 `environment-workbench` 的当前对象映射；展示层只能做业务键去重防御。修改 `scripts/generate_environment_basemap_node_details.mjs`、`frontend/capability-browser/components/EnvironmentBasemapViewer.js`、`frontend/capability-browser/generated/environmentBasemap.node-details.json`，新增 `scripts/audit_environment_basemap_node_details_contract.mjs`。目标样例 `工业控制网络||L3层||L3内部网络` 已对齐为 1 个作用域 `I-NT|网络`、4 个安全技术服务和 2 个安全技术模块；全量审计通过 `status=pass / checkCount=72 / failedCount=0 / comparedContextDetails=67 / boundNodes=92 / detailReadyNodes=92`。未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、用户 Issue / 批注数据库或 macOS DMG 产物。
- 2026-07-09 `Maturity Business Design Material Review`：按用户提供的 `SAPD_成熟度模型业务设计文档_v1.0_20260709.md`、`能力成熟度评估表_V1.2.md`、`网络安全成熟度模型介绍V 0.6.docx` 和 `网络安全成熟度评估工具-v1.1-20260513.xlsx` 做只读审阅，新增 `docs/08-maturity/business-design-material-review-2026-07-09.md`。结论：新业务设计文档可作为后续 maturity 数据契约、评分规则、ViewModel 和 API 草案输入；原始 Word / XLSX / V1.2 Markdown 只采信方法论、等级说明、rubric 文案和历史模板结构，能力、关注点、安全技术服务、作用域及关系仍以工程内权威字典为准。本轮未复制原始材料、未导入旧 XLSX、未修改主工程字典、未改 `knowledge_items`、未实现评分算法或前端页面。验证：`git diff --check` 通过；定向关键词核对通过；新增文档无行尾空白。
- 2026-07-09 `Security Capability Service Manual Export`：按用户要求只读当前已治理运行包导出 3 份业务清单，输出目录为 `data/exports/manual/security-capability-service-export-20260709/`。生成 Excel 工作簿 `sapd-security-capability-service-export-20260709.xlsx`，包含 `安全能力清单`、`安全技术服务清单`、`能力关注点服务作用域映射` 3 个 Sheet；同时生成对应 CSV 和 `export-manifest.json`。来源文件为 `capability-tree.json`、`capability-workbench.json`、`maintenance/services.json`。导出行数：安全能力清单 `136` 行，安全技术服务清单 `160` 行，能力关注点服务作用域映射 `160` 行。本轮未修改源 Excel、SQLite、正式 JSON 数据包、前端运行代码、用户库或 DMG 产物；导出产物未进入 Git 待提交列表。
- 2026-07-09 `Maturity Workbench Current Design Output`：按用户通过 `@product-design` 提出的要求，整理当前 `工作台 > 成熟度评估` 已有的一般前端设计和业务逻辑，新增 `frontend/design-handoff/implementation-specs/maturity-assessment-workbench-current-design-2026-07-09.md`。文档区分当前静态 mock、正式业务契约、评分对象、四要素评分、主工程能力权威源、页面结构、数据对象映射和后续实现缺口。本轮只新增设计说明文档，不修改前端运行代码、不改 SQLite、不改正式 JSON 数据包、不改源文件、不实现评分算法和 UI。验证：`git diff --check` 通过；定向关键词核对通过；新增文档无行尾空白。
- 2026-07-09 `Product Design Maturity IA Sync`：按用户通过 `@product-design` 给出的口径同步成熟度评估模块信息架构。本轮只更新文档和计划，不实现代码、不改数据包、不改 SQLite、不进入本轮打包。固化规则：成熟度说明类内容放入 `安全指南`，例如模型使用方法、等级解释、评估流程和模板填写说明；评估工作模块放入 `工作台 > SAPD 成熟度评估`，承载评估任务、填报、证据、匹配审查、评分、差距建议和报告导出；能力数据只读引用 `知识库字典 > 安全能力清单` 的权威数据，maturity 模块不另建能力主数据。修改 `CURRENT_STATE.md`、`task_plan.md`、`docs/08-maturity/requirements.md`、`docs/08-maturity/module-integration-review.md`、`docs/00-overview/frontend-menu-and-page-type-definition-v1.md`、`frontend/design-handoff/stitch-prompts/00-application-shell.md`、`frontend/design-handoff/stitch-input-v1-application-shell/stitch-design-handoff-v2.md` 和本文件。验证：`git diff --check` 通过；关键词核对已确认上述口径出现在状态入口、计划、成熟度文档、菜单定义和 Stitch handoff 文档中。
- 2026-07-09 `Demo-first Workflow Reset`：按用户最新裁定，删除旧额外运行面作为后续数据接入方案；已停止旧预览服务，删除额外 worktree 和本地实验分支，清理当前有效文档中的额外端口 / 额外分支 / 额外工作区规则。新规则：后续新数据、实验数据和前端试验先在当前 `main` 以受控 demo 页 / demo 数据验证；正式写入 SQLite、正式 JSON、源 Excel、用户库或 DMG 前必须另行确认权威源、对象粒度、写入范围、回退方案和审计清单。本轮未修改正式 SQLite、正式 JSON、源 Excel、用户库或 DMG。
- 2026-07-08 `Workbench Issue Zero Data Empty State Contract`：按用户截图修复 `Issue 清单` 在用户库无 Issue 时仍显示三栏处理台、空表头、右侧 `选中 Issue / 已保存` 和可点击 `导出全部` 的空数据体验问题。本轮根因规则：`Issue 清单` 必须区分“真实 0 条 Issue”和“已有 Issue 但筛选后为 0”；真实 0 条时进入单一页面空态，不渲染 scope / queue / inspector 三栏，不显示误导性的保存状态；筛选后为 0 时保留筛选上下文但不显示无意义表头；0 条时 `导出全部` 禁用。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html`、`scripts/audit_frontend_scroll_contract.mjs` 和本文件；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。验证通过：JS 语法检查、前端滚动 / Issue 工作流契约审计 `checkCount=36 / failedCount=0`、前端治理审计、工作台 Issue 轻量 smoke、GitHub 数据边界和定向 `git diff --check`；只读确认稳定用户库当前 `user_notes=40`。未启动系统 Chrome。
- 2026-07-08 `Mounted DMG Runtime Same-Path Continuation`：按用户确认“后续文档类变更不影响继续测试”的口径，继续用当前 mounted `0.1.7` 无授权版 DMG 做运行态取证，并把前端源码差异重新定界：当前 staged / mounted DMG 与源码的运行差异集中在 `index.html` cache token 和 `styles.css` 的 Issue 宽度修复，`app.js` 与关键前端组件哈希一致，因此标准 / 框架搜索逻辑、批注导出和 Runtime 后端可继续验；最新 Issue 宽度修复仍需 Web 5173 或后续重包后验。当前实际 App 为 `/Volumes/SAPD Wiki 0.1.7 无授权版/SAPD Wiki.app`，`Info.plist` 为 `CFBundleShortVersionString=0.1.7`、`SAPDWikiDisplayVersion=0.1.7`、`SAPDWikiLicenseMode=no-license`；Runtime 启动在 `http://127.0.0.1:18765/`，数据目录为 `/Users/kim1st/Documents/SAPD Wiki/Runtime`，用户库为 `data/user/sapd_wiki_user.sqlite3`，当前用户库 `user_notes=1 / user_meta=2 / schema=1`。启动自检 `startup-check-result.json` 全部通过；Web 5173 与 DMG Runtime 对比搜索 `运营` 的第一目标均为 `EMB-03 运营技术（OT）`、`target_ref=standard_control:...:621`，搜索 `纵深` 的第一目标均为 `SEA-03 纵深防御架构`、`target_ref=standard_control:...:1245`。批注导出 JSON 与 `download=1` 附件响应通过，`save=1` 已生成验收产物 `/Users/kim1st/Documents/SAPD Wiki/export/user-notes-export-20260708-042524Z.md`。GUI 自动点击 / 视觉截图未完成：当前环境拒绝 `System Events` 辅助访问，`screencapture` 返回黑屏，因此不能把本轮证据扩大解释为完整 WKWebView UI 自动验收。
- 2026-07-08 `Web / App Bug Root Cause Classification Rule`：按用户要求结合发布验收矩阵交接内容固化后续 bug 根因修复规则。新增主控规则：每个 bug 修复都必须先声明运行面影响分类，不再把 5173 Web 通过默认等同于 DMG App 通过，也不把每个小修都默认升级为重打包。分类为 `shared runtime`、`data / ETL / JSON package`、`web-only`、`app-only`、`release blocker`；完成反馈必须说明影响面、根因层和验证范围。修改 `AGENTS.md`、`docs/09-delivery/mac-dmg-browser-parity-contract.md`、`docs/09-delivery/release-acceptance-matrix-0.1.md`、`docs/07-governance/governance-index.md`、`scripts/audit_mac_dmg_browser_parity_contract.mjs`、`scripts/README.md`、`CURRENT_STATE.md` 和本文件；验证通过：`node --check scripts/audit_mac_dmg_browser_parity_contract.mjs`、`node scripts/audit_mac_dmg_browser_parity_contract.mjs`、定向 `git diff --check` 和关键词核对；审计新增 `agents_documents_bug_runtime_impact_classification`、`contract_documents_bug_runtime_impact_classification`、`release_matrix_documents_bug_fix_acceptance_routing` 均通过。未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。
- 2026-07-08 `Workbench Issue Split Pane Width Contract`：继续处理用户第四次反馈的 `Issue 清单` 右侧单个 Issue 操作按钮在浏览器高度 / 宽度不足时被裁掉的问题。本轮根因不是单个按钮 sticky 失效，而是 `workbench-route-page` 最小宽度叠加 Issue 三栏固定列宽，把右侧 inspector 和底部操作区推出常见桌面视口。固化规则：Issue 三栏页面必须同时满足纵向本地滚动 owner 和横向适配契约；在 `1440x760`、`1600x760` 等常见桌面窗口内，`Issue 范围`、`Issue 处理队列`、`选中 Issue` 不能把右侧保存 / 取消 / 标记已采纳 / 忽略 / 删除操作区推出可视区。修改 `frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html`、`scripts/audit_frontend_scroll_contract.mjs` 和本文件；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。验证通过：`node scripts/audit_frontend_scroll_contract.mjs` 为 `checkCount=35 / failedCount=0`；真实 Chrome 探针确认 `1440x760` 和 `1600x760` 下点击 Issue 行正文即可选中、全选框存在、排序列存在、自定义删除确认弹层可打开并取消、单选和批量按钮文案正确、右侧 inspector 与底部操作区均未越出视口。另以真实 Chrome 探针确认搜索 `纵深 -> SEA-03` 和 `运营 -> EMB-03` 均能落到标准表行级高亮目标；`node scripts/audit_global_search_index_contract.mjs --url http://127.0.0.1:5173` 在允许访问本机服务后通过 `checkCount=88 / failureCount=0`。当前 0.1.7 DMG 是本轮前构建的产物；本轮前端源码修复尚未进入该 DMG，后续正式 DMG 验收前必须先 checkpoint 并重打包。
- 2026-07-08 `Release Acceptance Matrix 0.1`：按用户指出“带用户状态的本地 App 本来就是目标，但缺测试方法 / 验收标准”的根因反馈，新增 `docs/09-delivery/release-acceptance-matrix-0.1.md`，把 macOS DMG 发布验收从“网页 + 打包可用”改为“构建输入、运行宿主、用户状态、核心工作流和证据留存”五层发布准入。矩阵定义 `dev-smoke`、`pre-dmg`、`internal-release`、`public-release` 四级，G0-G3 自动验收闸门、M1-M7 人工验收矩阵、P0-P3 阻断分级、证据目录和发布判定模板，并明确不做全量笛卡尔积、采用风险配对。同步修改 `scripts/audit_mac_dmg_browser_parity_contract.mjs`，新增 `release_acceptance_matrix_defines_execution_gates` 门禁；更新 `scripts/README.md`。未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。
- 2026-07-08 `macOS 0.1.7 DMG Build`：按用户要求基于当前最新工作区升级一个小版本并重新打包，当前正式版本从 `0.1.6` 升至 `0.1.7`。修改 `apps/macos/SAPDWiki/Sources/SAPDWiki/main.swift`、`apps/macos/SAPDWiki/script/build_and_run.sh`、`apps/macos/SAPDWiki/script/package_dmg.sh`、`scripts/build_zip_bundle.py`、`CURRENT_STATE.md` 和本文件；未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包或用户 Issue / 批注数据库。执行 `SAPD_WIKI_APP_VERSION=0.1.7`、`SAPD_WIKI_BUILD_STAMP=20260708-030834Z`、`SAPD_WIKI_DMG_VARIANT=all`、`SAPD_WIKI_REBUILD_BACKEND=1` 的 macOS 打包命令，输出授权版 `apps/macos/SAPDWiki/dist/license/SAPD-Wiki-0.1.7-license-20260708-030834Z-mac-arm64.dmg` 和无授权版 `apps/macos/SAPDWiki/dist/no-license/SAPD-Wiki-0.1.7-no-license-20260708-030834Z-mac-arm64.dmg`，两包均 `163M`。SHA-256：授权版 `25f5502795bd69539335c45020b09fdf047d48d937b8e330a9cc94329a0f0912`，无授权版 `904b55c29a9b8891b16adc1684348ebf301dd56639151b9850220cd9b51aab97`。验证通过：shell / Python 语法检查、双包打包、两包 `hdiutil verify`、两套 staging App `codesign --verify --deep --strict`、`Info.plist` 版本和授权模式核对、两包用户库 `user_notes=0`、两包外层 / Runtime README 的 `Changelog` 和路径说明核对、DMG 扩展属性无 `com.apple.quarantine`、DMG/Web parity 契约审计、5173 状态、首页轻量 smoke 和定向 `git diff --check`。审计保留已知 warning：当前 macOS wrapper 仍无显式 `WKUIDelegate`、`WKDownload` 和 fullscreen bridge，属于 Web / DMG 差异矩阵后续项，本轮不作为打包阻塞。
- 2026-07-08 `Guide Fullscreen Cleanup + Slide Catalog Click Contract`：按用户截图反馈修复跨页 UI 状态残留问题。固化规则：`安全架构建模语言` 的 ArchiMate 全页面 / fullscreen 预览关闭或切换路由时，必须清理 lightbox、fullscreen、拖拽和 pointer capture 状态；`安全技术架构设计方法` 等幻灯片目录缩略图点击必须像翻页按钮一样在捕获阶段处理，不能被前一页残留的全局点击 / 文本选择 / fullscreen 状态吞掉。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/index.html`、`scripts/audit_frontend_text_selection_contract.mjs` 和本文件；验证通过：`node --check frontend/capability-browser/app.js`、`node --check scripts/audit_frontend_text_selection_contract.mjs`、`node scripts/audit_frontend_text_selection_contract.mjs`、`node scripts/audit_frontend_governance.mjs`、`node scripts/audit_frontend_route_refresh_contract.mjs`、`node scripts/audit_user_annotation_contract.mjs`、5173 状态、`/guides/security-architecture-modeling-language` 与 `/guides/security-architecture-design` 轻量 smoke、定向 `git diff --check`。未启动系统 Chrome；真实点击路径仍需用户按 5173 手工验收。未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。
- 2026-07-08 `Web / DMG Same-Path Runtime Evidence`：按用户明确授权启动 DMG GUI，做真实 Web / DMG 同路径取证；本轮不改功能代码，只把差异拆到宿主、用户偏好、前端状态和业务锚点四层。固化规则：Web 5173 通过不能替代 DMG `WKWebView` 通过；同一 DMG 在不同 Mac 上比较前必须固定 `UserDefaults` 数据目录 / 下载目录、Runtime URL、用户库初始态、route、窗口状态和操作路径。取证目录为 `data/exports/worker-verify/web-dmg-same-path-evidence-20260708/`，报告为 `notes/web-dmg-same-path-audit-20260708.md`。关键结论：DMG 复用 `com.sapd.wiki.macos` 的历史 `SAPDWiki.DataRootPath` / `SAPDWiki.DownloadDirectoryPath` 是跨机器差异的首要嫌疑；标准 / 框架 `运营 -> EMB-03 运营技术（OT）` 定位失败在 Web 和 DMG 均可复现，属于搜索结果到标准虚拟表行级 reveal 的业务契约问题，不是 DMG-only；Issue 行点击、应用内删除确认、导出所选和当前按钮链路在 no-license staging App 中可用；页面内全屏在 DMG `WKWebView` 中点击后无明显变化，需要单独修宿主 / 应用内全屏策略。已停止 DMG 进程并恢复 `com.sapd.wiki.macos` 原偏好到 `/Users/kim1st/Documents/SAPD Wiki` 和 `/Users/kim1st/Documents/SAPD Wiki/export`。未修改前端功能、源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。
- 2026-07-08 `macOS DMG / Web Parity Root Cause Correction`：按用户纠偏修正上一轮差异审计路线。上一轮把“当前工作区前端源与 0.1.6 DMG staging 哈希不一致”当作根因证据，时间线不成立；该项只能作为发布新鲜度警告，不能解释用户当时反馈的同版本 Web 与 0.1.6 DMG 差异。本轮重新定性为交付架构根因：5173 是系统浏览器访问开发服务，DMG 是 macOS wrapper 启动复制后的 Runtime 后用 `WKWebView` 加载本地后端 URL，二者不是同一浏览器路线。当前 wrapper 没有显式 `WKUIDelegate`、`WKDownload` 或原生 fullscreen bridge，因此全屏、弹窗、下载、新窗口、缓存、滚动条和输入事件必须进入 Web / DMG 双环境验收矩阵。修改 `docs/09-delivery/mac-dmg-browser-parity-contract.md`、`scripts/audit_mac_dmg_browser_parity_contract.mjs`、`data/exports/worker-verify/mac-dmg-web-parity-audit-20260708.md` 和本文件；未修改前端功能、源 Excel、SQLite、正式 JSON 数据包、用户 Issue / 批注数据库或 macOS DMG 产物；未启动系统 Chrome / DMG GUI。
- 2026-07-08 `macOS DMG / Web 5173 Parity Audit Kickoff`：按用户要求发起 Web app 与 DMG app 打包后差异审计。本轮先做不启动 GUI 的静态 / 契约 / 包体审计，固化规则为：5173 通过不等于 DMG 通过，同一 DMG 在不同 Mac 上也必须固定 Runtime 指纹、前端文件哈希、用户库 schema / 数据量、授权态、窗口尺寸、WebView 能力、route 和操作路径后才能比较。已确认 0.1.6 DMG 授权版 / 无授权版包体校验和签名通过，base DB 与当前库一致，包内用户库为空且 `user_schema_0.3`，license/no-license staging 前端彼此一致；同时确认 0.1.6 DMG staging 的 `index.html`、`app.js`、`styles.css`、`components/EnvironmentBasemapViewer.js` 已落后于当前 `frontend/capability-browser` 源，且当前 `frontend_content_smoke_check.mjs --url http://127.0.0.1:5173` 仍失败在全局搜索激活契约。修改 `docs/09-delivery/mac-dmg-browser-parity-contract.md`、`scripts/audit_mac_dmg_browser_parity_contract.mjs` 和本文件；新增 `data/exports/worker-verify/mac-dmg-web-parity-audit-20260708.md`。未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物；未启动系统 Chrome / DMG GUI。
- 2026-07-08 `Count Popover Internal Scroll Follow-up`：按用户复验反馈修复数字详情浮层“能悬停但不能滚动”的遗漏。根因规则补充为：GB/T / Gartner / 关联流程组等数字详情浮层是可阅读长列表，浮层内部的 `wheel` / `scroll` 事件必须归浮层自己处理，不能被页面级 scroll-hide 监听器当作外部滚动而关闭或打断；外部页面滚动仍可关闭浮层。修改 `frontend/capability-browser/components/StandardFrameworkTable.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html`、`scripts/audit_frontend_scroll_contract.mjs` 和本文件。验证通过：相关 JS 语法检查、滚动契约审计、前端治理审计、批注契约审计、浮层内部滚动源级断言、5173 状态、`/knowledge/functions`、`/standards/workforce-reference`、`/standards/mlps-level-3` 轻量 smoke 和定向 `git diff --check`；未启动系统 Chrome。补充执行的 `frontend_content_smoke_check.mjs --skip-api` 失败在既有 `app.js` 全局搜索激活契约断言，本轮未修改该逻辑，未纳入本次浮层验收通过条件；误跑的 `node --check index.html` 为无效命令。未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。
- 2026-07-07 `DMG WebView Fullscreen + Dictionary Group + Count Popover Contract`：按用户 5 张测试截图修复三类通病。固化规则：环境底图 / SVG 全屏不能只依赖浏览器原生 `requestFullscreen()`，在 macOS DMG WebView 不可用或被拒绝时必须降级到应用内全屏；分组式字典表的“选中业务行”和“展开/收起父级”是两个动作，选中行后包含该行的父级路径必须保持展开；GB/T / Gartner / 关联流程组等数字详情是可阅读浮层，鼠标必须能进入浮层并滚动，滚动条不能依赖不同机器的系统滚动条设置。修改 `frontend/capability-browser/components/EnvironmentBasemapViewer.js`、`frontend/capability-browser/components/StandardFrameworkTable.js`、`frontend/capability-browser/components/WorkFunctionMaintenanceTable.js`、`frontend/capability-browser/components/TechnologyModuleMaintenanceTable.js`、`frontend/capability-browser/components/CapabilityDirectoryMaintenanceTable.js`、`frontend/capability-browser/components/StandardRoleReferenceTable.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html`、`scripts/audit_frontend_scroll_contract.mjs` 和本文件；验证通过：相关 JS 语法检查、前端滚动/交互契约审计、前端治理审计、分组字典组件级渲染断言、5173 状态、环境页/安全职能页/岗位参考页轻量 smoke 和定向 `git diff --check`。未启动系统 Chrome；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。
- 2026-07-07 `Annotation Tooltip Single Layer Contract`：按用户截图修复“同一业务值出现两层角标”的通病。固化规则：批注 / 复制值 / 业务对象悬停提示只允许项目自定义 `data-annotation-tooltip` 作为可见提示层，浏览器原生 `title` 不作为可见 UI；共享值级属性生成不再输出 `title`，运行时显示自定义 tooltip 前会清理遗留宿主 `title`，审计脚本新增单层提示契约。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/displayLabels.js`、`frontend/capability-browser/index.html`、`scripts/audit_user_annotation_contract.mjs` 和本文件；验证通过：前端 JS / 审计脚本语法检查、批注契约审计、前端治理审计、5173 服务状态、内容页 `/guides/security-architecture-modeling-language` 轻量 smoke、工作台 Issue 轻量 smoke 和定向 `git diff --check`；未启动系统 Chrome。未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-07 `macOS 0.1.6 DMG Overwrite Rebuild`：按用户要求基于当前最新工作区重新打包，覆盖上一轮同名 DMG，且不升级版本号。执行 `SAPD_WIKI_APP_VERSION=0.1.6`、`SAPD_WIKI_BUILD_STAMP=20260707-085841Z`、`SAPD_WIKI_DMG_VARIANT=all`、`SAPD_WIKI_REBUILD_BACKEND=1` 的 macOS 打包命令；输出仍为授权版 `apps/macos/SAPDWiki/dist/license/SAPD-Wiki-0.1.6-license-20260707-085841Z-mac-arm64.dmg` 和无授权版 `apps/macos/SAPDWiki/dist/no-license/SAPD-Wiki-0.1.6-no-license-20260707-085841Z-mac-arm64.dmg`，两包均 `164M`。新 SHA-256：授权版 `f414986d30f4ab52dcadb3247ed1ebb582277f3c3b59e67ffa5d34ab4f016792`，无授权版 `3b972ed53de4bf1a17d43ff5ab4efabb3d285cfb3ef5b7ebd20944e2eb0fe34c`。验证通过：shell / Python 语法检查、两包 `hdiutil verify`、两套 staging App `codesign --verify --deep --strict`、`Info.plist` 版本和授权模式核对、两包用户库 `user_notes=0`、两包外层 / Runtime README 的 `Changelog` 和路径说明核对。未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包或用户 Issue / 批注数据库。
- 2026-07-07 `Workbench Issue Queue Width Contract`：按用户截图修正工作台 `Issue 清单` 三栏空间分配。固化规则：`Issue 处理队列` 是带固定业务列的 Review Queue，不得把列强行压缩成省略号，也不得依赖整页底部横滚；队列自身必须支持横向滚动，三栏之间必须可拖动调整宽度，但分隔区只能是细拖拽线，不能叠加成大块空白。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html`、`scripts/audit_frontend_scroll_contract.mjs` 和本文件；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-07 `Dedicated GitHub Commit Session Contract`：按用户要求创建并置顶专用 Git / GitHub 收口会话 `SAPD Wiki GitHub 提交收口专用 2026-07-07`，thread id `019f3bee-d713-7301-be67-6d555012a5c9`；固化规则为普通开发会话默认不执行 commit / push / PR，提交和推送优先交由该会话按主题分组、显式 stage、执行 GitHub 数据边界检查后收口。修改 `AGENTS.md`、`CURRENT_STATE.md` 和本文件；未修改业务代码、前端数据包、源 Excel、SQLite、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-07 `Workbench Issue Priority + Cancel Workflow Contract`：按用户截图修复工作台 `Issue 清单` 单条 Issue 编辑器。固化规则：优先级业务顺序为 `未标注 -> 低 -> 中 -> 高`，筛选、排序、下拉选项和保存标签必须一致；`取消` 不是空按钮，必须恢复当前 Issue 上次保存内容并给可见反馈。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/index.html`、`scripts/audit_frontend_scroll_contract.mjs` 和本文件；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-07 `macOS External Distribution Contract Triage`：按用户 A / B 反馈检查当前 DMG 外部分发状态。结论：用户 A 的 `com.apple.quarantine` 问题不是业务功能故障，而是当前 `0.1.6` DMG 仍为 ad-hoc signed 非公证包，外部分发触发 macOS Gatekeeper 后需要手动允许或临时删除隔离属性；用户 B 的安装 / 更新问题结论为当前 DMG 是挂载载体，可手动拖拽或直接运行，但没有自动安装器，也没有打开 DMG 即更新旧 App 的机制。已按用户最新口径否决并忽略 Developer ID 签名公证方案与安装 / 自动更新机制方案，`OI-190` 标记为 `业务接受` 并归档；执行 `node scripts/govern_open_issues.mjs` 通过，当前问题总数 `192`、未关闭 `2`、归档 `190`。本轮未修改 macOS App 行为、打包脚本、源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包或用户 Issue / 批注数据库。
- 2026-07-07 `macOS 0.1.6 DMG + README Changelog`：按用户要求在最新打包要求下升级小版本号并重打 DMG。固化规则：当前正式版本从 `0.1.5` 升至 `0.1.6`；授权版和无授权版继续同版本、同时间戳、分目录打包；外层 DMG `README-FIRST.md` 和包内 Runtime `README-FIRST.md` 必须带 `Changelog`，后续打包持续记录版本变化。修改 `apps/macos/SAPDWiki/Sources/SAPDWiki/main.swift`、`apps/macos/SAPDWiki/script/build_and_run.sh`、`apps/macos/SAPDWiki/script/package_dmg.sh`、`scripts/build_zip_bundle.py`、`CURRENT_STATE.md` 和本文件；未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包或用户 Issue / 批注数据库。验证通过：Python / JS / shell 语法检查、双包打包、两包 `hdiutil verify`、两套 staging App `codesign --verify --deep --strict`、`Info.plist` 版本和授权模式核对、两包用户库 `user_notes=0`、两包外层 / Runtime README 的 `Changelog` 和路径说明核对、5173 状态、首页轻量 smoke 和定向 `git diff --check`。最终授权版 SHA-256 `f2dba825f488568c68aa421a13c85244265f216725d3fac4108cbd74c26f215d`，无授权版 SHA-256 `c3f14793b9aa66fff51a7fcbab7d0e5b2ca2105e81bd790f85c3e168b124b4a3`。
- 2026-07-07 `Workbench Issue Inspector Top Action Contract Fix`：按用户第 4 次截图反馈和主控审查机制继续收敛工作台 `Issue 清单` 右侧单条 Issue 操作区。根因规则修正为：第三次拆分 scroll body 与 footer 只解决“按钮遮挡内容”，没有解决“小视口下关键操作不可达”；右侧 inspector 的保存 / 取消 / 状态处理 / 删除属于当前 Issue 的关键操作，必须先于详情内容滚动区出现，不能依赖浏览器高度足够高、也不能做覆盖内容的浮层。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html`、`scripts/audit_frontend_scroll_contract.mjs` 和本文件：`renderWorkbenchIssueInspectorActions()` 调整到 `workbench-review-inspector-content` 之前，右侧面板网格改为标题 / 提示 / 顶部操作工具条 / 内容滚动区，审计新增渲染顺序约束，缓存版本提升到 `issue-inspector-actions-top-20260707-1`。未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-07 `Workbench Issue Inspector Footer Root Cause Fix`：按用户“三次返工，遵循主控审查机制”要求重新审查工作台 `Issue 清单` 右侧按钮遮挡根因。最终根因定位为：`workbench-review-inspector` 把“可滚动详情内容”和“底部操作栏”放在同一个滚动层，并用 `position: sticky; bottom: 0` 固定操作区；sticky 不会给前面的 `状态流转记录` 预留空间，所以单条 Issue 内容高度接近面板高度时必然遮挡。上一轮只修了多选批量按钮网格，没有覆盖单选详情 footer。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html`、`scripts/audit_frontend_scroll_contract.mjs` 和本文件：右侧 inspector 改为标题 / 提示 / 独立滚动内容区 / 底部操作 footer 四段结构，单选和多选按钮统一渲染在 scroll body 外；滚动提示改为读取 `data-review-inspector-scroll`；审计规则改为断言 inspector footer 不得用 sticky 覆盖内容。未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-07 `Workbench Issue Batch Action Overlay Follow-up`：按用户截图继续修复工作台 `Issue 清单` 多选态右侧操作区“没有盖好”的问题。根因规则补充为：单条 Issue 详情的保存 / 删除操作可以是 inspector 底部 sticky 操作区；多选批量操作不是详情保存栏，必须是右侧批量面板内部的稳定操作网格，按钮换行或 `清除选择` 单独成行时不得覆盖已选 Issue 摘要或贴住滚动边界。修改 `frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html`、`scripts/audit_frontend_scroll_contract.mjs` 和本文件；未修改 `app.js` 行为逻辑、源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。新增审计断言 `workbench_issue_batch_actions_are_static_grid_not_overlay`，把批量操作从“按钮存在”固化为“非 sticky 覆盖、两列网格、清除选择独占一行”的布局契约。
- 2026-07-07 `Standards Search Duplicate Identity Follow-up`：按用户截图追问“同一个 `EMB-03 运营技术（OT）` 为什么出现两个搜索值”。根因规则补充为：标准 / 框架全局搜索中，后端 API 行级索引和前端 fallback 行级索引必须使用同一 `standard_control:<framework>:<table>:<row>` 目标身份；同一标准控制项不得因为一个来自 API、一个来自 fallback 而在顶部预览中变成两个可点击结果，更不得让其中一个只落到 SCF 表首页。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/index.html`、`scripts/audit_global_search_index_contract.mjs` 和本文件；未修改后端 API、源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。本地 5173 API 只读抽样因沙箱禁止 localhost socket 且提权审批被环境策略拒绝，未作为验证依据；本轮以代码契约和静态审计固化。
- 2026-07-07 `Annotation Drawer Edge + Standards Search Click Timing Follow-up`：按用户新截图继续修复批注抽屉和标准 / 框架搜索定位。根因规则补充为：批注抽屉 tab 和 panel 必须共享同一 gutter 边缘，关闭态通过 drawer 整体位移露出 tab，不能在打开态让 tab 与 panel 分离；顶部全局搜索结果点击必须在 `pointerdown` 阶段先捕获并激活结果，避免输入框失焦或面板重绘先清空结果 payload，导致标准 / 框架行级定位没有触发；全局搜索激活保留原始查询，目标页局部搜索不被全局查询污染，标准结果只通过行级 reveal options 定位。修改 `frontend/capability-browser/styles.css`、`frontend/capability-browser/app.js`、`frontend/capability-browser/index.html`、`scripts/audit_user_annotation_contract.mjs`、`scripts/audit_global_search_index_contract.mjs`、`scripts/audit_search_state_isolation.mjs` 和本文件；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-07 `Annotation Target Meta + Standards Search Row Reveal Follow-up`：按用户截图反馈修复批注抽屉和标准 / 框架搜索定位回归。根因规则定位为：批注目标摘要只允许展示业务标题和显式业务 code，不得把 UUID、`target_ref` 或内部对象 id 当作副标题露出；标准 / 框架搜索点击不是文本查找，而是 `route + standardFramework + standardTableId + row id` 的行级定位，标准表异步加载完成后也必须继续 flush 定位。修改 `frontend/capability-browser/components/UserAnnotationDrawer.js`、`frontend/capability-browser/components/StandardFrameworkTable.js`、`frontend/capability-browser/app.js`、`frontend/capability-browser/index.html`、`scripts/audit_user_annotation_contract.mjs`、`scripts/audit_global_search_index_contract.mjs` 和本文件；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。运行态本地 API 探针因当前沙箱禁止直接连接 `127.0.0.1:5173` 被拒，未绕过执行；后续以静态审计、组件级断言和轻量 smoke 固化。
- 2026-07-07 `Search Test Case Matrix Hardening`：按用户反馈修正“搜索测试粗糙、缺少明确用例”的治理缺口。根因定位为：原有搜索验证分散在 `audit_global_search_index_contract.mjs`、`audit_search_state_isolation.mjs`、`frontend_content_smoke_check.mjs` 和 `audit_search_index_quality_probes.py` 中，脚本断言存在，但文档只有 `TC-011 / TC-012` 两条粗粒度用例，且搜索质量探针未接入统一 `run_project_test_suite`。本轮固化搜索专项用例 `SC-001` 至 `SC-016`，覆盖全局搜索 7 个业务域 golden query / counter query、点击定位、分页计数、弱命中裁剪、空结果、能力关系定位、标准行定位、LC-AP / LC-DT / 能力页局部搜索队列和搜索历史隔离；同时把 `python3 scripts/audit_search_index_quality_probes.py` 接入 `data:search-quality`。修改 `docs/07-governance/project-test-workflow-and-case-matrix.md`、`scripts/run_project_test_suite.mjs` 和本文件；未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。验证通过：`node --check scripts/run_project_test_suite.mjs`、搜索质量探针 `checkCount=38 / failureCount=0`、总控 `data` 套件实际执行通过、全局搜索契约 `checkCount=51 / failureCount=0`、搜索状态隔离 `checkCount=40 / failureCount=0`、`node scripts/frontend_content_smoke_check.mjs --skip-api` 通过。保留既有非阻断 warning：字典引用一致性中 `work_function` candidate reference id 提醒 `138` 条、`errors=0`。
- 2026-07-07 `Workbench Issue Follow-up Test Run`：按用户要求调用 `$sapd-wiki-project-test` 对当前工作台 Issue 修复做测试，并用 Product Design 已保存上下文确认本轮验收应围绕本地 `http://127.0.0.1:5173/`、密集业务工作台、低噪声弹层和字段边界。测试结论：`quick` 套件完整通过，覆盖 `static,boundaries,delivery`；定向通过 `python3 scripts/dev_server_guard.py --status`、`node scripts/audit_frontend_governance.mjs`、`node scripts/audit_frontend_scroll_contract.mjs`、`node scripts/frontend_smoke_check.mjs --page workbench-issues --url http://127.0.0.1:5173`，并补跑搜索、能力、环境、LC-AP、标准页 HTTP/API smoke 均通过。`--full --url http://127.0.0.1:5173` 在当前沙箱内部分执行：`static`、`boundaries`、`data` 和 `frontend` 前置审计通过；保留既有非阻断 warning：字典引用一致性 `work_function` candidate reference id 警告 `138` 条、`errors=0`。未完成项不是业务断言失败，而是当前沙箱拒绝 Node 本地 `fetch` / 临时端口绑定：`audit_capability_viewmodel_contract.mjs`、`frontend_content_smoke_check.mjs`、`audit_user_notes_integrity.mjs` 报 `fetch failed`，`smoke_user_data_basket_api.mjs` 临时 Runtime 端口绑定被拒；两次提权复跑请求均被环境策略拒绝，未绕过执行。修复前端治理闸门时对本轮新增弹层代码做了压缩，`app.js` 行数回到 `12266 / 12270`，`styles.css` 回到 `31561 / 31568`，没有放宽 `config/frontend-governance-baseline.json`。未启动系统 Chrome，未构建 DMG，未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、真实用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-07 `Workbench Issue Queue Workflow Contract Follow-up`：按用户复验反馈修正工作台 `Issue 清单` 三个未验收点。根因规则补充为：`Issue 处理队列` 的“选中”口径必须统一，点击任意行即把该行写入选中集合并打开当前 Issue，不能把行点击仅当作详情预览、把小方框当作唯一选中入口；删除确认属于应用级确认弹层，不能使用浏览器原生 `confirm()`，也不能作为右栏内容流中的内嵌红色提示块；筛选提示不得把业务范围渲染成含糊的 `页面 + 值` 胶囊，应显示为 `Issue 范围`。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html` 和 `scripts/audit_frontend_scroll_contract.mjs`；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。验证通过：`node --check frontend/capability-browser/app.js`、`node --check scripts/audit_frontend_scroll_contract.mjs`、前端滚动 / Issue 工作流契约审计 `checkCount=28`、`node scripts/frontend_smoke_check.mjs --page workbench-issues --url http://127.0.0.1:5173`、`python3 scripts/dev_server_guard.py --status` 和定向 `git diff --check`；未启动系统 Chrome。
- 2026-07-07 `Project Test Master Run + Contract Rebaseline`：按用户要求作为主控执行全工程测试设计落地后的总体验证。固化规则：工程测试以 `docs/07-governance/project-test-workflow-and-case-matrix.md` 为用例矩阵、`scripts/run_project_test_suite.mjs` 为统一入口；默认不启动系统 Chrome、不构建 DMG，真实浏览器和真实 DMG 构建必须显式授权。修正 3 个测试治理偏差：`audit_user_db_governance_contract.mjs` 改为当前 macOS 用户数据契约，即用户选择父级目录后使用 `<parent>/SAPDWiki/Runtime`，已有用户库只复用不覆盖，只在缺失时播种空模板库；`audit_search_state_isolation.mjs` 支持 LC 搜索 `target_ref` 优先锚点并保留 `data-lifecycle-id` 兜底；`frontend-governance-baseline.json` 更新为 2026-07-07 已交付体量硬上限，继续阻断后续无意增长。验证通过：`node --check` 两个审计脚本、用户库治理审计、搜索状态隔离审计、前端治理审计、`node scripts/run_project_test_suite.mjs --suite frontend --url http://127.0.0.1:5173`、`node scripts/run_project_test_suite.mjs --suite user --url http://127.0.0.1:5173`、`node scripts/run_project_test_suite.mjs --full --url http://127.0.0.1:5173`。总控 `--full` 覆盖 `static,boundaries,data,frontend,runtime,user,delivery` 并最终 `result=pass`；保留既有非阻断 warning：字典引用一致性中 `work_function` candidate reference id 提醒 `138` 条、`errors=0`。未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物；未启动系统 Chrome，未构建 DMG。
- 2026-07-07 `Workbench Issue Queue Workflow Contract`：按用户 7 张截图反馈收敛工作台 `Issue 清单` 的 Review Queue 交互契约。固化规则：`Issue 处理队列` 的行点击负责打开当前单条 Issue，复选框只负责批量选择；默认不再自动选中第一条 Issue；右侧 `选中 Issue` 面板必须区分未选择、单条详情和多选批量三种状态；批量操作只显示 `全部关闭`、`改为处理中`、`全部忽略`、`标记已采纳`、`清除选择`；表头提供全选 / 全不选和按 `Issue`、`所属页面 / 对象`、`状态`、`优先级`、`更新时间` 排序；删除确认使用应用内确认条，不再使用浏览器原生 `confirm()`；主详情合并展示 `关联页面/对象`，不再显示 `锚点 / 类型`；优先级通过现有 `tags` 更新契约写入 `高优先级` / `低优先级` 标签，不改用户库 schema。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html` 和 `scripts/audit_frontend_scroll_contract.mjs`；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。验证通过：`node --check frontend/capability-browser/app.js`、`node --check scripts/audit_frontend_scroll_contract.mjs`、前端滚动 / Issue 工作流契约审计 `checkCount=28`、`node scripts/frontend_smoke_check.mjs --page workbench-issues --url http://127.0.0.1:5173`、`python3 scripts/dev_server_guard.py --status` 和定向 `git diff --check`；未启动系统 Chrome。只读本地 HTTP `fetch` 版本探针因沙箱 `EPERM` 被拦，提权审批超时，未作为通过依据。
- 2026-07-07 `SAPD Wiki Project Test Skill`：按用户要求将全工程测试流程固化为可复用 Codex skill。新增全局 skill `sapd-wiki-project-test`，物理目录为 `/Users/kim1st/Documents/kim note/10_agent_system/01 Codex/CodexSkill/sapd-wiki-project-test`，并创建发现入口 symlink `/Users/kim1st/.codex/skills/sapd-wiki-project-test`。skill 触发范围覆盖 SAPD Wiki 工程测试、回归、`pre-commit`、`pre-dmg`、5173 smoke、用户库验证、数据边界检查、DMG 打包前审计和 macOS DMG 发布验收；默认读取 `AGENTS.md`、`CURRENT_STATE.md`、`progress.md` 和 `docs/07-governance/project-test-workflow-and-case-matrix.md`，并优先调用 `scripts/run_project_test_suite.mjs`。验证通过：`quick_validate.py` 返回 `Skill is valid!`，symlink 指向正确。未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。
- 2026-07-07 `Project Test Workflow + Case Matrix`：按用户要求设计全工程测试流程和测试用例，覆盖可独立执行的静态检查、GitHub / JSON / 受保护基线边界、核心业务数据契约、前端契约、5173 运行态、用户库 / 批注 / 导出链路、交付契约和 macOS DMG 打包过程；同时定义 `quick`、`pre-commit`、`pre-dmg`、`full`、`release-full` 分层执行方式。新增 `docs/07-governance/project-test-workflow-and-case-matrix.md` 和 `scripts/run_project_test_suite.mjs`，并更新 `docs/README.md`、`docs/07-governance/governance-index.md`、`scripts/README.md` 和本文件。固化规则：默认不启动系统 Chrome、不重打 DMG；真实浏览器回归需 `--allow-system-chrome`，真实 DMG 构建需 `--include-dmg-build`；5173 通过不能替代 DMG Runtime 验收。验证通过：`node --check scripts/run_project_test_suite.mjs`、`node scripts/run_project_test_suite.mjs --list`、`node scripts/run_project_test_suite.mjs --suite pre-dmg --dry-run --url http://127.0.0.1:5173`、`node scripts/run_project_test_suite.mjs --suite static,delivery`、`node scripts/run_project_test_suite.mjs --suite quick`、`node scripts/run_project_test_suite.mjs --suite release-full --dry-run --url http://127.0.0.1:5173` 和定向 `git diff --check`。未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。
- 2026-07-07 `macOS DMG Browser Parity Contract`：按用户要求明确 DMG 与 5173 开发版一致性方案、当前流程差异和影响可接受性。固化规则：DMG 是当前工作区发布快照，不是实时镜像；一致性定义为同源构建输入、同业务验收样例通过、交付层差异进入白名单。必须一致的范围为前端 `frontend/capability-browser`、后端运行源码 / helper、`data/database/sapd_wiki.sqlite3`、前端离线数据包、空用户库 `user_schema_0.3` 和核心业务工作流；允许差异为 macOS wrapper / WebView、授权版与无授权版、用户库内容、保存 / 下载路径、签名公证状态和打包后快照。已修正 `apps/macos/SAPDWiki/script/build_and_run.sh` 的 backend source hash，纳入 `check_bundle_runtime.py`、`create_user_db.py`、`export_diagnostics.py` 和 `package_backend_pyinstaller.py`，避免 helper 变更时复用旧 backend；新增 `docs/09-delivery/mac-dmg-browser-parity-contract.md` 和 `scripts/audit_mac_dmg_browser_parity_contract.mjs`，更新 `apps/macos/SAPDWiki/README.md`、`docs/README.md`、`scripts/README.md` 和本文件，并把 README 不得回退到旧 `Application Support` / 指纹 seed 口径纳入审计。验证通过：契约审计 `13` 项、5173 状态、5173 内容 smoke、搜索页轻量 smoke、JSON 包边界、GitHub 数据边界和定向 `git diff --check`；未启动系统 Chrome，未重打 DMG。未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS DMG 产物。
- 2026-07-07 `macOS Dual Variant Packaging + SAPDWiki Root Path`：按用户要求优化初始化保存路径和打包交付结构。固化规则：用户在首次启动 / 系统设置中选择的是父级保存位置，App 自动在其下创建 `SAPDWiki` 文件夹，`Runtime` 和默认 `export` 都在 `SAPDWiki` 下；打包默认一次生成授权版和无授权版两个 DMG，统一版本号和时间戳，文件名分别带 `license` / `no-license`，并分别存放在 `apps/macos/SAPDWiki/dist/license/` 与 `apps/macos/SAPDWiki/dist/no-license/`。无授权版通过 `SAPDWikiLicenseMode=no-license` 关闭授权弹窗，前端授权状态显示 `无限制版`；授权版保持 `Passc0de`、30 天试用和到期不可跳过规则。修改 `apps/macos/SAPDWiki/Sources/SAPDWiki/main.swift`、`apps/macos/SAPDWiki/script/build_and_run.sh`、`apps/macos/SAPDWiki/script/package_dmg.sh`、`scripts/build_zip_bundle.py`、`frontend/capability-browser/components/AppShell.js`、`CURRENT_STATE.md` 和本文件；未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包或用户 Issue / 批注数据库。验证通过：Python / JS / shell 语法检查、双包打包、两包 `hdiutil verify`、两套 staging App `codesign --verify --deep --strict`、`Info.plist` 版本和授权模式核对、两包用户库 `user_notes=0`、两包外层 / Runtime README 路径与授权说明核对、5173 状态、首页轻量 smoke 和定向 `git diff --check`。最终授权版 SHA-256 `4010b3882b79a0dfa04f171404c15543ad6ca1dc1579c4f9e115953e0d45cfca`，无授权版 SHA-256 `a9c42840571091df2e0950011e867dcaf2553c33d0d300d5d79a4f6a56b0f59d`。
- 2026-07-07 `Workbench Issue Split Pane Height Contract Follow-up`：用户确认全局搜索低命中布局和 `SEA-03` 行级定位可验收后，继续处理工作台 `Issue 清单` 三栏滚动在强制刷新后仍无可见结果的问题。根因规则补充为：工作台 Issue 页不是只给三个面板写 `overflow-y:auto`，外层 `workbench-workspace` 必须和其他 integrated 业务页一样拥有明确页面高度 `calc(100dvh - var(--topbar-height) - var(--page-header-height))`；`Issue 范围`、`Issue 处理队列`、`选中 Issue` 才能成为真正的本地滚动 owner，右侧保存 / 删除等操作区必须 sticky 在面板内且不能用负 `bottom` 裁出可视区。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html` 和 `scripts/audit_frontend_scroll_contract.mjs`；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。验证通过：`node --check frontend/capability-browser/app.js`、`node --check scripts/audit_frontend_scroll_contract.mjs`、前端滚动契约审计 `checkCount=20`、工作台 Issue 轻量 smoke 和 5173 服务状态；未启动系统 Chrome。
- 2026-07-07 `Global Search Standard Row Anchor + Workbench Issue Scroll Fix`：按用户截图处理三个通用问题：全局搜索低命中结果页空白过大、`SEA-03 纵深防御架构` 点击后停在标准页首屏、工作台 `Issue 清单` 三栏缺少可触达滚动并导致底部按钮在 DMG 窗口里疑似失效。固化规则：全局搜索点击必须以业务目标锚点为准，标准控制项结果必须落到标准表行级 `standard_control:<framework>:<table>:<row>`，不能用标准页首页或 SCF 分组代替；结果数小于等于 3 的搜索页使用紧凑密度；工作台 Issue 三栏分别是独立纵向滚动 owner，检查面板底部操作区必须 sticky 且保留保存 / 删除 / 批量处理委托事件。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/components/StandardFrameworkTable.js`、`frontend/capability-browser/styles.css`、`frontend/capability-browser/index.html`、`scripts/audit_global_search_index_contract.mjs` 和 `scripts/audit_frontend_scroll_contract.mjs`；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。验证通过：前端 / 组件 / 审计脚本语法检查、全局搜索契约审计 `checkCount=51`、前端滚动契约审计 `checkCount=19`、5173 服务状态、`纵深` 运行态 API 探针返回 `total=1` 且目标为 `/standards/dsp-level-2` 的 `standard_control`、组件级渲染断言确认第 `1245` 行 `SEA-03` 被补入渲染窗口并带 `data-standard-row-id` / `data-standard-row-code`、搜索页和工作台 Issue 轻量 smoke 通过。未启动系统 Chrome；DMG 内按钮真实点击仍需用户在新版包或下次打包后人工复核。
- 2026-07-07 `macOS License Gate 0.1.5 DMG`：按用户要求实现 DMG / App 授权机制并重打正式版本 `0.1.5`。固化规则：授权是 macOS 桌面交付启动门禁，不是前端 localStorage 状态；未激活时每次打开 App 先显示授权窗口，底部左侧为 `跳过输入`、右侧为 `确认`；试用未到期可跳过并获得 / 继续 30 天试用；试用到期后禁止跳过；正确授权码 `Passc0de` 激活后不受限制。前端顶栏原 `!` 入口替换为授权状态展示，从 `/api/v1/health.license` 读取；试用显示 `使用有效期至 yyyy-MM-dd（剩余XXd）`，激活显示 `已激活`。修改 `apps/macos/SAPDWiki/Sources/SAPDWiki/main.swift`、`apps/macos/SAPDWiki/script/build_and_run.sh`、`apps/macos/SAPDWiki/script/package_dmg.sh`、`scripts/build_zip_bundle.py`、`scripts/run_local_server.py`、`frontend/capability-browser/components/AppShell.js`、`frontend/capability-browser/app.js`、`frontend/capability-browser/index.html`、`frontend/capability-browser/styles.css`、`CURRENT_STATE.md` 和本文件；未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包或用户 Issue / 批注数据库。验证通过：Python / JS / shell 语法检查、旧版本号残留核对、`apps/macos/SAPDWiki/script/build_and_run.sh build`、`apps/macos/SAPDWiki/script/package_dmg.sh`、`hdiutil verify`、`codesign --verify --deep --strict`、`plutil` 版本键核对、包内用户库 `user_notes=0`、两份中文 `README-FIRST.md` 授权说明核对、base manifest 版本核对、5173 状态、首页轻量 smoke 和定向 `git diff --check`。最终 DMG 为 `apps/macos/SAPDWiki/dist/SAPD-Wiki-0.1.5-20260707-032041Z-mac-arm64.dmg`，SHA-256 `5d6267d8e76176f758e9868953f1645bb517a4a61119257f203feb171f1fe56f`。
- 2026-07-06 `macOS Titlebar Version V0.1.0 + Window Restore Fix`：按用户截图反馈修正版本展示位置和窗口按钮行为。固化规则：版本号属于 macOS 桌面交付元信息，不属于前端侧栏品牌区；前端侧栏不得显示版本胶囊，主窗口标题栏显示 `SAPD Wiki V0.1.0`，系统设置显示当前版本 `V0.1.0`；DMG 文件名继续使用 `版本号 + UTC 时间戳 + 架构`。红色关闭按钮现在隐藏主窗口并把 App 退到后台，Dock / 菜单重开、最小化恢复统一走 `showPrimaryWindow()`，恢复时确保 WebView 与 contentView 可见并触发重绘，降低黑屏风险。修改 `apps/macos/SAPDWiki/Sources/SAPDWiki/main.swift`、`apps/macos/SAPDWiki/script/build_and_run.sh`、`apps/macos/SAPDWiki/script/package_dmg.sh`、`scripts/build_zip_bundle.py`、`frontend/capability-browser/index.html`、`frontend/capability-browser/components/AppShell.js`、`frontend/capability-browser/styles.css`、`CURRENT_STATE.md` 和本文件；未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包或用户 Issue / 批注数据库。验证通过：Python / JS / shell 语法检查、`apps/macos/SAPDWiki/script/build_and_run.sh build`、`apps/macos/SAPDWiki/script/package_dmg.sh`、`hdiutil verify`、`codesign --verify --deep --strict`、`plutil` 版本键核对、包内用户库 `user_notes=0`、两份中文 `README-FIRST.md` 内容核对、打包 Runtime 前端无 `brand-version` 核对、5173 状态、首页轻量 smoke 和定向 `git diff --check`。最终 DMG 为 `apps/macos/SAPDWiki/dist/SAPD-Wiki-V0.1.0-20260706-101217Z-mac-arm64.dmg`，SHA-256 `37f602355a0123aaccdd71f355a86e8e56198c48c1b4b048dbf0a3b67d03549f`。
- 2026-07-06 `macOS App Display Version V0.1 + README-FIRST`：按用户要求把当前正式显示版本定义为 `V0.1`，并贯穿打包和系统内展示。固化规则：系统内只显示产品版本号 `V0.1`，不显示构建时间戳；DMG 文件名使用 `版本号 + UTC 时间戳 + 架构`，本轮产物为 `apps/macos/SAPDWiki/dist/SAPD-Wiki-V0.1-20260706-095709Z-mac-arm64.dmg`；`Info.plist` 继续保留 macOS 兼容数字版本 `0.1.0`，另用 `SAPDWikiDisplayVersion=V0.1` 供设置窗口读取。修改 `apps/macos/SAPDWiki/Sources/SAPDWiki/main.swift`、`apps/macos/SAPDWiki/script/build_and_run.sh`、`apps/macos/SAPDWiki/script/package_dmg.sh`、`scripts/build_zip_bundle.py`、`frontend/capability-browser/index.html`、`frontend/capability-browser/components/AppShell.js`、`frontend/capability-browser/styles.css`、`CURRENT_STATE.md` 和本文件；未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包或用户 Issue / 批注数据库。验证通过：Python / JS / shell 语法检查、`apps/macos/SAPDWiki/script/build_and_run.sh build`、`apps/macos/SAPDWiki/script/package_dmg.sh`、`hdiutil verify`、`codesign --verify --deep --strict`、`plutil` 版本键核对、包内用户库 `user_notes=0`、两份中文 `README-FIRST.md` 内容核对、打包 Runtime 前端 `V0.1` 核对、5173 状态、首页轻量 smoke 和定向 `git diff --check`。SHA-256 为 `c694760768b5eedd089c0c1718d653b196249ddc427855844682c1fb383a23cd`。
- 2026-07-06 `macOS System Settings Path Contract 2.1`：按用户截图反馈收敛 macOS 顶部 App 菜单和默认下载路径。固化规则：`SAPD Wiki` App 菜单只保留 `系统设置...` 和 `退出 SAPD Wiki` 两个用户动作；`App 保存位置` 和 `文件下载路径` 不再作为分散菜单项，而是统一进入系统设置窗口，两行配置均显示具体路径并在右侧提供 `更改路径` 按钮；首次初始化下载路径默认值为 `<App 保存位置>/export`，不再默认到 `~/Downloads/SAPD Wiki`；上一版错误默认目录会在读取设置时迁到新默认目录，但已自定义下载路径不强制覆盖。修改 `apps/macos/SAPDWiki/Sources/SAPDWiki/main.swift`、`apps/macos/SAPDWiki/script/package_dmg.sh`、`CURRENT_STATE.md` 和本文件；未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或前端页面。验证通过：`swift build --package-path apps/macos/SAPDWiki`、`SAPD_WIKI_APP_VERSION=0.1.0-alpha apps/macos/SAPDWiki/script/package_dmg.sh`、`hdiutil verify`、`codesign --verify --deep --strict`、包内用户库 `user_notes=0`、Runtime 无日志 / 导出残留、源码菜单 / 默认路径静态断言和打包 README 断言。最终 DMG 为 `apps/macos/SAPDWiki/dist/SAPD-Wiki-0.1.0-alpha-20260706-093822Z-mac-arm64.dmg`。
- 2026-07-06 `macOS DMG Desktop Delivery Contract 2.0`：按用户 7 点反馈修正 DMG 桌面交付契约。固化规则：DMG Runtime 内只携带干净空用户库模板；首次启动由用户选择 `App 保存位置` 和 `文件下载路径`；用户库创建在 `<App 保存位置>/Runtime/data/user/sapd_wiki_user.sqlite3`，仅缺失时 seed，Runtime / App 升级不得因指纹变化覆盖已有用户库；导出由后端写入下载路径并返回 `output_path`，前端展示导出中动画和导出完成路径；macOS 红色关闭按钮只隐藏窗口不退出，黄色按钮保留系统最小化语义，退出走 `SAPD Wiki > 退出 SAPD Wiki`；DMG 文件名必须带版本和构建时间戳。修改 `apps/macos/SAPDWiki/Sources/SAPDWiki/main.swift`、`apps/macos/SAPDWiki/script/package_dmg.sh`、`scripts/run_local_server.py`、`frontend/capability-browser/dataClient.js`、`frontend/capability-browser/app.js`、`frontend/capability-browser/styles.css`、`CURRENT_STATE.md` 和本文件；未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包或用户 Issue / 批注数据库。验证通过：`python3 -m py_compile scripts/run_local_server.py`、`node --check frontend/capability-browser/dataClient.js`、`node --check frontend/capability-browser/app.js`、`swift build --package-path apps/macos/SAPDWiki`、临时源码后端导出接口 smoke、打包后二进制导出接口 smoke、`SAPD_WIKI_APP_VERSION=0.1.0-alpha apps/macos/SAPDWiki/script/package_dmg.sh`、`hdiutil verify`、`codesign --verify --deep --strict`、包内 `user_notes=0`、Runtime 无日志 / 导出残留、最终包内前端导出状态断言。最终 DMG 为 `apps/macos/SAPDWiki/dist/SAPD-Wiki-0.1.0-alpha-20260706-092219Z-mac-arm64.dmg`。
- 2026-07-06 `macOS DMG Fresh User DB Seed Contract`：历史规则记录，已被本轮 `macOS DMG Desktop Delivery Contract 2.0` 取代。该轮曾按“Runtime 指纹变化时备份并 seed `~/Library/Application Support/SAPD Wiki/Runtime/data/user/sapd_wiki_user.sqlite3`”处理旧用户库污染；最新规则已改为首次启动选择 `App 保存位置` / `文件下载路径`，用户库只在所选位置缺失时从包内空模板创建，后续新版 App 默认复用已有用户库。
- 2026-07-06 `Design Documentation Governance`：按用户要求治理设计文档散乱问题，本轮不新增治理文档、不移动 / 删除历史文件，只在现有入口固化分层和生命周期。盘点结果：非归档 Markdown 约 `125` 个，设计相关命中集中在 `docs/06-implementation`、`frontend/design-handoff/implementation-specs`、`docs/04-user-guide`、`docs/04-frontend` 和 `frontend/design-handoff/stitch-*`。已固化规则：页面实现只以 `frontend/design-handoff/implementation-specs/` 中 active / implementation-source 规格为直接依据；Stitch 输入 / 输出、截图、prompt 和旧 brief 只能作为 reference，必须先转成 implementation spec 或补进现有 spec；小 UI 修复、文案、局部样式和一次性截图反馈不新增设计文档。修改 `AGENTS.md`、`docs/README.md`、`docs/07-governance/governance-index.md`、`frontend/design-handoff/README.md`、`CURRENT_STATE.md`、`findings.md`、`task_plan.md` 和本文件；未修改源 Excel、SQLite、正式 JSON 数据包、前端运行代码、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-06 `DMG Annotation Carryover Root Cause`：处理用户反馈“DMG 运行为什么会带一条批注”。根因不是包内用户库污染：当前 `.app/Contents/Resources/Runtime/data/user/sapd_wiki_user.sqlite3` 的 `user_notes=0`；真正来源是本机 `~/Library/Application Support/SAPD Wiki/Runtime/data/user/sapd_wiki_user.sqlite3` 中 2026-06-30 测试留下的 1 条批注，旧 DMG wrapper 复用了同一 Bundle ID 下的持久化用户库。已将当前工作区已有的 `userDbSeedFingerprint` 修复重新打入 DMG：Runtime 指纹变化时，旧本机用户库会先备份到 `Runtime/data/user/backups/`，再从包内空用户库 seed；同一 Runtime 指纹下则复用用户库。新 DMG SHA-256 为 `5dc8d42f1fb463944db037ce7628774bbdbd84b83124afd81cad51eb6a86cd5d`，Runtime fingerprint 为 `05009f0fbe72d10f57b37a13dfc2a122e8e39b5b66a642ba856be7b1358dc54c`。验证通过：`hdiutil verify`、`codesign --verify --deep --strict`、包内 `user_notes=0`、wrapper 二进制包含 `user-db-seeded / user-db-backed-up / .sapd-user-db-seed-fingerprint`、Runtime 副本 `--check-only` 和 Markdown 批注导出。未主动启动新 DMG 清理本机用户库，避免直接改写用户本机测试数据。
- 2026-07-06 `Latest macOS DMG Packaging`：按用户要求同步项目状态后，使用 `SAPD_WIKI_REBUILD_BACKEND=1 apps/macos/SAPDWiki/script/package_dmg.sh` 从当前工作区重新打包最新版 macOS DMG。打包流程强制重建 PyInstaller 后端、生成 Runtime bundle、组装并签名 `SAPD Wiki.app`、创建 `apps/macos/SAPDWiki/dist/SAPD-Wiki-mac-arm64.dmg`；网络恢复后复核当前磁盘产物，SHA-256 为 `a2da7a72d3c12109a57c980daecb990573c51b82a1886cd8bfff57395220ddf7`，大小 `164M`，Runtime fingerprint 为 `0f7670db3afe14a95d144d84192d1577bc66f7a0751003c0c2aa551b51f7377b`。验证通过：`hdiutil verify`、`codesign --verify --deep --strict`、Runtime 副本 `SAPD-Wiki-Backend --check-only`、批注 Markdown 导出头部断言、刷新工具栏源码入口检查。验证时发现直接在签名后的 `dist/SAPD Wiki.app` 里运行后端会写日志 / 导出文件并破坏资源封签，因此已重新打包恢复干净产物，运行态自检改在 `/tmp` Runtime 副本中完成。当前 DMG 包含 `user_schema_0.3`、base DB hash `2260ae512992348de1562c6723651476a04a7e08b0931506ec7dd8786b658b04`、批注 Markdown 导出和 macOS 工具栏 `刷新页面 / 强制刷新` 入口。本轮未修改源 Excel、正式 SQLite、标准包、字典包、LC 数据包、环境源数据或用户 Issue / 批注数据库；只更新 macOS 打包产物和状态记录。
- 2026-07-06 `OI-157 DMG Delivery Markdown Export Verification`：用户纠正当前 macOS 版本应按 DMG 验收后，撤销上一轮 ZIP 口径结论并重验真实交付物。旧 `apps/macos/SAPDWiki/dist/SAPD-Wiki-mac-arm64.dmg` 反例确认：包内 Runtime 为 `user_schema_0.1`，`--export-user-notes` 仍导出 `.json`，不能关闭 `OI-157`。已执行 `SAPD_WIKI_REBUILD_BACKEND=1 apps/macos/SAPDWiki/script/package_dmg.sh` 重建 backend、`.app` 和 DMG；新 DMG 为 `apps/macos/SAPDWiki/dist/SAPD-Wiki-mac-arm64.dmg`，SHA-256 `5e78d0d04e6165ce06254d406490317b84273877e5d37dd6bac49812e27d7ce0`。最终按 DMG 挂载后的 `.app` 验收通过：`hdiutil verify`、`codesign --verify --deep --strict`、`scripts/check_bundle_runtime.py --json`、包内 `SAPD-Wiki-Backend --help`、`--check-only`、`diagnostics/export-diagnostics.command`、`diagnostics/export-user-notes.command`、Markdown 内容断言、诊断包边界和 127.0.0.1 runtime API smoke；批注导出为 `user-notes-export-*.md`，诊断包不包含用户 SQLite 或批注正文。`OI-157` 已按 DMG 口径归档，当前台账 `active=2 / archived=189`。本轮未修改源 Excel、正式 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包或用户 Issue / 批注数据库；已更新 macOS DMG 产物。
- 2026-07-06 `OI-135 Runtime Schema Metadata Closure`：处理 `user_schema_0.3` 元数据被服务重启写回 `user_schema_0.2` 的回归风险。根因规则定位为：用户库 schema 版本是迁移与打包的运行契约，不是一次性 SQLite 字段；开发 API、打包 runtime、用户库创建脚本、ZIP bundle 默认参数和治理审计必须同源声明当前版本。已将 `src/sapd_wiki/api_server.py`、`scripts/run_local_server.py`、`scripts/create_user_db.py`、`scripts/build_zip_bundle.py`、`scripts/smoke_user_data_basket_api.mjs` 和 `scripts/audit_user_db_governance_contract.mjs` 统一到 `user_schema_0.3`。验证通过：Python / JS 语法检查、临时新用户库创建为 `schema_version=user_schema_0.3` 且 13 张 v0.3 表齐全；触发 `GET /api/v1/user/notes` 后正式 `data/user/sapd_wiki_user.sqlite3` 的 `user_meta.schema_version=user_schema_0.3`；`node scripts/audit_user_db_governance_contract.mjs --db data/user/sapd_wiki_user.sqlite3 --require-v03 --json` 通过。按用户确认，`OI-135` 已由 `node scripts/govern_open_issues.mjs` 归档，当前台账 `active=3 / archived=188`。未修改源 Excel、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包或 macOS 打包产物。
- 2026-07-06 `OI-135 Formal DB Apply`：按用户确认执行真实库 apply，把 clean base candidate 和用户库 target_ref 迁移正式落到项目数据库。固化规则：发布级基础库只能保留唯一主对象，`security_work` 多关注点关系通过 `maps_to_work` 表达；用户库基础对象引用必须迁到 `base:<object_type>:<stable_key>`，页面级 `v2` / 指南 / 幻灯片锚点不参与基础对象迁移。已先完成准生产 dry-run，再将 clean candidate 升级为 stable-key 副本 `data/exports/worker-verify/oi-135-formal-apply/20260706T063552Z/sapd_wiki_base.clean-candidate.stable.sqlite3`，备份并替换 `data/database/sapd_wiki.sqlite3`；正式基础库 hash 从 `e92675f2fcbe585ab4bc6d7de23274cc04a9ec472e08ec671393a87199532cfe` 变为 `2260ae512992348de1562c6723651476a04a7e08b0931506ec7dd8786b658b04`，`security_work=80`、重复标题 `0`、`knowledge_items.stable_ref=4678/4678`。正式用户库迁移 `legacyRefsFound=2 / updatedRefs=2 / pendingRefs=0 / conflictRefs=0`，`user_target_ref_migrations` 为 `applied=2`，旧 `base:information_object:418bd2f6-ff6e-431e-b2ef-059df3cdd2ae` 已迁到 `base:information_object:information_object:hash:9055299c885b70a0`。备份与回退报告见 `data/exports/worker-verify/oi-135-formal-apply/20260706T063552Z/oi135-formal-apply-report.md`；备份目录为 `data/database/backups/oi-135-formal-apply/20260706T063552Z/`。验证通过：stable key 合同审计 `legacyBaseRefs=0`、用户库治理审计 `--require-v03` 通过、两库 `PRAGMA integrity_check=ok`、字典 / 标准基线审计 `errors=0 warnings=0`、GitHub 数据边界 OK、5173 重启后首页和 workspace projection 通过。未修改源 Excel、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、macOS 打包产物或前端页面。
- 2026-07-06 `OI-135 User legacy target_ref Migration Dry-run`：继续处理 OI-135 用户库 legacy target_ref 迁移 / redirect 方案。固化规则：旧 `base:<type>:<uuid>` 只允许通过基础库 stable ref 映射迁移；页面级 `v2`、指南、幻灯片锚点不是基础对象 stable ref，必须保留页面上下文解析器，不参与本轮基础对象迁移。已增强 `scripts/migrate_db_contracts.mjs`：`user_schema_0.3` 迁移现在会扫描 `user_notes`、`user_favorites`、`user_item_tags`、`user_change_logs`、工作台、数据篮、导出、用户自定义关系、导入 staging、review decisions 等表的 `target_ref / source_ref`，把可解析旧 UUID 基础对象迁到 stable ref，并写入 `user_target_ref_migrations`；真实项目库仍必须 `--apply --confirm-project-db-write` 才会写入。本轮 dry-run 使用 clean base candidate 和复制用户库，迁移 `数据分析层` 旧引用 `base:information_object:418bd2f6-ff6e-431e-b2ef-059df3cdd2ae` 到 `base:information_object:information_object:hash:9055299c885b70a0`，覆盖 `user_notes.target_ref` 与 `user_change_logs.target_ref` 各 `1` 条，`legacyRefsFound=2 / updatedRefs=2 / pendingRefs=0 / conflictRefs=0`。迁移后 stable key 审计通过，`legacyBaseRefs=0`；用户库治理审计复制库 `--require-v03` 通过，仅保留既有 `legacy_favorite_notes=1` warning。同步更新 OI-135 台账、状态入口和计划；未修改源 Excel、真实 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-06 `OI-135 Clean Base Candidate + security_work Grain Fix`：按用户确认方向，不再对历史 SQLite 做零散补丁，改为“发布级干净基础库 + 用户库独立迁移 + 中间产物隔离”。本质规则已固化：`security_work` 在源表关系行中重复是正常的，一个安全工作可对应多个关注点；但 `knowledge_items` 主对象层必须唯一，多关注点关系只能由 `maps_to_work` 表达，不能复制主对象。新增 `scripts/build_oi135_clean_base_candidate.py`，只复制当前基础库到隔离候选目录并修正候选库，不写 `data/database/sapd_wiki.sqlite3`。候选产物为 `data/exports/worker-verify/oi-135-clean-base-candidate/20260706T061121Z/sapd_wiki_base.clean-candidate.sqlite3`，报告为同目录 `oi135-clean-base-candidate-report.md/json`；候选库将旧库 `93` 行 `security_work` 收敛为运行包权威的 `80` 个唯一标题，删除 `13` 个无关系重复主对象，保留 `92` 条 `maps_to_work` 关系。候选库执行 `node scripts/migrate_db_contracts.mjs --scope base --base-db ... --json` dry-run 通过，基础库 stable key 审计通过；完整 `audit_stable_key_contract` 仍因当前用户库存在 `2` 条 legacy `base:<type>:<uuid>` target_ref 失败，作为下一步用户库迁移 / redirect 问题保留。同步更新 `docs/06-implementation/open-issues.md`、`task_plan.md`、`CURRENT_STATE.md`、`scripts/audit_user_db_governance_contract.mjs` 和本文件；未修改源 Excel、真实 SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-06 `OI-146 / OI-147 Environment Target-System Formal Apply`：按用户截图二次复核并纠正上一轮样例错误。源表权威事实为：第 `198` 行是 `I-AP&T-AS.AM-01 软件资产清单 -> API安全防护`，不是 `API网关`；`API网关` 的源表行为第 `205-209` 行，H 列合并展开后均为 `应用安全防护`。根因补充为正式环境包中存在 `123` 条源表不存在的服务-目标组合，其中伪造的 `I-AP&T-AS.AM-01 -> API网关` 抢占了同名目标聚合，导致页面取到无系统目标而不连 `应用安全防护`。已修改 `scripts/apply_environment_measure_system_relations.mjs`，对源表不存在的服务-目标组合执行受控剪除；修改 `scripts/audit_environment_page_business_logic.mjs`，把 `unexpectedTargetCount > 0` 作为 hard fail；修改 `frontend/capability-browser/viewModels.js`，同名目标聚合时合并 `systems / linkedSystems / securitySystems / products / measures / mapping_sources`，避免合法多行关系被首个重复目标遮挡。已受控 apply `frontend/capability-browser/public/data/environment-workbench.json`，并重新生成 / 正式 apply OI-149 环境分片。业务样例通过：`云数据中心 / 业务应用 / API网关层` 中 `API网关` 仅保留 `5` 条源表真实服务关系，均连接到 `应用安全防护`；`I-AP&T-AS.AM-01` 只保留在 `API安全防护` 目标下。验证通过：环境目标系统 apply dry-run / apply / final dry-run、`node scripts/audit_environment_page_business_logic.mjs`（`expectedTargetKeys=899 / actualTargetKeys=899 / unexpectedTargetCount=0`）、正式 split runtime、组件级静态渲染抽样确认 `API网关` 的 `apiTargetCount=1 / apiServiceCount=5 / apiSystems` 非空。`docs/06-implementation/open-issues.md` 当前台账为 `active=4 / archived=187`；本轮未修改源 Excel、SQLite、标准包、字典包、LC 数据包、用户 Issue / 批注数据库或 macOS 打包产物，未启动系统 Chrome。
- 2026-07-06 `Issue Acceptance, Environment Target-System Root Cause, Search Anchor Fix`：按用户确认关闭并归档 `OI-150 / OI-156`；自动复核关闭 `OI-161 / OI-145`；按 LC-DT 行级策略投影权威口径关闭 `OI-141 / OI-170`。当前台账为 `active=6 / archived=185`。`OI-146 / OI-147` 已从“待进一步明确”改为“待修复”：初查确认源表 H 列第 `198-209` 行属于同一合并区，但二次复核已纠正目标粒度：第 `198` 行目标为 `API安全防护`，`API网关` 的源表行为第 `205-209` 行；当时正式 `environment-workbench.json` 中 `API网关` 目标级系统缺失，且后续发现还存在源表不存在的服务-目标组合。已修正 `scripts/apply_environment_measure_system_relations.mjs` 和 `scripts/audit_environment_page_business_logic.mjs` 的当前结构 `contextKey` 推导，dry-run 从旧 `unmatchedTargetKeys=899` 收敛为 `unmatchedTargetKeys=53`，真实缺失为 `missingTargetSystemKeys=210`，其中 `API网关` 缺 `5` 条目标级系统关系；正式 apply 和二次剪除结果见上一条最新记录。安全技术模块 / 措施搜索定位通病已修复：前端 fallback 维护字典结果现在与后端索引一样携带 `objectType / objectId / targetRef`，点击不再只能打开页签首页；`scripts/audit_global_search_index_contract.mjs` 新增断言并通过静态与 5173 runtime 审计。`PGA-0001 知情同意管理 -> 数据安全管理与运营` 当前全局保护审计已为 `P0/P1/P2/P3/P4=0`，属于历史候选项，不是当前未关闭问题。`OI-135` 复核发现 `node scripts/migrate_db_contracts.mjs --json` 失败于 `knowledge_items.stable_ref` 唯一约束，已同步台账、`task_plan.md`、用户库治理设计和审计脚本，真实库 apply 前必须先治理重复 stable_ref。验证通过：`node scripts/govern_open_issues.mjs`、`node scripts/audit_workbench_issue_interaction_contract.mjs`、`node scripts/audit_local_api_security_boundary.mjs`、`node scripts/audit_global_search_index_contract.mjs`、`node scripts/audit_global_search_index_contract.mjs --url http://127.0.0.1:5173 --runtime-only`、`node scripts/audit_user_db_governance_contract.mjs --json`、相关 `node --check`；未启动系统 Chrome，未修改源 Excel、SQLite、正式业务 JSON、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-06 `Open Issues Latest Acceptance + Annotation Locate Root Cause`：按用户最新口径关闭并归档可验收问题：`OI-187`、`OI-186`、`OI-178`、`OI-162`、`OI-174`、`OI-173`、`OI-169`、`OI-168`、`OI-172`、`OI-171`、`OI-149`、`OI-159`、`OI-137`、`OI-153`、`OI-152`、`OI-151`、`OI-148`，以及搜索第二组 `OI-184 / OI-183 / OI-181 / OI-180 / OI-179 / OI-175`。`OI-146 / OI-147` 改为待进一步明确；`OI-138` 按用户要求长期保留；`OI-141` 主体已完成，但只读复核 `python3 scripts/audit_lcdt_source_update.py --json` 仍有 `moduleOnlyMapping=1 / stageMismatch=1 / needs_user_confirmation=1`，剩余判断归入 `OI-170`。针对截图中的 `OI-150 / OI-156`，根因定为环境映射页批注恢复目标对象后，当前页内搜索词仍保留 `API网关层`，遮挡了旧 `shadow` 锚点对应的 `数据加密和令牌化` 目标；已固化规则：批注恢复出环境目标对象后，若当前页内搜索不匹配该批注对象，必须清空页内搜索再渲染 / 定位。同步补齐技术服务 / 技术措施维护表主标题值锚点和环境关联 fallback，避免全局批注定位审计缺少渲染锚点。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/components/TechnicalServiceMaintenanceTable.js`、`frontend/capability-browser/components/TechnicalMeasureMaintenanceTable.js`、`frontend/capability-browser/index.html`、`scripts/audit_user_annotation_contract.mjs`、`scripts/frontend_content_smoke_check.mjs`、`docs/06-implementation/open-issues.md`、`docs/06-implementation/open-issues-index.md`、`docs/05-archive/open-issues-history/2026-06.md`、`CURRENT_STATE.md` 和本文件；未修改源 Excel、SQLite、正式业务 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。已执行 `node scripts/govern_open_issues.mjs` 通过，台账为 `active=12 / archived=179`；`node --check` 覆盖相关 JS；`node scripts/audit_user_annotation_contract.mjs` 通过，`globalRenderedValueAnchors.measures=5 / services=5`；`node scripts/frontend_content_smoke_check.mjs --url http://127.0.0.1:5173` 通过，页面 API 为 `dataState=ready`。未启动系统 Chrome。
- 2026-07-06 `OI-189 User Acceptance Closure`：用户确认“可以验收”，已将 `OI-189` 标记为 `已关闭` 并由 `node scripts/govern_open_issues.mjs` 归档到 `docs/05-archive/open-issues-history/2026-06.md`。当前问题台账重算结果为总数 `191`、未关闭 `38`、已关闭归档 `153`。本轮只更新问题台账、索引、归档和恢复入口，未修改业务代码、源 Excel、SQLite、正式数据包、字典包、标准包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-06 `OI-189 LC-DT Policy Matrix Row Anchor + Projection Fix`：按用户截图和专家反馈处理 LC-DT 策略矩阵行级数据与搜索定位通病。根因规则定位为：`LC-DT 安全技术服务、模块、策略映射表` 的每一行是数据重要程度安全策略矩阵权威粒度，矩阵行的 `technical_services` / `module_or_measure_items` 必须逐行等于源 Excel 第 M / N 列，不能由阶段汇总或模块全集反向扩展；全局搜索和局部搜索必须用 `route + selected_process_id + target_ref` 定位矩阵行或行内 chip，不能靠同名文本猜。已重导 `frontend/capability-browser/public/data/lifecycle-knowledge.json` 和 `frontend/capability-browser/public/data/lifecycle-workbench.json`：第 40 行 `加工/使用 US.08` 为 `I-AP&T-PD.DP-01` + `I-OS&T-PD.DP-01`，第 55 行 `提供 PR.07` 为 `I-DI&T-PD.DP-01`。修改 `src/sapd_wiki/api_server.py`、`frontend/capability-browser/app.js`、`frontend/capability-browser/components/ApplicationSecurityLifecycle.js`、`frontend/capability-browser/index.html`、`scripts/audit_global_search_index_contract.mjs`、`scripts/frontend_content_smoke_check.mjs`、`scripts/govern_open_issues.mjs`、`docs/06-implementation/open-issues.md`、`docs/06-implementation/open-issues-index.md` 和本文件；新增 `scripts/audit_lcdt_policy_projection_contract.py`。未修改源 Excel、SQLite、标准包、字典包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。验证通过：LC-DT 行级投影审计 `sourceRows=63 / checkedRows=126 / failureCount=0`，全局搜索静态审计 `checkCount=49`，运行态审计 `checkCount=83`，运行态抽样返回 `lifecycle_policy_relation:*`，组件级渲染抽样确认矩阵 chip `data-lifecycle-target-ref` 和 `<mark>` 生效，内容 smoke 本地与 5173 API 通过，`/data-security` 和 `/search?q=数据流转监测和泄漏防护` 轻量 smoke 通过，JSON 边界、GitHub 数据边界、字典 / 标准基线审计通过。未启动系统 Chrome。
- 2026-07-06 `Document And Issue Governance Gate`：按用户要求治理文档膨胀和 Issue 滥建问题。本轮只更新治理入口和恢复入口，不批量移动 / 删除历史文档，不修改业务代码或数据包。当前非归档 Markdown 文档约 `107` 个、`31540` 行；已固化规则：默认不为小修、小 bug、一次性排查或临时方案新增文档；低严重性问题直接修复并在 `progress.md` 与任务完成反馈说明；只有全局契约、数据 / 审计 / 安全边界、中高严重性、无法本轮闭环或需要用户判断 / 验收的问题才进入 `open-issues.md`。修改 `AGENTS.md`、`docs/README.md`、`docs/07-governance/governance-index.md`、`docs/06-implementation/open-issues.md`、`CURRENT_STATE.md`、`findings.md`、`task_plan.md` 和本文件。未修改源 Excel、SQLite、正式 JSON 数据包、前端代码、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-06 `Open Issues Governance Closure`：按用户“已经验收过的直接关闭”要求，规范并归档已确认验收的问题记录。已关闭 / 归档 `OI-188`、`OI-177`、`OI-176`、`OI-142`、`OI-185`、`OI-182`、`OI-164`；修正台账误归档，将 `OI-172` 和 `OI-186` 恢复为待验收状态。执行 `node scripts/govern_open_issues.mjs` 通过，当前未关闭问题数为 `38`，已关闭归档问题数为 `152`；`git diff --check` 通过。本轮只改问题台账、索引和归档文档，未修改业务代码、数据包、SQLite、源 Excel 或前端页面。
- 2026-07-06 `OI-188 Global Search Target Anchor`：继续处理用户反馈的安全技术服务全局搜索定位通病。根因规则定位为：全局搜索不是“同名文本跳页”，而是 `route + object_type + target_ref` 三元组决定目标页面、目标视图和目标锚点；字典定义结果与能力关系结果是两个合法业务目标，点击后必须落到各自的业务粒度。已固化规则：`/knowledge/technical-services` 字典结果必须展开包含选中服务的分组并定位具体服务行，不能被旧表格滚动状态覆盖；`/capability-mapping` 的 `capability_relation:security_technical_service:<focusId>:<serviceId>` 必须自动打开 `技术视角`，并用 `data-capability-relation-target-ref` 定位表格中的安全技术服务 chip，不能停留在关系图谱或靠文本猜测。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/components/FocusScopeServiceMatrix.js`、`frontend/capability-browser/components/TechnicalServiceMaintenanceTable.js`、`frontend/capability-browser/index.html`、`scripts/audit_global_search_index_contract.mjs`、`docs/06-implementation/open-issues.md` 和本文件；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。验证通过：相关 JS 语法检查、全局搜索索引契约静态审计 `checkCount=47`、运行态审计 `checkCount=80`、内容 smoke、搜索页 / 能力页 / 技术服务清单轻量 smoke、5173 状态；未启动系统 Chrome。
- 2026-07-05 `OI-188 Management Service Dual Target Search`：按用户纠正处理 `M-PM.PR-00`、`M-SA.RM-00`、`M-SA.RE-00`、`M-SA.CO-00`、`M-SE.PE-00`、`M-PS.CT-00` 精确编码搜索结果被错误唯一化的问题。根因规则定位为：这些 `M-* -00` 是源 Excel `安全能力-安全技术服务` 明确定义的安全技术服务，不是生成聚合项，也不是作用域问题；同一源定义对象在全局搜索中可以有多个合法业务目标。上一轮把“字典定义结果 + 能力关系定位结果”误判为竞争关系并过滤字典行，已撤销。已固化规则：精确编码搜索必须同时保留 `/knowledge/technical-services` 的字典定义结果和 `/capability-mapping` 的能力关系结果；能力关系结果必须携带 `capability_relation:security_technical_service:<focusId>:<serviceId>` 与 `selected_capability_id`。修改 `src/sapd_wiki/api_server.py`、`frontend/capability-browser/app.js`、`frontend/capability-browser/index.html`、`scripts/audit_global_search_index_contract.mjs`、`scripts/audit_capability_mapping_integrity.py`、`docs/06-implementation/open-issues.md` 和本文件；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。验证通过：Python / JS 语法、6 个编码 direct search payload、能力映射完整性审计 `hard_issue_count=0` / `review_warning_count=0`、全局搜索索引契约审计、内容 smoke、搜索页 / 能力页轻量 smoke、5173 状态、JSON 边界、GitHub 数据边界和定向 `git diff --check`；未启动系统 Chrome。
- 2026-07-05 `OI-176 / OI-177 Dictionary Reference Canonicalization`：按主控问题处理契约修复数据正确性根因。固化规则：字典引用的 `type/id/code/title` 必须以权威字典为准，`scope_type` 统一从 `maintenance/scopes.json` / 安全能力作用域目录取 canonical title；安全技术模块与安全技术措施不得跨粒度兜底，模块专属名称不得以 `security_technical_measure` 类型出现在运行包或 split projection。修改 `src/sapd_wiki/exports.py`、`scripts/build_oi149_split_candidate.mjs`、`scripts/audit_dictionary_reference_consistency.mjs`，新增 `scripts/fix_dictionary_reference_canonicalization.mjs`，并受控修正当前正式 `public/data` 中 `159` 个 JSON 的作用域引用；未修改源 Excel、SQLite、标准包、用户 Issue / 批注数据库或 macOS 打包产物。验证通过：字典引用审计 `errors=0`（仅剩既有 `work-function-candidate` warnings `138`）、定向反例探针 `scopeOldTitle=0 / scopeCanonicalTitle=1278 / forbiddenMeasureTyped=0`、OI-149 split candidate 审计、正式 split runtime 审计、正式 split 分片预算、基线完整性审计、JSON 边界、GitHub 数据边界、5173 状态、提权前端内容 smoke 和定向 `git diff --check`。
- 2026-07-05 `OI-187 Capability Mapping Integrity Matrix`：按用户要求检查安全能力映射技术视角、管理视角和标准 / 框架映射的数据准确性、关联关系、一致性和业务合理性。根因规则定位为：安全能力映射的可信性不能由页面能渲染图谱证明，必须用源 Excel 明确关系和运行包 `capability-workbench.json` 在 `capability_focus` 粒度逐项对账。新增 `scripts/audit_capability_mapping_integrity.py`，固化源表到运行包的三视角矩阵：关注点-技术服务、服务-安全技术模块、关注点-安全工作、关注点-管理流程、关注点-标准控制项、作用域关系、重复关系和 relation endpoint 完整性；标准映射使用 `frameworkCode + originalControlId`，并保留 `PM-7(1)` 等 NIST enhancement 后缀。审计通过：硬错误 `0`，源关注点 / workbench 关注点 `91 / 91`，关注点-技术服务 `160 / 160`，服务-安全技术模块 `397 / 397`，关注点-安全工作 `92 / 92`，关注点-管理流程 `89 / 89`，关注点-标准控制项 `2288 / 2288`。审计报告已写入 `data/exports/worker-verify/capability-mapping-integrity-audit.json` 和 `.md`。用户纠正后已确认 `M-PM.PR-00`、`M-SA.RM-00`、`M-SA.RE-00`、`M-SA.CO-00`、`M-SE.PE-00`、`M-PS.CT-00` 是源表明确值，不是生成聚合项；审计口径已修正为 `review_warning_count=0`。同轮修复 `OI-178`：`workspace-view` / `workspace-projection` 只允许 `capability_focus` 返回 `localRelationMap`，非关注点级对象只保留同粒度 `graph`、映射表和 summary，避免上层能力节点夹带关注点级关系图。未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。
- 2026-07-05 `OI-186 Capability Correctness-First Load Contract`：处理用户再次反馈的安全能力映射页关注点切换时先显示其他或不完整图谱的问题。根因定位为上一轮“先可用”规则过度放宽：普通 ViewModel fallback 虽然能构造同名关注点结构，但不是可信 backend projection，不能在 `capability_focus` 标题下替代真实关系图谱。已改为正确性优先：L0 / L1 / L2 总览仍可渐进渲染；关注点粒度必须等 `workspace-view` 或 `workspace-projection` 返回同对象 `backend_projection` 后才显示图谱，pending / failed 期间显示加载或重试，不显示 fallback 错图；`workspace-view` 已覆盖当前关注点时不重复触发 projection；能力对象 API 请求保留 5 秒超时兜底。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/dataClient.js`、`frontend/capability-browser/index.html`、`scripts/audit_frontend_lazy_load_contract.mjs`、`scripts/audit_capability_viewmodel_contract.mjs`、`docs/06-implementation/open-issues.md` 和本文件；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。验证通过：前端 / 审计脚本语法检查、能力 ViewModel 契约审计、按需加载契约审计、内容 smoke、能力页轻量 smoke、5173 状态、JSON 边界、GitHub 数据边界和定向 `git diff --check`；未启动系统 Chrome。
- 2026-07-05 `OI-185 Search Coverage Matrix + Domain History Isolation`：按用户纠偏修正搜索系统契约。根因定位为两点：上一轮把所有局部搜索合并到同一个 `page` 历史桶，破坏“搜索词含义随业务域变化”的契约；搜索索引审计只有零散探针，缺少按能力 / 环境 / LC-AP / LC-DT / 知识库 / 标准 / 工作台分域的覆盖矩阵。已将搜索历史改为同一自定义组件、不同业务域历史：`global`、`capability`、`environment`、`lc-ap`、`lc-dt`、`knowledge`、`standards`、`workbench-issues`；删除 / 清空只影响当前域。工作台 Issue 搜索改为 Review Queue 筛选器语义，独立 `workbench-issues` 历史，筛选状态改为可换行 chip，队列标题只保留结果数。新增 `SEARCH_INDEX_COVERAGE_MATRIX`，七域分别固化黄金样例、反例、点击定位和字段边界。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/index.html`、`frontend/capability-browser/components/AppShell.js`、`frontend/capability-browser/components/EnvironmentLocalRelationMap.js`、`frontend/capability-browser/styles.css`、`scripts/audit_search_index_quality_probes.py`、`scripts/audit_global_search_index_contract.mjs`、`scripts/audit_search_state_isolation.mjs`、`scripts/frontend_content_smoke_check.mjs`、`docs/06-implementation/global-search-contract-2026-07-05.md`、`docs/06-implementation/open-issues.md` 和本文件；未修改源 Excel、SQLite、正式 JSON 数据包、标准包、字典包、LC 数据包、环境数据包、用户 Issue / 批注数据库或 macOS 打包产物。验证通过：前端 / 审计脚本语法检查、索引质量探针 `38` 项、全局搜索审计 `44` 项、搜索状态隔离审计 `40` 项、环境搜索审计 `20` 项、内容 smoke、工作台 Issue / 搜索页 / 环境页轻量 smoke、5173 状态、JSON 边界、GitHub 数据边界和定向 `git diff --check`；未启动系统 Chrome。
- 2026-07-05 `OI-182 Final LC-DT / Environment Service Set Acceptance`：用户完成源 Excel 最终修正后，按主控契约重新验收“字典全局关系”和“对象 / 阶段实际服务集合”边界。最终重导三张源表：`作用域-安全技术服务-安全技术模块映射`、`LC-DT 数据生命周期`、`LC-DT 安全技术服务、模块、策略映射表`，import job 为 `b8ed0cfd-42af-48be-83f6-7e56bccbff41`，`items_updated=315`、`relations_created=0`、`relations_deleted=0`。已重导维护包、`shared-lookups`、生命周期包、能力 / 环境 / 生命周期 workbench，并正式 apply OI-149 split projection。验收通过：安全技术服务字典 `160` 条唯一；安全技术模块 `102` 个、模块-服务关系 `397` 条且模块内重复 `0`；环境对象源表与正式投影均为 `67` 个上下文、`806` 条上下文-服务关系，缺失 / 多余均为 `0`；LC-DT 新增脱敏 / 水印服务已进入 `DT-04 加工/使用`、`DT-05 提供` 的阶段和策略投影。通过专项审计、基线审计、JSON 边界、GitHub 数据边界、通用内容 smoke、能力 / 环境 / LC-AP / LC-DT / 字典服务 / 字典模块页轻量 smoke 和 5173 状态检查；未启动系统 Chrome。`OI-182` 已关闭；`OI-170` 仍按用户此前要求保留为“加工/使用阶段模块/措施双表确认”问题，服务集合无差异。
- 2026-07-05 `OI-184 Search Baseline + Index Quality Audit`：按用户要求继续检查全局搜索、局部搜索和索引质量。根因定位为两类契约风险：页面内搜索历史基线没有覆盖所有生产搜索框；搜索索引优化缺少可重复的语义质量探针。当时确认信息化环境局部搜索 `environmentSearchInput` 已关闭浏览器原生 `autocomplete` 并补齐工作台 Issue 搜索框历史提交；后续 `OI-185` 已将“统一 `page` 历史桶”纠正为按业务域隔离。新增 `scripts/audit_search_index_quality_probes.py`，直接探测 `/api/v1/search-index` 的全量 facets、窗口分页、标准 / 框架覆盖、弱命中裁剪和反例隔离。修改 `frontend/capability-browser/app.js`、`frontend/capability-browser/index.html`、`scripts/audit_global_search_index_contract.mjs`、`scripts/audit_search_state_isolation.mjs`、`scripts/frontend_content_smoke_check.mjs`、`docs/06-implementation/global-search-contract-2026-07-05.md`、`docs/06-implementation/open-issues.md` 和本文件；未修改任何禁止数据源或打包产物。验证通过：索引质量探针 `10` 项、全局搜索审计 `44` 项、搜索状态隔离审计 `39` 项、环境搜索审计 `20` 项、内容 smoke、搜索 / 环境 / 工作台 Issue 轻量 smoke、5173 状态、JSON 边界、GitHub 数据边界和定向 `git diff --check`；未启动系统 Chrome。
- 2026-07-05 `OI-183 Global Search History Commit + Pagination Control Fix`：修复搜索历史只依赖输入防抖导致已执行查询未及时进入历史面板的问题，并修复分页 `上一页` / `下一页` 继承图标按钮宽度导致文字断行的问题。固化规则：搜索历史记录“已执行并完成加载的查询”；分页文字按钮必须保留稳定宽度和 `nowrap`。验证通过 JS 语法、全局搜索审计、状态隔离审计、内容 smoke、5173 状态、后端索引探针、搜索页轻量 smoke、JSON / GitHub 数据边界和定向 `git diff --check`；未启动系统 Chrome。
- 2026-07-05 `OI-182 Environment / LC-DT Business Service Set Re-audit`：只读复核安全能力映射、信息化环境安全能力映射、LC-AP、LC-DT、安全技术服务 / 模块 / 措施清单和 LC-DT 数据处理场景与技术映射。结论：未发现新的安全技术模块 / 服务映射数据问题；字典服务 `160` 条唯一，模块 `102` 个、模块-服务关系 `397` 条且模块内重复为 `0`；环境对象源表与正式投影均为 `67` 个对象上下文、`806` 条上下文-服务关系；LC-DT 阶段主表、策略表、workbench 和 split projection 数量一致。未修改源 Excel、SQLite 或正式业务数据包。
- 2026-07-05 `OI-181 Global Search Page Window + Search Memory Baseline`：修复搜索结果页和顶部预览请求状态互相污染、结果页只能看前 `120` 条窗口、环境页局部搜索沿用浏览器原生 autocomplete 等通病。固化规则：搜索结果页使用独立请求序列；`/api/v1/search-index` 支持 `offset/category/window`；全局搜索与搜索结果页共享 `global` 历史，页面内搜索使用同一自定义历史组件，默认显示 5 条、最多 10 条，支持展开、清空和单条删除，纳入输入框统一 `autocomplete="off"`；后续 `OI-185` 已将局部历史从共享 `page` 桶纠正为按业务域隔离。
- 2026-07-05 `OI-180 Global Search Pagination + Sticky Context`：全局搜索结果页增加每页 `20` 条分页、上下分页、跳页和 sticky 查询 / 分类上下文。固化规则：不改变搜索规则、排序、facets 计数、索引来源或数据，只改变结果阅读形态。
- 2026-07-05 `OI-179 Global Search Count Facets Contract Fix`：修复搜索结果页把当前返回窗口当成全量计数展示的问题。固化规则：总数和分类 chip 必须来自 API `facets` 全量命中计数，结果列表窗口必须和全量命中明确区分。
- 2026-07-05 `OI-175 Standards Detail Global Search Coverage Fix`：修复标准 / 框架局部搜索可命中而全局搜索漏掉标准明细行的问题。固化规则：标准 / 框架页面局部搜索可命中的业务明细，必须按同一业务粒度进入全局搜索，并携带可回到标准页的定位字段。

## 本轮执行计划

1. 已把方案 1 忠实复刻到真实评分路由：取消重复项目头，页头状态与“更多”归入 App Shell；评分主区保持 L0 / L1 / L2、L2 汇总、关注点摘要、关注点列表和四维表单原顺序。
2. 已用真实 `T-AS.AD` SERVICE 与 `G-SP.SM` FOCUS 数据完成两张独立运行截图和同构验收；保留搜索、筛选、导入导出、评分面板收起、自动保存与位置恢复。
3. 已完成专项审计 `82/82`、JS 语法、定向 diff、maturity smoke、`1440 / 1024 / 390px` 浏览器布局、字段边界与控制台检查；DMG App 回归和正式持久化继续后置。

## 维护规则

- 本文件只保留最近状态、最近 5-10 条重要执行和恢复入口；超过 120 行时继续压缩或归档到 `docs/05-archive/progress-history/`。
- 详细过程、长命令输出和阶段性历史不写入根目录 `progress.md`。
