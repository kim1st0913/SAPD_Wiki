# progress.md

本文件是当前会话恢复入口，只保留最近状态、最近完成事项、关键验证和历史索引。完整执行历史已归档到 `docs/05-archive/progress-history/`。

## 当前状态（2026-06-03）

- 当前分支：`main`。
- 当前主线：Frontend Baseline 1.0 四页关系工作台校正、数据契约治理、字段边界收口和 Open Issues 轻量维护。
- 固定预览入口：`http://127.0.0.1:5173/`。前端展示和用户验收默认只看该端口。
- 安全能力映射页对象级契约：`/api/v1/capabilities/workspace-view` 已接入；前端优先通过 `dataClient.getCapabilityWorkspaceView()` 按当前选中对象读取。
- Open Issues 当前未关闭：`OI-038`、`OI-128`。
- 当前重点治理项：Delivery Bundle 1.0-alpha 打包清理边界、Windows / macOS ZIP 基准包维护，以及 `USER-WRITE-UI-1` 最小用户写入入口后续实现。
- 当前禁止事项：不默认改 ETL、数据库、数据模型、导出 JSON、workbench JSON、`dataClient` 数据来源边界或 ViewModel 业务逻辑；主展示区不得暴露 `sheet`、`row`、`column`、`raw_value`、`source_file`、`import_id`、`source_id`、`source_ref`、`source_label`、`debug`、`raw`、`metadata`、`intermediate`、`generated_at`。


## 最近完成事项

- 2026-06-03 生命周期表格衍生说明文字清理：按用户反馈删除 `ApplicationSecurityLifecycle.js` 中 LC-AP / LC-DT 表格标题右侧的字段流向、来源表和复用关系说明，仅保留表格业务标题；保留表格内原始业务字段和业务参考来源展示。同步更新组件缓存版本，未改 ETL、数据库、数据包、`dataClient` 或 ViewModel。
- 2026-06-03 关系线图例 Access 三线型修正：按用户纠正和原始 draw.io 图例复核，恢复 `Serving / Flow / Access` 三类关系卡片的独立展示，仅在 `访问关系 Access` 卡片内部按原图例上下展示三种访问关系线型：无箭头虚线、双向开口虚线、单向开口虚线；同步将 Access 文案恢复为“三条线都是访问关系...”口径。未改原始 draw.io、ETL、数据库、数据包、`dataClient` 或 ViewModel。
- 2026-06-03 macOS / Windows C/S 客户端交付预研：新增 `docs/09-delivery/cs-client-delivery-presearch-macos-windows.md`，在不替代当前 ZIP alpha 主线的前提下，给出后续 `Tauri Client + SAPD-Wiki-Backend sidecar + base/user 双库 + 127.0.0.1 本地 API` 的 P1 客户端路线；同步在 `findings.md` 增加长期决策入口。未改前端、ETL、数据库、数据包、`dataClient` 或 ViewModel。
- 2026-06-03 安全流程图例说明口径修正：按用户确认将 ArchiMate / SAPD 图例中 `安全流程` 的定义文案改为“参考 PCF 流程分类框架，分为 L1-L5：L1 安全流程类别；L2 安全职能流程组；L3 安全职能流程；L4 流程活动；L5 活动任务。”；本轮仅修改前端图例展示文案，未改 ETL、数据库、数据包、`dataClient` 或 ViewModel。
- 2026-06-03 draw.io 第一页整体映射确认与文档去重：按最终确认表将 registry 补充 `sapd_subtype` / `sapd_subtypes` 和 `sapd_relation_subtype`，覆盖 32 个图例元素和 3 个关系线候选；信息化基础元素中“人员”记录为 `person / person_group`，关系线记录为 `Serving / serving_relation`、`Flow / flow_relation`、`Access / access_relation`。`archimate-sapd-element-registry-v1.md` 保留完整映射表，`archimate-sapd-environment-mapping-design.md` 删除重复图例清单，仅保留设计摘要和引用，避免两个文档口径漂移。未改 ETL、数据库、前端展示逻辑、数据包、`dataClient` 或 ViewModel。
- 2026-06-03 SAPD 管理元素角色口径写入建模文档：按用户确认补充“角色不是岗位和职能之外的第三类管理对象，而是 `security_work_function` 的同义业务口径”；明确当前版本中 `岗位 : 角色` 与 `岗位 : 职能` 是同一组 `1:N` 关系，RBAC 授权角色仍作为后续单独建模需求，不纳入当前 ArchiMate 管理元素图例。本轮只改建模文档和进度记录，未改前端、ETL、数据库、导出数据、`dataClient` 或 ViewModel。
- 2026-06-03 SAPD 安全管理元素映射口径补充：在 registry notes 和 ArchiMate / SAPD 建模文档中补齐已确认映射总表：`security_org_unit -> Business Actor`、`security_work_position -> Business Actor`、`security_person -> Business Actor`、`security_work_function -> Business Role`、`security_process_* -> Business Process`；验证 registry JSON 合法，四个显式类型和 5 个流程族条目均为 `active` 且映射正确。未改 ETL、数据库、前端展示逻辑、数据包、`dataClient` 或 ViewModel。
- 2026-06-03 SAPD 安全管理元素岗位 / 职能口径确认：将 `security_work_position -> Business Actor`、`security_work_function -> Business Role` 固定为当前版本建模基线；`安全工作岗位` 状态从 `review_required` 改为 `active`，`security_work_function` 显示名改为 `安全工作职能 / 角色`；文档补充 `岗位（Business Actor） assigned to 职能 / 角色（Business Role） assigned to 流程 / 活动 / 任务（Business Process）`，并明确 `security_authorization_role` 属于后续 RBAC，不纳入当前 ArchiMate 管理元素。同步更新前端元素图例文案和 `app.js` 缓存版本。未改 ETL、数据库、数据包、`dataClient` 或 ViewModel。
- 2026-06-03 ArchiMate 图例右上角 notation 官方化修正：确认前端此前只是受控 registry 渲染，仍存在自由 SVG 小标记；使用 draw.io 应用导出 `data/raw-samples/drawio sample.drawio` 第 1 页 SVG 作为参考，按导出的官方 ArchiMate 右上角路径比例替换 `app.js` 中 `renderArchimateCornerIcon()` 的自定义 glyph，覆盖 Location、Node、Device、System Software、Communication Network、Facility、Component / Artifact、Function、Service、Data Object、Technology Event、Business Role、Business Process、Grouping 等图例标记；同步更新 `index.html` 的 `app.js` 缓存版本。未改 ETL、数据库、数据包、`dataClient`、ViewModel 或建模 registry。
- 2026-06-02 ArchiMate / SAPD 建模 registry V1：新增 `config/archimate/archimate-sapd-notation-registry.v1.json` 和 `docs/02-data-model/archimate-sapd-element-registry-v1.md`，基于 draw.io 第一页 `图例` 建立 `sapd_element_type_id -> archimate_element_type_id -> sapd_archimate_notation -> source_cell_id` 清单；当前覆盖 18 个 ArchiMate 基准元素、32 个 SAPD 元素类型、32 条 notation、3 条关系线候选，`metamodel_edges` 暂为空等待第二页更新；同步在总体设计文档中登记 registry 位置。未改 ETL、数据库、前端、数据包、`dataClient` 或 ViewModel。
- 2026-06-02 页面刷新保留当前页面全局基线修复：先单独处理 route / refresh 问题；`app.js` 将路由同步改为 canonical base，固定根入口使用 `/#/route`，避免从 pretty path 进入时拼出重复路径；保留 hash route 优先级，直接输入 `/` 仍默认首页；新增 `scripts/audit_frontend_route_refresh_contract.mjs` 专项审计脚本，覆盖 hash route 优先、根路径不恢复旧 route、canonical base、页内 `activeModelingLanguageTab` 持久化 / 恢复。未改 ETL、数据库、数据包、`dataClient` 或 ViewModel。
- 2026-06-02 ArchiMate notation 受控渲染基线落地：采纳“前端不自由绘制 draw.io / ArchiMate 图标”的建议，在 `app.js` 增加 `ARCHIMATE_NOTATION_REGISTRY`，图例条目只允许通过登记的 ArchiMate / draw.io notation 渲染；未知 notation 进入 `待映射` 状态；元素图例统一为桌面 4 列；`activeModelingLanguageTab` 纳入本地状态恢复，刷新 `元素图例` tab 不再回到默认 tab；同步更新前端全局 route / refresh 基线、图谱展示原则和 ArchiMate / SAPD 建模设计文档。未改 ETL、数据库、数据包、`dataClient` 或 ViewModel。
- 2026-06-02 安全架构建模语言元素图例三次修正：按用户最新截图反馈，元素卡片右侧不再重复显示元素名称和英文名称，仅保留定义正文；删除 `定义` 标签；图标整体等比缩小，图标内部中英文标题按统一基线和最大宽度约束缩排，避免长中文出框；右上角 ArchiMate 标记继续依据原始 draw.io 第一页 `appType` 类型绘制，流程仍只保留一个 `安全流程`，定义正文说明 L1-L5 五级。未改 ETL、数据库、数据包、`dataClient` 或 ViewModel。
- 2026-06-02 矩阵滚动容器契约纳入全局设计基线：在 `frontend-global-design-baseline-2026-05-30.md` 和 `frontend-display-design-principles-2026-05-30.md` 中补充长矩阵 / 长表格滚动规则，明确禁止用 `100vh` / `100dvh` 减固定值或固定 `min-height` 推算矩阵高度；要求工作台、pane、内容区、tab panel、表体逐层传递 `min-height: 0` 和 `minmax(0, 1fr)`，并让表体作为主滚动边界。
- 2026-06-02 安全架构建模语言元素图例二次修正：按用户截图反馈，删除 `元素图例` tab 顶部来源说明块；图标改为内联 SVG，加入中文大标题和英文小标题，并按原始 draw.io 图例形态修正人员 stick figure、地点右上定位针、各 ArchiMate 元素右上角类型标记；安全管理元素中流程只显示一个 `安全流程` 图例，定义文本集中说明 L1-L5 五级流程。未改 ETL、数据库、数据包、`dataClient` 或 ViewModel。
- 2026-06-02 安全架构建模语言元素图例落地：按用户确认的样例，将第二个 tab 改为 `元素图例`；页面使用 HTML/CSS 渲染信息化基础元素、SAPD 安全元素、安全管理元素和关系线图例，每个元素展示按 draw.io 原始宽高等比缩放的图标、元素名称、ArchiMate / draw.io 基准和定义文本；图标比例采用第 1 页 `图例` 中识别到的 `26.5×50`、`150×75`、`150×82.947...` 等原始 `mxCell` 比例；未改 ETL、数据库、数据包、`dataClient` 或 ViewModel。
- 2026-06-02 安全能力映射矩阵滚动契约二次补强：撤掉矩阵区 `100dvh - ...` 估算高度和历史固定 `min-height` 影响，改为从 `workspace-stage -> capability-relation-pane -> #detail -> capability-map-preview-r2 -> semantic-scroll` 逐层传递 `minmax(0, 1fr)` 剩余高度；不同浏览器尺寸下滚动条应跟随右侧详情区可用高度自适应。未改数据、ETL、ViewModel、投影接口或导出 JSON。
- 2026-06-02 安全能力映射 Tab 状态与矩阵滚动契约修复：关系区页签切换后保留当前 Tab，避免后台重渲染把页面重置回图谱造成“每次切换都加载”的体感；技术视角、管理视角、标准 / 框架映射统一矩阵高度链、表体滚动和 sticky 表头；已登记 `OI-129` 并归档为已修复。未改 ETL、数据库、投影接口、导出 JSON 或业务字段。
- 2026-06-02 知识库字典页签横线清理：去掉 `安全工作清单 / 安全职能流程清单` 所在知识库字典页签头下方横线，仅调整 `maintenance-shell-head` 在知识库字典模式下的局部样式和 CSS 缓存版本；未改页面数据、组件逻辑或业务字段。
- 2026-06-02 安全架构建模语言二级页收口：参考现有双 tab 页签模式，将 `/guides/security-architecture-modeling-language` 从预留 / 内容包空态改为独立单工作区域页面；页面包含 `语言概览`、`元素与关系` 两个页签，使用统一 segmented tab 风格和表格化业务说明；未改 `dataClient`、ViewModel、ETL、数据库或导出 JSON。
- 2026-06-02 安全技术服务清列表格宽度优化：按全局关系胶囊基准保留 `安全技术模块 |`、`安全技术措施 |` 类型前缀且不恢复前置点；删除主表 `定义` 列，将释放宽度主要分配给 `关联安全技术模块/措施` 列，同步调整分组行 `colspan` 和组件 / 样式缓存版本；未改 ViewModel、ETL、导出 JSON 或业务字段。
- 2026-06-02 安全指南二级页面新增：在左侧 `安全指南` 下新增 `安全架构建模语言` 二级页面，路由为 `/guides/security-architecture-modeling-language`；当前按预留页接入，不伪造 PDF / PPT / HTML 内容包，后续可承载 ArchiMate / SAPD 元模型、元素、关系和图例说明。
- 2026-06-02 draw.io 第一页图例文字化设计补充：更新 `docs/02-data-model/archimate-sapd-environment-mapping-design.md`，将第一页 `图例` 中的信息化基础元素、SAPD 安全元素映射、安全管理元素和关系线说明转成文字化表格；明确第一页关系线只作为线型候选，不从第一页生成 `sapd_metamodel_edge`。
- 2026-06-02 draw.io raw sample 第一页识别：只读解析 `data/raw-samples/drawio sample.drawio`，确认第一页 `图例` 是 ArchiMate / SAPD 基础元素图例定义页，不是信息化环境实例图；识别出左侧信息化元素、右侧安全元素、底部关系线和安全管理元素区域。
- 2026-06-02 ArchiMate 3.2 与 SAPD 信息化环境映射建模设计：新增 `docs/02-data-model/archimate-sapd-environment-mapping-design.md`，明确 ArchiMate 基础元素、SAPD 安全元素类型、draw.io 图例 / 自定义图标、元模型图、信息化环境视图和安全环境视图的分层边界；本轮只形成设计文档，不改数据库、ETL、前端、数据包或 ViewModel。
- 2026-06-02 关系胶囊全局显示基准调整：全局设计基线明确关系胶囊默认保留类型前缀，例如 `安全技术服务 |`、`安全技术模块 |`、`安全技术措施 |`；本轮只全局移除胶囊前置黑点 / 色点。安全技术服务清单已回退为原有完整类型前缀胶囊展示，不再使用计数气泡或分组省略前缀方案。
- 2026-06-02 左侧全局目录字号统一修复：撤销 `信息化环境安全能力映射`、`LC-AP`、`LC-DT` 长入口的单独降字号规则，改为继承全局目录字号，同时保留单行保护；同步更新 CSS 缓存版本。
- 2026-06-02 打包清理清单治理：补充 `.gitignore`，忽略新拆出的 `frontend/capability-browser/public/data/maintenance/` 和 `frontend/capability-browser/public/data/source-evidence/` 生成目录；新增 `docs/09-delivery/packaging-cleanup-checklist.md`，固化可清理 / 必须保留 / 不进 GitHub / Windows 与 macOS 每次打包步骤。
- 2026-06-02 `OI-127` 知识库字典与安全标准 / 框架多分片加载契约治理完成：将 `Frontend Lazy Data Contract Baseline 1.0` 写入前端全局基线；`app.js` 增加知识库字典页面 `required` / `supplemental` 加载契约和补充包重渲染逻辑；标准 / 框架页新增 `activeStandardTableId` 和统一 table loader，`StandardFrameworkTable.js` 不再直接调用 `dataClient`；新增 `scripts/audit_frontend_lazy_load_contract.mjs` 并登记到治理入口。
- 2026-06-02 安全职能清单与参考页反向映射分片修复：安全职能清单首屏仍只等待 `work-functions`，后台补 `references`、`processes`；GB/T / Gartner 参考页首屏等待 `references`，后台补 `work-functions`；分片合并已按所属字段保护，非当前分片的空数组不再覆盖已加载业务分片。
- 2026-06-02 根路径默认首页修复：修正 `app.js` 初始化逻辑，直接输入 `127.0.0.1:5173` / 根路径 `/` 时不再恢复上次保存的非首页路由，固定进入首页；同步更新 `app.js` 缓存版本。
- 2026-06-01 `5173` 安全职能清单卡住修复：定位 `127.0.0.1:5173/#/knowledge/functions` 空白 / `RESULT_CODE_HUNG` 与知识库字典首屏加载过重有关；改为先加载 `maintenance-index` 和当前页主分片 `work-functions`，不再让安全职能清单首屏等待额外 `references` / `processes` 分片；更新 `app.js` 缓存版本并重启固定 `5173` 服务，普通 Chrome 新标签页和原始地址均已渲染出安全职能清单表格。
- 2026-06-01 `5173` 页面卡住诊断与恢复：检查固定预览服务，确认旧项目服务进程 `74224` 存在但用户侧页面刷新仍卡住；执行 `python3 scripts/dev_server_guard.py --restart` 重启固定端口，停止旧进程并启动新项目服务进程 `2583`。本轮未修改前端源码、数据包、ETL、数据库或业务字段。
- 2026-06-01 数据库备份全局保留规则落地：按用户确认将备份保留策略改为“只保留最新 5 个 `.sqlite3`，出现新备份后删除时间戳最早的旧备份”；新增 `scripts/prune_database_backups.py` dry-run / `--apply` 脚本，并写入 `docs/07-governance/data-governance.md` 与 `scripts/README.md`；当前实际备份已清理到 3 个。
- 2026-06-01 `OI-124` 临时流程引用占位清理：按用户纠正，业务上不存在只有 L2 流程组而无 L3 流程参考的流程映射；修正 `capability-tree` 导出逻辑，不再将 `capability -> process_group` 关系补成空 `process_reference`；`capability-workbench` 构建时跳过空流程引用，避免生成 `process_reference:* / 待确认流程`。
- 2026-06-01 `OI-124` `ALL / 全部作用域` 字典口径修复：按用户确认将 `ALL` 作为“适用于所有作用域”的权威虚拟作用域保留；修正 parser 中 `ALL` 作用域来源证据，改为指向实际 `ALL&...` 服务单元格；维护导出中将 `ALL` 写入字典分片但标记 `display_in_scope_catalog=false`，作用域清单 ViewModel 过滤该 authority-only 项。

## 最近验证

- 2026-06-03 生命周期表格衍生说明文字清理验证：`node --check frontend/capability-browser/components/ApplicationSecurityLifecycle.js`、`git diff --check -- frontend/capability-browser/components/ApplicationSecurityLifecycle.js frontend/capability-browser/index.html progress.md` 通过；`rg` 确认生命周期组件中已无表头解释性 `来自 / 复用 / 字段流向` 文案；固定 `5173` 服务状态通过；`dev-lifecycle` 与 `/data-security` 轻量 smoke 通过，未启动系统 Chrome。
- 2026-06-03 关系线图例 Access 三线型修正验证：只读复核原始 draw.io 第 1 页关系线 source cell，确认 Access 对应 `FsAD_74Q9mUPb6IP9m1h-38/-39/-40` 三条线；`node --check frontend/capability-browser/app.js`、`git diff --check -- frontend/capability-browser/app.js frontend/capability-browser/styles.css` 通过；固定 `5173` 服务状态通过；`/guides/security-architecture-modeling-language` 轻量 smoke 通过。实际系统 Chrome smoke 因 `Chrome DevTools target not available` 未完成，未作为页面失败判定。
- 2026-06-03 macOS / Windows C/S 客户端交付预研验证：官方资料只读校准 Apple Developer ID / notarization、Tauri sidecar / Windows installer / macOS signing / updater、Microsoft SmartScreen 和 Windows 签名 / 分发路径；`git diff --check -- docs/09-delivery/cs-client-delivery-presearch-macos-windows.md findings.md` 通过；`wc -l` 确认根目录 `progress.md` 仍在轻量范围内。
- 2026-06-03 安全流程图例说明口径修正验证：`node --check frontend/capability-browser/app.js`、`git diff --check -- frontend/capability-browser/app.js` 通过；`rg` 确认新文案已命中且旧“流程是分级的”文案不再出现在 `app.js`。
- 2026-06-02 ArchiMate / SAPD 建模 registry V1 验证：使用只读 XML 解析确认 draw.io 第一页图例来源 cell；`node` JSON 解析和 registry 结构断言通过，结果为 `archimate=18 / sapd=32 / notation=32 / relationCandidates=3 / metamodelEdges=0 / reviewRequired=1`；`git diff --check -- config/archimate/archimate-sapd-notation-registry.v1.json docs/02-data-model/archimate-sapd-element-registry-v1.md docs/02-data-model/archimate-sapd-environment-mapping-design.md progress.md` 通过；`python3 scripts/check_github_data_boundary.py` 通过。
- 2026-06-02 页面刷新保留当前页面全局基线验证：`node --check frontend/capability-browser/app.js`、`node --check scripts/audit_frontend_route_refresh_contract.mjs`、`node scripts/audit_frontend_route_refresh_contract.mjs`、`git diff --check -- frontend/capability-browser/app.js frontend/capability-browser/index.html scripts/audit_frontend_route_refresh_contract.mjs` 通过；固定 `5173` 服务状态通过；`/` 首页、`/guides/security-architecture-modeling-language`、`/knowledge/functions` 轻量 smoke 通过，未启动系统 Chrome；`python3 scripts/check_github_data_boundary.py` 和 `node scripts/audit_frontend_lazy_load_contract.mjs` 通过。
- 2026-06-02 ArchiMate notation 受控渲染基线验证：`node --check frontend/capability-browser/app.js` 通过；registry 命中断言返回 `registry=17 / legendIconTypes=17 / result=pass`；`git diff --check` 覆盖前端、治理文档和进度记录通过；固定 `5173` 服务状态通过；`/guides/security-architecture-modeling-language` 与 `/` 首页轻量 smoke 通过，未启动系统 Chrome；页内 tab 状态恢复静态断言通过；`python3 scripts/check_github_data_boundary.py` 和 `node scripts/audit_frontend_lazy_load_contract.mjs` 通过。
- 2026-06-02 安全架构建模语言元素图例三次修正验证：`node --check frontend/capability-browser/app.js`、`git diff --check -- frontend/capability-browser/app.js frontend/capability-browser/styles.css frontend/capability-browser/index.html`、固定 `5173` 服务状态检查、`/guides/security-architecture-modeling-language` 和 `/guides` 轻量 smoke、`python3 scripts/check_github_data_boundary.py` 通过；轻量 smoke 未启动系统 Chrome，应用内 Browser 工具本轮未暴露可用打开 / 截图接口。
- 2026-06-02 矩阵滚动容器契约纳入全局设计基线验证：`git diff --check -- docs/06-implementation/frontend-global-design-baseline-2026-05-30.md docs/06-implementation/frontend-display-design-principles-2026-05-30.md progress.md` 通过；本轮只改治理文档和进度记录，未触发前端运行验证。
- 2026-06-02 安全架构建模语言元素图例二次修正验证：`node --check frontend/capability-browser/app.js`、`git diff --check -- frontend/capability-browser/app.js frontend/capability-browser/styles.css frontend/capability-browser/index.html` 通过；固定 `5173` 服务状态通过；`/guides/security-architecture-modeling-language` 和 `/guides` 轻量 smoke 通过；`python3 scripts/check_github_data_boundary.py` 通过，未启动系统 Chrome。
- 2026-06-02 安全架构建模语言元素图例验证：`node --check frontend/capability-browser/app.js`、`git diff --check -- frontend/capability-browser/app.js frontend/capability-browser/styles.css frontend/capability-browser/index.html` 通过；固定 `5173` 服务状态通过；`/guides/security-architecture-modeling-language` 和 `/guides` 轻量 smoke 通过；`python3 scripts/check_github_data_boundary.py` 通过，未启动系统 Chrome。
- 2026-06-02 安全能力映射矩阵滚动契约二次补强验证：`node --check frontend/capability-browser/app.js`、`git diff --check -- frontend/capability-browser/styles.css frontend/capability-browser/index.html`、固定 `5173` 服务状态检查、`node scripts/frontend_smoke_check.mjs --page capability --url http://127.0.0.1:5173/frontend/capability-browser/` 通过；轻量 smoke 未启动系统 Chrome，浏览器实际尺寸下的滚动显示仍需用户刷新确认。
- 2026-06-02 安全能力映射 Tab 状态与矩阵滚动契约验证：`node --check frontend/capability-browser/components/CapabilityLocalRelationMap.js`、`node --check frontend/capability-browser/app.js`、`node scripts/govern_open_issues.mjs`、`git diff --check -- frontend/capability-browser/components/CapabilityLocalRelationMap.js frontend/capability-browser/app.js frontend/capability-browser/styles.css frontend/capability-browser/index.html progress.md docs/06-implementation/open-issues.md docs/06-implementation/open-issues-index.md docs/05-archive/open-issues-history/2026-06.md`、固定 `5173` 服务状态检查、`node scripts/frontend_smoke_check.mjs --page capability --url http://127.0.0.1:5173/frontend/capability-browser/` 均通过；轻量 smoke 未启动系统 Chrome。
- 2026-06-02 知识库字典页签横线清理验证：`git diff --check -- frontend/capability-browser/styles.css frontend/capability-browser/index.html progress.md` 通过；固定 `5173` 服务状态通过；`/knowledge/functions` 轻量 smoke 通过，未启动系统 Chrome。
- 2026-06-02 安全架构建模语言二级页验证：`node --check frontend/capability-browser/app.js frontend/capability-browser/components/AppShell.js scripts/frontend_smoke_check.mjs`、`git diff --check -- frontend/capability-browser/app.js frontend/capability-browser/styles.css frontend/capability-browser/index.html frontend/capability-browser/components/AppShell.js scripts/frontend_smoke_check.mjs` 通过；固定 `5173` 服务状态通过；`/guides/security-architecture-modeling-language` 和 `/guides` 轻量 smoke 通过；`python3 scripts/check_github_data_boundary.py` 通过，未启动系统 Chrome。
- 2026-06-02 安全指南二级页面新增验证：`node --check frontend/capability-browser/components/AppShell.js scripts/frontend_smoke_check.mjs`、`git diff --check -- frontend/capability-browser/components/AppShell.js frontend/capability-browser/index.html scripts/frontend_smoke_check.mjs` 通过；固定 `5173` 服务状态通过；`/guides/security-architecture-modeling-language` 和 `/guides` 轻量 smoke 通过；`python3 scripts/check_github_data_boundary.py` 通过，未启动系统 Chrome。
- 2026-06-02 draw.io 第一页图例文字化设计补充验证：只读解析第一页 `图例` 的节点坐标、标签、`appType`、`fillColor` 和 `strokeColor`，未读取或识别第二页 `元模型`；`git diff --check -- docs/02-data-model/archimate-sapd-environment-mapping-design.md progress.md` 通过。
- 2026-06-02 draw.io raw sample 第一页识别验证：执行 `file`、`sed`、`rg` 和只读 XML 解析脚本，确认文件包含 3 页：`图例`、`元模型`、`信息化环境及对象底图`；第一页可直接解析 `mxCell`，有效文字节点 92 个；本轮未改 draw.io 文件、ETL、数据库、前端或数据包。
- 2026-06-02 ArchiMate / SAPD 信息化环境映射设计文档验证：只读核对 `CURRENT_STATE.md`、`progress.md`、`docs/04-user-guide/environment-workbench-json-spec-v1.md`、`frontend/design-handoff/implementation-specs/environment-security-capability-graph-strategy-2026-05-20.md` 和 `docs/02-data-model/data-model.md` 相关段落；确认本轮未改数据库、ETL、前端、数据包、`dataClient` 或 ViewModel，未触发前端 smoke。
- 2026-06-02 `OI-127` 懒加载契约治理验证：`node --check frontend/capability-browser/app.js frontend/capability-browser/viewModels.js frontend/capability-browser/components/StandardFrameworkTable.js scripts/audit_frontend_lazy_load_contract.mjs` 通过；`node scripts/audit_frontend_lazy_load_contract.mjs` 返回 `result=pass`、`standardFrameworks=7`、`standardTabs=6`、`issues=[]`；`node scripts/govern_open_issues.mjs` 通过，当前 `active=2`、`archived=128`；固定 `5173` 状态检查通过；`/knowledge/functions`、`/knowledge/technical-services`、`/standards/nist-csf-2`、`/standards/dsp-level-2`、`/standards/crf`、`/standards/nist-800-53-rev5` 轻量 smoke 通过；二次 review 确认知识库字典和安全标准 / 框架均由显式加载契约、统一 loader、组件取数边界和专项审计覆盖，`git diff --cached --check` 与 `python3 scripts/check_github_data_boundary.py` 通过，未启动系统 Chrome。
- 2026-06-02 左侧全局目录字号统一验证：`git diff --check -- frontend/capability-browser/styles.css frontend/capability-browser/index.html` 通过；确认长入口选择器已改为 `font-size: inherit`；固定 `5173` 服务状态通过；`/` 首页轻量 smoke 通过，未启动系统 Chrome。
- 2026-06-02 安全职能清单与参考页反向映射分片验证：`node --check frontend/capability-browser/app.js frontend/capability-browser/components/StandardRoleReferenceTable.js`、分片顺序合并断言、ViewModel 断言、固定 `5173` 状态检查、`/knowledge/gbt-42446`、`/knowledge/role-references` 轻量 smoke、静态文件缓存版本检查和 `git diff --check` 通过；确认 GB/T `27/27` 行有反向安全职能、Gartner `28/28` 行有候选安全职能。
- 2026-06-02 根路径默认首页验证：`node --check frontend/capability-browser/app.js`、`git diff --check -- frontend/capability-browser/app.js frontend/capability-browser/index.html` 通过；`python3 scripts/dev_server_guard.py --status` 通过；`node scripts/frontend_smoke_check.mjs --page overview --url http://127.0.0.1:5173/` 通过，未启动系统 Chrome。
- 2026-06-01 `5173` 页面卡住恢复验证：`python3 scripts/dev_server_guard.py --status` 通过，固定端口当前只有一个项目服务进程 `2583`；`node scripts/frontend_smoke_check.mjs --page overview`、`node scripts/frontend_smoke_check.mjs --page maintenance --route /knowledge/functions`、`node scripts/frontend_smoke_check.mjs --page maintenance --route /knowledge/technical` 轻量 HTTP/API smoke 均通过；`node --check` 覆盖 `app.js`、`dataClient.js`、`viewModels.js` 和动态加载的关系图组件均通过。临时 headless Chrome runtime 验证因 `Chrome DevTools target not available` 未完成，未作为页面失败判定。
- 2026-06-01 数据库备份全局保留规则验证：手动删除除最新 3 个之外的旧 `.sqlite3` 备份后，`data/database/backups/` 下当前仅剩 `3` 个备份，目录大小约 `1.2GB`；`python3 -m py_compile scripts/prune_database_backups.py` 通过；`python3 scripts/prune_database_backups.py` dry-run 显示 `total_backups=3`、`keep_limit=5`、`would_delete: none`。
- 2026-06-01 `OI-124` 临时流程引用占位验证：SQLite 确认原始关系中存在 32 条 `capability -> process_group` 的 L2 映射关系，来源为 `安全能力-安全管理元素（high level）` 的 L2 流程组列；这些不是 L3 流程参考，不能生成临时 L3。`python3 -m py_compile src/sapd_wiki/exports.py`、`git diff --check -- src/sapd_wiki/exports.py`、`python3 scripts/sapd_wiki.py export-capability-tree`、`python3 scripts/sapd_wiki.py export-capability-workbench` 通过；验证 `capability-tree.json` 和 `capability-workbench.json` 中 `待确认流程=0`、`临时流程引用=0`、`process_reference:3190818408c5=0`、`process_reference:=0`；重跑字典审计仍为 `fail`，但 `issuesByType` 已无 `process_reference`；`/capability-map` 轻量 smoke 通过。
- 2026-06-01 `OI-124` `ALL / 全部作用域` 验证：`python3 -m py_compile src/sapd_wiki/parsers.py src/sapd_wiki/exports.py`、`node --check frontend/capability-browser/viewModels.js`、`git diff --check` 通过；`python3 scripts/sapd_wiki.py bootstrap-local-data --reset` 通过；`maintenance/scopes.json` 中 `scope_types=10` 且 `ALL` 标记 `display_in_scope_catalog=false / authority_only=true`、关联 12 个 `ALL&...` 服务；作用域页面 ViewModel 验证 `sourceScopes=10`、`visibleRows=9`、`hasAll=false`；SQLite 与 sidecar 来源证据确认 `ALL` 指向 `安全能力-安全技术服务!G49:G63` 的实际服务单元格；`/knowledge/scopes` 轻量 smoke 通过；重跑字典审计仍为 `fail`，但 `ALL` 不再作为 unknown scope 报告。
- 2026-06-01 数据库文件层清理 R0/R1 验证：`PRAGMA integrity_check` 清理前返回 `ok`；`data/database/backups/` 根目录保留 `sapd_wiki-before-cleanup-20260601-current.sqlite3`；第一轮整理时 34 个历史备份已移动到归档目录，后续全局保留规则已继续清理旧备份；`data/database/sapd_wiki.db`、`data/database/sapd_wiki.sqlite` 已不存在；`data/exports/worker-verify/` 仍保留且包含 `42` 个文件，未移动。

## 当前问题索引

- `OI-038`：Gartner 与安全职能候选映射需后续人工校对，状态 `待确认`。
- `OI-128`：USER-WRITE-UI-1：收藏 / 备注最小前端入口，状态 `待实现`。


## 历史索引

| 归档文件 | 内容 |
|---|---|
| `docs/05-archive/progress-history/2026-06.md` | 2026-06 完整执行记录、Open Issues 治理、前端治理、数据口径确认和本轮 progress 瘦身前快照 |
| `docs/05-archive/progress-history/2026-05.md` | 2026-05 完整执行记录、根目录 `progress.md` 瘦身快照和结构治理记录 |
| `docs/05-archive/findings-history/2026-05.md` | 2026-05 历史发现 |
| `docs/05-archive/open-issues-history/2026-06.md` | 已关闭 Open Issues 长记录 |
| `docs/05-archive/context-slimming-2026-05-15/` | 2026-05-15 上下文瘦身快照 |


## 维护规则

- 本文件只保留最近状态、最近 5-10 条重要执行和恢复入口；超过 120 行时继续归档到 `docs/05-archive/progress-history/`。
- 详细过程、长命令输出和阶段性历史不写入根目录 `progress.md`。
