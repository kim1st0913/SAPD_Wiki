# progress.md

本文件是当前会话恢复入口，只保留最近状态、最近关键动作、验证摘要和历史索引。完整执行历史已归档到 `docs/05-archive/progress-history/`。

## 当前状态（2026-06-15）

- 当前分支：`main`；工作区有多轮未提交改动，禁止随意 revert。
- 2026-06-15 P0 临时优先级：`OI-140` 知识库字典与安全标准 / 框架基准包被 core-only 导出覆盖的问题已止血，并完成页面恢复、runtime baseline 冻结、SQLite 事实源重建和替换后审计；当前运行基线与 SQLite 导出基线已对齐，关闭前只读巡检通过，`OI-140` 已关闭并归档。不恢复 Environment Mapping 写入线，后续任何原始数据修改、导入、导出或 workbench 重建，都必须先基于 runtime baseline 生成候选包和 normalized diff，不允许全量重导或直接覆盖。
- 固定预览入口：`http://127.0.0.1:5173/`；前端展示和用户验收默认只看该端口。
- 当前主控主线：`OI-142 / Security Technical Service Catalog Visibility` 已修复 `sortOrder=0` 被误判为空导致各分组 `AD-01` 被排到组尾的问题；当前安全技术服务清单恢复安全能力 / 关注点业务顺序，`I-DI/I-NT/I-AP/I-OS/I-HD/I-PE` 的 `AD-01` 均为各自分组第 1 行，并已刷新真实系统 Chrome 截图取证。`OI-141 / LC-DT Source Update` 已按用户确认正式替换 `lifecycle-workbench.json` 并备份，仍不恢复 Environment Mapping 写入线。
- 当前底图基础状态：用户更新后的 `drawio sample-信息化环境及对象底图.drawio.svg` 已同步到正式 Viewer；`semantic nodes=98`、`edges=74`、`bound=92`、`ignored=6`、`candidate=0`、`unbound=0`、`detailReadyNodes=92`、`missingDetailNodes=0`。
- 当前临时核对入口：`/environment-mapping` -> `数据核对（临时）`。该视图允许展示 Excel 行号、merged range、sourceCells 等维护证据；正式主展示区仍不得泄露 `sheet`、`row`、`column`、`raw_value`、`source_file`、`import_id`、`source_id`、`source_ref`、`source_label`、`debug`、`raw`、`metadata`、`intermediate`、`generated_at`。
- 当前浏览器验证治理：默认不启动系统 Chrome；如需临时打开系统 Chrome 做运行时 DOM / computed style 验收，必须使用独立测试用户数据目录，测试完成后关闭对应窗口 / 进程。
- 当前禁止事项：不随意修改 SVG / Draw.io、数据库 schema、正式页面视觉和业务绑定规则；不 `git add .`；不静默覆盖用户文件。

## 最近完成事项

- 2026-06-18 `OI-139 环境映射 D类修复后正式运行包替换`：按“基于 runtime baseline 先生成 candidate + normalized diff + 备份清单，再正式替换”的治理流程，基于最新 `data/raw-samples/wiki sample.xlsx` 重新执行源表归一化、模块清单一致性审计、candidate 构建、downstream 验证、replacement bundle 构建和临时 swap HTTP 回归。正式替换前验证通过后，已备份并替换 `frontend/capability-browser/public/data/environment-workbench.json` 与 `frontend/capability-browser/generated/environmentBasemap.node-details.json`，备份与 manifest 位于 `data/exports/worker-verify/environment-reimport-formal-apply/20260618T154506/`；随后重算 `frontend/capability-browser/public/data/review/environment-manual-review-checklist.json`。最新关键结果：`normalizedRowCount=834`、`objectServiceRelations=778`、`serviceModuleRelations=654`、`serviceMeasureRelations=181`、`moduleSystemRelations=211`、`moduleServiceMismatchCount=0`、`systemModuleMismatchCount=0`、`securitySystemCoverageOrMismatchCount=0`、`selectedRowCount=834`、`highRiskContextCount=0`。验证通过源表审计、模块一致性审计、candidate / downstream / replacement bundle 验证、5173 环境页 HTTP smoke、提升权限后的内容级 smoke、字典标准基线完整性、保护基线 no-regression、JSON 包边界、GitHub 数据边界和 `git diff --check`；本轮未修改 SQLite、字典 / 标准包、LC-DT、原始 SVG / Draw.io、数据库 schema 或前端视觉样式。
- 2026-06-17 `OI-144 Standard Search Clause Reveal + Query Persistence`：按用户截图反馈标准 / 框架页仍不高亮、定位不到条款，以及“每次选完搜索值不要直接清空搜索框”。本轮继续只修前端搜索交互，不修改数据包：`frontend/capability-browser/app.js` 新增 `routeHasPageSearch` 和 `searchTextForActivatedResult`，全局搜索结果激活后不再清空顶部搜索词，并将搜索词同步给目标页页面搜索；目标页为字典、标准 / 框架、能力映射、环境映射或指南时，会触发页面内搜索定位高亮。`frontend/capability-browser/components/StandardFrameworkTable.js` 为标准 / 框架分组行、控制行和单元格补充 `data-copy-text` / `data-standard-row-text` 业务锚点；即使无法定位到具体值，也能定位到标准控制行或父条款分组。`index.html` 将 `StandardFrameworkTable.js`、`app.js`、`styles.css` 等缓存版本提升到 `global-search-20260617-9`；`audit_search_state_isolation.mjs` 和 `frontend_content_smoke_check.mjs` 新增全局搜索词保留、目标页搜索继承、标准条款锚点防回归断言。验证通过语法检查、搜索状态隔离审计 `25/25`、内容级 smoke 本地与 5173 URL 模式、5173 开发服务状态、标准页 HTTP smoke、GitHub 数据边界和 scoped `git diff --check`；提升权限读取 5173 HTML 确认实际加载 `global-search-20260617-9`，且无 `global-search-20260617-8` 残留。未启动系统 Chrome，未修改数据包、SQLite、原始 Excel、标准 / 框架数据、LC-DT 运行包或 Environment Mapping 业务数据。
- 2026-06-17 `OI-144 Page Search Target Reveal & Highlight`：按用户反馈“所有页面的搜索都要支持定位高亮”，将修复范围从全局搜索结果点击扩展到页面内搜索框。本轮不修改任何数据包和业务排序，只修前端交互协议：`frontend/capability-browser/app.js` 新增 `pendingPageSearchReveal`、`queuePageSearchReveal`、`pageSearchTextTargetElement`、`revealPageSearchTarget` 和 `flushPageSearchReveal`，能力映射搜索、能力关系字段过滤、环境映射搜索、环境核对表过滤、知识库 / 标准页搜索、LC-AP 阶段搜索和 LC-DT 阶段搜索在渲染完成后，会在当前 active workspace 内定位第一个实际命中的业务节点，并滚动到该节点。`styles.css` 新增 `page-search-target-highlight` 并复用全局搜索 / 批注 active 的黄色底、蓝色定位环和柔和 pulse 高亮基准；`index.html`、`audit_search_state_isolation.mjs` 和 `frontend_content_smoke_check.mjs` 缓存版本提升到 `global-search-20260617-8`。验证通过语法检查、搜索状态隔离审计 `23/23`、内容级 smoke 本地与 5173 URL 模式、5173 开发服务状态、`/development-security` HTTP smoke、GitHub 数据边界和 scoped `git diff --check`；提升权限读取 5173 HTML 确认实际加载 `global-search-20260617-8` 且无 `global-search-20260617-7` 残留。未启动系统 Chrome，未修改数据包、SQLite、原始 Excel、标准 / 框架数据、LC-DT 运行包或 Environment Mapping 业务数据。
- 2026-06-17 `OI-144 Global Search Cross-page Target Resolution`：按用户反馈“检查所有页面，还有很多页面搜索定位只到页面，而不是到实际内容”，继续修复全局搜索结果激活逻辑。本轮不修改任何数据包和业务排序，只修前端定位协议：`frontend/capability-browser/app.js` 为能力、知识库字典、内容、导航、标准 / 框架、环境和生命周期等全局搜索结果统一补齐 `targetText`；`globalSearchTargetElement` 不再在对象 ID 未命中 DOM 时直接放弃，而是先按 ID / route-specific 目标定位，失败后通过 `globalSearchTextTargetElement` 在当前 active workspace 内按 `data-copy-text`、`data-annotation-value`、标题、表格行、chip 和目录行等业务锚点继续定位，且排除全局搜索面板、顶部栏和批注浮层，避免点击搜索结果只跳到页面顶部。`index.html`、`audit_search_state_isolation.mjs` 和 `frontend_content_smoke_check.mjs` 缓存版本提升到 `global-search-20260617-7`，搜索状态隔离审计新增跨页面文本锚点定位断言。验证通过 `node --check`、搜索状态隔离审计 `22/22`、内容级 smoke、5173 开发服务状态、`/development-security` HTTP smoke、GitHub 数据边界和 scoped `git diff --check`；提升权限读取 5173 HTML 确认实际加载 `global-search-20260617-7`。未启动系统 Chrome，未修改数据包、SQLite、原始 Excel、标准 / 框架数据、LC-DT 运行包或 Environment Mapping 业务数据。
- 2026-06-17 `OI-144 Search Highlight Design Baseline Alignment`：按用户和 Product Design 反馈，搜索高亮应尊重整体设计基准并与备注 / 批注高亮保持一致。本轮只修改 `frontend/capability-browser/styles.css` 和缓存版本，不修改搜索逻辑或数据：`lifecycle-search-mark` 与 `global-search-target-highlight` 改为复用批注 active 视觉语言，包括黄色高亮底、蓝色定位环、柔和 pulse 动效和低噪声 Morandi / Apple shell 质感；避免继续使用独立的弱黄色 mark。`index.html` 与搜索审计脚本缓存版本提升到 `global-search-20260617-6`，并扩展 `audit_search_state_isolation.mjs`、`frontend_content_smoke_check.mjs` 断言搜索高亮复用 `annotationGlowSweep / annotationSoftPulse`。验证通过语法检查、搜索状态隔离审计 `21/21`、内容级 smoke、5173 开发服务状态、`/development-security` HTTP smoke、GitHub 数据边界和 scoped `git diff --check`；提升权限读取 5173 HTML 确认实际加载 `global-search-20260617-6`。未启动系统 Chrome，未修改数据包、SQLite、原始 Excel、标准 / 框架数据、LC-DT 运行包或 Environment Mapping 业务数据。
- 2026-06-17 `OI-139 D-class System-Module Candidate Table`：按用户要求生成 D 类可明确修正候选表，产物目录为 `data/exports/worker-verify/environment-d-system-module-candidate/`。本轮不写回原始 Excel、不替换正式数据包、不修改 SQLite 或前端 UI。候选以 `安全技术模块清单` 为基准，只处理 D 类 `systemModuleMismatch`，原则为“仅修正 H 列安全系统，保护原始合并单元格格式”。结果：规范化 D 类问题 `117` 行；格式安全可写回 H 列目标 `17` 个，覆盖规范化问题 `104` 行；合并区域候选冲突 `4` 个，覆盖规范化问题 `12` 行，需要人工确认是否拆分合并区域或保持原值；C/D 交叉暂缓 `1` 个，覆盖规范化问题 `1` 行。输出包括 `environment-d-system-module-candidate.md/json`、`environment-d-system-module-format-safe-write-targets.csv`、`environment-d-system-module-merge-conflict-targets.csv`、`environment-d-system-module-blocked-targets.csv` 和 `environment-d-system-module-normalized-candidates.csv`。
- 2026-06-17 `OI-139 D-class Confirmed Source Writeback`：按用户确认，将 D 类候选中可写回项受控写入原始 `data/raw-samples/wiki sample.xlsx` 的 `作用域-安全技术服务-安全技术模块映射` H 列。新增 `scripts/apply_environment_d_system_module_safe_updates.py`，写回前备份到 `data/exports/worker-verify/environment-d-system-module-candidate/formal-source-apply/20260617T214711/source-excel-backup/wiki sample.before-environment-d-system-module-20260617T214711.xlsx`，初次写回 `17` 个 H 列源单元格且合并区域数保持 `419`。写回后复核发现 `H504`、`H571`、`H716` 虽格式安全但语义不安全：同一 H 列合并区域内其他非问题行会被改成错误安全系统；已立即备份当前文件到 `formal-source-apply/20260617T214937/source-excel-backup/wiki sample.before-rollback-unsafe-d-system-merge-20260617T214937.xlsx` 并回退这 3 个单元格。另修正 `scripts/audit_scope_service_module_mapping.py`，将 canonical 安全系统名 `监测、研判和响应` 作为整体值处理，不再按顿号拆成 `监测` / `研判和响应`。顺序重跑审计后，D 类 `systemModuleMismatchCount` 从 `117` 降至 `25`，`securitySystemCoverageOrMismatchCount` 从 `117` 降至 `25`；源表导入审计仍为 `pendingRelationCount=0`、`invalidRowCount=0`、`duplicateExactServiceChildRelationCount=0`、`scopeCompletenessIssueCount=0`，合并区域数仍为 `419`。本轮未替换正式 `environment-workbench.json`、未重算正式 node-details、未改 SQLite 或前端 UI。

- 2026-06-17 `OI-144 Global Search Highlight & Shortcut Button Fix`：按用户反馈“搜索到了，但是不高亮；`⌘K` 按钮没用”，继续收敛全局搜索体验。本轮不修改任何数据包，仅修前端交互：`frontend/capability-browser/components/ApplicationSecurityLifecycle.js` 新增生命周期搜索命中词高亮，页面内搜索 `Jira` 时会在右侧表格的 `Jira` 值上渲染 `<mark class="lifecycle-search-mark">Jira</mark>`；`frontend/capability-browser/app.js` 的全局搜索结果激活优先按 `targetText` 定位生命周期表格中的命中值，而不是只高亮左侧阶段按钮；`frontend/capability-browser/components/AppShell.js` 和 `index.html` 将 `⌘K` 从装饰性 keycap 改为真实按钮，点击后聚焦全局搜索，已有搜索词时打开 / 刷新结果面板；`styles.css` 新增按钮和命中词高亮样式，缓存版本提升到 `global-search-20260617-5`。验证通过语法检查、搜索状态隔离审计 `21/21`、内容级 smoke、组件渲染级 `Jira` mark 检查、5173 开发服务状态、`/development-security` HTTP smoke、GitHub 数据边界和 scoped `git diff --check`；提升权限读取 5173 HTML 确认实际加载 `global-search-20260617-5` 且包含 `globalSearchActionButton`。未启动系统 Chrome，未修改数据包、SQLite、原始 Excel、标准 / 框架数据、LC-DT 运行包或 Environment Mapping 业务数据。
- 2026-06-17 `OI-144 LC-AP / LC-DT Cell-level Search Fix`：按用户截图复核 `LC-AP安全开发生命周期` 页面中顶部全局搜索和页面内阶段搜索均搜不到 `Jira`，确认根因不是数据缺失，而是搜索链路只查阶段标题 / 编码，未把阶段内的开发技术服务、开发技术模块、安全技术服务、技术模块 / 措施等单元格内容纳入阶段搜索文本。已修改 `frontend/capability-browser/viewModels.js`，为 LC-AP / LC-DT workbench 阶段构建包含关联对象的 `searchText`；修改 `frontend/capability-browser/components/ApplicationSecurityLifecycle.js`，页面内导航过滤使用 `row.searchText`；修改 `frontend/capability-browser/app.js`，LC-AP / LC-DT 本地搜索将独立阶段搜索词传入 ViewModel，全局搜索新增生命周期单元格级服务 / 模块结果，并用所属阶段 ID 定位，避免只跳页面不选中阶段；`index.html` 缓存版本提升到 `global-search-20260617-4`。本地 ViewModel 回归确认 `Jira` 搜索命中 `AP-01 需求分析`，且选中阶段开发技术模块包含 `Jira`。验证通过语法检查、`audit_search_state_isolation.mjs`、`frontend_content_smoke_check.mjs --skip-api`、5173 开发服务状态、`/development-security` HTTP smoke、GitHub 数据边界和 scoped `git diff --check`；提升权限读取 5173 HTML 确认实际加载 `global-search-20260617-4` 脚本。未启动系统 Chrome，未修改数据包、SQLite、原始 Excel、标准 / 框架数据、LC-DT 运行包或 Environment Mapping 业务数据。
- 2026-06-17 `OI-144 Global Search Implementation`：按用户反馈“全局搜索当前无法使用”，在不修改任何数据包和页面局部搜索逻辑的前提下，实现顶部全局搜索功能。修改 `frontend/capability-browser/app.js`：新增全局搜索本地索引、结果面板、异步加载可检索数据包、标准 / 框架明细延迟加载、结果点击 / Enter 跳转和 Esc 收起；搜索覆盖导航页面、能力 / 关注点、知识库字典、标准 / 框架与控制项、LC-AP / LC-DT、指南内容和已加载环境对象。后续按用户截图修正结果预览不可见问题：全局搜索面板改为挂载到 `body` 的 fixed 顶层浮层，避免被 `topbar`、页面标题区或表格容器裁剪 / 遮挡。按用户反馈“点击预览栏任意项需要定位到值所在位置”，新增跳转后目标定位：能力树、知识库 / 标准表格、LC-AP / LC-DT、环境对象和指南目录命中后会等待渲染、展开隐藏分组、滚动到目标行并短暂高亮；缓存版本提升到 `global-search-20260617-3`。修改 `frontend/capability-browser/components/AppShell.js`：顶部搜索容器改为普通容器，避免结果按钮嵌套在 label 中；修改 `frontend/capability-browser/styles.css`：新增紧凑型全局搜索结果面板和目标行高亮样式；扩展 `scripts/audit_search_state_isolation.mjs` 与 `scripts/frontend_content_smoke_check.mjs`，断言全局搜索面板、索引、结果激活、目标定位和缓存版本。未修改数据包、SQLite、原始 Excel、标准 / 框架数据内容、Environment Mapping 业务数据或生命周期运行包。
- 2026-06-17 `OI-144 Search Logic Design & Full Search Box Isolation`：按用户要求将搜索状态隔离从顶部全局搜索、能力映射、知识库 / 标准页和环境映射扩展到当前所有搜索 / 过滤入口。已新增 `docs/06-implementation/search-logic-design.md`，明确顶部 `searchInput` 是跨知识域全局入口，不直接过滤当前页面；页面内搜索只保留在能力映射、环境映射、知识库字典、标准 / 框架、LC-AP / LC-DT、环境核对表和能力关系表等数据密集页面；总览、指南和占位页不设置页面内搜索。已统一 `frontend/capability-browser/index.html` 与 `frontend/capability-browser/components/AppShell.js` 顶部搜索占位文案为全局搜索口径，并扩展 `scripts/audit_search_state_isolation.mjs`：新增生命周期阶段搜索、能力关系字段过滤、环境核对表过滤和搜索框状态归属清单检查，防止后续新增搜索框再次串线。未修改数据包、SQLite、原始 Excel、标准 / 框架数据或 Environment Mapping 业务数据。
- 2026-06-17 `OI-144 Global/Page Search State Isolation`：按用户截图新增问题“全局搜索与页面内搜索状态串线”。根因是顶部全局搜索 `searchInput`、能力映射页 `capabilitySearchInput`、知识库 / 标准页 `sourceSearchInput` 和环境页 `environmentSearchInput` 共用 `state.search`，并在输入事件中互相写入 DOM 值，导致任意搜索框输入会污染其他页面搜索。已修改 `frontend/capability-browser/app.js`：新增 `globalSearch` 与按页面 scope 存储的 `pageSearches`，顶部全局搜索只更新自身状态，页面内搜索只更新当前页面 scope；路由 / 页内二级页面切换时恢复对应页面自己的搜索。已修改 `frontend/capability-browser/viewModels.js`：安全能力清单在搜索无结果时显示“未找到匹配的安全能力，请调整搜索条件。”，不再提示 `capability-tree.json` 缺失。已更新 `frontend/capability-browser/index.html` 缓存版本，并新增 `scripts/audit_search_state_isolation.mjs` 防回归。验证通过 `node --check`、搜索状态隔离审计、`dev_server_guard.py --status`、能力映射和安全能力清单 HTTP smoke；未启动系统 Chrome，未修改数据包、SQLite、原始 Excel、标准 / 框架数据或 Environment Mapping 业务数据。`OI-144` 当前状态为 `已修复 / 待页面验收`。
- 2026-06-16 `OI-139 Environment Mapping Data Issue Recheck`：按用户要求在 `OI-137` 暂不关闭后继续核查数据问题。本轮只重跑审计，不替换正式数据包、不修改原始 Excel、不改 SQLite、不改前端 UI。`audit_scope_service_module_mapping.py --max-differences 20` 通过，当前源表 `作用域-安全技术服务-安全技术模块映射` 为 `sheetMaxRow=1330`、`normalizedRowCount=776`、`objectScopeRelationCount=157`、`objectServiceRelationCount=742`、`serviceModuleRelationCount=646`、`serviceMeasureRelationCount=173`、`moduleSystemRelationCount=216`，`pendingRelationCount=0`、`invalidRowCount=0`、`duplicateExactServiceChildRelationCount=0`、`duplicateServicesInObjectContextCount=0`、`scopeCompletenessIssueCount=0`。`audit_environment_module_catalog_consistency.py` 通过，剩余跨表目录一致性问题为 `moduleServiceMismatchCount=76`、`systemModuleMismatchCount=117`、`serviceModuleCoverageGapCandidateCount=558`、`moduleServiceCoverageGapCandidateCount=79`、`moduleMeasureClassificationIssueCount=0`、`possibleAliasMatchCount=0`，Top 人工核对项 `34` 个；下一步优先从 C 类模块-服务目录不一致和 D 类系统-模块目录不一致开始逐项确认，E/F coverage gap 先按选择性引用候选处理。
- 2026-06-16 `Protected Dictionary & Standards Global Integrity Fix 1.0`：按用户确认进入修复。先修正全局审计误判规则：`I-AP&T-PD.DP-01 应用页面水印`、`I-AP&T-PD.DP-02 应用动态数据脱敏` 是合法安全技术服务，不再被全局替换；旧名称只在 `I-DI&T-PD.DP-01/02` 上判定。随后新增并执行 `scripts/apply_protected_dictionary_standard_global_reference_fixes.mjs`，按优先级修复 `P1` 安全系统 canonical 对齐、`P2` 旧服务编号 / 名称残留、`知情同意管理 / 隐私安全影响评估` 数据安全管理与运营归属。正式替换前已备份 6 个目标文件到 `data/exports/worker-verify/protected-dictionary-standard-global-audit/formal-apply/20260616T071533Z/backups/`，候选文件与 normalized diff 位于同目录 `candidate-files/` 和 `protected-global-reference-fix-normalized-diff.json`。本轮修改当前运行文件 `maintenance/modules.json`、`capability-tree.json`、`environment-workbench.json`、`lifecycle-knowledge.json`、`maintenance-knowledge.json`、`generated/environmentBasemap.node-details.json`，并更新保护审计脚本以接受本轮 apply 报告中的受控 hash 变化；未修改 SQLite、原始 Excel、标准包、数据库 schema、前端 UI、SVG / Draw.io。修复后全局审计 `status=pass`、`runtimeIssueCount=0`、`P0/P1/P2/P3/P4=0`；安全技术服务专项引用审计 `issueCount=0`。验证通过 `py_compile` / `node --check`、全局审计、字典标准基线完整性、保护基线回归、JSON 包边界、标准映射 canonical 审计、安全技术服务引用专项审计、内容级 smoke、GitHub 数据边界和 `git diff --check`；未启动系统 Chrome。
- 2026-06-16 `Protected Dictionary & Standards Global Integrity Audit 1.0`：按用户要求对知识库字典、安全标准 / 框架和当前正式运行包引用做只读全局完整性审计。本轮新增 `scripts/audit_protected_dictionary_standard_global_integrity.py` 与 `scripts/audit_protected_dictionary_standard_global_references.mjs`，输出目录为 `data/exports/worker-verify/protected-dictionary-standard-global-audit/`，包含 `global-baseline-integrity-audit.*`、`global-reference-integrity-audit.*`、`global-standards-framework-audit.*`、`global-runtime-package-reference-audit.*`、`candidate-fix-plan.*` 和 `summary-for-user-confirmation.*`。审计覆盖 `19` 类字典、`7` 个标准 / 框架、`4893` 条标准控制项和 `29` 个当前运行包 / 生成包；结果为 `P0=0`、`P1=2`、`P2=351`、`P3=0`、`P4=0`，标准控制引用问题为 `0`，主要待确认问题集中在旧安全技术服务名称 / 编号残留、`移动终端安全` 安全系统未命中 canonical 字典，以及 `隐私安全影响评估` 数据安全管理与运营归属守卫。历史 `worker-verify` 备份不纳入当前问题清单，避免旧快照噪声。本轮未修改正式 JSON、SQLite、原始 Excel、标准包、Environment Mapping 结构、前端 UI、SVG / Draw.io 或 schema。验证通过新脚本语法检查、Node 入口复核、字典标准基线完整性、保护基线回归、JSON 包边界、标准映射 canonical 审计、安全技术服务引用专项审计、GitHub 数据边界和 `git diff --check`；未启动系统 Chrome。
- 2026-06-16 `OI-143 review artifact Regenerate + Final Integrity Pass`：按用户要求“备份并生成”，处理最后的 review 派生产物 `frontend/capability-browser/public/data/review/environment-manual-review-checklist.json`。注意：本轮发现 `build_environment_manual_review_checklist.py --help` 与 `audit_environment_module_catalog_consistency.py --help` 没有 help 分支，会直接执行生成 / 审计；随后已显式备份并按正确链路重跑。先备份当前 public review 到 `data/exports/worker-verify/security-technical-service-reference-candidate/formal-apply/20260616T032547Z/review/`，再刷新 `audit_scope_service_module_mapping.py --max-differences 20`、`audit_environment_module_catalog_consistency.py` 和 `build_environment_manual_review_checklist.py`。初次重生成后专项引用审计仍有 `review_artifact=260`，原因是生成脚本会把源表 / consistency rows 中旧服务 raw value 原样带入 review artifact。已最小修正 `scripts/build_environment_manual_review_checklist.py`：输出前按 `maintenance/services.json` 的 `160` 条服务字典 canonical 化安全技术服务字段，包括旧 `I-OS&T-AS.DS-01..06 -> I-AP&T-AS.DS-01..06` 和旧名称 `应用页面水印 / 应用动态数据脱敏`。再次备份当前 review 到 `data/exports/worker-verify/security-technical-service-reference-candidate/formal-apply/20260616T032739Z/review/` 后重生成；public review hash 与 worker-verify 输出 hash 一致，为 `921c386da25fd5d63167976c7c305a3a9626c46255f0ff20a4c6bfbf3a079b3c`。最终安全技术服务引用专项审计 `status=pass`、`issueCount=0`、`formalRuntimeIssueCount=0`、`nonRuntimeIssueCount=0`；`OI-143` 更新为 `已修复 / 待观察`。本轮未修改服务字典、SQLite、原始 Excel、Environment Mapping、标准包、前端 UI、SVG / Draw.io 或 schema。验证通过 `py_compile`、scope 审计、catalog consistency、review 生成、专项引用审计、字典标准基线完整性、保护基线回归、JSON 包边界、内容级 smoke、GitHub 数据边界和 `git diff --check`；未启动系统 Chrome。
- 2026-06-16 `OI-143 shared-lookups.json Formal Apply`：按用户逐文件确认结果，用户已接受第四个文件 `shared-lookups.json` 的候选变更。本轮只替换该 supporting data 文件，review 产物暂不动。替换前已备份到 `data/exports/worker-verify/security-technical-service-reference-candidate/formal-apply/20260616T031710Z/shared-lookups.before-oi143-20260616T031710Z.json`；备份 hash 为 `de3a9f5fa7dd53bd9d720d78f22bb0743cb55492c9066b1d934f6062b362a9e9`，候选 hash 与替换后正式 hash 均为 `5512f00031af12368127d5858567e251c31744ce6e3786c5b880d950b952aeea`，`formalEqualsCandidate=true`。替换后专项引用审计为 `status=warnings`，正式运行包问题为 `0`，supporting data 问题为 `0`，剩余仅 review artifact `environment-manual-review-checklist.json=260`，待后续按生成脚本重生成而不是手工 patch。本轮未修改服务字典、SQLite、原始 Excel、Environment Mapping、标准包、前端 UI、SVG / Draw.io 或 schema。验证通过字典标准基线完整性、保护基线回归、JSON 包边界、内容级 smoke、GitHub 数据边界和 `git diff --check`；未启动系统 Chrome。
- 2026-06-16 `OI-143 maintenance/scopes.json Formal Apply`：按用户逐文件确认结果，用户已接受第三个文件 `maintenance/scopes.json` 的候选变更。本轮只替换该一个 supporting data 文件，`shared-lookups.json` 和 review 产物暂不动。替换前已备份到 `data/exports/worker-verify/security-technical-service-reference-candidate/formal-apply/20260616T031255Z/maintenance/scopes.before-oi143-20260616T031255Z.json`；备份 hash 为 `0dd24958c2168044c6b376d318ef789d36208167e6f07c38d3284dc8c2768d7b`，候选 hash 与替换后正式 hash 均为 `3cfae8f6f67a997d766392c7997f836c8ed433dbc4b890e9731aa38cb788c776`，`formalEqualsCandidate=true`。替换后专项引用审计为 `status=warnings`，正式运行包问题仍为 `0`，supporting data 剩余 `shared-lookups.json=8`，review artifact 为 `environment-manual-review-checklist.json=260`，待后续逐项确认 / 重生成。本轮未修改服务字典、SQLite、原始 Excel、Environment Mapping、标准包、前端 UI、SVG / Draw.io 或 schema。验证通过字典标准基线完整性、保护基线回归、JSON 包边界、内容级 smoke 和 `git diff --check`；未启动系统 Chrome。
- 2026-06-16 `OI-143 lifecycle-workbench.json Formal Apply`：按用户逐文件确认结果，用户已接受第二个文件 `lifecycle-workbench.json` 的候选变更。本轮只替换该一个正式文件，`maintenance/scopes.json`、`shared-lookups.json` 和 review 产物暂不动。替换前已备份到 `data/exports/worker-verify/security-technical-service-reference-candidate/formal-apply/20260616T030941Z/lifecycle-workbench.before-oi143-20260616T030941Z.json`；备份 hash 为 `f69e7afc7dbae2d784769b0c81cf3f39e9b4f6041e3ef6d56d0f3f4e753a0674`，候选 hash 与替换后正式 hash 均为 `5cca7b380d457fcc07a0592db80e592559dfabc4b48a58e9e9f10f0112bae312`，`formalEqualsCandidate=true`。替换后专项引用审计为 `status=warnings`，正式运行包问题已清零 `formalRuntimeIssueCount=0`；剩余 supporting data 为 `maintenance/scopes.json=8`、`shared-lookups.json=8`，review artifact 为 `environment-manual-review-checklist.json=260`，待后续逐项确认 / 重生成。本轮未修改服务字典、SQLite、原始 Excel、Environment Mapping、标准包、前端 UI、SVG / Draw.io 或 schema。验证通过字典标准基线完整性、保护基线回归、JSON 包边界、内容级 smoke 和 `git diff --check`；未启动系统 Chrome。
- 2026-06-16 `OI-143 lifecycle-knowledge.json Formal Apply`：按用户逐文件确认结果，用户已接受第一个文件 `lifecycle-knowledge.json` 的候选变更。本轮只替换该一个正式文件，其他候选文件暂不动。替换前已备份到 `data/exports/worker-verify/security-technical-service-reference-candidate/formal-apply/20260616T023312Z/lifecycle-knowledge.before-oi143-20260616T023312Z.json`；备份 hash 为 `29daa66df47633f89c278d129217a589df220222bdb4cd706cf0289c9eb24ee9`，候选 hash 与替换后正式 hash 均为 `43987c60000ea1d23918642bc57edced99c11386b60786cf35a5bec209508f6f`，`formalEqualsCandidate=true`。替换后专项引用审计按预期仍为 `status=fail`，但正式运行包问题已从 `208` 降至 `8`，剩余仅 `lifecycle-workbench.json=8`；`maintenance/scopes.json=8`、`shared-lookups.json=8` 和 `review/environment-manual-review-checklist.json=260` 待后续逐项确认 / 重生成。本轮未修改服务字典、SQLite、原始 Excel、Environment Mapping、标准包、前端 UI、SVG / Draw.io 或 schema。验证通过字典标准基线完整性、保护基线回归、JSON 包边界、内容级 smoke 和 `git diff --check`；未启动系统 Chrome。
- 2026-06-16 `OI-143 Security Technical Service Reference Candidate Fix 1.0`：按用户和 ChatGPT 建议，进入全工程安全技术服务引用一致性候选修复，但严格收窄边界：只修安全技术服务引用候选，不重导、不改字典、不改环境映射结构、不替换正式运行包。新增 `scripts/build_security_technical_service_reference_candidate.mjs`，以 `maintenance/services.json` 的 `160` 条安全技术服务字典为 canonical baseline，只读取并生成 4 个正式目标文件的 candidate：`lifecycle-knowledge.json`、`lifecycle-workbench.json`、`maintenance/scopes.json`、`shared-lookups.json`。产物目录为 `data/exports/worker-verify/security-technical-service-reference-candidate/`，包含 `candidate-files/`、`candidate-normalized-diff.json/md`、`auto-fix-mapping.json`、`needs-user-confirmation.json`、`candidate-fix-lists.md`、`candidate-apply-scope.json`、`candidate-validation.json` 和 `derived-artifacts-regeneration-plan.md`。结果：`autoFixCount=601`、`needsUserConfirmationCount=0`、`candidateValidationStatus=pass`；按类型为 `service_code_migrated=90`、`service_object_canonicalized=191`、`service_relation_id_canonicalized=92`、`service_string_canonicalized=228`；按文件为 `lifecycle-knowledge.json=346`、`lifecycle-workbench.json=171`、`maintenance/scopes.json=8`、`shared-lookups.json=76`。`review/environment-manual-review-checklist.json` 只标记为正式包替换后重生成，不手工 patch。当前未修改正式 `public/data`、服务字典、SQLite、原始 Excel、Environment Mapping、标准包、前端 UI、SVG / Draw.io 或 schema。验证通过：`node --check scripts/build_security_technical_service_reference_candidate.mjs`、候选生成脚本、`audit_security_technical_service_dictionary_update.py --post-apply`、`audit_lcdt_source_update.py`、字典标准基线完整性、保护基线回归、JSON 包边界、内容级 smoke、GitHub 数据边界和 `git diff --check`。
- 2026-06-15 `Security Technical Service Reference Integrity Audit`：按用户要求在安全技术服务清单更新后执行全工程安全技术服务映射一致性检查。新增只读脚本 `scripts/audit_security_technical_service_reference_integrity.mjs`，以 `frontend/capability-browser/public/data/maintenance/services.json` 的 `160` 条安全技术服务字典为基准，扫描正式运行包、supporting data、generated data 和 review artifact，并输出 `data/exports/worker-verify/security-technical-service-reference-integrity/security-technical-service-reference-integrity.json/md`。结论：安全技术服务字典自身、SQLite、维护清单页已对齐；但正式运行包仍有 `208` 个服务引用一致性问题，主要为 `lifecycle-knowledge.json=200`、`lifecycle-workbench.json=8`，集中在旧 `I-OS&T-AS.DS-01..06 -> I-AP&T-AS.DS-01..06`、生命周期服务对象 id 与字典 id 不一致，以及部分旧名称残留。派生 / review 产物另有 `276` 个问题，主要为 `review/environment-manual-review-checklist.json=260`、`maintenance/scopes.json=8`、`shared-lookups.json=8`。已登记 `OI-143`，当前状态为 `待用户确认 / 候选修复`；本轮只读检查，未修改 SQLite、原始 Excel、正式运行包、维护包、能力包、环境包、标准包、SVG / Draw.io、schema 或前端 UI。验证：`node --check scripts/audit_security_technical_service_reference_integrity.mjs` 通过；专项审计按发现问题预期返回 `status=fail`；`audit_security_technical_service_dictionary_update.py --post-apply`、`audit_lcdt_source_update.py`、`audit_scope_service_module_mapping.py --max-differences 20`、技术服务清单可见性审计、字典标准基线、保护基线、JSON 包边界、提升权限后的 5173 内容 smoke、GitHub 数据边界和 `git diff --check` 均通过。
- 2026-06-15 `Technical Service AD-01 Position Fix`：按用户截图复查发现上一轮真实浏览器取证只证明“能滚到目标行”，没有发现 `I-DI&T-AS.AD-01 数据分库分表` 被排在 `I-DI` 分组底部。根因是 `compareTechnicalServiceRows` 使用 `(Number(sortOrder) || 999999)`，把能力树第一个关注点的合法顺序值 `0` 误判为缺失，导致所有 `T-AS.AD-01` 对应服务落到组尾。已修复 `frontend/capability-browser/viewModels.js`，改用 `Number.isFinite` 保留 `0`；同时移除 `focusCode` 字符串兜底主排序，继续按 `sortOrder -> sourceOrder -> code -> title` 展示。`index.html` 已将 `viewModels.js` 和 `TechnicalServiceMaintenanceTable.js` 缓存版本提升到 `technical-service-order-visibility-20260615-2`。`scripts/frontend_content_smoke_check.mjs` 与 `scripts/audit_technical_service_maintenance_catalog_visibility.mjs` 新增 `expectedPositionInGroup=1` 断言，防止 `AD-01` 再次被排到组尾。刷新后的真实 Chrome 取证截图确认 `I-DI&T-AS.AD-01` 在 `I-DI 数据与信息` 分组第 1 行，`I-NT/I-AP/I-OS/I-HD/I-PE` 的 `AD-01` 也均为各自分组第 1 行；验证通过语法检查、`frontend_content_smoke_check.mjs --skip-api`、5173 专项审计、真实 Chrome 取证、技术服务页 HTTP smoke、提升权限后的内容级 smoke、字典标准基线完整性、保护基线回归、JSON 包边界、GitHub 数据边界和 `git diff --check`。
- 2026-06-15 `Technical Service Real Browser Visibility Proof 1.3`：按用户要求新增真实浏览器取证脚本 `scripts/prove_technical_service_real_browser_visibility.mjs`，使用独立临时 Chrome profile 打开 `http://127.0.0.1:5173/knowledge/technical-services`，完成后关闭对应 Chrome 进程。初次取证只证明 6 个目标服务可搜索、可滚动定位，但未证明 `AD-01` 位于分组首行；用户截图复查后已由上一条 `Technical Service AD-01 Position Fix` 修正并刷新同目录截图。当前有效取证目录仍为 `data/exports/worker-verify/technical-service-real-browser-proof/`，其中 `technical-service-real-browser-proof.json/md` 已更新到 `technical-service-order-visibility-20260615-2`。
- 2026-06-15 `Security Technical Service Catalog Order & Visibility Correction 1.2`：按用户纠正，上一轮“按服务编号排序”的修复方向错误；安全技术服务清单必须严格保持 `安全能力 -> 关注点 -> 安全技术服务` 的业务顺序。本轮已回退编号主排序，当前 `compareTechnicalServiceRows` 为 `sortOrder -> focusCode -> sourceOrder -> code`，其中 `sortOrder/focusCode` 来自 `capability-tree`，`sourceOrder` 来自服务字典稳定录入顺序，`code` 仅作兜底。显示不全的真实风险不是服务字典缺失，也不是应改排序，而是旧本地展开状态或渲染链路可能隐藏行；已将 `TechnicalServiceMaintenanceTable.js` 状态键提升到 `sapd:technical-service-maintenance-table:v4`，忽略 v2/v3 旧折叠状态，保留默认展开全部分组和搜索强制展开。`index.html` 将 `viewModels.js` 和表格组件缓存版本提升到 `technical-service-order-visibility-20260615-1`。升级 `scripts/audit_technical_service_maintenance_catalog_visibility.mjs`，新增报告 `data/exports/worker-verify/technical-service-order-and-visibility-correction.json/md`；报告确认 5173 实际入口 `200`，脚本版本已更新，`defaultCodeOrderPrimary=false`，默认渲染可见行 `160/160`。4 个目标服务均在 `网络安全体系架构管控能力 / 遵循安全设计原则对网络安全架构进行设计和管控` 路径下，分组位置为 `I-NT 24/24`、`I-AP 31/31`、`I-HD 18/18`、`I-OS 36/36`，均 `existsInDom=true`、`defaultVisibleWithoutSearch=true`、`searchByIdWorks=true`、`searchByNameWorks=true`、`issues=[]`。验证通过 `node --check`、提升权限后的 runtime trace、提升权限后的 `frontend_content_smoke_check.mjs --url http://127.0.0.1:5173`、技术服务页 HTTP smoke、字典标准基线完整性、保护基线回归、JSON 包边界、GitHub 数据边界和 `git diff --check`；未修改正式服务字典数据、LC-DT 正式包、Environment Mapping 数据、SQLite、原始 Excel、SVG / Draw.io、schema 或前端样式，未启动系统 Chrome。
- 2026-06-15 `Security Technical Service Catalog LEFT JOIN Coverage 1.3`：按用户要求停止排序 / 展开 / localStorage 方向，重新核查“缺少模块/措施关系是否导致服务行隐藏”。结论：当前构表链路以 `security_technical_services` 为主表，模块/措施、安全系统等只作为关联列；本轮新增强制审计 `missingRenderedServices = dictionaryServices - renderedServiceRows`，结果为 `0`。服务字典总数 `160`，ViewModel rows `160`，最终表格服务行 `160`；无模块/措施关系服务 `41` 条，隐藏数 `0`。专项目标中 `I-AP&T-AS.AD-01 应用架构管控`、`I-HD&T-AS.AD-01 计算与存储分离`、`I-PE&T-AS.AD-01 物理区域分区` 均无模块/措施关系但行保留，关联模块/措施列为 `/`，安全系统列为 `待补充安全系统`；`I-DI&T-AS.AD-01 数据分库分表` 关联 `安全技术模块 | 数据安全存储`、`数据安全防护`；`I-NT&T-AS.AD-01 网络平面及区域划分` 关联 `安全技术措施 | 网络基础架构安全设计`；`I-OS&T-AS.AD-01 主机/终端安全工作区划分` 关联 `终端安全工作区 / 移动终端安全工作区`。已扩展 `scripts/audit_technical_service_maintenance_catalog_visibility.mjs` 和 `scripts/frontend_content_smoke_check.mjs` 输出并断言 dictionary、ViewModel、group rows、rendered rows、DOM、搜索链路全覆盖。验证通过专项 trace、内容 smoke、技术服务页 HTTP smoke、三项保护审计、GitHub 数据边界和 `git diff --check`；未修改正式服务字典、SQLite、原始 Excel、LC-DT、Environment Mapping、标准包、SVG / Draw.io、schema 或样式。
- 2026-06-15 `LC-DT Source Update Formal Runtime Apply 1.2`：按用户确认“用候选文件替换正式 `frontend/capability-browser/public/data/lifecycle-workbench.json`，做好备份”，已先备份再替换。备份目录为 `data/exports/worker-verify/lcdt-source-update/formal-apply/20260615T115526Z/`，备份文件为 `lifecycle-workbench.before-lcdt-preapply-20260615T115526Z.json`；替换报告为 `lifecycle-workbench-formal-apply-report.json/md`。替换前正式 hash / 备份 hash 为 `c0f727ff1f74c83a9f9f48b5af9231b8fb11b76c9313a82ee9647691f73a7a83`，候选 hash 为 `7b6c956b2cd9b43a974653a695e229484551290c1289192d1f58c63498cdd9b3`，正式替换后 hash 为 `f69e7afc7dbae2d784769b0c81cf3f39e9b4f6041e3ef6d56d0f3f4e753a0674`；替换后正式文件只追加 `formalApply` 元信息和已应用 warning，业务内容与候选包语义一致。替换后 `lifecycle-workbench` 关键计数为 `security_technical_services=160`、`security_technology_modules=118`、`security_technical_measures=30`、`relations=542`。验证通过：LC-DT 源表审计仍为 `ready`，`audit_dictionary_standard_baseline_integrity.py`、`audit_protected_baseline_no_regression.py`、`audit_json_package_boundary.py`、`frontend_content_smoke_check.mjs --skip-api`、`dev_server_guard.py --status`、`frontend_smoke_check.mjs --page data-security --route /data-security --url http://127.0.0.1:5173`、`check_github_data_boundary.py` 和 `git diff --check` 均通过；未修改 SQLite、原始 Excel、维护包、能力包、环境包、标准包、SVG / Draw.io 或前端 UI，未启动系统 Chrome。
- 2026-06-15 `LC-DT Source Update Pre-Apply Verification 1.1`：按用户要求在正式 LC-DT 运行包替换前，基于当前 runtime baseline 生成候选包、normalized diff 和备份清单；新增 `scripts/verify_lcdt_pre_apply_candidate.py`，产物目录为 `data/exports/worker-verify/lcdt-source-update/pre-apply-candidate/`。源表变更审计写出 `lcdt-source-excel-change-audit.json/md`，指定备份 `20260615T112121Z` vs 当前源表仅 1 个预期变更、0 个非预期 / 阻断；最早可用备份链 vs 当前源表 19 个预期变更、0 个非预期 / 阻断。`数据加密 / 数据脱敏` 专项误替换检查写出 `lcdt-encryption-desensitization-replacement-audit.json/md`，`matchCount=90`、`riskCounts.ok=90`、`blocking=0`、`review=0`。候选包包含 `candidate-lifecycle-workbench.json`、`candidate-maintenance-knowledge.json`、`candidate-maintenance-index.json`、`candidate-maintenance-services.json`、`candidate-maintenance-measures.json`、`candidate-normalized-diff.*`、`candidate-apply-scope.*`、`candidate-backup-plan.*`、`candidate-readiness.*`；备份清单显示仅 `frontend/capability-browser/public/data/lifecycle-workbench.json` 会变，维护包 4 个文件 hash 全等且不修改。normalized diff 为 `expected_change=384`、`unexpected_change=0`、`blocking_change=0`，LC-DT 直接关系新增 16、移除 74，scope checks 均未影响 LC-AP、能力、环境、标准或维护包；readiness=`ready_for_user_confirmation`，`formalDataReplaced=false`。验证通过 `py_compile`、LC-DT 审计 / 候选生成 / pre-apply verification、字典标准基线完整性、保护基线回归、JSON 包边界、内容级 smoke、GitHub 数据边界和 `git diff --check`；未替换正式运行包，未修改 SQLite、原始 Excel、维护包、能力包、环境包、标准包、SVG / Draw.io 或前端 UI。
- 2026-06-15 `LC-DT Confirmed Source Updates + Technical Service Visibility Check`：按用户确认将 LC-DT 源表问题 1-6 以字典为准写回 `data/raw-samples/wiki sample.xlsx`，并将问题 7 的 `数据流转监测和泄漏防护` 补入 `LC-DT 数据生命周期` 表。写回脚本为 `scripts/apply_lcdt_confirmed_source_updates.py`，每次写入前自动备份，最新备份包括 `data/exports/worker-verify/lcdt-source-update/source-excel-backup/wiki sample.before-lcdt-confirmed-source-updates.20260615T112121Z.xlsx`；已修正 `I-DI&T-AS.AD-03 数据备份 -> I-DI&T-AS.DG-03 数据备份`、`I-DI&T-PD.DP-01 应用页面水印 -> 数据内容水印`、`I-DI&T-PD.DP-02 应用动态数据脱敏 -> 静态数据脱敏`、`数据加密 -> 数据加密和令牌化`、`数据脱敏 -> 数据脱敏(去标识化)`、`零信任访问代理（独立部署，接替/协同应用系统相关技术模块） -> 零信任访问代理`。复跑 `scripts/audit_lcdt_source_update.py` 后状态为 `ready`，`stageCompared=7`、`stagePassed=7`、服务双表差异 `0`、模块 / 措施双表差异 `0`、`serviceReferenceCount=156 matched=156`、模块 / 措施引用 `matched_module_catalog=91`、`matched_existing_measure=15`、`newMeasureCandidateCount=0`、`issueStatusCounts={}`；候选包 `scripts/build_lcdt_update_candidate.py` 已刷新，正式 `lifecycle-workbench.json`、维护包、能力 / 环境包和 SQLite 仍未替换。针对用户指出 `I-AP&T-AS.AD-01 应用架构管控` 搜得到但展开看不到的问题，确认正式数据中该服务存在于 `I-AP 软件应用` 分组且按能力树顺序可见序号为 1；新增 `scripts/audit_technical_service_maintenance_catalog_visibility.mjs` 并通过，前端 `TechnicalServiceMaintenanceTable.js` 的本地表格状态键提升到 `v2`，`index.html` 提升该组件缓存版本以清除旧滚动 / 展开状态。验证通过：`py_compile`、LC-DT 审计 / 候选生成、技术服务可见性审计、`audit_dictionary_standard_baseline_integrity.py`、`audit_protected_baseline_no_regression.py`、`audit_json_package_boundary.py`、`frontend_content_smoke_check.mjs --skip-api`、提升权限后的 `frontend_content_smoke_check.mjs --url http://127.0.0.1:5173`、`dev_server_guard.py --status`、`check_github_data_boundary.py` 和 `git diff --check`；未启动系统 Chrome，未全量重导，未恢复 Environment Mapping 写入线。
- 2026-06-15 `P0 Source-of-Truth Reconciliation 1.2`：按用户明确批准替换当前 SQLite，并按“务必做好备份”要求先备份再替换。替换前备份为 `data/exports/worker-verify/p0-source-of-truth-reconciliation-1.2/pre-replacement-backup/sapd_wiki.before-p0-sotr-1.2.20260615-162417.sqlite3`；替换前 current hash 为 `52470cbd6fd7cb15852fba352705dd6f028b21f06ffa580cbf7f6edcd5c49f0b`，候选库 hash、替换后 current hash 均为 `7880968a6d10cf2ed4d4b3546329098d555ca265e5965c2f3e23327c058fc8eb`，备份 hash 与替换前 current hash 一致。替换后 6 类受保护类型计数为 `work_function_layer=4`、`work_function=86`、`security_work=80`、`process_reference=78`、`application_system_type=3`、`standard_control=3416`。产物位于 `data/exports/worker-verify/p0-source-of-truth-reconciliation-1.2/`，包括 `sqlite-replacement-report.*`、`post-replacement-baseline-audit.*`、`post-replacement-json-hash-check.*`、`post-replacement-content-smoke-report.*`。替换后 `audit_dictionary_standard_baseline_integrity.py`、`audit_protected_baseline_no_regression.py`、`audit_json_package_boundary.py`、正式 JSON hash check、内容级 smoke 均通过且 `errors=0 warnings=0`；正式 `public/data` 未覆盖，runtime baseline JSON hash 保持一致，未执行全量导出 / core reset，未恢复 Environment Mapping 写入线，未修改前端 UI。当前运行基线与 SQLite 导出基线已对齐；关闭前只读巡检再次通过后，`OI-140` 已关闭并归档。
- 2026-06-15 `P0 Source-of-Truth Reconciliation 1.1`：按用户选择继续 `OI-140 / SQLite Source-of-Truth Reconciliation`，保持 Environment Mapping 冻结。已升级 `scripts/reconcile_p0_source_of_truth.py` 支持 `--mode candidate-only` 和 1.1 指定产物，生成隔离候选库 `data/exports/worker-verify/p0-source-of-truth-reconciliation-1.1/protected-baseline-reconciled-candidate.sqlite`，hash=`7880968a6d10cf2ed4d4b3546329098d555ca265e5965c2f3e23327c058fc8eb`；2026-06-01 备份路径为 `data/database/backups/sapd_wiki-before-cleanup-20260601-current.sqlite3`，hash=`d78d974ef1479609218ba42de5e6cbeab4d832fb3872dee9e2f0de0fac3ec4a7`。候选库复制当前 SQLite 后只从备份补入当前缺失的 reconciliation 范围对象，补入 `knowledge_items=4041`、`knowledge_relations=4529`、`source_references=10482`，6 类受保护类型均已补齐：`work_function_layer=4`、`work_function=86`、`security_work=80`、`process_reference=78`、`application_system_type=3`、`standard_control=3416`。候选导出目录为 `data/exports/worker-verify/p0-source-of-truth-reconciliation-1.1/exports-candidate-sqlite/`；隔离导出后按候选库 `standard_framework` 标题同步 8 处标准标题，未修改正式标准数据。normalized diff：当前 SQLite 导出 vs 运行冻结 JSON 为 `countDiff=2 / keySetDiff=19`；2026-06-01 备份导出 vs 运行冻结 JSON 为 `0 / 0`；候选导出 vs 运行冻结 JSON 为 `0 / 0`。候选库 baseline audit `errors=0 warnings=0`，候选导出 JSON boundary audit `errors=0 warnings=0`，readiness=`ready_for_manual_approval`。本轮未修改当前正式 SQLite、未覆盖正式 `public/data`、未修改正式前端页面、未恢复 Environment Mapping 写入线。
- 2026-06-15 `P0 Recovery Runtime Baseline Freeze 1.0`：按用户要求固化当前前端冻结 JSON 运行基线，新增 `scripts/build_p0_runtime_baseline_freeze.py`，只生成 hash / manifest / counts / risk boundary / 后续变更流程，不修改 SQLite、不重导 JSON、不改原始 Excel / SVG / schema / Environment Mapping / workbench 内容 / 前端 UI。产物目录为 `data/exports/worker-verify/p0-runtime-baseline-freeze/`，包含 `runtime-baseline-manifest.json/md`、`runtime-baseline-counts.json/md`、`runtime-baseline-risk-boundary.json/md`、`future-source-data-change-procedure.md` 和 `files/` 快照副本；`fileCount=45`，预期缺失项为旧名 `frontend/capability-browser/public/data/management-knowledge.json`，当前运行基线实际为 `maintenance-knowledge.json` + `maintenance/*`。关键计数：`securityWorks=80`、`securityProcesses=10`、`workFunctionLayers=4`、`securityTechnicalMeasures=30`、`standards.controls=4893`、`managementMapping=613`、`standardMapping=4033`、`lifecycle.relations=542`、`standards.frameworks=7`；4 个生命周期来源措施和 7 个 canonical 标准名称均为 `present=true`。冻结当时 SQLite hash 为 `52470cbd6fd7cb15852fba352705dd6f028b21f06ffa580cbf7f6edcd5c49f0b`，6 类受保护类型计数均为 `0`；该缺口已在 1.2 替换后补齐。验证通过：`py_compile`、`audit_dictionary_standard_baseline_integrity.py`（当时预期 `warnings=6`）、`audit_protected_baseline_no_regression.py`、`audit_json_package_boundary.py`、`audit_capability_standard_mapping_canonicalization.mjs`、提升权限后的 `frontend_content_smoke_check.mjs --url http://127.0.0.1:5173`、`dev_server_guard.py --status`、`check_github_data_boundary.py`、`git diff --check`。
- 2026-06-15 `P0 Recovery State Acceptance & Snapshot Refresh`：按用户带回的 ChatGPT 建议确认当时口径：P0 页面运行态基本恢复，标准 canonical title 已确认并作为正式显示名，4 个生命周期来源安全技术措施已确认并补入前端维护基线，前端冻结 JSON 是可运行基线，当时 SQLite 仍不完整且禁止作为字典 / 标准 / 生命周期 / 能力管理映射完整导出源；该 SQLite 缺口已在 1.2 替换后补齐。完成轻量页面验收与封存：`review_standard_framework_canonical_names.mjs` 返回 `requires_user_confirmation=0`、`manual_review=0`；`audit_capability_standard_mapping_canonicalization.mjs` 返回 `duplicate_standard_display_groups=0`、`duplicate_canonical_controls=0`、`placeholder_control_items=0`、`unmatched_standard_mappings=0`；`frontend_content_smoke_check.mjs --skip-api` 与提升权限后的 `--url http://127.0.0.1:5173` 均 `pass`，关键计数为 `securityWorks=80`、`securityTechnicalMeasures=30`、`securityProcesses=10`、`managementMapping=613`、`standardMapping=4033`、`standards.controls=4893`、`lifecycle.relations=542`；`frontend_smoke_check.mjs` 覆盖 `/standards/mlps-level-3`、`/standards/cis-csc-v8`、`/knowledge/management-workflows` 均通过且 `browserSkipped=true`。重新运行 `build_p0_recovery_closure_artifacts.py` 生成 `data/exports/worker-verify/p0-recovery-stable-snapshot/`，`fileCount=54`，预期缺失仍为旧名 `management-knowledge.json`；`audit_json_package_boundary.py` 为 `errors=0 warnings=0`，`check_github_data_boundary.py` 为 OK，`git diff --check` 通过。本轮未修改 SQLite、未重导数据、未覆盖正式环境映射包、未启动系统 Chrome、未恢复 Environment Mapping。
- 2026-06-15 `P0 Standard Framework Directory Canonical Name Sync`：按用户确认“标准 / 框架名称参考当前安全能力映射中的名称”，将 `安全标准 / 框架` 左侧目录和标准索引标题统一为 canonical 名称：`GB/T 22239-2019 网络安全等级保护基本要求 第三级`、`NIST Cybersecurity Framework 2.0`、`CIS Controls v8.1.2`、`CRF Safeguards Core Edition v2026`。同步修改 `AppShell.js`、`standards-data.json`、`standards-index.json` 及对应标准分片标题，并提升 `AppShell.js` 缓存版本；本轮未修改 SQLite、未重导标准数据、未覆盖环境映射包、未修改原始 Excel / SVG / schema 或胶囊样式。验证结果：标准名称 review 为 `requires_user_confirmation=0`，能力标准映射审计为 `duplicate_standard_display_groups=0`、`placeholder_control_items=0`、`standard_alias_candidates=0`。
- 2026-06-15 `P0 Baseline Canonical Data Correction 1.1`：按用户确认正式补入 4 个生命周期来源安全技术措施，不再停留在口径说明。新增 `scripts/apply_p0_baseline_canonical_data_correction.mjs`，从 reconciliation 候选导出定向提取 `应用程序威胁建模`、`制品安全加固`、`IaC代码安全测试`、`数据销毁` 的稳定 id、来源证据和维护页记录，写入 `maintenance-knowledge.json`、`maintenance/measures.json`、`maintenance-index.json` 和 `source-evidence/maintenance/measures.sources.json`；正式 `security_technical_measures` 从 `26` 更新为 `30`。新增 `scripts/review_standard_framework_canonical_names.mjs`，只读生成 `standard-framework-canonical-name-review.json/md`，本轮未修改 `standards-data.json`、`standards-index.json` 或标准分片。候选需用户确认的标准 canonical 名称为：`GB/T 22239-2019 网络安全等级保护基本要求 第三级`、`CIS Controls v8.1.2`、`NIST Cybersecurity Framework 2.0`、`CRF Safeguards Core Edition v2026`。已修订 `frontend_content_smoke_check.mjs` 和 `audit_protected_baseline_no_regression.py`：30 条措施为正式期望，不再把 4 项生命周期措施误判为 B 类风险。本轮未修改 SQLite、lifecycle-workbench、environment-workbench、node-details、原始 Excel、SVG / Draw.io、数据库 schema 或胶囊样式。
- 2026-06-15 `P0 Capability Standard Mapping Canonicalization 1.0`：针对用户截图中 `安全能力映射 -> 标准 / 框架映射` 同一标准重复展示和疑似表头 / 占位项混入问题，完成最小修复与专项审计。`viewModels.js` 改为以 `standards-data.json` 中的 `frameworkCode` / canonical title 作为标准分组基准，避免 `CIS Controls v8.1.2 / CIS CSC V8`、`CRF Safeguards Core Edition v2026 / CRF` 等 raw name 混用重复展示；`CapabilityLocalRelationMap.js` 增加展示层兜底，保留控制编号括号和连字符，避免 `SC-3(2)` 与 `SC-32` 被错误合并；`待补充` / 空编号控制项不进入正式控制项 chip。新增 `scripts/audit_capability_standard_mapping_canonicalization.mjs`，报告为 `data/exports/worker-verify/capability-standard-mapping-canonicalization-audit.json/md`，结果 `duplicate_standard_display_groups=0`、`duplicate_canonical_controls=0`、`placeholder_control_items=0`、`unmatched_standard_mappings=0`、`standard_alias_candidates=4`。同步修正 `scripts/reconcile_p0_source_of_truth.py` 与状态记录：`应用程序威胁建模`、`制品安全加固`、`IaC代码安全测试`、`数据销毁` 已重新定性为生命周期来源安全技术措施补齐项，不再作为旧 B 类误恢复风险。本轮未修改标准字典、SQLite、原始 Excel、环境映射包、node-details、lifecycle-workbench、SVG / Draw.io 或胶囊样式。
- 2026-06-15 `P0 Source-of-Truth Reconciliation 1.0`：按用户确认的硬边界完成只读对账、候选库生成、候选导出和 normalized diff；未替换当前 SQLite、未覆盖 `public/data`、未修改正式页面、未改原始 Excel / SVG / schema。新增 `scripts/reconcile_p0_source_of_truth.py`，产物位于 `data/exports/worker-verify/p0-source-of-truth-reconciliation/`，候选库为 `protected-baseline-reconciled-candidate.sqlite`，sha256=`7880968a6d10cf2ed4d4b3546329098d555ca265e5965c2f3e23327c058fc8eb`。对账确认当前 SQLite 中 6 类受保护基线为 0：`work_function_layer`、`work_function`、`security_work`、`process_reference`、`application_system_type`、`standard_control`；2026-06-01 备份与候选库均可补齐这些类型。候选库从备份补入 `24` 类当前缺失的 reconciliation 范围对象，共 `knowledge_items=4041`、`knowledge_relations=4529`、`source_references=10482`；候选库总对象数 `4646`。后续复核确认 `frontendVsCandidateExport` 中多出的 4 个 `security_technical_measures` 属于生命周期来源安全技术措施补齐项，不再作为旧 B 类误恢复风险；正式 SQLite 是否选择性恢复仍需单独确认。候选库运行 `audit_dictionary_standard_baseline_integrity.py --db ...candidate.sqlite` 返回 `errors=0 warnings=0`；`audit_json_package_boundary.py` 返回 `errors=0 warnings=0`。
- 2026-06-15 `P0 Baseline Recovery Closure 1.5`：按用户要求完成当前恢复状态备份、修改边界冻结、事故根因分析、长期防复发方案和人工核对计划。已新增 `scripts/build_p0_recovery_closure_artifacts.py`，生成固定快照目录 `data/exports/worker-verify/p0-recovery-stable-snapshot/`，共备份 `54` 个当前文件；当前 SQLite hash 为 `52470cbd6fd7cb15852fba352705dd6f028b21f06ffa580cbf7f6edcd5c49f0b`，manifest 记录 `management-knowledge.json` 当前不存在且实际维护包为 `maintenance-knowledge.json` + `maintenance/*`。新增 `scripts/audit_json_package_boundary.py`，当前 JSON 分拆边界审计 `errors=0 warnings=0`。已输出 `p0-recovery-modification-boundary.json/md`、`p0-root-cause-analysis.json/md`、`p0-prevention-plan.json/md`、`p0-post-recovery-manual-validation-plan.md`。当前关键计数：`security_works=80`、`security_processes=10`、`work_function_layers=4`、`standard_controls=4893`、`capabilityWorkbench.management-mapping=613`、`standard-mapping=4033`、`lifecycleWorkbench.relations=542`、`environmentBasemap.detailReadyNodes=92`。本轮未继续环境映射治理、未做全量导出、未做全量恢复、未修改原始 Excel / SVG / schema。
- 2026-06-15 `P0 Security Work List Empty Page Fix`：针对用户截图中 `/knowledge/management-workflows` 的 `安全工作清单` 仍显示空表的问题，确认维护包实际非空：`maintenance-knowledge.security_works=80`、`maintenance/security-works.json=80`。根因收敛为前端仍可能加载旧版 `viewModels.js` / 组件脚本，旧逻辑没有把 `security_works` 正确传入安全工作清单表格。已只做前端缓存版本、计数口径和内容级 smoke 加固：`index.html` 将 `dataClient.js`、`viewModels.js`、`StandardRoleReferenceTable.js`、`app.js` 的版本统一提升到 `p0-capability-work-binding-20260615-2`；`viewModels.js` 中安全工作清单导航 / 页签计数优先使用维护包安全工作数；`frontend_content_smoke_check.mjs` 新增安全工作清单 ViewModel 与导航计数断言。验证结果：本地和 5173 内容级 smoke 均生成 `securityWorkRows=92`、`uniqueSecurityWorks=80`、`navigationSecurityWorks=80`、`tabSecurityWorks=80`；5173 API 返回 `maintenanceSecurityWorks=92`，`/knowledge/management-workflows` HTTP smoke 通过且 `browserSkipped=true`。本轮未做全量恢复、未修改原始 Excel、SQLite、正式环境映射包、正式 node-details、标准 / 字典基线数据或胶囊样式。
- 2026-06-15 `P0 Capability & Work Dictionary Binding Recovery 1.4`：针对用户截图中 `/capability-mapping` 的 `管理视角` 和 `标准 / 框架映射` 仍显示 0 的问题，完成选择性恢复，不做全量 core reset、不做全量 SQLite restore、不修改原始 Excel、正式环境映射包、node-details、SVG、schema 或胶囊样式。已补回 `capability-tree.focus.security_works/process_mappings`、`capability-workbench` 中的 `security_work/work_function/process_group/process_reference/standard_control` 投影，并为维护知识包新增 `security_works` 与拆分包 `maintenance/security-works.json`。当前内容级结果：`securityWorks=80`、`securityWorkFocuses=91/91`、`processMappingFocuses=89`、`managementMapping=613`、`processMapping=89`、`standardControl=1745`、`standardMapping=4033`；5173 API 抽样确认 `T`、`T-AS`、`G-SP`、`T-AS.AD`、`T-AS.AD-01`、`T-PD.PP-02` 的管理视角均非空，且 `T-AS.AD-01` 有 `standardControls=39`。新增 `scripts/frontend_content_smoke_check.mjs` 和 `scripts/audit_p0_capability_work_binding.py`，报告位于 `data/exports/worker-verify/p0-capability-work-binding-audit.json/md`。`audit_dictionary_standard_baseline_integrity.py` 当前为 `pass_with_warnings`，warnings 明确当前 SQLite 仍缺受保护类型，后续不得从当前 SQLite 直接重导出字典 / 标准 / 生命周期基线包。
- 2026-06-15 `P0 Standards Page Loading Containment`：继续处理用户指出的标准 / 框架页面卡死问题，定位根因为 `frontend/capability-browser/public/data/standards-index.json` 与 `standards-data.json` 中 `dataPath` 指向 `/private/tmp/...` 临时恢复目录，浏览器无法通过 5173 拉取标准分片。已只修复索引路径元数据为 `./public/data/standards/...` 相对路径，未重置、未重导入、未重建业务数据行。同步修订 `src/sapd_wiki/exports.py`，避免临时目录导出时再次写入本机绝对路径；`scripts/audit_dictionary_standard_baseline_integrity.py` 增加 `dataPath` 审计；标准页加载失败不再无限 loading，大表首屏渲染设上限以防卡死。验证通过：`node --check`、`python3 -m py_compile`、`audit_dictionary_standard_baseline_integrity.py`、`audit_protected_baseline_no_regression.py`、`audit_standard_framework_data.py`、5173 实际 fetch 10 个标准分片、标准 / 字典 / 指南相关 HTTP smoke、内置浏览器后台打开标准 / 字典代表页面、`check_github_data_boundary.py` 和 `git diff --check`；本轮未启动系统 Chrome。
- 2026-06-15 `P0 Baseline Incident Containment & Selective Recovery 1.2`：按用户指出“不能全量恢复，只能恢复丢失数据”重新收敛 `OI-140`。已暂停 Environment Mapping 后续治理，生成 `p0-baseline-incident-impact-audit.json/md`、`p0-selective-recovery-classification.json/md`、`p0-selective-recovery-plan.json/md`、`p0-selective-recovery-report.json/md`、`p0-overrestore-rollback-report.json/md` 和 `p0-protected-baseline-guard-report.json/md`。事故前分类为 `A=10 / B=1 / C=0 / D=3`；B 类误恢复项为 `maintenance-knowledge.json.security_technical_measures`，已从 30 回滚到恢复前 26；A 类缺失项保留已确认恢复结果，并补回 `lifecycle-workbench.json`，`relations=542`。本轮未修改原始 Excel、SVG / Draw.io、数据库 schema、正式环境映射包、能力包或正式业务 UI。期间一次 guard 测试误用了 `--skip-frontend-export` 触发 core-only SQLite 重建，已立即从 `data/database/sapd_wiki.sqlite3.before-bootstrap.bak` 恢复，误生成库保留为 `data/database/sapd_wiki.sqlite3.accidental-core-reset-20260615.bak`；最终当前 SQLite hash 恢复为 `52470cbd6fd7cb15852fba352705dd6f028b21f06ffa580cbf7f6edcd5c49f0b`。已在 `src/sapd_wiki/cli.py` 增加 `core` profile 保护拦截：只要现有前端保护基线存在，`bootstrap-local-data --profile core --reset` 默认拒绝，显式授权 `--allow-protected-baseline-reset` 时先备份再执行。
- 2026-06-15 `P0 Dictionary & Standards Baseline Integrity Recovery`：确认 `bootstrap-local-data --profile core --reset` 曾把当前 SQLite 压成 core-only，导致 `maintenance-knowledge.json`、`lifecycle-knowledge.json` 和标准框架 split 包被导出为空。已从 `data/database/backups/sapd_wiki-before-cleanup-20260601-current.sqlite3` 导出并恢复前端基准包，恢复前损坏包备份到 `data/exports/worker-verify/protected-baseline-recovery-backup/frontend-data-20260615/`；恢复后 `work_function_layers=4`、`security_processes=10`、`work_functions=86`、`process_references=78`、`gbt_42446_references=27`、`gartner_roles=28`、`application_system_types=3`、`application_components=13`、`standards_index.controls=4893`。新增 `scripts/audit_dictionary_standard_baseline_integrity.py`，并在 `AGENTS.md`、`docs/07-governance/data-governance.md`、`findings.md`、`docs/06-implementation/open-issues.md` 记录字典 / 标准只读基准保护规则和 `OI-140`；当前 SQLite 仍缺保护基准类型，后续不得从当前 SQLite 直接重导出字典 / 标准基准包，除非先确认数据库恢复策略。
- 2026-06-15 安全技术措施导入口径纠偏：用户确认 `安全技术措施` 的来源规则为：一是 `作用域-安全技术服务-安全技术模块映射` 的 G 列灰色底色格子；二是 `LC-AP` / `LC-DT` 的“安全技术模块”字段先匹配 `安全技术模块清单`，匹配不成功则进入 `安全技术措施` 清单。已撤销此前错误加入的“G 列值命中安全技术模块清单就过滤出措施目录”逻辑，保留“安全技术模块目录页只以 `安全技术模块清单` 为权威来源”的规则；同时修订 `src/sapd_wiki/parsers.py`，取消 LC-AP / LC-DT 措施白名单，改为模块字典匹配失败即创建 `security_technical_measure`。用户进一步指出措施清单第 27-34 项实际均为安全技术模块，复查确认这些 G 列为浅蓝底模块样式，且 H 列通过合并单元格继承后不为空；根因是维护导出器只读底层 XML 行值并在 D 列新对象处清空 H 继承，误触发“G 有值 + H 空”措施兜底。已补齐作用域表解析中的合并单元格继承，并移除该兜底，措施候选只接受真实措施样式。刷新维护包后 `security_technical_measures=26`，`maintenance/measures.json` hash 为 `f2ae803d28055857df24f7da0f3cce27de73603d1405d7138cc6839b28547dd6`；第三批 Sheet 解析验证 `measure_count=4`、`lc_unmatched_warning_count=0`。`bootstrap-local-data` 临时覆盖的正式 `environment-workbench.json` 已从 replacement bundle 拷回，hash 保持 `16e7707d32ac07ce1a93a4d3b429eafbc3a4b651214a0e19d37180c9552547c9`。
- 2026-06-14 主机入侵防御（HIPS）源表修正后同步：用户已将 `安全技术模块清单` 中 `主机入侵防御（HIPS）` 调整为只出现在 `云安全 / 主机安全防护` 下。已重新执行核心导入、维护数据包刷新、维护页源数据一致性审计、环境映射 candidate / replacement bundle、人工核对表刷新和 HTTP 验证；`maintenance/modules.json` 当前确认 `主机入侵防御（HIPS） -> 云安全 / 主机安全防护`，`云网络防火墙 -> 云网络安全防护`，`容器网络防火墙 -> 容器安全防护` 且包含清单第 `114 / 115 / 116` 行的 `网络隔离 / 网络流量（行为）日志审计 / 网络访问控制` 三个服务。维护页审计报告 `maintenance-module-measure-catalog-source-audit.json/md` 已归零：`sourceModuleCount=102`、`pageModuleCount=102`、`moduleIssueCount=0`、`structuralRiskCount=0`、`pageMeasureCount=34`、`measureIssueCount=0`。最新正式 `environment-workbench.json` hash 为 `16e7707d32ac07ce1a93a4d3b429eafbc3a4b651214a0e19d37180c9552547c9`，`environmentBasemap.node-details.json` hash 为 `08afda38ec691d4582b082cca71aeb18a9022d13a25c131c81e4d30dd0253f61`；临时人工核对表为 `selectedRowCount=776`、`moduleServiceMismatchRowCount=76`、`systemModuleMismatchRowCount=110`、`shouldPauseUiAlignment=true`。
- 2026-06-14 模块主数据权威源修复与容器网络防火墙回位：用户指出 `安全技术模块` 的来源必须是 `安全技术模块清单`，不能由 `作用域-安全技术服务-安全技术模块映射` 反向定义。已修订 `scripts/build_environment_workbench_reimport_candidate.py`：候选包中的 `module_system` 关系改为从 `安全技术模块清单` 的 `D列 安全技术模块 -> C列 安全系统` 权威派生，作用域表 H 列只作为一致性审计证据，不再生成正式模块-系统归属。用户随后将源表中 `容器网络防火墙` 相关行移动到 `云数据中心||工作负载||容器`，重新审计、构建 replacement bundle 并正式替换后，正式 `environment-workbench.json` hash 更新为 `2dd249a3cbe24675986cb9b1bf5b6052832d69e5431e48cec0727b29f4913268`，正式 `environmentBasemap.node-details.json` hash 更新为 `46a0ccdfbb0811e2f9370831a6942bc389811ef540e58d67e5921f3313a8bede`。定向验证确认 `容器网络防火墙` 仅出现在 Excel 行 `79 / 80 / 81` 的 `云数据中心||工作负载||容器` 下，安全系统来自模块清单 `容器安全防护`，权威来源行为第 `117 / 118` 行，旧 `云数据中心||网络||云数据中心内部虚拟网络` 下残留关系为 `0`，`容器网络防火墙 -> 云网络安全防护` 正式关系为 `0`。
- 2026-06-14 维护清单包同步修复：用户截图发现 `安全技术模块清单` 页面仍把 `容器网络防火墙` 显示在 `云网络安全防护` 下。复查确认正式环境映射包已正确，但 `frontend/capability-browser/public/data/maintenance/modules.json` 仍是旧导出，其中 `容器网络防火墙.systems=[云网络安全防护, 容器安全防护]`，前端分组取第一个系统导致错组。已重新执行 `python3 scripts/sapd_wiki.py bootstrap-local-data --profile core --workbook "data/raw-samples/wiki sample.xlsx" --reset` 刷新维护知识包，再把已验证的 replacement bundle `environment-workbench.json` 拷回正式环境包，避免 bootstrap 覆盖正式环境映射。HTTP 按前端真实路径验证 `http://127.0.0.1:5173/public/data/maintenance/modules.json` 返回 `云网络防火墙 -> 云网络安全防护`、`容器网络防火墙 -> 容器安全防护`；正式环境包 hash 保持 `2dd249a3cbe24675986cb9b1bf5b6052832d69e5431e48cec0727b29f4913268`。
- 2026-06-14 `安全技术模块/措施清单` 全页源数据一致性审计：针对用户指出“因一个小问题修改错数据并忽略全局规则”，已检查截图页 `知识库字典 / 安全技术模块/措施清单` 与原始 Excel 的一致性。修订 `src/sapd_wiki/exports.py`：维护模块目录页的模块-服务、模块-安全系统、模块-产品只采纳来源为 `安全技术模块清单` 的关系，作用域表和生命周期表不再污染该目录；修订 `src/sapd_wiki/parsers.py`：模块清单遇到新模块锚点行时，C 列安全系统和 G 列产品为空即清空，不再继承上一模块。重导后审计报告位于 `data/exports/worker-verify/maintenance-module-measure-catalog-source-audit.json/md`，结果为 `sourceModuleCount=102`、`pageModuleCount=102`、`pageMeasureCount=34`、`measureIssueCount=0`；当时剩余的 `主机入侵防御（HIPS）` 同名跨目录结构性风险已在用户源表修正后同步清零，见上一条最新记录。
- 2026-06-14 H01 安全系统名称写回：按用户确认，已将原始 Excel `作用域-安全技术服务-安全技术模块映射` 中 `H504:H513`、`H571:H580` 两个合并区顶格从 `移动终端安全` 写回为 `移动终端安全管控`，写回前备份到 `data/exports/worker-verify/source-excel-backup/wiki sample.before-h01-security-system-writeback.20260614-231215.xlsx`。重新执行 `audit_scope_service_module_mapping.py`、`audit_environment_module_catalog_consistency.py` 和 `build_environment_manual_review_checklist.py` 后，H 类 `possibleAliasMatchCount=0`，`environment-h-possible-alias-list.md/csv` 已刷新为空清单；本轮只做源表写回、审计和临时核对表刷新，未替换正式 `environment-workbench.json` 或 `environmentBasemap.node-details.json`。
- 2026-06-14 `Environment Mapping Reimport after module/measure correction`：用户修正原始 Excel 后，已重新执行核心 ETL、维护数据包刷新、环境映射审计、candidate / replacement bundle 构建、正式数据包替换和人工核对表刷新。维护知识包当前为 `data_state=ready`，`security_technical_services=158`、`security_technology_modules=102`、`security_technical_measures=34`；环境映射审计为 `normalizedRowCount=776`、`objectServiceRelationCount=739`、`serviceModuleRelationCount=646`、`serviceMeasureRelationCount=173`、`moduleSystemRelationCount=216`、`duplicateExactServiceChildRelationCount=0`、`scopeCompletenessIssueCount=0`。正式 `environment-workbench.json` hash 为 `9a80aeedaea8eb9abc087bac158aeb9dfe864f9bfd48f473b3bab18d133540b6`，正式 `environmentBasemap.node-details.json` hash 为 `52c7fa6a4873beb16e3e34484f03c62839b41758efa79984f73cd3125327662a`。临时人工核对表为 `selectedRowCount=776`、`highRiskContextCount=67`、`duplicateServiceRowCount=0`、`duplicateExactServiceChildRelationRowCount=0`、`moduleServiceMismatchRowCount=75`、`systemModuleMismatchRowCount=120`、`shouldPauseUiAlignment=true`；用户已确认“同一对象下重复安全技术服务 / 1:N 展开”后续一起治理，本轮不强行收口。
- 2026-06-13 `Environment Mapping Dual-table Review UI 1.1`：按用户截图反馈“双表对照核对看不懂、展示不出对比”，将中间目录关系列表改为左右对照结构：`目录表这一边` 展示模块清单中的安全系统分类 / 安全系统 / 安全技术模块 / 安全技术服务，`环境映射表这一边` 展示该关系在环境映射表中的使用数量、环境 / 对象 / 作用域 / Excel 行摘要，`对照结论` 聚合差异标签和查看依据入口；右侧证据面板标题同步改为“选中关系的双表对照”，分成 `目录表这一边`、`为什么被标记`、`环境映射表这一边`。新增双表筛选状态胶囊和 `清空双表筛选`，空结果不再只显示空白，而是解释“当前组合没有精确目录关系”，并给出按单一安全系统 / 模块 / 服务 / 搜索词匹配的命中数。目录树点击模块时自动带上所属安全系统，点击安全系统时清空下游模块 / 服务筛选，避免不同目录分支组合导致误判。该改动只影响临时核对 UI；未修改正式 `environment-workbench.json`、正式 `environmentBasemap.node-details.json`、正式业务 UI、原始 Excel、SVG / Draw.io、数据库 schema 或胶囊基线。
- 2026-06-13 `Environment Mapping Dual-table Review UI 1.0`：按用户要求将 `/environment-mapping` -> `数据核对（临时）` 扩展为两个模式：`环境对象核对` 保留原 Excel 行级视图与 source row / merged range / sourceCells 证据；`双表对照核对` 新增以 `安全技术模块清单` 为目录基准的三栏视图，左侧为 `安全系统分类 -> 安全系统 -> 安全技术模块` 树，中间为 `安全系统分类 / 安全系统 / 安全技术模块 / 安全技术服务` 目录关系列表，右侧展示目录表证据、环境映射表全部使用位置、相关候选行、差异判断和人工核对建议。最新双表摘要：`directoryRelationCount=455`、`catalogRelationCount=390`、`environmentModuleRelationCount=652`、`environmentOnlyRelationCount=68`、`catalogUnusedRelationCount=230`、`moduleServiceMismatchRelationCount=27`、`systemModuleMismatchRelationCount=43`、`possibleAliasRelationCount=25`、`selectiveReferenceCandidateCount=230`；coverage gap / 目录未精确引用默认作为选择性引用候选，不标红。B 类已按当前审计清零：`moduleMeasureClassificationIssueCount=0`；可能别名按“右表已有清单精确模块则不再提示别名”继续降噪后为 `possibleAliasMatchCount=1`。本轮未修改正式 `environment-workbench.json`、正式 `environmentBasemap.node-details.json`、正式业务 UI、胶囊样式、SVG / Draw.io、原始 Excel 或数据库 schema。
- 2026-06-13 C/D/H 类问题清单拆分：按用户反馈“表格里不方便判断”，从 `environment-module-catalog-consistency-audit.json` 和 review rows 生成聚类级人工核对清单：`data/exports/worker-verify/environment-c-module-service-mismatch-list.md/csv`、`environment-d-system-module-mismatch-list.md/csv` 和 `environment-h-possible-alias-list.md/csv`。C 类清单聚合为 `11` 个模块-服务问题，覆盖 `75` 行；D 类清单聚合为 `12` 个系统-模块问题，覆盖 `132` 行；H 类清单在用户确认 `网络防火墙`、`云网络防火墙`、`容器网络防火墙` 是不同安全技术模块后，已降噪为 `1` 组安全系统命名差异，去重覆盖 `18` 行。用户确认 H 类左右口径为左表 `安全技术模块清单`、右表 `作用域-安全技术服务-安全技术模块映射`，且右表 G 列命中项在本轮均按安全技术模块处理，不按安全技术措施处理；清单已按该字段方向重写。清单按问题模式展示环境映射表写法、清单侧已有归属、涉及上下文、Excel 行号和建议判断，不修改正式数据包、正式 UI 或原始 Excel。
- 2026-06-13 跨表一致性差异聚类与人工核对降噪 1.1：结合 ChatGPT 提示词和本地数据审计结果，新增 `environment-module-catalog-consistency-triage.json/md`，将跨表差异按 A-I 治理口径分类、按 `patternSignature` 聚类，并从 771 行核对表中提炼 `topManualReviewItemCount=36`。用户确认 `云网络防火墙` 在环境映射中作为云厂商默认产品 / 云原生能力后补进入安全技术措施，不按安全产品模块拆分；已据此收紧 B 类规则，措施名称即使命中模块清单也不再自动判为模块/措施分类问题，当前 `moduleMeasureClassificationIssueCount=0`。最新审计结果为 `duplicateExactServiceChildRelationCount=0`、`moduleServiceMismatchPatternCount=11`、`systemModuleMismatchPatternCount=12`、`possibleAliasMatchCount=1`；`serviceModuleCoverageGapCandidate=552` 和 `moduleServiceCoverageGapCandidate=77` 已作为 E/F 类 coverage gap 候选进入人工确认，而不是直接视为错误。临时核对表新增 `issueType`、`triageCategory`、Top 人工核对、可能别名、1:N 展开、目录不一致和 coverage gap 筛选，并在右侧显示目录依据面板；本轮未修改正式 `environment-workbench.json`、正式 `environmentBasemap.node-details.json`、SVG / Draw.io、数据库 schema 或正式业务 UI。
- 2026-06-12 移动终端软件应用名称同步：用户将 Draw.io 中 `移动应用` 改为 `移动终端软件应用`，并同步修改原始 `作用域-安全技术服务-安全技术模块映射` 表。复查 Excel 行 `497 / 564 / 640` 已分别对应 `园区网 / 移动终端 / 移动终端软件应用`、`分支机构 / 移动终端 / 移动终端软件应用`、`客户访问接入 / 移动终端 / 移动终端软件应用`。已删除 `frontend/capability-browser/generated/environmentBasemap.binding-overrides.json` 中过期的 `ignoredMxIds` 覆盖，重新生成底图语义、重导入环境包、重建 replacement bundle 并替换正式 `environment-workbench.json` 与 `environmentBasemap.node-details.json`。当前绑定结果为 `bound=92`、`ignored=6`、`candidate=0`、`unbound=0`；三处 `移动终端软件应用` 均为 `information_object` 且上下文分别命中 `园区网||移动终端||移动终端软件应用`、`分支机构||移动终端||移动终端软件应用`、`客户访问接入||移动终端||移动终端软件应用`。正式 `environment-workbench.json` hash 为 `a51a7f099eb577572ad14643621b9a023c8217af2d06ffbb7d8bd0c87df54c23`，正式 `environmentBasemap.node-details.json` hash 为 `e16cfa8504a569f2d8d02ac40c8a7e23cf4397041921c02fef3e4256bd97cfcf`；`node-details` 已补齐 `directSecuritySystemCount/securitySystemCount` 和 `directScopeGroups[].securitySystems`。
- 2026-06-12 最新原始 Excel 与 SVG 同步：用户删除原始 Sheet 中唯一完整重复关系并再次更新 `data/raw-samples/drawio sample-信息化环境及对象底图.drawio.svg`，修复底图名称与表格不匹配并补充 `数据共享层`。已从 SVG 内嵌 draw.io XML 重新生成底图语义，当前 `nodes=98`、`edges=74`；绑定结果为 `bound=91`、`candidate=0`、`unbound=0`、`ignored=7`、`missingExpectedObjects=0`。重新审计 `作用域-安全技术服务-安全技术模块映射` 后 `normalizedRowCount=771`、`objectScope=156`、`objectService=739`、`serviceModule=640`、`serviceMeasure=180`、`moduleSystem=214`、`duplicateExactServiceChildRelationCount=0`、`duplicateServicesInObjectContextCount=0`、`scopeCompletenessIssueCount=0`。已重新生成并正式替换 `environment-workbench.json` 与 `environmentBasemap.node-details.json`，hash 分别为 `41422f6b418fe1ffe59d03cc608c99f6e699cda7d6090099dd47c93429043142`、`3a7573a5bcd7c336d265d807eb4efcc950b60afdad9e243daa44d4518111b97b`；同时修正 replacement bundle 中 node-details 摘要计数漏计 `objectId/objectName` 详情实体的问题。抽样确认 `外联网出口边界` 已命中 `network_boundary`，路径为 `网络周界 / 外联网边界 / 外联网出口边界`；`数据共享层` 已命中 `information_object`，路径为 `云数据中心 / 大数据平台/数据中台 / 数据共享层`。
- 2026-06-12 跨表一致性审计 1.0：按用户修订口径废止 `objectContextKey + securityTechnicalService` 判重，改为仅当 `objectContextKey + securityTechnicalService + childType + securityTechnologyModuleOrMeasure + securitySystem` 完全重复时才作为高风险。已修订 `scripts/audit_scope_service_module_mapping.py`，新增 `scripts/audit_environment_module_catalog_consistency.py`，并刷新临时人工核对表；审计读取 `安全技术模块清单` 为 `maxRow=439`、`mergedRangeCount=221`、`catalogRelations=390`。最新结果：`duplicateExactServiceChildRelationCount=1`、`repeatedServiceWithDifferentChildrenCount=79`、`moduleServiceMismatch=75`、`systemModuleMismatch=132`、`serviceModuleCoverageGapCandidate=552`、`moduleServiceCoverageGapCandidate=77`、`securitySystemCoverageOrMismatch=127`、`moduleMeasureClassificationIssue=3`、`scopeCompletenessIssues=0`、`hasIusScopeGap=false`。新增产物为 `data/exports/worker-verify/environment-module-catalog-consistency-audit.json/md` 和 `environment-module-catalog-consistency-review-rows.json`；本轮未修改正式 `environment-workbench.json`、`node-details`、SVG / Draw.io 或正式业务 UI。
- 2026-06-12 外联网出口边界底图详情命中复查：复查发现正式 `node-details` 已命中 `外联网出口边界`，但人工核对表仍显示 `nodeDetailsContains=否`，原因是 `environmentBasemap.node-details.json` 未输出 `objectContextKey / matchedObjectContextKey`，核对表无法按 `网络周界||外联网边界||外联网出口边界` 建索引。已修订 `scripts/generate_environment_basemap_node_details.mjs` 并重算详情与人工核对表；当前 `mxId=7M0SGe99AU4GMtvV9rx2-53` 同时输出 `objectContextKey` 和 `matchedObjectContextKey`，人工核对表中 `外联网出口边界` 6 行全部为 `nodeDetailsContains=是`、`nodeDetailMxIds=7M0SGe99AU4GMtvV9rx2-53`，`issueTypes=[]`。
- 2026-06-12 外联网出口边界底图同步：用户更新 `data/raw-samples/drawio sample-信息化环境及对象底图.drawio.svg`，将原网络周界中的 `外联网边界` 调整为 `外联网出口边界`。已同步到正式 Viewer 使用的 `frontend/capability-browser/generated/environmentBasemap.svg`，并为 `mxId=7M0SGe99AU4GMtvV9rx2-53` 增加人工绑定到 `网络周界||外联网边界||外联网出口边界` 的信息化对象实例。修订 `scripts/bind_environment_basemap_semantics.mjs`：绑定目录忽略不被正式关系引用的 `reimport:*` 镜像对象，并从 `information_object.contextKey` 补齐上下文路径，避免同名对象合法重复导致全图 candidate；修订 `scripts/generate_environment_basemap_node_details.mjs`：node-details 优先使用正式对象的 `scope_mappings`，并优先用 `contextKey` 生成信息化对象上下文。重算后 `bound=91`、`candidate=0`、`ignored=6`、`detailReadyNodes=91`、`missingDetailNodes=0`；抽样 `外联网出口边界` 显示 `contextPathText=网络周界 / 外联网边界 / 外联网出口边界`、`scopeCount=3`、`serviceCount=5`、`moduleCount=5`。
- 2026-06-12 同名不同上下文业务规则固化：用户确认同名信息化对象出现在不同 `信息化环境 / 环境子类` 下是允许的业务规则。已调整 `scripts/build_environment_manual_review_checklist.py`，临时人工核对表保留 `sameNameDifferentContext=是/否` 信息字段，但不再生成“同名信息化对象跨多个上下文”核对提示，也不再输出 `same_name_different_context` issue type。重新生成核对表后 `sameNameYesRows=233`、`promptOrIssueRows=0`，`highRiskContextCount=8`、`duplicateServiceRowCount=42` 不变，剩余高风险继续来自重复安全技术服务行等真实待核问题。
- 2026-06-12 G101 源数据修正同步：确认原始 `data/raw-samples/wiki sample.xlsx` 的 `作用域-安全技术服务-安全技术模块映射` Sheet 中 `G101` 已从 `N/A(云平台相关高可用设计如快照)` 修正为 `云平台相关高可用设计如快照`，全表 `N/A(...)` 残留为 `0`。重新审计、生成 candidate、验证下游兼容、生成 replacement bundle、替换正式 `environment-workbench.json` 并刷新人工核对表；重导入统计保持 `serviceMeasureRelations=180`、`pendingRelationCount=0`、`invalidRowCount=0`。正式 `environment-workbench.json` hash 更新为 `3b76a20908962018a14a990d5c6599bf5a28bd85ecdf57a4ee397f485bfbf3cb`，`environmentBasemap.node-details.json` hash 仍为 `9079bd79c74d36b971c569a68b4048c06b52799ab0c85608c043ea51216d1d64`。抽样 `excelRow=101` 显示 `moduleOrMeasureRaw=云平台相关高可用设计如快照`、`moduleOrMeasureKind=measure`、`risk=low`。
- 2026-06-12 安全技术措施目录导入口径修订：用户已将原始 Excel 中 `N/A（…）` 形式的安全技术措施值清理为括号内措施名称。复查发现旧脚本仍会因单元格保留旧说明类 / 灰色样式，把 `网络基础架构安全设计`、`应用系统高可用性设计` 等措施判为 pending / invalid。已修订 `scripts/audit_scope_service_module_mapping.py` 和 `src/sapd_wiki/exports.py`：模块仍优先按 `安全技术模块清单` 和模块样式识别；否则 G 列值在安全系统列为空 / `N/A` 或旧说明样式时，按 `securityTechnicalMeasure` 导入，样式只作为证据，不再一票否决措施目录。重导入后 `serviceMeasureRelations=180`、`pendingRelationCount=0`、`invalidRowCount=0`；源表扫描 `remaining_na_wrapped_values=0`；正式 `environment-workbench.json` hash 更新为 `e89fc7c7b54744a2f04ab6219900e688ad861f9e2a8fdaf3b94528be24a142ba`，`environmentBasemap.node-details.json` hash 为 `9079bd79c74d36b971c569a68b4048c06b52799ab0c85608c043ea51216d1d64`。临时核对表仍为 `67` 个上下文、`772` 行、`highRiskContextCount=8`、`duplicateServiceRowCount=42`，剩余风险继续来自重复安全技术服务行。
- 2026-06-12 `N/A（…）` 措施关系修复：用户指出 `I-NT&T-AS.AD-01 网络平面及区域划分`、`I-NT&T-AS.AD-02 网络冗余` 在正式关系中显示为 `/`，但原始 Excel 中分别有 `N/A（网络基础架构安全设计）`、`N/A（网络高可用性设计）`。已修订 `scripts/audit_scope_service_module_mapping.py`，将 `N/A（说明类安全设计）` 解析为 `securityTechnicalMeasure`，而不是 pending / 空关系；同时保持“措施可不关联安全系统”的规则。重导入后 `serviceMeasureRelations=180`、`pendingRelationCount=0`，正式 `environment-workbench.json` hash 为 `cdd9619a82fb2f92a3e03178cadeef54962cb626935a4f59a6aa9e362558bff8`，正式 `environmentBasemap.node-details.json` hash 为 `9079bd79c74d36b971c569a68b4048c06b52799ab0c85608c043ea51216d1d64`。抽样确认 Excel 行 60 / 61、340 / 341、455、728、755 均已进入 `安全技术措施`，不再显示为 `/`。
- 2026-06-12 修订临时核对表安全系统缺失口径：按用户确认，`安全技术措施` 不要求关联安全系统；只有 `安全技术模块` 在原表 `moduleSystemRelations` 中曾经关联过安全系统、但当前行安全系统为空时，才提示风险。已修改 `scripts/build_environment_manual_review_checklist.py`，用全表 `moduleSystemRelations` 建立模块-安全系统基准，不再把“措施无安全系统”列为问题。重新生成核对表后 `missing_security_system` / `missing_security_system_for_known_module` 均为 0，`highRiskContextCount=8`、`duplicateServiceRowCount=42`、`scopeCompletenessIssueContextCount=0`；剩余高风险均来自重复安全技术服务行。
- 2026-06-12 再次检查用户清理后的原始 Excel：检测到 `data/raw-samples/wiki sample.xlsx` 已于 18:46 保存，重新读取 `作用域-安全技术服务-安全技术模块映射` Sheet 并刷新审计、candidate、replacement bundle、正式 `environment-workbench.json`、正式 `environmentBasemap.node-details.json` 和临时核对表。重导入结果为 `sheetMaxRow=1326`、`normalizedRowCount=772`、`objectScope=156`、`objectService=739`、`serviceModule=640`、`serviceMeasure=123`、`moduleSystem=214`、`scopeCompletenessIssueCount=0`、`duplicateInformationObjectContextCount=0`、`sameNameDifferentContextCount=8`。正式 `environment-workbench.json` hash 为 `41aa2b91782fb250eb99effe0f41b4ed7d7f50479d910e3b205abf00b7245bbe`，正式 `environmentBasemap.node-details.json` hash 为 `dcc810a11f1f8dd47c341e17534fba4e997b91762867e8dd800c597396b64d99`。临时核对表更新为 `67` 个上下文、`772` 行、`highRiskContextCount=8`、`mediumRiskContextCount=3`、`duplicateServiceRowCount=42`、`scopeCompletenessIssueContextCount=0`；剩余高风险对象已记录到 `OI-139`。
- 2026-06-12 当前环境映射数据线状态：用户连续修正原始 `data/raw-samples/wiki sample.xlsx` 后，最近一次已完成 16:58 版本重导入与正式替换；审计结果为 `mergedRangeCount=417`、`normalizedRowCount=773`、`objectScope=156`、`objectService=731`、`serviceModule=640`、`serviceMeasure=123`、`moduleSystem=214`、`scopeCompletenessIssueCount=0`、`duplicateInformationObjectContextCount=0`、`sameNameDifferentContextCount=8`。正式 `environment-workbench.json` hash 为 `e7ca633b1ef64e7db9d6eda845518a3dd4d8d4bd3e9c8ce844efe119c8d25789`，正式 `environmentBasemap.node-details.json` hash 为 `a062682b92228d0a623207dfdd0a1dd5c42cb4f98a83bbd188a1126972836a16`。
- 2026-06-12 临时人工核对表状态：`frontend/capability-browser/public/data/review/environment-manual-review-checklist.json` 已更新到 `67` 个上下文、`773` 行；最近一轮为 `highRiskContextCount=15`、`mediumRiskContextCount=3`、`duplicateServiceRowCount=59`、`scopeCompletenessIssueContextCount=0`。操作系统类上下文中仅 `工业控制网络||L3层||L3主机操作系统` 仍残留 `duplicateServiceCount=2`；其他 PC / 移动 / 物理主机操作系统代表上下文已清理为无重复服务。
- 2026-06-12 已完成 `Environment Review Table Evidence Panel Cleanup + Source Scope Writeback`：删除 `数据核对（临时）` 右侧行证据面板；新增 `scripts/apply_scope_supplements_to_source_excel.py` 并将 `service_scope_reverse_check` 补出的作用域写回原始 Excel E 列合并单元格左上角，备份为 `data/exports/worker-verify/source-excel-backup/wiki sample.before-scope-writeback.20260612-155248.xlsx`；共更新 `32` 个 E 列合并单元格，`eColumnMergedRangesPreserved=true`。
- 2026-06-12 当前验证基线：最近一轮通过 `python3 -m py_compile` 覆盖重导入相关 Python 脚本、`node --check` 覆盖 `EnvironmentDataReviewTable.js` 与 `app.js`、`python3 scripts/dev_server_guard.py --status`、环境页 HTTP smoke、`python3 scripts/check_github_data_boundary.py`、`git diff --check`；replacement bundle 临时替换验证在 sandbox 内遇到本机 HTTP 限制，已按权限规则用同命令提升权限后通过并恢复正式文件。
- 2026-06-11 至 2026-06-12 环境归纳表和服务胶囊基线已收口：服务胶囊全局文本只保留 `编号 + 名称`；环境页服务 / 模块 / 措施胶囊复用 `relation-chip technical-chip service-chip / module-chip / measure-chip`；归纳表格按 Excel 行级关系展示，`作用域` 合并为对象唯一文本单元格，`安全技术模块/措施` 与 `安全系统` 按连续相同值做 `rowspan` 合并。
- 2026-06-15 `LC-DT Source Update Audit & Candidate 1.0`：按用户修改后的原始 `data/raw-samples/wiki sample.xlsx` 只读审计 `LC-DT 数据生命周期` 与 `LC-DT 安全技术服务、模块、策略映射表`，新增 `scripts/audit_lcdt_source_update.py` 和 `scripts/build_lcdt_update_candidate.py`，输出目录为 `data/exports/worker-verify/lcdt-source-update/`。本轮生成 `lcdt-lifecycle-table-normalized.json`、`lcdt-service-module-policy-mapping-normalized.json`、`lcdt-table-consistency-audit.json/md`、`lcdt-dictionary-validation-report.json/md`、`lcdt-update-candidate.json/md` 和 `lcdt-issues-for-user-confirmation.json/md`。结果：`lifecycleStageCount=7`、`mappingRowCount=63`、`relationCount=108`、`newMeasureCandidateCount=0`、`securitySystemCategoryCorrectionCount=2`；双表一致性 `stagePassed=6`、`stageMismatch=1`，仅 `加工/使用` 阶段的 `数据流转监测和泄漏防护` 只在策略映射表出现；服务字典校验发现 `I-DI&T-AS.AD-03 数据备份` 未命中服务编号但同名服务为 `I-DI&T-AS.DG-03 数据备份`，另有 `静态数据脱敏 / 数据内容水印` 与字典 canonical 名称不一致；模块短名需确认 `数据加密 -> 数据加密和令牌化`、`数据脱敏 -> 数据脱敏(去标识化)`、`零信任访问代理（独立部署，接替/协同应用系统相关技术模块） -> 零信任访问代理`。已将问题登记为 `OI-141`。本轮未修改正式 lifecycle / maintenance / capability / environment / standards JSON、SQLite、原始 Excel、SVG / Draw.io、schema、前端 UI 或 Environment Mapping 逻辑；验证通过 `py_compile`、LC-DT 审计 / 候选生成、字典标准基线完整性、保护基线回归、JSON 包边界、GitHub 数据边界和 `git diff --check`。
- 2026-06-15 `Protected Dictionary Update - Security Technical Services 1.0`：按用户确认的原始 `安全能力-安全技术服务` 修正，完成安全技术服务字典受控更新、候选包、备份和正式应用。新增 `scripts/audit_security_technical_service_dictionary_update.py`、`scripts/build_security_technical_service_update_candidate.py`、`scripts/apply_security_technical_service_dictionary_update.py`；候选与应用产物位于 `data/exports/worker-verify/security-technical-service-update/`，正式覆盖前备份为 `data/exports/worker-verify/security-technical-service-update/backups/20260615T100842Z/`。字典变化：安全技术服务从 `158` 更新为 `160`；新增 `I-AP&T-PD.DP-01 应用页面水印`、`I-AP&T-PD.DP-02 应用动态数据脱敏`；`I-DI&T-PD.DP-01` 改名为 `数据内容水印`，`I-DI&T-PD.DP-02` 改名为 `静态数据脱敏`；`I-OS&T-AS.DS-01..06` 迁移为 `I-AP&T-AS.DS-01..06`。已同步正式 `maintenance-knowledge.json`、`maintenance-index.json`、`maintenance/services.json`、`maintenance/modules.json`、`capability-tree.json`、`capability-workbench.json`、`lifecycle-workbench.json`、`environment-workbench.json` 和 SQLite；未修改原始 Excel、SVG / Draw.io、schema、标准包、前端 UI 或 Environment Mapping 导入 / triage 逻辑。post-apply 审计：`sourceServiceCount=160`、`currentJsonServiceCount=160`、`currentSqliteServiceCount=160`、`staleReferenceCount=0`、`unknownServiceReferenceCount=0`、`ambiguousReferenceCount=0`。LC-DT 候选包已刷新：`securitySystemCategoryCorrectionCount=0`，`知情同意管理` 与 `隐私安全影响评估` 已从原始模块清单合并单元格确认归入 `数据安全管理与运营`；当前仍需用户确认 `I-DI&T-AS.AD-03 数据备份` 3 处未知编号、`I-DI&T-PD.DP-01/02` 6 处源表旧名称、3 个模块短名归一和 `加工/使用` 阶段 `数据流转监测和泄漏防护` 双表差异。验证通过 `py_compile`、服务字典审计 / 候选 / 应用、LC-DT 审计 / 候选、字典标准基线完整性、保护基线回归、JSON 包边界、提升权限后的 5173 内容 smoke、GitHub 数据边界和 `git diff --check`。
- 2026-06-16 `LC-DT Raw Sample Writeback`：按用户确认将 `data/raw-samples/wiki sample.xlsx` 中 LC-DT 两张原始表定向写回 19 个唯一单元格，修正 `I-DI&T-AS.AD-03 数据备份 -> I-DI&T-AS.DG-03 数据备份`、`应用页面水印 -> 数据内容水印`、`应用动态数据脱敏 -> 静态数据脱敏`、`数据加密 -> 数据加密和令牌化`、`数据脱敏 -> 数据脱敏(去标识化)`、`零信任访问代理（独立部署...） -> 零信任访问代理`，并在 `LC-DT 数据生命周期!I22` 补入 `数据流转监测和泄漏防护`。写回前自动备份为 `data/exports/worker-verify/lcdt-source-update/source-excel-backup/wiki sample.before-lcdt-confirmed-source-updates.20260616T061634Z.xlsx`；格式结构校验通过，两张 LC-DT 表 `max_row/max_col/merged ranges` 保持不变，样式 ID 未变化。验证通过 `python3 scripts/audit_lcdt_source_update.py` 和 `python3 scripts/verify_lcdt_pre_apply_candidate.py`，结果为 `stagePassed=7`、`serviceOnlyLifecycle=0`、`serviceOnlyMapping=0`、`moduleOnlyLifecycle=0`、`moduleOnlyMapping=0`、`serviceMatchedCount=156`、`newMeasureCandidateCount=0`、`replacementAuditBlockingIssues=0`、`replacementAuditReviewItems=0`。
- 2026-06-16 `Work Function Source Rename Update`：按用户授权和原始 `data/raw-samples/wiki sample.xlsx` 修订，确认 `安全工作职能清单!E14=安全合规管理职能`、`E77=企业合规管理职能`，并将 `安全能力-安全管理元素（high level）!J75:J77` 合并单元格左上角 `J75` 写回为 `安全合规管理职能`，源表备份为 `data/exports/worker-verify/work-function-rename-source-update/source-excel-backup/wiki sample.before-work-function-rename-20260616T073640Z.xlsx`，合并区与样式保持不变。新增 `scripts/apply_work_function_rename_from_source.mjs`，按候选包 + 备份方式同步 `maintenance/work-functions.json`、`maintenance/processes.json`、`maintenance/references.json`、`maintenance-knowledge.json`、`capability-tree.json`、`capability-workbench.json` 中对应工作职能名称、来源追踪和高阶管理引用；最新应用产物为 `data/exports/worker-verify/work-function-rename-source-update/formal-apply/20260616T074214Z/`，精确旧值 `合规管理职能` 扫描为 0。验证通过 `audit_protected_dictionary_standard_global_integrity.py`、`audit_protected_dictionary_standard_global_references.mjs`、`audit_dictionary_standard_baseline_integrity.py`、`audit_protected_baseline_no_regression.py`、`frontend_content_smoke_check.mjs --skip-api`、提升权限后的 `frontend_content_smoke_check.mjs --url http://127.0.0.1:5173`、`audit_json_package_boundary.py`、`check_github_data_boundary.py` 和 `git diff --check`；本轮未修改 SQLite、标准包、LC-DT、Environment Mapping 结构、schema 或前端 UI。
- 2026-06-16 `Standards Audit Warning Classification Fix`：按用户复核结果修正 `scripts/audit_protected_dictionary_standard_global_integrity.py` 的标准审计口径。`DSP-SCF-2026` 的 `SCF Controls` 与 `SCF成熟度` 分片共享同一组 `1468` 个 SCF 编号，不再作为重复控制项 warning，仅记录为 `crossTabSharedControlCodeCount=1468` 的说明信息；`NIST-CSF-2.0` 的 `CSF Tiers` 4 行和 `CRF-SAFEGUARDS-CORE-2026` 的成熟度 5 行不再作为 `placeholderControlItems`，改为 `nonControlMetadataRows=9` 的说明信息。重跑后 `global-standards-framework-audit.json` 为 `status=pass`、`duplicateControlsCount=0`、`placeholderControlItemsCount=0`；本轮未修改标准数据、原始 Excel、SQLite、运行包业务数据或前端 UI。验证通过 `py_compile`、全局字典 / 标准审计、字典标准基线完整性和 `git diff --check`。
- 2026-06-16 `Work Function High-Level Source Restore + LC-DT Gate Scope Fix`：按用户复核，`安全能力-安全管理元素（high level）!J75:J77` 中 `安全管理职能` 不应删除，正确值应为 `安全管理职能\n安全合规管理职能`。已在写回前备份 `data/raw-samples/wiki sample.xlsx` 到 `data/exports/worker-verify/work-function-rename-source-update/source-excel-backup/wiki sample.before-restore-security-management-function-20260616T085917Z.xlsx`，并只修改合并单元格左上角 `J75`，`J75:J77` 合并区和样式保持不变。同步修正 `scripts/verify_lcdt_pre_apply_candidate.py`，LC-DT pre-apply 的源表变更审计只检查 `LC-DT 数据生命周期` 与 `LC-DT 安全技术服务、模块、策略映射表` 两张表，不再把工作职能清单 / high level 表的授权改动误判为 LC-DT blocking。验证结果：`audit_capability_management_mappings.py` 从 3 个 issue 修复为 `issue_count=0`；`verify_lcdt_pre_apply_candidate.py` 从 blocked 修复为 `ready_for_user_confirmation` 且 `sourceChangeAuditBlockingIssues=0`；`audit_lcdt_source_update.py` 仍为 `ready`；全局字典 / 标准审计、字典标准基线、保护基线、内容级 smoke、JSON 包边界、GitHub 数据边界和 `git diff --check` 均通过。本轮未修改 SQLite、标准数据、运行包业务数据、Environment Mapping 结构或前端 UI。
- 2026-06-17 `OI-139 C类模块清单确认修正 1/8/9/12`：按用户确认，只修改 `data/raw-samples/wiki sample.xlsx` 的 `安全技术模块清单`，写回前备份到 `data/exports/worker-verify/environment-c-module-catalog-source-apply/20260617T-c1-8-9-12/source-excel-backup/wiki sample.before-c-1-8-9-12.xlsx`。本轮补入 `桌面安全管理（UEM）` 的 6 条 `I-AP` 软件应用服务映射；将 `移动安全管理（MTD）`、`工业主机安全防护` 中重复的第二条 `I-OS&T-AS.AM-01 主机/终端资产清单` 改为 `I-OS&T-AS.AM-02 主机/终端资产安全管控`；将 `DNS威胁检测与响应` 的服务映射改为 `I-NT&T-AD.SA-01 网络高级威胁检测（启发式、行为式）`。写回后 `安全技术模块清单` 合并区域数保持 `219`，`作用域-安全技术服务-安全技术模块映射` 未改且归一化行数保持 `776`；重跑审计后 `moduleServiceMismatchCount` 从 `76` 降为 `48`、`moduleServiceMismatchPatternCount` 从 `12` 降为 `8`，本轮 4 类问题共 `28` 条已消除。未修改 SQLite、正式 JSON、环境映射正式运行包、标准包、前端 UI 或数据库 schema。
- 2026-06-17 `OI-139 C类剩余两类候选表生成`：按用户要求生成剩余 C 类两类候选表，不写回原始 Excel、不替换正式包。新增只读脚本 `scripts/build_environment_c_remaining_candidates.py`，输出目录为 `data/exports/worker-verify/environment-c-remaining-candidates/`。`environment-mapping-candidates.*` 覆盖零信任访问代理 / 控制台拆分、安全Web浏览器、网络准入控制（NAC）、安全接入网关（VPN）、容器网络防火墙，共 `50` 条候选；其中 10 条是零信任合并区域拆分时必须保留的伴随正确行。`module-to-measure-candidates.*` 覆盖 API安全防护与数据加密和令牌化改措施，共 `8` 条候选；候选标明 `API网关` 与 `应用自身数据加解密模块，数据加密后传至数据库存储` 当前未命中维护措施清单，正式写回前需确认是否新增措施。覆盖校验显示剩余 `48` 条 `moduleServiceMismatch` 均已被候选覆盖，`missingMismatchRows=[]`。验证通过 `py_compile`、候选生成和 `git diff --check`。
- 2026-06-17 `OI-139 C类第2项零信任代理/控制台完整展开写回`：按用户纠正后的口径，`作用域-安全技术服务-安全技术模块映射` 中 4 个零信任对象块不再只拆当前已有行，而是按 `安全技术模块清单` 完整展开为 `零信任访问代理` 8 条服务 + `零信任访问控制台` 15 条服务，并分别合并 G 列模块单元格和 H 列安全系统单元格。写回前备份到 `data/exports/worker-verify/environment-zero-trust-full-dictionary/source-excel-backup/wiki sample.before-zero-trust-full-dictionary-20260617T234500.xlsx`；H 列最终补拆前追加备份到 `data/exports/worker-verify/environment-zero-trust-full-dictionary/source-excel-backup/wiki sample.before-zero-trust-h-split-20260617T235200.xlsx`。最终检查行号为 `144:166`、`250:272`、`344:366`、`437:459`；4 个块均为前 8 行 `零信任访问代理`、后 15 行 `零信任访问控制台`，G/H 列均分别按 8 行和 15 行合并，H 列值均为 `零信任网络访问控制`。写回后 `sheetMaxRow=1380`、`mergedRangeCount=427`、`normalizedRowCount=826`；归一化审计 `pendingRelationCount=0`、`invalidRowCount=0`，一致性审计 `moduleServiceMismatchCount=16`、`moduleServiceMismatchPatternCount=6`，剩余 C 类不含零信任相关项。刷新剩余候选表后 `environment-mapping-candidates=8`、`module-to-measure-candidates=8`。未修改 SQLite、正式 JSON、标准包、前端 UI 或数据库 schema。
- 2026-06-18 `OI-139 C类第5/6项复核`：按用户要求只读检查 `安全Web浏览器` 与 `网络准入控制（NAC）`。先重跑 `audit_scope_service_module_mapping.py` 生成当前原始 Excel 归一化行，再重跑 `audit_environment_module_catalog_consistency.py`，避免一致性审计读取旧 normalized rows。当前 `安全Web浏览器` 在 `G523:G525`、`G591:G593`、`G668:G670` 三个合并块均为模块清单定义的 `I-AP&T-PD.PP-01 应用安全访问`、`I-AP&T-AS.IA-03 应用资源授权`、`I-AP&T-PD.TP-03 应用攻击入侵防御`；`网络准入控制（NAC）` 在 `G573:G579`、`G650:G656`、`G744:G750` 三个合并块均包含模块清单定义的 7 条服务，其中 `I-HD&T-AS.IA-04 硬件凭证管理` 已存在。最新逐行审计中这两项 `moduleServiceMismatchRows=0`；整体 `moduleServiceMismatchCount=8`、`moduleServiceMismatchPatternCount=2`，剩余仅 `API安全防护` 6 条和 `数据加密和令牌化` 2 条。未修改原始 Excel、SQLite、正式 JSON、标准包、前端 UI 或数据库 schema；发现 `scripts/build_environment_c_remaining_candidates.py` 仍会输出旧的 5/6 候选，后续使用前需先修候选脚本过滤口径。

- 2026-06-18 `OI-139 C类第4/7项局部复核`：按用户说明只读复核当前原始 `data/raw-samples/wiki sample.xlsx`。先重跑 `audit_scope_service_module_mapping.py`，再重跑 `audit_environment_module_catalog_consistency.py`；当前 `serviceModuleRelationCount=656`、`serviceMeasureRelationCount=175`，说明 `应用层数据加解密` 已有 2 条从模块关系转为措施关系。进一步逐行确认：`云数据中心||业务应用||应用服务层` 的 `F207=I-AP&T-AS.CG-01 应用层数据加解密` 已改为 `G207=应用自身数据加解密模块`，按措施灰底解析；但 `传统数据中心||应用及数据||应用后端` 的 `F474=I-AP&T-AS.CG-01 应用层数据加解密` 仍为 `G474=数据加密和令牌化(应用自带或SDK嵌入，数据加密后传至数据库存储)`，仍触发 1 条 `moduleServiceMismatch`。API 复核确认普通出口边界 `API安全防护` 行 `54:60`、`377:383` 与模块清单服务集合一致；`API网关层` 行 `186:193` 中 `186`、`191` 与 `API安全防护` 字典一致，`187`、`188`、`189`、`190`、`192`、`193` 共 6 条不一致，属于后续可按 `API网关` 措施/候选单独处理的范围。本轮未修改原始 Excel、SQLite、正式 JSON、标准包、前端 UI 或数据库 schema。

- 2026-06-18 `OI-139 C类模块-服务不一致清零复核`：用户再次修改原始 `data/raw-samples/wiki sample.xlsx` 后完成只读审查。按正确顺序先重跑 `audit_scope_service_module_mapping.py`，再重跑 `audit_environment_module_catalog_consistency.py`；当前源表 `sheetMaxRow=1388`、`mergedRangeCount=424`、`normalizedRowCount=834`、`serviceModuleRelationCount=654`、`serviceMeasureRelationCount=181`、`pendingRelationCount=0`、`invalidRowCount=0`。一致性审计结果 `moduleServiceMismatchCount=0`、`moduleServiceMismatchPatternCount=0`，C 类模块-服务不一致已清零。原表确认：`API网关层` 行 `186:192` 保留为 `API安全防护` 且服务集合与模块清单一致，行 `193:197` 独立为 `API网关`；`应用层数据加解密` 两处已改为措施 `应用自身数据加解密模块`，分别位于 `F211/G211` 和 `F478/G478`。剩余人工审查项已转为 `systemModuleMismatch`、coverage gap candidate 和 repeated service expansion，不再是 C 类模块-服务不一致。未修改 SQLite、正式 JSON、标准包、前端 UI 或数据库 schema。

- 2026-06-18 `OI-139 C类修正后环境映射候选导入与问题分析`：按用户要求，基于已修改原始 `data/raw-samples/wiki sample.xlsx` 重新执行环境映射源表归一化、模块清单一致性审计和 environment-workbench reimport candidate 生成；未替换正式 `frontend/capability-browser/public/data/environment-workbench.json`。最新源表导入结果为 `sheetMaxRow=1388`、`mergedRangeCount=424`、`normalizedRowCount=834`、`objectScopeRelations=163`、`objectServiceRelations=778`、`serviceModuleRelations=654`、`serviceMeasureRelations=181`、`moduleSystemRelations=211`、`pendingRelationCount=0`、`invalidRowCount=0`、`duplicateInformationObjectContextCount=0`、`duplicateServicesInObjectContextCount=0`、`scopeCompletenessIssueCount=0`。C 类 `moduleServiceMismatchCount=0`、`moduleServiceMismatchPatternCount=0`，已从初始 `76` 清零；候选包 `environment-workbench-reimport-candidate.json` 生成成功，`issues=0`，但 diff 显示正式包与候选仍有较大差异，尤其 `moduleSystemRelations candidate=211 / current=0`，因此不建议直接替换正式包。新增分析报告 `data/exports/worker-verify/environment-reimport-post-c-fix-analysis.json/md`；剩余重点转为 D 类 `systemModuleMismatch=25`（5 个模式）、G 类系统覆盖提示、E/F 类 coverage gap 候选和 I 类 1:N 展开提示。验证通过 `check_github_data_boundary.py` 和 `git diff --check`；未修改 SQLite、正式 JSON、标准包、前端 UI 或数据库 schema。
- 2026-06-18 `OI-139 D类问题清单生成`：按用户要求从最新 `environment-module-catalog-consistency-review-rows.json` 提取 D 类 `systemModuleMismatch` 清单，生成 `data/exports/worker-verify/environment-d-system-module-issues.json`、`.csv`、`.md`。当前 D 类共 `25` 行、`5` 个模式：`网络边界安全防护 / 工业运维安全审计` 10 行、`终端安全管控 / 主机入侵防御（HIPS）` 8 行、`终端安全管控 / 终端环境感知` 4 行、`移动终端安全管控 / 移动终端环境感知` 2 行、`网络边界安全防护 / DNS威胁检测与响应` 1 行。清单包含 Excel 行号、对象上下文、服务、当前 H 列安全系统、G 列模块和模块清单中的预期安全系统；未修改原始 Excel、SQLite、正式 JSON、标准包、前端 UI 或数据库 schema。
- 2026-06-18 `OI-139 D类前4项原始表写回与第5项复核`：按用户确认，将 D 类前 4 个系统-模块归属问题写回原始 `data/raw-samples/wiki sample.xlsx` 的 `作用域-安全技术服务-安全技术模块映射` H 列；写回前备份到 `data/exports/worker-verify/environment-d-system-module-source-apply/20260618T150006/source-excel-backup/wiki sample.before-d-system-module-1-4-20260618T150006.xlsx`。本轮只拆分 H 列合并区并保留非目标段：`工业运维安全审计 -> 工业网络安全防护`、`主机入侵防御（HIPS） -> 主机安全防护`、`终端环境感知 / 移动终端环境感知 -> 零信任网络访问控制`；第 5 项 `DNS威胁检测与响应` 已由用户在原始数据改为 `网络边界安全防护` 口径，复核后不再报错。写回报告为 `data/exports/worker-verify/environment-d-system-module-source-apply/20260618T150006/d-system-module-source-apply-report.json/md`。重跑审计后 `moduleServiceMismatchCount=0`、`systemModuleMismatchCount=0`、`securitySystemCoverageOrMismatchCount=0`，D 类清单刷新为 0 行；候选导入包已重建，仍未替换正式 `environment-workbench.json`。未修改 SQLite、正式 JSON、标准包、前端 UI 或数据库 schema。

- 2026-06-12 根目录 `progress.md` 再次瘦身；完整重导入过程、候选包、replacement bundle、正式替换、临时核对表和历史验证细节已归档到 `docs/05-archive/progress-history/2026-06.md`。

## 当前问题索引

- `OI-139`：`作用域-安全技术服务-安全技术模块映射` 重导入与环境映射事实源修正，状态 `进行中`；当前完整重复服务-模块/措施-系统关系已清零，重点转为继续人工核对跨表目录一致性缺口、模块/系统不匹配和 UI 字段对齐。
- `OI-142`：`安全技术服务清单` 页面默认分组折叠 / 真实可见性争议，状态 `真实浏览器取证通过 / 待用户页面验收`；正式字典、split 包、index、ViewModel、搜索、默认渲染和真实系统 Chrome 截图取证均已通过。
- `OI-141`：`LC-DT 数据生命周期` 与 `LC-DT 安全技术服务、模块、策略映射表` 源表更新候选，状态 `正式投影已替换 / 待页面验收`；安全技术服务字典和引用已受控更新到 160 条，LC-DT 源表双表一致性、字典校验、误替换专项检查、候选包 normalized diff、备份清单和正式替换后审计均通过。
- `OI-140`：知识库字典与安全标准 / 框架基准包被 core-only 导出覆盖，状态 `已关闭`，已归档到 `docs/05-archive/open-issues-history/2026-06.md`；后续原始数据修改、导入、导出和 workbench 重建必须走 runtime baseline 候选包与 normalized diff 流程，不恢复 Environment Mapping 写入线。
- `OI-143`：安全技术服务字典更新后运行包和派生产物旧服务引用，状态 `已关闭`；2026-06-16 重跑引用一致性审计通过，`issueCount=0`、`formalRuntimeIssueCount=0`、`nonRuntimeIssueCount=0`，已归档到 `docs/05-archive/open-issues-history/2026-06.md`。
- `OI-137`：信息化环境底图，状态 `已修复 / 待人工验收`；正式页已使用共享 Viewer，当前 Draw.io 导入节点 `98`、边 `74`，绑定正式环境数据节点 `92`，候选 `0`、未绑定 `0`、忽略 `6`，当前数据事实源由 OI-139 继续校正。
- `OI-038`：Gartner 与安全职能候选映射需后续人工校对，状态 `待确认`。
- `OI-128`：USER-WRITE-UI-1：批注 / 工作台用户写入入口，状态 `部分完成`。
- `OI-133`：ArchiMate 建模语言页显示效果与加载效率优化，状态 `已关闭`，已归档到 `docs/05-archive/open-issues-history/2026-06.md`。
- `OI-135`：用户库治理与兼容表迁移清理，状态 `正式迁移脚本完成 / 真实库 apply 待显式确认`。
- `OI-138`：关注点关系图谱标签 / 连线避让，状态 `部分修复 / 剩余管理与标准分支问题`。

## 历史索引

| 归档文件 | 内容 |
|---|---|
| `docs/05-archive/progress-history/2026-06.md` | 2026-06 完整执行记录、Open Issues 治理、前端治理、数据口径确认和本轮 progress 瘦身摘要 |
| `docs/05-archive/progress-history/2026-05.md` | 2026-05 完整执行记录、根目录 `progress.md` 瘦身快照和结构治理记录 |
| `docs/05-archive/findings-history/2026-05.md` | 2026-05 历史发现 |
| `docs/05-archive/open-issues-history/2026-06.md` | 已关闭 Open Issues 长记录 |
| `docs/05-archive/context-slimming-2026-05-15/` | 2026-05-15 上下文瘦身快照 |

## 维护规则

- 本文件只保留最近状态、最近 5-10 条重要执行和恢复入口；超过 120 行时继续归档到 `docs/05-archive/progress-history/`。
- 详细过程、长命令输出和阶段性历史不写入根目录 `progress.md`。
