# progress.md

本文件是当前会话恢复入口，只保留最近状态、最近完成事项、关键验证和历史索引。完整执行历史已归档到 `docs/05-archive/progress-history/2026-05.md`。

## 当前状态（2026-05-28）

- 当前分支：`codex-frontend-backend-separation-closure`。
- 当前主线：Frontend Baseline 1.0 四页关系工作台校正；重点仍是已导入 Sheet 的业务语义复核、前端关系展示校正、数据契约治理和字段边界收口。
- Delivery Bundle 1.0-alpha 第一优先级已改为 `.zip` 解压即用版；目标是后端可执行文件 + 前端静态资源 + base/user 双库 + start/stop 脚本 + manifest + logs + diagnostics，用于内部团队快速分发和验证。
- Delivery Bundle 1.0 设计团队沟通材料已启动，当前优先支持内部传递和评审。
- 固定预览入口：`http://127.0.0.1:5173/`。多个线程并行验证时可以临时使用其它端口，但验证后必须关闭；面向用户的常驻预览页只保留 `5173`。
- 本轮处理开发体验问题：前端验证频繁弹出 `Google Chrome 意外退出`，以及固定 `5173` 刷新后偶尔看不到最新页面。

## 最近完成事项

- 2026-05-29 代码全量 review 后首轮修复：修复 ZIP 本地写接口安全边界、非 loopback 监听风险、诊断包脱敏不足、`dataClient` 环境旧接口、maintenance API section 漂移和轻量 smoke route 覆盖缺口。新增 / 更新 `OI-099` 至 `OI-104` 为已修复；同步更新 ZIP runtime / Windows 构建 / user DB 文档中的写接口 token 说明。固定 `5173` 项目服务已恢复并验证 `/environment-mapping` 直达路由。
- 2026-05-29 安全技术服务清单口径调整：按用户要求将服务表“适用作用域”列改为“归属安全能力-关注点”；`ALL` 服务表内分组显示为 `安全运行类安全技术服务`，并排在 `M` 开头服务前；`M` 开头服务单独归入 `安全管理类安全技术服务`，不再混入用户作用域分组；普通作用域分组内服务继续按安全能力 / 关注点顺序排序。同步更新服务页说明文案、摘要徽标、详情面板和 `docs/04-user-guide/special-maintenance-pages-prototype-brief.md`。
- 2026-05-28 未设计页面占位化：按用户确认规则，将 `SAPD成熟度评估`、`安全管控模式设计方法`、`成熟度模型使用方法`、`其他指南`、`Hype Cycle`、`其他知识目录`、`其他标准 / 框架` 切到独立 `placeholder` 视图；未设计页面不再复用内容页、维护页或其他工作台结构。新增 `placeholderWorkspace` 和占位页样式，保留已设计页面（安全能力映射、信息化环境映射、LC-AP、LC-DT、知识目录、标准框架）的原有渲染路径。
- 2026-05-27 high-level 新流程复核与同步：按 `安全能力-安全管理元素（high level）!H` 与 `安全职能流程清单（完善L4）!E` 做规范化精确比对。用户修改原始数据后，当前 high-level 表 78 个唯一 L3 流程参考已全部存在于流程目录主表，原 6 条待复核项已解除；随后重新 stage / approve 第二批 job `6bfc916c-efc9-4b6b-805c-9ac7c279485d`，重新导出 `maintenance-knowledge.json`、`capability-tree.json`、`capability-workbench.json` 和 second-batch summary。当前 `maintenance-knowledge.json` 已同步为 `process_references=78`，`攻防演练/沙盘推演流程` 已进入流程目录导出包。
- 2026-05-27 安全知识全量数据审计：按源表 -> parser -> 数据包 -> 页面契约链路只读核对 `wiki sample.xlsx`、`maintenance-knowledge.json`、`lifecycle-knowledge.json`、`capability-workbench.json`。通过项：安全技术模块目录 102/102、安全管理工作映射审计通过、工作职能 86/86、GB/T 27/27、Gartner 28/28。发现并记录 `OI-093` 至 `OI-096`：作用域主数据被关联表覆盖、流程目录混入 high-level 并发生顿号误拆、措施目录混入 LC-AP/LC-DT 措施、应用系统目录顺序丢失。新增审计报告 `docs/06-implementation/security-knowledge-data-audit-2026-05-27.md`。本轮未修改 Excel、数据库、ETL、前端运行代码或 public data JSON。
- 2026-05-27 安全知识 093-096 确认后修复：按用户确认完成数据边界收口。`安全能力作用域清单` 只按 `安全能力作用域目录` 导出 9 条，`ALL` 和关联表短标题不进目录主表；`安全职能流程清单` 只展示 `安全职能流程清单（完善L4）` 原表显式 L3 流程，high-level 流程不补入目录；`安全技术措施目录` 保留 4 条 LC-AP / LC-DT 生命周期措施并补充 `source_label` / `source_kind` / `mapping_status_label`，前端显示来源标签和待补充关联；`应用系统目录` 按 `LC-AP 应用安全开发生命周期元素目录` 来源行排序。已重新导出 `maintenance-knowledge.json`、`lifecycle-knowledge.json`，并更新 `OI-093` 至 `OI-096` 为已修复。
- 2026-05-27 安全知识 093-096 复查：按用户要求重新执行数据包摘要、管理映射审计、093-096 源表对数据包定点审计和安全知识页面 smoke。复查结论为通过：`scope_types=9`、`process_references=77`、`security_technical_measures=32`、`application_system_types=3`、`application_components=13`；`/knowledge/scopes`、`/knowledge/technical-measures`、`/knowledge/management-workflows`、`/knowledge/application-systems` smoke 均通过。
- 2026-05-26 安全能力映射页加载慢修复：新增 `/api/v1/capabilities/workspace-initial` 轻量初始投影和 `workspace-projection?focus_id=<id>` 按关注点投影，直接进入 `#/capability-mapping` 约 `1.0MB`，业务 fetch 仅 `workspace-initial≈204KB`；`OI-081` 已修复。
- 2026-05-26 全局刷新状态恢复：刷新时恢复当前路由、当前页面选中对象、目录展开状态、维护页 Tab、指南页选中内容和生命周期选择；如果 URL 明确指定页面则优先尊重 URL。
- 2026-05-26 安全能力映射页数据语义修正：`OI-085`、`OI-086`、`OI-087`、`OI-088` 已修复，分别覆盖小投影加载中状态、无服务矩阵计数、管理 high level `/` 空值继承和标准 / 框架空态样式。
- 2026-05-27 开发体验修复：`OI-089` 已修复，`frontend_smoke_check.mjs` 改为优雅关闭 headless Chrome；本地静态预览禁用浏览器缓存；`dev_server_guard.py` 增加 `--restart` 并可清理旧 `http.server` / 失效 Python 预览占用。
- 2026-05-27 Chrome 崩溃报告二次止血：系统 Google Chrome 148 在 Codex 拉起的 headless 场景下仍会触发 macOS `SIGABRT` 报告，因此 `frontend_smoke_check.mjs` 改为默认不启动系统 Chrome，只做轻量 HTTP/API smoke；只有用户明确同意时才允许 `--allow-system-chrome`。
- 2026-05-27 全局前端预览规则：项目级规则已明确，前端展示和用户验收默认只使用 `http://127.0.0.1:5173/`；修改前端后必须确认该端口热刷新到最新文件，失效时用 `python3 scripts/dev_server_guard.py --restart`，不再把 `python -m http.server 5173` 作为常驻服务。
- 2026-05-27 整体能力节点刷新加载修复：`OI-090` 已修复。安全能力映射页刷新恢复到 `T-OF 进攻 Offense`、`安全治理能力 G`、`安全管理能力 M` 等非关注点节点时，会自动补载完整 `capability-workbench` 并重渲染，避免轻量初始投影导致整体关系数据空白。
- 2026-05-27 标准 / 框架空态微调：按用户截图将 `标准 / 框架映射` 空矩阵行收敛为单句 `暂无条款/控制项对应能力关注点`，不再显示额外说明文字。
- 2026-05-27 关注点刷新管理视角修复：`OI-091` 已修复，`workspace-projection?focus_id=...` 同时支持关注点 UUID 和业务 code，前端不再用轻量首屏空壳 workbench 覆盖后端投影，`T-OF.AT-02` 刷新后可恢复管理映射。
- 2026-05-27 管理视角 ETL 全量审计与修复：`OI-092` 已修复。`安全能力-安全管理元素（high level）` 解析现在按 Excel 合并单元格锚点值读取 L2 流程组、L3 流程参考和四层职能，L3 流程名不再按中文顿号拆分，显式 `/` 继续清空对应职能层级；新增 `scripts/audit_capability_management_mappings.py` 做全量回归审计。
- 2026-05-27 LC-DT 页面数据全面复核：已按源表 -> parser -> `lifecycle-knowledge.json` -> `lifecycle-workbench.json` -> `/data-security` 页面链路检查继承、换行拆分、漏导入和模块 / 措施归类；业务数据核对通过，7 个过程、31 个场景、74 个过程级服务关联、29 个模块关联、1 个措施关联均与源表一致，parser validation 为 0，workbench 端点缺失 0；发现来源证据重复记录治理问题，已新增 `OI-092`，不影响当前主展示。
- 2026-05-27 标准 / 框架模块全量数据复核：新增 `scripts/audit_standard_framework_data.py`，按原始 workbook、标准拆包 JSON 和 capability-first 标准映射表检查 7 个页面 / 8 个源表的行数、唯一编号、继承字段、错误换行、漏导入、索引 dataPath 和映射双向一致性；审计报告已写入 `data/processed/reviews/standard-framework-full-data-audit-20260527.md`，问题明细 CSV 只有表头。

## 最近验证

- 2026-05-29 review 修复验证：`python3 -m py_compile scripts/run_local_server.py scripts/check_bundle_runtime.py scripts/export_diagnostics.py src/sapd_wiki/api_server.py`、`node --check frontend/capability-browser/dataClient.js`、`node --check scripts/frontend_smoke_check.mjs`、`git diff --check`、`python3 scripts/check_github_data_boundary.py` 均通过；临时 ZIP bundle 安全回归通过（无 token 403、恶意 Origin 403、错误 Content-Type 415、正确 token 200、非 loopback host 检查失败、恢复 loopback 检查通过）；诊断包脱敏检查通过；`dataClient` 环境方法 Node VM 定点测试通过；maintenance API 定点 section 测试通过；固定 `5173` 重启后 `/environment-mapping` 轻量 smoke 通过。
- 2026-05-29 安全技术服务清单验证：`node .agents/skills/impeccable/scripts/load-context.mjs` 通过，`register=product`；`node --check frontend/capability-browser/viewModels.js`、`TechnicalServiceMaintenanceTable.js`、`MaintenanceShell.js`、`AppShell.js` 均通过；Node 定点 ViewModel 断言确认表内分组包含 `安全运行类安全技术服务` 和 `安全管理类安全技术服务`，且运行类位于管理类之前，管理类组内服务均为 `M` 开头；`git diff --check` 通过；固定 `5173` 项目服务已恢复，`node scripts/frontend_smoke_check.mjs --page maintenance --route /knowledge/technical-services --url http://127.0.0.1:5173/` 轻量 HTTP smoke 通过，未启动系统 Chrome。
- 2026-05-28 未设计页面占位化验证：`node .agents/skills/impeccable/scripts/load-context.mjs` 通过，`hasProduct=true`、`hasDesign=true`；`node --check frontend/capability-browser/app.js`、`node --check frontend/capability-browser/components/AppShell.js`、目标文件 `git diff --check` 通过；固定 `5173` 服务沙箱外检查通过，首页和 `workspace-projection` 均为 `200 OK`。
- 2026-05-27 high-level 新流程复核与同步验证：`stage-excel --sheets second-batch` 输出 `objects_staged=512`、`process_reference=78`、`validations=[]`；`approve-import 6bfc916c-efc9-4b6b-805c-9ac7c279485d` 输出 `items_updated=512`、`items_deprecated=5`、`relations_created=45`、`relations_deleted=50`、`warnings=[]`；导出后 `maintenance` data package summary 通过，`process_references=78`；定点 `openpyxl` + JSON 审计输出 `high_level_not_in_directory=[]`、`directory_not_in_maintenance_export=[]`、`attack_drill_in_export=true`；`audit_capability_management_mappings.py` 通过，`issue_count=0`；固定 `5173` 项目服务已恢复，`/knowledge/management-workflows` smoke 通过，`consoleIssues=0`、`bodyOverflowX=0`。
- 2026-05-27 安全知识全量数据审计验证：`python3 scripts/data_package_summary.py --package maintenance`、`python3 scripts/data_package_summary.py --package lifecycle`、`python3 scripts/data_package_summary.py --package capability-workbench`、`python3 scripts/audit_capability_management_mappings.py`、直接 parser validation 脚本均通过；直接 `openpyxl` + JSON 审计输出已写入 `docs/06-implementation/security-knowledge-data-audit-2026-05-27.md`。
- 2026-05-27 安全知识 093-096 修复验证：`python3 -m py_compile src/sapd_wiki/exports.py`、`node --check frontend/capability-browser/viewModels.js frontend/capability-browser/components/TechnicalMeasureMaintenanceTable.js`、`python3 scripts/sapd_wiki.py export-maintenance-knowledge`、`python3 scripts/sapd_wiki.py export-lifecycle-knowledge`、`python3 scripts/data_package_summary.py --package maintenance`、`python3 scripts/data_package_summary.py --package lifecycle`、`python3 scripts/audit_capability_management_mappings.py`、定点 `openpyxl` + JSON 093-096 审计、目标文件 `git diff --check` 均通过；修复后 `scope_types=9`、`process_references=77`、`security_technical_measures=32`、`application_system_types=3`、`application_components=13`。固定 `5173` 旧服务清理需提升权限，已用 `python3 scripts/dev_server_guard.py --fix-duplicates --start` 恢复项目服务；`/knowledge/scopes`、`/knowledge/technical-measures`、`/knowledge/management-workflows`、`/knowledge/application-systems` smoke 均通过，`consoleIssues=0`、`bodyOverflowX=0`。
- 2026-05-27 安全知识复查命令：`python3 -m py_compile src/sapd_wiki/exports.py`、`node --check frontend/capability-browser/viewModels.js`、`node --check frontend/capability-browser/components/TechnicalMeasureMaintenanceTable.js`、`python3 scripts/data_package_summary.py --package maintenance`、`--package lifecycle`、`--package capability-workbench`、`python3 scripts/audit_capability_management_mappings.py`、定点 `openpyxl` + JSON 审计、`git diff --check`、4 个 `node scripts/frontend_smoke_check.mjs --page maintenance --route ...` 均通过；本轮只检查并更新 `progress.md`，未改业务代码或数据包。
- 2026-05-26 capability smoke：固定 `5173` 页面通过，刷新选中关注点可恢复并按需加载小投影，未拉完整 `capability-workbench` 大包。
- 2026-05-26 lifecycle / data-lifecycle / guides / maintenance 等页面近几轮 smoke 均通过；详细命令和截图路径见 `docs/05-archive/progress-history/2026-05.md`。
- 2026-05-27 开发体验验证：`node --check scripts/frontend_smoke_check.mjs`、`python3 -m py_compile src/sapd_wiki/api_server.py scripts/dev_server_guard.py`、`python3 scripts/dev_server_guard.py --restart`、capability smoke、`curl -I http://127.0.0.1:5173/app.js` 和目标文件 `git diff --check` 通过；固定端口 `5173` 当前为项目服务，`app.js` 已返回 `Cache-Control: no-store`。
- 2026-05-27 Chrome 二次止血验证：`node --check scripts/frontend_smoke_check.mjs` 通过；`node scripts/frontend_smoke_check.mjs --page capability --url http://127.0.0.1:5173/` 轻量模式通过，输出 `browserSkipped=true`，未启动系统 Chrome。
- 2026-05-27 全局预览规则文档更新：`AGENTS.md`、`CURRENT_STATE.md`、`docs/07-governance/codex-performance-workflow.md`、`frontend/capability-browser/README.md` 已同步；待执行 `git diff --check`。
- 2026-05-27 整体能力节点刷新验证：`node --check frontend/capability-browser/app.js`、`node --check scripts/frontend_smoke_check.mjs` 通过；用 `--workspace-state-json` 模拟刷新恢复到 `T-OF`、`安全治理能力 G`、`安全管理能力 M`，三次 capability smoke 均通过，`capabilityMap=true`、`consoleIssues=0`、`bodyOverflowX=0`。
- 2026-05-27 标准 / 框架空态微调验证：`node --check frontend/capability-browser/components/CapabilityLocalRelationMap.js`、目标文件 `git diff --check`、组件定点渲染断言通过；`python3 scripts/dev_server_guard.py --restart` 已恢复 `5173` 项目服务；capability smoke 受本机 Chrome DevTools target 不可用影响未完成。
- 2026-05-27 关注点刷新管理视角验证：`python3 -m py_compile src/sapd_wiki/api_server.py`、`node --check frontend/capability-browser/viewModels.js`、`python3 scripts/dev_server_guard.py --restart`、`T-OF.AT-02` API 定点检查、刷新 ViewModel 模拟和 capability 定点 smoke 通过；API 返回 `managementRows=1`、安全工作 `入侵目标选择及评估分析`、职能层级 `1/1/2/0`。
- 2026-05-27 管理视角 ETL 全量审计验证：重新 `stage-excel --sheets second-batch` 并 approve `f270bf0f-e2ae-4003-8c7f-f0bdec4c5bcb`，审批结果 `items_updated=517`、`relations_created=21`、`relations_deleted=22`、`warnings=[]`；重新导出 `capability-tree`、`capability-workbench`、second-batch summary；`python3 scripts/audit_capability_management_mappings.py` 通过，`issue_count=0`、`placeholder_leak_count=0`。
- 2026-05-27 LC-DT 数据复核验证：`python3 scripts/data_package_summary.py --package lifecycle-workbench`、`--package lifecycle`、LC-DT 源表 / 投影定点 Python 审计、`node --check frontend/capability-browser/viewModels.js`、`node --check frontend/capability-browser/components/ApplicationSecurityLifecycle.js`、`python3 scripts/dev_server_guard.py --fix-duplicates --start`、`node scripts/frontend_smoke_check.mjs --page data-lifecycle --route /data-security --url http://127.0.0.1:5173/ --debug-port 9340`、`git diff --check` 通过。
- 2026-05-27 LC-AP 数据全面复核：对 `LC-AP 应用安全开发生命周期` 源表 8 个阶段、13 个主展示字段与 `lifecycle-workbench` / ViewModel 做字段级比对，阶段数、开发模式填充色继承、开发技术服务/模块、安全技术服务、安全技术模块/措施均已核对；发现并修复 `OI-092`，AP-03 `应用程序静态安全测试（安全函数和组件库）` 不再因规范关系回填而从页面少显示；`node --check`、`git diff --check` 和固定 `5173` lifecycle smoke 通过，`5173` 项目服务已恢复。
- 2026-05-27 标准 / 框架全量复核验证：`python3 scripts/audit_standard_framework_data.py --stamp 20260527` 通过，`errors=0`、`warnings=0`、原始 / JSON 行数均一致（等保 113、CIS 153、CSF core 106、ISO 93、DSP 1468、CRF core 476、CRF maturity 5、NIST 1007），标准映射 `sourcePairs=2288`、`projectionPairs=2288`、缺失 / 额外均为 0；`python3 -m py_compile scripts/audit_standard_framework_data.py`、`python3 scripts/data_package_summary.py --package standards` 通过。
- 2026-05-28 信息化环境页面双 Tab 原型：`/environment-mapping` 外层改为单区域，不再显示独立环境对象树；默认 Tab 为撑满区域的 HTML 网络物理拓扑首页，节点使用 `data-environment-*` 可点击切换焦点；第二个 Tab 内部为左侧环境对象树 + 右侧现有映射表结构，点击对象、环境子类或表格行后保持在第二个 Tab，不再跳回拓扑页。同步适配 `environment-workbench` 契约数据到环境树与映射行；验证 `node --check`、ViewModel 定点构建、组件渲染断言、`git diff --check`、`data_package_summary --package environment-workbench`、固定 `5173` 服务检查和 environment 轻量 smoke 通过。
- 2026-05-28 环境对象树交互对齐：第二个 Tab 内的环境对象树已从 `details` 全展开改为安全能力映射树同款逐级展开结构，层级为 `E0` 环境、`E1` 环境子类、`E2` 信息化对象；搜索时展示全部匹配层级，点击展开按钮不切换对象，点击对象仍保持在第二个 Tab。验证 `node --check`、环境树渲染断言、`git diff --check`、`data_package_summary --package environment-workbench`、固定 `5173` 服务检查和 environment 轻量 smoke 通过。
- 2026-05-28 环境目录区域收起展开：第二个 Tab 的环境对象树目录区已增加“收起目录 / 展开目录”交互，效果对齐安全能力目录；收起后右侧现有映射表扩展显示，左侧保留竖向展开入口，状态写入本地工作区恢复。验证 `node --check`、目录折叠渲染断言、`git diff --check`、`data_package_summary --package environment-workbench`、固定 `5173` 服务检查和 environment 轻量 smoke 通过。
- 2026-05-28 环境映射表完整显示与技术对象 chip 统一：环境映射表和能力关注点映射表中的安全技术服务、安全技术模块 / 措施默认完整显示，不再折叠为 `+N`；安全技术模块与安全技术措施统一为绿色技术对象 chip，缓存版本已同步更新。验证 `node --check`、完整 chip 渲染断言、`git diff --check`、固定 `5173` 服务检查和 environment 轻量 smoke 通过；系统 Chrome 回归仍按规则跳过。
- 2026-05-28 安全技术服务清单与作用域映射修复：完成 `作用域-安全技术服务-安全技术模块映射`、`信息化环境-信息化对象-安全作用域映射` 数据检查，发现各 123 条服务行的 E 列继承作用域未覆盖服务编号作用域；parser 已按安全技术服务编号补入对象作用域，服务自身只挂编号作用域，导出层删除“无匹配时复制全部对象服务”的旧兜底。新增安全知识页 `安全技术服务清单`，服务在表格 chip 中带编号显示；修复模块因 `name` 字段被误判为安全技术措施的问题。重新 stage/approve 两张作用域映射表，导出 `maintenance-knowledge.json`、`shared-lookups.json`、`capability-workbench.json`、`environment-workbench.json`、`lifecycle-workbench.json`；`maintenance` 当前 `security_technical_services=159`，`environment-workbench` 服务作用域错配关系为 0。按用户标注移除服务清单页内 tab，并从模块 / 措施清单页内 tab 中移除“安全技术服务目录”；新增 `OI-097` 已修复，`OI-098` 已按用户澄清修正为多来源措施清单口径。
- 2026-05-28 安全技术服务清单表内作用域分组：修复服务名称下方误显示的前端状态“待补充”，该值不是原始业务字段；按用户纠正撤销左侧作用域目录方案，服务清单保持完整表格，在表格内部按作用域生成汇总分组行并支持展开 / 收起，分组内服务按能力树中的能力 / 关注点顺序排序。后续类似“收纳总结展开”默认优先落在表格内部，不擅自改成独立目录。
- 2026-05-28 安全技术服务清单字段口径微调：按用户标注将服务清单表头“实现安全技术模块”改为“关联技术模块”，并同步服务详情与统计条中的同类文案，统一表达为关联关系。
- 2026-05-28 Delivery Bundle 1.0 方案评估：新增 `docs/01-architecture/delivery-bundle-1.0-prebuilt-database.md`，明确面向普通用户的 macOS / Windows 安装包与 zip 解压即用双形态交付、内置本地后端 / Tauri command 评估、预构建 SQLite 种子库、前端数据包、manifest、初始化 / 修复 / 升级策略、平台签名与安全边界；同步更新 `task_plan.md` 和 `findings.md`。本轮未修改前端、ETL、SQLite schema、数据库、`public/data/*.json` 或发布资源。
- 2026-05-28 Delivery Bundle 1.0 文档验证：`git diff --check` 通过；`rg` 检索确认新文档、`task_plan.md`、`findings.md`、`progress.md` 均可定位 `Delivery Bundle 1.0`、预构建 SQLite、macOS / Windows、`sapd_wiki.seed.sqlite3` 和 `manifest.json` 关键入口。本轮未运行前端 smoke 或数据包生成命令。
- 2026-05-28 Delivery Bundle 1.0 设计沟通简报：新增 `frontend/design-handoff/implementation-specs/delivery-bundle-1.0-design-brief-2026-05-28.md`，把打包交付方案转成设计团队可讨论的用户旅程、必要界面状态、信息架构、推荐文案、视觉语气、设计交付物、非本轮范围和评审问题；同步更新 `task_plan.md`、`findings.md` 和本文件。未修改前端运行代码、样式、数据包或 SQLite。
- 2026-05-28 ChatGPT 反馈回收与 Delivery Bundle 正式设计升级：采纳“普通用户交付版不是一键导入版，而是预构建知识库运行版”的主结论；将下载草案引入 `docs/09-delivery/delivery-bundle-1.0-prebuilt-database.md` 作为正式设计文档；明确 `sapd_wiki_base.sqlite3` 只读基础库、`sapd_wiki_user.sqlite3` 可写用户库、`stable_key` / deterministic ID、`base_id_redirects`、`base-manifest.json`、用户库 schema / migration、base/user 合并 read model、zip alpha、Tauri sidecar 和诊断包边界；同步更新架构评估入口、设计团队简报、`task_plan.md` 和 `findings.md`。未修改前端运行代码、ETL、SQLite schema 或生成数据包。
- 2026-05-28 Delivery Bundle 1.0-alpha ZIP 主目标重定义：新增 `docs/09-delivery/zip-bundle-1.0-alpha-design.md`，将 1.0 第一优先级锁定为 ZIP 解压即用版；明确 P0 为 ZIP，P1 为 macOS / Windows 安装包，P2 为自动更新 / 签名 / 企业管理，P3 为 Docker / Server / 多人协作；ZIP alpha 不先卡 Tauri，采用后端可执行文件提供 API 和前端静态资源、浏览器访问 localhost、包内携带 base/user 双库、manifest、start/stop 脚本、logs 和 diagnostics。同步更新 `CURRENT_STATE.md`、`task_plan.md` 和 `findings.md`。
- 2026-05-28 Delivery Bundle ZIP 口径验证：`git diff --check` 通过；检索确认交付文档、设计简报、计划和发现中不再残留“zip 不是主路径”“alpha 必须 Tauri”“README-USER”等旧口径。
- 2026-05-28 ZIP-DB-1 双数据库与 manifest 最小工程闭环：新增 `docs/09-delivery/zip-bundle-1.0-alpha-runtime-design.md`、`base-manifest-contract.md`、`user-database-minimum-schema.md`，明确 ZIP alpha bundle root、`base-manifest.json` 契约、`sapd_wiki_user.sqlite3` 最小 schema、base/user 只读 / 可写边界、启动检查、端口切换和诊断包内容；新增 `scripts/create_user_db.py`、`check_bundle_runtime.py`、`export_diagnostics.py`、`build_zip_bundle.py`、`start-windows.bat`、`start-macos.command` 最小脚手架；同步更新 `scripts/README.md`、`task_plan.md` 和 `findings.md`。本轮未修改 `frontend/capability-browser/` 主展示逻辑、`src/sapd_wiki/*`、ETL、SQLite schema 或生成数据包。
- 2026-05-28 ZIP-DB-1 验证：`python3 -m py_compile scripts/create_user_db.py scripts/check_bundle_runtime.py scripts/export_diagnostics.py scripts/build_zip_bundle.py` 通过；`git diff --check` 通过；在 `/private/tmp/sapd_zip_alpha_check` 构造最小 base SQLite、后端占位文件和前端占位文件后，`scripts/build_zip_bundle.py --make-zip` 成功生成 `SAPD-Wiki-0.1.0-alpha-win-x64.zip`；`scripts/check_bundle_runtime.py --create-user-db --json` 返回 `ok=true`、`selected_port=18765`，base hash、user schema、logs 可写均通过；`scripts/export_diagnostics.py` 成功生成脱敏诊断包。
- 2026-05-28 ZIP-RUN-1 分平台本地运行闭环：新增 `scripts/run_local_server.py` 作为后续平台运行组件源码入口；增强 `scripts/build_zip_bundle.py`，输出 `SAPD-Wiki-v0.1.0-win-x64.zip`、`SAPD-Wiki-v0.1.0-mac-arm64.zip` 等分平台 ZIP，按平台生成 `SAPD-Wiki-Backend.exe` 或 `SAPD-Wiki-Backend`、start/stop 脚本和 diagnostics 脚本；增强 `check_bundle_runtime.py` 检查平台和运行组件；增强 `export_diagnostics.py` 导出 `runtime-state.json` 和当前端口信息；新增 `scripts/stop-windows.bat`、`scripts/stop-macos.command`；同步更新 Delivery 文档、`CURRENT_STATE.md`、`task_plan.md`、`findings.md` 和 `scripts/README.md`。本轮未修改 `frontend/capability-browser/` 主展示逻辑、`src/sapd_wiki/*`、Tauri、安装器、签名、Docker 或用户端 ETL。
- 2026-05-28 ZIP-RUN-1 验证：开工前已执行 `git status --short --branch` 和 `git diff --stat`，确认工作区存在既有前端和 `src/sapd_wiki/*` 改动；`python3 -m py_compile scripts/create_user_db.py scripts/check_bundle_runtime.py scripts/export_diagnostics.py scripts/build_zip_bundle.py scripts/run_local_server.py` 通过；在 `/private/tmp/sapd_zip_run_11rgch1j` 构造临时 base SQLite、前端 `index.html` 和 macOS 后端 wrapper 后，`build_zip_bundle.py --platform mac-arm64 --make-zip` 输出 `SAPD-Wiki-v0.1.0-mac-arm64.zip`，`check_bundle_runtime.py --create-user-db --json` 返回 `ok=true`；临时占用 `18765` 后启动本地服务，runtime-state 显示切换到备用端口 `18767`；`curl /` 返回前端静态页，`curl /api/v1/base/summary` 返回 `knowledge_items=1`，`curl /api/v1/base/items?limit=3` 返回测试对象；`POST /api/v1/user/favorites` 成功写入 `user_favorites`，用户库存在 `upsert_favorite` change log，base DB sha256 保持 `681288002cc9a5dc7078662eb472742fc505f57127ddbcce264067188c333e68`；`export_diagnostics.py` 成功生成脱敏诊断包；构造缺失 base DB 的 bad bundle 后 `run_local_server.py --check-only` 启动失败并写入 `logs/runtime.log`；Windows 形态 builder 输出 `SAPD-Wiki-v0.1.0-win-x64.zip`，runtime check 通过。端口服务验证因 macOS sandbox 限制使用了经批准的本机临时进程，验证后已停止。
- 2026-05-28 ZIP-PACK-1 分平台真实运行组件打包：选择 PyInstaller 作为 alpha 主打包工具，新增 `scripts/package_backend_pyinstaller.py`、`scripts/package_backend_windows.ps1` 和 `docs/09-delivery/windows-zip-build-guide.md`；`build_zip_bundle.py` 增加 `--backend-binary`，真实运行 ZIP 不再允许缺省生成后端占位，结构验证包必须显式 `--allow-placeholder`；macOS arm64 后端已打包为 Mach-O `SAPD-Wiki-Backend`，大小约 9.1MB，并组装为 `/private/tmp/sapd_zip_pack/bundle/SAPD-Wiki-v0.1.0-mac-arm64.zip`。Windows 构建链路已就绪，但当前机器不是 Windows，未生成或验证真实 `SAPD-Wiki-Backend.exe`。
- 2026-05-28 ZIP-PACK-1 验证：PyInstaller 初次构建因沙箱写 `~/Library/Application Support/pyinstaller` 失败，已将 `PYINSTALLER_CONFIG_DIR` 指向 `/private/tmp/sapd_zip_pack/pyinstaller-config` 后构建通过；真实 macOS ZIP 解压到 `/private/tmp/sapd_zip_pack/uat/SAPD-Wiki-v0.1.0-mac-arm64` 后，从 `start-macos.command --no-browser` 启动真实 `SAPD-Wiki-Backend`，缺失 user DB 自动重建，临时占用 `18765` 时服务切换到 `18766`；`curl /` 返回前端 `index.html`，`/api/v1/base/summary` 返回 `knowledge_items=5241`、`knowledge_relations=7678`，`POST /api/v1/user/favorites` 写入 user DB，base DB sha256 保持 `3e1f50d9af9700e2ab0bde7dd46dd4cad05514faefc8ca0cbd61f718c278dd80`；诊断包 `sapd-wiki-diagnostics-20260528-182052.zip` 只含 summary、startup check、platform、runtime state、database-files、redaction note、manifest、config、runtime log；临时移走 base DB 后真实后端 `--check-only` 返回失败并报告 `base_db_exists=false`。验证进程和临时端口占用已停止。
- 2026-05-29 ZIP-UAT-0 macOS alpha 试发准备：新增 `docs/09-delivery/zip-uat-0-internal-trial-guide.md`、`zip-uat-0-checklist.md`、`zip-uat-feedback-template.md`，固化 macOS arm64 alpha 包指纹、内部试用步骤、UAT checklist 和问题反馈模板；更新 `windows-zip-build-guide.md`，明确 Windows x64 仍为构建脚本就绪 / 未实机验证，并补充 Windows 实机验收记录和安全软件拦截记录；同步更新 ZIP alpha 设计文档、runtime 设计文档、`CURRENT_STATE.md`、`task_plan.md`、`findings.md` 和 `scripts/README.md`。本轮未修改前端主展示逻辑、`src/sapd_wiki/*`、ETL、正式数据输出、Tauri、安装包、签名、自动更新或 Docker。
- 2026-05-29 ZIP-UAT-0 验证：开工前执行 `git status --short --branch` 和 `git diff --stat`；重新生成 macOS arm64 试发包 `/private/tmp/sapd_zip_uat0/bundle/SAPD-Wiki-v0.1.0-mac-arm64.zip`，当前 `build_time=2026-05-29T02:21:47Z`、ZIP hash `bb216ce25d3e6e76db16ce5d84017f3e799ff2963cac52110bba5fc97edc90f9`、ZIP size `190,149,724 bytes`、backend hash `6c538044ec337a112127191ba1834aa233dabf3f5c624110594cda0e6ed71419`、base hash `3e1f50d9af9700e2ab0bde7dd46dd4cad05514faefc8ca0cbd61f718c278dd80`；解压后 `README-FIRST.md` 与试发说明一致，`check_bundle_runtime.py --json` 返回 `ok=True`、`selected_port=18765`。
- 2026-05-29 ZIP bundle 默认生成路径收口：按用户指定，将 `scripts/build_zip_bundle.py` 的默认输出目录维护为 `/Users/kim1st/Documents/kim note/04_workspace/analysis/research/知识库工程/sapd wiki bundle`，同时保留 `--output-dir` 和 `SAPD_WIKI_BUNDLE_OUTPUT_DIR` 覆盖能力；同步更新 `CURRENT_STATE.md`、`task_plan.md`、`findings.md`、`scripts/README.md` 和 Delivery / UAT 文档。

## 历史索引

| 归档文件 | 内容 |
|---|---|
| `docs/05-archive/progress-history/2026-05.md` | 2026-05 完整执行记录、根目录 `progress.md` 瘦身快照和本轮轻量结构治理记录 |
| `docs/05-archive/findings-history/2026-05.md` | 2026-05 历史发现 |
| `docs/05-archive/context-slimming-2026-05-15/` | 2026-05-15 上下文瘦身快照 |

## 维护规则

- 本文件只保留最近 1-3 次重要执行；超过 120 行时继续归档到 `docs/05-archive/`。
- 详细过程、长命令输出和阶段性历史不写入根目录 `progress.md`。
