# progress.md

本文件是当前会话恢复入口，只保留最近状态、执行摘要和历史索引。完整执行历史位于 `docs/05-archive/progress-history/`。

## 当前状态（2026-07-24）

- 2026-07-24 `C1 前开发 C0-4—C0-7 PASS`：完成证书 CurrentUser 信任合同、macOS / Windows 默认禁写平台适配器、Web-dev Keychain secret provider、generation 清单、事务轮换与中断恢复、跨进程单写者、24 小时旧代保留和精确清理、信任漂移停机及重置闭环；macOS Web 平台接线必须显式 `--mcp-platform-integration`，稳定 5173 默认仍走隔离 fake。控制合同新增完整 operation phase，AI 功能集成与顶部状态补齐信任、密钥、证书 60 / 30 / 7 / 0 天窗口、清理、重连和重新初始化提示。MCP Python `155` 项、系统设置前端合同、用户库治理合同和语法检查通过；应用内页面 `/#/settings/ai-integration` 为 `not_configured`，未误报“剩余 0 天”，`319px` 无横向溢出。C1、真实 Codex、真实 Keychain / 系统信任写入、Windows 实机、App / DMG 均未启动；未写正式 / 用户业务数据，未 stage、commit 或 push。
- 2026-07-24 `MCP 基础知识库全部业务内容只读接入 PASS`：按用户最终业务裁定新增 `MCP-BASE-KNOWLEDGE-ACCESS-v1`，正式 scope 改为 `sapd.base.knowledge.read`，Sidecar 从 synthetic/public-summary 服务切换为正式基础库只读投影。5 个固定工具可检索并返回全部 stable-ref 对象的完整描述、业务元数据、关系和脱敏来源证据，包含 deprecated 对象，不再依赖 `ai_summary` 或 public 来源；所有返回继续标记为不可信参考数据。SQLite authorizer、`mode=ro&immutable=1`、表白名单和字段投影阻断用户库、源文件读取、本机路径、凭据、日志、写入、ATTACH 与客户端 SQL。正式 GB/T 22239 完整条款和 deprecated 对象实测可返回，MCP 查询前后基础库 / 用户库 SHA-256 不变。页面验收发现并修复既有 schema 检查无条件刷新 `user_meta.updated_at` 的副作用；隔离回归和正式页面重载后用户库哈希、40 条批注及业务更新时间均不变。MCP `155` 项、系统设置前端合同、应用内页面文案、无横向溢出和 console warning/error `0` 通过；5173 守护修复虚拟环境符号链接解析后 stable profile 五项、home、health、workspace projection 均通过。runtime content smoke 后续仍有既有全局搜索去重断言失败，保持在本轮范围外。未写系统信任、真实客户端配置、正式/用户业务数据或 App/DMG，未 stage、commit 或 push。
- 2026-07-24 `MCP 稳定证书 C0-B Web 安全传递 PASS`：C0-A 的固定证书身份已接入真实 Web Sidecar；父进程使用 install/profile/generation/device 绑定的 fake platform custody，仅在匿名继承通道完成 PID、UID、nonce、generation 和父进程身份验证后读取并单次交付口令。Sidecar 不再自行生成长期证书，停止 / 再启动复用同一 CA 指纹；错误进程、用户、generation、nonce、普通文件端点、超限帧和重复读取全部 fail closed，`KEY_PASSPHRASE_IPC_UNSAFE` 时端口不监听。口令、secret reference 和私钥路径在 argv、环境、日志、lease 与公开快照中均为 `0`。MCP Python `126` 项和系统设置前端合同审计通过；5173 实际完成生成证书、启动、TLS 两项诊断通过、停止并保留证书，console warning / error 为 `0`。页面已留在证书有效、MCP 已停止状态供人工 UAT。真实 Keychain/DPAPI、CurrentUser 系统信任、Codex 配置、真实客户端授权、App/打包、正式数据和用户数据均未写入；未 stage、commit 或 push。
- 2026-07-24 `Git 工作区本地收敛完成`：先在仓库外建立完整恢复点，保存全 refs bundle、两处 dirty patch、untracked allowlist archive、主用户库热备份和成熟度报告历史；再把当前产品 checkpoint、MCP 组件 / Web 集成历史及经过评审的证书文档提交纳入 `main`。正式主目录已安全切换为 `main`，10 个旧本地功能分支和 8 个 linked worktree 已移除，当前 `git branch` / `git worktree list` 均只剩正式主目录 main；远端未 push、未删除。完整 pre-commit、搜索 `38/38`、成熟度 `232` 项、MCP `102/102`、runtime suite、5173 stable profile、数据 / 交付边界和 diff-check 通过。收敛验收修复了 MCP 合并误删顶栏 License 状态、P1-5 旧“导出文件夹”审计术语，并将前端治理预算精确更新到已验收实现。SQLite 逻辑 dump 与恢复点一致，Issue `40`、决策 `0`、收藏 `1`、工作区项 `0`；唯一测试报告已移入 recovery quarantine，报告文件清单恢复一致。Windows / Electron 打包源码、`data/` 和证书设计 v1.1 WIP 保持未提交；未启动系统 Chrome，未构建 DMG。
- 2026-07-24 `5173 错误 worktree 数据空态恢复与防复发`：截图中的 ISSUE `0` 和“安全技术架构设计方法内容待补充”来自 `/private/tmp/SAPD_Wiki-main-merged` 占用稳定 5173，并非正式数据删除。只读核对确认正式主目录 `data/user/sapd_wiki_user.sqlite3` 仍有 `40` 条 Issue（临时 main 独立库为 `0`），正式指南包含 `2` 个目录项，安全技术架构指南 `75` 页且首张 `1920×1080` 图片可读。已用 `dev_server_guard --restart` 把 5173 切回正式主目录；应用内浏览器验证 ISSUE 页 `40 / 33 / 3 / 0 / 1`、Dashboard 核心统计、指南 `imageCount=76 / placeholderCount=0`，console warning / error 为 `0`。防复发补丁让守卫识别 primary Git worktree、完整 stable runtime paths，并在 `--start` 遇到错误 project root 时自动重启；linked worktree 不能占用 5173。定向单测 `10/10`、MCP 组合套件 `102/102`、Python 语法、guard 五项 runtime profile、端口 `28775` 释放和 `git diff --check` 通过。未写、复制、覆盖或删除正式数据 / 用户库，未构建 DMG，分支收敛仍未开始。
- 2026-07-24 `Git 工作区收敛方案完成 / 尚未执行`：已只读核对本地 11 个分支、9 个附加 worktree 和三类现场。`main` 位于临时 `/private/tmp/SAPD_Wiki-main-merged`、HEAD `75a5c49`、领先 `origin/main` 4 个 MCP 重建 / 集成提交；主项目目录仍在 `codex/windows-electron@8b46b83` 且包含大量 tracked / untracked 用户工作；`codex/local-mcp-web-integration` 另有一处证书 / 信任后续 dirty 工作。方案固定为先建立全 refs bundle、两处 binary patch 和 untracked allowlist 恢复点，再按交付物等价性把 Windows / Electron、shared frontend、成熟度、App / 导出、MCP 证书和治理改动直接重建到 `main`，最后迁回主项目目录并删除旧 worktree / 分支。不会逐支机械 merge，不会放宽后端路径安全。此前临时 main 预览的 5 项搜索失败已归因为外部 data symlink 越过 `DATA_PACKAGE_ROOT` 被正确拒绝；主项目原生数据路径搜索探针为 `38/38`。本轮只形成方案与记录，未 commit、stage、push、删分支、移除 worktree、切换主目录、修改数据、用户库或 DMG。
- 2026-07-23 `完整 MCP 功能代码审查并接入 stable 5173`：没有直接整分支合并，而是按 shared runtime / Web 开发入口边界选择性纳入 MCP 合同、fixture、core / policy / control / Sidecar、API / CLI、系统设置前端和测试，保留当前成熟度、Windows Electron 与 App 并行修改。审查发现并修复三项 P0：Web 父进程退出可能遗留 Sidecar、stable 5173 的系统 Python 缺少 MCP 依赖、旧 synthetic 状态机可能与真实控制链路并存；同时补齐 loopback-only、浏览器 Origin、控制请求体上限、端口冲突恢复、能力开关、过期状态清理和 5173 保留端口。MCP 使用 `.venv-local-mcp-web` 隔离依赖，真实 Web 回路已在 `5173` 完成 stopped → ready → stopped，MCP 地址为 `https://127.0.0.1:28775/mcp`，停止后端口释放；成熟度 workspace 保持 `5` 条。统一 MCP 套件 Python `99` 条与前端审计、quick suite、数据 / 交付边界、内容 smoke、设置页 HTTP smoke 和 `git diff --check` 通过。未启动系统 Chrome、未构建 App / DMG、未修改系统信任、真实客户端配置、正式数据或用户库，未提交或推送。

- 2026-07-23 `系统设置 / AI 功能集成正式并回 5173`：顶部全局操作固定为“系统设置”与“MCP 状态监测”两个圆形按钮；修复顶部 `overflow:hidden` 导致状态浮层被裁切的问题，悬停 / 键盘聚焦现在显示 MCP 链接、客户端授权和 License 授权，且全局入口会同时读取 Runtime License 与 MCP 状态。系统设置页包含版本、App 保存位置、文件上传路径和文件下载路径；AI 功能集成增加本地端口编辑，限定 `1024–65535`，仅在 MCP 停止时保存，端口写入当前浏览器 / WebView 本地设置并在本地服务重启后恢复，服务地址同步更新并清除旧客户端授权。所有非根页面返回键统一直达全局导航，不再落入工作台、知识库字典或标准框架的过渡页。Web 开发环境提供不触碰用户库的 synthetic MCP 启停 / 端口状态机；macOS / Electron 通过最小权限原生目录选择桥提供三类路径修改，前端不能传入任意路径。系统设置专项、JS / Python 语法、端口边界、定向 diff-check 和 5173 stable health 通过；Electron `6/6`、Swift 类型检查、adaptive UI、目录 / DMG parity 和 quick suite 沿用本轮前序证据。未构建新 DMG，未修改真实用户设置、正式数据、用户库、证书、系统信任或真实客户端配置；实际 App 包仍需下一打包批次验证。

- 2026-07-23 `本地 MCP M0-T T0–T2 开发计划`：Work 对 v0.5 给出 `PASS`，无新增技术 P0；唯一执行前治理条件是将 v0.5 纳入版本控制并关联审批记录。已新增 `docs/06-implementation/local-mcp-m0t-t0-t2-execution-plan.md`，按当前 `codex/windows-electron` dirty checkout 的现实，计划从 tracked base `8b46b837965c...` 建立独立 worktree，再串行执行 T0 合同/fixture、T1 `ReadOnlyRuntimeProbe`、T2 self-client 协议 harness。该等待确认状态已被后续用户授权覆盖，隔离开发转入 `codex/local-mcp-product-development`；本条只保留原计划摘要，当前 UI / 5173 状态以上一条为准。

- 2026-07-23 `本地 MCP PRD v0.5 Work 复评修订`：接受 v0.4“有条件批准”结论并消除三处 P0 门禁冲突。Gate 0 现拆为 `M0-T` 技术验证与 `D0` 内容就绪：前者只允许隔离 spike、synthetic fixture、测试证书、`ReadOnlyRuntimeProbe`、真实客户端和状态机模拟，后者须单独数据变更授权；`D0-Pilot` 是 M1 计划门禁，`D0-Release` 是真实摘要接入/发布门禁。正式 `ReadOnlyRuntimeContext` 后移 M1，生产双平台 UI 验收后移 M3。补入加密 PKCS#8（PEM 编码）+ Keychain/DPAPI 口令候选、系统信任逐次授权、fixture 正负样本合同及摘要 `summary_hash`、唯一键、manifest digest、内部审核追责规则。入口为 `docs/01-architecture/sapd-wiki-local-mcp-requirements-and-prd-v0.5.md`，追踪为 `OI-199`。本轮只改设计与治理文档，未启动 M0-T、未改运行代码、Schema、SQLite、正式 JSON、用户库、证书、系统信任、App 设置或 DMG；文档仍未提交，成为正式工程门禁前须进入版本控制并关联审批记录。

- 2026-07-21 Windows Electron 安装交付补齐路径与卸载契约：NSIS 改为 assisted installer，可选择程序安装目录并生成内置卸载程序；首次启动按 macOS 同一规则选择数据父目录，创建 `SAPDWiki/import`、`export`、`Runtime` 及成熟度 / Issue / 诊断分类目录，App 菜单可分别调整工作、导入和导出目录，设置变更重启生效且不自动迁移或覆盖旧数据。成熟度模型使用指南目录增加同源 `postMessage` 双向导航，保留直接滚动兼容路径。新增 Windows Electron 打包指南并更新整体交付设计与历史预研指向。Electron 单测 `4/4`、P1-6 静态 / 5173 运行审计、指南真实“结果与报告”点击、前端内容 smoke、GitHub / JSON 数据边界和 `git diff --check` 通过；重新生成 `SAPD-Wiki-Setup-0.2.0-win-x64.exe`，约 `202M`，SHA-256 `3a077fc92197fe295bbad32442557d3bbb1e52f366e5953616418e04accc9e7c`，构建日志确认 `oneClick=false` 与 `__uninstaller.exe`。未签名，仍需真实 Windows 10/11 完成首次启动路径、导入导出、覆盖安装和卸载保留数据 UAT；本轮未提交或推送。

- 2026-07-21 Windows Electron 交付支线已在本地创建 `codex/windows-electron`，新增 GitHub `windows-2022` 后端构建工作流：仅打包完整 PyInstaller `win-x64` onedir 目录，执行 `--help` smoke，输出提交版本、SHA-256 和文件数，再以 14 天 Artifact 上传；触发只监听该隔离分支及后端源码路径，明确不读取 / 上传 `data/`、基础 SQLite、用户库、报告、日志或导出。工作流尚未提交、推送或实跑；当前不构建 Electron、ZIP、DMG，不改正式数据、用户库或主线。
- 2026-07-21 Windows Electron 支线已接入下载的 CI 后端 Artifact：新增 `apps/electron` 外壳、用户 Runtime 初始化 / 指纹复用、后端 sidecar 启停、日志目录菜单和 NSIS 配置；`scripts/prepare_windows_electron_runtime.py` 只提取 `SAPD-Wiki-Backend.exe` + `_internal`，再复用 `build_zip_bundle.py` 组装前端、基础库和空用户库模板，不触碰开发机真实用户库。已用 Artifact SHA-256 `5616b98204d3638edf31039afef24850fbb6411ec12d04ae933f59d3211c7860`、后端 SHA-256 `620d9649a77446cc6eb4eb3b611411cac90d63b2b7170de447c059ca20e16cf1` 真实组装 Runtime；`npm run package:win:dir` 与 `npm run package:win` 均通过，生成 `apps/electron/dist/SAPD-Wiki-Setup-0.2.0-win-x64.exe`，约 `202M`，未签名，仍需真实 Windows 10/11 启动 / 写入 / 退出 / 卸载保留数据 UAT。构建目录、node_modules、Artifact ZIP 均不进 Git；当前未提交本轮 Electron 代码。
- 成熟度方案 2 已完成 shared runtime 实现并按用户复验统一目标下限：评分执行在现有四维行内并排显示“当前状态 / 目标状态”，两侧复用 L1—L5 矩阵、Rubric 和可选说明；旧 `targetLevel / targetReason` 仅在新字段缺失时扩展，不覆盖现有四维目标。下级设置保留现有页面结构，固定为“下级当前状态设置 / 下级目标状态设置”，两行共用三列轨道，上下对齐文字、滑块和按钮；当前清空文案为“清空下级所有评分”，目标动作为“统一设置下级目标状态”，目标矩阵、滑块和按钮使用既有金棕语义色。目标规则统一为大于等于当前：逐项目标维度不得低于同维度当前状态；统一目标从当前最高等级起可选，统一当前最多可选到已有目标最低等级，L5 当前允许 L5 目标。前端双向禁选与提示、后端 `targetDimensionConflicts`、综合真实指数兼容校验、新版逐维 XLSX 和旧版最高维度校验同步。专项审计 `231/231`、XLSX `22/22`、5173 maturity smoke、语法 / diff-check 和应用内浏览器通过；报告历史巡检仅有 `demo-project-002` 一份既有旧模型报告未被改写。真实用户库、正式基础数据、ETL 和 DMG 未改，App 回归后置。
- 成熟度项目队列已按使用场景分层：稳定开发服务恢复 `demo-project-001—005` 共 `5` 条受控测试项目，Mac App / ZIP bundle 通过 `runtime_label=bundle` 选择 delivery profile，继续只投影 `001 / 002` 两条交付测试项目。项目概览新增项目信息编辑，覆盖名称、组织、行业 / 规模、客户特征 / 约束、负责人、计划日期、评估人员和备注；保存后同步客户上下文快照、写入 `PROJECT_INFO_UPDATED` 历史，并使已有结果 / 报告按现有后端计算流程失效重算。专项审计 `230/230`、开发 / 交付 profile 直测与 macOS bundle parity 通过；三份成熟度规格同步。本轮未修改评分 / 聚合规则、正式基础数据、SQLite、真实用户库或既有 DMG，下一次打包再执行 App 实包验收。
- 后续导入防复发契约已固化为 `docs/03-import-etl/import-approval-idempotency-and-retention-contract.md`，状态为 `specified / not implemented`：approve 只能从 `reviewing` 在单事务内进入且重复 / 并发批准不得二次写入；`source_references` 按 target、来源文件、Sheet / 单元格、raw value 和 hash 的完整证据键幂等复用；业务验收后使用默认 dry-run、显式 apply 的按 job `finalize-import` 清理 staging / review；未显式传 job ID 的正式导出只选择最新 approved 任务。MVP 设计、ETL 索引、清理执行文档、findings 和 Open Issue 索引已回链，建立 `OI-198 / 待实现`。本轮只补文档，不修改 ETL / CLI / schema、正式 SQLite、源 Excel、正式 JSON、用户库或 DMG。
- 数据库导入中间数据已按固定 allowlist 一次性清理，不新建或迁移主库：新增默认 dry-run、真实库显式确认、备份指纹门禁和事务回滚的 `scripts/cleanup_import_intermediate_data.py`；完整恢复点为 `data/database/backups/sapd_wiki.before-import-intermediate-cleanup-20260719T132403Z.sqlite3`，`integrity_check=ok`、SHA-256 为 `d819d66553afac0368b96dc9c619ad5f2e5dfc436d9be439ad56a77b8618d280`。正式 apply 删除 `review_decisions=48003 / staging_relations=37335 / staging_items=12994`，合计 `98332` 行；两个 reviewing 任务改为 rejected，29 条任务摘要保留。原地 `VACUUM` 后主库由 `283496448` 降至 `136200192` bytes，释放约 `140.5MiB`；正式对象 / 关系 / 来源保持 `4678 / 7757 / 194074` 且五类正式表逻辑指纹不变，`integrity_check=ok / foreign_key_check=0`，用户库 hash 与修改时间不变。主库 summary、受保护基线、JSON package boundary 和 GitHub data boundary 通过；报告为 `data/database/cleanup-reports/import-intermediate-cleanup-20260719T132403Z.json`，备份目录已按全局规则恢复为最新 5 个。
- 数据库既有清理白名单已执行，不新建或迁移主库：`scripts/prune_database_backups.py --apply` 将 `data/database/backups/` 从 `13` 个备份收敛为最新 `5` 个，删除 `8` 个超额旧备份，目录由约 `2.1GB` 降至 `613MB`；定点删除唯一孤儿 `source_references.id=d4e64564-ce59-4d86-b5aa-cf88eed299ab`，来源引用变为 `194074`、孤儿数为 `0`。当时保留的 `5` 条 staging 外键告警已在后续导入中间数据清理中归零。未触碰用户库、正式对象关系、根目录事故 `.bak` 或其他正式导出。
- macOS `V0.2.0` 双版本测试交付已完成：`license` 与 `no-license` 使用统一时间戳 `20260718-143253Z`，两份 DMG 均约 `170M`；SHA-256 为 `4ea0148c373ddb7cc8aa5c948dcf41bcf48dcb4ef0ef852f7850afc55162f8d1` 和 `88f277e4fa955204b7147aa451870275b752b01f0bb8228d61dd3feca6be90dd`。Runtime 包含当前成熟度前端 / API / 基础数据、3 个受控项目 manifest 与 4 套既有报告制品，并把 `openpyxl 3.1.5` 冻结进后端；首次安装只在报告目标目录不存在时复制种子，已有报告与用户库不覆盖。两份 DMG 校验、App 深度签名、版本 / license mode、Runtime `--check-only`、空 `user_schema_0.3` 用户库、成熟度 `3 / 10 / 32 / 91 / 160 / 185` API smoke、185 行 XLSX 模板导出和最终完整 pre-DMG 套件均通过。同步收口了最新源码后的旧门禁假阳性和 Runtime health 响应归一化；未修改成熟度评分 / 聚合规则、正式基础数据或真实用户库。剩余为外部测试机人工 UAT，当前包仍是 ad-hoc 签名、未公证的内部测试交付。
- Codex 工程指令完成两轮恢复优化：根 `AGENTS.md` 恢复权威顺序、任务型上下文、设计基线、项目记忆触发条件和条件式验收，并新增用户对 SAPD 内部子 Agent 的持续授权。父 Agent 保留关键路径、集成和合并态验收；编码子任务使用 forked workspace、最小上下文和不重叠 write set，小修不拆分，独立 task 只承载用户明确要求的长期 worktree。`codex-session-handoff` 已从“慢即交接”修正为先区分模型、命令、审批、工具挂起、上下文压力和并发写入，优先恢复内部子 Agent，再按用户要求交接。旧 300 行执行线/fan-in 工作流已收敛为历史说明；工程测试、数据 QA Skills 继续按边界触发。未修改业务代码、数据、用户库或 DMG。
- 成熟度第三十一轮按用户最新截图只收口 L2 摘要的列宽与组内对齐：共享网格把评估点继续按 `0.85fr` 分配，后置通用样式又把评估点内部改成 intrinsic flex，并让 `.maturity-v24-level-score` 继承 `align-self:end`，造成评估点偏宽、各组内容贴左、当前 / 目标值组比四维“名称 + 数值”组合块低约 `16.5px`。现中宽三组约为 `0.90 : 1.60 : 0.65`，评估点恢复两个等宽子格；当前 / 目标、四维、适用性和进度均在自身子格水平居中，成熟度等级 / 连续分数与四维均值共享 `28px` token，垂直中心差为 `2.5px`。应用内 Browser `1600×900` 实测评估点 / 四维宽度 `210 / 503.68px`、标签中心偏差不超过 `0.5px`、横向溢出与 console warning / error 为 `0`；`T-AS.AD / T-AS.AM / G-SP.SM` 三个 L2 与项目概览负向对照通过，`1280×720` 四维仍为四列，`540×900` 的 `429px` 容器按两列回流，均无横向溢出。专项审计 `222/222`、JS / Python 语法、maturity HTTP/API smoke、GitHub 数据边界、定向 diff-check 与 5173 stable 通过；三份强制文档同步至第三十一轮 / 业务 `0.1.44`，本轮无评分规则变化。治理检查仅命中其他活动改动 `app.js=12772 > 12758`，本轮未抬预算或越权修改。未修改正式 JSON / SQLite、源 Excel、用户库、保护基线、Draw.io 或 DMG；影响面 Web / App shared runtime，App 回归后置。
- 成熟度第三十轮只收口用户最新图中的 L2“评估维度均值”单行：根因是把 `1130px` 当作两列回流条件，导致可用宽度 `735px` 的实际 `1280×720` 工作台仍把四个数字拆成两行。现中宽摘要的成熟度 / 四维均值 / 评估点宽度改为 `0.85 : 1.40 : 0.85`，四维均值在桌面和中宽保持四个完整名称与四个 `28px` 数值同一行；仅容器小于 `480px` 才回流两列两行。应用内 Browser 实测每列 `169.25px`、数值基线跨度 `0`、完整名称无截断、横向溢出 `0`；同画布对照为 `/private/tmp/sapd-maturity-r30-reference-vs-after.png`。专项审计 `220/220`、JS / Python 语法、maturity HTTP/API smoke、GitHub 数据边界、定向 diff-check 与 5173 stable 通过；三份成熟度强制文档同步至第三十轮 / 业务 `0.1.43`，本轮无评分规则变化。未修改正式 JSON / SQLite、源 Excel、用户库、保护基线、Draw.io 或 DMG；影响面 Web / App shared runtime，App 回归后置。
- 项目执行规则已按用户裁定增加“共性优先、定向收口”：用户样例只作为首个黄金样例，先批量定位共享 owner 和页面家族；共性问题只修共享层，并验收“原样例 + 2—3 个同类 + 1 个反例”。调用 Product Design / Root Cause QA 继续承担共性诊断，但不自动触发无关重设计、长文档同步、多视口证据或全工程门禁；只有共享基础设施、数据 / 包、长期契约、App / 发布或用户明确要求时才升级重型流程。本轮仅更新治理规则与性能工作流，未修改业务代码、数据、用户库或 DMG。
- 成熟度第二十九轮只收口用户图中的 L2“评估维度均值”：根因是四个业务标签与主数值横向混排，且旧 `ellipsis + hidden` 在窄列把名称显示为“组织…”。现组织与角色、制度与流程、平台与工具、数据与信息各自按“完整标签在上 / 均值在下”组成读数列；宽屏标签与“适用性 / 评估进度”标题轨道最大偏差 `1.4px`，四个 `28px` 数值使用 `tabular-nums` 且处于同一基线。中窄工作台回流两列两行，完整标签不截断，`2048×1152 / 1280×720` 横向溢出均为 `0`。专项审计升级为 `218/218`，JS / Python 语法、maturity HTTP/API smoke、GitHub 数据边界、定向 diff-check 与 5173 stable 通过；同画布证据为 `/private/tmp/sapd-maturity-r29-reference-vs-after.png`。三份成熟度强制文档同步到第二十九轮 / 业务 `0.1.42`，本轮无评分规则变化。未修改正式 JSON / SQLite、源 Excel、用户库、保护基线、Draw.io 或 DMG；影响面 Web / App shared runtime，App 回归后置。
- 成熟度第二十八轮只修复用户最新截图中的 current / target 双单元：根因是父成熟度组错误拥有“标题 + 空内容”两行，直接子格只有约 `24px` 高，主数字依靠溢出显示；同时旧 `230px` 最小组宽无法为两个 `28px` 等级 / 分数组合保留稳定归属。现父组使用完整高度单轨、子格各自拥有 `20px + 56px` 两行，宽桌面成熟度组最小 `280px`，`1130px` 以下提前进入身份独占行的三列结构，组内间距 `8px`。应用内 Browser 实测 `1600×900` 两格均高 `108.8px`、两组间距 `32.2px`，`2048×1152 / 786×900` 间距 `41.7 / 202.4px`，三视口横向溢出、console warning / error 和禁显字段均为 `0`。专项审计升级为 `216/216`，JS / Python 语法和 maturity HTTP/API smoke 通过；三份成熟度强制文档同步到第二十八轮 / 业务 `0.1.41`，明确本轮无评分规则变化。正式 JSON / SQLite、源 Excel、用户库、保护基线、Draw.io 与 DMG 未修改，App 回归后置。
- 成熟度第二十七轮 Web 复验修订：按最新 4 张截图把评分滚动 owner 从完整评分表单再提升到项目页外层，评分表单和四维打分区自然展开，右侧概览保留 sticky；窄屏新增 `760px` 容器级摘要回流，当前 / 目标等级与连续分数同为 `28px` 且不再互相挤压。项目概览取消视口最小高度和阶段底部推挤，保持 `2:1` 并放大事实 / 进度字号。结果“分层评价”改为“能力类别评分”，T / G / M 与当前分数紧邻，目标作为各类别第二行；目标成熟度、适用性、评估进度、能力类别标题统一提升到 `14px`。报告页明确两种格式都包含完整评估结果和人工文字，并把 Markdown / HTML 做成两个独立出口；后端 / 模板标题同步。三份成熟度强制文档同步到第二十七轮 / 业务 `0.1.40`，专项审计 `214/214`；JS / Python 语法、成熟度 HTTP/API smoke、GitHub 数据边界、定向 diff-check 与 5173 stable 通过。应用内 Browser `2048×1152 / 786×1458` 实测单外层滚动、摘要同线 / 安全回流、项目概览 `2.0`、能力类别代码—分数间距 `7px`、报告四个文字区 / 两按钮 / `11200` 字符 HTML 预览、横向溢出、console 与禁显字段均为 `0`。组合 `frontend` 仍被另一活动会话的 P1-5 范围清理断言阻断，`runtime` 仍被技术服务可见行旧断言阻断，治理仅命中另一会话的 `app.js +14` 行预算；本轮未越权修改。正式 JSON / SQLite、源 Excel、用户库、受保护基线、Draw.io 和 DMG 未修改，App 回归后置。
- 成熟度第二十六轮 Web 复验修订：按最新 8 张截图把评分滚动 owner 从四维打分列提升为完整评分表单，打分列取消内部滚动条，评估概览在评分表单内 sticky；L2 四标题同基线，等级 / 分数统一 `26px`。结果标题改为 `L3 充分定义` 等“等级 + 名称”，四维雷达真实画布提升到 `390px`、半径上限 `150px`，四维值改为两列信息块；项目概览固定 `2:1`、移除知识快照，首页宽屏按约 `1.85:1` 左右分栏，默认模板 canonical 名称统一为“SAPD标准能力成熟度模板”。三份成熟度强制文档同步到第二十六轮 / 业务 `0.1.39`，专项审计 `213/213`；成熟度 HTTP/API smoke 已改为校验正式 Markdown / HTML 报告契约并通过。应用内 Browser `1280×720` 与 `2048×1152` 实测表单单滚动、标题基线、雷达几何、概览比例、首页响应式、页签切换、横向溢出、console 与禁显字段均通过。组合套件仍被共享脏工作区的 Issue P1-5 与技术服务可见行旧断言阻断，前端治理仍只命中另一会话新增的 `app.js` 行数预算；本轮未越权修改。正式 JSON / SQLite、源 Excel、用户库、受保护基线、Draw.io 和 DMG 未修改，App 回归后置。
- 首页 P2-1 V8 已按用户最新裁定纠偏：标题改为“工作事项”，删除无业务意义的 ISSUE / 成熟度混合进度区；两条工作流仍独立展示真实数量、快捷继续、最近 `5 / 3` 条记录和更多菜单。知识库六项基础统计改为非对称 Mosaic，区块面积只建立视觉节奏，不表示权重、达成率或跨粒度总数。属于 Web / App shared frontend；正式 JSON / SQLite、源 Excel、用户库、受保护基线与 DMG 未修改，App 回归后置。
- 成熟度第二十五轮 Web 复验修订：评分上下文退出 sticky，评分表单内仅四维打分列纵向滚动，评估概览与保存区固定；结果宽桌面左列固定“全能力分组雷达 → 紧接四维雷达”、右列分层统计；首排只显示目标成熟度 `L4  4.09`，删除冗余指数 / 聚合文案；结果页签改为“客户评估结果 / 评分明细清单”并复用下划线基准。三份强制文档同步到第二十五轮 / 业务 `0.1.38`，专项审计 `207/207`；应用内 Browser 实测只有打分区 `scrollTop 0→360`，项目页与概览不动，结果双列 `600px + 360px`，横向溢出、console 和主区禁显字段均为 `0`。5173 stable；全套剩余失败来自其他活动会话的 Issue v6、技术服务可见行和 `app.js` 治理预算，未越权修改。正式数据、用户库、基线和 DMG 未改。
- 成熟度第二十四轮 Web 已完成：完成评估门禁只保留适用项未完成与目标冲突，不适用 / 无证据不阻塞；项目反馈改为路由内联并在返回首页后清零；首页新增项目进展与模板管理双区域，默认 / 自定义模板可导出，导入只建自定义副本；全能力分组雷达后接四维雷达，等级与指数分层；报告预留四个汇报文字区且只导出 Markdown / HTML。三份强制文档、`OI-192`、Design QA 与专项审计同步；专项 `205/205`、前端治理、语法、数据边界、diff-check、5173 stable 和应用内 Browser 通过，横向溢出、console warning / error、主区禁显字段均为 `0`。完整 `frontend` 套件通过；组合 `frontend + runtime` 仅被并行技术服务分组改动造成的旧 smoke“默认 160 行全部可见”断言阻断，本轮未越权修改。正式 JSON / SQLite、源 Excel、用户库、受保护基线、Draw.io 和 DMG 未修改，App 回归后置。
- 工作台列表名称已按用户裁定统一为 `ISSUE清单`；安全指南目录项使用 `成熟度模型使用指南`，源文档与下载文件保留完整名称“SAPD 成熟度模型使用指南”。指南正文 v1.3 第 3 章已按 V2.1 业务设计和真实工程重写为项目队列、三步新建浮层、模板、评分执行、评分检查、结果与报告快照主流程；文件解析、对象匹配和人工审查只作为导入辅助路径。页面标题、可访问名称、下载名、P1-6 V5 配置、专项审计和三份成熟度文档同步；P1-6 / P2 静态及 5173 专项、语法、diff-check 和应用内浏览器通过，旧默认流程、横向溢出与 console warning / error 均为 `0`。两项均属于 Web / App shared frontend，DMG App 后置发布矩阵。
- 能力目录工具栏 shared frontend 已按用户裁定精简为“全部展开 / 收起到 L0”两个动作；“目录层级”标题、默认收起说明、外框容器，以及“定位最近”按钮、最近对象状态、快照持久化和动作处理均已删除。保留默认全部收起、搜索祖先路径展开及全量展开 / 回到 L0 行为，P2 契约升级为 V5；语法、静态 / 5173 P2 审计、轻量 smoke、定向 diff-check 和应用内浏览器通过，展开 / 收起可见行 `3→136→3`，横向溢出与控制台日志均为 `0`。完整 `static + frontend` 的本任务相关 P0 / P1 / P2、治理、搜索、滚动等门禁均通过，最终仅被并行成熟度样式变更的 `100px locked project-header height` 断言阻断，本轮未越权修改。未修改其他字典、标准、数据、API、正式 JSON / SQLite、保护基线、用户库或 DMG，App 回归后置。
- 首页标题 shared frontend 已按用户指定收口：根路由 AppShell 唯一显示 `SAPD WIKI / 工作台与知识库概览 / 说明文案`，旧“全局导航 / 应用壳”标题和 Dashboard 正文重复标题已移除，但侧栏“全局导航”入口保持原名。P2 契约升级为 V3；语法、P2 静态 / 5173 审计、完整 `static + frontend` 门禁、应用内浏览器 `1280×720` 和同视口 / 聚焦视觉对照均通过，标题出现次数 `1`、业务面板 `2`、横向溢出与 console error 均为 `0`；未改图 1 目录控件、数据、API、正式 JSON / SQLite、保护基线、用户库或 DMG，App 回归后置。
- 成熟度第二十二轮 Web 已完成：目标冲突短时提示迁移到顶部反馈槽，与底部“保存并转到下一项”几何不相交；“某集团网络安全成熟度评估”受控 demo 固定为恰好 `3` 个目标冲突，并通过 `controlledDemoRevision` 只迁移该 demo 的旧浏览器快照。L0 / L1 / L2 目标等级使用低彩度金棕紧凑横向强调；L2 摘要按当前成熟度、目标成熟度、评估维度均值、评估点情况排序，后两组共享标题—子项—数值语法，评估点情况只显示适用性与评估进度。三份文档同步为第二十二轮 / 业务 `0.1.33`，专项审计 `190/190`、Apple Shell、HTTP/API smoke、语法、定向 `diff-check`、5173 stable、应用内浏览器 `1440×1024 / 820×1024` 均通过；提示与按钮重叠、横向溢出、console error 与主区禁显字段为 `0`。`dataState=ready`，L0 `3`、L1 `10`、L2 `32`、关注点 `91`、服务 `160`、评分项 `185`；未改评分公式 / 聚合、dataClient、ViewModel、ETL、正式数据、源 Excel、用户库、保护基线、Draw.io 或 DMG，App 回归后置。
- 成熟度第十七轮 Web 已完成：`MaturityAssessmentWorkbench.js` / CSS / 资源版本实现连续评分焦点、对象头减法、完整定义、适用性与后端完成计数、固定展开概览、目标等级横排、业务等级 Rubric、紧凑目标设置、只读复核摘要、完整分层统计与统计 Vibrancy；三份成熟度强制文档同步为第十七轮 / 业务 `0.1.28`，专项审计升级为 `159/159`，`design-qa.md` 为 `final result: passed`。验证通过 `node --check`、`py_compile`、成熟度审计、Apple Shell 5173 审计、maturity HTTP/API smoke、相关 `git diff --check` 和 `dev_server_guard --status`；应用内浏览器覆盖评分 / 保存后焦点、L0 / L1 / L2、复核、结果、`1440×1024 / 640×800`，全局溢出与 console error 为 `0`，主区禁显字段命中 `0`。`dataState=ready`，L0 `3`、L1 `10`、L2 `32`、关注点 `91`、服务 `160`、服务项 `154`、关注点评估项 `31`、评分项 `185`；未修改正式数据包、dataClient、ViewModel、评分 / 聚合、ETL、SQLite、源 Excel、用户库、受保护基线、Draw.io 或 DMG。影响面为 Web / App 共享前端，DMG App 留待下一次发布矩阵验收。
- 应用内 viewport override 已在当前有效 tab 上重新设置为桌面基线 `1440×1024`，页面实时 `innerWidth / innerHeight` 与目标一致，文档 `scrollWidth=clientWidth=1440`；后续桌面截图和 L0 / L1 / L2 对照均以该视口为准。
- 桌面 L2 证据 `/private/tmp/sapd-maturity-hierarchy-radar-qa-20260715/02-l2-1440x1024.png` 已人工检查：雷达四轴标签、蓝色实线 / 金色虚线、1—5 量尺和“总体目标参考”说明可读，右侧精确统计在同一首屏连续呈现。目录已取得稳定目标 ID：L0 `category:94b...`、L1 `domain:526...`、L2 `capability:99d...`、关注点 `focus:72c...`，后续只按这些显式对象切换，不使用首项兜底。
- L0 `T` 已通过稳定 `data-category-id` 显式选择：selected ID 与雷达 ID 同为 `category:94b...`，雷达级别 `L0`，右侧标题 `T 安全技术能力 T`；下级列表准确为 5 个 L1，分布为 `L3=4 / L4=1`，总体目标非逐维说明存在，全局横向溢出 `0`。
- L0 初次桌面截图 `/private/tmp/sapd-maturity-hierarchy-radar-qa-20260715/03-l0-1440x1024.png` 出现统计表面黑色捕获异常；DOM 计算样式仍为层级工作台 `rgb(247,250,252)`、Vibrancy `rgba(241,247,253,0.58)` 与 `blur(28px)`，对象与统计内容未丢失。当前不把该截图误报为通过，继续用稳定等待后的复截图和真实可见状态区分截图合成异常与页面回归。
- 等待 `500ms` 后 L0 稳定截图 `/private/tmp/sapd-maturity-hierarchy-radar-qa-20260715/04-l0-1440x1024-stable.png` 恢复正常浅色统计表面，确认前一张是 in-app 截图合成瞬态而非页面样式回归。随后显式选择 L1 `T-AS`，selected ID 与雷达 ID 同为 `domain:526...`，标题正确，下级 L2 `9` 个、分布 `L3=6 / L4=3`、总体目标说明存在、全局横向溢出 `0`。进度记录第一次补丁因尾部锚点文字不一致未写入，读取目标行后已局部重试，业务文件不受影响。
- L1 第一次截图同样出现短暂 Vibrancy 黑色合成，继续等待 `750ms` 后 `/private/tmp/sapd-maturity-hierarchy-radar-qa-20260715/06-l1-1440x1024-stable.png` 恢复稳定浅色；人工检查确认四轴标签、两条不同线型、精确四维值与下级分布可读。该现象仅发生在页面重绘后的 in-app 截图捕获，稳定页面与 DOM 样式正常，未据此修改产品代码。
- 关注点 `T-AS.AD-01` 已用稳定 `data-focus-id` 显式选择；层级雷达数量变为 `0`，右侧进入真实评估点双栏，四维 Slider 分别显示 L3 / L2 / L3 / 未评分，当前 score item 为明确服务评估点。桌面稳定截图 `/private/tmp/sapd-maturity-hierarchy-radar-qa-20260715/07-focus-1440x1024.png` 显示普通态无跨行 Tip、四行评分依据未被遮挡、全局横向溢出 `0`。
- 评分 Tip 已用应用内浏览器真实指针触发：`organization` 当前行进入 `is-tip-animating`，完整 L3 Rubric 在本行 feedback lane 可见，气泡属于本行、完全落在本行几何范围且与下一评分行无重叠；`1500ms` 后 `visibility=hidden / opacity=0`、全页可见 Tip 数恢复为 `0`。触发前后评分值保持 L3，未写入或改动演示评分数据。
- 响应式回归完成：L0 在 `820×900` 下雷达与统计按单列堆叠，selected ID 与 radar ID 一致、画布贴合容器、全局横向溢出 `0`；关注点在 `660×900` 下评分工作台为单列，评分行、依据输入和 Tip lane 均落在本行范围；`640×900` 下内容宽 `603px`，Tip 槽按契约隐藏、Slider 回流为单列，横向溢出仍为 `0`。稳定窄屏证据为 `/private/tmp/sapd-maturity-hierarchy-radar-qa-20260715/09-l0-narrow-820x900.png`、`10-focus-narrow-660x900.png`、`12-focus-score-row-narrow-660x900-stable.png`；浏览器日志 error / warning 为 `0`。
- 本轮最终门禁通过：`node --check`、`py_compile`、成熟度专项审计 `138/138`、P0-2 5173 运行审计、相关文件 `git diff --check` 与 maturity HTTP/API smoke 均通过；`dev_server_guard --status` 在解除沙箱的只读本机访问中确认主页 / health / workspace projection 均为 `200`、`runtime_label=stable`。`dataState=ready`，关键数仍为 L0 `3`、L1 `10`、L2 `32`、关注点 `91`、服务 `160`、服务项 `154`、关注点评估项 `31`、评分项 `185`。未修改正式数据包、`dataClient`、ViewModel、评分 / 聚合 / Rubric、ETL、SQLite、源 Excel、用户库、受保护基线、Draw.io 原图或 DMG；影响面为 Web / App 共享前端，DMG App 留待下次发布矩阵回归。
- 当前分支：`main`；工作区存在用户与其他任务改动，普通开发会话不提交、不推送、不建 PR，后续只 stage 明确相关文件且禁止 `git add .`。
- 固定预览入口：`http://127.0.0.1:5173/`，当前 `runtime_label=stable`，主页、health 与 workspace projection 均为 `200`。
- 能力目录四角问题已抽象为共享目录外壳契约：能力、环境、维护、内容四类常驻目录统一使用 `.shell-directory-pane`，只有父 pane 持有 `1px + 10px` 轮廓，滚动正文不再单独画边框、圆角或阴影；能力目录折叠态残留 `2px` 细缝也已在交互回归中修正。成熟度评分目录不属于本轮共享粒度，未修改。
- 成熟度评分执行页底部连续性已完成根因修复：评分壳层按真实 `100px` 项目锁定头计算可用高度，不再误按 `43px` 多撑 `57px`；评分项目正文取消 `22px` 独立底部 padding，左侧目录上限与壳层一致并在目录内部滚动。三份成熟度强制文档已同步；本轮无业务、评分、聚合、Rubric、模板、API projection 或正式数据变化。
- 禁止范围未修改：P0-3 图谱布局 / 碰撞、Draw.io 原图、ViewModel / 关系推断、ETL、正式 JSON / SQLite、受保护字典 / 标准 / 生命周期数据、源 Excel、用户库和 DMG。本轮只调整生命周期共享组件、专项样式与门禁，不新增关系或改写数据。

## 最近完成事项

- 2026-07-17 `成熟度评分依据 × 当前能力字典映射审计`：使用当前 `capability-workbench` 的真实 maturity 基础模板，逐项映射 91 个关注点、160 条服务关系和 185 个评估点。字典完整性通过；映射为关注点 `76 已对齐 / 8 待身份复核 / 7 无来源`、服务关系 `125 / 27 / 8`、评估点 `147 / 28 / 10`，门禁 `BLOCKED`。完整审计写入 `docs/08-maturity/assessment-rubric-dictionary-mapping-audit-2026-07-17.md`；对象专用评分依据附录已改为“映射通过前冻结分类”，并建立 `OI-197`。
- 2026-07-17 `成熟度对象专用评分依据附录`：从 `data/raw-samples/assesment samples.xlsx` 提取能力 / 关注点实际四维 L1—L5 依据，按合并区域形成 32 个 rubric set（27 个共享、5 个单关注点），并区分业务不可评分、遗漏、未结构化和通用参考。完整逐组原文、缺失项、API / 模板状态契约已写入 `docs/08-maturity/assessment-rubric-source-appendix-2026-07-17.md`，三份成熟度强制文档同步；未修改运行代码、源 Excel、正式 JSON / SQLite、用户库或 DMG。
- 2026-07-17 `P2-1 V8 工作事项与知识统计纠偏`：移除跨 ISSUE / 成熟度的“本地工作进展”，保留两个完整独立工作流；六项知识统计使用 6 列非对称 Mosaic 与两级响应式回流。P2 配置新增 `crossStreamSummary=forbidden`、`asymmetric-six-tile-mosaic` 和跨粒度禁止规则；专项审计、Design QA、三份成熟度强制文档与缓存版本同步。
- 2026-07-16 `P2-1 V7 首页完整工作台纠偏`：用户否决 V6 的纯列表化方向；现恢复原图的 WORKBENCH、继续工作、本地用户库、两条业务入口与大状态读数，在其内部增加 4 项真实工作概览、5 条 ISSUE、3 个成熟度项目、快捷继续和两个更多菜单。P2 契约新增 `recent-lists-are-subsections-not-the-whole-dashboard` 防回归规则，专项审计、Design QA、成熟度三份强制文档与缓存版本已同步。
- 2026-07-15 `OI-196 P1-5 V5 Issue 范围唯一入口`：删除顶部范围下拉和冗余范围状态，工具栏回收为状态、优先级、完整局部搜索和清除筛选；专项审计新增左侧目录唯一所有者、清除筛选不得重置范围、无空列和缓存版本断言。用户截图与修改后工具栏同画布证据为 `/private/tmp/sapd-issue-scope-reference-vs-after-20260715.png`。
- 2026-07-15 `OI-196 第三轮共享组件基线修订`：以图 1 / 图 3 / 图 5 分别固化全局 segmented、完整局部搜索和能力摘要图片盒契约；移除标题区私有 Tab 压缩，Issue 搜索补齐稳定对象计数 / 前后导航，T 根能力摘要改为全宽单区与真实四边框。专项审计从“类名存在”提升为 DOM 完整性、位置无关几何、稳定 Issue ID 和图片边框所有者断言。
- 2026-07-15 `OI-196 第二轮复验修订`：撤销 V9 的摘要局部滚动和环境线型导航；图片以剩余高度网格 + 绝对贴合 + `object-fit:contain` 同时适配宽高，环境回到全局 segmented 胶囊。Issue 搜索直接使用全局 `page-search-control`；Web 的全部 / 所选导出生成 Blob 并走浏览器下载，App 通过 `state.bundle_root` 识别后写入 wrapper 配置目录。应用内浏览器 `1280×720` 完成图片轨道、胶囊计算样式、搜索计算样式和真实“导出全部”点击；未修改用户库、正式数据或 DMG。
- 2026-07-14 `成熟度评分执行底部连续性`：在应用内浏览器 `2048×1152` 真实工程页复现，确认 `900px` 实际可用区被旧公式撑到 `957px`，并叠加 `22px` 正文 padding 与目录固有高度。现项目正文、评分壳层、目录和操作栏共享高度 / 背景契约；专项成熟度审计 `126/126`、语法与 diff-check 通过，前后截图写入 `design-qa.md`。未修改评分数据、ETL、正式 JSON / SQLite、字典 / 标准 / 生命周期基线、用户库或 DMG。
- 2026-07-14 `P1-3 V3 生命周期滚动性能与空值契约`：V2 只验证滚动 owner 数量，遗漏 LC-AP 13 个、LC-DT 57 个 sticky 单元格随纵向滚动重绘的成本，也只替换部分 `/` 空值。现 lifecycle lane 继续作为唯一业务纵向 owner并增加绘制隔离，所有生命周期表头 / 首列退出 sticky；删除“横向滚动查看更多字段”；`null`、空串和纯 `/`、`-`、破折号占位统一显示 `—`，业务标点与数值 `0` 保留。P1-3 V3 审计真实渲染全部 15 个阶段，应用内浏览器同视口确认两页 sticky / 提示 / 旧占位 / 页面横向溢出均为 0。`OI-195` 已自动验收归档，2026-07-16 经用户确认关闭；`OI-194` 的后续复验范围不足已建立追踪。
- 2026-07-14 `共享目录外壳四角与组件统一`：根因是旧 `.tree / .environment-tree / .source-nav` 的 `18px` 轮廓与 Apple Shell 父 pane 的 `10px` 轮廓叠加。现新增共享目录外壳样式和契约门禁，覆盖能力、环境、维护、内容四个目录家族；应用内浏览器验证能力展开 / 折叠、环境对象树和内容目录均为父单轮廓、正文 full-bleed，console error `0`。未改目录业务行、数据、图谱、Draw.io 原图、成熟度或 DMG。
- 2026-07-14 `P1-3 V2 生命周期滚动与字典引用回归`：根因不是生命周期数据包错误，而是 P1-3 样式把横向容器计算成双轴滚动并让阶段上下文 sticky 覆盖内容，以及 ViewModel 按原始文本重新创建模块对象。现固定 lane 为唯一纵向 owner、宽表为单轴横向 owner、上下文随正文滚动；模块始终保留 `maintenance/modules.json` 规范 ID，括号限定语不创建新对象。`OI-193`、`OI-194` 已自动验收并归档。
- 2026-07-14 `P1-2 V3 环境双 Tab 四角单轮廓`：把用户两张截图抽象为同一外壳所有权问题，固定 `.environment-detail-pane` 唯一持有边框、Apple Shell `10px` surface radius 与裁切；`.environment-relation-map.environment-tabbed-map`、Tab panel、`.environment-basemap-lab-main` 和映射工作台均为无边框 / 无圆角的 full-bleed 子层。Draw.io 原图、环境数据、选择逻辑、工具栏动作和详情抽屉未改；配置、专项审计、缓存版本、运行 smoke 四角断言与治理基线同步到 V3。
- 2026-07-14 `顶栏操作收敛`：移除全局“能力 / 关注点 / 服务 / 环境 / 生命周期”统计区及其运行时渲染路径，将授权状态、设置和本地数据包作为唯一顶栏操作组固定在原统计区右侧。Apple Shell 专项审计新增“顶栏不回填统计、操作组与授权状态仍存在、两列布局”断言；未改业务数据、用户库或 DMG。
- 2026-07-14 `P1-2 V2 能力 / 环境外部画布壳层`：撤销被用户否决的“当前对象 / Tab / 搜索单控制头”，直接恢复图 2 的 OI-159 结构：对象标题在画布外，画布内为左 Tab / 右搜索，外层 / 内层圆角为 `26px / 18px`；目录宽度、折叠与搜索行为不变。环境主容器恢复 `18px` 连续弧角和裁切，Draw.io 图形内容不改。四个冻结资产哈希不变，未重启 P0-3。
- 2026-07-16 `P1-6 指南内容 v1.3`：保留源 HTML 文档封面、`244px` 目录与隔离 iframe，按 V2.1 业务设计和真实页面重写第 3 章；下载名、缓存 URL、配置、专项审计和三份成熟度强制文档同步。业务规则、评分、聚合、模板结构、运行页控件和正式数据均无变化。

## 验证摘要

- 对象专用评分依据结构与映射分层核对：源表仍为 84 个关注点、32 个 rubric set、640 个状态槽；初步矩阵投影 `112 完整组 / 63 不完整组 / 10 无源关注点` 不作为分类结论。当前字典映射进一步核对为关注点 `76 已对齐 / 8 待身份复核 / 7 无来源`、服务关系 `125 / 27 / 8`、评估点 `147 / 28 / 10`；模板校验错误 / 警告、重复编码、源表孤立关注点和父 L2 冲突均为 0。三份基线文件哈希未变。
- 首页 P2-1 V8：组件与审计 JS 语法、P2 静态 / 5173 运行审计、成熟度专项 `213/213`、overview HTTP smoke、5173 stable 通过；应用内浏览器 `1280×720 / 720×900` 验证标题“工作事项”、混合概览 `0`、独立工作流 `2`、ISSUE / 成熟度行 `5 / 3`、更多菜单 `2`、快捷入口 `2`、知识统计 `6` 且实际尺寸 `3` 种、横向溢出 `0`、console warning / error `0`、禁显字段 `0`。ISSUE 更多菜单显示“查看全部 40 条 ISSUE”，Escape 关闭并恢复焦点；同画布证据为 `/private/tmp/sapd-dashboard-v8-reference-vs-final.png`。前端治理仍只被其他并行任务的 `app.js=12772 > 12758` 旧预算阻断；本轮 P2 CSS 由删除混合概览而净缩减，未抬阈值或越权修改 `app.js`。
- 首页 P2-1 V7：相关 JS 语法、P2 静态 / 5173 运行审计、成熟度专项 `207/207`、overview HTTP/API smoke 与定向 `diff-check` 通过；应用内浏览器 `1280×720` 验证工作概览 `1`、统计项 `4`、并排工作流 `2`、ISSUE 行 `5`、成熟度行 `3`、更多菜单 `2`、快捷入口 `2`、纯列表退化 `false`、横向溢出 `0`、console warning / error `0`、禁显字段 `0`。`继续处理` 的入口 ID 与 Inspector ID 同为 `930bba9f-aebb-44c3-912c-a54e330df956`，`继续评估` 直达 `demo-project-002`；Escape 焦点恢复通过。同画布证据为 `/private/tmp/sapd-dashboard-v7-reference-vs-final.png`，`design-qa.md` 为 `final result: passed`。全局治理旧 `app.js` 预算与 maturity JSON 报告旧断言仍由对应任务收口，本轮不抬阈值或越权混改。
- 成熟度指南下载：AppShell / 审计脚本语法、P1-6 V2 静态与 5173 运行审计、content HTTP smoke、完整 `static + frontend`、定向 `diff-check`、5173 stable 均通过；治理基线只登记本轮 `p1-guide-reading.css=103` 的标题栏按钮与可见性增量，其他阈值不变。应用内浏览器 `1280×720 / 390×844` 确认唯一按钮 `70×30px`、真实下载媒体命令通过、原路由与单一 iframe 保持、横向溢出及 console warning / error 为 `0`。对照证据为 `/private/tmp/sapd-maturity-guide-download-reference-vs-after-20260716.png`。
- `node --check frontend/capability-browser/app.js`、P1-5 V5 静态 / 5173 审计、`audit_frontend_scroll_contract.mjs`：通过；完整 `frontend + runtime` 套件通过。
- 应用内浏览器 `1280×720`：顶部范围控件 `0`、左侧目录 `1`；目录范围在“清除筛选”后保持，搜索宽 `440px`，页面横向溢出 `0`，控制台 warning / error `0`，主区禁显字段命中 `0`。
- `node --check frontend/capability-browser/app.js`、P1-2 V11 与 P1-5 V4 静态 / 5173 运行审计：通过；P0-2 `2026-07-15.7` 已把 segmented 与完整局部搜索写入共享 Shell 契约并通过运行审计。应用内浏览器确认环境 segmented `42/34px + 16/12px`；Issue 搜索宽 `404px`、含计数和两个箭头，稳定 Issue ID 导航 `1/33 → 2/33`；能力摘要图片 `455×202px`、四边 `1px`，figure 无内部滚动，三页横向溢出 `0`。
- `node scripts/run_project_test_suite.mjs --suite frontend --suite runtime --url http://127.0.0.1:5173/`：通过；P0-1 / P0-2 / P0-4、P1-1—P1-6、前端治理、内容 smoke 和能力 / 环境 / 生命周期 / 标准 / 搜索 / 成熟度 HTTP/API smoke 全部通过，按规则未启动系统 Chrome。
- `node scripts/audit_frontend_governance.mjs`：通过；基线升级为 `p1-2-v11 / p1-5-v4`，只登记 `app.js=12758` 和 `p1-review-search.css=307` 的本轮契约增量，颜色、`!important`、`:root`、关系胶囊和 `color-mix` 上限未提高。
- 六条 `frontend_smoke_check` 覆盖能力、环境、标准、安全职能、全局搜索和 Issue，全部通过且未启动系统 Chrome；`check_github_data_boundary.py`、`git diff --check`、`dev_server_guard.py --status` 均通过。
- `node scripts/audit_frontend_p1_3_lifecycle_workbench_contract.mjs --url http://127.0.0.1:5173`：通过；契约升级为 V3，检查 8 个 LC-AP、7 个 LC-DT、共 15 个真实阶段渲染和 7 个模块规范引用。
- 应用内浏览器 `1280×720`：LC-AP 与 LC-DT 均为单一业务纵向 owner、`contain=content`、`overscroll-behavior-y=contain`、sticky `0`、提示 `0`、纯 `/` / `-` `0`、页面横向溢出 `0`；前后同状态截图确认 AP-01 的 `/` 已显示为 `—`，LC-DT 提示已删除。
- 两条 `frontend_smoke_check` lifecycle HTTP/API 检查通过，均未启动系统 Chrome；`dev_server_guard --status` 为 stable pass。
- `static,frontend` 总控此前因本轮 P1-2 / P1-4 / P1-5 修复已超过旧预算而停在治理门禁；确认增量均属于 OI-196 的滚动、导出与统一样式契约后，仅更新对应四个文件预算，最终全套通过，未抬高其他模块阈值。
- `node scripts/audit_frontend_p0_2_apple_shell_layout_contract.mjs --url http://127.0.0.1:5173`：通过；契约版本为 `frontend-p0-2-apple-shell-layout-2026-07-14.5`，四类目录已纳入父单轮廓断言。
- `node scripts/frontend_smoke_check.mjs --page capability --route /capability-mapping --url http://127.0.0.1:5173` 与 environment 对应命令：通过；按项目规则未启动系统 Chrome。`python3 scripts/dev_server_guard.py --status` 同步确认 5173 stable、主页 / health / workspace projection 均为 `200`。
- 应用内浏览器 `1280×720`：能力目录展开、折叠和恢复通过；环境对象树、内容目录通过；外壳为 `1px + 10px`、正文为 `0px + 0px`，能力折叠宽度为 `0`，console error `0`。维护 source-nav 当前路由隐藏，静态注册与专项审计通过。
- `node scripts/audit_frontend_governance.mjs`：通过；`shared-directory-shell.css` 仍为 `46 / 46` 行、`important=0`、`hexColors=0`、`colorMix=2 / 2`，P1-2 / P1-4 / P1-5 治理预算已按最终实现收口。
- P1-2 V3 收口时的 `node scripts/audit_frontend_p1_2_canvas_workbench_contract.mjs` 与 5173 运行审计曾通过，契约为 `2026-07-14.p1-2-canvas-workbench-v3`，四个 Draw.io / 图谱冻结资产哈希不变；当前工作区后续把入口提升到 `...20260714-4` 后出现上述审计版本漂移，待对应 P1 会话收口。
- `node scripts/audit_frontend_p0_2_apple_shell_layout_contract.mjs`：通过；共享 surface radius 仍为 `10px`，未新增第三套圆角标准。
- `node scripts/run_project_test_suite.mjs --suite quick`：通过；GitHub / JSON / 受保护基线和 DMG / 5173 同源契约无回归，未构建 DMG。
- 应用内浏览器 `1898×1064`：先验证“信息化环境视图”，再切换“信息化环境-安全技术”并复现“云数据中心 → 管理平台”选中态；两页父外壳均为 `1px + 10px radius + overflow:hidden`，内部 relation / canvas / workbench 均为 `0px border + 0px radius`，视觉对照确认四角只剩一条轮廓，console error `0`。
- `node scripts/run_project_test_suite.mjs --suite static --suite boundaries --suite data --suite frontend --suite runtime --url http://127.0.0.1:5173`：首次运行在治理门禁识别出 `viewModels.js` 净增 19 行；确认其全部对应规范 ID 解析并由 8 阶段专项审计覆盖后更新基线。随后 `frontend,runtime` 完整通过；静态、边界、受保护基线、数据和字典引用均无 error，既有 138 条 `work_function` candidate warning 未变化。
- 应用内浏览器 `1280×720`：AP-01、AP-03、AP-05、DT-01 均无页面横向溢出；宽表只有局部横向滚动，上下文不覆盖区块；AP-03 只显示“应用程序静态安全测试”和“软件成分分析（SCA）”两个规范模块。系统 Chrome 未启动。
- 系统 Chrome 两次在建立 DevTools target 前退出，未产生页面截图，不计为验收通过；随后使用应用内浏览器完成同视口视觉回归。
- `python3 scripts/dev_server_guard.py --restart`：通过；清理失效监听后 5173 stable。未做 DMG App 回归。

## 下一步

- 成熟度下一轮先关闭 `OI-197` 映射门禁：确认 8 个同编码但名称语义不同的关注点是否继续使用候选 rubric set，并为 7 个无来源关注点确认继承或新建 rubric set；91 / 160 / 185 全部唯一映射后，再重新输出缺级、遗漏、业务不可评分和 KPI 未结构化分类。
- P2-4 继续承接全局 AppShell 的窄屏主内容可达性；本轮首页组件本身在 390px 下无横向溢出，不从 P2-1 越权修改全局侧栏。下次 DMG 发布矩阵补 macOS App 回归；不重打本轮 DMG，不扩大到正式数据、评分公式、聚合、ETL 或字典基线改造。

## 维护规则

- 本文件只保留最近状态与 5—10 条重要执行，超过 120 行时压缩并归档；详细过程、长命令输出和阶段性历史不写入根目录 `progress.md`。
