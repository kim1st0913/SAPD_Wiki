# progress.md
本文件是当前会话恢复入口，只保留最近状态、最近关键动作、验证摘要和历史索引。完整执行历史已归档到 `docs/05-archive/progress-history/`。

## 当前状态（2026-07-02）

- 当前分支：`main`；交接时 `main...origin/main` 同步，当前工作区已有本轮与前序未提交改动，后续提交需只 stage 明确相关文件。
- 固定预览入口：`http://127.0.0.1:5173/`；当前 5173 为项目服务，`python3 scripts/dev_server_guard.py --status` 通过。
- 最新 GitHub checkpoint：`d69063a Checkpoint environment object demos`，已推送。
- 当前主线：`OI-149` P4 正式 split apply 已执行并通过 post-apply readiness；`OI-156` 全局批注定位 / 高亮上一条任务已收口，后续 7 项批注治理也已落地：上下文展示、无效批注 dry-run 审计、测试批注 seed/cleanup 脚本、全局设计原则、前端运行时索引、`user_notes` 查询索引声明、抽屉贴边与恒定侧耳动画。
- 当前用户要求：修正能力页控制轨回归中误移动搜索框的问题；只回退搜索框位置 / 宽度到已认可基线，保留 `AppShell.js` 的 tab 槽位修复，不改数据链路。
- 数据核对边界：连接语义保持 `安全技术服务 -> 安全技术模块`、`安全技术服务 -> 安全技术措施`、`安全技术模块 / 措施 -> 安全系统`；不得新增 `安全技术模块 -> 安全技术措施`；安全系统关系必须落在模块 / 措施目标节点自身，不能从服务级聚合字段继承。
- 禁止范围：不修改原始 Excel、SQLite、`frontend/capability-browser/generated/environmentBasemap.node-details.json`、正式 review checklist、标准包、字典包、LC-AP / LC-DT 数据包；不写用户批注数据库。

## 最近完成事项

- 2026-07-02 `OI-159 工作区控制轨圆角裁切修复`：按用户截图反馈，能力页工作区控制轨左右两端出现半截圆弧不是有意设计，而是外层 `capability-workspace-surface` 的圆角边界与内部 `capability-workspace-control` 背景未同步裁切造成的视觉缺陷。已只修改控制轨外观：为 `capability-workspace-control` 增加 `overflow: hidden` 和 `border-radius: 25px 25px 0 0`，让顶部控制轨背景贴合外层 Apple shell 面板圆角；同步提升 `styles.css` 缓存版本并补契约审计断言。真实 Chrome 回归通过，截图为 `/var/folders/81/8nwy3h2n00s1dw1g5bnvj7j40000gn/T/sapd-capability-corner-fix-20260702-smoke.png`。本轮未修改搜索框位置 / 宽度、能力数据、ViewModel、正式 JSON、SQLite、原始 Excel、环境映射页、工作台模块、LC 页面、标准包、字典包或用户批注数据库。
- 2026-07-02 `OI-159 搜索框误移动回退`：按用户反馈“怎么又移动了搜索框？”校正上一轮修复。真实根因是 `AppShell.js` 运行时模板缺少 `#capabilityViewControls` 槽位，不是搜索框宽度本身；上一轮为保证 tab 可见而把搜索框改成 `360px`、`margin-left:auto` 属于额外误改。现已将 `.capability-workbench-tools` 恢复为已认可的右侧搜索基线：`width: min(34vw, 430px)`、`max-width: 430px`、`min-width: 300px`，并把工作区控制轨恢复为 `justify-content: space-between`；保留 `AppShell.js` 的 `capabilityViewControls` 槽位和 Chrome smoke 的 tab 可见探针。真实 Chrome 回归通过：`capabilityControlProbe.present=true`、`visibleCount=2`、`labels=["关系图谱","总览摘要"]`、`searchRect.width=430`，截图为 `/var/folders/81/8nwy3h2n00s1dw1g5bnvj7j40000gn/T/sapd-capability-tabs-real-chrome-20260702-search-baseline-smoke.png`。补充关闭批注抽屉截图的自定义 Chrome 脚本因当前环境拒绝提权未执行，未绕行。本轮未修改能力数据、ViewModel、正式 JSON、SQLite、原始 Excel、环境映射页、工作台模块、LC 页面、标准包、字典包或用户批注数据库。
- 2026-07-02 `OI-159 工作区控制轨 tab 缓存修复`：按用户截图“tab 页呢？”定位并修复正式能力页 tab 不显示问题。根因是正式迁移时 `app.js` 已改为向 `capabilityViewControls` 注入 tab，但动态加载的 `CapabilityLocalRelationMap.js` URL 未同步提升缓存版本，浏览器仍可能使用旧组件，旧组件没有 `renderTabControls`，导致控制轨只剩搜索框。已提升动态组件缓存版本和 `index.html` 的 `app.js` 缓存版本，并在能力 ViewModel 契约审计中新增门禁：`CapabilityLocalRelationMap.renderTabControls` 必须存在，且动态组件 URL 必须包含 `oi159-attached-control`。本轮未修改能力数据、ViewModel、正式 JSON、SQLite、原始 Excel、环境映射页、工作台模块、LC 页面、标准包、字典包或用户批注数据库。
- 2026-07-02 `OI-159 工作区控制轨 Chrome 回归修复`：按用户要求执行系统 Chrome 真实截图回归，`frontend_smoke_check --allow-system-chrome` 首次返回 `result=fail`，截图显示右侧抽屉打开时工作区控制轨只稳定显示搜索框，tab 没有作为左侧主控件可见。根因补充为控制轨 flex 分配不合理：搜索框固定宽度过大，tab 槽位可被压缩到不可见。已调整 `styles.css`，让 `capabilityViewControls` 固定优先显示，搜索框改为 `flex: 0 1 360px`、最大 `360px`、最小 `260px` 并靠右自适应；`index.html` 提升 CSS 缓存版本。`frontend_smoke_check.mjs` 新增 `capabilityControlProbe`，系统 Chrome 回归会断言能力页 `capabilityViewControls` 至少有 2 个可见 tab，防止后续只剩搜索框还通过。
- 2026-07-02 `OI-159 AppShell 运行模板补槽`：真实 Chrome 二次回归继续失败，`capabilityControlProbe.present=false`，确认真实运行 DOM 没有 `#capabilityViewControls`。根因是正式运行时 `AppShell.js` 会重写能力页 workspace，而 `renderCapabilityWorkspace()` 仍是旧模板：标题区后只放 `capability-workbench-tools` 搜索框，没有工作区控制轨和 tab 槽位。已将 `AppShell.js` 能力页模板同步为 `capability-workspace-surface / capability-workspace-control / capabilityViewControls / capability-workbench-tools / detail` 结构，并提升 `AppShell.js` 缓存版本；能力 ViewModel 契约审计新增 AppShell 模板门禁。
- 2026-07-02 `OI-159 Chrome 真实回归通过`：重新执行 `node scripts/frontend_smoke_check.mjs --page capability-mapping --route /capability-mapping --url http://127.0.0.1:5173 --allow-system-chrome --screenshot-name capability-tabs-real-chrome-20260702-pass`，结果 `pass`。真实 Chrome 指标：`capabilityControlProbe.present=true`、`visibleCount=2`、`labels=["关系图谱","总览摘要"]`，tab rect 位于工作区控制轨左侧，搜索框位于其右侧。最终截图为 `/var/folders/81/8nwy3h2n00s1dw1g5bnvj7j40000gn/T/sapd-capability-tabs-real-chrome-20260702-pass-smoke.png`。随后 JSON 包边界、GitHub 数据边界和 `git diff --check` 通过；`audit_capability_viewmodel_contract` 在沙箱内 fetch 失败，尝试外部上下文重跑被当前环境拒绝，未绕行。
- 2026-07-02 `OI-159 工作区吸附控制轨正式迁移`：按用户认可的 demo 方案迁移到正式安全能力映射页。正式页结构已调整为：标题区 `capabilityFocusHeader` 只承载当前能力编码 / 标题 / 路径；新增 `capability-workspace-surface` 与 `capability-workspace-control`，将视图 tab 注入到独立 `capabilityViewControls`，搜索框放在同一工作区控制轨右侧；关系内容继续由 `#detail` 渲染。搜索 placeholder 收短为 `搜索能力、服务、流程或模块/措施`，旧静态 `关注点 / 服务 / 流程 / 模块` filter chip 已移除。同步提升 `index.html` 的 `app.js` / `styles.css` 缓存版本，并更新能力 ViewModel 契约审计门禁，防止 tab / 搜索回到标题区硬分割线。本轮未修改能力数据、ViewModel、正式 JSON、SQLite、原始 Excel、环境映射页、工作台模块、LC 页面、标准包、字典包或用户批注数据库。
- 2026-07-02 `OI-159 工作区吸附控制轨回退与 demo`：按用户要求回退上一版正式能力页吸附控制轨变更，正式页恢复到 `oi159-header-shelf-20260702-1` 标题区 shelf 基线：标题、tab、搜索框仍在标题区工程工具栏内，不再使用把 tab / 搜索压到工作区上沿的大面板。新的吸附方向改为只在统一 demo 页新增 `能力页控制轨` tab，用独立静态效果图表达“标题区只保留对象身份，tab + 搜索作为工作区顶部控制轨”的视觉方案，等待人工评审后再决定是否迁移到正式页。本轮未修改能力数据、ViewModel、正式 JSON、SQLite、原始 Excel、环境映射页、工作台模块、LC 页面、标准包、字典包或用户批注数据库。
- 2026-07-02 `OI-159 标题区搜索框缩短与底板压线修复`：按用户最新截图反馈继续只修能力页标题区。搜索框桌面最大宽度从 `760px` 缩短到 `560px`；标题区取消旧硬 `border-bottom` 横线，改为底部柔和 shelf 阴影与渐隐分隔，tab / search 加层级后不再压在硬横线上。同步提升 `index.html` CSS 缓存版本，并更新 `audit_capability_viewmodel_contract.mjs`，新增门禁防止恢复 760px 搜索框或旧硬底线。本轮未修改能力数据、ViewModel、正式 JSON、SQLite、原始 Excel、环境映射页、工作台模块、LC 页面、标准包、字典包或用户批注数据库。验证通过：相关 JS `node --check`、能力 ViewModel 契约审计、能力页 smoke、`frontend_content_smoke_check --skip-api`、JSON 包边界、GitHub 数据边界、5173 服务状态、`git diff --check`；未启动系统 Chrome。
- 2026-07-02 `OI-159 标题区方案 2 实际前端视图`：按用户确认的方案 2，把能力页标题区改为工程工具栏式 Apple shell 排布。当前第一行只承载当前能力编码 / 标题，第二行左侧为视图 tab，右侧为搜索框；搜索框宽度限制为桌面最大 `760px`，不再横跨整屏，L0 / L1 双 tab 和 L2 / 关注点四 tab 共用同一工具栏位置。只修改 `styles.css`、`index.html` 缓存版本和 `audit_capability_viewmodel_contract.mjs` 标题区布局门禁；未修改能力数据、ViewModel、正式 JSON、SQLite、原始 Excel、环境映射页、工作台模块、LC 页面、标准包、字典包或用户批注数据库。验证通过：相关 JS `node --check`、能力 ViewModel 契约审计、能力页 smoke、`frontend_content_smoke_check --skip-api`、JSON 包边界、GitHub 数据边界、5173 服务状态、`git diff --check`；未启动系统 Chrome。
- 2026-07-02 `OI-159 下级能力入口纵向截断修复`：按用户最新截图反馈，继续修能力页 L0 / L1 `总览摘要`。根因是上一版把 `下级能力入口` 放在每个父项底部的大块展示区，父项行高被翻倍，导致后续入口在工作区底部被纵向截断。`CapabilityLocalRelationMap.js` 已取消可见的单独 `下级能力` 小标题行，改为父项行内 `aria-label="下级能力"` 的小型 code chip 入口区；`styles.css` 将父项压缩为 `主信息 / 结构统计 / 覆盖胶囊 / 下级入口` 四列，缩小摘要卡、核对信号、父项、覆盖胶囊和下级 code chip 的间距与尺寸，同时保持直接下级 code 完整显示、完整名称保留在 tooltip；`app.js` / `index.html` 缓存版本提升为 `oi159-summary-compact-20260702-1`；`audit_capability_viewmodel_contract.mjs` 增加门禁，禁止把 `下级能力` 恢复为占一整行的可见小标题。本轮未改正式 JSON、SQLite、原始 Excel、标准包、字典包、LC 数据包、环境映射页、工作台模块或用户批注数据库。
- 2026-07-02 `OI-159 总览摘要布局溢出修复`：按用户截图反馈，先把能力页 L0 / L1 `总览摘要` 恢复为正常前端页面。根因是完整下级能力使用 `code + title` 长 chip 直接铺在右侧列，长文本未受限后横向溢出，压到右侧 `覆盖结构` 面板。`CapabilityLocalRelationMap.js` 已将下级能力 chip 改为 code-only 显示，完整名称保留在 tooltip；`styles.css` 将下级能力区改为每行底部独立区域，并为面板、行、chip 增加溢出约束和文本省略，右侧 `覆盖结构` 不再被左侧内容覆盖；`app.js` / `index.html` 提升缓存版本为 `oi159-summary-layout-fix-20260702-1`。验证通过：相关 JS `node --check`、能力 ViewModel 契约审计（`T`、`T-AS`、`T-OF` 均为 `overview_two_tab_relation_graph_and_summary`）、能力页 smoke、`frontend_content_smoke_check --skip-api`、JSON 包边界、GitHub 数据边界、5173 服务状态、`git diff --check`。未启动系统 Chrome；未修改正式 JSON、SQLite、原始 Excel、标准包、字典包、LC 数据包、环境映射页、工作台模块或用户批注数据库。
- 2026-07-01 `OI-159 总览摘要页面说明移出与视觉收口`：按用户反馈修正上一版“说明写进页面”的错误，只修能力页 L0 / L1 `总览摘要`。`CapabilityLocalRelationMap.js` 已把每块意义和统计价值说明从页面移出，页面只保留业务标题、统计数字、核对信号、覆盖结构和下级能力入口；`阅读摘要` 不再展示解释路径，三类统计卡不再展示价值脚注，`下级能力入口` / `覆盖结构` 不再展示说明段落。`viewModels.js` 去掉 `previewChildren.slice(0, 4)`，组件去掉 `.slice(0, 3)`，并以 `下级能力` 字段展示所有直接下级能力；`styles.css` 将总览摘要压回克制的工作台摘要表：信号卡更薄、结构 chip 横排、下级行按业务列扫读、覆盖条恢复紧凑高度；`app.js` / `index.html` 提升缓存版本；`audit_capability_viewmodel_contract.mjs` 新增反向门禁，禁止 L0 / L1 总览摘要出现说明书式标签和说明段落，同时逐个断言样本能力的直接下级 code 全部渲染。验证通过：相关 JS `node --check`、能力 ViewModel 契约审计（`T`、`T-AS`、`T-OF` 均为 `overview_two_tab_relation_graph_and_summary`）、能力页 smoke、`frontend_content_smoke_check --skip-api`、JSON 包边界、GitHub 数据边界、5173 服务状态、`git diff --check`。未启动系统 Chrome；未修改正式 JSON、SQLite、原始 Excel、标准包、字典包、LC 数据包、环境映射页、工作台模块或用户批注数据库。
- 2026-07-01 `OI-159 L0/L1 总览摘要有效信息设计`：按用户要求继续设计 `总览摘要`，使其符合 Apple shell 工作台风格并提取有效信息展示；随后按用户截图反馈，将能力页视角 tab 从图谱画布浮层移到当前能力标题下方，成为标题区二级视图条。`CapabilityLocalRelationMap.js` 中 L0 / L1 仍只渲染 `关系图谱` 和 `总览摘要` 两个 tab：`关系图谱` 为默认 tab，继续使用原 `LocalRelationNetworkGraph`；`总览摘要` 保持不渲染大明细表，也不重复页面顶部大标题和大统计卡。新摘要从现有 ViewModel 的 `capabilityOverview.stats / coverage / children` 提取 `技术覆盖`、`管理落地`、`标准映射` 三类有效信号，并新增 `重点核对` 区，自动提示技术服务缺口、服务映射歧义、管理活动缺失、标准控制项缺失和优先下钻对象；下级能力入口改为紧凑覆盖信号 pill 展示技术 / 管理 / 标准状态。`styles.css` 补 Apple shell 轻量信号格、重点核对条、下级覆盖 pill 和标题下方 tab 样式；`app.js` / `index.html` 提升缓存版本；`audit_capability_viewmodel_contract.mjs` 新增门禁，要求 L0 / L1 双 tab 可切换、旧 `management / standard` tab 状态自动回退到 `关系图谱`、tab 必须位于标题区下方、第二个 tab 必须包含有效信号、重点核对、下级入口和覆盖结构，且继续禁止大表 DOM。验证通过：`node --check`、能力 ViewModel 契约审计（`T`、`T-AS`、`T-OF` 均为 `overview_two_tab_relation_graph_and_summary`）、能力页 smoke、批注契约审计、`frontend_content_smoke_check --skip-api`、JSON 包边界、GitHub 数据边界、5173 服务状态、`git diff --check`。未启动系统 Chrome；未修改正式 JSON、SQLite、原始 Excel、标准包、字典包、LC 数据包、环境映射页或用户批注数据库。
- 2026-07-01 `Workbench Issue Real Data Wiring`：按用户要求将正式 `/workbench/annotations` 从 mock Issue 队列切到真实 `user_notes` 数据。`app.js` 现在通过既有 `ensureUserNotesLoaded()` / `dataClient.getUserNotes()` 读取本地用户库，统计卡、页面分组、队列行、最近 Issue、Dashboard 工作台入口和右侧 inspector 均由真实字段生成：`status`、`page_route`、`page_title`、`anchor_type`、`object_type`、`object_title`、`body`、`tags`、`created_at`、`updated_at`。当前本机只读样本为 `45` 条 Issue，状态分布：`todo=40`、`reviewing=1`、`waiting_confirm=1`、`closed=3`。右侧保存和状态按钮已接入 `dataClient.updateUserNote()`，删除接入 `dataClient.deleteUserNote()` 并带确认；筛选结果 / 所选导出因后端接口尚不支持过滤参数，保留为禁用占位，只有 `导出全部` 走真实导出接口。`index.html` 提升 app / CSS 缓存版本，`styles.css` 补真实数据加载、空结果、只读字段和禁用按钮状态。未修改真实 `user_notes` 数据内容、正式 JSON 数据包、SQLite、原始 Excel、环境映射页、能力映射页、LC 页面、review checklist、标准包、字典包或 LC 数据包。验证通过：`node --check`、真实数据静态契约断言、旧 mock 文案扫描、工作台路由 HTTP smoke、`frontend_content_smoke_check --skip-api`、JSON 包边界、GitHub 数据边界、5173 服务状态、`git diff --check`；未启动系统 Chrome。
- 2026-07-01 `OI-159 L0/L1 关系图谱空白二次修复`：按用户截图“关系图谱没有显示，回退失败了”继续返工。根因不是数据缺失，而是第一次恢复时 `renderSummaryPanel()` 外层仍固定输出 `summary-panel`，但 `关系图谱` radio tab 只显示 `.technical-panel`，导致原 `LocalRelationNetworkGraph` 已在 DOM 里却被 CSS 隐藏。`CapabilityLocalRelationMap.js` 已让 `renderSummaryPanel()` 支持传入 panel class，并让 L0 / L1 的 `关系图谱` tab 使用 `technical-panel` 承载原关系图谱；`audit_capability_viewmodel_contract.mjs` 新增门禁，要求 `.technical-panel` 内必须出现 `local-relation-network-graph`，避免“有图但不可见”的假通过。验证时发现批注审计被全局抽屉 `Issue` 文案挡住，已将 `UserAnnotationDrawer.js` 中抽屉 tab / header / aria / 空态文案恢复为 `批注` 基线，不影响工作台 `Issue 总览` 页面命名。验证通过：相关 JS `node --check`、能力 ViewModel 契约审计（`T`、`T-AS`、`T-OF` 均为 `overview_no_detail_dom_with_relation_graph`）、批注契约审计、`frontend_content_smoke_check --skip-api`、JSON 包边界、GitHub 数据边界、5173 服务状态、`git diff --check`。未启动系统 Chrome；未修改正式 JSON、SQLite、原始 Excel、标准包、字典包、LC 数据包、环境映射页或用户批注数据库。
- 2026-07-01 `Workbench Module Formal Code Migration`：按用户确认将工作台原型迁移到正式工程。`AppShell.js` 在 Dashboard 后新增一级导航 `工作台`，子页面为 `成熟度评估` 与 `Issue 总览`，并登记正式路由 `/workbench`、`/workbench/annotations`、`/workbench/maturity`、`/workbench/maturity/project-001`；`index.html` 新增 `workbenchWorkspace` 容器并提升缓存版本；`app.js` 新增 Dashboard 工作台入口、工作台首页、Issue Review Queue、成熟度评估项目列表和项目详情静态渲染与轻量 mock 交互；`styles.css` 新增 Apple shell 工作台样式；`UserAnnotationDrawer.js`、`dataClient.js` 和顶部导出入口的用户可见文案从 `批注` 收口为 `Issue`，但内部 `annotation` / `user_notes` 标识保持不变，避免数据契约迁移。本轮未修改环境映射页、能力映射页、LC 页面、正式 JSON 数据包、SQLite、原始 Excel、review checklist、标准包、字典包、LC 数据包或真实 `user_notes` 数据。验证通过：相关 JS `node --check`、前端非数据文件 `批注` 文案扫描、`frontend_content_smoke_check --skip-api`、4 个工作台路由 HTTP smoke、JSON 包边界、GitHub 数据边界、5173 服务状态、`git diff --check`；未启动系统 Chrome。
- 2026-07-01 `OI-159 L0/L1 关系图谱恢复`：按用户“先把 L0-L1 的关系图谱恢复回来，替代现在的关系图谱 tab”要求，只修能力页 L0 / L1 的关系图谱 tab。`CapabilityLocalRelationMap.js` 中 L0 / L1 的 `关系图谱` tab 重新调用原 `renderSummaryPanel()` / `LocalRelationNetworkGraph` 组件，不再使用临时下级卡片式图；`总览`、`覆盖统计`、`下级索引` 仍保持轻量摘要，技术 / 管理 / 标准大明细表仍不在 L0 / L1 默认渲染。`audit_capability_viewmodel_contract.mjs` 新增门禁，要求 L0 / L1 渲染包含 `local-relation-network-graph` 且不包含 `semantic-mapping-table`、`preview-mapping-table` 或 `original-matrix-panel`。验证通过：相关 JS `node --check`、能力 ViewModel 契约审计（`T`、`T-AS`、`T-OF` 均为 `overview_no_detail_dom_with_relation_graph`）、批注契约审计、`frontend_content_smoke_check --skip-api`、JSON 包边界、GitHub 数据边界、5173 服务状态、`git diff --check`。本轮未修改 demo 工作台、环境映射页、LC 页面、正式 JSON 包结构、SQLite、原始 Excel、标准包、字典包、review checklist 或用户批注数据库。
- 2026-07-01 `Workbench Issue Review Queue Prototype`：用户已认可 `frontend/capability-browser/environment-object-apple-shell-demo.html` 中 `#workbench-module/workbench/annotations` Review Queue 方案；本轮完成命名收口，将工作台 demo 中用户可见的 `批注总览 / 批注处理工作台 / 批注范围 / 批注处理队列 / 选中批注` 等统一改为 `Issue 总览 / Issue 处理工作台 / Issue 范围 / Issue 处理队列 / 选中 Issue`，并在 `DESIGN.md` 记录后续工程命名规范：新 UI 文案统一使用 `Issue`，仅引用历史记录或 legacy code identifiers 时保留 `批注`。当前页面保持 Apple shell 三栏人工处理队列：左侧 Issue 范围导航，中栏 Issue Review Queue，右侧 inspector 编辑面板；保留统计筛选、范围导航、批量操作、未保存提示、保存回写、取消放弃、导出占位等静态原型交互。使用 mock data，不接真实 API，不修改成熟度评估页、正式 Dashboard、环境映射页、能力映射页、LC 页面、正式数据包、SQLite、原始 Excel、review checklist 或真实 `user_notes` 数据。
- 2026-07-01 `OI-159 能力页 L0/L1 总览型阅读模式落地`：已将能力页分层阅读策略落到正式页面。`viewModels.js` 新增 `capabilityOverview.detailPolicy`，L0 / L1 固定为 `overview`，映射量超过阈值的 L2 可进入 `mixed_summary`，关注点保持 `full_detail`；`app.js` 在 `overview` / `mixed_summary` 下不再把完整技术 / 管理 / 标准明细行传入关系组件；`CapabilityLocalRelationMap.js` 将 L0 / L1 tab 调整为 `总览 / 关系图谱 / 覆盖统计 / 下级索引`，默认展示结构统计、覆盖摘要、轻量关系图和下钻入口；`styles.css` 补 Apple shell 轻量总览样式；`index.html` 与动态组件 URL 提升缓存版本。`audit_capability_viewmodel_contract.mjs` 新增 L0 / L1 overview policy 与 `overview_no_detail_dom` 渲染断言。验证通过：相关 JS `node --check`、能力 ViewModel 契约审计、批注契约审计、`frontend_content_smoke_check --skip-api`、JSON 包边界、GitHub 数据边界、5173 服务状态、`git diff --check`。未启动系统 Chrome，未修改环境映射页、工作台模块、LC 页面、正式 JSON 包结构、SQLite、原始 Excel、标准包、字典包、review checklist 或用户批注数据库。
- 2026-07-01 `OI-160 Knowledge Directory Sticky Tabs Fix`：按用户截图反馈修复知识库字典维护页顶部页签不冻结、滚动后被下方内容遮挡的问题。`styles.css` 将 `#maintenanceWorkspace.knowledge-directory-mode .maintenance-shell-head` 改为滚动区内 sticky 顶栏，补背景遮罩、层级和 `scroll-padding-top`，并让 `maintenance-section-tabs` 保持在表格 / details 内容之上；`index.html` 提升 CSS 缓存版本。新增并关闭 `OI-160`，适用同类 `maintenance-section-tabs` 页面，重点覆盖 `安全职能清单`、`GB/T 42446-2023`、`Gartner 工作岗位参考`。本轮未修改原始 Excel、SQLite、ViewModel、ETL、正式业务数据包或用户批注数据库。
- 2026-07-01 `OI-159 能力页 L0/L1 总览型阅读模式登记`：按用户提醒核对 `OI-149` 设计记录，确认“能力页 L0 / L1 参考环境模块，仅保留关联图谱和统计摘要，不再默认铺大批量技术 / 管理 / 标准映射明细”此前只埋在 `OI-149` P4-B 设计和 progress 中，没有独立 open issue。已新增 `OI-159`，作为后续产品设计 / 前端实现任务独立追踪；本轮只更新 issue / progress 文档，不修改主控当前设计文件、前端代码、正式 JSON、split 分片、SQLite、原始 Excel、标准包、字典包、LC 数据包或用户批注数据库。
- 2026-07-01 `OI-038 Gartner 候选映射人工确认关闭`：按用户逐条确认结果，将 28 条 Gartner 岗位参考到安全职能的候选映射收敛为人工确认映射，共 46 条确认后的安全职能引用。新增 `scripts/apply_oi038_gartner_mapping_review.mjs` 和 `scripts/audit_oi038_gartner_mapping_review.mjs`；同步更新 `data/exports/worker-verify/sheet-review-2-2-gartner-to-work-function-candidates.csv`、`frontend/capability-browser/public/data/maintenance-knowledge.json`、`frontend/capability-browser/public/data/maintenance/references.json`、`frontend/capability-browser/public/data/maintenance/sections/references.json`。关闭性审计通过：三份运行数据均为 `gartnerRoles=28`、`reviewedRoles=28`、`confirmedRoles=28`、`confirmedFunctionRefs=46`、`issueCount=0`，所有 code / id / title 均命中 `work-functions.json` 标准字典。已将 `OI-038` 标记为 `已关闭` 并通过 `node scripts/govern_open_issues.mjs` 归档，当前未关闭问题数为 `20`。本轮未修改原始 Excel、SQLite、能力 / 环境 / LC / 标准数据包或用户批注数据库。
- 2026-07-01 `OI-139 Issue Close And Archive`：按用户“如果当前没问题，关闭这个 Issue，归档当前会话”要求，将 `OI-139` 从 `已修复 / 数据链审计通过 / 待页面人工验收` 更新为 `已关闭`，并通过 `node scripts/govern_open_issues.mjs` 归档到 `docs/05-archive/open-issues-history/2026-06.md`；当前未关闭问题数从 `22` 降到 `21`。本轮只处理 OI-139 issue/progress 文档，不修改主控当前设计文件、正式 JSON、split 分片、SQLite、原始 Excel、标准包、字典包、LC 数据包、环境底图 node-details 或用户批注数据库。
- 2026-07-01 `Workbench Module Multi-page Prototype Demo`：按用户纠偏，已将 `frontend/capability-browser/environment-object-apple-shell-demo.html` 的 `工作台设计` tab 从方案对比页改为真实多子页面产品原型；原型固定 Apple / visionOS 轻量工作台方向，支持点击切换 `#workbench-module/dashboard`、`#workbench-module/workbench`、`#workbench-module/workbench/annotations`、`#workbench-module/workbench/maturity`、`#workbench-module/workbench/maturity/project-001`。当前目录中 `工作台` 位于 `Dashboard` 后第二位，工作台下只挂 `成熟度评估` 和 `批注总览` 两个子页面；页面分别表达 Dashboard 目录入口、工作台首页、批注总览页、成熟度评估页和成熟度评估项目详情页。成熟度评估页已补评估工作流暂存、历史项目查询、编辑和导出占位。使用 mock data，不接真实 API，不修改正式 Dashboard、环境映射页、能力映射页、LC 页面、数据包、SQLite、原始 Excel、review checklist 或用户批注数据库。验证通过：内联脚本语法检查、关键静态内容搜索、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`；未启动系统 Chrome。
- 2026-07-01 `OI-139 Environment Mapping Data Chain Closure`：按用户要求在本会话完成 `OI-139`。本轮只做只读审计和 issue/progress 状态更新，未修改正式 JSON、split 分片、SQLite、原始 Excel、标准包、字典包、LC 数据包、环境底图 node-details 或用户批注数据库。关闭依据：`audit_scope_service_module_mapping.py` 返回 `result=pass`，当前原始 Sheet normalized 后 `871` 行，`objectScopeRelationCount=163`、`objectServiceRelationCount=778`、`serviceModuleRelationCount=690`、`serviceMeasureRelationCount=181`、`moduleSystemRelationCount=217`，`pendingRelationCount=0`、`invalidRowCount=0`、完整重复关系 `0`、重复服务上下文 `0`、作用域完整性问题 `0`；`audit_environment_module_catalog_consistency.py` 返回 `moduleServiceMismatchCount=0`、`systemModuleMismatchCount=0`、`securitySystemCoverageOrMismatchCount=0`、`moduleMeasureClassificationIssueCount=0`；`audit_environment_page_business_logic.mjs` 返回 `expectedTargetKeys=871`、`actualTargetKeys=871`、`missingTargetCount=0`、`missingSystemCount=0`、`unexpectedSystemCount=0`、`ownTargetSystemMissingCount=0`、`mismatchRows=0`；正式 OI-149 split runtime 契约通过，环境 navigator `environmentCount=10`、`projectionCount=144`，navigator 中 `144` 个 projection 条目均有正式文件。`OI-139` 已通过关闭性复核，随后按用户确认关闭并归档。
- 2026-07-01 `OI-156 Apple 侧边把手与锚点契约补丁`：按用户截图反馈，右侧批注抽屉收起态触发器从大半圆数字徽章改为低噪声 Apple shell 窄边缘把手，保持贴 viewport 右侧、恒定尺寸和整体 `transform` 收回；`audit_user_annotation_contract.mjs` 同步增加窄把手防回归契约。`global-annotation-requirements-and-regression-matrix.md` 增补全局锚点准入要求：后续页面 / ViewModel / projection 修改必须保留稳定 `page_route`、`target_ref`、对象身份字段、值级上下文和旧批注兼容映射，定位优先走运行时索引，文本扫描只能作为受限兜底。未修改 OI-149 split manifest、正式 JSON 分片、SQLite 数据内容、原始 Excel、标准包、字典包、LC 数据包或用户批注数据库。
- 2026-07-01 `Unified Demo Workspace And Color Demo Merge`：按用户要求将颜色系统 demo 合并到 `frontend/capability-browser/environment-object-apple-shell-demo.html` 的统一 demo 工作台，默认打开 `颜色系统` tab，支持 `#color-system`、`#funnel` 等 hash 直达；移除用户可见的 `当前布局` tab，保留旧布局 DOM 作为隐藏兼容层，避免旧连线脚本因节点缺失报错。顶部文案从环境对象 demo 改为 `SAPD Wiki Demo 工作台`，旧 `apple-morandi-color-demo.html` 直接打开时会跳转到统一工作台、作为 iframe 嵌入时保留显示；并更新 `DESIGN.md`、前端展示原则、全局设计基线、Apple 色彩交接和执行线文档中的 canonical demo URL：后续新页面 demo 统一加入 `http://127.0.0.1:5173/environment-object-apple-shell-demo.html`，颜色基准入口为 `#color-system`。本轮未修改正式业务页面、正式 JSON、SQLite、原始 Excel、标准包、字典包、LC 数据包、环境数据包或用户批注数据库。
- 2026-07-01 `OI-156 批注治理 7 项落地`：按用户要求处理批注记录上下文不足、无效批注清理、测试批注数据、批注与前后端解耦原则、批注索引、抽屉右侧贴边和收回动画。`app.js` 新增 `userNoteIndex` 运行时索引和每条批注的业务上下文生成；`UserAnnotationDrawer.js` 在折叠卡片内显示页面 / 所属能力或对象 / 视角等上下文；`styles.css` 将抽屉改为贴 viewport 右侧、恒定侧耳尺寸、整体 `transform` 收回；`api_server.py` 与 `run_local_server.py` 为 `user_notes` 声明 page / target / anchor / object / status 查询索引；新增 `scripts/audit_user_notes_integrity.mjs` 和 `scripts/seed_user_annotation_test_notes.mjs`，默认 dry-run，不写用户库；`global-annotation-requirements-and-regression-matrix.md` 补上下文、索引、清理、测试数据和抽屉动画原则；`open-issues.md` 更新 `OI-156` 状态。本轮未修改正式 JSON、OI-149 split 数据、SQLite 数据内容、原始 Excel、标准包、字典包、LC 数据包、环境数据包或用户批注数据库。
  - 验证命令：`node --check frontend/capability-browser/app.js`、`node --check frontend/capability-browser/components/UserAnnotationDrawer.js`、`node --check scripts/audit_user_annotation_contract.mjs`、`node --check scripts/audit_user_notes_integrity.mjs`、`node --check scripts/seed_user_annotation_test_notes.mjs`、`python3 -m py_compile src/sapd_wiki/api_server.py scripts/run_local_server.py`、`node scripts/audit_user_annotation_contract.mjs`、授权本机 dry-run `node scripts/audit_user_notes_integrity.mjs --url http://127.0.0.1:5173/ --write-report`、授权本机 dry-run `node scripts/seed_user_annotation_test_notes.mjs --url http://127.0.0.1:5173/`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`，全部通过或完成。无效批注审计结果：45 条批注 `invalid_count=0`，发现 1 组同 route / target / body 疑似重复，需人工确认是否清理；测试批注 dry-run 生成 17 条覆盖能力、环境、LC、知识库、标准和指南。未启动系统 Chrome，本轮贴边 / 动画仍待人工视觉验收或用户明确批准后执行真实浏览器截图级回归。
- 2026-06-30 `OI-156 能力页值级批注 projection 恢复`：按用户截图继续修复 `/capability-mapping` 能力映射模块值级批注定位不到 / 不高亮。根因不是单纯没有 DOM 索引，而是 OI-149 后 L0 / L1 总览 projection 不再默认渲染完整标准 / 技术 / 管理明细，旧批注 `base:field_value:v2` 中的 capability id 也可能已不在当前导航树，导致当前 DOM 索引里没有目标值。`app.js` 新增能力页批注 projection 懒索引恢复：定位前若当前 DOM 无目标锚点，则按需读取 `capability/index.json` projection 清单，命中包含批注值的对象分片后恢复正确能力对象和 tab，再走运行时 anchor index；同时补业务值归一，兼容 `安全技术模块...` 前缀和 `持续管理流程组` 等旧文本。`index.html` 提升 `app.js` 缓存版本，`audit_user_annotation_contract.mjs` 增加能力值级 projection 懒索引门禁。未修改 OI-149 split manifest、正式 JSON 分片、SQLite、原始 Excel、标准包、字典包、LC 数据包、环境数据包或用户批注数据库。
  - 验证命令：`node --check frontend/capability-browser/app.js`、`node --check frontend/capability-browser/components/UserAnnotationDrawer.js`、`node --check scripts/audit_saved_user_annotations.mjs`、`node --check scripts/audit_user_annotation_contract.mjs`、`node scripts/audit_user_annotation_contract.mjs`、真实 Chrome 全量回归 `node scripts/audit_saved_user_annotations.mjs --url 'http://127.0.0.1:5173/?v=env-fix-7#/capability-mapping' --allow-system-chrome --compact` 通过，45 条保存批注 `passed=45 failed=0`，覆盖 `ALL` 行级、`ISO/IEC 27001:2022`、`PR.DS-02`、`AT-6`、环境页服务 / 系统、LC 和指南 / 幻灯片等对象 / 行 / 值 / 页面批注；`node scripts/frontend_content_smoke_check.mjs --skip-api`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/dev_server_guard.py --status`、`git diff --check` 通过。
- 2026-06-30 `OI-156 调度 + OI-158 关闭 + 信息化环境视图 tab 小修`：按用户指示，已向现有 `issue 处理` 会话 `019f1622-6d2c-78e2-adc5-c234a44f5b8c` 派发 `OI-156 全局批注定位 / 高亮性能退化导致页面假死` 终态修复任务，要求其不修改 `OI-149` split 数据和主控本轮 UI 小修范围。`OI-158` 已由用户确认可先关闭，并通过 `node scripts/govern_open_issues.mjs` 归档，当前未关闭问题数从 `23` 降到 `22`。信息化环境页默认仍为 `topology` 底图视图；主控将顶部 tab 文案统一从 `环境底图 / 环境层级视图` 改为 `信息化环境视图`，并在从其他模块进入环境页时重置为底图视图。本轮未修改正式 JSON、SQLite、原始 Excel、标准包、字典包、LC 数据包、环境数据包或用户批注数据库。
  - 验证命令：`node --check frontend/capability-browser/app.js`、`node --check frontend/capability-browser/components/AppShell.js`、静态 tab 文案 / 默认底图契约断言、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`，全部通过。未默认启动系统 Chrome。
- 2026-06-30 `OI-157 批注一键导出 Markdown 化`：按用户要求将首页顶部“批注一键导出”下载产物从 JSON 改为 Markdown。`src/sapd_wiki/api_server.py` 与 `scripts/run_local_server.py` 现在复用批注 payload 生成可读 `.md`，内容包括导出摘要、状态 / 页面 / 锚点 / 对象统计、页面路由、对象标题、标签、创建更新时间和批注正文；`frontend/capability-browser/dataClient.js` 与 `app.js` 同步将下载兜底文件名和 MIME 期望改为 Markdown。`docs/06-implementation/open-issues.md` 已同步更新 `OI-157` 说明。验证通过：`node --check frontend/capability-browser/app.js`、`node --check frontend/capability-browser/dataClient.js`、`python3 -m py_compile src/sapd_wiki/api_server.py scripts/run_local_server.py`、`python3 scripts/dev_server_guard.py --restart`、授权本机抽样 `GET /api/v1/user/notes/export?download=1` 返回 `text/markdown` 和 `user-notes-export-*.md`，正文以 `# SAPD Wiki 批注导出` 开头且不是 JSON；`node scripts/frontend_content_smoke_check.mjs --skip-api`、`python3 scripts/dev_server_guard.py --status`、`git diff --check` 通过。未修改用户批注数据库、正式业务 JSON、SQLite、原始 Excel、标准包、字典包、LC 数据包或 DMG 产物。
- 2026-06-30 `OI-158 指南幻灯片响应式与翻页控件修复`：按用户截图反馈修复 Web 端幻灯片随窗口拖动尺寸异常、底部左右翻页控件不可用、DMG 中幻灯片显示大小不一致的问题。已核对源图均为 16:9，问题来自前端播放器容器而非图片导出。`app.js` 现在从指南包或 content index 读取 `slide_width / slide_height` 并输出稳定比例 CSS 变量；`styles.css` 将 slide stage 收口为固定比例画布，并用视口高度约束最大宽度，避免宽屏下无限放大；翻页控件从批注锚点 stage 内移到播放器层，常态可见，提升层级并保持可点击；`index.html` 提升 `app.js` / `styles.css` 缓存版本。同步更新 `frontend_smoke_check.mjs` 的幻灯片契约，要求 stage 接近 16:9、不得溢出视口、控件常态可见且可点击翻页。本轮未修改正式 JSON、SQLite、原始 Excel、标准包、字典包、LC 数据包、环境数据包、用户批注数据库或 DMG 产物。
  - 验证命令：`node --check frontend/capability-browser/app.js`、`node --check scripts/frontend_smoke_check.mjs`、`node --check scripts/frontend_content_smoke_check.mjs`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、静态幻灯片契约断言、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`，全部通过。未默认启动系统 Chrome；DMG 真实验收需要重新打包或用户打开新版页面确认。
- 2026-06-30 `SAPD Wiki macOS App 支线`：DMG 支线、启动与能力页修复、测试机 Gatekeeper / quarantine 修复长记录已归档到 `docs/05-archive/progress-history/2026-06.md`。当前仅保留关键结论：独立 macOS App wrapper、Runtime 指纹复用、后端 API 补齐、quarantine 清理和 DMG 打包验证已完成；该支线未修改正式 JSON、SQLite、原始 Excel、标准包、字典包、LC 数据包或用户批注数据库。
- 2026-06-30 `OI-156 全局批注定位 / 高亮根因修复`：按用户截图复核，问题不是个例，而是批注 active 高亮生命周期、环境汇聚图样式覆盖、以及环境页上下文恢复未兼容 `environment_scope_tree` / `scope_mappings` 的组合缺陷：旧逻辑没有从全量环境对象树恢复到包含目标服务 / 模块 / 措施 / 安全系统的对象，导致当前页第 2 条 `I-AP&T-AS.AD-02 应用冗余` 和第 3 条 `零信任访问控制台` 均找不到 anchor。仍只修批注链路，不改 split manifest、`public/data` 分片、原始 Excel、SQLite、标准包、字典包、LC 数据包或用户批注数据库。`app.js` 保持既有 `12` 次 / `1800ms` 熔断和 `annotationJumpToken`，新增当前定位批注持久态，补环境对象选择遍历、`scope_mappings[].services`、service 下 modules / measures / systems 扫描和 title 前缀 code alias；环境图定位新增横向容器滚动。`styles.css` 新增环境汇聚图专用 active / marked overlay。`index.html` 提升 `app.js` 缓存版本；`audit_user_annotation_contract.mjs` 增加 active 持久化、环境图 overlay、`environment_scope_tree` / `scope_mappings` 门禁。真实 Chrome 回归通过：环境页当前第 2 条 `ordinal=5`、第 3 条 `ordinal=6` 均 `found=true`、`normalVisualHighlight=true`、`activeVisualHighlight=true`、`persistentAfterClick=true`；第 3 条旧值级批注命中 `零信任访问控制台` 节点。其余验证继续执行。
- 2026-06-30 `OI-149 P4 split runtime contract`：继续推进 OI-149，但仍未执行正式 JSON 分层 apply，未覆盖正式 `frontend/capability-browser/public/data/*.json`。候选生成脚本现在会在候选 `public-data` 中输出 `oi149-split-manifest.json`，声明 `oi149-p4-split-v1`、能力首屏 `capability/index.json`、环境 navigator / projection、LC、维护、shared-lookups 和 standards 分片路径。`dataClient.js` 新增 split manifest 探测：正式目录存在 manifest 且契约匹配时，`getCapabilityWorkspaceInitial()` 优先读取 `capability/index.json` 并转换为当前 App 可消费的 initial workbench；manifest 缺失时保持旧行为，回退 `/api/v1/capabilities/workspace-initial`，再回退完整 `capability-workbench.json`。对象详情仍走现有 `/api/v1/capabilities/workspace-view`，不把新候选 projection 直接塞进旧 ViewModel。新增 `scripts/audit_oi149_split_runtime_contract.mjs`，用 fake fetch 证明 manifest 存在时不会请求 workspace-initial API，manifest 缺失时才回退 API；同步提升 `dataClient.js` 缓存版本。未修改 SQLite、原始 Excel、正式数据包、标准包、字典包、LC 数据包、环境数据包或用户批注数据库。
  - 验证命令：`node --check frontend/capability-browser/dataClient.js`、`node --check frontend/capability-browser/app.js`、`node --check scripts/build_oi149_split_candidate.mjs`、`node --check scripts/audit_oi149_split_candidate.mjs`、`node --check scripts/audit_frontend_lazy_load_contract.mjs`、`node --check scripts/audit_oi149_split_runtime_contract.mjs`、`node scripts/build_oi149_split_candidate.mjs`、`node scripts/audit_oi149_split_candidate.mjs`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/audit_oi149_split_runtime_contract.mjs`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`node scripts/audit_search_state_isolation.mjs`、`node scripts/audit_frontend_route_refresh_contract.mjs`、`node scripts/audit_capability_viewmodel_contract.mjs --url http://127.0.0.1:5173`、`node scripts/audit_global_search_index_contract.mjs --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page capability-mapping --route /capability-mapping --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page environment-mapping --route /environment-mapping --url http://127.0.0.1:5173`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/data_package_summary.py --package all`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`，全部通过。未启动系统 Chrome。

- 2026-06-30 `OI-149 P4 formal apply + post-apply readiness`：已按用户“继续执行”完成正式 split apply。`node scripts/apply_oi149_split_candidate.mjs --apply --confirm-oi149-public-data-write` 返回 `apply_pass`：从候选 `public-data` 创建 `315` 个正式分片文件，`wouldOverwriteCount=0`、`backupFileCount=0`，旧全量包未删除；apply 报告为 `data/exports/worker-verify/oi-149-p4-json-split-candidate/formal-apply-reports/oi149-p4-apply-20260630T123957Z.json/md`。正式 `public/data` 现包含 `oi149-split-manifest.json`、`capability/`、`environment/`、`lifecycle/`、`maintenance/`、`shared-lookups/`、`standards/` 分片目录；旧 `capability-workbench.json`、`environment-workbench.json`、`lifecycle-workbench.json`、`maintenance-knowledge.json`、`shared-lookups.json` 等全量包继续保留作为 fallback。完整 post-apply readiness 通过：`node scripts/audit_oi149_readiness.mjs --mode postapply --url http://127.0.0.1:5173` 返回 `commandCount=27`、`failedCommandCount=0`、`failedGateCount=0`；离线 post-apply readiness 也通过 `commandCount=22`。回退方式：删除本次新增 split manifest / 分片目录即可恢复旧全量包路径；如未来出现 overwrite，再按 apply report 和 `formal-apply-backups/<timestamp>/` 恢复。
  - 验证命令：`node --check frontend/capability-browser/app.js`、`node --check frontend/capability-browser/dataClient.js`、`node --check frontend/capability-browser/components/UserAnnotationDrawer.js`、`node --check frontend/capability-browser/components/AppShell.js`、`node --check scripts/build_oi149_split_candidate.mjs`、`node --check scripts/apply_oi149_split_candidate.mjs`、`node --check scripts/audit_oi149_readiness.mjs`、`node --check scripts/audit_oi149_split_runtime_contract.mjs`、`node scripts/audit_oi149_split_runtime_contract.mjs --source formal`、`node scripts/audit_oi149_readiness.mjs --mode postapply --skip-runtime`、`node scripts/audit_oi149_readiness.mjs --mode postapply --url http://127.0.0.1:5173`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/audit_oi149_split_candidate.mjs`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`node scripts/frontend_smoke_check.mjs --page capability-mapping --route /capability-mapping --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page environment-mapping --route /environment-mapping --url http://127.0.0.1:5173`、`node scripts/audit_search_state_isolation.mjs`、`node scripts/audit_frontend_route_refresh_contract.mjs`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/data_package_summary.py --package all`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`，全部通过。未启动系统 Chrome；未修改原始 Excel、SQLite、标准包、字典包、LC 数据包、用户批注数据库。

- 2026-06-30 `OI-149 P4 runtime-ready split payload + environment projection contract`：继续推进正式 apply 前的运行时契约，但仍未执行正式 apply，未覆盖正式 `frontend/capability-browser/public/data/*.json`。修正候选生成脚本，候选 `public-data` 中待复制到正式运行目录的分片文件现在为 `dataState=ready`，`packageType` 不再携带 `candidate` 语义；外层 `candidate-manifest.json` 仍标识候选目录和 `formalPublicDataModified=false`。候选审计新增 `runtime_public_data_not_candidate_state` 门禁。`dataClient.js` 新增 `getEnvironmentNavigatorFromSplit()` / `getEnvironmentNavigator()` / `getEnvironmentWorkspaceProjection()`：正式 split manifest 存在时，环境首屏导航优先读取 `environment/navigator.json`，环境详情按 `projectionKey/path/id/type` 读取对应 `environment/projections/*`；manifest 缺失时回退旧 `environment-workbench`。`index.html` 已提升 `dataClient.js` 缓存版本到 `oi149-env-projection-20260630-1`。
  - 验证命令：`node --check frontend/capability-browser/dataClient.js`、`node --check scripts/build_oi149_split_candidate.mjs`、`node --check scripts/audit_oi149_split_candidate.mjs`、`node --check scripts/audit_oi149_split_runtime_contract.mjs`、`node --check scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/build_oi149_split_candidate.mjs`、`node scripts/audit_oi149_split_candidate.mjs`、`node scripts/audit_oi149_split_runtime_contract.mjs`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/apply_oi149_split_candidate.mjs`。未修改 SQLite、原始 Excel、正式数据包、标准包、字典包、LC 数据包、环境数据包或用户批注数据库。

- 2026-06-30 `OI-149 Fix-A + P4-B 能力页加载态与总览型候选`：按用户处理建议继续推进，但未执行正式 P4 apply，未覆盖正式 `frontend/capability-browser/public/data/*.json`。Fix-A：`app.js` 中能力页相关 `packageLoads` / projection 请求在 `finally` 清理后会再次触发 `renderCapabilities()`；能力对象 projection mismatch / error 会记录到 `capabilityProjectionLoadResults`，进入“加载失败 / 已回退到已有视图数据 / 重试加载”状态，不再无限显示加载态；用户点击 `重试加载` 才会清掉失败状态重新请求；`index.html` 已提升 `app.js` 缓存版本到 `oi149-loading-state-20260630-1`。新增懒加载审计门禁，防止后续删除 `finally -> renderCapabilities()`、失败态或 retry 入口。能力 ViewModel 审计新增 `T-PD.AC-01` 样本，运行时验证为 `backend_projection`，标准控制项 `41`。P4-B：候选拆包中能力 L0 / L1 projection 改为总览型，不默认带完整 `objects` / `relations` 明细、完整标准控制项或完整映射行；L2 可按阈值为 `detail` / `mixed_summary`，关注点保留 `detail`。重新生成候选后首屏最大 `758.7 KB`，详情最大 `915.5 KB`，字段边界失败 `0`；能力 projection 摘要为 `capability_category:overview=3`、`capability_domain:overview=10`、`capability:detail=31`、`capability:mixed_summary=1`、`capability_focus:detail=91`；environment projection 唯一命中仍为 `duplicateProjectionPathCount=0`。
  - 验证命令：`node --check frontend/capability-browser/app.js`、`node --check scripts/audit_frontend_lazy_load_contract.mjs`、`node --check scripts/audit_capability_viewmodel_contract.mjs`、`node --check scripts/build_oi149_split_candidate.mjs`、`node --check scripts/audit_oi149_split_candidate.mjs`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/build_oi149_split_candidate.mjs`、`node scripts/audit_oi149_split_candidate.mjs`、`node scripts/audit_capability_viewmodel_contract.mjs --url http://127.0.0.1:5173`、`node scripts/audit_search_state_isolation.mjs`、`node scripts/audit_global_search_index_contract.mjs`、`node scripts/audit_global_search_index_contract.mjs --url http://127.0.0.1:5173`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`node scripts/frontend_smoke_check.mjs --page capability-mapping --route /capability-mapping --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page environment-mapping --route /environment-mapping --url http://127.0.0.1:5173`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/data_package_summary.py --package all`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`，全部通过。未修改 SQLite、原始 Excel、标准包、字典包、LC 数据包、正式环境包或用户批注数据库。

- 2026-06-30 `OI-149 P4 environment projection key pre-apply guard`：按用户确认，接受 P4 候选方向、预算、顶层能力标准明细延迟加载和 `shared-lookups` 暂不继续拆，但正式 apply 前先修复 environment projection key 并补 `navigator -> projection` 唯一命中审计。本轮仅修改候选生成 / 审计脚本和治理文档，不进入正式 apply，不覆盖 `frontend/capability-browser/public/data/*.json`。`environment` navigator 仍有重复 `node.id` 的业务现状，但候选拆包不再用 `node.id` 作为 projection 文件唯一键；每个导航行写入 `navOrdinal`、`projectionKey`、`projectionPath` 和 `navigationPath`，projection 本体回写同一组字段，审计会阻断重复 path、缺文件、roundtrip 不一致和 projection index 不匹配。重新生成候选后：`navigatorRows=144`、`projectionIndexRows=144`、`projectionFileCount=144`、`duplicateProjectionPathCount=0`，首屏最大 `758.7 KB`，详情最大 `1202.2 KB`，字段边界失败 `0`。
  - 验证命令：`node --check scripts/build_oi149_split_candidate.mjs`、`node --check scripts/audit_oi149_split_candidate.mjs`、`node scripts/build_oi149_split_candidate.mjs`、`node scripts/audit_oi149_split_candidate.mjs`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/audit_search_state_isolation.mjs`、`node scripts/audit_global_search_index_contract.mjs`、`node scripts/audit_global_search_index_contract.mjs --url http://127.0.0.1:5173`、`node scripts/audit_capability_viewmodel_contract.mjs --url http://127.0.0.1:5173`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`node scripts/frontend_smoke_check.mjs --page capability-mapping --route /capability-mapping --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page environment-mapping --route /environment-mapping --url http://127.0.0.1:5173`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/data_package_summary.py --package all`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`，全部通过；正式数据包、SQLite、原始 Excel、标准包、字典包、LC 数据包和用户批注数据库未修改。

- 2026-06-30 `OI-149 P4 候选拆包与预算审计`：继续推进 JSON 加载治理，但未 apply 正式数据包。新增 `scripts/build_oi149_split_candidate.mjs` 和 `scripts/audit_oi149_split_candidate.mjs`，从当前正式运行包生成候选拆包到 `data/exports/worker-verify/oi-149-p4-json-split-candidate/`。候选结构包括 capability index / object projections / evidence、environment navigator / object projections / evidence、lifecycle index / process projections / evidence、maintenance section split、shared-lookups service-module index、standards index。审计通过：首屏候选最大 `758.7 KB`，对象详情候选最大 `1202.2 KB`，字段边界失败 `0`；正式 `frontend/capability-browser/public/data/*.json` 未修改。
  - 验证命令：`node --check scripts/build_oi149_split_candidate.mjs`、`node --check scripts/audit_oi149_split_candidate.mjs`、`node scripts/build_oi149_split_candidate.mjs`、`node scripts/audit_oi149_split_candidate.mjs`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/audit_search_state_isolation.mjs`、`node scripts/audit_global_search_index_contract.mjs`、`node scripts/audit_global_search_index_contract.mjs --url http://127.0.0.1:5173`、`node scripts/audit_capability_viewmodel_contract.mjs --url http://127.0.0.1:5173`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`node scripts/frontend_smoke_check.mjs --page capability-mapping --route /capability-mapping --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page environment-mapping --route /environment-mapping --url http://127.0.0.1:5173`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/data_package_summary.py --package all`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`。完整回归通过；含本机 fetch 的脚本需在允许访问 5173 的执行上下文运行。

- 2026-06-30 `OI-149 P1/P2 完整收口`：继续推进 JSON 加载治理。新增 `/api/v1/search-index` 运行时轻量索引，前端全局搜索优先调用 `dataClient.getSearchIndex()`，不在输入阶段预加载完整 workbench、完整字典、环境全量包或标准详情全表；保留当前会话已加载数据作为补充结果。能力页 L0 / L1 / L2 / 关注点对象级 projection 已补齐聚合 `localRelationMap` 与 `localRelationMapsByFocusId`，`audit_capability_viewmodel_contract` 样本全部收口为 `backend_projection`。新增 `scripts/audit_global_search_index_contract.mjs` 并更新搜索状态审计，防止搜索链路回退为全包扫描。未修改正式 `public/data/*.json`、SQLite、原始 Excel、标准包、字典包或用户批注数据库。
  - 验证命令：`python3 -m py_compile src/sapd_wiki/api_server.py`、`node --check` 覆盖 `app.js` / `dataClient.js` / `viewModels.js` / 新增审计脚本、`node scripts/audit_global_search_index_contract.mjs --url http://127.0.0.1:5173`、`node scripts/audit_capability_viewmodel_contract.mjs --url http://127.0.0.1:5173`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/audit_search_state_isolation.mjs`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`node scripts/frontend_smoke_check.mjs --page capability-mapping --route /capability-mapping --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page environment-mapping --route /environment-mapping --url http://127.0.0.1:5173`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/data_package_summary.py --package all`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`。含本机 fetch 的脚本需在允许访问 5173 的执行上下文运行，结果通过。

- 2026-06-30 `OI-149 P2/P3 实施`：完成能力页 projection 回退收口与后端包缓存两项收口。`app.js` 去掉能力焦点/非焦点向 `capabilityWorkbench` 的兜底强制加载入口，`api_server.py` 增加读取缓存（按 path/mtime/size key）以收紧后端重复 parse；同时保持 fallback 记录与兼容 fallback 的行为未替换。
  - 验证命令：`node scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/audit_capability_viewmodel_contract.mjs --url http://127.0.0.1:5173`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/data_package_summary.py --package all`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`。其中 `audit_capability_viewmodel_contract` 在受限环境需用可联网执行上下文运行，结果 `pass`。

- 2026-06-30 `OI-149 P0 验证收口`：完成 OI‑149 止血链路的完整验收并确认未触发数据加载回退。`dataClient.getHealth()` 与全局搜索不再预加载全量业务包；`getHealth` 仅做运行健康读取。执行命令：`node --check frontend/capability-browser/dataClient.js`、`node --check frontend/capability-browser/app.js`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/dev_server_guard.py --status`、`python3 scripts/data_package_summary.py --package all`、`node scripts/audit_frontend_route_refresh_contract.mjs`、`node scripts/audit_search_state_isolation.mjs`、`node scripts/frontend_smoke_check.mjs --page capability-mapping --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page environment-mapping --url http://127.0.0.1:5173`。全部结果通过；5173 页面返回 `200`，所有数据包 `data_state=ready`，未报告 JSON 边界异常与 GitHub 边界异常。
- 2026-06-30 `Global Search Demo Tabs`：按 `OI-155` 设计先新增全局搜索方案 demo，随后按用户纠偏改为参考现有工程页面的 `全局搜索页` tab，并补 6 个现有页面内搜索栏测试样例。该内容只修改 demo 文件和进度记录，未接入正式 `search-index`、未读取正式 JSON、未改正式搜索逻辑或页面路由；相关内联脚本、内容 smoke、5173 状态和本地 HTTP 抽样通过，未启动系统 Chrome。
- 2026-06-30 `Global Search Product Redesign`：按用户要求重新设计全局搜索功能，新增 `docs/06-implementation/global-search-redesign-2026-06-30.md`。本轮设计结论为：顶部保留一个全局搜索入口，新增独立搜索结果页 `#/search?q=...`，页面内搜索保留但明确限定为当前模块 / 当前树 / 当前表格 / 当前映射图的局部查找或筛选工具。全局搜索输入阶段只能走轻量 `search-index`，不得加载完整 workbench、完整字典、完整标准详情或环境全量包。已新增 `OI-155` 作为全局搜索产品形态与搜索结果页重设计问题，新增 `OI-154` 作为页面内搜索功能回归与环境页搜索不可用问题，并更新 `docs/06-implementation/search-logic-design.md` 和 `docs/06-implementation/oi-149-json-loading-governance-design.md`，明确 `OI-149` 只负责搜索加载边界和索引预算，不负责搜索产品形态。本轮仅修改文档，未改运行代码、未修改正式 JSON、SQLite、原始 Excel、标准包、字典包、LC 数据包或用户批注数据库。
- 2026-06-29 `OI-149 JSON Loading Governance Design`：按用户要求重新设计 `OI-149` 治理方案，新增 `docs/06-implementation/oi-149-json-loading-governance-design.md`。本轮确认慢加载反复出现不是单一包体过大，而是加载职责边界被多处绕过：`dataClient.getHealth()` 会触发全部 `DATA_PATHS` 预热；全局搜索输入会加载 `capability`、`maintenanceKnowledge`、`lifecycle`、`lifecycleWorkbench`、`content`、`standards`、`environmentWorkbench` 并加载标准详情全表；能力页 L0 / L1 / L2 样本仍为 `viewmodel_fallback`，关注点样本为 `backend_projection`；后端 `capability_workspace_projection()` 每次请求会重复读取大 JSON 且缺包级缓存。治理设计将 OI-149 拆为 P0 止血与可观测、P1 搜索索引独立化、P2 能力页 projection 收口、P3 后端包缓存、P4 正式 JSON 分层，并明确可能导致不可用的页面影响面和必跑验证。已更新 `docs/06-implementation/open-issues.md` 状态为 `治理设计完成 / 待 P0 实施`。本轮未修改运行代码、未修改正式 JSON、未写 SQLite 或原始 Excel。
- 2026-06-29 `Annotation Drawer Expanded Note Visibility Fix`：按用户截图修复右侧批注抽屉靠底部批注展开后只显示标题、正文和操作区被底部截住的问题。`UserAnnotationDrawer.js` 新增 `ensureAnnotationNoteFullyVisible()` 和 `scheduleAnnotationNoteVisibilityCheck()`：当 `details.annotation-note-card` 展开时，在布局完成后计算卡片与 `.annotation-drawer-scroll` 可视区，底部超出则自动补滚，卡片过高则优先对齐顶部；`styles.css` 为抽屉滚动层补 `scroll-padding`；`index.html` 提升抽屉组件和样式缓存版本；`audit_user_annotation_contract.mjs` 增加 `expandedNoteVisibilityContract` 防回归。验证通过：相关 `node --check`、`audit_user_annotation_contract.mjs`、`frontend_content_smoke_check.mjs --skip-api`、`audit_json_package_boundary.py`、`check_github_data_boundary.py`、`dev_server_guard.py --status`、`git diff --check`。未修改用户批注数据库、正式 JSON、原始 Excel、SQLite、标准包、字典包或 LC 数据包；未启动系统 Chrome，真实点击效果待用户页面验收。
- 2026-06-29 `OI-145 Local API Host Token Boundary Fix`：已修复固定 5173 预览服务实际使用的 `src/sapd_wiki/api_server.py` 中 `/api/v1/*` Host/token 边界缺口。所有 API 读请求现在先校验 `Host` 必须为 loopback + 当前端口；用户写接口 `POST` / `PATCH` / `DELETE` 统一要求 `Content-Type: application/json`、有效 `X-SAPD-Session-Token`，并拒绝非 loopback 同端口的 `Origin` / `Referer`。`/api/v1/health` 返回进程内 session token，现有前端 `dataClient` 已自动带 token；若服务重启导致旧页面 token 过期，用户写接口首次 `403` 后会清理 health 缓存、重取 token 并重试一次。新增 `scripts/audit_local_api_security_boundary.mjs`，用 raw socket 覆盖异常 Host，并用临时批注创建 / 删除验证合法写入未被误伤；脚本会清理 `page:/oi-145-regression*` 临时记录。影响评估：固定入口 `http://127.0.0.1:5173/` 与 `http://localhost:5173/` 下的能力映射、信息化环境映射、LC-AP / LC-DT、知识库字典、标准 / 框架、指南、幻灯片、批注和收藏应继续可用；外部域名、局域网 IP、端口不一致、DNS rebinding Host、`file://` 跨源调用 5173 API、手工脚本不带 token 写用户数据会被拒绝。验证通过：`python3 -m py_compile src/sapd_wiki/api_server.py`、`node --check scripts/audit_local_api_security_boundary.mjs`、`python3 scripts/dev_server_guard.py --restart`、提升权限执行 `node scripts/audit_local_api_security_boundary.mjs`；异常 Host health / notes / write 均为 `403`，正常 loopback 读为 `200`，无 token 写为 `403`，带 token 临时批注创建 / 删除为 `200`。未修改原始 Excel、SQLite、正式 JSON、node-details、标准包、字典包或 LC 数据包。
- 2026-06-29 `Lifecycle Annotation Legacy Target Restore`：按用户反馈修复 LC-AP / LC-DT 旧批注定位不到、不高亮。根因是现有生命周期批注仍使用旧 `base:field_value:v2` 值级锚点，锚点内阶段 / 过程 ID 已不在当前 lifecycle 数据中，导致上下文匹配拦截文本兜底。已在 `app.js` 增加生命周期旧 ID 恢复逻辑：旧 ID 失效时用批注标题 / 短正文在当前 LC ViewModel 搜索文本中反查阶段 / 过程，并恢复 `selectedDevProcessId` 或 `selectedDataProcessId`；`viewModels.js` 将 `originalBusinessFields` 纳入生命周期搜索文本；`ApplicationSecurityLifecycle.js` 为 LC 阶段 / 过程导航按钮补稳定 `data-annotation-target-ref` 和 `data-annotation-prefer-target`；`audit_user_annotation_contract.mjs` 增加 LC 导航稳定锚点与搜索文本防回归。真实本地批注 API 抽样 6 条生命周期批注均可反查到当前阶段 / 过程：`窃取凭证 -> AP-04 集成构建`、`接收其他应用数据 -> DT-01 收集/采集`、其余 LC-AP 旧批注 -> `AP-01 需求分析`。验证通过：相关 `node --check`、`audit_user_annotation_contract.mjs`、`audit_search_state_isolation.mjs`、`frontend_content_smoke_check.mjs --skip-api`、`audit_json_package_boundary.py`、`check_github_data_boundary.py`、`dev_server_guard.py --status`。未修改 LC 数据包、原始 Excel、SQLite、用户批注数据库、标准包或字典包；未启动系统 Chrome，真实点击黄色高亮待用户页面验收。
- 2026-06-29 `Annotation Jump Stable Target Highlight Fix`：按用户截图修复信息化环境汇聚图批注“定位后目标节点不变黄”。根因是点击定位 active 属性已能打到 `button[data-annotation-prefer-target="true"]` 稳定对象锚点，但黄色定位态 CSS 只覆盖值锚点、chip 和表格单元格，未覆盖这类业务对象按钮。已在 `styles.css` 将 `[data-annotation-prefer-target="true"]` 纳入保存后常驻态和点击定位临时态样式；`app.js` 新增 `clearAnnotationActiveAnchorState()` 并让定位结束时统一清除 active 状态，避免多次 resolve 后高亮残留；`audit_user_annotation_contract.mjs` 增加对应防回归断言；`index.html` 更新缓存版本为 `annotation-jump-target-highlight-20260629-1`。验证通过：`node --check` 相关 JS、`node scripts/audit_user_annotation_contract.mjs`、运行时断言确认稳定对象锚点高亮样式 / active 清除 / 缓存 / 审计覆盖生效、`frontend_content_smoke_check.mjs --skip-api`、`audit_json_package_boundary.py`、`check_github_data_boundary.py`、`dev_server_guard.py --status`、`git diff --check`。未修改正式 JSON、原始 Excel、SQLite、用户批注数据库、node-details、标准包、字典包或 LC 数据包；未启动系统 Chrome，真实点击效果待用户页面验收。
- 2026-06-29 `Environment Backup Tab Removal`：按用户截图要求删除正式信息化环境页中的 `信息化环境-安全技术备份页`。已从 `AppShell.js` 和 `app.js` 的环境页 tab 列表移除 `mapping-backup`，`EnvironmentLocalRelationMap.js` 不再为 backup tab 选择 legacy 图分支，正式环境映射只保留 `环境底图` 与 `信息化环境-安全技术` 两个 tab；历史本地状态若不是 `mapping` 会回到 `topology`。同步提升 `AppShell.js`、`app.js` 和动态加载 `EnvironmentLocalRelationMap.js` 缓存版本。验证计划：提交前执行相关 JS 语法检查、前端内容 smoke、批注契约、数据边界、GitHub 数据边界、5173 服务状态和 `git diff --check`。未修改正式 JSON、SQLite、原始 Excel、用户批注数据库、node-details、标准包、字典包或 LC 数据包。
- 2026-06-29 `Annotation Anchor Runtime Index And Environment Stable Targets`：按用户纠偏，将“批注值定位慢 / 定位不到拖慢页面”从 `OI-149` JSON 加载治理中拆出，新增 `OI-150` 全局批注值定位运行时索引、`OI-151` 信息化环境汇聚图稳定业务锚点。`app.js` 新增 `annotationAnchorIndex` / `annotationHiddenAnchorIndex` / `annotationNoteIndex`，当前页面渲染后建立 `target_ref -> anchors`、旧 `field_value`、表格行、能力行、维护行和文本候选索引；`applyAnnotationAnchorMarkers()` 改为构建一次索引后按 `target_ref` 批量标记，`findAnnotationAnchorElement()` 改为索引优先，找不到再进入历史坐标和文本兜底；DOM 变更或批注标记调度会使索引失效。`EnvironmentScopeServiceMatrix.js` 为汇聚图安全技术服务、模块、措施和安全系统节点补 `data-annotation-target-ref` 稳定锚点，并加 `data-annotation-prefer-target="true"`，避免新批注优先落到易漂移的 `field_value`。同步更新 `index.html` 缓存版本和 `audit_user_annotation_contract.mjs` 防回归断言。验证通过：`node --check` 相关 JS、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`python3 scripts/dev_server_guard.py --status`。未启动系统 Chrome，真实点击耗时和定位回归待用户明确批准后再做；未修改正式 JSON、SQLite、原始 Excel 或用户批注数据库。
- 2026-06-29 `Environment Statistics Object Coverage Unit Label`：按用户要求将正式 `信息化环境-安全技术` 统计页中 `对象覆盖` 每行右侧裸数字改为带业务单位的显示，例如 `12` 改为 `12 模块/措施`；同步为右侧单位文本增加不换行样式，并更新 `app.js` / `index.html` 缓存版本为 `environment-statistics-object-coverage-unit-20260629-1`。本轮仅调整前端展示文案，不改统计口径、正式 JSON、原始 Excel、SQLite、node-details、标准包、字典包或 LC 数据包。验证通过：`node --check` 相关 JS、运行时断言确认行单位 / 缓存 / 样式生效、`frontend_content_smoke_check.mjs --skip-api`、`audit_json_package_boundary.py`、`check_github_data_boundary.py`、`dev_server_guard.py --status`、`git diff --check`。
- 2026-06-29 `Environment Statistics Neutral Background Fix`：按用户截图反馈删除正式 `信息化环境-安全技术` 统计页中无业务含义的蓝色底图效果。处理范围仅限环境统计页 / 子类统计页的 `environment-statistics-*` 样式：禁用 `environment-statistics-view::before/::after` 大色带，统计页外壳、汇总块和行容器改为中性玻璃底；对象映射页的自适应路由色带未修改。同步更新 `app.js` / `index.html` 缓存版本为 `environment-statistics-neutral-bg-20260629-1`。验证通过：`node --check` 相关 JS、运行时断言确认统计页无蓝色 radial / 侧向铺底且伪元素禁用、`frontend_content_smoke_check.mjs --skip-api`、`audit_json_package_boundary.py`、`check_github_data_boundary.py`、`dev_server_guard.py --status`、`git diff --check`。未修改正式 JSON、原始 Excel、SQLite、node-details、标准包、字典包或 LC 数据包。
- 2026-06-29 `Environment Workbench Target Relation Re-ETL Apply`：按用户要求从当前仓库磁盘源 `data/raw-samples/wiki sample.xlsx` 重新执行信息化环境映射 ETL，并更新正式运行包 `frontend/capability-browser/public/data/environment-workbench.json`。用户要求“再看一眼”后，已确认源表同步了截图修改：`F34/F35` 均为 `I-NT&T-PD.PP-03 网络隔离`，`G34='单向光闸'`、`G35='双向网闸'`，安全系统由 `H34:H39` 合并区承接为 `网络安全隔离与交换`。源表审计通过：`normalizedRowCount=871`、`objectServiceRelationCount=778`、`serviceModuleRelationCount=690`、`serviceMeasureRelationCount=181`、`moduleSystemRelationCount=217`、`pendingRelationCount=0`、`invalidRowCount=0`；目录一致性审计通过：`reviewRowCount=871`、`moduleServiceMismatchCount=0`、`systemModuleMismatchCount=0`、`securitySystemCoverageOrMismatchCount=0`、`moduleMeasureClassificationIssueCount=0`。候选生成通过：`targetSystemRelations=700`、`blockedDuplicateObjectContexts=0`、`issues=0`；downstream shadow 通过：`validationErrorCount=0`；replacement bundle 通过：`environmentCount=10`、`objectContextCount=67`、`relationCount=2739`、`matrixRows=1483`、`validationErrors=[]`。本轮修正 ETL 输出口径：措施目标有安全系统时只写目标自身 `systems`，不再重复写 `securitySystems`；replacement bundle 的对象索引只从最终 `environment_scope_tree` 抽取，避免 `shadow:*` 与 `reimport:*` 双套 ID 混入。正式运行包已先备份到 `data/exports/worker-verify/environment-runtime-backups/environment-workbench.before-source-synced-reetl-20260629.json`，随后用 replacement bundle 覆盖；`data_package_summary.py --package environment-workbench` 显示 `data_state=ready`、`information_environment=10`、`information_object=67`、`security_technology_module=57`、`security_technical_measure=28`。严格业务审计通过：`expectedTargetKeys=871`、`actualTargetKeys=871`、`missingTargetCount=0`、`missingSystemCount=0`、`unexpectedSystemCount=0`、`ownTargetSystemMissingCount=0`、`jsonWarningCount=0`。本地正式 JSON 抽样确认 `combo=0`，正式入口 JSON 中 `单向光闸`、`双向网闸` 是两个独立模块并分别关联 `网络安全隔离与交换`，有系统的措施只写 `systems`。验证通过：Python / JS 语法检查、`frontend_content_smoke_check.mjs --skip-api`、`audit_json_package_boundary.py`、`check_github_data_boundary.py`、`audit_dictionary_standard_baseline_integrity.py`、`dev_server_guard.py --restart`、`git diff --check`。HTTP 抽样因当前 Codex 权限 / 额度限制未执行；未修改 SQLite、node-details、标准包、字典包或 LC 数据包。
- 2026-06-29 `Environment Statistics Compact Scope Kind Fix`：按用户截图反馈继续收紧正式 `信息化环境-安全技术` 统计页。已删除层级阅读说明 rail，压缩环境 / 子类实体块和 `主要安全技术模块/措施` 胶囊尺寸；子类页统计从原先累加作用域数量改为 `作用域种类`，按 `scopeServiceRows` 中唯一 scope code / id / title 计数，避免把同类作用域多次计入。同步更新 `app.js` / `index.html` 缓存版本。验证通过：`node --check` 相关 JS、残留搜索确认无 layer rail / 旧作用域指标、运行时断言确认 scope kind helper 与紧凑样式存在、`frontend_content_smoke_check.mjs --skip-api`、`audit_json_package_boundary.py`、`check_github_data_boundary.py`、`dev_server_guard.py --status`、`git diff --check`。未修改正式 JSON、原始 Excel、SQLite、node-details、标准包、字典包或 LC 数据包。
- 2026-06-29 `Environment Statistics Full Segment And Interaction Fix`：按用户三张截图反馈修正正式 `信息化环境-安全技术` 主 tab 统计页。环境层级不再隐藏环境子类，所有环境子类全量展示，因此不再出现 `还有 N 个环境子类未展开`；图 2 的 `环境核心层 / 环境子类层 / 信息化对象层 / 能力层` 保持为纯层级阅读说明，不作为交互入口；图 3 的 `对象覆盖` 和 `主要安全技术模块/措施` 两个统计块移除点击命中链路，仅左侧实体按钮保留选择环境子类 / 对象能力，避免点统计块触发混乱跳转。验证通过：`node --check` 相关 JS、运行时断言确认 5 个环境子类全量输出且统计 pane / row 无 `data-environment-*` 命中属性、`frontend_content_smoke_check.mjs --skip-api`、`dev_server_guard.py --status`、`git diff --check`。未修改数据包、原始 Excel、SQLite 或正式 JSON。
- 2026-06-29 `Environment Statistics Object Coverage Full Display`：按用户截图反馈，正式 `信息化环境-安全技术` 主 tab 的环境统计页中，“对象覆盖”不再限制为前 4 个对象，改为同一环境子类下对象全量展示，因此不会再出现 `还有 N 个对象未展开`。模块 / 措施过多时仍保留 `+N 更多模块/措施` 收束。验证通过：`node --check frontend/capability-browser/components/EnvironmentLocalRelationMap.js`、运行时断言确认 6 个对象全量输出且无 `对象未展开`、`git diff --check`。未修改数据包、原始 Excel、SQLite 或正式 JSON。
- 2026-06-29 `Environment Statistics Level Code Migration`：按用户要求将 `environment-object-apple-shell-demo.html` 中环境层级 / 环境子类层级统计型设计代码级迁移到正式信息化环境安全技术映射页。正式主 tab `信息化环境-安全技术` 在选择环境时显示环境统计视图：环境、子类、对象、主要安全技术模块 / 措施覆盖，并对未展示的环境子类、对象和能力项显示 `还有 N 个...` / `+N 更多模块/措施`；选择环境子类时显示对象全量滚动列表，单个对象内模块 / 措施默认前 6 个，超出以 `+N` 收束。对象层级仍走现有 `EnvironmentScopeServiceMatrix` 映射图；`信息化环境-安全技术备份页` 保持旧层级 / legacy 分支。修改范围为 `EnvironmentLocalRelationMap.js`、`styles.css`、`app.js`、`index.html` 和本文件；未修改正式 JSON、原始 Excel、SQLite、node-details、标准包、字典包或 LC 数据包。验证通过：`node --check` 三个相关 JS、`frontend_content_smoke_check.mjs --skip-api`、`audit_json_package_boundary.py`、`check_github_data_boundary.py`、`dev_server_guard.py --status`、`git diff --check`，并用 Node 运行时抽查确认环境页隐藏提示、子类页 `+N`、备份页 legacy 分支均生效；未启动系统 Chrome。
- 2026-06-29 `Environment Statistical Demo Overflow Indicators`：按用户要求在 `environment-object-apple-shell-demo.html` 的统计型 demo 中补充“未完全展示”的业务表达。环境页新增对象覆盖续接提示、模块 / 措施 `+N 更多模块/措施` chip，以及 `还有 2 个环境子类未展开` 的收束提示；子类页补齐 6 个对象行，并增加说明：对象行全量滚动展示，单个对象内模块 / 措施默认展示前 6 个，超出以 `+N` 收束。未迁移正式页，未修改正式 JSON、原始 Excel、SQLite 或数据包。验证通过内联脚本语法检查、文案搜索、`frontend_content_smoke_check.mjs --skip-api`、`dev_server_guard.py --status` 和 `git diff --check`；未启动系统 Chrome。
- 2026-06-29 `Environment Target System Relation Business Audit`：按用户要求重新核对 `信息化环境-安全技术` 页业务关系和正式输出 JSON。真实问题不是原始表错误，而是页面 JSON / ViewModel 旧口径混用了服务级聚合字段：模块 / 措施目标节点缺少自身安全系统时，会从 `service.securitySystems` 继承，导致 H 列为 `N/A` 的措施假阳性，同时源表 H 列有值的部分目标又没有落到目标节点自身。已新增 `scripts/audit_environment_page_business_logic.mjs`，用 normalized rows / review rows 对页面消费的目标级关系做严格审计：`871` 个目标键全部存在，其中 `690` 个模块目标、`181` 个措施目标；`700` 个目标级安全系统关系全部落到目标节点自身 `systems`，`service.securitySystems` 只保留聚合信息，不再作为模块 / 措施继承来源。已更新 `scripts/apply_environment_measure_system_relations.mjs` 写入模块和措施目标级 `systems`，清理措施重复 `securitySystems` 字段，并处理 `单向光闸 / 双向网闸` 组合目标拆分；同步修复候选生成、downstream shadow 和 replacement bundle 链路，生成链路输出 `targetSystemRelations=700`。验证通过：`audit_environment_page_business_logic.mjs` 为 `pass`，`missingTargetCount=0`、`missingSystemCount=0`、`unexpectedSystemCount=0`、`ownTargetSystemMissingCount=0`、`jsonWarningCount=0`；目标补丁审计为 `missingTargetSystemKeys=0`、`unexpectedTargetSystemKeys=0`、`unmatchedTargetKeys=0`；候选生成 / downstream 验证 / 前端 smoke / 字典标准边界 / GitHub 数据边界 / `git diff --check` / 5173 服务重启均通过。授权本地 HTTP 抽样确认 `API安全防护`、`API网关`、`单向光闸`、`双向网闸` 均从目标节点自身 `systems` 返回正确系统。未修改原始 Excel、SQLite、node-details、标准包、字典包或 LC 数据包。
- 2026-06-29 `Environment Statistical Demo Apple Shell Polish`：按用户认可方向继续优化 `environment-object-apple-shell-demo.html` 中的 `环境统计页` / `子类统计页`，增强 Apple shell 视觉：统计画布增加半透明高光、路由感背景带、玻璃面板层次和更接近对象页的大颗粒模块 / 措施 chip。同步修正文案，将 `模块 / 措施分布` 与 `重点能力` 统一为 `主要安全技术模块/措施`。本轮仍只修改 demo，不迁移正式页，不修改正式 JSON、原始 Excel、SQLite 或数据包。验证通过内联脚本语法检查、`frontend_content_smoke_check.mjs --skip-api`、`dev_server_guard.py --status` 和文案搜索；未启动系统 Chrome。
- 2026-06-29 `Environment And Subcategory Statistical Demo Views`：按用户要求在 `environment-object-apple-shell-demo.html` 中新增两个安全能力映射统计型设计 tab：`环境统计页` 和 `子类统计页`。设计方向区别于信息化对象页的服务关系流：环境页表达环境、环境子类、对象与模块 / 措施的整体覆盖；子类页表达当前环境子类下各信息化对象的模块 / 措施分布。新增内容仅在 demo 文件中实现，未迁移正式页面，未修改任何正式 JSON、原始 Excel、SQLite 或环境数据包。验证已通过内联脚本语法检查、`frontend_content_smoke_check.mjs --skip-api`、`audit_json_package_boundary.py`、`check_github_data_boundary.py`、`dev_server_guard.py --status` 和 `git diff --check`；未启动系统 Chrome。
- 2026-06-29 `Global Annotation And Environment Data Audit`：按用户最新反馈排查全局批注、路由切换和数据加载。根因之一是新环境映射组件未接入全局值级批注锚点，`audit_user_annotation_contract` 动态样例中 `environment=0`；已在 `EnvironmentScopeServiceMatrix.js` 为环境上下文、作用域、服务、模块 / 措施、安全系统节点接入 `display.annotationValueAttrs`，在 `EnvironmentBasemapViewer.js` 为底图详情 chip 输出值级批注属性，并在 `app.js` 为信息化环境页设置当前选中的环境 / 子类 / 对象批注目标。另修复批注抽屉 closing 状态面板仍可吞点击的问题：`styles.css` 中 closing 面板禁用 `pointer-events`。数据关联复核通过：`audit_scope_service_module_mapping.py` 为 `pass`，`pendingRelationCount=0`、`invalidRowCount=0`、`currentObjectScopeRelationCount=163`；`audit_environment_module_catalog_consistency.py` 为 `pass`，`moduleServiceMismatchCount=0`、`systemModuleMismatchCount=0`、`securitySystemCoverageOrMismatchCount=0`。追加 ViewModel 断言确认 67 个对象上下文、2364 条展示行、2499 个关系节点中，措施节点 `469` 个且 `measureNodesWithSystems=0`，`API网关层` 的 `API安全防护 -> 应用安全防护` 仍保留。JSON 治理已登记 `OI-149`，本轮只读审计，未修改任何正式 JSON。执行完成后已重启 5173 项目预览服务，首页和 workspace projection 均返回 OK。
- 2026-06-29 `Environment Object Measure System Boundary Fix`：按用户截图核对 `作用域-安全技术服务-安全技术模块映射` 原始表，确认 `应用系统自身身份管理模块 / 认证模块 / 授权管理模块 / 凭证管理模块 / 日志记录模块` 等措施行 H 列为 `N/A`，不应在汇聚图连到安全系统。该阶段先定位到服务级系统继承会造成措施假阳性，并临时防止措施继承服务级 `securitySystems`；阶段性“仍缺 8 条 / 需后续重建”的结论已被后续 `Environment Target System Relation Business Audit` 覆盖，最终改为完整目标级关系：模块和措施都只读取目标自身 `systems`，`871` 个目标键和 `700` 个目标系统关系已审计通过。未修改原始 Excel、SQLite、node-details、标准包、字典包或 LC 数据包。
- 2026-06-29 `Environment Object Funnel Adaptive Route Bands`：按用户截图反馈，将汇聚图中两条无独立业务含义的视觉路由色带改为随当前节点映射数据自适应。`EnvironmentScopeServiceMatrix.js` 根据当前服务数、模块 / 措施数和安全系统数输出 CSS 变量；`styles.css` 改为用变量控制色带高度、宽度和透明度，数据少时自动收缩，缺少系统承接时右侧色带淡出。验证通过：`node --check frontend/capability-browser/components/EnvironmentScopeServiceMatrix.js`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`。未修改数据包或原始数据。
- 2026-06-29 `Environment Object Funnel Mini-map Removal`：按用户要求删除新汇聚图右下角无业务含义的 `关系流` mini-map 装饰块；已从 `EnvironmentScopeServiceMatrix.js` 渲染中移除 `environment-object-funnel-mini-map`，并删除对应 CSS。同步提升 `styles.css` 和 `EnvironmentScopeServiceMatrix.js` 缓存版本。验证通过：`node --check frontend/capability-browser/components/EnvironmentScopeServiceMatrix.js`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`。
- 2026-06-29 `Environment Object Funnel Security System Projection Fix`：修复用户抽查发现的汇聚图安全系统列为空问题。该阶段曾用服务级 `securitySystems` 做前端兜底；后续 `OI-147` 已收紧为目标级关系修复，`viewModels.js` 不再让模块 / 措施继承服务级系统，`API网关层` 的 `API安全防护 -> 应用安全防护` 等关系来自目标节点自身 `systems`。已登记 `OI-146`，最终验证见本轮 `Environment Target System Relation Business Audit`。
- 2026-06-29 `Environment Object Funnel Mapping Code Migration`：已新增正式环境页 tab `信息化环境-安全技术备份页`，该 tab 走旧 `legacy` 中轴映射图分支；正式 `信息化环境-安全技术` tab 改走新的 `funnel` 汇聚映射分支。迁移仅作用于映射图板，继续复用原对象摘要区 `renderHud`，未替换截图中的对象摘要信息。已更新 `index.html` 和 lazy load 缓存版本。验证通过：`node --check` 四个前端 JS 文件、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`。未做系统 Chrome / 真实浏览器回归。
- 2026-06-24 至 2026-06-29 环境对象图与 demo 长记录已归档到 `docs/05-archive/progress-history/2026-06.md`；当前只保留关键结论：正式对象图仍以中轴结构为回退基线，demo 迁移必须保留对象摘要区边界，关系语义不得新增 `安全技术模块 -> 安全技术措施`。

## 当前问题索引

- `OI-159`：能力页 L0 / L1 双 tab 总览型阅读模式已落地，状态 `工作区吸附控制轨正式迁移 / 待页面验收`；正式能力页已将 tab / 搜索迁入工作区顶部控制轨，标题区只保留对象身份；L0 / L1 仍只保留 `关系图谱` 和 `总览摘要` 两个 tab，不默认展示大批量技术 / 管理 / 标准映射明细。
- `OI-155`：全局搜索产品形态与搜索结果页重设计，状态 `设计完成 / 待用户确认`；设计入口为 `docs/06-implementation/global-search-redesign-2026-06-30.md`。
- `OI-156`：全局批注定位 / 高亮性能退化导致页面假死，状态 `已修复 / 45 条保存批注真实 Chrome 全量通过 / 待视觉与数据治理设计`；已补当前定位态持久化、能力 projection 懒恢复、行级旧锚点兼容、标准值锚点归一、抽屉当前卡片可见性和失败熔断。
- `OI-154`：页面内搜索功能回归与环境页搜索不可用，状态 `待设计落地 / 待修复`；建议在 `OI-155` 产品形态确认后优先治理环境页搜索。
- `OI-153`：批注抽屉底部卡片展开后当前批注显示不完整，状态 `已修复 / 待页面验收`；已在抽屉展开事件后自动滚动校正，使靠底部批注展开后正文和操作区进入可视区域。
- `OI-152`：LC-AP / LC-DT 旧批注阶段 ID 失效导致定位不到，状态 `已修复 / 待页面验收`；已用批注标题 / 短正文反查当前生命周期阶段 / 过程，并给 LC 导航补稳定批注锚点，真实点击黄色高亮待用户页面验收。
- `OI-151`：信息化环境汇聚图节点缺少稳定业务锚点导致定位不到，状态 `已修复 / 待页面验收`；已给服务、模块、措施和安全系统节点补稳定 `data-annotation-target-ref`，真实点击回归待用户批准。
- `OI-150`：全局批注值定位缺少运行时索引导致定位慢，状态 `已修复 / 待真实浏览器验收`；已新增运行时 anchor / note 索引并让定位索引优先，真实点击耗时回归待用户批准。
- `OI-146`：信息化环境安全技术汇聚图安全系统列未带出，状态 `已修复 / 待页面验收`；用户抽查 `API网关层` 时安全系统列显示 0，已由 `OI-147` 收敛为目标节点自身 `systems` 关系，不再依赖服务级继承。
- `OI-149`：前端 JSON 加载体量与首屏性能治理，状态 `P4 正式 split apply 已完成 / 待页面人工验收与观察`；P0 health 止血、P1 `/api/v1/search-index`、P2 能力页 projection 收口、P3 后端包缓存、P4 正式 split manifest / 分片落地和 post-apply readiness 均已通过；下一步不要继续扩大 JSON 拆包，先人工抽查页面加载和批注熔断，再进入 `OI-156` 批注终态修复或 `OI-154/OI-155` 搜索治理。
- `OI-148`：全局批注新页面接入与路由切换遮挡，状态 `已修复 / 待页面验收`；本轮已补环境页值级批注锚点、当前环境对象批注目标和抽屉 closing 状态 pointer-events。
- `OI-147`：信息化环境安全技术汇聚图措施误继承安全系统，状态 `已修复 / 待页面验收`；前端已防止模块 / 措施继承服务级系统，正式运行包已补齐 `871` 个目标键下的 `700` 个目标级安全系统关系。
- `OI-139`：`作用域-安全技术服务-安全技术模块映射` 重导入与环境映射事实源修正，状态 `已关闭`；源表 normalized、目录一致性、正式 JSON 目标级关系和正式 split projection 契约均已通过，已按用户确认关闭并归档到 open issues history。
- `OI-145`：本地 API Host/token 边界未统一拦截，状态 `已修复 / 待页面验收`；`api_server.py` 已补 Host、Content-Type、Origin / Referer 和 `X-SAPD-Session-Token` 写入边界，专项 raw socket 回归通过。
- `OI-142`：`安全技术服务清单` 页面默认分组折叠 / 真实可见性争议，状态 `真实浏览器取证通过 / 待用户页面验收`。
- `OI-141`：`LC-DT 数据生命周期` 与 `LC-DT 安全技术服务、模块、策略映射表` 源表更新候选，状态 `正式投影已替换 / 待页面验收`。
- `OI-137`：信息化环境底图，状态 `已修复 / 待人工验收`；当前数据事实源由 `OI-139` 继续校正。
- `OI-038`：Gartner 与安全职能候选映射人工校对，状态 `已关闭`；28 条 Gartner 岗位已收敛为 46 条人工确认安全职能引用并归档。
- `OI-128`：USER-WRITE-UI-1：批注 / 工作台用户写入入口，状态 `部分完成`。
- `OI-135`：用户库治理与兼容表迁移清理，状态 `正式迁移脚本完成 / 真实库 apply 待显式确认`。
- `OI-138`：关注点关系图谱标签 / 连线避让，状态 `部分修复 / 剩余管理与标准分支问题`。

## 历史索引

| 归档文件 | 内容 |
|---|---|
| `docs/05-archive/progress-history/2026-06.md` | 2026-06 完整执行记录、Open Issues 治理、前端治理、数据口径确认和本轮 progress 瘦身摘要 |
| `docs/05-archive/progress-history/2026-05.md` | 2026-05 完整执行记录、根目录 `progress.md` 瘦身快照和结构治理记录 |
| `docs/05-archive/findings-history/2026-05.md` | 2026-05 历史发现 |
| `docs/05-archive/open-issues-history/2026-06.md` | 已关闭 Open Issues 长记录 |
| `docs/05-archive/context-slimming-2026-05-15/` | 2026-05-15 上下文瘦身快照 |

- 2026-06-30 `OI-157 Packaged User Notes Export`：新增批注专项导出接口 `/api/v1/user/notes/export` 和 `?download=1` 附件模式，打包 Runtime 生成 `diagnostics/export-user-notes.command/.bat`，并修复 PyInstaller 旧 dist 非空导致后端缓存复用的问题；重新生成 macOS DMG，最终 `SHA-256=0f0e0a64f1d6cd603e640428f8a1425e8431aafa02d21b7b5f1efa18e59d4209`，验证通过 `py_compile`、临时 bundle 导出、5173 API 只读导出、`codesign --verify --deep --strict`、`hdiutil verify`、`check_bundle_runtime.py`、`git diff --check`。
- 2026-07-01 `Workbench Issue List Header Polish`：按用户截图微调正式工作台 Issue 页。`Issue 总览` 已统一改名为 `Issue 清单`，导航、Dashboard 入口、工作台入口和页面标题同步更新；原独立 6 列大统计卡改为紧跟 `Issue 清单` 标题后的紧凑状态筛选胶囊；三个导出按钮移动到页面头部右侧操作区，并同步提升 `styles.css`、`AppShell.js` 和 `app.js` 缓存版本。本轮只修改前端排版和文案，不改真实 Issue 数据链路、数据包、SQLite、原始 Excel、环境映射页或能力映射页。验证通过：`node --check frontend/capability-browser/app.js`、`node --check frontend/capability-browser/components/AppShell.js`、`node scripts/frontend_smoke_check.mjs --page workbench-annotations --route /workbench/annotations --url http://127.0.0.1:5173`、`node scripts/frontend_content_smoke_check.mjs --skip-api`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`；未启动系统 Chrome。
- 2026-07-01 `Workbench Issue List Acceptance Rework`：按用户验收反馈继续收口正式工作台 Issue 页。删除内容区内重复的 `Issue 清单` header，状态统计胶囊上移到 AppShell 大标题 `Issue 清单` 后方；导出动作上移到页面头部右侧，只保留 `导出全部` 与 `导出所选`，删除 `Issue 一键导出` 顶栏入口和 `导出筛选结果` 占位。`导出全部` 继续走现有 `/api/v1/user/notes/export` 真实接口，`导出所选` 改为根据当前勾选的真实 Issue 在前端生成 Markdown 文件；勾选数量会同步到顶部按钮。右侧 `选中 Issue` inspector 改为固定工作区高度内滚动，长内容不再被底部截断，操作按钮保持在 inspector 底部可达。本轮仍只改前端 UI 与导出交互，不改真实 Issue 数据结构、数据包、SQLite、原始 Excel、环境映射页或能力映射页。验证通过：相关 JS `node --check`、残留文案搜索、`frontend_smoke_check.mjs --page workbench-annotations`、`frontend_content_smoke_check.mjs --skip-api`、`audit_json_package_boundary.py`、`check_github_data_boundary.py`、`dev_server_guard.py --status`、`git diff --check`；未启动系统 Chrome。
- 2026-07-01 `Capability View Tabs Page-turn Title Placement`：按用户截图继续调整安全能力映射页内视角 tab。`能力关系图谱 / 技术视角 / 管理视角 / 标准 / 框架映射` 已从外层独立控件移入内容区标题位，改为贴在内容页上沿的翻页式 tab；图谱 / 矩阵内部重复大标题隐藏，只保留必要副标题、图例和统计，从而释放下方关系工作区高度。本轮只修改 `CapabilityLocalRelationMap.js`、`app.js`、`styles.css` 与 `index.html` 缓存版本，不改能力数据、ViewModel、JSON、SQLite、原始 Excel、环境映射页或工作台 Issue 页。验证通过：相关 JS `node --check`、`frontend_smoke_check.mjs --page capability-mapping`、`frontend_content_smoke_check.mjs --skip-api`、`audit_json_package_boundary.py`、`check_github_data_boundary.py`、`dev_server_guard.py --status`、`git diff --check`；未启动系统 Chrome。
- 2026-07-01 `OI-161 Workbench Issue List Click Fix`：按用户反馈二次修复左侧目录 `Issue 清单` 进不去。根因修正为：前一轮把 `工作台` 父级 `<summary>` 也做成 `data-app-route`，会和目录展开行为冲突；Workbench 默认路由还可能在状态恢复 / 重渲染路径中把 `/workbench/annotations` 折回 `/workbench`。本轮将 `工作台` 父级恢复为只展开 / 收起，`Issue 清单` 子项保留独立 `/workbench/annotations` 路由，并在 `routeForCurrentState("workbench")` 中保持 Workbench 子路由；Issue 队列行点击、勾选选中和键盘进入修复继续保留。更新 `scripts/audit_workbench_issue_interaction_contract.mjs` 防回归，并提升 `AppShell.js` / `app.js` 缓存版本到 `issue-list-click-20260701-3`；已重启 5173。本轮不改真实 Issue 数据、用户库、导出接口、正式 JSON、SQLite、原始 Excel、环境映射页或能力映射页。验证通过：相关 JS `node --check`、`audit_workbench_issue_interaction_contract.mjs`、`frontend_smoke_check.mjs --page workbench-annotations`、`frontend_content_smoke_check.mjs --skip-api`、`python3 scripts/dev_server_guard.py --restart`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`；未启动系统 Chrome。
- 2026-07-01 `OI-162 Capability Management Security Work Re-ETL`：按用户反馈重新核对 `安全能力-安全工作` 原始表合并单元格。当前源表已修正为 `G35:G36=网络访问控制策略持续管理`，覆盖 `T-PD.AC-01` 与 `T-PD.AC-02`；`G37:G41=入侵检测规则持续管理`，覆盖 `T-PD.TP-*` 行。已重新执行 second-batch 导入并批准 `import_job_id=5e41b67c-622e-4012-910f-a6d93e761db1`，批准结果为 `items_created=1`、`items_updated=524`、`items_deprecated=1`、`relations_created=1`、`relations_deleted=1`；随后重导 `capability-tree`、`maintenance-knowledge`、`capability-workbench`，重建并正式 apply OI-149 split 候选，正式 apply 报告为 `data/exports/worker-verify/oi-149-p4-json-split-candidate/formal-apply-reports/oi149-p4-apply-20260701T135545Z.json`，结果 `apply_pass`。新增 `scripts/audit_capability_management_source_alignment.py`，从源表、维护字典、能力全量包和 split projection 四层防回归；当前审计 `result=pass`、`sourceIssueCount=0`、`runtimeIssueCount=0`。能力页标题区同步按用户截图收口为新基线：删除右上角来源不稳定的派生统计卡，只保留当前能力标题、视图 tab 和搜索框；能力契约审计新增 `preview-focus-stats` 不得渲染的断言。验证通过：`python3 -m py_compile scripts/audit_capability_management_source_alignment.py`、`python3 scripts/audit_capability_management_source_alignment.py --json`、相关 JS `node --check`、`node scripts/audit_oi149_split_runtime_contract.mjs --source formal`、`node scripts/audit_oi149_split_candidate.mjs`、`node scripts/audit_oi149_readiness.mjs --mode postapply --skip-runtime`、`node scripts/audit_capability_viewmodel_contract.mjs --url http://127.0.0.1:5173`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/frontend_smoke_check.mjs --page capability-mapping --route /capability-mapping --url http://127.0.0.1:5173`、`node scripts/frontend_content_smoke_check.mjs --skip-api --url http://127.0.0.1:5173`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/audit_dictionary_standard_baseline_integrity.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/data_package_summary.py --package all`、`python3 scripts/dev_server_guard.py --status`。`audit_capability_viewmodel_contract` 因沙箱内本地 fetch 失败，已按规则在可访问 5173 的执行上下文重跑并通过；未启动系统 Chrome，页面视觉仍待用户人工验收。
- 2026-07-01 `Capability Header Layout And Security Work 1:N Display`：按用户截图继续修正两个正式页面展示问题。能力页标题区不再采用搜索框右侧孤岛排布，改为第一行只放当前能力标题，第二行以 `tab + 搜索框` 组成连续工具栏；`audit_capability_viewmodel_contract.mjs` 已更新对应 CSS 契约。`安全管理工作/流程清单 -> 安全工作清单` 改为逻辑安全工作展示：`viewModels.js` 按安全工作名称聚合，底层包仍保留 `92` 条安全工作-关注点关系行，页面显示 `80` 个安全工作主行，并在同一行内用 1:N 标签展示多个关联关注点；`网络访问控制策略持续管理` 已聚合 `T-PD.AC-01` 与 `T-PD.AC-02`。本轮不改 ETL、不改原始 Excel、不改 SQLite、不改正式 JSON 数据包，只修改前端展示、缓存版本和审计脚本。验证通过：`node --check frontend/capability-browser/viewModels.js`、`node --check frontend/capability-browser/components/StandardRoleReferenceTable.js`、`node --check scripts/frontend_content_smoke_check.mjs`、`node --check scripts/audit_capability_viewmodel_contract.mjs`、`node scripts/frontend_content_smoke_check.mjs --skip-api --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page capability-mapping --route /capability-mapping --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page management-workflows --route /knowledge/management-workflows --url http://127.0.0.1:5173`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/audit_capability_viewmodel_contract.mjs --url http://127.0.0.1:5173`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/dev_server_guard.py --status`、`git diff --check`。`audit_capability_viewmodel_contract` 因沙箱内本地 fetch 失败，已按规则在可访问 5173 的执行上下文重跑并通过；未启动系统 Chrome。
- 2026-07-01 `Capability Security Work Unique Code And Header Spacing Fix`：按用户最新验收反馈继续收口安全工作编码与能力页标题区。安全工作显示编码改为逻辑对象唯一编码，规则为 `SW-{所属能力编码}-{两位序号}`，不再把关注点编码和关系行序号拼进安全工作编码；`viewModels.js` 新增安全工作编码 lookup，并同步应用到 `安全工作清单` 和能力页管理视角 `localRelationMap`，`StandardRoleReferenceTable.js` 只展示单枚安全工作编码。当前抽样确认 `网络访问控制策略持续管理=SW-T-PD.AC-01`，关联 `T-PD.AC-01 / T-PD.AC-02`；`入侵检测规则持续管理=SW-T-PD.TP-01`，关联 `T-PD.TP-01..05`，80 个逻辑安全工作无重复编码、无多编码行。能力页标题区调整为标题与搜索同层、视图 tab 独占下一层，并增加底部留白，避免搜索框底边压住内容分隔线；`audit_capability_viewmodel_contract.mjs` 和 `frontend_content_smoke_check.mjs` 已补对应防回归断言。本轮不改 ETL、不改原始 Excel、不改 SQLite、不改正式 JSON 数据包。验证通过：相关 JS / 审计脚本 `node --check`、`node scripts/frontend_content_smoke_check.mjs --skip-api --url http://127.0.0.1:5173`、安全工作编码抽样、`node scripts/frontend_smoke_check.mjs --page capability-mapping --route /capability-mapping --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page management-workflows --route /knowledge/management-workflows --url http://127.0.0.1:5173`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/audit_capability_viewmodel_contract.mjs --url http://127.0.0.1:5173`、`python3 scripts/audit_json_package_boundary.py`、`python3 scripts/check_github_data_boundary.py`、`python3 scripts/dev_server_guard.py --status`；`audit_capability_viewmodel_contract` 在沙箱内本地 fetch 失败后已按规则用可访问 5173 的执行上下文重跑并通过，未启动系统 Chrome。
- 2026-07-01 `Capability Security Work Source Order And Title Tabs Polish`：按用户反馈修复上一轮唯一编码带来的安全工作清单排序回退，并继续调整能力页 tab 位置。`viewModels.js` 新增能力树关注点顺序索引，安全工作逻辑行按其关联关注点在能力树中的最小顺序排序，保留唯一显示编码但不再用维护包内顺序或编码顺序驱动列表；当前抽样前 10 行从 `T-AS.AD-01/02/03` 开始，`网络访问控制策略持续管理` 位于第 28 行并聚合 `T-PD.AC-01 / T-PD.AC-02`，`入侵检测规则持续管理` 位于第 35 行并聚合 `T-PD.TP-01..05`，80 个逻辑安全工作无重复编码、无多编码行。能力页视角 tab 改为标题行右侧的轻量 Apple shell segmented control，搜索框独立为下一行工具栏，避免大胶囊贴着内容分隔线；`audit_capability_viewmodel_contract.mjs` 和 `frontend_content_smoke_check.mjs` 已补排序与 tab 布局防回归断言。本轮不改 ETL、不改原始 Excel、不改 SQLite、不改正式 JSON 数据包。映射更新专项审计通过：`python3 scripts/audit_capability_management_source_alignment.py --json` 返回 `sourceIssueCount=0`、`runtimeIssueCount=0`，确认原始表 `G35:G36=网络访问控制策略持续管理`、`G37:G41=入侵检测规则持续管理` 且运行包一致。验证通过：相关 JS / 审计脚本 `node --check`、`node scripts/frontend_content_smoke_check.mjs --skip-api --url http://127.0.0.1:5173`、安全工作排序和编码抽样、`node scripts/frontend_smoke_check.mjs --page capability-mapping --route /capability-mapping --url http://127.0.0.1:5173`、`node scripts/frontend_smoke_check.mjs --page management-workflows --route /knowledge/management-workflows --url http://127.0.0.1:5173`、`node scripts/audit_frontend_lazy_load_contract.mjs`、`node scripts/audit_user_annotation_contract.mjs`、`node scripts/audit_capability_viewmodel_contract.mjs --url http://127.0.0.1:5173`；`audit_capability_viewmodel_contract` 在沙箱内本地 fetch 失败后已按规则用可访问 5173 的执行上下文重跑并通过，未启动系统 Chrome。

## 维护规则

- 本文件只保留最近状态、最近 5-10 条重要执行和恢复入口；超过 120 行时继续归档到 `docs/05-archive/progress-history/`。
- 详细过程、长命令输出和阶段性历史不写入根目录 `progress.md`。
