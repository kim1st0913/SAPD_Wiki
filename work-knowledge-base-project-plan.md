# 本地化结构化工作知识库系统项目建议书

## 1. 项目定位

本项目旨在建设一个本地化运行的结构化工作知识库系统，用于统一管理 Excel 表格、Draw.io 架构视图、PPT 幻灯片、DOCX 文档、Markdown 文件等多种形式的工作知识资产。

系统以 GitHub 作为工程维护载体，通过文件解析、字段映射、ETL 处理和人工编辑等方式，将非结构化与半结构化资料转化为统一的知识对象，并以数据库形式进行持久化存储。系统支持知识分类、标签管理、全文检索、关联查询、来源追踪、版本管理、批量更新、多维导出和全量备份，并通过本地 HTML 前端提供高性能展示。

最终交付形态为可在本地运行和打包分发的桌面程序，并为后续知识模型扩展、数据更新机制、专题知识产品生成和 AI 检索增强预留能力。

---

## 2. 总体判断

该需求不应被简单理解为“文件管理系统”或“资料库”，而应定位为：

> **结构化知识资产管理系统 + 文件解析/ETL 系统 + 本地知识门户。**

其核心价值不在于“能保存文件”，而在于：

1. 将多种格式的文件内容转化为可查询、可关联、可导出的知识对象；
2. 保留原始文件与结构化数据之间的来源关系；
3. 支持长期的数据更新、版本管理和知识演进；
4. 通过本地网页或桌面应用提供统一展示和查询入口；
5. 支持后续 AI/RAG、知识图谱、专题知识包等能力扩展。

---

## 3. 需求中已经明确的关键能力

| 能力方向 | 需求说明 | 评价 |
|---|---|---|
| 工程维护 | 工程存储在 GitHub 上 | 正确，便于版本管理、协作和持续维护 |
| 多格式管理 | 支持 Excel、Draw.io、PPT、DOCX 等多种文件 | 符合真实工作知识资产场景 |
| 数据库存储 | 结构化后以数据库形式保存 | 必要，避免长期停留在散乱文件层面 |
| 更新机制 | 支持人工修改、批量上传、第三方 ETL 映射更新 | 关键能力，决定系统可持续性 |
| 页面展示 | 通过 HTML 网页在本地展示 | 适合做跨平台知识门户 |
| 本地交付 | 本地执行并打包相关组件 | 适合个人或团队内部知识库 |
| 查询导出 | 支持关联查询、多维导出和全量导出 | 防止系统形成新的数据孤岛 |
| 页面扩展 | 功能页面方便扩展 | 必须预留配置化和模块化能力 |

---

## 4. 当前需求需要补充的问题

### 4.1 明确知识颗粒度

不同文件类型内部结构不同，必须先定义知识拆解颗粒度。

| 文件类型 | 可能的知识颗粒度 |
|---|---|
| Excel | 工作簿、工作表、表格、行记录、字段、指标、矩阵 |
| Draw.io | 视图、节点、连线、架构元素、关系 |
| PPT | 演示文稿、单页幻灯片、标题、图示、要点、备注 |
| DOCX | 文档、章节、段落、表格、图片、引用 |
| PDF | 页面、章节、图表、注释、引用 |
| Markdown | 标题层级、段落、代码块、表格、引用块 |

建议采用三层颗粒度设计：

```text
文件层：原始文件，如 xxx.docx、xxx.xlsx、xxx.drawio
内容层：文件内部结构，如章节、表格、幻灯片、图、工作表
知识对象层：被抽取出来的业务对象，如能力、流程、系统、组件、风险、控制、指标、案例、术语
```

---

### 4.2 建立统一知识模型

不能按文件类型各自建一套孤立模型，否则后续关联查询、统一展示和导出会非常困难。

建议抽象出以下核心对象：

```text
KnowledgeItem       知识条目
KnowledgeSource     知识来源
KnowledgeType       知识类型
KnowledgeRelation   知识关系
Tag                 标签
Category            分类
Version             版本
Attachment          原始附件
View                展示视图
ExportProfile       导出配置
```

可结合实际工作场景，将知识对象划分为：

| 类型 | 示例 |
|---|---|
| 方法论 | TOGAF、APQC、NIST CSF、ATT&CK |
| 能力项 | 威胁检测能力、访问控制能力、资产管理能力 |
| 流程项 | 风险评估流程、漏洞处置流程、安全运营流程 |
| 架构元素 | 系统、应用、组件、接口、数据对象 |
| 控制措施 | 身份认证、日志审计、网络隔离、终端防护 |
| 指标 | 成熟度评分项、覆盖率、响应时间 |
| 案例 | 行业案例、项目案例、事件案例 |
| 模板 | PPT 模板、PRD 模板、评估表模板 |
| 术语 | 专业术语、缩写、定义 |

---

### 4.3 明确原始文件与结构化数据的关系

系统应同时保留原始文件与结构化数据。

建议采用：

```text
原始文件仓库 + 结构化数据库 + 解析索引 + 版本记录
```

关键原则：

1. 原始文件必须保留；
2. 数据库保存可查询、可展示、可关联的结构化数据；
3. 每条知识记录必须能追溯到来源文件；
4. 初期不建议做结构化数据自动回写原始文件；
5. 文件重新上传时，通过 hash、来源位置和导入任务识别变化。

建议每条知识记录保留：

```text
source_file_id
source_path
source_hash
source_location
import_job_id
version
```

---

### 4.4 增加更新冲突机制

当人工编辑、批量上传、第三方 ETL 同时影响同一条知识时，必须有冲突处理机制。

建议设计“变更审查中心”，用于处理：

- 新增知识；
- 删除知识；
- 字段修改；
- 关系变化；
- 重复数据；
- 字段冲突；
- 是否确认入库。

建议策略：

| 更新场景 | 建议策略 |
|---|---|
| 批量导入新文件 | 新增 source version，不直接覆盖人工内容 |
| 同源文件重新上传 | 根据文件 hash 判断是否变化 |
| 结构化字段变化 | 生成 diff，允许确认后合并 |
| 人工编辑 | 优先级高于自动 ETL |
| 第三方 ETL | 进入待审核区 |
| 字段冲突 | 保留冲突记录，不静默覆盖 |

---

### 4.5 增加备份、恢复和迁移机制

知识库长期积累后，备份和迁移是刚性需求。

建议支持：

- 数据库自动备份；
- 手动导出备份包；
- 全量数据导出；
- 原始文件导出；
- 配置导出；
- 数据模型迁移脚本；
- 导入恢复。

建议备份包结构：

```text
backup_YYYY-MM-DD.zip
├── database/
│   ├── knowledge.db
│   └── migrations/
├── files/
│   ├── original/
│   └── preview/
├── exports/
├── config/
│   ├── schema.json
│   ├── import_rules.json
│   └── display_config.json
└── manifest.json
```

---

### 4.6 明确检索能力边界

建议分阶段建设检索能力：

| 检索能力 | 建议阶段 |
|---|---|
| 关键词检索 | V1 必须支持 |
| 标签筛选 | V1 必须支持 |
| 分类筛选 | V1 必须支持 |
| 字段组合查询 | V1/V2 支持 |
| 全文检索 | V1/V2 支持 |
| 关系查询 | V2 支持 |
| 图谱查询 | V2/V3 支持 |
| 语义检索 / RAG | V3 可选 |

---

## 5. 推荐总体架构

建议采用以下架构：

```text
GitHub 工程仓库
        ↓
本地桌面程序 / 本地 Web 程序
        ↓
前端展示层：HTML + React/Vue
        ↓
本地服务层：Node.js / Python / Rust
        ↓
数据层：SQLite + 文件仓库 + 可选 DuckDB
        ↓
ETL 层：文件解析、字段映射、变更检测、数据入库
```

---

## 6. 推荐技术路线

### 6.1 方案 A：轻量本地知识库，优先推荐

```text
前端：React / Vue
桌面壳：Tauri
后端：Rust 或 Node sidecar
数据库：SQLite
全文检索：SQLite FTS5
文件存储：本地 files 目录
ETL：Python 脚本 / Node.js 脚本
图表展示：ECharts / Mermaid / Draw.io viewer
导出：CSV / Excel / JSON / Markdown / HTML / DOCX
```

适用原因：

- 本地运行；
- 可打包；
- GitHub 维护方便；
- 数据量中等时性能足够；
- SQLite 单文件数据库方便备份；
- 前端页面扩展容易；
- 后续可接入 AI/RAG。

---

### 6.2 方案 B：数据分析增强型

```text
前端：React
桌面壳：Tauri
数据库：SQLite + DuckDB
全文检索：SQLite FTS5 / Tantivy
分析查询：DuckDB
数据文件：Parquet / JSON / CSV
ETL：Python
```

适合 Excel、CSV、JSON、Parquet 等结构化文件较多，且需要复杂统计、分析和多维导出的场景。

---

### 6.3 方案 C：未来知识图谱增强型

```text
SQLite / DuckDB
+
图数据库：Neo4j / KuzuDB / SurrealDB
+
语义检索：本地向量库
+
LLM 问答
```

该方案不建议一开始建设，可作为 V3 以后增强方向。

---

## 7. 建议数据模型

### 7.1 knowledge_item：知识条目表

| 字段 | 说明 |
|---|---|
| id | 知识 ID |
| title | 标题 |
| type | 知识类型 |
| summary | 摘要 |
| content | 正文内容 |
| status | 草稿 / 已发布 / 废弃 |
| source_id | 来源文件 ID |
| source_location | 文件内位置，如 sheet/page/slide/section |
| created_at | 创建时间 |
| updated_at | 更新时间 |
| version | 版本号 |

---

### 7.2 knowledge_source：知识来源表

| 字段 | 说明 |
|---|---|
| id | 来源 ID |
| file_name | 文件名 |
| file_path | 文件路径 |
| file_type | xlsx/docx/pptx/drawio/pdf/md |
| file_hash | 文件哈希 |
| imported_at | 导入时间 |
| parser | 使用的解析器 |
| source_status | 正常 / 已删除 / 已更新 |

---

### 7.3 knowledge_relation：知识关系表

| 字段 | 说明 |
|---|---|
| id | 关系 ID |
| source_item_id | 起点知识 |
| target_item_id | 终点知识 |
| relation_type | 支撑 / 引用 / 实现 / 影响 / 属于 / 关联 |
| confidence | 关系置信度 |
| created_by | 人工 / ETL / AI |
| note | 说明 |

---

### 7.4 tag：标签表

| 字段 | 说明 |
|---|---|
| id | 标签 ID |
| name | 标签名称 |
| category | 标签类别 |

---

### 7.5 item_tag：知识-标签关系表

| 字段 | 说明 |
|---|---|
| item_id | 知识 ID |
| tag_id | 标签 ID |

---

### 7.6 import_job：导入任务表

| 字段 | 说明 |
|---|---|
| id | 导入任务 ID |
| source_path | 导入路径 |
| job_type | 单文件 / 批量 / 第三方 ETL |
| status | 待处理 / 成功 / 失败 / 待审核 |
| result_summary | 导入结果 |
| created_at | 创建时间 |

---

### 7.7 change_log：变更记录表

| 字段 | 说明 |
|---|---|
| id | 变更 ID |
| item_id | 知识条目 ID |
| change_type | 新增 / 修改 / 删除 / 合并 |
| before_value | 修改前 |
| after_value | 修改后 |
| changed_by | 修改来源 |
| changed_at | 修改时间 |

---

## 8. 功能模块规划

### 8.1 文件导入模块

支持：

- 单文件上传；
- 文件夹批量导入；
- Excel 批量导入；
- PPT 批量解析；
- DOCX 章节解析；
- Draw.io XML 解析；
- CSV / JSON 导入；
- 导入预览；
- 字段映射；
- 导入日志；
- 错误提示。

核心问题不是“能不能上传”，而是：

```text
这个文件属于什么知识域？
这个文件中有哪些知识对象？
这些对象之间是否存在关系？
是否已有重复数据？
是否需要覆盖？
```

---

### 8.2 数据建模模块

建议配置化管理：

- 知识类型配置；
- 字段配置；
- 标签体系配置；
- 分类体系配置；
- 关系类型配置；
- 展示模板配置；
- 导出模板配置。

这样后续新增“安全能力项”“教材插图类型”“成熟度评分项”“架构组件”等对象时，不需要大量修改代码。

---

### 8.3 知识浏览模块

建议包括：

- 知识首页；
- 分类导航；
- 标签导航；
- 文件来源导航；
- 最近更新；
- 高频知识；
- 待审核知识；
- 废弃知识；
- 我的收藏。

---

### 8.4 查询检索模块

基础查询：

- 标题搜索；
- 正文搜索；
- 标签搜索；
- 类型筛选；
- 来源筛选；
- 更新时间筛选。

高级查询：

- 多字段组合查询；
- 关系查询；
- 来源文件查询；
- 按项目、主题、方法论查询；
- 按知识类型查询。

关联查询示例：

```text
查询“零信任访问控制”
→ 返回相关方法论
→ 返回相关架构图
→ 返回相关 PPT 页面
→ 返回相关 Excel 能力项
→ 返回相关控制措施
→ 返回相关项目案例
```

---

### 8.5 页面展示模块

不同知识类型应使用不同页面模板：

| 知识类型 | 页面形式 |
|---|---|
| 表格类知识 | 数据表格 + 筛选 + 导出 |
| 架构类知识 | 图视图 + 节点属性 + 关系 |
| 文档类知识 | 章节树 + 正文 + 引用来源 |
| PPT 类知识 | 幻灯片缩略图 + 页面摘要 |
| 指标类知识 | 指标卡片 + 趋势图 |
| 方法论类知识 | 层级结构 + 关联对象 |
| 案例类知识 | 案例卡片 + 标签 + 引用关系 |

---

### 8.6 编辑维护模块

建议支持：

- 新增知识；
- 编辑知识；
- 删除知识；
- 废弃知识；
- 恢复知识；
- 标签维护；
- 分类维护；
- 关系维护；
- 批量修改；
- 批量删除；
- 批量标签；
- 版本回滚。

---

### 8.7 导出模块

建议支持：

| 导出类型 | 说明 |
|---|---|
| 当前查询结果导出 | 导出筛选后的数据 |
| 单个知识条目导出 | Markdown / HTML / DOCX |
| 某类知识导出 | 如全部能力项、全部流程项 |
| 某主题导出 | 如零信任专题 |
| 来源文件导出 | 按原始文件导出 |
| 关系图导出 | JSON / CSV / GraphML |
| 全量数据导出 | SQLite / JSON / CSV / ZIP |
| 备份包导出 | 数据库 + 文件 + 配置 |

---

## 9. GitHub 工程结构建议

建议仓库结构：

```text
work-knowledge-base/
├── README.md
├── AGENTS.md
├── docs/
│   ├── project-plan.md
│   ├── architecture.md
│   ├── data-model.md
│   ├── import-rules.md
│   └── user-guide.md
├── apps/
│   └── desktop/
│       ├── src/
│       ├── src-tauri/
│       └── package.json
├── packages/
│   ├── core/
│   ├── ui/
│   ├── db/
│   ├── parsers/
│   ├── exporters/
│   └── schemas/
├── etl/
│   ├── excel/
│   ├── docx/
│   ├── pptx/
│   ├── drawio/
│   └── common/
├── data/
│   ├── raw/
│   ├── processed/
│   ├── database/
│   ├── previews/
│   └── exports/
├── config/
│   ├── knowledge-types.json
│   ├── relation-types.json
│   ├── import-mapping.json
│   └── display-views.json
├── tests/
├── scripts/
└── migrations/
```

目录说明：

| 目录 | 用途 |
|---|---|
| docs | 项目文档 |
| apps/desktop | 本地桌面程序 |
| packages/core | 核心业务逻辑 |
| packages/db | 数据库访问 |
| packages/parsers | 文件解析 |
| packages/exporters | 导出能力 |
| etl | 各类文件 ETL 脚本 |
| data/raw | 原始文件 |
| data/processed | 解析后中间文件 |
| data/database | SQLite / DuckDB 数据库 |
| data/previews | 文件预览图、缩略图 |
| config | 可配置规则 |
| migrations | 数据库迁移脚本 |

---

## 10. 项目实施路线

### 阶段 0：需求澄清与原型定义

目标：将知识库愿望转化为明确的软件需求。

工作内容：

1. 梳理知识类型；
2. 梳理现有文件类型和样例；
3. 定义知识颗粒度；
4. 定义最小可用数据模型；
5. 确定 V1 范围；
6. 确定导入、查询、展示、导出边界。

输出物：

- 《需求规格说明书》；
- 《知识类型清单》；
- 《数据模型初版》；
- 《文件样例清单》；
- 《V1 功能范围说明》。

---

### 阶段 1：MVP 原型

目标：先做一个能跑通闭环的最小系统。

建议范围：

- Excel 导入；
- Markdown / DOCX 导入；
- 基础知识条目表；
- 标签体系；
- 分类体系；
- 关键词搜索；
- 列表页；
- 详情页；
- CSV / JSON 导出；
- SQLite 本地存储。

暂不建议一开始做：

- 完整知识图谱；
- AI 问答；
- 多人权限；
- 复杂回写原文件；
- 全格式深度解析；
- 高级可视化。

---

### 阶段 2：多格式导入与预览增强

目标：扩展到核心文件类型。

工作内容：

1. Excel 多 sheet 解析；
2. DOCX 章节和表格解析；
3. PPT 页面解析；
4. Draw.io XML 解析；
5. 文件预览；
6. 文件来源追踪；
7. 导入日志；
8. 重复数据检测；
9. 文件 hash 检测。

---

### 阶段 3：关联查询与关系管理

目标：从资料库升级为知识关系库。

工作内容：

1. 知识关系表；
2. 手动建立关系；
3. 批量导入关系；
4. 关系查询；
5. 关联推荐；
6. 关系图展示；
7. 按主题聚合知识。

---

### 阶段 4：更新机制与变更审查

目标：解决长期维护问题。

工作内容：

1. 批量更新；
2. 文件重导入；
3. 变更 diff；
4. 冲突检测；
5. 变更审查；
6. 版本记录；
7. 回滚机制；
8. 审核入库。

---

### 阶段 5：高级导出与知识产品化

目标：让知识库不仅能查，还能产出材料。

工作内容：

1. 多维导出；
2. 全量备份；
3. 专题包导出；
4. HTML 页面导出；
5. Markdown 文档导出；
6. Excel 矩阵导出；
7. PPT 素材包导出；
8. 架构图关系导出。

---

### 阶段 6：AI / RAG 增强，可选

目标：在结构化知识库基础上增加智能问答。

工作内容：

1. 文档切片；
2. 向量索引；
3. 本地语义检索；
4. RAG 问答；
5. 引用来源返回；
6. 自动摘要；
7. 自动标签；
8. 自动关系推荐。

注意：该阶段应放在结构化、关系化、检索化之后，不建议一开始就做。

---

## 11. V1 最小可行版本建议

### 11.1 V1 必须支持

1. GitHub 工程管理；
2. 本地运行；
3. SQLite 数据库；
4. Excel 导入；
5. DOCX / Markdown 导入；
6. 原始文件入库管理；
7. 知识条目管理；
8. 标签与分类；
9. 基础全文搜索；
10. 知识详情页；
11. 来源文件追踪；
12. CSV / JSON / Excel 导出；
13. 全量备份包导出。

### 11.2 V1 可以暂缓

1. Draw.io 深度解析；
2. PPT 逐页结构化解析；
3. 知识图谱；
4. AI 问答；
5. 复杂权限；
6. 原文件回写；
7. 多人协同；
8. 云端同步。

---

## 12. 主要风险与控制建议

### 12.1 风险一：一开始就支持所有格式

多格式导入难点不在于读取文件，而在于不同格式如何映射到统一知识模型。

建议导入顺序：

```text
Excel → Markdown/DOCX → PPT → Draw.io → PDF → 其他格式
```

---

### 12.2 风险二：系统退化为文件预览器

如果只是能打开 Excel、PPT、DOCX，则价值有限。

真正价值在于：

```text
文件中的知识对象被抽取出来；
知识对象之间能建立关系；
关系能查询、展示、导出、复用。
```

---

### 12.3 风险三：没有版本管理

必须记录：

- 文件版本；
- 知识条目版本；
- 字段变更；
- 来源变更；
- 关系变更；
- 导入任务。

---

### 12.4 风险四：原文件和数据库脱节

必须避免：

- 文件改了，数据库不知道；
- 数据库改了，文件不知道；
- 不知道哪条数据来自哪个文件；
- 不知道哪次导入造成了变化。

建议强制使用：

```text
source_file_id
source_location
source_hash
import_job_id
version
change_log
```

---

### 12.5 风险五：过早做 AI

AI 可以作为增强层，但不能替代知识库的数据工程。

推荐建设顺序：

```text
先结构化
再关系化
再检索化
最后智能化
```

---

## 13. 推荐推进顺序

建议按以下步骤推进：

1. 确定知识对象模型；
2. 确定文件入库规范；
3. 搭建 GitHub 工程骨架；
4. 实现 SQLite 数据库和基础页面；
5. 实现 Excel / DOCX / Markdown 导入；
6. 实现查询、标签、详情、导出；
7. 加入 PPT / Draw.io 解析；
8. 加入关系管理和图谱展示；
9. 加入更新审查和版本管理；
10. 加入 AI / RAG 增强。

---

## 14. 建议优先编写的项目文档

在正式开发前，建议先编写三份核心文档：

```text
1. knowledge-schema.md      知识模型设计
2. import-rules.md          文件导入与映射规则
3. project-roadmap.md       项目实施路线图
```

这三份文件确定后，再交给 Codex 或开发者实现，会比直接写代码稳定很多。

---

## 15. 最终建议

1. 先定义知识模型，不要先写页面；
2. 先跑通 Excel / DOCX / Markdown，不要一开始追求所有格式；
3. 先做结构化存储和来源追踪，再做图谱和 AI；
4. 必须设计更新、冲突、版本、备份机制；
5. 本地交付优先考虑 Tauri + React/Vue + SQLite；
6. 后续复杂分析可引入 DuckDB；
7. Draw.io 和 PPT 应作为可解析的知识来源，而不只是附件预览；
8. GitHub 中不仅要管理代码，也要管理数据模型、导入规则、展示配置和迁移脚本。

